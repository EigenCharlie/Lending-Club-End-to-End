"""Guardrails for the Paper 4 living-lab evidence pack."""

from __future__ import annotations

import json
import os
import shutil
import subprocess
from pathlib import Path

import numpy as np
import pandas as pd
import pytest

PAPER4_ROOT = Path("reports/paper_material/paper4")
TABLE_DIR = PAPER4_ROOT / "tables"
STATUS_DIR = PAPER4_ROOT / "status"
BOOK_DIR = Path("book")

EXPECTED_PAPER1_CHAMPION_LABEL = "bound_aware_276k_economic_champion"
EXPECTED_PAPER1_CHAMPION_RETURN = 170464.5429284627
CURATED_PAPER4_PAGES = {
    "index.qmd",
    "19a-proposal-and-scope.qmd",
    "19b-current-assets-and-gaps.qmd",
    "19c-integrated-architecture.qmd",
    "19f-sequential-decision-framework.qmd",
    "19h-mvp-evidence-pack.qmd",
    "19i-regret-auditability-frontier.qmd",
    "19n-online-mdcp-fairness.qmd",
    "19t-multi-period-solver.qmd",
    "19ca-v38-final-synthesis.qmd",
}


def _read_csv(name: str) -> pd.DataFrame:
    return pd.read_csv(TABLE_DIR / name)


def _read_json(name: str) -> dict:
    return json.loads((STATUS_DIR / name).read_text(encoding="utf-8"))


def _registered_paper4_pages() -> list[str]:
    pages: list[str] = []
    for raw_line in (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8").splitlines():
        stripped = raw_line.strip()
        if not stripped.startswith("- chapters/19-paper-mega-extension/"):
            continue
        pages.append(Path(stripped.removeprefix("- ")).name)
    return pages


def test_paper4_source_manifest_exists() -> None:
    manifest_path = TABLE_DIR / "paper4_table0_source_manifest.csv"
    assert manifest_path.exists()

    manifest = pd.read_csv(manifest_path)
    expected_columns = {
        "artifact",
        "source_paper",
        "role",
        "status",
        "run_tag",
        "caveat",
        "path_exists",
    }
    assert expected_columns.issubset(manifest.columns)
    assert len(manifest) >= 8
    assert manifest["path_exists"].astype(bool).all()


def test_paper4_sdam_schema_exists() -> None:
    schema = _read_json("paper4_sequential_decision_schema.json")
    assert schema["schema_version"]

    elements = set(schema["elements"])
    assert {"R_t", "I_t", "B_t", "x_t", "W_t_plus_1", "S_M", "C_t", "X_pi"}.issubset(elements)

    post_state = _read_json("paper4_post_decision_state_schema.json")
    fields = {entry["name"] for entry in post_state["fields"]}
    assert {"funded_exposure", "capital_used", "budget_remaining", "stage_mix_proxy"}.issubset(
        fields
    )


def test_paper4_policy_class_registry_schema() -> None:
    registry = _read_csv("paper4_policy_class_registry.csv")
    expected_columns = {
        "policy_id",
        "source",
        "policy_class",
        "objective_type",
        "evaluation_mode",
        "decision_scope",
        "status",
        "is_paper1_champion",
        "source_artifact",
        "notes",
    }
    assert expected_columns.issubset(registry.columns)
    assert registry["policy_id"].is_unique
    assert "paper1_economic_champion" in set(registry["policy_id"])
    assert {"CFA", "PFA", "DLA", "VFA/CFA"}.intersection(set(registry["policy_class"]))
    assert {"implemented_frozen", "planned", "blocked_by_identification"}.issubset(
        set(registry["status"])
    )


def test_paper4_artifacts_do_not_override_paper1_champion() -> None:
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    paper4_status = _read_json("paper4_mvp_status.json")
    assert paper4_status["paper1_champion_protected"] is True
    assert paper4_status["paper4_final_promotion_created"] is False

    paper1_promotion = json.loads(
        Path("models/final_project_promotion.json").read_text(encoding="utf-8")
    )
    champion = paper1_promotion["final_champion"]
    assert champion["label"] == EXPECTED_PAPER1_CHAMPION_LABEL
    assert champion["realized_total_return"] == pytest.approx(EXPECTED_PAPER1_CHAMPION_RETURN)


def test_paper4_mvp_tables_have_policy_id() -> None:
    for name in (
        "paper4_table1_policy_ecl_scenario.csv",
        "paper4_table2_net_return_after_ecl.csv",
        "paper4_table3_tail_risk_oce_cvar_by_policy.csv",
        "paper4_table4_satisficing_screen.csv",
        "paper4_table10_policy_pairwise_differences.csv",
    ):
        table = _read_csv(name)
        assert "policy_id" in table.columns, name
        assert not table.empty, name
        assert table["policy_id"].notna().all(), name

    universe = _read_csv("paper4_policy_universe.csv")
    assert len(universe) == 45
    assert universe["policy_id"].is_unique


def test_paper4_evidence_parquets_exist() -> None:
    expectations = {
        "paper4_loan_level_policy_evidence.parquet": {
            "loan_id",
            "period",
            "policy_id",
            "ecl_proxy_lgd45",
        },
        "paper4_policy_level_evidence.parquet": {"policy_id", "realized_total_return", "gamma_cp"},
        "paper4_monthly_replay.parquet": {"month", "period", "policy_id", "coverage_90"},
        "online_conformal_coverage_regret.parquet": {"month", "coverage_regret_90_cum"},
    }

    for name, columns in expectations.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_extended_policy_loan_matrix_exists() -> None:
    matrix = pd.read_parquet(TABLE_DIR / "paper4_policy_loan_matrix.parquet")
    evidence = pd.read_parquet(TABLE_DIR / "paper4_policy_loan_level_evidence.parquet")
    status = _read_json("paper4_extended_lanes_status.json")

    assert not matrix.empty
    assert not evidence.empty
    assert {"policy_id", "loan_id", "funded_flag", "reconstruction_method"}.issubset(matrix.columns)
    assert evidence["policy_id"].nunique() == 45
    assert "policy-implied greedy proxies" in status["reconstruction_caveat"]
    assert evidence.loc[
        evidence["policy_id"].eq("paper1_economic_champion"), "is_champion_exact"
    ].all()


def test_paper4_ifrs9_full_and_monthly_replay_exist() -> None:
    ifrs9 = _read_csv("paper4_table12_ifrs9_policy_full_eval.csv")
    monthly = pd.read_parquet(TABLE_DIR / "paper4_monthly_policy_replay.parquet")

    assert {"policy_id", "scenario", "ecl", "provision", "net_return_after_ecl_full"}.issubset(
        ifrs9.columns
    )
    assert set(ifrs9["scenario"]) == {"baseline", "adverse", "severe"}
    assert ifrs9["policy_id"].nunique() == 45
    assert monthly["policy_id"].nunique() == 45
    assert {"month", "policy_id", "coverage_alpha01", "net_return_after_ecl"}.issubset(
        monthly.columns
    )


def test_paper4_diagnostic_selector_does_not_promote() -> None:
    selector = _read_csv("paper4_table14_ifrs9_tail_satisficing_selector.csv")
    config = _read_json("paper4_diagnostic_selector_config.json")

    assert not selector.empty
    assert "diagnostic_selector_rank" in selector.columns
    assert selector["diagnostic_selector_rank"].min() == 1
    assert config["mode"] == "diagnostic_no_promotion"
    assert config["promotion_json_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_p1_p2_lanes_exist() -> None:
    expected_csvs = {
        "paper4_policy_pairwise_bootstrap_ci.csv": {"policy_id", "p05_net_return_diff"},
        "paper4_robust_satisficing_policy_eval.csv": {
            "policy_id",
            "robust_satisficing_decision",
        },
        "paper4_mdcp_worst_source_coverage.csv": {
            "policy_id",
            "source_family",
            "worst_source_coverage_alpha01",
        },
        "paper4_fairness_constraint_screen.csv": {"policy_id", "fairness_proxy_pass"},
        "paper4_dla_toy_horizon_summary.csv": {"policy_id", "horizon_months"},
        "paper4_cate_policy_value_toy.csv": {"policy_id", "toy_loss_reduction_value"},
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    expected_parquets = {
        "paper4_online_conformal_aci_replay.parquet": {"month", "recommended_alpha_next"},
        "paper4_online_conformal_grade_replay.parquet": {
            "grade",
            "recommended_alpha_next",
        },
        "paper4_dla_toy_state_replay.parquet": {"policy_id", "budget_start", "budget_end"},
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v2_priority_artifacts_exist() -> None:
    expected_csvs = {
        "paper4_exact_limited_topk_policy_eval.csv": {
            "policy_id",
            "solver_status",
            "n_funded_exact_limited",
        },
        "paper4_exact_vs_proxy_comparison.csv": {
            "policy_id",
            "jaccard_exact_limited_vs_proxy",
            "status",
        },
        "paper4_ifrs9_v2_policy_summary.csv": {
            "policy_id",
            "scenario",
            "sicr_rule",
            "net_return_after_ecl_v2",
        },
        "paper4_selector_v2_results.csv": {
            "policy_id",
            "selector_v2_rank",
            "selector_v2_decision",
        },
        "paper4_online_conformal_coverage_regret_v2.csv": {
            "policy_id",
            "month",
            "coverage_regret_90_cum",
        },
        "paper4_regret_auditability_pareto_v2.csv": {
            "policy_id",
            "mean_regret_net_ecl",
            "auditability_score_v2",
        },
        "paper4_oce_cvar_constraint_grid.csv": {
            "cvar_cap",
            "oce_penalty",
            "selected_policy_id",
        },
        "paper4_multi_period_policy_path.csv": {
            "month",
            "chosen_policy_id",
            "budget_end",
        },
        "paper4_mdcp_worst_source_frontier.csv": {
            "policy_id",
            "source_id",
            "worst_source_coverage_online_90",
        },
        "paper4_cate_exact_topk_value.csv": {
            "policy_id",
            "cate_loss_reduction_value",
        },
        "paper4_claim_artifact_test_matrix.csv": {
            "priority",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    expected_parquets = {
        "paper4_v2_policy_loan_evidence.parquet": {"loan_id", "policy_id", "exact_scope"},
        "paper4_ifrs9_lifetime_ecl_grid.parquet": {
            "loan_id",
            "policy_id",
            "ifrs9_stage_v2",
            "ecl_v2",
        },
        "paper4_online_conformal_intervals.parquet": {
            "loan_id",
            "covered_online_90",
            "online_source",
        },
        "paper4_regret_auditability_replay_v2.parquet": {
            "policy_id",
            "decision_regret_net_ecl",
            "auditability_score_v2",
        },
        "paper4_multi_period_solver_results.parquet": {
            "chosen_policy_id",
            "budget_start",
            "budget_end",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v2_does_not_promote_and_declares_exact_scope() -> None:
    status = _read_json("paper4_v2_priorities_status.json")
    exact = _read_json("paper4_exact_replay_topk_status.json")

    assert status["phase"] == "v2_priorities_1_to_10"
    assert status["mode"] == "diagnostic_no_promotion"
    assert status["paper1_champion_protected"] is True
    assert status["paper4_final_promotion_created"] is False
    assert status["priorities_completed"] == 10
    assert exact["promotion_json_created"] is False
    assert "not a full 276k-loan exact rerun" in exact["caveat"]
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    comparison = _read_csv("paper4_exact_vs_proxy_comparison.csv")
    assert "paper1_economic_champion" in set(comparison["policy_id"])
    champion = comparison.loc[comparison["policy_id"].eq("paper1_economic_champion")]
    assert champion["jaccard_exact_limited_vs_proxy"].iloc[0] == pytest.approx(1.0)
    assert (comparison["jaccard_exact_limited_vs_proxy"].between(0, 1)).all()


def test_paper4_v2_quarto_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_claim_artifact_test_matrix.csv")

    expected_pages = {
        "19p-exact-replay-topk.qmd",
        "19q-ifrs9-v2-lifetime-ecl.qmd",
        "19r-online-conformal-real.qmd",
        "19s-regret-auditability-replay.qmd",
        "19t-multi-period-solver.qmd",
        "19u-artifact-catalog-and-claims.qmd",
        "19v-tail-mdcp-causal-v2.qmd",
    }
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page

    assert expected_pages.issubset(set(claims["quarto_page"]))


def test_paper4_next_wave_artifacts_exist() -> None:
    expected_csvs = {
        "paper4_full_universe_topk_policy_eval.csv": {
            "policy_id",
            "solver_status",
            "full_n_funded",
        },
        "paper4_full_universe_vs_exact_limited_comparison.csv": {
            "policy_id",
            "jaccard_full_vs_exact_limited",
        },
        "paper4_full_universe_ifrs9_tail_eval.csv": {
            "policy_id",
            "scenario",
            "net_return_after_ecl_next_wave",
        },
        "paper4_online_conformal_method_search.csv": {
            "online_method",
            "coverage_online_mean",
            "coverage_online_min_month_policy",
        },
        "paper4_selector_gate_sensitivity_summary.csv": {
            "mdcp_threshold",
            "fairness_gap_threshold",
            "max_gate_pass",
        },
        "paper4_tail_penalty_lp_search.csv": {
            "tail_lp_policy_id",
            "tail_penalty",
            "solver_status",
        },
        "paper4_mdcp_source_family_search.csv": {
            "policy_id",
            "source_id",
            "worst_source_coverage",
        },
        "paper4_fairness_proxy_governance_grid.csv": {
            "policy_id",
            "source_id",
            "fairness_proxy_pass_25",
        },
        "paper4_causal_treatment_identification_grid.csv": {
            "treatment_id",
            "identification_gate",
            "promotion_allowed",
        },
        "paper4_multi_period_sample_path_search.csv": {
            "strategy",
            "scenario",
            "policy_path",
        },
        "paper4_nonpromoted_lane_dashboard.csv": {
            "lane",
            "current_status",
            "promotion_condition",
        },
        "paper4_next_wave_claim_artifact_matrix.csv": {
            "priority",
            "artifact",
            "quarto_page",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    expected_parquets = {
        "paper4_full_universe_topk_allocations.parquet": {
            "loan_id",
            "policy_id",
            "allocation_fraction",
        },
        "paper4_full_universe_ifrs9_tail_grid.parquet": {
            "loan_id",
            "policy_id",
            "scenario",
            "ecl_next_wave",
        },
        "paper4_online_conformal_method_policy_month.parquet": {
            "policy_id",
            "month",
            "online_method",
        },
        "paper4_tail_penalty_lp_allocations.parquet": {
            "loan_id",
            "tail_lp_policy_id",
            "allocation_fraction",
        },
        "paper4_best_online_policy_month_replay.parquet": {
            "policy_id",
            "month",
            "coverage_online",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_next_wave_no_promotion_and_full_exact_scope() -> None:
    status = _read_json("paper4_next_wave_status.json")
    exact = _read_json("paper4_full_universe_exact_topk_status.json")

    assert status["phase"] == "next_wave_top10_plus_nonpromoted_lanes"
    assert status["mode"] == "searches_and_gates_no_promotion"
    assert status["paper1_champion_protected"] is True
    assert status["paper4_final_promotion_created"] is False
    assert status["priorities_completed"] == 10
    assert status["nonpromoted_lanes_dashboard_rows"] >= 10
    assert exact["candidate_pool_n"] == 276869
    assert exact["promotion_json_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    full_eval = _read_csv("paper4_full_universe_topk_policy_eval.csv")
    challengers = full_eval.loc[
        full_eval["policy_id"].ne("paper1_economic_champion"), "solver_status"
    ].astype(str)
    assert not challengers.empty
    assert challengers.str.contains("optimal", case=False).all()

    comparison = _read_csv("paper4_full_universe_vs_exact_limited_comparison.csv")
    assert comparison["jaccard_full_vs_exact_limited"].between(0, 1).all()
    assert comparison["jaccard_full_vs_exact_limited"].min() >= 0.98

    causal = _read_csv("paper4_causal_treatment_identification_grid.csv")
    assert not causal["promotion_allowed"].astype(bool).any()


def test_paper4_next_wave_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_next_wave_claim_artifact_matrix.csv")

    expected_pages = {
        "19w-next-wave-exact-topk.qmd",
        "19x-next-wave-method-searches.qmd",
        "19y-nonpromoted-lanes-and-causal-gates.qmd",
        "19z-next-wave-promotion-dashboard.qmd",
    }
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page

    assert expected_pages.issubset(set(claims["quarto_page"]))


def test_paper4_v3_deepening_artifacts_exist() -> None:
    expected_csvs = {
        "paper4_v3_full_universe_all_policy_eval.csv": {
            "policy_id",
            "solver_status",
            "full_realized_return",
            "return_delta_vs_champion",
        },
        "paper4_v3_ifrs9_cashflow_policy_summary.csv": {
            "policy_id",
            "scenario",
            "cashflow_ecl_v3",
            "net_return_after_cashflow_ecl_v3",
        },
        "paper4_v3_online_conformal_method_summary.csv": {
            "online_method_v3",
            "coverage_policy_month_mean",
            "coverage_policy_month_min",
            "coverage_source_month_min",
        },
        "paper4_v3_formal_cvar_lp_results.csv": {
            "cvar_policy_id",
            "formal_cvar_loss",
            "solver_status",
            "candidate_pool_n",
        },
        "paper4_v3_selector_results.csv": {
            "policy_id",
            "v3_decision",
            "v3_gate_online",
            "v3_gate_mdcp",
        },
        "paper4_v3_causal_high_rate_dossier.csv": {
            "treatment_id",
            "max_smd_ipw_att",
            "promotion_allowed",
        },
        "paper4_v3_fairness_attribute_audit.csv": {
            "attribute",
            "available_in_lending_club_artifacts",
            "usable_for_fair_lending_claim",
            "decision",
        },
        "paper4_v3_multiperiod_loan_level_summary.csv": {
            "strategy",
            "policy_id",
            "net_cash_result",
        },
        "paper4_v3_claim_artifact_matrix.csv": {
            "priority",
            "claim_status",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    expected_parquets = {
        "paper4_v3_full_universe_all_policy_allocations.parquet": {
            "loan_id",
            "policy_id",
            "allocation_fraction",
            "exact_scope",
        },
        "paper4_v3_ifrs9_cashflow_loan_month_grid.parquet": {
            "loan_id",
            "policy_id",
            "scenario",
            "ead_start",
            "ecl_contribution_v3",
        },
        "paper4_v3_online_conformal_intervals.parquet": {
            "loan_id",
            "online_method_v3",
            "covered_online_v3",
            "interval_width_online_v3",
        },
        "paper4_v3_multiperiod_loan_level_state.parquet": {
            "strategy",
            "month",
            "cash_budget_end",
            "outstanding_exposure_end",
        },
        "paper4_v3_multiperiod_loan_level_decisions.parquet": {
            "strategy",
            "decision_month",
            "loan_id",
            "funded_exposure",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v3_no_promotion_and_scope() -> None:
    status = _read_json("paper4_v3_deepening_status.json")
    exact = _read_json("paper4_v3_full_universe_all_policy_status.json")

    assert status["phase"] == "v3_deepening_eight_open_priorities"
    assert status["mode"] == "research_deepening_no_promotion"
    assert status["paper1_champion_protected"] is True
    assert status["paper4_final_promotion_created"] is False
    assert status["priorities_completed"] == 8
    assert status["full_exact_policy_count"] == 45
    assert status["full_exact_target_policy_count"] == 45
    assert exact["candidate_pool_n"] == 276869
    assert exact["completed_policy_count"] == 45
    assert exact["promotion_json_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    full_eval = _read_csv("paper4_v3_full_universe_all_policy_eval.csv")
    assert full_eval["policy_id"].nunique() == 45
    assert "paper1_economic_champion" in set(full_eval["policy_id"])
    challengers = full_eval.loc[
        full_eval["policy_id"].ne("paper1_economic_champion"), "solver_status"
    ].astype(str)
    assert challengers.str.contains("optimal", case=False).all()

    online = _read_csv("paper4_v3_online_conformal_method_summary.csv")
    assert online["coverage_policy_month_min"].min() < 0.80
    assert not online["promotion_candidate"].astype(bool).any()

    selector = _read_csv("paper4_v3_selector_results.csv")
    assert set(selector["v3_decision"]) == {"park", "protected_paper1_champion"}
    champion = selector.loc[selector["policy_id"].eq("paper1_economic_champion")]
    assert champion["v3_decision"].iloc[0] == "protected_paper1_champion"
    assert not selector["v3_gate_online"].astype(bool).any()

    causal = _read_csv("paper4_v3_causal_high_rate_dossier.csv")
    assert not causal["promotion_allowed"].astype(bool).any()


def test_paper4_v3_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v3_claim_artifact_matrix.csv")

    expected_pages = {
        "19aa-v3-full-exact-and-ifrs9-realistic.qmd",
        "19ab-v3-online-cvar-governance.qmd",
        "19ac-v3-causal-fairness-multiperiod.qmd",
    }
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page

    assert expected_pages.issubset(set(claims["quarto_page"]))


def test_paper4_v4_open_priority_artifacts_exist() -> None:
    expected_csvs = {
        "paper4_challenger_local_search.csv": {
            "policy_id",
            "risk_tolerance",
            "gamma",
            "uncertainty_aversion",
            "realized_return_proxy_lgd45",
        },
        "paper4_challenger_local_bootstrap.csv": {
            "policy_id",
            "paired_monthly_diff",
            "prob_diff_positive",
        },
        "paper4_online_conformal_v4_method_summary.csv": {
            "online_method_v4",
            "coverage_policy_month_min",
            "coverage_source_month_min",
            "gate_pass",
        },
        "paper4_ifrs9_v4_contractual_policy_summary.csv": {
            "policy_id",
            "scenario",
            "contractual_ecl_v4",
            "net_return_after_contractual_ecl_v4",
        },
        "paper4_cvar_oce_v4_constraint_frontier.csv": {
            "cvar_policy_id",
            "cvar_cap",
            "return_floor",
            "solver_status",
        },
        "paper4_selector_v4_results.csv": {
            "policy_id",
            "paper4_v4_decision",
            "selector_v4_score",
            "gate_online_global_method",
        },
        "paper4_sdam_v4_dynamic_solver_summary.csv": {
            "strategy",
            "net_cash_result",
            "total_deployed",
        },
        "paper4_causal_high_rate_v4_dossier.csv": {
            "treatment_id",
            "cate_policy_value_allowed",
            "decision",
        },
        "paper4_fairness_v4_proxy_strategy.csv": {"item", "decision"},
        "paper4_mdcp_v4_source_coverage.csv": {
            "policy_id",
            "source_id",
            "worst_source_coverage_v4",
        },
        "paper4_regret_auditability_v4_frontier.csv": {
            "policy_id",
            "regret_gross_v4",
            "auditability_score_v4",
        },
        "paper4_common_sample_path_pairwise_ci.csv": {
            "policy_id",
            "scenario",
            "mean_diff_vs_paper1",
            "prob_beats_paper1",
        },
        "paper4_v4_claim_artifact_matrix.csv": {
            "priority",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    local = _read_csv("paper4_challenger_local_search.csv")
    assert local["policy_id"].nunique() == 100

    ci = _read_csv("paper4_common_sample_path_pairwise_ci.csv")
    assert ci["policy_id"].nunique() >= 100
    assert ci["n_paths"].min() >= 300

    expected_parquets = {
        "paper4_challenger_local_allocations.parquet": {"loan_id", "policy_id"},
        "paper4_online_conformal_v4_policy_month.parquet": {
            "policy_id",
            "online_method_v4",
            "coverage_online_v4",
        },
        "paper4_ifrs9_v4_contractual_loan_level.parquet": {
            "loan_id",
            "policy_id",
            "scenario",
            "contractual_ecl_v4",
        },
        "paper4_common_sample_paths_v4.parquet": {
            "policy_id",
            "scenario",
            "path_id",
            "simulated_return",
        },
        "paper4_sdam_v4_dynamic_solver_state.parquet": {
            "strategy",
            "month",
            "cash_budget_end",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v4_keeps_paper1_frozen_and_uses_working_champion_scope() -> None:
    status = _read_json("paper4_v4_open_priorities_status.json")
    working = _read_json("paper4_v4_working_champion.json")
    selector = _read_csv("paper4_selector_v4_results.csv")
    online = _read_csv("paper4_online_conformal_v4_method_summary.csv")

    assert status["phase"] == "v4_eleven_open_priorities"
    assert status["mode"] == "paper4_working_champions_allowed_paper1_frozen"
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["priorities_completed"] == 11
    assert working["scope"] == "paper4_working_champion_only"
    assert working["does_not_modify_paper1"] is True
    assert working["decision"] in {"review_candidate", "promote_to_paper4_working_champion"}
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    best_online_passes = bool(online.iloc[0]["gate_pass"])
    if not best_online_passes:
        assert not selector["paper4_v4_decision"].eq("promote_to_paper4_working_champion").any()
        assert not selector["gate_online_global_method"].astype(bool).any()

    paper1_promotion = json.loads(
        Path("models/final_project_promotion.json").read_text(encoding="utf-8")
    )
    champion = paper1_promotion["final_champion"]
    assert champion["label"] == EXPECTED_PAPER1_CHAMPION_LABEL
    assert champion["realized_total_return"] == pytest.approx(EXPECTED_PAPER1_CHAMPION_RETURN)

    for figure in status["generated_figures"]:
        path = PAPER4_ROOT / "figures" / figure
        assert path.exists(), figure
        assert path.stat().st_size > 0, figure


def test_paper4_v4_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v4_claim_artifact_matrix.csv")
    manifest = _read_csv("paper4_table0_source_manifest.csv")

    expected_pages = {
        "19ad-v4-challenger-online-mdcp.qmd",
        "19ae-v4-ifrs9-cvar-selector.qmd",
        "19af-v4-sdam-causal-fairness-regret.qmd",
        "19ag-v4-sample-paths-working-champion.qmd",
    }
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page

    assert expected_pages.issubset(set(claims["quarto_page"]))
    assert manifest["artifact"].str.contains("paper4_v4", regex=False).any()
    assert manifest["path_exists"].astype(bool).all()


def test_paper4_v5_blocker_resolution_artifacts_exist() -> None:
    expected_csvs = {
        "paper4_v5_online_source_month_search.csv": {
            "online_method_v5",
            "coverage_source_month_min",
            "avg_width_loan",
            "gate_pass_80",
            "efficiency_gate_width_98",
        },
        "paper4_v5_ifrs9_contractual_policy_summary.csv": {
            "policy_id",
            "scenario",
            "sicr_rule",
            "contractual_ecl_v5",
            "net_return_after_contractual_ecl_v5",
            "ecl_estimation_scope_v5",
        },
        "paper4_v5_sicr_rule_comparison.csv": {
            "scenario",
            "sicr_rule",
            "stage",
            "ecl",
            "ead",
        },
        "paper4_v5_cvar_topk_expanded_frontier.csv": {
            "policy_id",
            "cvar90_expected_loss_v5",
            "return_after_cvar90_v5",
            "pareto_cvar_return_v5",
        },
        "paper4_v5_mdcp_aware_search.csv": {
            "policy_id",
            "mdcp_aware_score_v5",
            "mdcp_aware_decision_v5",
            "v5_source_month_min",
        },
        "paper4_v5_selector_threshold_protocol.csv": {
            "threshold_id",
            "value",
            "committee_rationale",
        },
        "paper4_v5_sdam_realistic_transition_summary.csv": {
            "horizon_months",
            "strategy",
            "cumulative_losses_v5",
            "cumulative_expected_loss_v5",
        },
        "paper4_v5_dla_endogenous_policy_trace.csv": {
            "horizon_months",
            "chosen_action",
            "coverage_signal",
            "cumulative_expected_loss",
        },
        "paper4_v5_causal_cate_policy_value_screen.csv": {
            "share_ci_crosses_zero",
            "policy_value_allowed",
        },
        "paper4_v5_fairness_protocol_decision.csv": {"item", "decision"},
        "paper4_v5_correlated_sample_path_ci.csv": {
            "policy_id",
            "scenario",
            "prob_beats_paper1",
            "cvar90_simulated_loss",
        },
        "paper4_v5_spo_dfl_hybrid_screen.csv": {"hybrid_id", "status"},
        "paper4_v5_temporal_ecl_downstream_value.csv": {
            "lane",
            "coverage_gate",
            "downstream_value",
        },
        "paper4_v5_cqr_decision_aware_screen.csv": {
            "method",
            "promotion_allowed",
            "status",
        },
        "paper4_v5_claim_artifact_matrix.csv": {
            "priority",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    expected_parquets = {
        "paper4_v5_online_source_month_funded_intervals.parquet": {
            "policy_id",
            "loan_id",
            "online_method_v5",
            "interval_width_online_v5",
        },
        "paper4_v5_ifrs9_servicing_panel.parquet": {
            "policy_id",
            "loan_id",
            "month_index",
            "ead_start_proxy",
            "loss_cash_proxy",
        },
        "paper4_v5_correlated_sample_paths.parquet": {
            "policy_id",
            "scenario",
            "path_id",
            "simulated_return",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v5_keeps_paper1_frozen_and_documents_blockers() -> None:
    status = _read_json("paper4_v5_blocker_resolution_status.json")
    online = _read_csv("paper4_v5_online_source_month_search.csv")
    cate = _read_csv("paper4_v5_causal_cate_policy_value_screen.csv")
    fairness = _read_csv("paper4_v5_fairness_protocol_decision.csv")

    assert status["phase"] == "v5_blocker_resolution_wave"
    assert status["mode"] == "paper4_living_lab_no_paper1_changes"
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["priorities_completed"] == 13
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    best = online.iloc[0]
    assert bool(best["gate_pass_80"]) is True
    if float(best["avg_width_loan"]) > 0.98:
        assert status["online_efficiency_blocker"] is True
        assert bool(best["operational_gate_pass"]) is False

    assert not cate["policy_value_allowed"].astype(bool).any()
    assert "no fair-lending legal claim" in set(fairness["decision"])


def test_paper4_v5_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v5_claim_artifact_matrix.csv")

    expected_pages = {
        "19ah-v5-online-ifrs9-sicr.qmd",
        "19ai-v5-tail-mdcp-selector-sdam.qmd",
        "19aj-v5-causal-fairness-paths-hybrids.qmd",
    }
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page

    assert expected_pages.issubset(set(claims["quarto_page"]))


def test_paper4_v6_priority_resolution_artifacts_exist() -> None:
    expected_csvs = {
        "paper4_v6_online_source_month_efficiency_search.csv": {
            "online_method_v6",
            "deployable_without_outcomes",
            "coverage_policy_month_min",
            "coverage_source_month_min",
            "avg_width_loan",
            "promotion_eligible",
        },
        "paper4_v6_sicr_calibration_grid.csv": {
            "scenario",
            "sicr_rule_v6",
            "mean_stage2_share_v6",
            "sicr_recommendation_v6",
        },
        "paper4_v6_contractual_data_audit.csv": {"source_path", "rows", "loan_status"},
        "paper4_v6_servicing_gap_register.csv": {
            "requirement",
            "available_in_current_artifacts",
            "decision",
        },
        "paper4_v6_contractual_ifrs9_readiness.csv": {
            "readiness_score",
            "claim_scope",
            "promotion_allowed",
        },
        "paper4_v6_mdcp_solver_summary.csv": {
            "policy_id",
            "solver_status",
            "solver_lane",
        },
        "paper4_v6_cvar_constraint_summary.csv": {
            "policy_id",
            "solver_status",
            "cvar_cap",
            "return_floor",
            "solver_lane",
        },
        "paper4_v6_dla_loan_level_summary.csv": {
            "policy_id",
            "horizon_months",
            "claim_scope",
        },
        "paper4_v6_correlated_sample_path_ci.csv": {
            "policy_id",
            "scenario",
            "prob_beats_reference_v6",
            "cvar90_simulated_loss_v6",
        },
        "paper4_v6_cate_gate.csv": {
            "policy_value_allowed",
            "blocking_reason",
        },
        "paper4_v6_fairness_protocol.csv": {"item", "decision"},
        "paper4_v6_spo_dfl_hybrid_summary.csv": {
            "policy_id",
            "hybrid_status",
            "auditability_constraint_retained",
        },
        "paper4_v6_claim_artifact_matrix.csv": {
            "priority",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    expected_parquets = {
        "paper4_v6_online_source_month_intervals.parquet": {
            "policy_id",
            "loan_id",
            "online_method_v6",
            "interval_width_online_v6",
        },
        "paper4_v6_solver_allocations.parquet": {
            "policy_id",
            "loan_id",
            "funded_exposure",
        },
        "paper4_v6_dla_loan_level_decisions.parquet": {
            "policy_id",
            "decision_month",
            "loan_id",
            "funded_exposure",
        },
        "paper4_v6_correlated_sample_paths.parquet": {
            "policy_id",
            "scenario",
            "path_id",
            "simulated_return_v6",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v6_no_promotion_and_documents_oracle_boundary() -> None:
    status = _read_json("paper4_v6_priority_resolution_status.json")
    online = _read_csv("paper4_v6_online_source_month_efficiency_search.csv")
    readiness = _read_csv("paper4_v6_contractual_ifrs9_readiness.csv")
    cate = _read_csv("paper4_v6_cate_gate.csv")
    fairness = _read_csv("paper4_v6_fairness_protocol.csv")

    assert status["phase"] == "v6_priority_resolution_wave"
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["online_promotion_eligible"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    passing = online[online["gate_pass_80"].astype(bool)]
    if not passing.empty:
        assert not passing["deployable_without_outcomes"].astype(bool).any()
    assert not readiness["promotion_allowed"].astype(bool).any()
    assert not cate["policy_value_allowed"].astype(bool).any()
    assert "no fair-lending legal claim" in set(fairness["decision"])


def test_paper4_v6_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v6_claim_artifact_matrix.csv")

    expected_pages = {
        "19ak-v6-online-ifrs9-efficient.qmd",
        "19al-v6-solvers-dla-sample-paths.qmd",
        "19am-v6-causal-fairness-hybrids.qmd",
    }
    pending_page = "19an-v6-pending-backlog.qmd"
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page
    assert (BOOK_DIR / "chapters/19-paper-mega-extension" / pending_page).exists()
    assert pending_page not in set(_registered_paper4_pages())

    assert expected_pages.issubset(set(claims["quarto_page"]))


def test_paper4_v7_resolution_artifacts_exist() -> None:
    expected_csvs = {
        "paper4_v7_online_deployable_weak_cell_search.csv": {
            "online_method_v7",
            "method_family",
            "coverage_source_month_defended_min",
            "avg_width_loan",
            "gate_pass_80_defended",
            "promotion_eligible",
        },
        "paper4_v7_online_min_support_pooling.csv": {
            "online_method_v7",
            "gate_scope",
            "min_effective_sample_size",
            "pooled_small_cells",
        },
        "paper4_v7_mdcp_soft_penalty_solver_summary.csv": {
            "policy_id",
            "solver_status",
            "mdcp_gate_proxy_v7",
            "auditability_score_v7",
        },
        "paper4_v7_cvar_adaptive_frontier.csv": {
            "policy_id",
            "solver_status",
            "cvar_cap",
            "return_floor",
            "frontier_feasible_v7",
        },
        "paper4_v7_sicr_mrm_shortlist.csv": {
            "sicr_rule_v6",
            "mean_stage2_share_v6",
            "mrm_decision_v7",
        },
        "paper4_v7_ifrs9_contractual_build_plan.csv": {
            "ifrs9_contractual_component",
            "current_status_v7",
            "next_step_v7",
        },
        "paper4_v7_dla_capital_state_summary.csv": {
            "policy_id",
            "horizon_months",
            "cumulative_realized_loss",
            "cumulative_capital_used",
            "claim_scope",
        },
        "paper4_v7_sample_path_calibration_grid.csv": {
            "scenario",
            "macro_ar1_rho",
            "lgd_cycle_beta",
            "claim_scope_v7",
        },
        "paper4_v7_common_sample_path_ci.csv": {
            "policy_id",
            "scenario",
            "prob_beats_reference_v6",
            "n_paths",
        },
        "paper4_v7_causal_fairness_blocker_matrix.csv": {
            "lane",
            "current_result_v7",
            "promotion_allowed",
        },
        "paper4_v7_fairness_no_legal_claim_statement.csv": {
            "statement_id",
            "statement",
            "must_appear_in_quarto",
        },
        "paper4_v7_hybrid_solver_candidate_registry.csv": {
            "policy_id",
            "hybrid_family_v7",
            "hybrid_candidate_status_v7",
        },
        "paper4_v7_claim_artifact_matrix.csv": {
            "priority",
            "claim_status",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    expected_parquets = {
        "paper4_v7_online_deployable_intervals.parquet": {
            "policy_id",
            "loan_id",
            "online_method_v7",
            "interval_width_online_v7",
        },
        "paper4_v7_online_deployable_policy_month.parquet": {
            "online_method_v7",
            "policy_id",
            "standalone_gate_cell",
        },
        "paper4_v7_online_deployable_source_month.parquet": {
            "online_method_v7",
            "source_id",
            "coverage_online_v7",
            "standalone_gate_cell",
        },
        "paper4_v7_mdcp_soft_penalty_allocations.parquet": {
            "policy_id",
            "loan_id",
            "funded_exposure",
        },
        "paper4_v7_cvar_adaptive_allocations.parquet": {
            "policy_id",
            "loan_id",
            "funded_exposure",
        },
        "paper4_v7_dla_capital_state_decisions.parquet": {
            "policy_id",
            "decision_month",
            "loan_id",
            "funded_exposure",
            "capital_charge_v7",
        },
        "paper4_v7_common_sample_paths.parquet": {
            "policy_id",
            "scenario",
            "path_id",
            "simulated_return_v6",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v7_no_promotion_and_documents_efficiency_blocker() -> None:
    status = _read_json("paper4_v7_resolution_status.json")
    online = _read_csv("paper4_v7_online_deployable_weak_cell_search.csv")
    causal = _read_csv("paper4_v7_causal_fairness_blocker_matrix.csv")
    fairness = _read_csv("paper4_v7_fairness_no_legal_claim_statement.csv")
    dla = _read_csv("paper4_v7_dla_capital_state_summary.csv")

    assert status["phase"] == "v7_resolution_loop"
    assert status["mode"] == "paper4_living_lab_no_paper1_changes"
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["online_gate80_defended_deployable_exists"] is True
    assert status["online_promotion_eligible"] is False
    assert status["online_efficiency_blocker"] is True
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    best = online.iloc[0]
    assert bool(best["gate_pass_80_defended"]) is True
    assert float(best["coverage_source_month_defended_min"]) >= 0.80
    assert float(best["avg_width_loan"]) > 0.95
    assert not online["promotion_eligible"].astype(bool).any()
    assert not causal["promotion_allowed"].astype(bool).any()
    assert (
        fairness["statement"]
        .str.contains("does not make a fair-lending legal compliance claim")
        .any()
    )
    assert float(dla["cumulative_realized_loss"].iloc[0]) > 0


def test_paper4_v7_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v7_claim_artifact_matrix.csv")

    expected_pages = {
        "19ao-v7-online-mdcp-resolution.qmd",
        "19ap-v7-ifrs9-dla-causal-governance.qmd",
        "19aq-v7-pending-and-results.qmd",
    }
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page

    assert {
        "19ao-v7-online-mdcp-resolution.qmd",
        "19ap-v7-ifrs9-dla-causal-governance.qmd",
    }.issubset(set(claims["quarto_page"]))


def test_paper4_v8_resolution_wave_artifacts_exist() -> None:
    expected_csvs = {
        "paper4_v8_literature_to_method_registry.csv": {
            "source_id",
            "primary_source_url",
            "concept",
            "implemented_artifact",
            "caveat",
        },
        "paper4_v8_online_efficiency_frontier.csv": {
            "online_method_v8",
            "coverage_source_month_defended_min",
            "avg_width_loan",
            "gate_pass_80_defended",
            "promotion_eligible",
        },
        "paper4_v8_online_breakpoint_report.csv": {
            "breakpoint_id",
            "online_method_v8",
            "width_gap_to_0p95",
            "interpretation",
        },
        "paper4_v8_cvar_bisection_frontier.csv": {
            "policy_id",
            "tested_cvar_cap",
            "feasible_v8",
            "bisection_floor",
            "solver_lane_v8",
        },
        "paper4_v8_mdcp_family_cap_solver_summary.csv": {
            "policy_id",
            "solver_status",
            "mdcp_family_gate_v8",
            "auditability_score_v8",
        },
        "paper4_v8_selector_governance_results.csv": {
            "policy_id",
            "lane",
            "selector_decision_v8",
            "primary_blocker_v8",
        },
        "paper4_v8_selector_committee_memo.csv": {
            "threshold_id",
            "threshold_value",
            "committee_rationale",
            "decision_effect",
        },
        "paper4_v8_dla_value_function_summary.csv": {
            "policy_id",
            "final_state_value_proxy_v8",
            "cumulative_realized_loss",
            "total_funded_exposure",
        },
        "paper4_v8_dla_vs_static_path_comparison.csv": {
            "comparison_id",
            "delta_final_state_value",
            "delta_cumulative_realized_loss",
            "interpretation",
        },
        "paper4_v8_causal_dossier.csv": {
            "treatment_id",
            "cate_policy_value_allowed",
            "decision_v8",
            "required_to_unblock",
        },
        "paper4_v8_fairness_permanent_proxy_protocol.csv": {
            "protocol_item",
            "decision_v8",
            "allowed_claim",
            "blocked_claim",
        },
        "paper4_v8_dfl_surrogate_training.csv": {
            "policy_id",
            "decision_loss_proxy_v8",
            "training_status_v8",
        },
        "paper4_v8_blocker_dashboard.csv": {
            "blocker_id",
            "status_v8",
            "current_diagnosis",
            "next_action",
        },
        "paper4_v8_v6_v7_change_report.csv": {
            "metric",
            "v6_value",
            "v7_value",
            "v8_value",
        },
        "paper4_v8_claim_artifact_matrix.csv": {
            "priority",
            "claim_status",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    expected_parquets = {
        "paper4_v8_online_selected_intervals.parquet": {
            "policy_id",
            "loan_id",
            "online_method_v8",
            "interval_width_online_v8",
        },
        "paper4_v8_online_policy_month.parquet": {
            "online_method_v8",
            "policy_id",
            "standalone_gate_cell",
        },
        "paper4_v8_online_source_month.parquet": {
            "online_method_v8",
            "source_id",
            "coverage_online_v8",
            "standalone_gate_cell",
        },
        "paper4_v8_cvar_bisection_allocations.parquet": {
            "policy_id",
            "loan_id",
            "funded_exposure",
        },
        "paper4_v8_mdcp_family_cap_allocations.parquet": {
            "policy_id",
            "loan_id",
            "funded_exposure",
        },
        "paper4_v8_dla_value_function_decisions.parquet": {
            "policy_id",
            "decision_month",
            "loan_id",
            "funded_exposure",
            "action_v8",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v8_no_promotion_and_blockers_are_explicit() -> None:
    status = _read_json("paper4_v8_resolution_status.json")
    online = _read_csv("paper4_v8_online_efficiency_frontier.csv")
    selector = _read_csv("paper4_v8_selector_governance_results.csv")
    fairness = _read_csv("paper4_v8_fairness_permanent_proxy_protocol.csv")
    causal = _read_csv("paper4_v8_causal_dossier.csv")
    dashboard = _read_csv("paper4_v8_blocker_dashboard.csv")

    assert status["phase"] == "v8_resolution_wave"
    assert status["mode"] == "paper4_living_lab_no_paper1_changes"
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["causal_policy_value_allowed"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    assert online["gate_pass_80_defended"].astype(bool).any()
    best = (
        online[online["gate_pass_80_defended"].astype(bool)].sort_values("avg_width_loan").iloc[0]
    )
    assert float(best["coverage_source_month_defended_min"]) >= 0.80
    assert status["selector_promote_count"] == int(
        selector["selector_decision_v8"].eq("promote_to_paper4_working_candidate_only").sum()
    )
    assert (
        fairness["decision_v8"].str.contains("no_fair_lending_legal_claim|proxy", regex=True).any()
    )
    assert not causal["cate_policy_value_allowed"].astype(bool).any()
    assert {"resolved", "near_resolved", "data_blocked", "theory_blocked", "active"}.intersection(
        set(dashboard["status_v8"])
    )


def test_paper4_v8_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v8_claim_artifact_matrix.csv")

    expected_pages = {
        "19ar-v8-method-foundations-and-online.qmd",
        "19as-v8-solvers-selector-dla.qmd",
        "19at-v8-causal-fairness-hybrids-dashboard.qmd",
        "19au-v8-results-and-pending.qmd",
    }
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page

    assert expected_pages.issubset(set(claims["quarto_page"]))


def test_paper4_v9_online_goal_resolution() -> None:
    status = _read_json("paper4_v9_online_goal_status.json")
    frontier = _read_csv("paper4_v9_online_efficiency_frontier.csv")
    breakpoint = _read_csv("paper4_v9_online_breakpoint_report.csv")
    claims = _read_csv("paper4_v9_claim_artifact_matrix.csv")

    assert status["phase"] == "v9_online_goal_resolution"
    assert status["mode"] == "paper4_living_lab_no_paper1_changes"
    assert status["online_goal_achieved"] is True
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    best = frontier[frontier["goal_pass"].astype(bool)].sort_values("avg_width_loan").iloc[0]
    assert float(best["coverage_source_month_defended_min"]) >= 0.80
    assert float(best["coverage_policy_month_defended_min"]) >= 0.90
    assert float(best["avg_width_loan"]) <= 0.95
    assert status["online_best_method_v9"] == best["online_method_v9"]

    assert {"v9_v8_reference", "v9_best_goal_passing_width"}.issubset(
        set(breakpoint["breakpoint_id"])
    )
    assert "19av-v9-online-goal-resolution.qmd" in set(claims["quarto_page"])

    selected = pd.read_parquet(TABLE_DIR / "paper4_v9_online_selected_intervals.parquet")
    policy = pd.read_parquet(TABLE_DIR / "paper4_v9_online_policy_month.parquet")
    source = pd.read_parquet(TABLE_DIR / "paper4_v9_online_source_month.parquet")
    assert {"online_method_v9", "qhat_v9", "interval_width_online_v9"}.issubset(selected.columns)
    assert {"online_method_v9", "coverage_online_v9", "standalone_gate_cell"}.issubset(
        policy.columns
    )
    assert {"online_method_v9", "source_id", "coverage_online_v9"}.issubset(source.columns)


def test_paper4_v9_page_is_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    page = "19av-v9-online-goal-resolution.qmd"
    assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
    assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())


def test_paper4_v10_resolution_wave_artifacts_and_guardrails() -> None:
    status = _read_json("paper4_v10_resolution_status.json")
    assert status["phase"] == "v10_resolution_wave"
    assert status["mode"] == "paper4_living_lab_no_paper1_changes"
    assert status["online_goal_achieved"] is True
    assert status["online_robustness_all_pass"] is True
    assert float(status["online_best_source_month_defended_min"]) >= 0.80
    assert float(status["online_best_policy_month_defended_min"]) >= 0.90
    assert float(status["online_best_width"]) <= 0.95
    assert status["selector_promote_count"] == 0
    assert status["primary_remaining_blocker"] == "ifrs9_contractual_data_blocker"
    assert status["contractual_ifrs9_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    expected_csvs = {
        "paper4_v10_online_robustness_summary.csv": {
            "robustness_item",
            "source_month_defended_min",
            "policy_month_defended_min",
            "avg_width_loan",
            "pass_v10",
        },
        "paper4_v10_online_min_support_sensitivity.csv": {
            "online_method_v9",
            "min_support",
            "gate_source80_policy90_width95",
        },
        "paper4_v10_mdcp_empirical_cap_solver_summary.csv": {
            "policy_id",
            "solver_status",
            "mdcp_family_gate_v8",
            "empirical_coverage_calibrated_v10",
        },
        "paper4_v10_cvar_expanded_frontier.csv": {
            "policy_id",
            "tested_cvar_cap_v10",
            "feasible_v10",
            "non_dominated_v10",
            "solver_lane_v10",
        },
        "paper4_v10_dfl_decision_loss_training.csv": {
            "policy_id",
            "decision_loss_proxy_v10",
            "constraint_pd_pass",
            "constraint_mdcp_proxy_pass",
            "training_status_v10",
        },
        "paper4_v10_dla_rollout_comparison.csv": {
            "policy_id",
            "delta_state_value_vs_static",
            "delta_loss_vs_static",
        },
        "paper4_v10_sample_path_stress_ci.csv": {
            "policy_id",
            "mean_loss",
            "p95_loss",
            "mean_default_count",
        },
        "paper4_v10_ifrs9_proxy_readiness.csv": {
            "readiness_score",
            "contractual_ifrs9_claim_allowed",
            "claim_scope_v10",
        },
        "paper4_v10_causal_dossier.csv": {
            "cate_policy_value_allowed",
            "decision_v10",
            "required_to_unblock_v10",
        },
        "paper4_v10_fairness_proxy_protocol.csv": {
            "legal_fair_lending_claim_allowed",
            "allowed_scope_v10",
        },
        "paper4_v10_selector_rerun_with_v9_online.csv": {
            "policy_id",
            "lane",
            "selector_decision_v10",
            "primary_blocker_v10",
        },
        "paper4_v10_blocker_dashboard.csv": {
            "blocker_id",
            "status_v10",
            "selector_promote_count_v10",
        },
        "paper4_v10_claim_artifact_matrix.csv": {
            "priority",
            "claim_status",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    online = _read_csv("paper4_v10_online_robustness_summary.csv")
    assert online["pass_v10"].astype(bool).all()

    selector = _read_csv("paper4_v10_selector_rerun_with_v9_online.csv")
    assert (
        not selector["selector_decision_v10"].eq("promote_to_paper4_working_candidate_only").any()
    )
    assert (
        selector["primary_blocker_v10"]
        .str.contains("ifrs9_contractual_data_blocker|solver_infeasible", regex=True)
        .all()
    )

    dfl = _read_csv("paper4_v10_dfl_decision_loss_training.csv")
    assert len(dfl) >= 100
    assert dfl["constraint_pd_pass"].astype(bool).any()
    assert dfl["constraint_mdcp_proxy_pass"].astype(bool).any()

    readiness = _read_csv("paper4_v10_ifrs9_proxy_readiness.csv")
    assert not readiness["contractual_ifrs9_claim_allowed"].astype(bool).any()
    assert float(readiness["readiness_score"].iloc[0]) < 0.75

    fairness = _read_csv("paper4_v10_fairness_proxy_protocol.csv")
    assert not fairness["legal_fair_lending_claim_allowed"].astype(bool).any()

    expected_parquets = {
        "paper4_v10_mdcp_empirical_cap_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v10_cvar_expanded_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v10_dfl_decision_loss_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v10_dla_rollout_decisions.parquet": {"policy_id", "path_id", "loan_id"},
        "paper4_v10_dla_rollout_trace.parquet": {"policy_id", "path_id", "state_value_proxy_v10"},
        "paper4_v10_sample_path_stress_paths.parquet": {
            "policy_id",
            "path_id",
            "portfolio_loss_v10",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v10_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v10_claim_artifact_matrix.csv")
    expected_pages = {
        "19aw-v10-online-selector-mdcp.qmd",
        "19ax-v10-solvers-dla-dfl.qmd",
        "19ay-v10-ifrs9-causal-fairness-dashboard.qmd",
    }
    for page in expected_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists(), page
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages()), page
    assert expected_pages.issubset(set(claims["quarto_page"]))


def test_paper4_v11_promising_lanes_artifacts_and_guardrails() -> None:
    status = _read_json("paper4_v11_promising_lanes_status.json")
    assert status["phase"] == "v11_promising_lanes"
    assert status["mode"] == "paper4_living_lab_no_paper1_changes"
    assert status["online_goal_achieved"] is True
    assert status["candidate_universe_source_v11"] == "base_full_universe"
    assert int(status["cvar_universe_n_v11"]) >= 276_000
    assert int(status["cvar_pool_n_v11"]) > 12_000
    assert int(status["cvar_feasible_count_v11"]) > 0
    assert int(status["cvar_non_dominated_count_v11"]) > 0
    assert int(status["spo_dfl_candidate_count_v11"]) >= 4
    assert int(status["sample_path_policy_count_v11"]) > 0
    assert int(status["working_candidate_count_v11"]) > 0
    assert status["ifrs9_contractual_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["final_promotion_allowed"] is False
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    expected_csvs = {
        "paper4_v11_cvar_topk_warm_frontier.csv": {
            "policy_id",
            "feasible_v11",
            "non_dominated_v11",
            "pool_n_v11",
            "universe_n_v11",
            "pool_design_v11",
        },
        "paper4_v11_dla_adp_value_coefficients.csv": {
            "feature",
            "coefficient",
            "model_scope_v11",
        },
        "paper4_v11_dla_adp_comparison.csv": {
            "policy_id",
            "delta_state_value_vs_static",
            "adp_scope_v11",
        },
        "paper4_v11_spo_dfl_training_summary.csv": {
            "training_id",
            "top1000_target_precision",
            "claim_scope_v11",
        },
        "paper4_v11_spo_dfl_candidate_summary.csv": {
            "policy_id",
            "constraint_pd_pass",
            "constraint_mdcp_proxy_pass",
            "training_scope_v11",
        },
        "paper4_v11_sample_path_calibration_table.csv": {
            "period",
            "original_grade",
            "default_multiplier_v11",
            "support_status_v11",
        },
        "paper4_v11_sample_path_calibrated_ci.csv": {
            "policy_id",
            "mean_loss",
            "p95_loss",
            "mean_default_multiplier",
        },
        "paper4_v11_working_candidate_registry.csv": {
            "policy_id",
            "lane_v11",
            "working_candidate_score_v11",
            "registry_decision_v11",
            "final_promotion_allowed",
        },
        "paper4_v11_blocker_dashboard.csv": {
            "blocker_id",
            "status_v11",
            "current_diagnosis",
        },
        "paper4_v11_claim_artifact_matrix.csv": {
            "priority",
            "claim_status",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    cvar = _read_csv("paper4_v11_cvar_topk_warm_frontier.csv")
    assert cvar["feasible_v11"].astype(bool).any()
    assert cvar["pool_design_v11"].str.contains("balanced_topk").any()

    spo = _read_csv("paper4_v11_spo_dfl_candidate_summary.csv")
    assert spo["constraint_pd_pass"].astype(bool).any()
    assert spo["constraint_mdcp_proxy_pass"].astype(bool).all()

    registry = _read_csv("paper4_v11_working_candidate_registry.csv")
    assert not registry["final_promotion_allowed"].astype(bool).any()
    assert registry["registry_decision_v11"].str.contains("candidate_for_v12|keep_as_lane").all()

    expected_parquets = {
        "paper4_v11_cvar_topk_warm_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v11_dla_adp_decisions.parquet": {"policy_id", "path_id", "loan_id"},
        "paper4_v11_dla_adp_trace.parquet": {"policy_id", "path_id", "state_value_proxy_v11"},
        "paper4_v11_spo_dfl_allocations.parquet": {"policy_id", "loan_id", "spo_target_score_v11"},
        "paper4_v11_sample_path_calibrated_paths.parquet": {
            "policy_id",
            "path_id",
            "portfolio_loss_v11",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v11_page_is_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v11_claim_artifact_matrix.csv")
    page = "19az-v11-promising-lanes.qmd"
    assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
    assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())
    assert page in set(claims["quarto_page"])


def test_paper4_v12_resolution_wave_artifacts_and_guardrails() -> None:
    status = _read_json("paper4_v12_status.json")
    champion = _read_json("paper4_v12_working_champion.json")

    assert status["phase"] == "v12_resolution_wave"
    assert status["mode"] == "paper4_working_champion_allowed_no_paper1_changes"
    assert status["online_goal_achieved"] is True
    assert int(status["candidate_universe_n_v12"]) >= 276_000
    assert int(status["cvar_pool_n_v12"]) > 18_000
    assert int(status["cvar_feasible_count_v12"]) > 0
    assert int(status["cvar_non_dominated_count_v12"]) > 0
    assert int(status["mdcp_optimal_count_v12"]) > 0
    assert int(status["spo_candidate_count_v12"]) >= 3
    assert int(status["sample_path_policy_count_v12"]) > 0
    assert int(status["working_candidate_count_v12"]) > 0
    assert status["working_champion_created_v12"] is True
    assert status["working_champion_policy_id_v12"] == champion["policy_id"]
    assert status["ifrs9_contractual_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert champion["scope"] == "paper4_working_champion_only"
    assert champion["paper1_promotion_allowed"] is False
    assert champion["paper4_final_promotion_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    paper1_promotion = json.loads(
        Path("models/final_project_promotion.json").read_text(encoding="utf-8")
    )
    assert paper1_promotion["final_champion"]["label"] == EXPECTED_PAPER1_CHAMPION_LABEL
    assert paper1_promotion["final_champion"]["realized_total_return"] == pytest.approx(
        EXPECTED_PAPER1_CHAMPION_RETURN
    )

    expected_csvs = {
        "paper4_v12_artifact_audit.csv": {"artifact", "kind", "version_guess", "exists"},
        "paper4_v12_method_reference_registry.csv": {
            "method_lane",
            "url",
            "claim_boundary_v12",
        },
        "paper4_v12_cvar_column_generation_frontier.csv": {
            "policy_id",
            "feasible_v12",
            "non_dominated_v12",
            "pool_n_v12",
            "cap_relaxation_v12",
        },
        "paper4_v12_cvar_active_constraints.csv": {
            "policy_id",
            "constraint_name",
            "slack_v12",
            "active_v12",
        },
        "paper4_v12_mdcp_source_cap_rationale.csv": {
            "source_id",
            "mapped_cap",
            "empirical_cap_v12",
        },
        "paper4_v12_mdcp_source_cap_solver_summary.csv": {
            "policy_id",
            "solver_status",
            "auditability_score_v12",
        },
        "paper4_v12_dla_state_schema.csv": {"sdam_element", "definition_v12", "role"},
        "paper4_v12_dla_fitted_value_coefficients.csv": {
            "feature",
            "coefficient",
            "model_scope_v12",
        },
        "paper4_v12_dla_fvi_comparison.csv": {
            "policy_id",
            "delta_state_value_vs_static",
            "adp_scope_v12",
        },
        "paper4_v12_spo_plus_surrogate_training.csv": {
            "training_id",
            "split",
            "mean_decision_regret",
            "claim_scope_v12",
        },
        "paper4_v12_spo_plus_surrogate_candidates.csv": {
            "policy_id",
            "decision_regret_proxy_v12",
            "training_scope_v12",
        },
        "paper4_v12_sample_path_macro_calibrated_ci.csv": {
            "policy_id",
            "mean_loss",
            "p95_loss",
            "mean_default_multiplier",
        },
        "paper4_v12_sample_path_pairwise_champion_ci.csv": {
            "policy_id",
            "reference_policy_id",
            "n_common_paths",
        },
        "paper4_v12_ifrs9_sicr_sensitivity.csv": {
            "scenario",
            "stage2_share_v12",
            "contractual_ifrs9_claim_allowed",
        },
        "paper4_v12_causal_cate_dossier.csv": {
            "dossier_item",
            "policy_value_allowed_v12",
            "blocker_v12",
        },
        "paper4_v12_fairness_proxy_governance.csv": {
            "protocol_item",
            "legal_fair_lending_claim_allowed",
            "allowed_scope_v12",
        },
        "paper4_v12_working_candidate_registry.csv": {
            "policy_id",
            "lane_v12",
            "working_candidate_score_v12",
            "registry_decision_v12",
        },
        "paper4_v12_blocker_dashboard.csv": {
            "blocker_id",
            "status_v12",
            "current_diagnosis",
        },
        "paper4_v12_claim_artifact_matrix.csv": {
            "priority",
            "claim_status",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    cvar = _read_csv("paper4_v12_cvar_column_generation_frontier.csv")
    assert cvar["feasible_v12"].astype(bool).any()
    assert cvar["non_dominated_v12"].astype(bool).any()

    registry = _read_csv("paper4_v12_working_candidate_registry.csv")
    assert registry["registry_rank_v12"].min() == 1
    assert registry.iloc[0]["registry_decision_v12"] == "paper4_working_champion"
    assert registry.iloc[0]["policy_id"] == champion["policy_id"]
    assert not registry["paper1_promotion_allowed"].astype(bool).any()
    assert not registry["paper4_final_promotion_allowed"].astype(bool).any()

    ifrs9 = _read_csv("paper4_v12_ifrs9_sicr_sensitivity.csv")
    assert not ifrs9["contractual_ifrs9_claim_allowed"].astype(bool).any()
    fairness = _read_csv("paper4_v12_fairness_proxy_governance.csv")
    assert not fairness["legal_fair_lending_claim_allowed"].astype(bool).any()

    expected_parquets = {
        "paper4_v12_cvar_column_generation_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v12_mdcp_source_cap_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v12_dla_fvi_decisions.parquet": {"policy_id", "path_id", "loan_id"},
        "paper4_v12_dla_fvi_trace.parquet": {"policy_id", "path_id", "state_value_proxy_v12"},
        "paper4_v12_spo_plus_surrogate_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v12_sample_path_macro_calibrated_paths.parquet": {
            "policy_id",
            "path_id",
            "portfolio_loss_v12",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v12_page_is_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v12_claim_artifact_matrix.csv")
    page = "19ba-v12-resolution-wave.qmd"
    assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
    assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())
    assert page in set(claims["quarto_page"])


def test_paper4_v13_resolution_wave_artifacts_and_guardrails() -> None:
    status = _read_json("paper4_v13_status.json")
    champion = _read_json("paper4_v13_working_champion.json")

    assert status["phase"] == "v13_resolution_wave"
    assert status["mode"] == "paper4_working_champion_allowed_no_paper1_changes"
    assert status["online_goal_achieved"] is True
    assert int(status["candidate_universe_n_v13"]) >= 276_000
    assert int(status["cvar_pool_n_v13"]) > 24_000
    assert int(status["cvar_feasible_count_v13"]) > 0
    assert int(status["cvar_non_dominated_count_v13"]) > 0
    assert int(status["mdcp_optimal_count_v13"]) > 0
    assert int(status["spo_candidate_count_v13"]) >= 3
    assert int(status["sample_path_policy_count_v13"]) > 0
    assert int(status["working_candidate_count_v13"]) > 0
    assert status["working_champion_created_v13"] is True
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["working_champion_policy_id_v13"] == champion["policy_id"]
    assert champion["scope"] == "paper4_working_champion_only"
    assert champion["paper1_promotion_allowed"] is False
    assert champion["paper4_final_promotion_created"] is False
    assert champion["contractual_ifrs9_claim_allowed"] is False
    assert champion["fair_lending_legal_claim_allowed"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    paper1_promotion = json.loads(
        Path("models/final_project_promotion.json").read_text(encoding="utf-8")
    )
    assert paper1_promotion["final_champion"]["label"] == EXPECTED_PAPER1_CHAMPION_LABEL
    assert paper1_promotion["final_champion"]["realized_total_return"] == pytest.approx(
        EXPECTED_PAPER1_CHAMPION_RETURN
    )

    expected_csvs = {
        "paper4_v13_v12_artifact_audit.csv": {
            "artifact",
            "source_version",
            "kind",
            "exists",
        },
        "paper4_v13_method_reference_registry.csv": {
            "method_lane",
            "primary_source",
            "url",
            "claim_boundary_v13",
        },
        "paper4_v13_cvar_stronger_decomposition_frontier.csv": {
            "policy_id",
            "feasible_v13",
            "non_dominated_v13",
            "pool_n_v13",
            "frontier_scope_v13",
            "exact_full_universe_claim_v13",
        },
        "paper4_v13_cvar_active_constraints.csv": {
            "policy_id",
            "constraint_name",
            "slack_v12",
            "active_v12",
        },
        "paper4_v13_mdcp_cap_regime_rationale.csv": {
            "source_id",
            "mapped_cap",
            "empirical_cap_v12",
            "version_v13",
        },
        "paper4_v13_mdcp_cap_regime_solver_summary.csv": {
            "policy_id",
            "solver_status",
            "cap_regime_scope_v13",
        },
        "paper4_v13_dla_fitted_value_coefficients.csv": {
            "feature",
            "coefficient",
            "version_v13",
        },
        "paper4_v13_dla_fvi_comparison.csv": {
            "policy_id",
            "delta_state_value_vs_static",
            "adp_scope_v12",
        },
        "paper4_v13_spo_decision_loss_report.csv": {
            "split",
            "mean_decision_regret",
            "differentiable_layer_implemented_v13",
            "claim_scope_v13",
        },
        "paper4_v13_spo_decision_loss_candidates.csv": {
            "policy_id",
            "decision_regret_proxy_v12",
            "training_scope_v12",
        },
        "paper4_v13_sample_path_macro_calibrated_ci.csv": {
            "policy_id",
            "mean_loss",
            "p95_loss",
            "mean_default_multiplier",
        },
        "paper4_v13_sample_path_pairwise_champion_ci.csv": {
            "policy_id",
            "reference_policy_id",
            "n_common_paths",
        },
        "paper4_v13_ifrs9_readiness.csv": {
            "readiness_item",
            "claim_scope_v12",
            "contractual_ifrs9_claim_allowed",
        },
        "paper4_v13_ifrs9_sicr_sensitivity.csv": {
            "scenario",
            "stage2_share_v12",
            "contractual_ifrs9_claim_allowed",
        },
        "paper4_v13_ifrs9_data_blocker_register.csv": {
            "required_item",
            "available_in_current_artifacts",
            "v13_decision",
        },
        "paper4_v13_causal_cate_dossier.csv": {
            "dossier_item",
            "policy_value_allowed_v12",
            "blocker_v12",
        },
        "paper4_v13_fairness_proxy_governance.csv": {
            "protocol_item",
            "legal_fair_lending_claim_allowed",
            "allowed_scope_v12",
        },
        "paper4_v13_working_candidate_registry.csv": {
            "policy_id",
            "lane_v13",
            "working_candidate_score_v13",
            "registry_decision_v13",
        },
        "paper4_v13_champion_stress_test.csv": {
            "policy_id",
            "reference_champion_v13",
            "stress_decision_v13",
        },
        "paper4_v13_blocker_dashboard.csv": {
            "blocker_id",
            "status_v13",
            "current_diagnosis",
        },
        "paper4_v13_claim_artifact_matrix.csv": {
            "priority",
            "claim_status",
            "artifact",
            "quarto_page",
            "caveat",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    cvar = _read_csv("paper4_v13_cvar_stronger_decomposition_frontier.csv")
    assert cvar["feasible_v13"].astype(bool).any()
    assert cvar["non_dominated_v13"].astype(bool).any()
    assert not cvar["exact_full_universe_claim_v13"].astype(bool).any()

    mdcp = _read_csv("paper4_v13_mdcp_cap_regime_solver_summary.csv")
    assert mdcp["solver_status"].astype(str).str.contains("optimal", case=False).any()

    registry = _read_csv("paper4_v13_working_candidate_registry.csv")
    assert registry["registry_rank_v13"].min() == 1
    assert registry.iloc[0]["registry_decision_v13"] == "paper4_working_champion"
    assert registry.iloc[0]["policy_id"] == champion["policy_id"]
    assert not registry["paper1_promotion_allowed"].astype(bool).any()
    assert not registry["paper4_final_promotion_allowed"].astype(bool).any()

    stress = _read_csv("paper4_v13_champion_stress_test.csv")
    assert champion["policy_id"] in set(stress["policy_id"])
    assert set(stress["reference_champion_v13"]) == {champion["policy_id"]}

    ifrs9 = _read_csv("paper4_v13_ifrs9_sicr_sensitivity.csv")
    assert not ifrs9["contractual_ifrs9_claim_allowed"].astype(bool).any()
    causal = _read_csv("paper4_v13_causal_cate_dossier.csv")
    assert not causal["policy_value_allowed_v12"].astype(bool).any()
    fairness = _read_csv("paper4_v13_fairness_proxy_governance.csv")
    assert not fairness["legal_fair_lending_claim_allowed"].astype(bool).any()

    expected_parquets = {
        "paper4_v13_cvar_stronger_decomposition_allocations.parquet": {
            "policy_id",
            "loan_id",
        },
        "paper4_v13_mdcp_cap_regime_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v13_dla_fvi_decisions.parquet": {"policy_id", "path_id", "loan_id"},
        "paper4_v13_dla_fvi_trace.parquet": {
            "policy_id",
            "path_id",
            "state_value_proxy_v12",
        },
        "paper4_v13_dla_representative_allocations.parquet": {
            "policy_id",
            "loan_id",
            "allocation_scope_v13",
        },
        "paper4_v13_spo_decision_loss_allocations.parquet": {"policy_id", "loan_id"},
        "paper4_v13_sample_path_macro_calibrated_paths.parquet": {
            "policy_id",
            "path_id",
            "portfolio_loss_v13",
        },
    }
    for name, columns in expected_parquets.items():
        table = pd.read_parquet(TABLE_DIR / name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v13_page_is_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v13_claim_artifact_matrix.csv")
    page = "19bb-v13-resolution-wave.qmd"
    assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
    assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())
    assert page in set(claims["quarto_page"])


def test_paper4_v14_powell_framing_artifacts_and_guardrails() -> None:
    status = _read_json("paper4_v14_status.json")

    assert status["phase"] == "v14_powell_framing_audit"
    assert status["mode"] == "powell_framing_governance_no_paper1_changes"
    assert status["powell_source_exists"] is True
    assert status["previous_working_champion_policy_id_v13"] == "v13_fvi_return_recovery"
    assert status["working_champion_policy_id_v14"] == "v13_fvi_return_recovery"
    assert status["champion_replaced_v14"] is False
    assert int(status["framing_lane_count_v14"]) >= 12
    assert int(status["metric_count_v14"]) >= 13
    assert int(status["decision_count_v14"]) >= 11
    assert int(status["uncertainty_class_count_v14"]) == 12
    assert int(status["uncertainty_form_count_v14"]) >= 10
    assert int(status["decision_metric_interaction_rows_v14"]) > 100
    assert int(status["uncertainty_metric_interaction_rows_v14"]) > 100
    assert int(status["decision_uncertainty_interaction_rows_v14"]) > 100
    assert int(status["policy_class_evidence_rows_v14"]) >= 5
    assert int(status["claim_count_v14"]) >= 13
    assert status["required_v14_artifacts_exist"] is True
    assert status["ifrs9_contractual_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    paper1_promotion = json.loads(
        Path("models/final_project_promotion.json").read_text(encoding="utf-8")
    )
    assert paper1_promotion["final_champion"]["label"] == EXPECTED_PAPER1_CHAMPION_LABEL
    assert paper1_promotion["final_champion"]["realized_total_return"] == pytest.approx(
        EXPECTED_PAPER1_CHAMPION_RETURN
    )

    expected_csvs = {
        "paper4_v14_v1_v13_artifact_audit.csv": {
            "artifact",
            "artifact_kind",
            "version_guess",
            "exists",
            "v14_use",
        },
        "paper4_v14_powell_framing_audit.csv": {
            "lane_id",
            "lane_name",
            "powell_stage",
            "metric_ids",
            "controlled_decision",
            "uncertainty_classes",
            "policy_class",
            "evidence_artifact",
            "current_status",
            "allowed_claim",
            "caveat",
        },
        "paper4_v14_metric_pyramid.csv": {
            "metric_id",
            "pyramid_level",
            "metric_family",
            "metric_name",
            "powell_role",
            "deployability_gate",
        },
        "paper4_v14_objective_target_limit_registry.csv": {
            "metric_id",
            "powell_role",
            "target_or_limit",
            "strictness",
            "v14_governance_decision",
        },
        "paper4_v14_decision_inventory.csv": {
            "decision_id",
            "decision_name",
            "decision_type",
            "decision_maker",
            "frequency",
            "lag",
            "affected_state_variables",
            "policy_class",
        },
        "paper4_v14_uncertainty_taxonomy.csv": {
            "uncertainty_id",
            "powell_uncertainty_class",
            "paper4_example",
            "affected_metrics",
            "affected_decisions",
            "stage_readiness",
        },
        "paper4_v14_uncertainty_forms_sample_path_design.csv": {
            "form_id",
            "uncertainty_form",
            "lending_club_manifestation",
            "v14_sample_path_design_spec",
            "overclaim_guardrail",
        },
        "paper4_v14_decision_metric_interaction_matrix.csv": {
            "decision_id",
            "metric_id",
            "impact_code",
            "impact_score",
            "rationale",
        },
        "paper4_v14_uncertainty_metric_interaction_matrix.csv": {
            "uncertainty_id",
            "metric_id",
            "impact_code",
            "impact_score",
            "rationale",
        },
        "paper4_v14_decision_uncertainty_interaction_matrix.csv": {
            "decision_id",
            "uncertainty_id",
            "impact_code",
            "impact_score",
            "rationale",
        },
        "paper4_v14_policy_class_evidence_matrix.csv": {
            "policy_class",
            "paper4_examples",
            "minimum_evidence_artifact",
            "claim_boundary",
        },
        "paper4_v14_base_vs_lookahead_model_registry.csv": {
            "artifact",
            "lane",
            "model_role",
            "powell_element",
            "decision_use",
            "evaluation_use",
            "current_limit",
        },
        "paper4_v14_working_champion_powell_audit.csv": {
            "policy_id",
            "criterion_id",
            "powell_metric_family",
            "metric_id",
            "gate_status_v14",
            "decision_impact",
        },
        "paper4_v14_stage_readiness_dashboard.csv": {
            "lane_id",
            "stage1_framing_readiness",
            "stage2_modeling_readiness",
            "stage3_implementation_readiness",
            "status_v14",
            "blocker_category",
        },
        "paper4_v14_claim_artifact_matrix.csv": {
            "priority",
            "claim",
            "claim_status",
            "artifact",
            "quarto_page",
            "claim_boundary_v14",
            "blocker_if_any",
            "no_claim_contractual_ifrs9",
            "no_claim_cate_policy_value",
            "no_claim_fair_lending_legal",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    framing = _read_csv("paper4_v14_powell_framing_audit.csv")
    assert {"DLA/FVI", "IFRS9/SICR proxy", "fairness proxy governance"}.issubset(
        set(framing["lane_name"])
    )
    policy_classes_text = " ".join(framing["policy_class"].astype(str))
    assert "PFA" in policy_classes_text
    assert "CFA" in policy_classes_text

    metrics = _read_csv("paper4_v14_metric_pyramid.csv")
    assert {"base", "risk", "estimation", "implementation"}.issubset(set(metrics["metric_family"]))
    assert {"objective", "target", "limit"}.issubset(set(metrics["powell_role"]))

    uncertainties = _read_csv("paper4_v14_uncertainty_taxonomy.csv")
    assert len(uncertainties) == 12
    assert uncertainties["uncertainty_id"].is_unique

    decision_metric = _read_csv("paper4_v14_decision_metric_interaction_matrix.csv")
    assert set(decision_metric["impact_code"]).issubset({"H", "M", "L", "N"})
    assert decision_metric["impact_code"].eq("H").any()

    model_registry = _read_csv("paper4_v14_base_vs_lookahead_model_registry.csv")
    assert {"base_evaluation_model", "lookahead_policy_model"}.issubset(
        set(model_registry["model_role"])
    )

    champion_audit = _read_csv("paper4_v14_working_champion_powell_audit.csv")
    assert set(champion_audit["policy_id"]) == {"v13_fvi_return_recovery"}
    assert {"blocked", "pass_working"}.intersection(set(champion_audit["gate_status_v14"]))

    claims = _read_csv("paper4_v14_claim_artifact_matrix.csv")
    assert claims["quarto_page"].eq("19bc-v14-powell-framing-audit.qmd").all()
    assert claims["no_claim_contractual_ifrs9"].astype(bool).any()
    assert claims["no_claim_cate_policy_value"].astype(bool).any()
    assert claims["no_claim_fair_lending_legal"].astype(bool).any()


def test_paper4_v14_page_is_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    claims = _read_csv("paper4_v14_claim_artifact_matrix.csv")
    page = "19bc-v14-powell-framing-audit.qmd"
    assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
    assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())
    assert page in set(claims["quarto_page"])


def test_paper4_v15_dynamic_engine_artifacts_exist() -> None:
    status = _read_json("paper4_v15_status.json")
    assert status["phase"] == "v15_dynamic_stress_engine"
    assert int(status["dynamic_policy_count_v15"]) >= 10
    assert int(status["dynamic_path_count_v15"]) >= 16
    assert int(status["dynamic_trace_rows_v15"]) > 0
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False

    schema = _read_json("paper4_v15_dynamic_state_schema.json")
    state_fields = {field["name"] for field in schema["fields"]}
    assert {
        "cash",
        "outstanding_principal",
        "repayments",
        "defaults",
        "recoveries",
        "ECL",
        "budget_remaining",
        "wealth",
    }.issubset(state_fields)

    trace = pd.read_parquet(TABLE_DIR / "paper4_v15_dynamic_policy_trace.parquet")
    assert not trace.empty
    assert {
        "policy_id",
        "path_id",
        "month",
        "cash",
        "outstanding_principal",
        "funded_exposure",
        "repayments",
        "defaults",
        "recoveries",
        "losses",
        "ECL",
        "wealth",
        "no_temporal_leakage_flag",
    }.issubset(trace.columns)
    assert trace["no_temporal_leakage_flag"].astype(bool).all()

    summary = _read_csv("paper4_v15_dynamic_policy_summary.csv")
    assert {
        "policy_id",
        "dynamic_value_score_v15",
        "paper4_champion_score_v15",
        "dynamic_governance_gate_pass_v15",
        "registry_decision_v15",
    }.issubset(summary.columns)
    assert summary["registry_decision_v15"].eq("paper4_working_champion_candidate").sum() == 1


def test_paper4_v16_cvar_spo_and_champion_decomposition_exist() -> None:
    status = _read_json("paper4_v16_status.json")
    assert status["phase"] == "v16_cvar_spo_champion_decomposition"
    assert int(status["cvar_frontier_rows_v16"]) >= 10
    assert int(status["cvar_infeasibility_rows_v16"]) >= 1
    assert status["spo_differentiable_layer_implemented_v16"] is False
    assert status["paper4_final_promotion_created"] is False

    frontier = _read_csv("paper4_v16_cvar_full_or_colgen_frontier.csv")
    assert {"policy_id", "v16_regime_label", "exact_full_universe_claim_v16"}.issubset(
        frontier.columns
    )
    assert not frontier["exact_full_universe_claim_v16"].astype(bool).any()

    cert = _read_csv("paper4_v16_cvar_strict_infeasibility_certificate.csv")
    assert {"policy_id", "certificate_scope", "academic_interpretation"}.issubset(cert.columns)
    assert cert["certificate_scope"].str.contains("not mathematical Farkas").any()

    blockers = _read_csv("paper4_v16_spo_dependency_blockers.csv")
    assert {"package", "available_v16", "decision_v16", "blocker_detail"}.issubset(blockers.columns)
    assert {"cvxpy", "cvxpylayers", "torch"}.issubset(set(blockers["package"]))
    assert (
        not blockers.loc[
            blockers["package"].isin(["cvxpy", "cvxpylayers", "torch"]), "available_v16"
        ]
        .astype(bool)
        .any()
    )

    report = _read_csv("paper4_v16_spo_training_report.csv")
    assert {"split", "mean_decision_regret_proxy", "claim_scope_v16"}.issubset(report.columns)
    assert report["claim_scope_v16"].str.contains("not_formal_spo_plus").all()

    decomp = _read_csv("paper4_v16_champion_decomposition_summary.csv")
    assert {"champion_policy_id", "challenger_policy_id", "selection_bucket"}.issubset(
        decomp.columns
    )
    assert {"champion_only", "challenger_only"}.issubset(set(decomp["selection_bucket"]))


def test_paper4_v17_ifrs9_cate_fairness_claims_blocked() -> None:
    status = _read_json("paper4_v17_status.json")
    assert status["phase"] == "v17_ifrs9_causal_fairness_gates"
    assert status["ifrs9_contractual_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["paper4_final_promotion_created"] is False

    readiness = _read_csv("paper4_v17_ifrs9_proxy_panel_readiness.csv")
    assert {"requirement", "available_for_contractual_ifrs9", "v17_decision"}.issubset(
        readiness.columns
    )
    assert not readiness["available_for_contractual_ifrs9"].astype(bool).all()

    cate = _read_csv("paper4_v17_cate_gate_report.csv")
    assert {"gate", "status_v17", "policy_value_allowed"}.issubset(cate.columns)
    assert not cate["policy_value_allowed"].astype(bool).any()

    fairness = _read_csv("paper4_v17_fairness_proxy_only_protocol.csv")
    assert {"protocol_item", "status_v17", "allowed_claim"}.issubset(fairness.columns)
    assert fairness["allowed_claim"].str.contains("no fair-lending legal claim").any()


def test_paper4_v18_academic_synthesis_and_working_champion_only() -> None:
    status = _read_json("paper4_v18_status.json")
    champion = _read_json("paper4_v18_working_champion.json")
    assert status["phase"] == "v18_academic_synthesis"
    assert status["working_champion_policy_id_v18"] == champion["policy_id"]
    assert champion["scope"] == "paper4_working_champion_only"
    assert champion["paper1_promotion_allowed"] is False
    assert champion["paper4_final_promotion_created"] is False
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    contributions = _read_csv("paper4_v18_academic_contribution_map.csv")
    assert {"finding", "primary_artifact", "publishability_class"}.issubset(contributions.columns)
    assert len(contributions) >= 8

    blockers = _read_csv("paper4_v18_blocker_dashboard.csv")
    assert {"blocker_id", "status_v18", "current_diagnosis", "next_action"}.issubset(
        blockers.columns
    )
    assert {"data_blocked", "theory_blocked", "prohibited_claim"}.issubset(
        set(blockers["status_v18"])
    )

    claims = _read_csv("paper4_v18_claim_artifact_matrix.csv")
    assert {"claim", "allowed", "artifact", "quarto_page", "boundary"}.issubset(claims.columns)
    assert (
        not claims.loc[claims["claim"].str.contains("contractual IFRS9"), "allowed"]
        .astype(bool)
        .any()
    )
    assert (
        not claims.loc[claims["claim"].str.contains("CATE policy value"), "allowed"]
        .astype(bool)
        .any()
    )
    assert (
        not claims.loc[claims["claim"].str.contains("fair-lending legal"), "allowed"]
        .astype(bool)
        .any()
    )


def test_paper4_v15_v18_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    for page in [
        "19bd-v15-dynamic-stress-engine.qmd",
        "19be-v16-cvar-spo-champion-decomposition.qmd",
        "19bf-v17-ifrs9-causal-fairness-gates.qmd",
        "19bg-v18-academic-synthesis.qmd",
    ]:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())


def test_paper4_v19_dynamic_engine_v2_artifacts_exist() -> None:
    status = _read_json("paper4_v19_status.json")
    assert status["phase"] == "v19_dynamic_engine_v2"
    assert int(status["dynamic_policy_count_v19"]) >= 10
    assert int(status["dynamic_path_count_v19"]) >= 64
    assert int(status["horizon_months_v19"]) >= 36
    assert float(status["no_temporal_leakage_min_rate_v19"]) == pytest.approx(1.0)
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False

    for name in [
        "paper4_v19_dynamic_state_schema.json",
        "paper4_v19_status.json",
    ]:
        assert (STATUS_DIR / name).exists(), name

    trace = pd.read_parquet(TABLE_DIR / "paper4_v19_dynamic_policy_trace.parquet")
    assert {
        "policy_id",
        "path_id",
        "month",
        "cash",
        "outstanding_principal",
        "defaults",
        "prepayments",
        "recoveries",
        "losses",
        "ECL",
        "wealth",
        "no_temporal_leakage_flag",
    }.issubset(trace.columns)
    assert trace["no_temporal_leakage_flag"].astype(bool).all()

    summary = _read_csv("paper4_v19_dynamic_policy_summary.csv")
    assert {"policy_id", "dynamic_value_score_v15", "claim_scope_v19"}.issubset(summary.columns)
    assert "paper1_economic_champion" in set(summary["policy_id"])

    for name, columns in {
        "paper4_v19_horizon_sensitivity.csv": {"policy_id", "horizon_months"},
        "paper4_v19_gate_sensitivity.csv": {"gate_variant_v19", "winning_policy_id"},
        "paper4_v19_policy_pairwise_common_path_ci.csv": {
            "policy_id",
            "reference_policy_id",
            "prob_higher_wealth",
        },
        "paper4_v19_path_family_sensitivity.csv": {
            "policy_id",
            "macro_regime_v15",
            "final_wealth_mean",
        },
    }.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v20_dla_cvar_spo_resolution_artifacts_exist() -> None:
    status = _read_json("paper4_v20_status.json")
    assert status["phase"] == "v20_dla_cvar_spo_resolution"
    assert int(status["dynamic_policy_count_v20"]) >= 17
    assert int(status["endogenous_dla_policy_count_v20"]) >= 3
    assert status["spo_formal_differentiable_claim_allowed"] is False
    assert status["cvar_full_universe_exact_claim_v20"] is False
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False

    decisions = pd.read_parquet(TABLE_DIR / "paper4_v20_endogenous_dla_decisions.parquet")
    assert {"policy_id", "loan_id", "funded_exposure"}.issubset(decisions.columns)
    assert decisions["policy_id"].str.contains("v20_dla").any()

    dla_summary = _read_csv("paper4_v20_endogenous_dla_policy_summary.csv")
    assert {"policy_id", "final_wealth_mean", "cumulative_losses_p95"}.issubset(dla_summary.columns)

    cvar = _read_csv("paper4_v20_cvar_oce_frontier_v2.csv")
    assert {"policy_id", "regime_v20", "full_universe_exact_claim_v20"}.issubset(cvar.columns)
    assert not cvar["full_universe_exact_claim_v20"].astype(bool).any()

    deps = _read_csv("paper4_v20_spo_dependency_blockers.csv")
    assert {"package", "available_v20", "decision_v20"}.issubset(deps.columns)
    assert {"cvxpy", "cvxpylayers", "torch"}.issubset(set(deps["package"]))

    report = _read_csv("paper4_v20_spo_training_report.csv")
    assert {"model_id", "split", "claim_scope_v20"}.issubset(report.columns)
    assert report["claim_scope_v20"].str.contains("not formal differentiable SPO").any()

    memo = _read_csv("paper4_v20_champion_decision_memo.csv")
    assert {
        "candidate_policy_id",
        "current_reference_policy_id",
        "working_champion_change_recommended_v20",
        "decision_scope",
    }.issubset(memo.columns)
    assert memo["decision_scope"].str.contains("Paper 4 working champion only").all()


def test_paper4_v21_ifrs9_cate_fairness_gates_blocked() -> None:
    status = _read_json("paper4_v21_status.json")
    assert status["phase"] == "v21_ifrs9_causal_fairness_gates"
    assert status["ifrs9_contractual_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["paper4_final_promotion_created"] is False

    readiness = _read_csv("paper4_v21_ifrs9_readiness_matrix.csv")
    assert {"requirement", "contractual_ready", "contractual_claim_allowed"}.issubset(
        readiness.columns
    )
    assert not readiness["contractual_claim_allowed"].astype(bool).any()

    proxy = pd.read_parquet(TABLE_DIR / "paper4_v21_ifrs9_proxy_monthly_panel.parquet")
    assert {"policy_id", "scenario", "ecl_proxy_v21", "stage2_composite_v21"}.issubset(
        proxy.columns
    )

    cate = _read_csv("paper4_v21_cate_gate_report.csv")
    assert {"gate", "status_v21", "cate_policy_value_allowed"}.issubset(cate.columns)
    assert not cate["cate_policy_value_allowed"].astype(bool).any()

    flags = _read_csv("paper4_v21_no_legal_claim_flags.csv")
    assert {"claim_or_requirement", "allowed_v21", "status_v21"}.issubset(flags.columns)
    legal = flags.loc[flags["claim_or_requirement"].eq("fair_lending_legal_claim")]
    assert not legal["allowed_v21"].astype(bool).any()


def test_paper4_v22_synthesis_and_working_champion_only() -> None:
    status = _read_json("paper4_v22_status.json")
    champion = _read_json("paper4_v22_working_champion.json")
    assert status["phase"] == "v22_academic_synthesis"
    assert status["working_champion_policy_id_v22"] == champion["policy_id"]
    assert champion["scope"] == "paper4_working_champion_only"
    assert champion["paper1_promotion_allowed"] is False
    assert champion["paper4_final_promotion_created"] is False
    assert champion["fair_lending_legal_claim_allowed"] is False
    assert champion["contractual_ifrs9_claim_allowed"] is False
    assert champion["cate_policy_value_allowed"] is False
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    blockers = _read_csv("paper4_v22_blocker_dashboard.csv")
    assert {"blocker_id", "status_v22", "current_diagnosis", "next_action"}.issubset(
        blockers.columns
    )
    assert {"data_blocked", "theory_blocked", "prohibited_claim"}.issubset(
        set(blockers["status_v22"])
    )

    claims = _read_csv("paper4_v22_claim_artifact_matrix.csv")
    assert {
        "claim",
        "allowed",
        "artifact",
        "quarto_page",
        "artifact_exists",
        "no_claim_contractual_ifrs9",
        "no_claim_cate_policy_value",
        "no_claim_fair_lending_legal",
    }.issubset(claims.columns)
    assert claims["artifact_exists"].astype(bool).all()
    assert (
        not claims.loc[claims["claim"].str.contains("contractual IFRS9"), "allowed"]
        .astype(bool)
        .any()
    )
    assert (
        not claims.loc[claims["claim"].str.contains("CATE policy value"), "allowed"]
        .astype(bool)
        .any()
    )
    assert (
        not claims.loc[claims["claim"].str.contains("fair-lending legal"), "allowed"]
        .astype(bool)
        .any()
    )


def test_paper4_v19_v22_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    for page in [
        "19bh-v19-dynamic-engine-v2.qmd",
        "19bi-v20-dla-cvar-spo-resolution.qmd",
        "19bj-v21-ifrs9-causal-fairness-gates.qmd",
        "19bk-v22-academic-synthesis.qmd",
    ]:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())


def test_paper4_v23_dynamic_scale_and_paths_artifacts_exist() -> None:
    status = _read_json("paper4_v23_status.json")
    assert status["phase"] == "v23_dynamic_scale_and_paths"
    assert int(status["dynamic_path_count_v23"]) >= 128
    assert int(status["dynamic_trace_rows_v23"]) > 100_000
    assert float(status["no_temporal_leakage_min_rate_v23"]) == pytest.approx(1.0)
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False

    trace = pd.read_parquet(TABLE_DIR / "paper4_v23_dynamic_policy_trace.parquet")
    assert {"policy_id", "path_id", "month", "wealth", "no_temporal_leakage_flag"}.issubset(
        trace.columns
    )
    assert trace["no_temporal_leakage_flag"].astype(bool).all()

    for name, columns in {
        "paper4_v23_scale_convergence.csv": {"policy_id", "n_paths", "wealth_ci95_width"},
        "paper4_v23_ranking_stability.csv": {"comparison", "spearman_rank_correlation"},
        "paper4_v23_performance_report.csv": {"run_id", "runtime_seconds", "bottleneck"},
        "paper4_v23_path_calibration_diagnostics.csv": {
            "macro_regime_v15",
            "path_claim_boundary",
        },
    }.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name


def test_paper4_v24_dla_cvar_spo_upgrade_artifacts_exist() -> None:
    status = _read_json("paper4_v24_status.json")
    assert status["phase"] == "v24_dla_cvar_spo_upgrade"
    assert int(status["adp_policy_count_v24"]) >= 4
    assert int(status["adp_trace_rows_v24"]) > 0
    assert status["cvar_exact_full_universe_claim_v24"] is False
    assert status["spo_formal_differentiable_claim_allowed"] is False
    assert status["paper1_artifacts_modified"] is False
    assert status["paper4_final_promotion_created"] is False

    decisions = pd.read_parquet(TABLE_DIR / "paper4_v24_dla_adp_decisions.parquet")
    assert {"policy_id", "loan_id", "funded_exposure", "decision_priority_score_v15"}.issubset(
        decisions.columns
    )
    assert decisions["policy_id"].str.contains("v24_adp").any()

    summary = _read_csv("paper4_v24_dla_adp_dynamic_summary.csv")
    assert {"policy_id", "final_wealth_mean", "cumulative_losses_p95"}.issubset(summary.columns)

    cvar = _read_csv("paper4_v24_cvar_frontier_non_dominated.csv")
    assert {"policy_id", "full_universe_exact_claim_v24", "column_generation_claim_v24"}.issubset(
        cvar.columns
    )
    assert not cvar["full_universe_exact_claim_v24"].astype(bool).any()

    cert = _read_csv("paper4_v24_cvar_infeasibility_certificate_formalized.csv")
    assert {"certificate_type_v24", "mathematical_infeasibility_proof_claim"}.issubset(cert.columns)
    assert not cert["mathematical_infeasibility_proof_claim"].astype(bool).any()

    memo = _read_csv("paper4_v24_committee_profile_memos.csv")
    assert {"committee_profile", "winning_policy_id", "decision_scope"}.issubset(memo.columns)
    assert memo["decision_scope"].str.contains("Paper 4 committee memo only").all()


def test_paper4_v25_ifrs9_causal_fairness_upgrade_claims_blocked() -> None:
    status = _read_json("paper4_v25_status.json")
    assert status["phase"] == "v25_ifrs9_causal_fairness_upgrade"
    assert status["ifrs9_contractual_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["paper4_final_promotion_created"] is False

    panel = pd.read_parquet(TABLE_DIR / "paper4_v25_ifrs9_proxy_cashflow_panel.parquet")
    assert {
        "policy_id",
        "loan_id",
        "month_index",
        "ead_start_proxy_v25",
        "ecl_proxy_v25",
        "claim_scope_v25",
    }.issubset(panel.columns)
    assert panel["claim_scope_v25"].str.contains("not contractual IFRS9").all()

    cate = _read_csv("paper4_v25_cate_gate_report.csv")
    assert {"gate", "status_v25", "cate_policy_value_allowed"}.issubset(cate.columns)
    assert not cate["cate_policy_value_allowed"].astype(bool).any()

    flags = _read_csv("paper4_v25_no_legal_claim_flags.csv")
    assert {"claim_or_requirement", "allowed_v25", "status_v25"}.issubset(flags.columns)
    assert (
        not flags.loc[flags["claim_or_requirement"].eq("fair_lending_legal_claim"), "allowed_v25"]
        .astype(bool)
        .any()
    )


def test_paper4_v26_registry_docs_synthesis_and_working_champion_only() -> None:
    status = _read_json("paper4_v26_status.json")
    champion = _read_json("paper4_v26_working_champion.json")
    assert status["phase"] == "v26_registry_docs_synthesis"
    assert int(status["artifact_registry_rows_v26"]) >= 50
    assert int(status["candidate_registry_rows_v26"]) >= 10
    assert status["all_claim_artifacts_exist_v26"] is True
    assert status["working_champion_policy_id_v26"] == champion["policy_id"]
    assert champion["scope"] == "paper4_working_champion_only"
    assert champion["paper1_promotion_allowed"] is False
    assert champion["paper4_final_promotion_created"] is False
    assert champion["contractual_ifrs9_claim_allowed"] is False
    assert champion["cate_policy_value_allowed"] is False
    assert champion["fair_lending_legal_claim_allowed"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    registry = _read_csv("paper4_v26_artifact_registry.csv")
    assert {"artifact", "version", "path", "sha16", "path_exists"}.issubset(registry.columns)
    assert registry["path_exists"].astype(bool).all()

    candidates = _read_csv("paper4_v26_candidate_registry.csv")
    assert {"policy_id", "decision_v26", "full_candidate_score_v26"}.issubset(candidates.columns)
    assert "v13_spo_regret_audit_guarded" in set(candidates["policy_id"])
    spo_decision = candidates.loc[
        candidates["policy_id"].eq("v13_spo_regret_audit_guarded"), "decision_v26"
    ].iloc[0]
    assert spo_decision == "serious_challenger"

    claims = _read_csv("paper4_v26_claim_artifact_matrix.csv")
    assert claims["artifact_exists"].astype(bool).all()
    assert (
        not claims.loc[claims["claim"].str.contains("contractual IFRS9"), "allowed"]
        .astype(bool)
        .any()
    )
    assert (
        not claims.loc[claims["claim"].str.contains("CATE policy value"), "allowed"]
        .astype(bool)
        .any()
    )
    assert (
        not claims.loc[claims["claim"].str.contains("fair-lending legal"), "allowed"]
        .astype(bool)
        .any()
    )


def test_paper4_v23_v26_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    for page in [
        "19bl-v23-dynamic-scale-paths.qmd",
        "19bm-v24-dla-cvar-spo-upgrade.qmd",
        "19bn-v25-ifrs9-causal-fairness-upgrade.qmd",
        "19bo-v26-registry-and-synthesis.qmd",
    ]:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())


def test_paper4_v27_dynamic_scale_champion_stress_artifacts_exist() -> None:
    status = _read_json("paper4_v27_status.json")
    assert status["phase"] == "v27_dynamic_scale_champion_stress"
    assert int(status["dynamic_path_count_v27"]) >= 256
    assert int(status["dynamic_trace_rows_v27"]) > 200_000
    assert status["no_temporal_leakage_min_rate_v27"] == pytest.approx(1.0)
    assert status["paper4_final_promotion_created"] is False

    trace = pd.read_parquet(TABLE_DIR / "paper4_v27_dynamic_policy_trace.parquet")
    assert {"policy_id", "path_id", "month", "wealth", "no_temporal_leakage_flag"}.issubset(
        trace.columns
    )
    assert trace["no_temporal_leakage_flag"].astype(bool).all()

    stress = _read_csv("paper4_v27_champion_vs_cvar_stress_memo.csv")
    assert {
        "reference_policy_id",
        "challenger_policy_id",
        "prob_challenger_beats_reference",
        "decision_v27",
        "decision_scope",
    }.issubset(stress.columns)
    assert stress["decision_scope"].str.contains("Paper 4 working champion only").all()


def test_paper4_v28_cvar_dla_spo_upgrade_claim_boundaries() -> None:
    status = _read_json("paper4_v28_status.json")
    assert status["phase"] == "v28_cvar_dla_spo_upgrade"
    assert int(status["adp_policy_count_v28"]) >= 4
    assert status["cvar_exact_full_universe_claim_v28"] is False
    assert status["spo_formal_differentiable_claim_allowed"] is False
    assert status["paper4_final_promotion_created"] is False

    cvar = _read_csv("paper4_v28_cvar_frontier_non_dominated.csv")
    assert {"policy_id", "exact_full_universe_claim_v28", "restricted_master_claim_v28"}.issubset(
        cvar.columns
    )
    assert not cvar["exact_full_universe_claim_v28"].astype(bool).any()

    deps = _read_csv("paper4_v28_spo_dependency_blockers.csv")
    assert {"package", "formal_differentiable_spo_claim_allowed"}.issubset(deps.columns)
    assert not deps["formal_differentiable_spo_claim_allowed"].astype(bool).any()

    cert = _read_csv("paper4_v28_cvar_infeasibility_certificate_formalized.csv")
    assert {"certificate_type_v28", "mathematical_infeasibility_proof_claim"}.issubset(cert.columns)
    assert not cert["mathematical_infeasibility_proof_claim"].astype(bool).any()


def test_paper4_v29_ifrs9_causal_fairness_source_claims_blocked() -> None:
    status = _read_json("paper4_v29_status.json")
    assert status["phase"] == "v29_ifrs9_causal_fairness_source_upgrade"
    assert status["ifrs9_contractual_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False
    assert status["paper4_final_promotion_created"] is False

    panel = pd.read_parquet(TABLE_DIR / "paper4_v29_ifrs9_proxy_cashflow_panel.parquet")
    assert {
        "policy_id",
        "scenario_v29",
        "ecl_proxy_v29",
        "contractual_ifrs9_claim_allowed",
    }.issubset(panel.columns)
    assert not panel["contractual_ifrs9_claim_allowed"].astype(bool).any()

    cate = _read_csv("paper4_v29_cate_gate_report.csv")
    assert {"gate", "cate_policy_value_allowed", "claim_boundary_v29"}.issubset(cate.columns)
    assert not cate["cate_policy_value_allowed"].astype(bool).any()

    flags = _read_csv("paper4_v29_no_legal_claim_flags.csv")
    assert "allowed_v29" in flags.columns
    assert (
        not flags.loc[flags["claim_or_requirement"].eq("fair_lending_legal_claim"), "allowed_v29"]
        .astype(bool)
        .any()
    )


def test_paper4_v30_registry_docs_synthesis_and_working_champion_only() -> None:
    status = _read_json("paper4_v30_status.json")
    champion = _read_json("paper4_v30_working_champion.json")
    assert status["phase"] == "v30_registry_docs_synthesis"
    assert int(status["artifact_registry_rows_v30"]) >= 100
    assert int(status["candidate_registry_rows_v30"]) >= 10
    assert status["all_claim_artifacts_exist_v30"] is True
    assert status["working_champion_policy_id_v30"] == champion["policy_id"]
    assert champion["scope"] == "paper4_working_champion_only"
    assert champion["paper1_promotion_allowed"] is False
    assert champion["paper4_final_promotion_created"] is False
    assert champion["contractual_ifrs9_claim_allowed"] is False
    assert champion["cate_policy_value_allowed"] is False
    assert champion["fair_lending_legal_claim_allowed"] is False
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()

    candidates = _read_csv("paper4_v30_candidate_registry.csv")
    assert {"policy_id", "decision_v30", "full_candidate_score_v30"}.issubset(candidates.columns)
    assert "v13_spo_regret_audit_guarded" in set(candidates["policy_id"])

    claims = _read_csv("paper4_v30_claim_artifact_matrix.csv")
    assert claims["artifact_exists"].astype(bool).all()
    assert (
        not claims.loc[claims["claim"].str.contains("contractual IFRS9"), "allowed"]
        .astype(bool)
        .any()
    )
    assert (
        not claims.loc[claims["claim"].str.contains("CATE policy value"), "allowed"]
        .astype(bool)
        .any()
    )
    assert (
        not claims.loc[claims["claim"].str.contains("fair-lending legal"), "allowed"]
        .astype(bool)
        .any()
    )


def test_paper4_v27_v30_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    for page in [
        "19bp-v27-dynamic-scale-and-champion-stress.qmd",
        "19bq-v28-cvar-dla-spo-upgrade.qmd",
        "19br-v29-ifrs9-causal-fairness-source.qmd",
        "19bs-v30-registry-and-synthesis.qmd",
    ]:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())


def test_paper4_v31_v38_full_project_wave_artifacts_exist() -> None:
    v31 = _read_json("paper4_v31_status.json")
    assert v31["phase"] == "v31_dynamic_512_stress"
    assert int(v31["dynamic_path_count_v31"]) >= 256
    assert v31["paper4_final_promotion_created"] is False
    trace = pd.read_parquet(TABLE_DIR / "paper4_v31_dynamic_policy_trace.parquet")
    assert {"policy_id", "path_id", "month", "wealth", "no_temporal_leakage_flag"}.issubset(
        trace.columns
    )
    assert trace["no_temporal_leakage_flag"].astype(bool).all()

    v32 = _read_json("paper4_v32_status.json")
    assert v32["phase"] == "v32_spo_environment_and_oracle_regret"
    assert v32["formal_differentiable_spo_claim_allowed"] is False
    deps = _read_csv("paper4_v32_spo_dependency_blockers.csv")
    assert {"package", "formal_differentiable_spo_claim_allowed"}.issubset(deps.columns)
    assert not deps["formal_differentiable_spo_claim_allowed"].astype(bool).any()

    v33 = _read_json("paper4_v33_status.json")
    assert v33["phase"] == "v33_cvar_certificate_full_universe"
    assert v33["exact_full_universe_claim_v33"] is False
    cvar_guard = _read_csv("paper4_v33_cvar_full_universe_feasibility_attempt.csv")
    assert {"exact_full_universe_claim", "claim_boundary"}.issubset(cvar_guard.columns)
    assert not cvar_guard["exact_full_universe_claim"].astype(bool).any()

    v34 = _read_json("paper4_v34_status.json")
    assert v34["phase"] == "v34_dla_redesign_failure_analysis"
    assert v34["bellman_exact_claim_allowed"] is False

    v35 = _read_json("paper4_v35_status.json")
    assert v35["phase"] == "v35_online_macro_validation"
    assert v35["external_forecast_validation_claim_allowed"] is False

    v36 = _read_json("paper4_v36_status.json")
    assert v36["phase"] == "v36_ifrs9_sicr_upgrade"
    assert v36["contractual_ifrs9_claim_allowed"] is False

    v37 = _read_json("paper4_v37_status.json")
    assert v37["phase"] == "v37_cate_fairness_protocol"
    assert v37["causal_policy_value_allowed"] is False
    assert v37["fair_lending_legal_claim"] is False

    global_status = json.loads(
        Path("reports/paper_material/global/status/global_v38_status.json").read_text(
            encoding="utf-8"
        )
    )
    assert global_status["phase"] == "global_v38_project_synthesis"
    assert global_status["paper1_promotion_happened_v38"] is False
    assert global_status["paper4_final_promotion_created"] is False


def test_paper4_v31_v38_pages_are_registered() -> None:
    config = (BOOK_DIR / "_quarto.yml").read_text(encoding="utf-8")
    for page in [
        "19bt-v31-dynamic-512-stress.qmd",
        "19bu-v32-spo-environment-oracle.qmd",
        "19bv-v33-cvar-certificate.qmd",
        "19bw-v34-dla-redesign.qmd",
        "19bx-v35-online-macro-validation.qmd",
        "19by-v36-ifrs9-sicr-upgrade.qmd",
        "19bz-v37-cate-fairness-protocol.qmd",
        "19ca-v38-final-synthesis.qmd",
    ]:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()
        assert page in CURATED_PAPER4_PAGES or page not in set(_registered_paper4_pages())


def test_paper4_curated_quarto_surface_is_compact() -> None:
    registered = _registered_paper4_pages()
    assert len(registered) <= 12
    assert set(registered) == CURATED_PAPER4_PAGES

    historical_pages = {
        "19u-artifact-catalog-and-claims.qmd",
        "19bs-v30-registry-and-synthesis.qmd",
        "19bt-v31-dynamic-512-stress.qmd",
        "19bz-v37-cate-fairness-protocol.qmd",
    }
    assert historical_pages.isdisjoint(set(registered))
    for page in historical_pages:
        assert (BOOK_DIR / "chapters/19-paper-mega-extension" / page).exists()


def test_paper4_living_lab_notebook_exists_and_has_backlog() -> None:
    notebook = PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md"
    assert notebook.exists()
    text = notebook.read_text(encoding="utf-8")

    for heading in [
        "## Current State",
        "## What Enters Quarto",
        "## Consolidated Findings",
        "## Claim Boundaries",
        "## Implementable Backlog",
        "## Template For Future Waves",
    ]:
        assert heading in text

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    assert {"horizon", "lane", "executable_item", "status"}.issubset(backlog.columns)
    assert {"immediate", "short", "medium", "long"}.issubset(set(backlog["horizon"]))


def test_paper4_current_official_claims_are_artifact_backed() -> None:
    findings = _read_csv("paper4_current_official_findings.csv")
    claims = _read_csv("paper4_current_claim_boundaries.csv")
    page_registry = _read_csv("paper4_quarto_page_registry.csv")
    status = _read_json("paper4_quarto_restructure_status.json")

    assert status["official_quarto_page_count"] <= 12
    assert status["paper4_final_promotion_created"] is False
    assert status["contractual_ifrs9_claim_allowed"] is False
    assert status["causal_policy_value_allowed"] is False
    assert status["fair_lending_legal_claim"] is False

    assert {"finding_id", "evidence_artifact", "quarto_page", "status"}.issubset(findings.columns)
    for artifact in findings["evidence_artifact"]:
        assert Path(artifact).exists(), artifact

    assert {"claim", "allowed", "evidence_artifact", "prohibited_claim_flag"}.issubset(
        claims.columns
    )
    for artifact in claims["evidence_artifact"]:
        assert Path(artifact).exists(), artifact

    forbidden = claims.loc[
        claims["claim"].str.contains("Contractual IFRS9|CATE policy value|Fair-lending", case=False)
    ]
    assert not forbidden["allowed"].astype(bool).any()

    rendered = page_registry.loc[page_registry["rendered_in_quarto"].astype(bool), "page"]
    assert set(rendered) == CURATED_PAPER4_PAGES
    assert page_registry["path_exists"].astype(bool).all()
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v39_v40_living_lab_artifacts_exist_and_do_not_expand_quarto() -> None:
    v39 = _read_json("paper4_v39_status.json")
    v40 = _read_json("paper4_v40_status.json")

    assert v39["phase"] == "v39_living_lab_execution_after_quarto_restructure"
    assert v40["phase"] == "v40_spo_dla_claim_boundary_and_publishability"
    assert v39["quarto_compact_guardrail_pass"] is True
    assert int(v39["official_quarto_page_count"]) <= 12
    assert v39["paper4_final_promotion_created"] is False
    assert v40["paper4_final_promotion_created"] is False
    assert v39["contractual_ifrs9_claim_allowed"] is False
    assert v39["causal_policy_value_allowed"] is False
    assert v39["fair_lending_legal_claim"] is False
    assert v40["formal_differentiable_spo_claim_allowed"] is False
    assert v40["bellman_exact_claim_allowed"] is False

    expected_csvs = {
        "paper4_v39_online_source_family_holdout.csv": {
            "validation_item",
            "gate_source80_policy90_width95_v39",
            "live_deployability_claim_allowed",
            "claim_boundary_v39",
        },
        "paper4_v39_dynamic_candidate_stress.csv": {
            "policy_id",
            "paired_wealth_robustness_gate_v39",
            "tail_challenger_gate_v39",
            "decision_v39",
        },
        "paper4_v39_cvar_certificate_delta.csv": {
            "policy_id",
            "exact_full_universe_claim_v39",
            "mathematical_infeasibility_proof_claim_v39",
            "claim_boundary_v39",
        },
        "paper4_v39_source_governance_caps.csv": {
            "policy_id",
            "source_family",
            "recommended_source_cap_v39",
            "fair_lending_legal_claim_allowed",
        },
        "paper4_v39_candidate_registry.csv": {
            "policy_id",
            "full_candidate_score_v39",
            "paper1_promotion_allowed_v39",
            "claim_boundary_v39",
        },
        "paper4_v39_ifrs9_sicr_proxy_update.csv": {
            "policy_id",
            "sicr_rule_v36",
            "contractual_ifrs9_claim_allowed",
            "claim_boundary_v39",
        },
        "paper4_v40_spo_dependency_audit.csv": {
            "package",
            "formal_differentiable_spo_claim_allowed",
            "decision_v40",
        },
        "paper4_v40_spo_oracle_regret_v4.csv": {
            "month",
            "regret_ratio_v40",
            "formal_differentiable_spo_claim_allowed",
            "claim_boundary_v40",
        },
        "paper4_v40_dla_rollout_reaudit.csv": {
            "policy_id",
            "bellman_exact_claim_allowed",
            "underperformance_diagnosis_v40",
            "claim_boundary_v40",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    online = _read_csv("paper4_v39_online_source_family_holdout.csv")
    assert not online["live_deployability_claim_allowed"].astype(bool).any()

    cvar = _read_csv("paper4_v39_cvar_certificate_delta.csv")
    assert not cvar["exact_full_universe_claim_v39"].astype(bool).any()
    assert not cvar["mathematical_infeasibility_proof_claim_v39"].astype(bool).any()

    fairness = _read_csv("paper4_v39_source_governance_caps.csv")
    assert not fairness["fair_lending_legal_claim_allowed"].astype(bool).any()

    registry = _read_csv("paper4_v39_candidate_registry.csv")
    assert not registry["paper1_promotion_allowed_v39"].astype(bool).any()

    for note_name in [
        "paper4_spo_isolated_env_repro.md",
        "paper4_sample_path_claim_boundary.md",
        "paper4_cate_identification_reaudit.md",
        "paper4_fairness_protocol_update.md",
        "paper4_publishability_focus_memo.md",
        "paper4_v39_v40_documentation_cleanup.md",
    ]:
        note_path = PAPER4_ROOT / "notes" / note_name
        assert note_path.exists(), note_name
        assert note_path.stat().st_size > 0, note_name

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v39-v40" in notebook
    assert "No new Quarto page is promoted from v39-v40" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v41_v44_living_lab_artifacts_exist_and_preserve_claim_boundaries() -> None:
    v41 = _read_json("paper4_v41_status.json")
    v42 = _read_json("paper4_v42_status.json")
    v43 = _read_json("paper4_v43_status.json")
    v44 = _read_json("paper4_v44_status.json")

    assert v41["phase"] == "v41_online_cvar_source_dynamic_gate"
    assert v42["phase"] == "v42_spo_dependency_and_oracle_regret_training"
    assert v43["phase"] == "v43_dla_ifrs9_samplepaths_cate_fairness"
    assert v44["phase"] == "v44_registry_publishability_docs"
    assert v41["strict_live_deployability_claim_allowed"] is False
    assert v41["exact_full_universe_cvar_claim_allowed"] is False
    assert v42["formal_differentiable_spo_claim_allowed"] is False
    assert v43["bellman_exact_claim_allowed"] is False
    assert v43["contractual_ifrs9_claim_allowed"] is False
    assert v43["cate_policy_value_allowed"] is False
    assert v43["fair_lending_legal_claim_allowed"] is False
    assert v44["paper1_promotion_allowed_v44"] is False
    assert v44["quarto_compact_guardrail_pass"] is True
    assert int(v44["official_quarto_page_count"]) <= 12

    expected_csvs = {
        "paper4_v41_online_method_grid.csv": {
            "source_family",
            "method_v41",
            "gate_source80_policy90_width95_v41",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v41_online_source_family_solver.csv": {
            "source_family",
            "decision_v41",
            "claim_boundary_v41",
        },
        "paper4_v41_source_governance_solver_caps.csv": {
            "policy_id",
            "source_family",
            "recommended_source_cap_v41",
            "fair_lending_legal_claim_allowed",
        },
        "paper4_v41_source_solver_feasibility.csv": {
            "policy_id",
            "source_family",
            "required_relaxation_v41",
            "solver_constraint_use_v41",
        },
        "paper4_v41_cvar_column_generation_v2.csv": {
            "policy_id",
            "pricing_score_v41",
            "claim_scope_v41",
        },
        "paper4_v41_cvar_strict_infeasibility_v2.csv": {
            "policy_id",
            "exact_full_universe_claim_v41",
            "claim_boundary_v41",
        },
        "paper4_v41_dynamic_rerun_gate.csv": {
            "policy_id",
            "new_candidate_screen_v41",
            "expensive_512_1024_rerun_action_v41",
        },
        "paper4_v42_spo_dependency_isolation.csv": {
            "package",
            "formal_differentiable_spo_claim_allowed",
            "decision_v42",
        },
        "paper4_v42_spo_training_report.csv": {
            "model_v42",
            "mae_candidate_to_oracle_ratio",
            "formal_differentiable_spo_claim_allowed",
        },
        "paper4_v42_spo_temporal_regret_validation.csv": {
            "month",
            "model_v42",
            "predicted_regret_v42",
            "claim_boundary_v42",
        },
        "paper4_v43_dla_adp_rollout_grid.csv": {
            "policy_id",
            "bellman_exact_claim_allowed",
            "claim_boundary_v43",
        },
        "paper4_v43_external_macro_registry.csv": {
            "series_id",
            "source_url",
            "fetch_success",
        },
        "paper4_v43_ifrs9_proxy_policy_summary.csv": {
            "policy_id",
            "total_ecl_proxy",
            "contractual_ifrs9_claim_allowed",
        },
        "paper4_v43_ifrs9_sicr_sensitivity.csv": {
            "policy_id",
            "sicr_rule_v36",
            "contractual_ifrs9_claim_allowed",
        },
        "paper4_v43_cate_treatment_search.csv": {
            "gate",
            "cate_policy_value_allowed",
            "claim_boundary_v43",
        },
        "paper4_v43_fairness_source_protocol.csv": {
            "source_family",
            "fair_lending_legal_claim_allowed",
            "claim_boundary_v43",
        },
        "paper4_v44_candidate_registry.csv": {
            "policy_id",
            "full_candidate_score_v44",
            "paper1_promotion_allowed_v44",
            "claim_boundary_v44",
        },
        "paper4_v44_champion_decomposition.csv": {
            "policy_id",
            "wealth_gap_vs_reference_v44",
            "economic_interpretation_v44",
        },
        "paper4_v44_claim_matrix.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    online = _read_csv("paper4_v41_online_method_grid.csv")
    assert not online["strict_live_deployability_claim_allowed"].astype(bool).any()

    cvar = _read_csv("paper4_v41_cvar_strict_infeasibility_v2.csv")
    assert not cvar["exact_full_universe_claim_v41"].astype(bool).any()

    spo = _read_csv("paper4_v42_spo_training_report.csv")
    assert not spo["formal_differentiable_spo_claim_allowed"].astype(bool).any()

    ifrs9 = _read_csv("paper4_v43_ifrs9_proxy_policy_summary.csv")
    assert not ifrs9["contractual_ifrs9_claim_allowed"].astype(bool).any()

    fairness = _read_csv("paper4_v43_fairness_source_protocol.csv")
    assert not fairness["fair_lending_legal_claim_allowed"].astype(bool).any()

    registry = _read_csv("paper4_v44_candidate_registry.csv")
    assert not registry["paper1_promotion_allowed_v44"].astype(bool).any()

    champion = _read_json("paper4_v44_working_champion.json")
    assert champion["paper4_working_only"] is True
    assert champion["paper1_promotion_allowed"] is False

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v41-v44" in notebook
    assert "Keep v41-v44 in the living notebook" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v45_v48_living_lab_artifacts_exist_and_preserve_claim_boundaries() -> None:
    v45 = _read_json("paper4_v45_status.json")
    v46 = _read_json("paper4_v46_status.json")
    v47 = _read_json("paper4_v47_status.json")
    v48 = _read_json("paper4_v48_status.json")

    assert v45["phase"] == "v45_online_cvar_source_solver"
    assert v46["phase"] == "v46_spo_dla_dynamic"
    assert v47["phase"] == "v47_ifrs9_cate_fairness_paths"
    assert v48["phase"] == "v48_registry_docs_guardrails"
    assert v45["strict_live_deployability_claim_allowed"] is False
    assert v45["exact_full_universe_cvar_claim_allowed"] is False
    assert v45["fair_lending_legal_claim_allowed"] is False
    assert v46["formal_differentiable_spo_claim_allowed"] is False
    assert v46["bellman_exact_claim_allowed"] is False
    assert v47["contractual_ifrs9_claim_allowed"] is False
    assert v47["cate_policy_value_allowed"] is False
    assert v47["fair_lending_legal_claim_allowed"] is False
    assert v48["paper1_promotion_allowed_v48"] is False
    assert v48["quarto_compact_guardrail_pass"] is True
    assert int(v48["official_quarto_page_count"]) <= 12

    expected_csvs = {
        "paper4_v45_online_source_family_direct_holdout.csv": {
            "source_family",
            "source_family_defended_min_v45",
            "policy_month_direct_min_v45",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v45_online_recalibration_grid.csv": {
            "source_family",
            "method_v45",
            "gate_source80_policy90_width95_v45",
            "claim_boundary_v45",
        },
        "paper4_v45_cvar_slack_lp_certificate.csv": {
            "policy_id",
            "required_cvar_slack_v45",
            "exact_lp_certificate_claim_allowed_v45",
            "claim_boundary_v45",
        },
        "paper4_v45_cvar_full_universe_attempt.csv": {
            "attempt_id",
            "full_universe_lp_executed_v45",
            "exact_full_universe_claim_v45",
            "blocker_v45",
        },
        "paper4_v45_mdcp_source_solver_frontier.csv": {
            "policy_id",
            "source_family",
            "source_solver_cap_pass_v45",
            "fair_lending_legal_claim_allowed",
        },
        "paper4_v46_spo_isolated_env_smoke_test.csv": {
            "package",
            "formal_differentiable_spo_claim_allowed",
            "decision_v46",
        },
        "paper4_v46_spo_loan_level_oracle_regret.csv": {
            "model_v46",
            "decision_regret_v46",
            "formal_differentiable_spo_claim_allowed",
            "claim_boundary_v46",
        },
        "paper4_v46_dla_common_path_replay.csv": {
            "policy_id",
            "candidate_family_v46",
            "bellman_exact_claim_allowed",
            "claim_boundary_v46",
        },
        "paper4_v46_focused_dynamic_1024_ci.csv": {
            "candidate_policy_id",
            "prob_higher_wealth_v46",
            "focused_1024_rerun_executed_v46",
            "claim_boundary_v46",
        },
        "paper4_v47_ifrs9_proxy_policy_summary.csv": {
            "policy_id",
            "scenario",
            "total_ecl_proxy",
            "contractual_ifrs9_claim_allowed",
        },
        "paper4_v47_sicr_robust_calibration.csv": {
            "policy_id",
            "sicr_rule_v47",
            "contractual_ifrs9_claim_allowed",
            "claim_boundary_v47",
        },
        "paper4_v47_sample_path_macro_alignment.csv": {
            "path_family_v19",
            "external_forecast_validation_claim_allowed",
            "claim_boundary_v47",
        },
        "paper4_v47_champion_case_studies.csv": {
            "policy_id",
            "selection_relation_v47",
            "economic_case_label_v47",
        },
        "paper4_v47_cate_treatment_outcome_search.csv": {
            "diagnostic_v47",
            "cate_policy_value_allowed",
            "claim_boundary_v47",
        },
        "paper4_v47_fairness_source_protocol.csv": {
            "policy_id",
            "support_gate_v47",
            "fair_lending_legal_claim_allowed",
        },
        "paper4_v48_candidate_registry.csv": {
            "policy_id",
            "full_governance_score_v48",
            "paper1_promotion_allowed_v48",
            "claim_boundary_v48",
        },
        "paper4_v48_claim_matrix.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    online = _read_csv("paper4_v45_online_source_family_direct_holdout.csv")
    assert not online["strict_live_deployability_claim_allowed"].astype(bool).any()

    cvar = _read_csv("paper4_v45_cvar_full_universe_attempt.csv")
    assert not cvar["exact_full_universe_claim_v45"].astype(bool).any()

    spo = _read_csv("paper4_v46_spo_loan_level_oracle_regret.csv")
    assert not spo["formal_differentiable_spo_claim_allowed"].astype(bool).any()

    ifrs9 = _read_csv("paper4_v47_ifrs9_proxy_policy_summary.csv")
    assert not ifrs9["contractual_ifrs9_claim_allowed"].astype(bool).any()

    cate = _read_csv("paper4_v47_cate_treatment_outcome_search.csv")
    assert not cate["cate_policy_value_allowed"].astype(bool).any()

    fairness = _read_csv("paper4_v47_fairness_source_protocol.csv")
    assert not fairness["fair_lending_legal_claim_allowed"].astype(bool).any()

    registry = _read_csv("paper4_v48_candidate_registry.csv")
    assert not registry["paper1_promotion_allowed_v48"].astype(bool).any()

    champion = _read_json("paper4_v48_working_champion.json")
    assert champion["paper4_working_only"] is True
    assert champion["paper1_promotion_allowed"] is False

    decomposition = pd.read_parquet(
        TABLE_DIR / "paper4_v47_champion_decomposition_loan_level.parquet"
    )
    assert not decomposition.empty
    assert {"policy_id", "selection_relation_v47", "tail_loss_proxy_v47"}.issubset(
        decomposition.columns
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v45-v48" in notebook
    assert "Keep v45-v48 in the living notebook" in notebook
    assert (PAPER4_ROOT / "notes" / "paper4_future_wave_template.md").exists()
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v49_v54_self_directed_loop_repairs_artifacts_and_claim_boundaries() -> None:
    v49 = _read_json("paper4_v49_status.json")
    v50 = _read_json("paper4_v50_status.json")
    v51 = _read_json("paper4_v51_status.json")
    v52 = _read_json("paper4_v52_status.json")
    v53 = _read_json("paper4_v53_status.json")
    v54 = _read_json("paper4_v54_status.json")

    assert v49["phase"] == "v49_loss_matrix_online_repair"
    assert v50["phase"] == "v50_cvar_source_lp_solver"
    assert v51["phase"] == "v51_spo_dla_books_sicr_cases"
    assert v52["phase"] == "v52_registry_backlog_claims_self_directed"
    assert v53["phase"] == "v53_expected_loss_cvar_repair_and_budget_books"
    assert v54["phase"] == "v54_dynamic_budget_capped_book_replay"
    assert v49["strict_live_deployability_claim_allowed"] is False
    assert v50["exact_full_universe_cvar_claim_allowed"] is False
    assert v51["formal_differentiable_spo_claim_allowed"] is False
    assert v51["bellman_exact_claim_allowed"] is False
    assert v51["contractual_ifrs9_claim_allowed"] is False
    assert v52["paper1_promotion_allowed_v52"] is False
    assert v53["v50_zero_cvar_artifact_repaired_v53"] is True
    assert v53["exact_full_universe_cvar_claim_allowed"] is False
    assert v53["paper1_promotion_allowed_v53"] is False
    assert v54["bellman_exact_claim_allowed"] is False
    assert v54["live_no_leakage_claim_allowed"] is False
    assert v54["paper1_promotion_allowed_v54"] is False

    expected_csvs = {
        "paper4_v49_online_qhat_recalibration_search.csv": {
            "source_family",
            "delta_qhat_v49",
            "gate_source80_policy90_width95_v49",
            "claim_boundary_v49",
        },
        "paper4_v50_cvar_source_lp_frontier.csv": {
            "regime_v50",
            "solver_success_v50",
            "scenario_loss_cvar90_v50",
            "exact_full_universe_claim_v50",
        },
        "paper4_v51_policy_scenario_replay.csv": {
            "policy_id",
            "mean_scenario_return_v51",
            "p95_scenario_loss_v51",
            "claim_boundary_v51",
        },
        "paper4_v52_claim_matrix.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
        "paper4_v53_expected_loss_frontier.csv": {
            "regime_v53",
            "solver_success_v53",
            "scenario_loss_cvar90_v53",
            "exact_full_universe_claim_v53",
        },
        "paper4_v53_binary_vs_expected_diagnostic.csv": {
            "diagnostic_id",
            "source_artifact",
            "interpretation",
            "claim_impact",
        },
        "paper4_v53_budget_capped_policy_replay.csv": {
            "policy_id",
            "funded_exposure_v53",
            "budget_capped_v53",
            "p95_scenario_loss_v53",
        },
        "paper4_v53_claim_matrix_delta.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
        "paper4_v54_dynamic_budget_capped_book_summary.csv": {
            "policy_id",
            "final_wealth_mean_v54",
            "final_loss_p95_v54",
            "live_no_leakage_claim_allowed_v54",
        },
        "paper4_v54_dynamic_champion_decision_memo.csv": {
            "memo_id",
            "best_policy_id",
            "working_champion_change_allowed",
            "claim_boundary",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    matrix_v49 = pd.read_parquet(TABLE_DIR / "paper4_v49_loan_scenario_loss_matrix.parquet")
    matrix_v53 = pd.read_parquet(TABLE_DIR / "paper4_v53_expected_loss_matrix.parquet")
    trace_v54 = pd.read_parquet(TABLE_DIR / "paper4_v54_dynamic_budget_capped_book_trace.parquet")
    assert len(matrix_v49) >= 1_000_000
    assert len(matrix_v53) == len(matrix_v49)
    assert {"hybrid_loss_amount_v53", "hybrid_return_amount_v53"}.issubset(matrix_v53.columns)
    assert pd.to_numeric(matrix_v53["hybrid_loss_amount_v53"], errors="coerce").max() > 0
    assert not trace_v54.empty
    assert {"cash_v54", "outstanding_principal_v54", "wealth_v54"}.issubset(trace_v54.columns)

    v50_frontier = _read_csv("paper4_v50_cvar_source_lp_frontier.csv")
    v53_frontier = _read_csv("paper4_v53_expected_loss_frontier.csv")
    assert pd.to_numeric(v50_frontier["scenario_loss_cvar90_v50"], errors="coerce").max() == 0
    assert pd.to_numeric(v53_frontier["scenario_loss_cvar90_v53"], errors="coerce").min() > 0

    replay = _read_csv("paper4_v53_budget_capped_policy_replay.csv")
    assert replay["budget_capped_v53"].astype(bool).all()
    summary = _read_csv("paper4_v54_dynamic_budget_capped_book_summary.csv")
    assert not summary["live_no_leakage_claim_allowed_v54"].astype(bool).any()

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v49-v52" in notebook
    assert "Wave v53: Expected-Loss CVaR Repair" in notebook
    assert "Wave v54: Dynamic Replay Of Repaired Books" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v55_v61_unlock_loop_keeps_claim_boundaries() -> None:
    v55 = _read_json("paper4_v55_status.json")
    v56 = _read_json("paper4_v56_status.json")
    v57 = _read_json("paper4_v57_status.json")
    v58 = _read_json("paper4_v58_status.json")
    v59 = _read_json("paper4_v59_status.json")
    v60 = _read_json("paper4_v60_status.json")
    v61 = _read_json("paper4_v61_status.json")
    v62 = _read_json("paper4_v62_status.json")

    assert v55["phase"] == "v55_full_comparable_universe_lineage_unlock"
    assert v55["maximal_comparable_universe_rows_v55"] == 276_869
    assert v55["prediction_test_join_rate_v55"] == pytest.approx(1.0)
    assert v56["phase"] == "v56_expanded_restricted_master_cvar_source_solver"
    assert v56["restricted_master_columns_v56"] >= 36_000
    assert v57["phase"] == "v57_online_spo_dla_ifrs9_gate_updates"
    assert v58["phase"] == "v58_registry_claims_storage_notebook"
    assert v59["phase"] == "v59_adaptive_cvar_feasibility_frontier"
    assert v59["adaptive_feasible_rows_v59"] >= 1
    assert v60["phase"] == "v60_dynamic_stress_gate_for_v59_candidates"
    assert v61["phase"] == "v61_source_diversified_cvar_frontier"
    assert v62["phase"] == "v62_source_diversification_slack_certificate"

    for status in (v55, v56, v57, v58, v59, v60, v61, v62):
        assert status.get("paper4_final_promotion_created") is False

    assert v56["exact_full_universe_cvar_claim_allowed"] is False
    assert v57["strict_live_deployability_claim_allowed"] is False
    assert v57["formal_differentiable_spo_claim_allowed"] is False
    assert v57["bellman_exact_claim_allowed"] is False
    assert v57["contractual_ifrs9_claim_allowed"] is False
    assert v57["cate_policy_value_allowed"] is False
    assert v57["fair_lending_legal_claim_allowed"] is False
    assert v60["focused_512_or_1024_rerun_executed_v60"] is False
    assert v60["paper1_promotion_allowed_v60"] is False
    assert v61["exact_full_universe_cvar_claim_allowed"] is False
    assert v62["paper1_promotion_allowed_v62"] is False
    assert v62["max_required_cap_slack_share_v62"] > 0

    expected_csvs = {
        "paper4_v55_full_universe_lineage_audit.csv": {
            "lineage_item",
            "status_v55",
            "match_rows_v55",
            "claim_boundary_v55",
        },
        "paper4_v55_join_match_rate_table.csv": {
            "left_source",
            "right_source",
            "intersection_n",
            "left_match_rate",
        },
        "paper4_v56_cvar_full_comparable_frontier.csv": {
            "regime_v56",
            "solver_success_v56",
            "exact_full_universe_claim_v56",
            "claim_boundary_v56",
        },
        "paper4_v56_cvar_slack_certificate.csv": {
            "regime_v56",
            "required_cvar_slack_v56",
            "certificate_scope_v56",
        },
        "paper4_v57_online_source_family_direct_repair.csv": {
            "source_family",
            "gate_source80_policy90_width95_v57",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v57_spo_dependency_probe.csv": {
            "package",
            "formal_differentiable_spo_claim_allowed",
            "claim_boundary_v57",
        },
        "paper4_v57_ifrs9_sicr_proxy_panel_update.csv": {
            "sicr_rule_v57",
            "contractual_ifrs9_claim_allowed",
            "claim_boundary_v57",
        },
        "paper4_v58_candidate_registry.csv": {
            "policy_id",
            "full_governance_score_v58",
            "paper1_promotion_allowed_v58",
        },
        "paper4_v58_claim_matrix.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
        "paper4_v59_cvar_adaptive_feasible_frontier.csv": {
            "regime_v59",
            "solver_success_v59",
            "scenario_loss_cvar90_v59",
            "claim_boundary_v59",
        },
        "paper4_v60_dynamic_rerun_gate_memo.csv": {
            "memo_id",
            "focused_512_or_1024_rerun_executed_v60",
            "working_champion_change_allowed_v60",
        },
        "paper4_v61_source_diversified_frontier.csv": {
            "regime_v61",
            "solver_success_v61",
            "hard_family_caps_v61",
            "claim_boundary_v61",
        },
        "paper4_v61_blocker_dashboard.csv": {
            "lane",
            "status",
            "evidence_artifact",
            "next_unlock",
        },
        "paper4_v62_source_diversification_slack_certificate.csv": {
            "policy_id",
            "source_family",
            "required_cap_slack_share_v62",
            "certificate_scope_v62",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    universe = pd.read_parquet(
        TABLE_DIR / "paper4_v55_maximal_comparable_universe.parquet",
        columns=["loan_id", "pd_point", "qhat_v4", "grade"],
    )
    assert len(universe) == 276_869
    assert universe["loan_id"].is_unique
    assert universe["pd_point"].between(0, 1).all()

    lineage = _read_csv("paper4_v55_full_universe_lineage_audit.csv")
    assert (
        lineage.loc[
            lineage["lineage_item"].eq("exact_prediction_to_test_join"), "match_rows_v55"
        ].iloc[0]
        == 276_869
    )

    v56_frontier = _read_csv("paper4_v56_cvar_full_comparable_frontier.csv")
    assert not v56_frontier["exact_full_universe_claim_v56"].astype(bool).any()

    v57_online = _read_csv("paper4_v57_online_source_family_direct_repair.csv")
    assert not v57_online["strict_live_deployability_claim_allowed"].astype(bool).any()

    v58_claims = _read_csv("paper4_v58_claim_matrix.csv")
    prohibited = v58_claims.loc[v58_claims["claim_id"].str.contains("exact|online|spo|ifrs9")]
    assert not prohibited["allowed"].astype(bool).all()

    v60_memo = _read_csv("paper4_v60_dynamic_rerun_gate_memo.csv")
    assert not v60_memo["focused_512_or_1024_rerun_executed_v60"].astype(bool).any()
    assert not v60_memo["working_champion_change_allowed_v60"].astype(bool).any()

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Source-diversified CVaR challenger is feasible and promotable." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v55-v58" in notebook
    assert "Wave v59" in notebook
    assert "Wave v60" in notebook
    assert "Wave v61" in notebook
    assert "Wave v62" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v63_source_governance_repair_is_lab_only() -> None:
    status = _read_json("paper4_v63_status.json")

    assert status["phase"] == "v63_source_governance_repair_frontier"
    assert status["source_repair_frontier_rows_v63"] >= 4
    assert status["source_repair_success_rows_v63"] >= 1
    assert status["source_repair_book_rows_v63"] > 0
    assert status["dynamic_rerun_recommended_rows_v63"] == 0
    assert status["exact_full_universe_cvar_claim_allowed"] is False
    assert status["paper1_promotion_allowed_v63"] is False
    assert status["paper4_working_champion_changed_v63"] is False
    assert status["paper4_final_promotion_created"] is False

    expected_csvs = {
        "paper4_v63_source_repair_frontier.csv": {
            "policy_id",
            "source_repair_success_v63",
            "scenario_loss_cvar90_v63",
            "max_required_cap_slack_share_v63",
            "exact_full_universe_claim_v63",
            "paper1_promotion_allowed_v63",
            "claim_boundary_v63",
        },
        "paper4_v63_source_repair_concentration.csv": {
            "policy_id",
            "source_family",
            "top_exposure_share_v63",
            "target_cap_v63",
            "required_cap_slack_share_v63",
        },
        "paper4_v63_dynamic_gate_memo.csv": {
            "policy_id",
            "dynamic_512_or_1024_rerun_recommended_v63",
            "working_champion_change_allowed_v63",
            "rerun_decision_reason_v63",
        },
        "paper4_v63_claim_matrix_delta.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    books = pd.read_parquet(TABLE_DIR / "paper4_v63_source_repair_candidate_books.parquet")
    assert not books.empty
    assert {"policy_id_v63", "allocated_exposure_v63", "claim_boundary_v63"}.issubset(books.columns)

    frontier = _read_csv("paper4_v63_source_repair_frontier.csv")
    assert frontier["source_repair_success_v63"].astype(bool).any()
    assert not frontier["exact_full_universe_claim_v63"].astype(bool).any()
    assert not frontier["paper1_promotion_allowed_v63"].astype(bool).any()
    assert frontier["max_source_share_v63"].max() < 0.90

    concentration = _read_csv("paper4_v63_source_repair_concentration.csv")
    grade_top = concentration.loc[concentration["source_family"].eq("grade")]
    assert grade_top["top_exposure_share_v63"].max() < 0.90
    assert concentration["required_cap_slack_share_v63"].max() < 0.01

    gate = _read_csv("paper4_v63_dynamic_gate_memo.csv")
    assert not gate["dynamic_512_or_1024_rerun_recommended_v63"].astype(bool).any()
    assert not gate["working_champion_change_allowed_v63"].astype(bool).any()

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has heuristic source-governance repair candidates after v59-v62." in set(
        current_boundaries["claim"]
    )
    assert "A v63 source-repaired candidate replaces Paper Estrella." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v63: Source-Governance Repair Frontier" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v64_online_pseudo_unseen_stress_is_lab_only() -> None:
    status = _read_json("paper4_v64_status.json")

    assert status["phase"] == "v64_online_pseudo_unseen_stress"
    assert status["stress_grid_rows_v64"] >= 2_000
    assert status["method_split_summary_rows_v64"] >= 500
    assert status["winner_rows_v64"] == 7
    assert status["cell_summary_rows_v64"] > 0
    assert status["local_split_gate_pass_rows_v64"] > 0
    assert status["strict_all_split_pass_rows_v64"] == 0
    assert status["external_unseen_data_available_v64"] is False
    assert status["strict_live_deployability_claim_allowed_v64"] is False
    assert status["paper1_promotion_allowed_v64"] is False
    assert status["paper4_working_champion_changed_v64"] is False
    assert status["paper4_final_promotion_created"] is False

    expected_csvs = {
        "paper4_v64_online_input_audit.csv": {
            "audit_item_v64",
            "value_v64",
            "claim_boundary_v64",
        },
        "paper4_v64_online_pseudo_unseen_stress_grid.csv": {
            "source_family",
            "pseudo_unseen_split_v64",
            "source_month_defended_min_v64",
            "policy_month_defended_min_v64",
            "avg_width_loan_v64",
            "gate_source80_policy90_width95_v64",
            "strict_live_deployability_claim_allowed",
            "claim_boundary_v64",
        },
        "paper4_v64_online_method_split_summary.csv": {
            "source_family",
            "split_gate_pass_rows_v64",
            "all_splits_gate_pass_v64",
            "width_margin_to_0p95_v64",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v64_online_winner_memo.csv": {
            "source_family",
            "recommendation_v64",
            "all_splits_gate_pass_v64",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v64_claim_matrix_delta.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    grid = _read_csv("paper4_v64_online_pseudo_unseen_stress_grid.csv")
    assert grid["gate_source80_policy90_width95_v64"].astype(bool).any()
    assert not grid["strict_live_deployability_claim_allowed"].astype(bool).any()
    assert not grid["external_unseen_data_available_v64"].astype(bool).any()

    summary = _read_csv("paper4_v64_online_method_split_summary.csv")
    assert not summary["all_splits_gate_pass_v64"].astype(bool).any()
    assert summary["split_gate_pass_rows_v64"].max() < summary["evaluated_splits_v64"].max()

    winners = _read_csv("paper4_v64_online_winner_memo.csv")
    assert len(winners) == 7
    assert not winners["all_splits_gate_pass_v64"].astype(bool).any()
    assert winners["split_gate_pass_rows_v64"].max() == 3
    assert winners["max_avg_width_loan_v64"].min() > 0.95
    assert not winners["strict_live_deployability_claim_allowed"].astype(bool).any()

    cells = pd.read_parquet(TABLE_DIR / "paper4_v64_online_stress_cell_summary.parquet")
    assert not cells.empty
    assert {
        "source_family",
        "pseudo_unseen_split_v64",
        "coverage_v64",
        "avg_width_v64",
        "claim_boundary_v64",
    }.issubset(cells.columns)

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has pseudo-unseen online conformal stress diagnostics." in set(
        current_boundaries["claim"]
    )
    assert "Online conformal coverage is strictly live-deployable after v64." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v64: Online Pseudo-Unseen Stress" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v65_online_margin_repair_keeps_live_claim_blocked() -> None:
    status = _read_json("paper4_v65_status.json")

    assert status["phase"] == "v65_online_margin_repair"
    assert status["margin_grid_rows_v65"] >= 1_000
    assert status["margin_summary_rows_v65"] >= 250
    assert status["winner_rows_v65"] == 2
    assert status["cell_summary_rows_v65"] > 0
    assert status["all_split_gate_pass_rows_v65"] > 0
    assert status["families_with_all_split_pass_v65"] == 2
    assert status["best_width_margin_to_0p95_v65"] > 0
    assert status["external_unseen_data_available_v65"] is False
    assert status["strict_live_deployability_claim_allowed_v65"] is False
    assert status["paper1_promotion_allowed_v65"] is False
    assert status["paper4_working_champion_changed_v65"] is False
    assert status["paper4_final_promotion_created"] is False

    expected_csvs = {
        "paper4_v65_online_margin_repair_input_audit.csv": {
            "audit_item_v64",
            "value_v64",
            "claim_boundary_v65",
        },
        "paper4_v65_online_margin_repair_grid.csv": {
            "source_family",
            "pseudo_unseen_split_v65",
            "global_delta_v65",
            "source_month_defended_min_v65",
            "policy_month_defended_min_v65",
            "avg_width_loan_v65",
            "gate_source80_policy90_width95_v65",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v65_online_margin_repair_summary.csv": {
            "source_family",
            "all_splits_gate_pass_v65",
            "source_margin_to_0p80_v65",
            "policy_margin_to_0p90_v65",
            "width_margin_to_0p95_v65",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v65_online_margin_repair_winners.csv": {
            "source_family",
            "global_delta_v65",
            "recommendation_v65",
            "all_splits_gate_pass_v65",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v65_claim_matrix_delta.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    summary = _read_csv("paper4_v65_online_margin_repair_summary.csv")
    passing = summary.loc[summary["all_splits_gate_pass_v65"].astype(bool)]
    assert not passing.empty
    assert set(passing["source_family"].unique()) == {"period", "term"}
    assert passing["source_margin_to_0p80_v65"].min() > 0
    assert passing["policy_margin_to_0p90_v65"].min() >= 0
    assert passing["width_margin_to_0p95_v65"].min() > 0
    assert not passing["strict_live_deployability_claim_allowed"].astype(bool).any()

    winners = _read_csv("paper4_v65_online_margin_repair_winners.csv")
    assert set(winners["source_family"]) == {"period", "term"}
    assert winners["all_splits_gate_pass_v65"].astype(bool).all()
    assert winners["global_delta_v65"].min() == pytest.approx(0.012)
    assert winners["width_margin_to_0p95_v65"].min() > 0
    assert not winners["strict_live_deployability_claim_allowed"].astype(bool).any()

    cells = pd.read_parquet(TABLE_DIR / "paper4_v65_online_margin_repair_cells.parquet")
    assert not cells.empty
    assert {
        "source_family",
        "pseudo_unseen_split_v65",
        "coverage_v64",
        "avg_width_v64",
        "claim_boundary_v65",
    }.issubset(cells.columns)

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert (
        "Paper 4 has an internal pseudo-unseen online margin repair that passes all historical splits."
        in set(current_boundaries["claim"])
    )
    assert "v65 margin repair proves live online deployability." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v65: Online Margin Repair" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v66_external_holdout_protocol_is_not_validation() -> None:
    status = _read_json("paper4_v66_status.json")

    assert status["phase"] == "v66_external_holdout_protocol"
    assert status["frozen_method_rows_v66"] == 2
    assert status["required_schema_rows_v66"] >= 10
    assert status["gate_spec_rows_v66"] >= 7
    assert status["protocol_rows_v66"] >= 6
    assert status["leakage_check_rows_v66"] >= 7
    assert status["method_frozen_for_future_holdout_v66"] is True
    assert status["external_holdout_data_available_v66"] is False
    assert status["strict_live_deployability_claim_allowed_v66"] is False
    assert status["paper1_promotion_allowed_v66"] is False
    assert status["paper4_working_champion_changed_v66"] is False
    assert status["paper4_final_promotion_created"] is False

    expected_csvs = {
        "paper4_v66_frozen_method_manifest.csv": {
            "frozen_method_id_v66",
            "source_family",
            "selection_artifact_sha256_v66",
            "allow_parameter_changes_before_external_holdout_v66",
            "strict_live_deployability_claim_allowed",
        },
        "paper4_v66_required_holdout_schema.csv": {
            "column_name_v66",
            "required_v66",
            "validation_rule_v66",
        },
        "paper4_v66_holdout_gate_spec.csv": {
            "gate_id_v66",
            "threshold_v66",
            "operator_v66",
            "gate_required_for_live_claim_v66",
        },
        "paper4_v66_external_holdout_protocol.csv": {
            "protocol_step_v66",
            "action_v66",
            "locked_instruction_v66",
            "editable_after_v66_freeze",
        },
        "paper4_v66_leakage_prevention_checklist.csv": {
            "leakage_check_id_v66",
            "rule_v66",
            "failure_action_v66",
        },
        "paper4_v66_claim_matrix_delta.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    manifest = _read_csv("paper4_v66_frozen_method_manifest.csv")
    assert set(manifest["source_family"]) == {"period", "term"}
    assert manifest["global_delta_v65"].eq(0.012).all()
    assert not manifest["allow_parameter_changes_before_external_holdout_v66"].astype(bool).any()
    assert not manifest["strict_live_deployability_claim_allowed"].astype(bool).any()
    assert manifest["selection_artifact_sha256_v66"].str.len().eq(64).all()
    assert manifest["script_sha256_v66"].str.len().eq(64).all()

    schema = _read_csv("paper4_v66_required_holdout_schema.csv")
    assert {"loan_id", "issue_month", "y_true", "y_pred", "qhat_v9", "period", "term"}.issubset(
        set(schema["column_name_v66"])
    )
    assert schema["required_v66"].astype(bool).all()

    gates = _read_csv("paper4_v66_holdout_gate_spec.csv")
    assert {
        "source_month_coverage_min",
        "policy_month_coverage_min",
        "avg_interval_width",
    }.issubset(set(gates["gate_id_v66"]))
    assert gates["gate_required_for_live_claim_v66"].astype(bool).all()

    protocol = _read_csv("paper4_v66_external_holdout_protocol.csv")
    assert not protocol["editable_after_v66_freeze"].astype(bool).any()

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a frozen external-holdout protocol for v65 online candidates." in set(
        current_boundaries["claim"]
    )
    assert "v66 protocol itself validates live online deployment." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v66: External Holdout Protocol Freeze" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v67_external_holdout_scorer_blocks_without_data() -> None:
    status = _read_json("paper4_v67_status.json")

    assert status["phase"] == "v67_external_holdout_scorer"
    assert status["readiness_rows_v67"] >= 4
    assert status["scorecard_rows_v67"] == 2
    assert status["holdout_data_available_v67"] is False
    assert status["passing_methods_v67"] == 0
    assert status["strict_live_deployability_claim_allowed_v67"] is False
    assert status["paper1_promotion_allowed_v67"] is False
    assert status["paper4_working_champion_changed_v67"] is False
    assert status["paper4_final_promotion_created"] is False

    expected_csvs = {
        "paper4_v67_scorer_readiness.csv": {
            "readiness_item_v67",
            "pass_v67",
            "detail_v67",
            "claim_boundary_v67",
        },
        "paper4_v67_external_holdout_scorecard.csv": {
            "frozen_method_id_v66",
            "source_family",
            "holdout_data_available_v67",
            "all_gates_pass_v67",
            "strict_live_deployability_claim_allowed",
            "score_status_v67",
            "claim_boundary_v67",
        },
        "paper4_v67_claim_matrix_delta.csv": {
            "claim_id",
            "allowed",
            "artifact",
            "boundary",
        },
    }
    for name, columns in expected_csvs.items():
        table = _read_csv(name)
        assert not table.empty, name
        assert columns.issubset(table.columns), name

    readiness = _read_csv("paper4_v67_scorer_readiness.csv")
    readiness_map = dict(zip(readiness["readiness_item_v67"], readiness["pass_v67"], strict=False))
    assert readiness_map["frozen_manifest_exists"] is True
    assert readiness_map["selection_hash_matches_manifest"] is True
    assert readiness_map["holdout_file_available"] is False
    assert readiness_map["holdout_schema_complete"] is False

    scorecard = _read_csv("paper4_v67_external_holdout_scorecard.csv")
    assert set(scorecard["source_family"]) == {"period", "term"}
    assert not scorecard["holdout_data_available_v67"].astype(bool).any()
    assert not scorecard["all_gates_pass_v67"].astype(bool).any()
    assert not scorecard["strict_live_deployability_claim_allowed"].astype(bool).any()
    assert set(scorecard["score_status_v67"]) == {"blocked_missing_external_holdout_data"}

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has an executable frozen scorer for the v66 holdout protocol." in set(
        current_boundaries["claim"]
    )
    assert "v67 validates live online deployment." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v67: External Holdout Scorer" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v68_full_universe_pricing_screen_blocks_exact_claims() -> None:
    status = _read_json("paper4_v68_status.json")

    assert status["phase"] == "v68_full_universe_source_pricing_screen"
    assert status["screened_policy_rows_v68"] == 4
    assert status["candidate_screen_rows_v68"] >= 200
    assert status["benchmark_rows_v68"] == 4
    assert status["source_relief_rows_v68"] >= 24
    assert status["policies_with_unpriced_columns_v68"] == 4
    assert status["exact_dual_pricing_performed_v68"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v68"] is False
    assert status["paper1_promotion_allowed_v68"] is False
    assert status["paper4_working_champion_changed_v68"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = pd.read_parquet(TABLE_DIR / "paper4_v68_full_universe_candidate_screen.parquet")
    assert not candidates.empty
    assert {
        "policy_id_v68",
        "loan_id",
        "pricing_screen_score_v68",
        "source_relief_share_v68",
        "screen_scope_v68",
        "candidate_rank_v68",
    }.issubset(candidates.columns)
    assert candidates["policy_id_v68"].nunique() == 4
    assert candidates.groupby("policy_id_v68")["candidate_rank_v68"].max().eq(50).all()
    assert candidates["screen_scope_v68"].str.contains("not exact dual pricing").all()

    benchmark = _read_csv("paper4_v68_screen_vs_book_benchmark.csv")
    assert {
        "policy_id",
        "screened_universe_rows_v68",
        "out_of_book_rows_v68",
        "screen_detects_unpriced_columns_v68",
        "exact_full_universe_cvar_claim_allowed_v68",
        "claim_boundary_v68",
    }.issubset(benchmark.columns)
    assert benchmark["screened_universe_rows_v68"].eq(276869).all()
    assert benchmark["screen_detects_unpriced_columns_v68"].astype(bool).all()
    assert not benchmark["exact_full_universe_cvar_claim_allowed_v68"].astype(bool).any()
    assert benchmark["claim_boundary_v68"].str.contains("proxy screen only").all()

    relief = _read_csv("paper4_v68_source_relief_summary.csv")
    assert {
        "policy_id",
        "source_family",
        "top_candidate_share_not_top_source_v68",
        "claim_boundary_v68",
    }.issubset(relief.columns)
    assert relief["source_family"].nunique() >= 6
    assert relief["top_candidate_share_not_top_source_v68"].between(0, 1).all()

    claim_delta = _read_csv("paper4_v68_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v68_full_universe_proxy_screen_exists"]) is True
    assert bool(claim_map["v68_exact_full_universe_cvar_optimality"]) is False
    assert bool(claim_map["v68_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a full-universe source/pricing screen for v63 repair books." in set(
        current_boundaries["claim"]
    )
    assert "v68 proves exact full-universe CVaR optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v68: Full-Universe Source/Pricing Screen" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v69_restricted_master_expansion_is_protocol_only() -> None:
    status = _read_json("paper4_v69_status.json")

    assert status["phase"] == "v69_restricted_master_expansion_protocol"
    assert status["candidate_rows_v69"] == 200
    assert status["expanded_master_rows_v69"] == 1824
    assert status["swap_audit_rows_v69"] == 100
    assert status["protocol_rows_v69"] == 7
    assert status["policies_ready_for_restricted_solver_v69"] == 4
    assert status["positive_swap_score_rows_v69"] == 100
    assert status["restricted_master_pack_ready_v69"] is True
    assert status["exact_column_generation_certificate_v69"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v69"] is False
    assert status["paper1_promotion_allowed_v69"] is False
    assert status["paper4_working_champion_changed_v69"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = pd.read_parquet(
        TABLE_DIR / "paper4_v69_source_pricing_expansion_candidates.parquet"
    )
    assert not candidates.empty
    assert {
        "policy_id_v69",
        "source_policy_id_v63",
        "candidate_rank_v68",
        "pricing_screen_score_v69",
        "recommended_for_restricted_master_v69",
        "claim_boundary_v69",
    }.issubset(candidates.columns)
    assert candidates["policy_id_v69"].nunique() == 4
    assert candidates.groupby("policy_id_v69")["candidate_rank_v68"].max().eq(50).all()
    assert candidates["recommended_for_restricted_master_v69"].astype(bool).all()

    master = pd.read_parquet(TABLE_DIR / "paper4_v69_expanded_restricted_master.parquet")
    assert not master.empty
    assert {
        "policy_id_v69",
        "master_role_v69",
        "pricing_screen_score_v69",
        "exact_column_generation_certificate_v69",
        "claim_boundary_v69",
    }.issubset(master.columns)
    assert set(master["master_role_v69"]) == {"incumbent_v63_book", "v68_pricing_candidate"}
    assert not master["exact_column_generation_certificate_v69"].astype(bool).any()
    assert master["claim_boundary_v69"].str.contains("not exact full-universe").all()

    swaps = _read_csv("paper4_v69_candidate_swap_audit.csv")
    assert {
        "policy_id",
        "swap_rank_v69",
        "add_loan_id_v69",
        "drop_loan_id_v69",
        "delta_pricing_score_v69",
        "valid_budget_after_swap_v69",
        "claim_boundary_v69",
    }.issubset(swaps.columns)
    assert swaps["policy_id"].nunique() == 4
    assert swaps.groupby("policy_id")["swap_rank_v69"].max().eq(25).all()
    assert swaps["delta_pricing_score_v69"].gt(0).all()
    assert swaps["valid_budget_after_swap_v69"].astype(bool).all()
    assert swaps["claim_boundary_v69"].str.contains("heuristic").all()

    protocol = _read_csv("paper4_v69_exact_column_generation_protocol.csv")
    assert {
        "protocol_step_v69",
        "step_name_v69",
        "artifact_v69",
        "required_evidence_v69",
        "claim_if_missing_v69",
        "locked_v69",
    }.issubset(protocol.columns)
    assert protocol["locked_v69"].astype(bool).all()
    assert {
        "solve_expanded_restricted_master",
        "persist_duals",
        "price_omitted_universe",
        "iterate_negative_reduced_cost_columns",
    }.issubset(set(protocol["step_name_v69"]))

    claim_delta = _read_csv("paper4_v69_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v69_restricted_master_expansion_pack_exists"]) is True
    assert bool(claim_map["v69_exact_column_generation_certificate"]) is False
    assert bool(claim_map["v69_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert (
        "Paper 4 has a v69 restricted-master expansion pack for column-generation follow-up."
        in set(current_boundaries["claim"])
    )
    assert "v69 is an exact full-universe column-generation certificate." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v69: Restricted-Master Expansion Pack" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v70_restricted_master_solver_keeps_full_universe_claim_blocked() -> None:
    status = _read_json("paper4_v70_status.json")

    assert status["phase"] == "v70_restricted_master_continuous_lp_solver"
    assert status["frontier_rows_v70"] == 8
    assert status["successful_lp_rows_v70"] == 8
    assert status["allocation_rows_v70"] > 0
    assert status["scenario_rows_v70"] == 1024
    assert status["active_constraint_rows_v70"] >= 1000
    assert status["policies_with_successful_lp_v70"] == 4
    assert status["best_delta_return_vs_incumbent_v70"] > 0
    assert status["exact_restricted_master_lp_claim_allowed_v70"] is True
    assert status["exact_full_universe_cvar_claim_allowed_v70"] is False
    assert status["paper1_promotion_allowed_v70"] is False
    assert status["paper4_working_champion_changed_v70"] is False
    assert status["paper4_final_promotion_created"] is False

    frontier = _read_csv("paper4_v70_restricted_master_solver_frontier.csv")
    assert {
        "policy_id",
        "regime_v70",
        "solver_success_v70",
        "delta_return_vs_incumbent_v70",
        "candidate_allocation_share_v70",
        "exact_restricted_master_lp_v70",
        "exact_full_universe_cvar_claim_allowed_v70",
        "claim_boundary_v70",
    }.issubset(frontier.columns)
    assert frontier["policy_id"].nunique() == 4
    assert set(frontier["regime_v70"]) == {
        "incumbent_cvar_relaxed_source_lp",
        "target_source_cap_probe_lp",
    }
    assert frontier["solver_success_v70"].astype(bool).all()
    assert frontier["delta_return_vs_incumbent_v70"].gt(0).all()
    assert frontier["candidate_allocation_share_v70"].gt(0).all()
    assert frontier["exact_restricted_master_lp_v70"].astype(bool).all()
    assert not frontier["exact_full_universe_cvar_claim_allowed_v70"].astype(bool).any()
    assert frontier["claim_boundary_v70"].str.contains("no omitted-column pricing").all()

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v70_restricted_master_allocations.parquet")
    assert not allocations.empty
    assert {
        "policy_id",
        "regime_v70",
        "loan_id",
        "master_role_v69",
        "allocation_fraction_v70",
        "allocated_exposure_v70",
        "claim_boundary_v70",
    }.issubset(allocations.columns)
    assert allocations["allocation_fraction_v70"].between(0, 1).all()
    assert "v68_pricing_candidate" in set(allocations["master_role_v69"])
    assert allocations["claim_boundary_v70"].str.contains("not full-universe").all()

    scenarios = _read_csv("paper4_v70_restricted_master_scenario_losses.csv")
    assert {
        "policy_id",
        "regime_v70",
        "path_id",
        "scenario_loss_v70",
        "scenario_return_v70",
    }.issubset(scenarios.columns)
    assert scenarios.groupby(["policy_id", "regime_v70"])["path_id"].nunique().eq(128).all()

    active = _read_csv("paper4_v70_solver_active_constraints.csv")
    assert {
        "policy_id",
        "regime_v70",
        "constraint_type_v70",
        "lhs_v70",
        "rhs_v70",
        "slack_v70",
        "binding_v70",
        "claim_boundary_v70",
    }.issubset(active.columns)
    assert {"budget_lower", "cvar_cap", "source_share", "cvar_path_excess"}.issubset(
        set(active["constraint_type_v70"])
    )

    blockers = _read_csv("paper4_v70_solver_claim_blockers.csv")
    assert {
        "blocker_id_v70",
        "status_v70",
        "required_next_artifact_v70",
        "claim_boundary_v70",
    }.issubset(blockers.columns)
    assert "omitted_column_reduced_costs_missing" in set(blockers["blocker_id_v70"])
    assert "continuous_relaxation_not_whole_loan_milp" in set(blockers["blocker_id_v70"])

    claim_delta = _read_csv("paper4_v70_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v70_exact_restricted_master_continuous_lp"]) is True
    assert bool(claim_map["v70_exact_full_universe_cvar_optimality"]) is False
    assert bool(claim_map["v70_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has exact continuous LP solves over the v69 restricted master." in set(
        current_boundaries["claim"]
    )
    assert "v70 proves exact full-universe CVaR optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v70: Restricted-Master Continuous LP Solver" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v71_reduced_cost_screen_blocks_column_generation_termination() -> None:
    status = _read_json("paper4_v71_status.json")

    assert status["phase"] == "v71_full_universe_reduced_cost_screen"
    assert status["dual_rows_v71"] == 1410
    assert status["reduced_cost_rows_v71"] == 2211304
    assert status["summary_rows_v71"] == 8
    assert status["source_cap_dual_rows_v71"] == 48
    assert status["claim_blocker_rows_v71"] == 3
    assert status["improving_omitted_columns_v71"] == 5738
    assert status["policies_priced_v71"] == 4
    assert status["regime_rows_priced_v71"] == 8
    assert status["negative_reduced_cost_detected_v71"] is True
    assert status["full_universe_termination_claim_allowed_v71"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v71"] is False
    assert status["paper1_promotion_allowed_v71"] is False
    assert status["paper4_working_champion_changed_v71"] is False
    assert status["paper4_final_promotion_created"] is False

    reduced_costs = pd.read_parquet(TABLE_DIR / "paper4_v71_full_universe_reduced_costs.parquet")
    assert not reduced_costs.empty
    assert {
        "policy_id",
        "regime_v71",
        "loan_id",
        "minimization_reduced_cost_v71",
        "return_improvement_signal_v71",
        "improving_column_v71",
        "pricing_scope_v71",
        "claim_boundary_v71",
    }.issubset(reduced_costs.columns)
    assert reduced_costs["policy_id"].nunique() == 4
    assert set(reduced_costs["regime_v71"]) == {
        "incumbent_cvar_relaxed_source_lp",
        "target_source_cap_probe_lp",
    }
    assert (
        int(reduced_costs["improving_column_v71"].sum()) == status["improving_omitted_columns_v71"]
    )
    assert reduced_costs["minimization_reduced_cost_v71"].min() < 0
    assert reduced_costs["claim_boundary_v71"].str.contains("not full-universe termination").all()

    summary = _read_csv("paper4_v71_reduced_cost_summary.csv")
    assert {
        "policy_id",
        "regime_v71",
        "omitted_rows_priced_v71",
        "improving_columns_v71",
        "negative_reduced_cost_detected_v71",
        "column_generation_termination_certificate_v71",
        "exact_full_universe_cvar_claim_allowed_v71",
    }.issubset(summary.columns)
    assert summary["omitted_rows_priced_v71"].sum() == status["reduced_cost_rows_v71"]
    assert summary["improving_columns_v71"].sum() == status["improving_omitted_columns_v71"]
    assert summary["negative_reduced_cost_detected_v71"].astype(bool).any()
    assert not summary["column_generation_termination_certificate_v71"].astype(bool).any()
    assert not summary["exact_full_universe_cvar_claim_allowed_v71"].astype(bool).any()

    duals = _read_csv("paper4_v71_restricted_master_duals.csv")
    assert {
        "policy_id",
        "regime_v71",
        "constraint_index_v71",
        "constraint_type_v71",
        "marginal_v71",
        "binding_v71",
        "claim_boundary_v71",
    }.issubset(duals.columns)
    assert {"budget_lower", "source_share", "cvar_path_excess"}.issubset(
        set(duals["constraint_type_v71"])
    )
    assert duals["marginal_v71"].abs().gt(0).any()
    assert duals["claim_boundary_v71"].str.contains("not full-universe certificate").all()

    source_diag = _read_csv("paper4_v71_source_cap_dual_diagnostics.csv")
    assert {
        "policy_id",
        "regime_v71",
        "source_family",
        "missing_source_ids_v71",
        "source_constraint_scope_complete_v71",
        "claim_boundary_v71",
    }.issubset(source_diag.columns)
    assert source_diag["missing_source_ids_v71"].sum() > 0
    assert not source_diag["source_constraint_scope_complete_v71"].astype(bool).all()

    blockers = _read_csv("paper4_v71_claim_blockers.csv")
    assert {"blocker_id_v71", "blocking_v71", "evidence_count_v71", "claim_boundary_v71"}.issubset(
        blockers.columns
    )
    assert blockers["blocking_v71"].astype(bool).all()
    assert {
        "negative_reduced_cost_columns_detected",
        "source_constraint_scope_not_full_universe",
        "continuous_relaxation_not_whole_loan_milp",
    }.issubset(set(blockers["blocker_id_v71"]))

    claim_delta = _read_csv("paper4_v71_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v71_omitted_column_reduced_cost_screen"]) is True
    assert bool(claim_map["v71_full_universe_column_generation_termination"]) is False
    assert bool(claim_map["v71_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has v71 reduced-cost pricing for omitted v55 columns under v70 duals." in set(
        current_boundaries["claim"]
    )
    assert "v71 proves full-universe column-generation termination." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v71: Full-Universe Reduced-Cost Screen" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v72_column_generation_iteration_requires_repricing() -> None:
    status = _read_json("paper4_v72_status.json")

    assert status["phase"] == "v72_column_generation_iteration_1"
    assert status["candidate_rows_v72"] == 5738
    assert status["frontier_rows_v72"] == 5
    assert status["successful_iteration_rows_v72"] == 5
    assert status["allocation_rows_v72"] > 0
    assert status["scenario_rows_v72"] == 640
    assert status["active_constraint_rows_v72"] == 880
    assert status["comparison_rows_v72"] == 5
    assert status["best_delta_return_vs_v70_iteration_v72"] > 0
    assert status["v71_candidate_allocated_exposure_v72"] > 0
    assert status["reprice_after_iteration_performed_v72"] is False
    assert status["column_generation_termination_claim_allowed_v72"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v72"] is False
    assert status["paper1_promotion_allowed_v72"] is False
    assert status["paper4_working_champion_changed_v72"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = pd.read_parquet(TABLE_DIR / "paper4_v72_iteration_1_candidates.parquet")
    assert not candidates.empty
    assert {
        "policy_id",
        "regime_v71",
        "loan_id",
        "minimization_reduced_cost_v71",
        "master_role_v69",
        "claim_boundary_v69",
    }.issubset(candidates.columns)
    assert len(candidates) == status["candidate_rows_v72"]
    assert candidates["minimization_reduced_cost_v71"].lt(0).all()
    assert set(candidates["master_role_v69"]) == {"v71_negative_reduced_cost_column"}

    frontier = _read_csv("paper4_v72_iteration_1_frontier.csv")
    assert {
        "policy_id",
        "regime_v72",
        "solver_success_v72",
        "negative_reduced_cost_candidates_added_v72",
        "delta_return_vs_v70_iteration_v72",
        "v71_candidate_allocated_exposure_v72",
        "reprice_after_iteration_performed_v72",
        "column_generation_termination_claim_allowed_v72",
        "exact_full_universe_cvar_claim_allowed_v72",
        "claim_boundary_v72",
    }.issubset(frontier.columns)
    assert frontier["solver_success_v72"].astype(bool).all()
    assert (
        frontier["negative_reduced_cost_candidates_added_v72"].sum() == status["candidate_rows_v72"]
    )
    assert frontier["delta_return_vs_v70_iteration_v72"].gt(0).all()
    assert frontier["v71_candidate_allocated_exposure_v72"].gt(0).all()
    assert not frontier["reprice_after_iteration_performed_v72"].astype(bool).any()
    assert not frontier["column_generation_termination_claim_allowed_v72"].astype(bool).any()
    assert not frontier["exact_full_universe_cvar_claim_allowed_v72"].astype(bool).any()
    assert frontier["claim_boundary_v72"].str.contains("re-pricing still required").all()

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v72_iteration_1_allocations.parquet")
    assert not allocations.empty
    assert {
        "policy_id",
        "regime_v72",
        "loan_id",
        "master_role_v72",
        "allocation_fraction_v72",
        "allocated_exposure_v72",
        "claim_boundary_v72",
    }.issubset(allocations.columns)
    assert allocations["allocation_fraction_v72"].between(0, 1).all()
    assert "v71_negative_reduced_cost_column" in set(allocations["master_role_v72"])
    assert allocations["claim_boundary_v72"].str.contains("re-pricing still required").all()

    scenarios = _read_csv("paper4_v72_iteration_1_scenario_losses.csv")
    assert {
        "policy_id",
        "regime_v72",
        "path_id",
        "scenario_loss_v72",
        "scenario_return_v72",
    }.issubset(scenarios.columns)
    assert scenarios.groupby(["policy_id", "regime_v72"])["path_id"].nunique().eq(128).all()

    active = _read_csv("paper4_v72_iteration_1_active_constraints.csv")
    assert {
        "policy_id",
        "regime_v72",
        "constraint_type_v72",
        "lhs_v72",
        "rhs_v72",
        "slack_v72",
        "binding_v72",
    }.issubset(active.columns)
    assert {"budget_lower", "source_share", "cvar_path_excess"}.issubset(
        set(active["constraint_type_v72"])
    )

    comparison = _read_csv("paper4_v72_iteration_1_comparison.csv")
    assert {
        "policy_id",
        "regime_v72",
        "delta_return_vs_v70_iteration_v72",
        "delta_cvar90_vs_v70_iteration_v72",
        "column_generation_termination_claim_allowed_v72",
    }.issubset(comparison.columns)
    assert comparison["delta_return_vs_v70_iteration_v72"].gt(0).all()
    assert comparison["delta_cvar90_vs_v70_iteration_v72"].lt(0).all()
    assert not comparison["column_generation_termination_claim_allowed_v72"].astype(bool).any()

    blockers = _read_csv("paper4_v72_claim_blockers.csv")
    assert {
        "blocker_id_v72",
        "blocking_v72",
        "required_next_artifact_v72",
        "claim_boundary_v72",
    }.issubset(blockers.columns)
    assert blockers["blocking_v72"].astype(bool).all()
    assert {
        "post_iteration_repricing_missing",
        "source_constraint_scope_needs_reaudit",
        "continuous_relaxation_not_whole_loan_milp",
    }.issubset(set(blockers["blocker_id_v72"]))

    claim_delta = _read_csv("paper4_v72_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v72_column_generation_iteration_1_completed"]) is True
    assert bool(claim_map["v72_column_generation_converged"]) is False
    assert bool(claim_map["v72_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v72 first column-generation iteration over negative v71 columns." in set(
        current_boundaries["claim"]
    )
    assert "v72 proves column-generation convergence." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v72: Column-Generation Iteration 1" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v73_post_iteration_reprice_blocks_convergence() -> None:
    status = _read_json("paper4_v73_status.json")

    assert status["phase"] == "v73_reprice_after_column_generation_iteration_1"
    assert status["reprice_rows_v73"] == 1376398
    assert status["summary_rows_v73"] == 5
    assert status["dual_rows_v73"] == 880
    assert status["source_scope_rows_v73"] == 30
    assert status["claim_blocker_rows_v73"] == 3
    assert status["improving_columns_after_iteration_v73"] == 83328
    assert status["negative_reduced_cost_detected_v73"] is True
    assert status["source_scope_missing_ids_v73"] == 30
    assert status["post_iteration_reprice_performed_v73"] is True
    assert status["column_generation_termination_claim_allowed_v73"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v73"] is False
    assert status["paper1_promotion_allowed_v73"] is False
    assert status["paper4_working_champion_changed_v73"] is False
    assert status["paper4_final_promotion_created"] is False

    repriced = pd.read_parquet(TABLE_DIR / "paper4_v73_reprice_after_iteration_1.parquet")
    assert not repriced.empty
    assert {
        "policy_id",
        "regime_v73",
        "loan_id",
        "minimization_reduced_cost_v73",
        "return_improvement_signal_v73",
        "improving_column_v73",
        "post_iteration_reprice_v73",
        "claim_boundary_v73",
    }.issubset(repriced.columns)
    assert repriced["policy_id"].nunique() == 4
    assert (
        int(repriced["improving_column_v73"].sum())
        == status["improving_columns_after_iteration_v73"]
    )
    assert repriced["minimization_reduced_cost_v73"].min() < 0
    assert repriced["post_iteration_reprice_v73"].eq(1).all()
    assert repriced["claim_boundary_v73"].str.contains("termination allowed only").all()

    summary = _read_csv("paper4_v73_reprice_summary.csv")
    assert {
        "policy_id",
        "regime_v73",
        "omitted_rows_priced_v73",
        "improving_columns_v73",
        "negative_reduced_cost_detected_v73",
        "post_iteration_reprice_performed_v73",
        "column_generation_termination_claim_allowed_v73",
        "exact_full_universe_cvar_claim_allowed_v73",
    }.issubset(summary.columns)
    assert summary["omitted_rows_priced_v73"].sum() == status["reprice_rows_v73"]
    assert summary["improving_columns_v73"].sum() == status["improving_columns_after_iteration_v73"]
    assert summary["negative_reduced_cost_detected_v73"].astype(bool).all()
    assert summary["post_iteration_reprice_performed_v73"].astype(bool).all()
    assert not summary["column_generation_termination_claim_allowed_v73"].astype(bool).any()
    assert not summary["exact_full_universe_cvar_claim_allowed_v73"].astype(bool).any()

    duals = _read_csv("paper4_v73_restricted_master_duals.csv")
    assert {
        "policy_id",
        "regime_v73",
        "constraint_index_v73",
        "constraint_type_v73",
        "marginal_v73",
        "binding_v73",
        "post_iteration_reprice_v73",
        "claim_boundary_v73",
    }.issubset(duals.columns)
    assert {"budget_lower", "source_share", "cvar_path_excess"}.issubset(
        set(duals["constraint_type_v73"])
    )
    assert duals["marginal_v73"].abs().gt(0).any()
    assert duals["post_iteration_reprice_v73"].eq(1).all()
    assert duals["claim_boundary_v73"].str.contains("not full-universe certificate").all()

    source_scope = _read_csv("paper4_v73_source_scope_after_iteration.csv")
    assert {
        "policy_id",
        "regime_v73",
        "source_family",
        "missing_source_ids_v73",
        "source_constraint_scope_complete_v73",
        "claim_boundary_v73",
    }.issubset(source_scope.columns)
    assert source_scope["missing_source_ids_v73"].sum() == status["source_scope_missing_ids_v73"]
    assert not source_scope["source_constraint_scope_complete_v73"].astype(bool).all()

    blockers = _read_csv("paper4_v73_claim_blockers.csv")
    assert {"blocker_id_v73", "blocking_v73", "evidence_count_v73", "claim_boundary_v73"}.issubset(
        blockers.columns
    )
    assert blockers["blocking_v73"].astype(bool).all()
    assert {
        "negative_reduced_cost_columns_after_iteration_1",
        "source_scope_after_iteration_incomplete",
        "continuous_relaxation_not_whole_loan_milp",
    }.issubset(set(blockers["blocker_id_v73"]))

    claim_delta = _read_csv("paper4_v73_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v73_post_iteration_reprice_executed"]) is True
    assert bool(claim_map["v73_column_generation_converged"]) is False
    assert bool(claim_map["v73_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has v73 post-iteration re-pricing after v72 column generation." in set(
        current_boundaries["claim"]
    )
    assert "v73 proves column-generation convergence." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v73: Re-Price After Column-Generation Iteration 1" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v74_column_generation_iteration_2_requires_repricing() -> None:
    status = _read_json("paper4_v74_status.json")

    assert status["phase"] == "v74_column_generation_iteration_2"
    assert status["candidate_rows_v74"] == 83328
    assert status["frontier_rows_v74"] == 5
    assert status["successful_iteration_rows_v74"] == 5
    assert status["allocation_rows_v74"] == 1091
    assert status["scenario_rows_v74"] == 640
    assert status["active_constraint_rows_v74"] == 880
    assert status["comparison_rows_v74"] == 5
    assert status["best_delta_return_vs_v72_iteration_v74"] > 800
    assert status["v73_candidate_allocated_exposure_v74"] > 180000
    assert status["v71_previous_candidate_allocated_exposure_v74"] > 2700000
    assert status["reprice_after_iteration_performed_v74"] is False
    assert status["column_generation_termination_claim_allowed_v74"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v74"] is False
    assert status["paper1_promotion_allowed_v74"] is False
    assert status["paper4_working_champion_changed_v74"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = pd.read_parquet(TABLE_DIR / "paper4_v74_iteration_2_candidates.parquet")
    assert not candidates.empty
    assert {
        "policy_id",
        "regime_v73",
        "loan_id",
        "minimization_reduced_cost_v73",
        "return_improvement_signal_v73",
        "master_role_v69",
        "claim_boundary_v69",
    }.issubset(candidates.columns)
    assert len(candidates) == status["candidate_rows_v74"]
    assert candidates["minimization_reduced_cost_v73"].lt(0).all()
    assert set(candidates["master_role_v69"]) == {"v73_negative_reduced_cost_column"}
    assert candidates.groupby(["policy_id", "regime_v73"]).size().sum() == 83328

    frontier = _read_csv("paper4_v74_iteration_2_frontier.csv")
    assert {
        "policy_id",
        "regime_v74",
        "solver_success_v74",
        "negative_reduced_cost_candidates_added_v74",
        "delta_return_vs_v72_iteration_v74",
        "v73_candidate_allocated_exposure_v74",
        "v71_previous_candidate_allocated_exposure_v74",
        "reprice_after_iteration_performed_v74",
        "column_generation_termination_claim_allowed_v74",
        "exact_full_universe_cvar_claim_allowed_v74",
        "claim_boundary_v74",
    }.issubset(frontier.columns)
    assert frontier["solver_success_v74"].astype(bool).all()
    assert (
        frontier["negative_reduced_cost_candidates_added_v74"].sum() == status["candidate_rows_v74"]
    )
    assert frontier["delta_return_vs_v72_iteration_v74"].max() == pytest.approx(
        status["best_delta_return_vs_v72_iteration_v74"]
    )
    assert frontier["delta_return_vs_v72_iteration_v74"].max() > 800
    assert frontier["v73_candidate_allocated_exposure_v74"].sum() == pytest.approx(
        status["v73_candidate_allocated_exposure_v74"]
    )
    assert frontier["v73_candidate_allocated_exposure_v74"].sum() > 180000
    assert frontier["v71_previous_candidate_allocated_exposure_v74"].sum() > 2700000
    assert not frontier["reprice_after_iteration_performed_v74"].astype(bool).any()
    assert not frontier["column_generation_termination_claim_allowed_v74"].astype(bool).any()
    assert not frontier["exact_full_universe_cvar_claim_allowed_v74"].astype(bool).any()
    assert frontier["claim_boundary_v74"].str.contains("re-pricing still required").all()

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v74_iteration_2_allocations.parquet")
    assert not allocations.empty
    assert {
        "policy_id",
        "regime_v74",
        "loan_id",
        "master_role_v74",
        "allocation_fraction_v74",
        "allocated_exposure_v74",
        "claim_boundary_v74",
    }.issubset(allocations.columns)
    assert allocations["allocation_fraction_v74"].between(0, 1).all()
    assert {
        "v71_negative_reduced_cost_column",
        "v73_negative_reduced_cost_column",
    }.issubset(set(allocations["master_role_v74"]))
    assert allocations["claim_boundary_v74"].str.contains("re-pricing still required").all()

    scenarios = _read_csv("paper4_v74_iteration_2_scenario_losses.csv")
    assert {
        "policy_id",
        "regime_v74",
        "path_id",
        "scenario_loss_v74",
        "scenario_return_v74",
    }.issubset(scenarios.columns)
    assert scenarios.groupby(["policy_id", "regime_v74"])["path_id"].nunique().eq(128).all()

    active = _read_csv("paper4_v74_iteration_2_active_constraints.csv")
    assert {
        "policy_id",
        "regime_v74",
        "constraint_type_v74",
        "lhs_v74",
        "rhs_v74",
        "slack_v74",
        "binding_v74",
    }.issubset(active.columns)
    assert {"budget_lower", "source_share", "cvar_path_excess"}.issubset(
        set(active["constraint_type_v74"])
    )

    comparison = _read_csv("paper4_v74_iteration_2_comparison.csv")
    assert {
        "policy_id",
        "regime_v74",
        "delta_return_vs_v72_iteration_v74",
        "delta_cvar90_vs_v72_iteration_v74",
        "v73_candidate_allocated_exposure_v74",
        "column_generation_termination_claim_allowed_v74",
    }.issubset(comparison.columns)
    assert comparison["delta_return_vs_v72_iteration_v74"].max() > 800
    assert comparison["v73_candidate_allocated_exposure_v74"].sum() > 180000
    assert not comparison["column_generation_termination_claim_allowed_v74"].astype(bool).any()

    blockers = _read_csv("paper4_v74_claim_blockers.csv")
    assert {
        "blocker_id_v74",
        "blocking_v74",
        "required_next_artifact_v74",
        "claim_boundary_v74",
    }.issubset(blockers.columns)
    assert blockers["blocking_v74"].astype(bool).all()
    assert {
        "post_iteration_2_repricing_missing",
        "source_constraint_scope_needs_reaudit_after_iteration_2",
        "continuous_relaxation_not_whole_loan_milp",
    }.issubset(set(blockers["blocker_id_v74"]))

    claim_delta = _read_csv("paper4_v74_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v74_column_generation_iteration_2_completed"]) is True
    assert bool(claim_map["v74_column_generation_converged"]) is False
    assert bool(claim_map["v74_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v74 second column-generation iteration over negative v73 columns." in set(
        current_boundaries["claim"]
    )
    assert "v74 proves column-generation convergence." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v74: Column-Generation Iteration 2" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v75_post_iteration_2_reprice_blocks_convergence() -> None:
    status = _read_json("paper4_v75_status.json")
    v73_status = _read_json("paper4_v73_status.json")

    assert status["phase"] == "v75_reprice_after_column_generation_iteration_2"
    assert status["reprice_rows_v75"] == 1293070
    assert status["summary_rows_v75"] == 5
    assert status["dual_rows_v75"] == 880
    assert status["source_scope_rows_v75"] == 30
    assert status["claim_blocker_rows_v75"] == 3
    assert status["improving_columns_after_iteration_2_v75"] == 3897
    assert (
        status["improving_columns_after_iteration_2_v75"]
        < v73_status["improving_columns_after_iteration_v73"]
    )
    assert status["negative_reduced_cost_detected_v75"] is True
    assert status["source_scope_missing_ids_v75"] == 30
    assert status["post_iteration_reprice_performed_v75"] is True
    assert status["column_generation_termination_claim_allowed_v75"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v75"] is False
    assert status["paper1_promotion_allowed_v75"] is False
    assert status["paper4_working_champion_changed_v75"] is False
    assert status["paper4_final_promotion_created"] is False

    repriced = pd.read_parquet(TABLE_DIR / "paper4_v75_reprice_after_iteration_2.parquet")
    assert not repriced.empty
    assert {
        "policy_id",
        "regime_v75",
        "loan_id",
        "minimization_reduced_cost_v75",
        "return_improvement_signal_v75",
        "improving_column_v75",
        "post_iteration_reprice_v75",
        "claim_boundary_v75",
    }.issubset(repriced.columns)
    assert repriced["policy_id"].nunique() == 4
    assert (
        int(repriced["improving_column_v75"].sum())
        == status["improving_columns_after_iteration_2_v75"]
    )
    assert repriced["minimization_reduced_cost_v75"].min() < 0
    assert repriced["post_iteration_reprice_v75"].eq(1).all()
    assert repriced["claim_boundary_v75"].str.contains("termination allowed only").all()

    summary = _read_csv("paper4_v75_reprice_summary.csv")
    assert {
        "policy_id",
        "regime_v75",
        "omitted_rows_priced_v75",
        "improving_columns_v75",
        "negative_reduced_cost_detected_v75",
        "post_iteration_reprice_performed_v75",
        "column_generation_termination_claim_allowed_v75",
        "exact_full_universe_cvar_claim_allowed_v75",
    }.issubset(summary.columns)
    assert summary["omitted_rows_priced_v75"].sum() == status["reprice_rows_v75"]
    assert (
        summary["improving_columns_v75"].sum() == status["improving_columns_after_iteration_2_v75"]
    )
    assert summary["improving_columns_v75"].gt(0).sum() == 1
    assert summary["negative_reduced_cost_detected_v75"].astype(bool).sum() == 1
    assert summary["post_iteration_reprice_performed_v75"].astype(bool).all()
    assert not summary["column_generation_termination_claim_allowed_v75"].astype(bool).any()
    assert not summary["exact_full_universe_cvar_claim_allowed_v75"].astype(bool).any()

    duals = _read_csv("paper4_v75_restricted_master_duals.csv")
    assert {
        "policy_id",
        "regime_v75",
        "constraint_index_v75",
        "constraint_type_v75",
        "marginal_v75",
        "binding_v75",
        "post_iteration_reprice_v75",
        "claim_boundary_v75",
    }.issubset(duals.columns)
    assert {"budget_lower", "source_share", "cvar_path_excess"}.issubset(
        set(duals["constraint_type_v75"])
    )
    assert duals["marginal_v75"].abs().gt(0).any()
    assert duals["post_iteration_reprice_v75"].eq(1).all()
    assert duals["claim_boundary_v75"].str.contains("not full-universe certificate").all()

    source_scope = _read_csv("paper4_v75_source_scope_after_iteration_2.csv")
    assert {
        "policy_id",
        "regime_v75",
        "source_family",
        "missing_source_ids_v75",
        "source_constraint_scope_complete_v75",
        "claim_boundary_v75",
    }.issubset(source_scope.columns)
    assert source_scope["missing_source_ids_v75"].sum() == status["source_scope_missing_ids_v75"]
    assert not source_scope["source_constraint_scope_complete_v75"].astype(bool).all()

    blockers = _read_csv("paper4_v75_claim_blockers.csv")
    assert {"blocker_id_v75", "blocking_v75", "evidence_count_v75", "claim_boundary_v75"}.issubset(
        blockers.columns
    )
    assert blockers["blocking_v75"].astype(bool).all()
    assert {
        "negative_reduced_cost_columns_after_iteration_2",
        "source_scope_after_iteration_2_incomplete",
        "continuous_relaxation_not_whole_loan_milp",
    }.issubset(set(blockers["blocker_id_v75"]))

    claim_delta = _read_csv("paper4_v75_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v75_post_iteration_2_reprice_executed"]) is True
    assert bool(claim_map["v75_column_generation_converged"]) is False
    assert bool(claim_map["v75_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has v75 post-iteration-2 re-pricing after v74 column generation." in set(
        current_boundaries["claim"]
    )
    assert "v75 proves column-generation convergence." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v75: Re-Price After Column-Generation Iteration 2" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v76_column_generation_iteration_3_requires_repricing() -> None:
    status = _read_json("paper4_v76_status.json")

    assert status["phase"] == "v76_column_generation_iteration_3"
    assert status["candidate_rows_v76"] == 3897
    assert status["frontier_rows_v76"] == 1
    assert status["successful_iteration_rows_v76"] == 1
    assert status["allocation_rows_v76"] == 174
    assert status["scenario_rows_v76"] == 128
    assert status["active_constraint_rows_v76"] == 175
    assert status["comparison_rows_v76"] == 1
    assert abs(status["best_delta_return_vs_v74_iteration_v76"]) < 1e-6
    assert status["v75_candidate_allocated_exposure_v76"] == 0
    assert status["v73_previous_candidate_allocated_exposure_v76"] > 180000
    assert status["v71_previous_candidate_allocated_exposure_v76"] > 490000
    assert status["reprice_after_iteration_performed_v76"] is False
    assert status["column_generation_termination_claim_allowed_v76"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v76"] is False
    assert status["paper1_promotion_allowed_v76"] is False
    assert status["paper4_working_champion_changed_v76"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = pd.read_parquet(TABLE_DIR / "paper4_v76_iteration_3_candidates.parquet")
    assert not candidates.empty
    assert {
        "policy_id",
        "regime_v75",
        "loan_id",
        "minimization_reduced_cost_v75",
        "return_improvement_signal_v75",
        "master_role_v69",
        "claim_boundary_v69",
    }.issubset(candidates.columns)
    assert len(candidates) == status["candidate_rows_v76"]
    assert candidates["minimization_reduced_cost_v75"].lt(0).all()
    assert set(candidates["master_role_v69"]) == {"v75_negative_reduced_cost_column"}
    assert set(candidates["regime_v75"]) == {"incumbent_cvar_relaxed_source_lp"}

    frontier = _read_csv("paper4_v76_iteration_3_frontier.csv")
    assert {
        "policy_id",
        "regime_v76",
        "solver_success_v76",
        "negative_reduced_cost_candidates_added_v76",
        "delta_return_vs_v74_iteration_v76",
        "v75_candidate_allocated_exposure_v76",
        "v73_previous_candidate_allocated_exposure_v76",
        "v71_previous_candidate_allocated_exposure_v76",
        "reprice_after_iteration_performed_v76",
        "column_generation_termination_claim_allowed_v76",
        "exact_full_universe_cvar_claim_allowed_v76",
        "claim_boundary_v76",
    }.issubset(frontier.columns)
    assert frontier["solver_success_v76"].astype(bool).all()
    assert (
        frontier["negative_reduced_cost_candidates_added_v76"].sum() == status["candidate_rows_v76"]
    )
    assert frontier["delta_return_vs_v74_iteration_v76"].abs().max() < 1e-6
    assert frontier["v75_candidate_allocated_exposure_v76"].sum() == 0
    assert frontier["v73_previous_candidate_allocated_exposure_v76"].sum() > 180000
    assert frontier["v71_previous_candidate_allocated_exposure_v76"].sum() > 490000
    assert not frontier["reprice_after_iteration_performed_v76"].astype(bool).any()
    assert not frontier["column_generation_termination_claim_allowed_v76"].astype(bool).any()
    assert not frontier["exact_full_universe_cvar_claim_allowed_v76"].astype(bool).any()
    assert frontier["claim_boundary_v76"].str.contains("re-pricing still required").all()

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v76_iteration_3_allocations.parquet")
    assert not allocations.empty
    assert {
        "policy_id",
        "regime_v76",
        "loan_id",
        "master_role_v76",
        "allocation_fraction_v76",
        "allocated_exposure_v76",
        "claim_boundary_v76",
    }.issubset(allocations.columns)
    assert allocations["allocation_fraction_v76"].between(0, 1).all()
    assert "v75_negative_reduced_cost_column" not in set(allocations["master_role_v76"])
    assert {
        "v71_negative_reduced_cost_column",
        "v73_negative_reduced_cost_column",
    }.issubset(set(allocations["master_role_v76"]))
    assert allocations["claim_boundary_v76"].str.contains("re-pricing still required").all()

    scenarios = _read_csv("paper4_v76_iteration_3_scenario_losses.csv")
    assert {
        "policy_id",
        "regime_v76",
        "path_id",
        "scenario_loss_v76",
        "scenario_return_v76",
    }.issubset(scenarios.columns)
    assert scenarios.groupby(["policy_id", "regime_v76"])["path_id"].nunique().eq(128).all()

    active = _read_csv("paper4_v76_iteration_3_active_constraints.csv")
    assert {
        "policy_id",
        "regime_v76",
        "constraint_type_v76",
        "lhs_v76",
        "rhs_v76",
        "slack_v76",
        "binding_v76",
    }.issubset(active.columns)
    assert {"budget_lower", "source_share", "cvar_path_excess"}.issubset(
        set(active["constraint_type_v76"])
    )

    comparison = _read_csv("paper4_v76_iteration_3_comparison.csv")
    assert {
        "policy_id",
        "regime_v76",
        "delta_return_vs_v74_iteration_v76",
        "delta_cvar90_vs_v74_iteration_v76",
        "v75_candidate_allocated_exposure_v76",
        "column_generation_termination_claim_allowed_v76",
    }.issubset(comparison.columns)
    assert comparison["delta_return_vs_v74_iteration_v76"].abs().max() < 1e-6
    assert comparison["v75_candidate_allocated_exposure_v76"].sum() == 0
    assert not comparison["column_generation_termination_claim_allowed_v76"].astype(bool).any()

    blockers = _read_csv("paper4_v76_claim_blockers.csv")
    assert {
        "blocker_id_v76",
        "blocking_v76",
        "required_next_artifact_v76",
        "claim_boundary_v76",
    }.issubset(blockers.columns)
    assert blockers["blocking_v76"].astype(bool).all()
    assert {
        "post_iteration_3_repricing_missing",
        "source_constraint_scope_needs_reaudit_after_iteration_3",
        "continuous_relaxation_not_whole_loan_milp",
    }.issubset(set(blockers["blocker_id_v76"]))

    claim_delta = _read_csv("paper4_v76_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v76_column_generation_iteration_3_completed"]) is True
    assert bool(claim_map["v76_column_generation_converged"]) is False
    assert bool(claim_map["v76_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert (
        "Paper 4 has a v76 third column-generation iteration over remaining negative v75 columns."
        in set(current_boundaries["claim"])
    )
    assert "v76 proves column-generation convergence." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v76: Column-Generation Iteration 3" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v77_post_iteration_3_reprice_clears_pricing_only() -> None:
    status = _read_json("paper4_v77_status.json")

    assert status["phase"] == "v77_reprice_after_column_generation_iteration_3"
    assert status["reprice_rows_v77"] == 257954
    assert status["summary_rows_v77"] == 1
    assert status["dual_rows_v77"] == 175
    assert status["source_scope_rows_v77"] == 6
    assert status["claim_blocker_rows_v77"] == 3
    assert status["improving_columns_after_iteration_3_v77"] == 0
    assert status["negative_reduced_cost_detected_v77"] is False
    assert status["pricing_blocker_cleared_v77"] is True
    assert status["source_scope_missing_ids_v77"] == 7
    assert status["post_iteration_reprice_performed_v77"] is True
    assert status["column_generation_termination_claim_allowed_v77"] is False
    assert status["exact_full_universe_cvar_claim_allowed_v77"] is False
    assert status["paper1_promotion_allowed_v77"] is False
    assert status["paper4_working_champion_changed_v77"] is False
    assert status["paper4_final_promotion_created"] is False

    repriced = pd.read_parquet(TABLE_DIR / "paper4_v77_reprice_after_iteration_3.parquet")
    assert not repriced.empty
    assert {
        "policy_id",
        "regime_v77",
        "loan_id",
        "minimization_reduced_cost_v77",
        "return_improvement_signal_v77",
        "improving_column_v77",
        "post_iteration_reprice_v77",
        "claim_boundary_v77",
    }.issubset(repriced.columns)
    assert len(repriced) == status["reprice_rows_v77"]
    assert repriced["policy_id"].nunique() == 1
    assert repriced["regime_v77"].nunique() == 1
    assert int(repriced["improving_column_v77"].sum()) == 0
    assert repriced["minimization_reduced_cost_v77"].min() > 0
    assert repriced["post_iteration_reprice_v77"].eq(1).all()
    assert repriced["claim_boundary_v77"].str.contains("source and integrality blockers").all()

    summary = _read_csv("paper4_v77_reprice_summary.csv")
    assert {
        "policy_id",
        "regime_v77",
        "omitted_rows_priced_v77",
        "improving_columns_v77",
        "negative_reduced_cost_detected_v77",
        "post_iteration_reprice_performed_v77",
        "column_generation_termination_claim_allowed_v77",
        "exact_full_universe_cvar_claim_allowed_v77",
    }.issubset(summary.columns)
    assert summary["omitted_rows_priced_v77"].sum() == status["reprice_rows_v77"]
    assert summary["improving_columns_v77"].sum() == 0
    assert not summary["negative_reduced_cost_detected_v77"].astype(bool).any()
    assert summary["post_iteration_reprice_performed_v77"].astype(bool).all()
    assert not summary["column_generation_termination_claim_allowed_v77"].astype(bool).any()
    assert not summary["exact_full_universe_cvar_claim_allowed_v77"].astype(bool).any()

    duals = _read_csv("paper4_v77_restricted_master_duals.csv")
    assert {
        "policy_id",
        "regime_v77",
        "constraint_index_v77",
        "constraint_type_v77",
        "marginal_v77",
        "binding_v77",
        "post_iteration_reprice_v77",
        "claim_boundary_v77",
    }.issubset(duals.columns)
    assert {"budget_lower", "source_share", "cvar_path_excess"}.issubset(
        set(duals["constraint_type_v77"])
    )
    assert duals["marginal_v77"].abs().gt(0).any()
    assert duals["post_iteration_reprice_v77"].eq(1).all()
    assert duals["claim_boundary_v77"].str.contains("not full-universe certificate").all()

    source_scope = _read_csv("paper4_v77_source_scope_after_iteration_3.csv")
    assert {
        "policy_id",
        "regime_v77",
        "source_family",
        "missing_source_ids_v77",
        "source_constraint_scope_complete_v77",
        "claim_boundary_v77",
    }.issubset(source_scope.columns)
    assert source_scope["missing_source_ids_v77"].sum() == status["source_scope_missing_ids_v77"]
    assert not source_scope["source_constraint_scope_complete_v77"].astype(bool).all()

    blockers = _read_csv("paper4_v77_claim_blockers.csv")
    assert {"blocker_id_v77", "blocking_v77", "evidence_count_v77", "claim_boundary_v77"}.issubset(
        blockers.columns
    )
    blocker_map = dict(zip(blockers["blocker_id_v77"], blockers["blocking_v77"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v77"], blockers["evidence_count_v77"], strict=False)
    )
    assert bool(blocker_map["negative_reduced_cost_columns_after_iteration_3"]) is False
    assert int(evidence_map["negative_reduced_cost_columns_after_iteration_3"]) == 0
    assert bool(blocker_map["source_scope_after_iteration_3_incomplete"]) is True
    assert int(evidence_map["source_scope_after_iteration_3_incomplete"]) == 7
    assert bool(blocker_map["continuous_relaxation_not_whole_loan_milp"]) is True

    claim_delta = _read_csv("paper4_v77_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v77_post_iteration_3_reprice_executed"]) is True
    assert bool(claim_map["v77_pricing_blocker_cleared"]) is True
    assert bool(claim_map["v77_exact_full_universe_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has v77 post-iteration-3 re-pricing after v76 column generation." in set(
        current_boundaries["claim"]
    )
    assert "v77 proves exact full-universe CVaR optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v77: Re-Price After Column-Generation Iteration 3" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v78_source_scope_expanded_reprice_clears_source_scope_only() -> None:
    status = _read_json("paper4_v78_status.json")

    assert status["phase"] == "v78_source_scope_expanded_reprice"
    assert status["reprice_rows_v78"] == 257954
    assert status["summary_rows_v78"] == 1
    assert status["dual_rows_v78"] == 182
    assert status["source_scope_rows_v78"] == 6
    assert status["claim_blocker_rows_v78"] == 3
    assert status["improving_columns_v78"] == 0
    assert status["negative_reduced_cost_detected_v78"] is False
    assert status["pricing_blocker_cleared_v78"] is True
    assert status["missing_source_constraint_ids_v78"] == 0
    assert status["source_scope_blocker_cleared_v78"] is True
    assert status["exact_full_universe_cvar_claim_allowed_v78"] is False
    assert status["paper1_promotion_allowed_v78"] is False
    assert status["paper4_working_champion_changed_v78"] is False
    assert status["paper4_final_promotion_created"] is False

    repriced = pd.read_parquet(TABLE_DIR / "paper4_v78_source_scope_expanded_reprice.parquet")
    assert not repriced.empty
    assert {
        "policy_id",
        "regime_v78",
        "loan_id",
        "minimization_reduced_cost_v78",
        "return_improvement_signal_v78",
        "improving_column_v78",
        "source_scope_expanded_reprice_v78",
        "claim_boundary_v78",
    }.issubset(repriced.columns)
    assert len(repriced) == status["reprice_rows_v78"]
    assert int(repriced["improving_column_v78"].sum()) == 0
    assert repriced["minimization_reduced_cost_v78"].min() > 0
    assert repriced["source_scope_expanded_reprice_v78"].eq(1).all()
    assert repriced["claim_boundary_v78"].str.contains("integrality blocker remains").all()

    summary = _read_csv("paper4_v78_source_scope_expanded_summary.csv")
    assert {
        "policy_id",
        "regime_v78",
        "omitted_rows_priced_v78",
        "improving_columns_v78",
        "negative_reduced_cost_detected_v78",
        "source_scope_expanded_reprice_v78",
        "exact_full_universe_cvar_claim_allowed_v78",
    }.issubset(summary.columns)
    assert summary["omitted_rows_priced_v78"].sum() == status["reprice_rows_v78"]
    assert summary["improving_columns_v78"].sum() == 0
    assert not summary["negative_reduced_cost_detected_v78"].astype(bool).any()
    assert summary["source_scope_expanded_reprice_v78"].astype(bool).all()
    assert not summary["exact_full_universe_cvar_claim_allowed_v78"].astype(bool).any()

    duals = _read_csv("paper4_v78_source_scope_expanded_duals.csv")
    assert {
        "policy_id",
        "regime_v78",
        "constraint_index_v78",
        "constraint_type_v78",
        "marginal_v78",
        "source_scope_expanded_v78",
        "source_id_present_in_master_v78",
        "source_scope_expanded_reprice_v78",
        "claim_boundary_v78",
    }.issubset(duals.columns)
    assert {"budget_lower", "source_share", "cvar_path_excess"}.issubset(
        set(duals["constraint_type_v78"])
    )
    source_rows = duals.loc[duals["constraint_type_v78"].eq("source_share")]
    assert len(source_rows) == 51
    assert source_rows["source_scope_expanded_v78"].astype(bool).all()
    assert source_rows["source_id_present_in_master_v78"].eq(False).sum() == 7
    assert duals["marginal_v78"].abs().gt(0).any()
    assert duals["source_scope_expanded_reprice_v78"].eq(1).all()
    assert duals["claim_boundary_v78"].str.contains("not whole-loan certificate").all()

    source_scope = _read_csv("paper4_v78_source_scope_expanded_diagnostics.csv")
    assert {
        "policy_id",
        "regime_v78",
        "source_family",
        "universe_source_ids_v78",
        "constrained_source_ids_v78",
        "missing_source_constraint_ids_v78",
        "source_constraint_scope_complete_v78",
        "claim_boundary_v78",
    }.issubset(source_scope.columns)
    assert (
        source_scope["constrained_source_ids_v78"] == source_scope["universe_source_ids_v78"]
    ).all()
    assert source_scope["missing_source_constraint_ids_v78"].sum() == 0
    assert source_scope["source_constraint_scope_complete_v78"].astype(bool).all()

    blockers = _read_csv("paper4_v78_claim_blockers.csv")
    assert {"blocker_id_v78", "blocking_v78", "evidence_count_v78", "claim_boundary_v78"}.issubset(
        blockers.columns
    )
    blocker_map = dict(zip(blockers["blocker_id_v78"], blockers["blocking_v78"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v78"], blockers["evidence_count_v78"], strict=False)
    )
    assert bool(blocker_map["negative_reduced_cost_columns_after_full_source_scope"]) is False
    assert int(evidence_map["negative_reduced_cost_columns_after_full_source_scope"]) == 0
    assert bool(blocker_map["source_constraint_scope_incomplete"]) is False
    assert int(evidence_map["source_constraint_scope_incomplete"]) == 0
    assert bool(blocker_map["continuous_relaxation_not_whole_loan_milp"]) is True

    claim_delta = _read_csv("paper4_v78_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v78_full_source_scope_reprice_executed"]) is True
    assert bool(claim_map["v78_pricing_and_source_scope_cleared"]) is True
    assert bool(claim_map["v78_exact_full_universe_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has v78 full source-scope rows for the focused pricing check." in set(
        current_boundaries["claim"]
    )
    assert "v78 proves whole-loan full-universe optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v78: Source-Scope Expanded Re-Price" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v79_integrality_probe_is_support_only() -> None:
    status = _read_json("paper4_v79_status.json")

    assert status["phase"] == "v79_support_integrality_probe"
    assert status["summary_rows_v79"] == 2
    assert status["allocation_rows_v79"] == 174
    assert status["source_summary_rows_v79"] == 102
    assert status["claim_blocker_rows_v79"] == 3
    assert status["support_rows_v79"] == 174
    assert status["lp_fractional_rows_v79"] == 3
    assert status["milp_solver_success_v79"] is True
    assert status["milp_selected_rows_v79"] == 172
    assert status["milp_delta_return_vs_lp_v79"] == pytest.approx(-43.68935963552258)
    assert status["milp_delta_cvar90_vs_lp_v79"] == pytest.approx(277.15277429476555)
    assert status["milp_source_cap_violations_v79"] == 0
    assert status["whole_loan_full_universe_claim_allowed_v79"] is False
    assert status["paper1_promotion_allowed_v79"] is False
    assert status["paper4_working_champion_changed_v79"] is False
    assert status["paper4_final_promotion_created"] is False

    summary = _read_csv("paper4_v79_integrality_probe_summary.csv")
    assert {
        "portfolio_label_v79",
        "solver_success_v79",
        "support_rows_v79",
        "selected_rows_v79",
        "fractional_rows_v79",
        "portfolio_exposure_v79",
        "objective_return_v79",
        "scenario_loss_cvar90_v79",
        "delta_return_vs_lp_v79",
        "delta_cvar90_vs_lp_v79",
        "claim_boundary_v79",
    }.issubset(summary.columns)
    assert set(summary["portfolio_label_v79"]) == {
        "continuous_lp_reference",
        "support_binary_milp",
    }
    lp = summary.loc[summary["portfolio_label_v79"].eq("continuous_lp_reference")].iloc[0]
    milp_row = summary.loc[summary["portfolio_label_v79"].eq("support_binary_milp")].iloc[0]
    assert int(lp["fractional_rows_v79"]) == 3
    assert int(milp_row["fractional_rows_v79"]) == 0
    assert int(milp_row["selected_rows_v79"]) == status["milp_selected_rows_v79"]
    assert float(milp_row["portfolio_exposure_v79"]) == pytest.approx(845000.0)
    assert float(milp_row["delta_return_vs_lp_v79"]) == pytest.approx(
        status["milp_delta_return_vs_lp_v79"]
    )
    assert float(milp_row["delta_cvar90_vs_lp_v79"]) == pytest.approx(
        status["milp_delta_cvar90_vs_lp_v79"]
    )
    assert bool(milp_row["solver_success_v79"]) is True
    assert int(milp_row["source_cap_violations_v79"]) == 0
    assert summary["claim_boundary_v79"].str.contains("not full-pool or full-universe").all()

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v79_integrality_probe_allocations.parquet")
    assert not allocations.empty
    assert {
        "policy_id",
        "regime_v79",
        "loan_id",
        "allocation_fraction_v76",
        "support_binary_selected_v79",
        "support_binary_exposure_v79",
        "claim_boundary_v79",
    }.issubset(allocations.columns)
    assert set(allocations["support_binary_selected_v79"].unique()).issubset({0.0, 1.0})
    assert int(allocations["support_binary_selected_v79"].sum()) == status["milp_selected_rows_v79"]
    assert allocations["support_binary_exposure_v79"].sum() == pytest.approx(845000.0)
    assert (
        allocations.loc[allocations["support_binary_selected_v79"].eq(0), "loan_amnt"].sum() == 3500
    )
    assert allocations["claim_boundary_v79"].str.contains("support-restricted").all()

    source_summary = _read_csv("paper4_v79_integrality_probe_source_summary.csv")
    assert {
        "portfolio_label_v79",
        "source_family",
        "source_id",
        "cap_share_v79",
        "source_share_v79",
        "source_cap_violated_v79",
    }.issubset(source_summary.columns)
    assert set(source_summary["portfolio_label_v79"]) == {
        "continuous_lp_reference",
        "support_binary_milp",
    }
    assert not source_summary["source_cap_violated_v79"].astype(bool).any()

    blockers = _read_csv("paper4_v79_claim_blockers.csv")
    assert {"blocker_id_v79", "blocking_v79", "evidence_count_v79", "claim_boundary_v79"}.issubset(
        blockers.columns
    )
    blocker_map = dict(zip(blockers["blocker_id_v79"], blockers["blocking_v79"], strict=False))
    assert bool(blocker_map["support_integrality_gap_quantified"]) is False
    assert bool(blocker_map["full_pool_or_full_universe_milp_missing"]) is True
    assert bool(blocker_map["paper_estrella_or_final_promotion_not_allowed"]) is True

    claim_delta = _read_csv("paper4_v79_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v79_support_integrality_probe_executed"]) is True
    assert bool(claim_map["v79_whole_loan_full_universe_optimality"]) is False
    assert bool(claim_map["v79_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v79 support-restricted binary MILP integrality probe." in set(
        current_boundaries["claim"]
    )
    assert "v79 proves whole-loan full-universe optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v79: Support Integrality Probe" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v80_full_pool_milp_gap_probe_is_not_full_universe() -> None:
    status = _read_json("paper4_v80_status.json")

    assert status["phase"] == "v80_focused_full_pool_milp_gap_probe"
    assert status["summary_rows_v80"] == 2
    assert status["allocation_rows_v80"] == 18915
    assert status["source_summary_rows_v80"] == 102
    assert status["constraint_rows_v80"] == 181
    assert status["claim_blocker_rows_v80"] == 3
    assert status["pool_rows_v80"] == 18915
    assert status["lp_fractional_rows_v80"] == 3
    assert status["milp_solver_success_v80"] is True
    assert status["milp_incumbent_available_v80"] is True
    assert status["milp_status_v80"] == 0
    assert status["milp_gap_v80"] < 1e-5
    assert status["milp_node_count_v80"] > 0
    assert status["milp_selected_rows_v80"] == 171
    assert status["milp_delta_return_vs_lp_v80"] == pytest.approx(-28.726313016571112)
    assert status["milp_delta_cvar90_vs_lp_v80"] == pytest.approx(-35.06590165325906)
    assert status["milp_source_cap_violations_v80"] == 0
    assert status["whole_loan_full_universe_claim_allowed_v80"] is False
    assert status["paper1_promotion_allowed_v80"] is False
    assert status["paper4_working_champion_changed_v80"] is False
    assert status["paper4_final_promotion_created"] is False

    summary = _read_csv("paper4_v80_full_pool_milp_gap_summary.csv")
    assert {
        "portfolio_label_v80",
        "solver_success_v80",
        "pool_rows_v80",
        "selected_rows_v80",
        "fractional_rows_v80",
        "portfolio_exposure_v80",
        "objective_return_v80",
        "scenario_loss_cvar90_v80",
        "milp_gap_v80",
        "binary_variables_v80",
        "delta_return_vs_lp_v80",
        "delta_cvar90_vs_lp_v80",
        "claim_boundary_v80",
    }.issubset(summary.columns)
    assert set(summary["portfolio_label_v80"]) == {
        "continuous_lp_reference",
        "focused_full_pool_binary_milp",
    }
    lp = summary.loc[summary["portfolio_label_v80"].eq("continuous_lp_reference")].iloc[0]
    milp_row = summary.loc[summary["portfolio_label_v80"].eq("focused_full_pool_binary_milp")].iloc[
        0
    ]
    assert int(lp["fractional_rows_v80"]) == 3
    assert int(milp_row["fractional_rows_v80"]) == 0
    assert int(milp_row["pool_rows_v80"]) == status["pool_rows_v80"]
    assert int(milp_row["binary_variables_v80"]) == status["pool_rows_v80"]
    assert int(milp_row["selected_rows_v80"]) == status["milp_selected_rows_v80"]
    assert float(milp_row["portfolio_exposure_v80"]) == pytest.approx(842400.0)
    assert float(milp_row["milp_gap_v80"]) == pytest.approx(status["milp_gap_v80"])
    assert float(milp_row["delta_return_vs_lp_v80"]) == pytest.approx(
        status["milp_delta_return_vs_lp_v80"]
    )
    assert float(milp_row["delta_cvar90_vs_lp_v80"]) == pytest.approx(
        status["milp_delta_cvar90_vs_lp_v80"]
    )
    assert bool(milp_row["solver_success_v80"]) is True
    assert int(milp_row["source_cap_violations_v80"]) == 0
    assert summary["claim_boundary_v80"].str.contains("not full-universe").all()

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v80_full_pool_milp_gap_allocations.parquet")
    assert not allocations.empty
    assert {
        "policy_id",
        "regime_v80",
        "loan_id",
        "allocation_fraction_v76",
        "focused_full_pool_binary_selected_v80",
        "focused_full_pool_binary_exposure_v80",
        "claim_boundary_v80",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v80"]
    assert set(allocations["focused_full_pool_binary_selected_v80"].unique()).issubset({0.0, 1.0})
    assert (
        int(allocations["focused_full_pool_binary_selected_v80"].sum())
        == status["milp_selected_rows_v80"]
    )
    assert allocations["focused_full_pool_binary_exposure_v80"].sum() == pytest.approx(842400.0)
    selected_roles = set(
        allocations.loc[
            allocations["focused_full_pool_binary_selected_v80"].eq(1), "master_role_v69"
        ]
    )
    assert {
        "incumbent_v63_book",
        "v68_pricing_candidate",
        "v71_negative_reduced_cost_column",
        "v73_negative_reduced_cost_column",
    }.issubset(selected_roles)
    assert allocations["claim_boundary_v80"].str.contains("restricted-pool binary").all()

    source_summary = _read_csv("paper4_v80_full_pool_milp_gap_source_summary.csv")
    assert {
        "portfolio_label_v80",
        "source_family",
        "source_id",
        "cap_share_v80",
        "source_share_v80",
        "source_cap_violated_v80",
    }.issubset(source_summary.columns)
    assert set(source_summary["portfolio_label_v80"]) == {
        "continuous_lp_reference",
        "focused_full_pool_binary_milp",
    }
    assert not source_summary["source_cap_violated_v80"].astype(bool).any()

    constraints = _read_csv("paper4_v80_full_pool_milp_gap_constraints.csv")
    assert {"constraint_type_v80", "constraint_row_v80"}.issubset(constraints.columns)
    assert len(constraints) == status["constraint_rows_v80"]
    assert (constraints["constraint_type_v80"] == "source_share").sum() == 51
    assert (constraints["constraint_type_v80"] == "cvar_path_excess").sum() == 128
    assert (constraints["constraint_type_v80"] == "budget_range").sum() == 1

    blockers = _read_csv("paper4_v80_claim_blockers.csv")
    assert {"blocker_id_v80", "blocking_v80", "evidence_count_v80", "claim_boundary_v80"}.issubset(
        blockers.columns
    )
    blocker_map = dict(zip(blockers["blocker_id_v80"], blockers["blocking_v80"], strict=False))
    assert bool(blocker_map["focused_full_pool_milp_gap_recorded"]) is False
    assert bool(blocker_map["full_universe_integer_pricing_missing"]) is True
    assert bool(blocker_map["paper_estrella_or_final_promotion_not_allowed"]) is True

    claim_delta = _read_csv("paper4_v80_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v80_focused_full_pool_milp_gap_probe_executed"]) is True
    assert bool(claim_map["v80_whole_loan_full_universe_optimality"]) is False
    assert bool(claim_map["v80_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v80 focused full-pool binary MILP/gap probe." in set(
        current_boundaries["claim"]
    )
    assert "v80 proves whole-loan full-universe optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v80: Focused Full-Pool MILP Gap Probe" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v81_integer_single_add_screen_is_not_global_optimality() -> None:
    status = _read_json("paper4_v81_status.json")

    assert status["phase"] == "v81_integer_omitted_single_add_screen"
    assert status["screen_rows_v81"] == 257954
    assert status["summary_rows_v81"] == 1
    assert status["source_summary_rows_v81"] == 6
    assert status["top_candidate_rows_v81"] == 200
    assert status["claim_blocker_rows_v81"] == 3
    assert status["omitted_rows_screened_v81"] == 257954
    assert status["budget_feasible_rows_v81"] == 63528
    assert status["positive_return_rows_v81"] == 3
    assert status["source_feasible_rows_v81"] == 203707
    assert status["cvar_feasible_rows_v81"] == 49492
    assert status["single_add_feasible_rows_v81"] == 49492
    assert status["single_add_improving_rows_v81"] == 0
    assert status["best_single_add_return_delta_v81"] == pytest.approx(-92.29890441894531)
    assert status["best_any_omitted_return_delta_v81"] == pytest.approx(161.50875854492188)
    assert status["integer_single_add_screen_cleared_v81"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v81"] is False
    assert status["paper1_promotion_allowed_v81"] is False
    assert status["paper4_working_champion_changed_v81"] is False
    assert status["paper4_final_promotion_created"] is False

    screen = pd.read_parquet(TABLE_DIR / "paper4_v81_integer_omitted_single_add_screen.parquet")
    assert not screen.empty
    assert {
        "policy_id",
        "regime_v78",
        "loan_id",
        "loan_amnt",
        "mean_return_if_added_v81",
        "budget_add_feasible_v81",
        "source_add_feasible_v81",
        "cvar_add_feasible_v81",
        "single_add_feasible_v81",
        "single_add_improves_return_v81",
        "claim_boundary_v81",
    }.issubset(screen.columns)
    assert len(screen) == status["screen_rows_v81"]
    assert int(screen["budget_add_feasible_v81"].sum()) == status["budget_feasible_rows_v81"]
    assert int(screen["source_add_feasible_v81"].sum()) == status["source_feasible_rows_v81"]
    assert int(screen["cvar_add_feasible_v81"].sum()) == status["cvar_feasible_rows_v81"]
    assert int(screen["single_add_feasible_v81"].sum()) == status["single_add_feasible_rows_v81"]
    assert int(screen["single_add_improves_return_v81"].sum()) == 0
    assert int(screen["mean_return_if_added_v81"].gt(0).sum()) == status["positive_return_rows_v81"]
    positive = screen.loc[screen["mean_return_if_added_v81"].gt(0)]
    assert not positive.empty
    assert not positive["single_add_feasible_v81"].astype(bool).any()
    assert screen["claim_boundary_v81"].str.contains("not multi-swap or global proof").all()

    summary = _read_csv("paper4_v81_integer_omitted_single_add_summary.csv")
    assert {
        "omitted_rows_screened_v81",
        "budget_feasible_rows_v81",
        "positive_return_rows_v81",
        "source_feasible_rows_v81",
        "cvar_feasible_rows_v81",
        "single_add_feasible_rows_v81",
        "single_add_improving_rows_v81",
        "integer_single_add_screen_cleared_v81",
        "full_universe_integer_optimality_claim_allowed_v81",
        "claim_boundary_v81",
    }.issubset(summary.columns)
    row = summary.iloc[0]
    assert int(row["omitted_rows_screened_v81"]) == status["omitted_rows_screened_v81"]
    assert int(row["single_add_improving_rows_v81"]) == 0
    assert bool(row["integer_single_add_screen_cleared_v81"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v81"]) is False
    assert "multi-loan swaps" in str(row["claim_boundary_v81"])

    source_summary = _read_csv("paper4_v81_integer_omitted_single_add_source_summary.csv")
    assert {
        "source_family",
        "rows_checked_v81",
        "source_blocked_rows_v81",
        "source_feasible_rows_v81",
        "claim_boundary_v81",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v81"]
    assert source_summary["rows_checked_v81"].eq(status["screen_rows_v81"]).all()
    assert source_summary["source_blocked_rows_v81"].sum() > 0

    top_candidates = _read_csv("paper4_v81_integer_omitted_single_add_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v81"]
    assert not top_candidates["single_add_improves_return_v81"].astype(bool).any()
    assert top_candidates["single_add_feasible_v81"].astype(bool).any()

    blockers = _read_csv("paper4_v81_claim_blockers.csv")
    assert {"blocker_id_v81", "blocking_v81", "evidence_count_v81", "claim_boundary_v81"}.issubset(
        blockers.columns
    )
    blocker_map = dict(zip(blockers["blocker_id_v81"], blockers["blocking_v81"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v81"], blockers["evidence_count_v81"], strict=False)
    )
    assert bool(blocker_map["single_add_integer_improvement_found"]) is False
    assert int(evidence_map["single_add_integer_improvement_found"]) == 0
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v81_claim_matrix_delta.csv")
    assert {"claim_id", "allowed", "artifact", "boundary"}.issubset(claim_delta.columns)
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v81_single_add_integer_screen_executed"]) is True
    assert bool(claim_map["v81_single_add_integer_screen_cleared"]) is True
    assert bool(claim_map["v81_full_universe_integer_optimality"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v81 single-add integer screen over omitted v55 loans." in set(
        current_boundaries["claim"]
    )
    assert "v81 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v81: Integer Omitted Single-Add Screen" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v82_one_swap_probe_finds_repair_signals_not_optimality() -> None:
    status = _read_json("paper4_v82_status.json")

    assert status["phase"] == "v82_one_swap_integer_pricing_probe"
    assert status["summary_rows_v82"] == 1
    assert status["stage_summary_rows_v82"] == 6
    assert status["candidate_pair_rows_v82"] == 8126
    assert status["top_candidate_rows_v82"] == 200
    assert status["claim_blocker_rows_v82"] == 3
    assert status["selected_rows_v82"] == 171
    assert status["omitted_rows_v82"] == 257954
    assert status["total_pair_rows_screened_v82"] == 44110134
    assert status["return_improving_pair_rows_v82"] == 784725
    assert status["budget_return_feasible_pair_rows_v82"] == 596651
    assert status["source_prefilter_pair_rows_v82"] == 9636
    assert status["source_exact_pair_rows_v82"] == 8126
    assert status["cvar_feasible_pair_rows_v82"] == 8126
    assert status["one_swap_improving_rows_v82"] == 8126
    assert status["best_one_swap_return_delta_v82"] == pytest.approx(201.74349922313803)
    assert status["best_one_swap_cvar90_after_v82"] == pytest.approx(91625.73939095305)
    assert status["one_swap_local_optimality_cleared_v82"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v82"] is False
    assert status["paper1_promotion_allowed_v82"] is False
    assert status["paper4_working_champion_changed_v82"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v82_one_swap_integer_pricing_probe.csv")
    assert {
        "added_loan_id_v82",
        "dropped_loan_id_v82",
        "return_delta_v82",
        "objective_return_after_swap_v82",
        "exposure_after_swap_v82",
        "budget_swap_feasible_v82",
        "source_swap_feasible_v82",
        "source_cap_violations_after_swap_v82",
        "cvar90_after_swap_v82",
        "cvar_swap_feasible_v82",
        "one_swap_improves_return_v82",
        "claim_boundary_v82",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v82"]
    assert probe["return_delta_v82"].gt(0).all()
    assert probe["budget_swap_feasible_v82"].astype(bool).all()
    assert probe["source_swap_feasible_v82"].astype(bool).all()
    assert probe["cvar_swap_feasible_v82"].astype(bool).all()
    assert probe["one_swap_improves_return_v82"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v82"].sum()) == 0
    assert probe["return_delta_v82"].max() == pytest.approx(
        status["best_one_swap_return_delta_v82"]
    )
    assert probe["claim_boundary_v82"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v82_one_swap_integer_pricing_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v82"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v82"]) == "154722969"
    assert str(best["dropped_loan_id_v82"]) == "127135245"
    assert float(best["return_delta_v82"]) == pytest.approx(
        status["best_one_swap_return_delta_v82"]
    )
    assert bool(best["one_swap_improves_return_v82"]) is True

    summary = _read_csv("paper4_v82_one_swap_integer_pricing_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v82"]) == status["one_swap_improving_rows_v82"]
    assert bool(row["one_swap_local_optimality_cleared_v82"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v82"]) is False
    assert "feasible improving swaps" in str(row["claim_boundary_v82"])

    stage_summary = _read_csv("paper4_v82_one_swap_screen_stage_summary.csv")
    assert {"stage_v82", "pair_rows_v82", "claim_boundary_v82"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v82"], stage_summary["pair_rows_v82"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v82"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v82"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v82"]

    blockers = _read_csv("paper4_v82_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v82"], blockers["blocking_v82"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v82"], blockers["evidence_count_v82"], strict=False)
    )
    assert bool(blocker_map["one_swap_integer_improvement_found"]) is True
    assert int(evidence_map["one_swap_integer_improvement_found"]) == 8126
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v82_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v82_one_swap_integer_probe_executed"]) is True
    assert bool(claim_map["v82_feasible_improving_one_swaps_found"]) is True
    assert bool(claim_map["v82_v80_one_swap_local_optimality"]) is False
    assert bool(claim_map["v82_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v82_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v82 one-swap integer pricing probe over omitted v55 loans." in set(
        current_boundaries["claim"]
    )
    assert "v82 proves v80 is one-swap locally optimal." in set(current_boundaries["claim"])
    assert "v82 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v82: One-Swap Integer Pricing Probe" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v83_best_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v83_status.json")

    assert status["phase"] == "v83_best_one_swap_repair"
    assert status["allocation_rows_v83"] == 171
    assert status["summary_rows_v83"] == 1
    assert status["action_rows_v83"] == 1
    assert status["source_summary_rows_v83"] == 51
    assert status["claim_blocker_rows_v83"] == 4
    assert status["added_loan_id_v83"] == "154722969"
    assert status["dropped_loan_id_v83"] == "127135245"
    assert status["selected_rows_v83"] == 171
    assert status["portfolio_exposure_v83"] == pytest.approx(842400.0)
    assert status["objective_return_v83"] == pytest.approx(-3854.1520292167534)
    assert status["scenario_loss_cvar90_v83"] == pytest.approx(91625.73939095305)
    assert status["source_cap_violations_v83"] == 0
    assert status["delta_return_vs_v80_v83"] == pytest.approx(201.74350209599243)
    assert status["delta_cvar90_vs_v80_v83"] == pytest.approx(231.72304392619117)
    assert status["delta_exposure_vs_v80_v83"] == pytest.approx(0.0)
    assert status["budget_feasible_v83"] is True
    assert status["source_feasible_v83"] is True
    assert status["cvar_feasible_v83"] is True
    assert status["repair_candidate_feasible_v83"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v83"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v83"] is False
    assert status["paper1_promotion_allowed_v83"] is False
    assert status["paper4_working_champion_changed_v83"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v83_best_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v83",
        "selected_v83",
        "portfolio_label_v83",
        "repair_action_v83",
        "claim_boundary_v83",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v83"]
    assert int(allocations["selected_v83"].sum()) == status["selected_rows_v83"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v83"])
    assert "154722969" in set(allocations["loan_id"].astype(str))
    assert "127135245" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v83"]) == {
        "added_from_v82_best_swap",
        "kept_from_v80",
    }
    assert allocations["claim_boundary_v83"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v83_best_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v83"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v83"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v83"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v83"])

    action = _read_csv("paper4_v83_best_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v83"]) == status["added_loan_id_v83"]
    assert str(action_row["dropped_loan_id_v83"]) == status["dropped_loan_id_v83"]
    assert float(action_row["return_delta_v83"]) == pytest.approx(201.74349922313803)
    assert int(action_row["source_cap_violations_after_repair_v83"]) == 0

    source_summary = _read_csv("paper4_v83_best_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v83",
        "source_slack_v83",
        "source_cap_violated_v83",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v83"]
    assert not source_summary["source_cap_violated_v83"].astype(bool).any()

    blockers = _read_csv("paper4_v83_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v83"], blockers["blocking_v83"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v83"], blockers["evidence_count_v83"], strict=False)
    )
    assert bool(blocker_map["best_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["best_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v83_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v83_best_one_swap_repair_executed"]) is True
    assert bool(claim_map["v83_repair_candidate_feasible"]) is True
    assert bool(claim_map["v83_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v83_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v83_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v83 best one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v83 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v83 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v83: Best One-Swap Repair Candidate" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v84_post_repair_one_swap_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v84_status.json")

    assert status["phase"] == "v84_post_repair_one_swap_reprice"
    assert status["summary_rows_v84"] == 1
    assert status["stage_summary_rows_v84"] == 6
    assert status["candidate_pair_rows_v84"] == 8000
    assert status["top_candidate_rows_v84"] == 200
    assert status["claim_blocker_rows_v84"] == 3
    assert status["selected_rows_v84"] == 171
    assert status["candidate_add_rows_v84"] == 276698
    assert status["total_pair_rows_screened_v84"] == 47315358
    assert status["return_improving_pair_rows_v84"] == 2142058
    assert status["budget_return_feasible_pair_rows_v84"] == 1290982
    assert status["source_prefilter_pair_rows_v84"] == 9323
    assert status["source_exact_pair_rows_v84"] == 8000
    assert status["cvar_feasible_pair_rows_v84"] == 8000
    assert status["one_swap_improving_rows_v84"] == 8000
    assert status["best_one_swap_return_delta_v84"] == pytest.approx(198.2450405042807)
    assert status["best_one_swap_cvar90_after_v84"] == pytest.approx(91667.73069994824)
    assert status["post_repair_one_swap_local_optimality_cleared_v84"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v84"] is False
    assert status["paper1_promotion_allowed_v84"] is False
    assert status["paper4_working_champion_changed_v84"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v84_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v84",
        "dropped_loan_id_v84",
        "return_delta_v84",
        "objective_return_after_swap_v84",
        "budget_swap_feasible_v84",
        "source_swap_feasible_v84",
        "source_cap_violations_after_swap_v84",
        "cvar_swap_feasible_v84",
        "one_swap_improves_return_v84",
        "claim_boundary_v84",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v84"]
    assert probe["return_delta_v84"].gt(0).all()
    assert probe["budget_swap_feasible_v84"].astype(bool).all()
    assert probe["source_swap_feasible_v84"].astype(bool).all()
    assert probe["cvar_swap_feasible_v84"].astype(bool).all()
    assert probe["one_swap_improves_return_v84"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v84"].sum()) == 0
    assert probe["return_delta_v84"].max() == pytest.approx(
        status["best_one_swap_return_delta_v84"]
    )
    assert probe["claim_boundary_v84"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v84_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v84"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v84"]) == "143293919"
    assert str(best["dropped_loan_id_v84"]) == "127392383"
    assert float(best["return_delta_v84"]) == pytest.approx(
        status["best_one_swap_return_delta_v84"]
    )
    assert bool(best["one_swap_improves_return_v84"]) is True

    summary = _read_csv("paper4_v84_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v84"]) == status["one_swap_improving_rows_v84"]
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v84"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v84"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v84"])

    stage_summary = _read_csv("paper4_v84_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v84", "pair_rows_v84", "claim_boundary_v84"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v84"], stage_summary["pair_rows_v84"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v84"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v84"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v84"]

    blockers = _read_csv("paper4_v84_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v84"], blockers["blocking_v84"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v84"], blockers["evidence_count_v84"], strict=False)
    )
    assert bool(blocker_map["post_repair_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_repair_one_swap_improvement_found"]) == 8000
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v84_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v84_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v84_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v84_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v84_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v84 post-repair one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v84 proves the v83 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v84 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v84: Post-Repair One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v85_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v85_status.json")

    assert status["phase"] == "v85_next_one_swap_repair"
    assert status["allocation_rows_v85"] == 171
    assert status["summary_rows_v85"] == 1
    assert status["action_rows_v85"] == 1
    assert status["source_summary_rows_v85"] == 51
    assert status["claim_blocker_rows_v85"] == 4
    assert status["added_loan_id_v85"] == "143293919"
    assert status["dropped_loan_id_v85"] == "127392383"
    assert status["selected_rows_v85"] == 171
    assert status["portfolio_exposure_v85"] == pytest.approx(842400.0)
    assert status["objective_return_v85"] == pytest.approx(-3655.9069887124715)
    assert status["scenario_loss_cvar90_v85"] == pytest.approx(91667.73069994825)
    assert status["source_cap_violations_v85"] == 0
    assert status["delta_return_vs_v83_v85"] == pytest.approx(198.24504050428231)
    assert status["delta_cvar90_vs_v83_v85"] == pytest.approx(41.99130899521697)
    assert status["delta_exposure_vs_v83_v85"] == pytest.approx(0.0)
    assert status["budget_feasible_v85"] is True
    assert status["source_feasible_v85"] is True
    assert status["cvar_feasible_v85"] is True
    assert status["repair_candidate_feasible_v85"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v85"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v85"] is False
    assert status["paper1_promotion_allowed_v85"] is False
    assert status["paper4_working_champion_changed_v85"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v85_next_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v85",
        "selected_v85",
        "portfolio_label_v85",
        "repair_action_v85",
        "claim_boundary_v85",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v85"]
    assert int(allocations["selected_v85"].sum()) == status["selected_rows_v85"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v85"])
    assert "143293919" in set(allocations["loan_id"].astype(str))
    assert "127392383" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v85"]) == {
        "added_from_v84_best_swap",
        "kept_from_v83",
    }
    assert allocations["claim_boundary_v85"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v85_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v85"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v85"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v85"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v85"])

    action = _read_csv("paper4_v85_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v85"]) == status["added_loan_id_v85"]
    assert str(action_row["dropped_loan_id_v85"]) == status["dropped_loan_id_v85"]
    assert float(action_row["return_delta_v85"]) == pytest.approx(198.2450405042807)
    assert int(action_row["source_cap_violations_after_repair_v85"]) == 0

    source_summary = _read_csv("paper4_v85_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v85",
        "source_slack_v85",
        "source_cap_violated_v85",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v85"]
    assert not source_summary["source_cap_violated_v85"].astype(bool).any()

    blockers = _read_csv("paper4_v85_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v85"], blockers["blocking_v85"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v85"], blockers["evidence_count_v85"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v85_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v85_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v85_repair_candidate_feasible"]) is True
    assert bool(claim_map["v85_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v85_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v85_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v85 second one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v85 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v85 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v85: Second One-Swap Repair Candidate" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v86_post_v85_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v86_status.json")

    assert status["phase"] == "v86_post_v85_one_swap_reprice"
    assert status["summary_rows_v86"] == 1
    assert status["stage_summary_rows_v86"] == 6
    assert status["candidate_pair_rows_v86"] == 7847
    assert status["top_candidate_rows_v86"] == 200
    assert status["claim_blocker_rows_v86"] == 3
    assert status["selected_rows_v86"] == 171
    assert status["candidate_add_rows_v86"] == 276698
    assert status["total_pair_rows_screened_v86"] == 47315358
    assert status["return_improving_pair_rows_v86"] == 2127315
    assert status["budget_return_feasible_pair_rows_v86"] == 1283115
    assert status["source_prefilter_pair_rows_v86"] == 9142
    assert status["source_exact_pair_rows_v86"] == 7847
    assert status["cvar_feasible_pair_rows_v86"] == 7847
    assert status["one_swap_improving_rows_v86"] == 7847
    assert status["best_one_swap_return_delta_v86"] == pytest.approx(186.46858045165027)
    assert status["best_one_swap_cvar90_after_v86"] == pytest.approx(91778.02003085389)
    assert status["post_repair_one_swap_local_optimality_cleared_v86"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v86"] is False
    assert status["paper1_promotion_allowed_v86"] is False
    assert status["paper4_working_champion_changed_v86"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v86_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v86",
        "dropped_loan_id_v86",
        "return_delta_v86",
        "objective_return_after_swap_v86",
        "budget_swap_feasible_v86",
        "source_swap_feasible_v86",
        "source_cap_violations_after_swap_v86",
        "cvar_swap_feasible_v86",
        "one_swap_improves_return_v86",
        "claim_boundary_v86",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v86"]
    assert probe["return_delta_v86"].gt(0).all()
    assert probe["budget_swap_feasible_v86"].astype(bool).all()
    assert probe["source_swap_feasible_v86"].astype(bool).all()
    assert probe["cvar_swap_feasible_v86"].astype(bool).all()
    assert probe["one_swap_improves_return_v86"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v86"].sum()) == 0
    assert probe["return_delta_v86"].max() == pytest.approx(
        status["best_one_swap_return_delta_v86"]
    )
    assert probe["claim_boundary_v86"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v86_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v86"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v86"]) == "129036264"
    assert str(best["dropped_loan_id_v86"]) == "127726917"
    assert float(best["return_delta_v86"]) == pytest.approx(
        status["best_one_swap_return_delta_v86"]
    )
    assert bool(best["one_swap_improves_return_v86"]) is True

    summary = _read_csv("paper4_v86_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v86"]) == status["one_swap_improving_rows_v86"]
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v86"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v86"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v86"])

    stage_summary = _read_csv("paper4_v86_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v86", "pair_rows_v86", "claim_boundary_v86"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v86"], stage_summary["pair_rows_v86"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v86"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v86"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v86"]

    blockers = _read_csv("paper4_v86_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v86"], blockers["blocking_v86"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v86"], blockers["evidence_count_v86"], strict=False)
    )
    assert bool(blocker_map["post_v85_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v85_one_swap_improvement_found"]) == 7847
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v86_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v86_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v86_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v86_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v86_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v86 post-v85 one-swap pricing screen." in set(current_boundaries["claim"])
    assert "v86 proves the v85 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v86 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v86: Post-v85 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v87_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v87_status.json")

    assert status["phase"] == "v87_next_one_swap_repair"
    assert status["allocation_rows_v87"] == 171
    assert status["summary_rows_v87"] == 1
    assert status["action_rows_v87"] == 1
    assert status["source_summary_rows_v87"] == 51
    assert status["claim_blocker_rows_v87"] == 4
    assert status["added_loan_id_v87"] == "129036264"
    assert status["dropped_loan_id_v87"] == "127726917"
    assert status["selected_rows_v87"] == 171
    assert status["portfolio_exposure_v87"] == pytest.approx(842500.0)
    assert status["objective_return_v87"] == pytest.approx(-3469.4384082608176)
    assert status["scenario_loss_cvar90_v87"] == pytest.approx(91778.02003085389)
    assert status["source_cap_violations_v87"] == 0
    assert status["delta_return_vs_v85_v87"] == pytest.approx(186.46858045165436)
    assert status["delta_cvar90_vs_v85_v87"] == pytest.approx(110.2893309056526)
    assert status["delta_exposure_vs_v85_v87"] == pytest.approx(100.0)
    assert status["budget_feasible_v87"] is True
    assert status["source_feasible_v87"] is True
    assert status["cvar_feasible_v87"] is True
    assert status["repair_candidate_feasible_v87"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v87"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v87"] is False
    assert status["paper1_promotion_allowed_v87"] is False
    assert status["paper4_working_champion_changed_v87"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v87_next_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v87",
        "selected_v87",
        "portfolio_label_v87",
        "repair_action_v87",
        "claim_boundary_v87",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v87"]
    assert int(allocations["selected_v87"].sum()) == status["selected_rows_v87"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v87"])
    assert "129036264" in set(allocations["loan_id"].astype(str))
    assert "127726917" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v87"]) == {
        "added_from_v86_best_swap",
        "kept_from_v85",
    }
    assert allocations["claim_boundary_v87"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v87_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v87"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v87"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v87"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v87"])

    action = _read_csv("paper4_v87_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v87"]) == status["added_loan_id_v87"]
    assert str(action_row["dropped_loan_id_v87"]) == status["dropped_loan_id_v87"]
    assert float(action_row["return_delta_v87"]) == pytest.approx(186.46858045165027)
    assert int(action_row["source_cap_violations_after_repair_v87"]) == 0

    source_summary = _read_csv("paper4_v87_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v87",
        "source_slack_v87",
        "source_cap_violated_v87",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v87"]
    assert not source_summary["source_cap_violated_v87"].astype(bool).any()

    blockers = _read_csv("paper4_v87_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v87"], blockers["blocking_v87"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v87"], blockers["evidence_count_v87"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v87_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v87_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v87_repair_candidate_feasible"]) is True
    assert bool(claim_map["v87_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v87_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v87_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v87 third one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v87 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v87 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v87: Third One-Swap Repair Candidate" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v88_post_v87_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v88_status.json")

    assert status["phase"] == "v88_post_v87_one_swap_reprice"
    assert status["summary_rows_v88"] == 1
    assert status["stage_summary_rows_v88"] == 6
    assert status["candidate_pair_rows_v88"] == 9044
    assert status["top_candidate_rows_v88"] == 200
    assert status["claim_blocker_rows_v88"] == 3
    assert status["selected_rows_v88"] == 171
    assert status["candidate_add_rows_v88"] == 276698
    assert status["total_pair_rows_screened_v88"] == 47315358
    assert status["return_improving_pair_rows_v88"] == 2114323
    assert status["budget_return_feasible_pair_rows_v88"] == 1277960
    assert status["source_prefilter_pair_rows_v88"] == 11763
    assert status["source_exact_pair_rows_v88"] == 9044
    assert status["cvar_feasible_pair_rows_v88"] == 9044
    assert status["one_swap_improving_rows_v88"] == 9044
    assert status["best_one_swap_return_delta_v88"] == pytest.approx(193.93226645730118)
    assert status["best_one_swap_cvar90_after_v88"] == pytest.approx(91851.98990775931)
    assert status["post_repair_one_swap_local_optimality_cleared_v88"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v88"] is False
    assert status["paper1_promotion_allowed_v88"] is False
    assert status["paper4_working_champion_changed_v88"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v88_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v88",
        "dropped_loan_id_v88",
        "return_delta_v88",
        "objective_return_after_swap_v88",
        "budget_swap_feasible_v88",
        "source_swap_feasible_v88",
        "source_cap_violations_after_swap_v88",
        "cvar_swap_feasible_v88",
        "one_swap_improves_return_v88",
        "claim_boundary_v88",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v88"]
    assert probe["return_delta_v88"].gt(0).all()
    assert probe["budget_swap_feasible_v88"].astype(bool).all()
    assert probe["source_swap_feasible_v88"].astype(bool).all()
    assert probe["cvar_swap_feasible_v88"].astype(bool).all()
    assert probe["one_swap_improves_return_v88"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v88"].sum()) == 0
    assert probe["return_delta_v88"].max() == pytest.approx(
        status["best_one_swap_return_delta_v88"]
    )
    assert probe["claim_boundary_v88"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v88_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v88"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v88"]) == "165964183"
    assert str(best["dropped_loan_id_v88"]) == "126916681"
    assert float(best["return_delta_v88"]) == pytest.approx(
        status["best_one_swap_return_delta_v88"]
    )
    assert bool(best["one_swap_improves_return_v88"]) is True

    summary = _read_csv("paper4_v88_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v88"]) == status["one_swap_improving_rows_v88"]
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v88"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v88"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v88"])

    stage_summary = _read_csv("paper4_v88_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v88", "pair_rows_v88", "claim_boundary_v88"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v88"], stage_summary["pair_rows_v88"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v88"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v88"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v88"]

    blockers = _read_csv("paper4_v88_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v88"], blockers["blocking_v88"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v88"], blockers["evidence_count_v88"], strict=False)
    )
    assert bool(blocker_map["post_v87_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v87_one_swap_improvement_found"]) == 9044
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v88_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v88_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v88_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v88_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v88_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v88 post-v87 one-swap pricing screen." in set(current_boundaries["claim"])
    assert "v88 proves the v87 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v88 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v88: Post-v87 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v89_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v89_status.json")

    assert status["phase"] == "v89_next_one_swap_repair"
    assert status["allocation_rows_v89"] == 171
    assert status["summary_rows_v89"] == 1
    assert status["action_rows_v89"] == 1
    assert status["source_summary_rows_v89"] == 51
    assert status["claim_blocker_rows_v89"] == 4
    assert status["added_loan_id_v89"] == "165964183"
    assert status["dropped_loan_id_v89"] == "126916681"
    assert status["selected_rows_v89"] == 171
    assert status["portfolio_exposure_v89"] == pytest.approx(842450.0)
    assert status["objective_return_v89"] == pytest.approx(-3275.50614180352)
    assert status["scenario_loss_cvar90_v89"] == pytest.approx(91851.98990775933)
    assert status["source_cap_violations_v89"] == 0
    assert status["delta_return_vs_v87_v89"] == pytest.approx(193.93226645729737)
    assert status["delta_cvar90_vs_v87_v89"] == pytest.approx(73.96987690545211)
    assert status["delta_exposure_vs_v87_v89"] == pytest.approx(-50.0)
    assert status["budget_feasible_v89"] is True
    assert status["source_feasible_v89"] is True
    assert status["cvar_feasible_v89"] is True
    assert status["repair_candidate_feasible_v89"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v89"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v89"] is False
    assert status["paper1_promotion_allowed_v89"] is False
    assert status["paper4_working_champion_changed_v89"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v89_next_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v89",
        "selected_v89",
        "portfolio_label_v89",
        "repair_action_v89",
        "claim_boundary_v89",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v89"]
    assert int(allocations["selected_v89"].sum()) == status["selected_rows_v89"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v89"])
    assert "165964183" in set(allocations["loan_id"].astype(str))
    assert "126916681" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v89"]) == {
        "added_from_v88_best_swap",
        "kept_from_v87",
    }
    assert allocations["claim_boundary_v89"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v89_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v89"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v89"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v89"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v89"])

    action = _read_csv("paper4_v89_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v89"]) == status["added_loan_id_v89"]
    assert str(action_row["dropped_loan_id_v89"]) == status["dropped_loan_id_v89"]
    assert float(action_row["return_delta_v89"]) == pytest.approx(193.93226645730118)
    assert int(action_row["source_cap_violations_after_repair_v89"]) == 0

    source_summary = _read_csv("paper4_v89_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v89",
        "source_slack_v89",
        "source_cap_violated_v89",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v89"]
    assert not source_summary["source_cap_violated_v89"].astype(bool).any()

    blockers = _read_csv("paper4_v89_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v89"], blockers["blocking_v89"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v89"], blockers["evidence_count_v89"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v89_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v89_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v89_repair_candidate_feasible"]) is True
    assert bool(claim_map["v89_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v89_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v89_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v89 fourth one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v89 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v89 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v89: Fourth One-Swap Repair Candidate" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v90_post_v89_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v90_status.json")

    assert status["phase"] == "v90_post_v89_one_swap_reprice"
    assert status["summary_rows_v90"] == 1
    assert status["stage_summary_rows_v90"] == 6
    assert status["candidate_pair_rows_v90"] == 7594
    assert status["top_candidate_rows_v90"] == 200
    assert status["claim_blocker_rows_v90"] == 3
    assert status["selected_rows_v90"] == 171
    assert status["candidate_add_rows_v90"] == 276698
    assert status["total_pair_rows_screened_v90"] == 47315358
    assert status["return_improving_pair_rows_v90"] == 2099862
    assert status["budget_return_feasible_pair_rows_v90"] == 1263338
    assert status["source_prefilter_pair_rows_v90"] == 8738
    assert status["source_exact_pair_rows_v90"] == 7594
    assert status["cvar_feasible_pair_rows_v90"] == 7594
    assert status["one_swap_improving_rows_v90"] == 7594
    assert status["best_one_swap_return_delta_v90"] == pytest.approx(164.41628375317225)
    assert status["best_one_swap_cvar90_after_v90"] == pytest.approx(91931.29075279877)
    assert status["post_repair_one_swap_local_optimality_cleared_v90"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v90"] is False
    assert status["paper1_promotion_allowed_v90"] is False
    assert status["paper4_working_champion_changed_v90"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v90_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v90",
        "dropped_loan_id_v90",
        "return_delta_v90",
        "objective_return_after_swap_v90",
        "budget_swap_feasible_v90",
        "source_swap_feasible_v90",
        "source_cap_violations_after_swap_v90",
        "cvar_swap_feasible_v90",
        "one_swap_improves_return_v90",
        "claim_boundary_v90",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v90"]
    assert probe["return_delta_v90"].gt(0).all()
    assert probe["budget_swap_feasible_v90"].astype(bool).all()
    assert probe["source_swap_feasible_v90"].astype(bool).all()
    assert probe["cvar_swap_feasible_v90"].astype(bool).all()
    assert probe["one_swap_improves_return_v90"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v90"].sum()) == 0
    assert probe["return_delta_v90"].max() == pytest.approx(
        status["best_one_swap_return_delta_v90"]
    )
    assert probe["claim_boundary_v90"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v90_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v90"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v90"]) == "135930178"
    assert str(best["dropped_loan_id_v90"]) == "126739229"
    assert float(best["return_delta_v90"]) == pytest.approx(
        status["best_one_swap_return_delta_v90"]
    )
    assert bool(best["one_swap_improves_return_v90"]) is True

    summary = _read_csv("paper4_v90_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v90"]) == status["one_swap_improving_rows_v90"]
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v90"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v90"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v90"])

    stage_summary = _read_csv("paper4_v90_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v90", "pair_rows_v90", "claim_boundary_v90"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v90"], stage_summary["pair_rows_v90"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v90"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v90"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v90"]

    blockers = _read_csv("paper4_v90_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v90"], blockers["blocking_v90"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v90"], blockers["evidence_count_v90"], strict=False)
    )
    assert bool(blocker_map["post_v89_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v89_one_swap_improvement_found"]) == 7594
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v90_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v90_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v90_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v90_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v90_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v90 post-v89 one-swap pricing screen." in set(current_boundaries["claim"])
    assert "v90 proves the v89 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v90 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v90: Post-v89 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v91_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v91_status.json")

    assert status["phase"] == "v91_next_one_swap_repair"
    assert status["allocation_rows_v91"] == 171
    assert status["summary_rows_v91"] == 1
    assert status["action_rows_v91"] == 1
    assert status["source_summary_rows_v91"] == 51
    assert status["claim_blocker_rows_v91"] == 4
    assert status["added_loan_id_v91"] == "135930178"
    assert status["dropped_loan_id_v91"] == "126739229"
    assert status["selected_rows_v91"] == 171
    assert status["portfolio_exposure_v91"] == pytest.approx(842450.0)
    assert status["objective_return_v91"] == pytest.approx(-3111.089858050349)
    assert status["scenario_loss_cvar90_v91"] == pytest.approx(91931.29075279877)
    assert status["source_cap_violations_v91"] == 0
    assert status["delta_return_vs_v89_v91"] == pytest.approx(164.41628375317123)
    assert status["delta_cvar90_vs_v89_v91"] == pytest.approx(79.30084503945545)
    assert status["delta_exposure_vs_v89_v91"] == pytest.approx(0.0)
    assert status["budget_feasible_v91"] is True
    assert status["source_feasible_v91"] is True
    assert status["cvar_feasible_v91"] is True
    assert status["repair_candidate_feasible_v91"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v91"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v91"] is False
    assert status["paper1_promotion_allowed_v91"] is False
    assert status["paper4_working_champion_changed_v91"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v91_next_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v91",
        "selected_v91",
        "portfolio_label_v91",
        "repair_action_v91",
        "claim_boundary_v91",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v91"]
    assert int(allocations["selected_v91"].sum()) == status["selected_rows_v91"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v91"])
    assert "135930178" in set(allocations["loan_id"].astype(str))
    assert "126739229" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v91"]) == {
        "added_from_v90_best_swap",
        "kept_from_v89",
    }
    assert allocations["claim_boundary_v91"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v91_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v91"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v91"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v91"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v91"])

    action = _read_csv("paper4_v91_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v91"]) == status["added_loan_id_v91"]
    assert str(action_row["dropped_loan_id_v91"]) == status["dropped_loan_id_v91"]
    assert float(action_row["return_delta_v91"]) == pytest.approx(164.41628375317225)
    assert int(action_row["source_cap_violations_after_repair_v91"]) == 0

    source_summary = _read_csv("paper4_v91_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v91",
        "source_slack_v91",
        "source_cap_violated_v91",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v91"]
    assert not source_summary["source_cap_violated_v91"].astype(bool).any()

    blockers = _read_csv("paper4_v91_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v91"], blockers["blocking_v91"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v91"], blockers["evidence_count_v91"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v91_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v91_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v91_repair_candidate_feasible"]) is True
    assert bool(claim_map["v91_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v91_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v91_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v91 fifth one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v91 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v91 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v91: Fifth One-Swap Repair Candidate" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v92_post_v91_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v92_status.json")

    assert status["phase"] == "v92_post_v91_one_swap_reprice"
    assert status["summary_rows_v92"] == 1
    assert status["stage_summary_rows_v92"] == 6
    assert status["candidate_pair_rows_v92"] == 7563
    assert status["top_candidate_rows_v92"] == 200
    assert status["claim_blocker_rows_v92"] == 3
    assert status["selected_rows_v92"] == 171
    assert status["candidate_add_rows_v92"] == 276698
    assert status["total_pair_rows_screened_v92"] == 47315358
    assert status["return_improving_pair_rows_v92"] == 2088968
    assert status["budget_return_feasible_pair_rows_v92"] == 1257771
    assert status["source_prefilter_pair_rows_v92"] == 8693
    assert status["source_exact_pair_rows_v92"] == 7563
    assert status["cvar_feasible_pair_rows_v92"] == 7563
    assert status["one_swap_improving_rows_v92"] == 7563
    assert status["best_one_swap_return_delta_v92"] == pytest.approx(159.45366133732136)
    assert status["best_one_swap_cvar90_after_v92"] == pytest.approx(91819.71312940198)
    assert status["post_repair_one_swap_local_optimality_cleared_v92"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v92"] is False
    assert status["paper1_promotion_allowed_v92"] is False
    assert status["paper4_working_champion_changed_v92"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v92_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v92",
        "dropped_loan_id_v92",
        "return_delta_v92",
        "objective_return_after_swap_v92",
        "budget_swap_feasible_v92",
        "source_swap_feasible_v92",
        "source_cap_violations_after_swap_v92",
        "cvar_swap_feasible_v92",
        "one_swap_improves_return_v92",
        "claim_boundary_v92",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v92"]
    assert probe["return_delta_v92"].gt(0).all()
    assert probe["budget_swap_feasible_v92"].astype(bool).all()
    assert probe["source_swap_feasible_v92"].astype(bool).all()
    assert probe["cvar_swap_feasible_v92"].astype(bool).all()
    assert probe["one_swap_improves_return_v92"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v92"].sum()) == 0
    assert probe["return_delta_v92"].max() == pytest.approx(
        status["best_one_swap_return_delta_v92"]
    )
    assert probe["claim_boundary_v92"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v92_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v92"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v92"]) == "145248195"
    assert str(best["dropped_loan_id_v92"]) == "126782976"
    assert float(best["return_delta_v92"]) == pytest.approx(
        status["best_one_swap_return_delta_v92"]
    )
    assert bool(best["one_swap_improves_return_v92"]) is True

    summary = _read_csv("paper4_v92_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v92"]) == status["one_swap_improving_rows_v92"]
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v92"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v92"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v92"])

    stage_summary = _read_csv("paper4_v92_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v92", "pair_rows_v92", "claim_boundary_v92"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v92"], stage_summary["pair_rows_v92"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v92"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v92"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v92"]

    blockers = _read_csv("paper4_v92_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v92"], blockers["blocking_v92"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v92"], blockers["evidence_count_v92"], strict=False)
    )
    assert bool(blocker_map["post_v91_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v91_one_swap_improvement_found"]) == 7563
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v92_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v92_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v92_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v92_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v92_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v92 post-v91 one-swap pricing screen." in set(current_boundaries["claim"])
    assert "v92 proves the v91 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v92 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v92: Post-v91 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v93_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v93_status.json")

    assert status["phase"] == "v93_next_one_swap_repair"
    assert status["allocation_rows_v93"] == 171
    assert status["summary_rows_v93"] == 1
    assert status["action_rows_v93"] == 1
    assert status["source_summary_rows_v93"] == 51
    assert status["claim_blocker_rows_v93"] == 4
    assert status["added_loan_id_v93"] == "145248195"
    assert status["dropped_loan_id_v93"] == "126782976"
    assert status["selected_rows_v93"] == 171
    assert status["portfolio_exposure_v93"] == pytest.approx(842450.0)
    assert status["objective_return_v93"] == pytest.approx(-2951.636196713027)
    assert status["scenario_loss_cvar90_v93"] == pytest.approx(91819.71312940196)
    assert status["source_cap_violations_v93"] == 0
    assert status["delta_return_vs_v91_v93"] == pytest.approx(159.45366133732205)
    assert status["delta_cvar90_vs_v91_v93"] == pytest.approx(-111.57762339679175)
    assert status["delta_exposure_vs_v91_v93"] == pytest.approx(0.0)
    assert status["budget_feasible_v93"] is True
    assert status["source_feasible_v93"] is True
    assert status["cvar_feasible_v93"] is True
    assert status["repair_candidate_feasible_v93"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v93"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v93"] is False
    assert status["paper1_promotion_allowed_v93"] is False
    assert status["paper4_working_champion_changed_v93"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v93_next_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v93",
        "selected_v93",
        "portfolio_label_v93",
        "repair_action_v93",
        "claim_boundary_v93",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v93"]
    assert int(allocations["selected_v93"].sum()) == status["selected_rows_v93"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v93"])
    assert "145248195" in set(allocations["loan_id"].astype(str))
    assert "126782976" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v93"]) == {
        "added_from_v92_best_swap",
        "kept_from_v91",
    }
    assert allocations["claim_boundary_v93"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v93_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v93"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v93"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v93"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v93"])

    action = _read_csv("paper4_v93_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v93"]) == status["added_loan_id_v93"]
    assert str(action_row["dropped_loan_id_v93"]) == status["dropped_loan_id_v93"]
    assert float(action_row["return_delta_v93"]) == pytest.approx(159.45366133732136)
    assert int(action_row["source_cap_violations_after_repair_v93"]) == 0

    source_summary = _read_csv("paper4_v93_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v93",
        "source_slack_v93",
        "source_cap_violated_v93",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v93"]
    assert not source_summary["source_cap_violated_v93"].astype(bool).any()

    blockers = _read_csv("paper4_v93_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v93"], blockers["blocking_v93"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v93"], blockers["evidence_count_v93"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v93_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v93_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v93_repair_candidate_feasible"]) is True
    assert bool(claim_map["v93_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v93_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v93_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v93 sixth one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v93 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v93 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v93: Sixth One-Swap Repair Candidate" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v94_post_v93_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v94_status.json")

    assert status["phase"] == "v94_post_v93_one_swap_reprice"
    assert status["summary_rows_v94"] == 1
    assert status["stage_summary_rows_v94"] == 6
    assert status["candidate_pair_rows_v94"] == 7334
    assert status["top_candidate_rows_v94"] == 200
    assert status["claim_blocker_rows_v94"] == 3
    assert status["selected_rows_v94"] == 171
    assert status["candidate_add_rows_v94"] == 276698
    assert status["total_pair_rows_screened_v94"] == 47315358
    assert status["return_improving_pair_rows_v94"] == 2076873
    assert status["budget_return_feasible_pair_rows_v94"] == 1251997
    assert status["source_prefilter_pair_rows_v94"] == 8416
    assert status["source_exact_pair_rows_v94"] == 7334
    assert status["cvar_feasible_pair_rows_v94"] == 7334
    assert status["one_swap_improving_rows_v94"] == 7334
    assert status["best_one_swap_return_delta_v94"] == pytest.approx(157.0655454317594)
    assert status["best_one_swap_cvar90_after_v94"] == pytest.approx(92095.88635549336)
    assert status["post_repair_one_swap_local_optimality_cleared_v94"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v94"] is False
    assert status["paper1_promotion_allowed_v94"] is False
    assert status["paper4_working_champion_changed_v94"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v94_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v94",
        "dropped_loan_id_v94",
        "return_delta_v94",
        "objective_return_after_swap_v94",
        "budget_swap_feasible_v94",
        "source_swap_feasible_v94",
        "source_cap_violations_after_swap_v94",
        "cvar_swap_feasible_v94",
        "one_swap_improves_return_v94",
        "claim_boundary_v94",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v94"]
    assert probe["return_delta_v94"].gt(0).all()
    assert probe["budget_swap_feasible_v94"].astype(bool).all()
    assert probe["source_swap_feasible_v94"].astype(bool).all()
    assert probe["cvar_swap_feasible_v94"].astype(bool).all()
    assert probe["one_swap_improves_return_v94"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v94"].sum()) == 0
    assert probe["return_delta_v94"].max() == pytest.approx(
        status["best_one_swap_return_delta_v94"]
    )
    assert probe["claim_boundary_v94"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v94_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v94"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v94"]) == "152314563"
    assert str(best["dropped_loan_id_v94"]) == "127844421"
    assert float(best["return_delta_v94"]) == pytest.approx(
        status["best_one_swap_return_delta_v94"]
    )
    assert bool(best["one_swap_improves_return_v94"]) is True

    summary = _read_csv("paper4_v94_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v94"]) == status["one_swap_improving_rows_v94"]
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v94"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v94"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v94"])

    stage_summary = _read_csv("paper4_v94_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v94", "pair_rows_v94", "claim_boundary_v94"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v94"], stage_summary["pair_rows_v94"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v94"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v94"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v94"]

    blockers = _read_csv("paper4_v94_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v94"], blockers["blocking_v94"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v94"], blockers["evidence_count_v94"], strict=False)
    )
    assert bool(blocker_map["post_v93_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v93_one_swap_improvement_found"]) == 7334
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v94_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v94_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v94_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v94_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v94_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v94 post-v93 one-swap pricing screen." in set(current_boundaries["claim"])
    assert "v94 proves the v93 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v94 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v94: Post-v93 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v95_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v95_status.json")

    assert status["phase"] == "v95_next_one_swap_repair"
    assert status["allocation_rows_v95"] == 171
    assert status["summary_rows_v95"] == 1
    assert status["action_rows_v95"] == 1
    assert status["source_summary_rows_v95"] == 51
    assert status["claim_blocker_rows_v95"] == 4
    assert status["added_loan_id_v95"] == "152314563"
    assert status["dropped_loan_id_v95"] == "127844421"
    assert status["selected_rows_v95"] == 171
    assert status["portfolio_exposure_v95"] == pytest.approx(842450.0)
    assert status["objective_return_v95"] == pytest.approx(-2794.570651281263)
    assert status["scenario_loss_cvar90_v95"] == pytest.approx(92095.88635549332)
    assert status["source_cap_violations_v95"] == 0
    assert status["delta_return_vs_v93_v95"] == pytest.approx(157.0655454317639)
    assert status["delta_cvar90_vs_v93_v95"] == pytest.approx(276.1732260913559)
    assert status["delta_exposure_vs_v93_v95"] == pytest.approx(0.0)
    assert status["budget_feasible_v95"] is True
    assert status["source_feasible_v95"] is True
    assert status["cvar_feasible_v95"] is True
    assert status["repair_candidate_feasible_v95"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v95"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v95"] is False
    assert status["paper1_promotion_allowed_v95"] is False
    assert status["paper4_working_champion_changed_v95"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v95_next_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v95",
        "selected_v95",
        "portfolio_label_v95",
        "repair_action_v95",
        "claim_boundary_v95",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v95"]
    assert int(allocations["selected_v95"].sum()) == status["selected_rows_v95"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v95"])
    assert "152314563" in set(allocations["loan_id"].astype(str))
    assert "127844421" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v95"]) == {
        "added_from_v94_best_swap",
        "kept_from_v93",
    }
    assert allocations["claim_boundary_v95"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v95_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v95"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v95"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v95"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v95"])

    action = _read_csv("paper4_v95_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v95"]) == status["added_loan_id_v95"]
    assert str(action_row["dropped_loan_id_v95"]) == status["dropped_loan_id_v95"]
    assert float(action_row["return_delta_v95"]) == pytest.approx(157.0655454317594)
    assert int(action_row["source_cap_violations_after_repair_v95"]) == 0

    source_summary = _read_csv("paper4_v95_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v95",
        "source_slack_v95",
        "source_cap_violated_v95",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v95"]
    assert not source_summary["source_cap_violated_v95"].astype(bool).any()

    blockers = _read_csv("paper4_v95_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v95"], blockers["blocking_v95"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v95"], blockers["evidence_count_v95"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v95_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v95_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v95_repair_candidate_feasible"]) is True
    assert bool(claim_map["v95_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v95_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v95_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v95 seventh one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v95 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v95 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v95: Seventh One-Swap Repair Candidate" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v96_post_v95_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v96_status.json")

    assert status["phase"] == "v96_post_v95_one_swap_reprice"
    assert status["summary_rows_v96"] == 1
    assert status["stage_summary_rows_v96"] == 6
    assert status["candidate_pair_rows_v96"] == 7239
    assert status["top_candidate_rows_v96"] == 200
    assert status["claim_blocker_rows_v96"] == 3
    assert status["selected_rows_v96"] == 171
    assert status["candidate_add_rows_v96"] == 276698
    assert status["total_pair_rows_screened_v96"] == 47315358
    assert status["return_improving_pair_rows_v96"] == 2066025
    assert status["budget_return_feasible_pair_rows_v96"] == 1247288
    assert status["source_prefilter_pair_rows_v96"] == 8318
    assert status["source_exact_pair_rows_v96"] == 7239
    assert status["cvar_feasible_pair_rows_v96"] == 7239
    assert status["one_swap_improving_rows_v96"] == 7239
    assert status["best_one_swap_return_delta_v96"] == pytest.approx(152.74243610682214)
    assert status["best_one_swap_cvar90_after_v96"] == pytest.approx(92221.79683903579)
    assert status["post_repair_one_swap_local_optimality_cleared_v96"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v96"] is False
    assert status["paper1_promotion_allowed_v96"] is False
    assert status["paper4_working_champion_changed_v96"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v96_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v96",
        "dropped_loan_id_v96",
        "return_delta_v96",
        "objective_return_after_swap_v96",
        "budget_swap_feasible_v96",
        "source_swap_feasible_v96",
        "source_cap_violations_after_swap_v96",
        "cvar_swap_feasible_v96",
        "one_swap_improves_return_v96",
        "claim_boundary_v96",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v96"]
    assert probe["return_delta_v96"].gt(0).all()
    assert probe["budget_swap_feasible_v96"].astype(bool).all()
    assert probe["source_swap_feasible_v96"].astype(bool).all()
    assert probe["cvar_swap_feasible_v96"].astype(bool).all()
    assert probe["one_swap_improves_return_v96"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v96"].sum()) == 0
    assert probe["return_delta_v96"].max() == pytest.approx(
        status["best_one_swap_return_delta_v96"]
    )
    assert probe["claim_boundary_v96"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v96_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v96"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v96"]) == "142037168"
    assert str(best["dropped_loan_id_v96"]) == "126777878"
    assert float(best["return_delta_v96"]) == pytest.approx(
        status["best_one_swap_return_delta_v96"]
    )
    assert bool(best["one_swap_improves_return_v96"]) is True

    summary = _read_csv("paper4_v96_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v96"]) == status["one_swap_improving_rows_v96"]
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v96"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v96"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v96"])

    stage_summary = _read_csv("paper4_v96_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v96", "pair_rows_v96", "claim_boundary_v96"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v96"], stage_summary["pair_rows_v96"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v96"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v96"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v96"]

    blockers = _read_csv("paper4_v96_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v96"], blockers["blocking_v96"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v96"], blockers["evidence_count_v96"], strict=False)
    )
    assert bool(blocker_map["post_v95_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v95_one_swap_improvement_found"]) == 7239
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v96_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v96_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v96_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v96_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v96_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v96 post-v95 one-swap pricing screen." in set(current_boundaries["claim"])
    assert "v96 proves the v95 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v96 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v96: Post-v95 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v97_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v97_status.json")

    assert status["phase"] == "v97_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.97"
    assert status["allocation_rows_v97"] == 171
    assert status["summary_rows_v97"] == 1
    assert status["action_rows_v97"] == 1
    assert status["source_summary_rows_v97"] == 51
    assert status["claim_blocker_rows_v97"] == 4
    assert status["added_loan_id_v97"] == "142037168"
    assert status["dropped_loan_id_v97"] == "126777878"
    assert status["selected_rows_v97"] == 171
    assert status["portfolio_exposure_v97"] == pytest.approx(842450.0)
    assert status["objective_return_v97"] == pytest.approx(-2641.8282151744443)
    assert status["scenario_loss_cvar90_v97"] == pytest.approx(92221.79683903579)
    assert status["source_cap_violations_v97"] == 0
    assert status["delta_return_vs_v95_v97"] == pytest.approx(152.7424361068188)
    assert status["delta_cvar90_vs_v95_v97"] == pytest.approx(125.91048354246595)
    assert status["delta_exposure_vs_v95_v97"] == pytest.approx(0.0)
    assert status["budget_feasible_v97"] is True
    assert status["source_feasible_v97"] is True
    assert status["cvar_feasible_v97"] is True
    assert status["repair_candidate_feasible_v97"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v97"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v97"] is False
    assert status["paper1_promotion_allowed_v97"] is False
    assert status["paper4_working_champion_changed_v97"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v97_next_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v97",
        "selected_v97",
        "portfolio_label_v97",
        "repair_action_v97",
        "claim_boundary_v97",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v97"]
    assert int(allocations["selected_v97"].sum()) == status["selected_rows_v97"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v97"])
    assert "142037168" in set(allocations["loan_id"].astype(str))
    assert "126777878" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v97"]) == {
        "added_from_v96_best_swap",
        "kept_from_v95",
    }
    assert allocations["claim_boundary_v97"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v97_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v97"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v97"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v97"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v97"])

    action = _read_csv("paper4_v97_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v97"]) == status["added_loan_id_v97"]
    assert str(action_row["dropped_loan_id_v97"]) == status["dropped_loan_id_v97"]
    assert float(action_row["return_delta_v97"]) == pytest.approx(152.74243610682214)
    assert int(action_row["source_cap_violations_after_repair_v97"]) == 0

    source_summary = _read_csv("paper4_v97_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v97",
        "source_slack_v97",
        "source_cap_violated_v97",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v97"]
    assert not source_summary["source_cap_violated_v97"].astype(bool).any()

    blockers = _read_csv("paper4_v97_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v97"], blockers["blocking_v97"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v97"], blockers["evidence_count_v97"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v97_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v97_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v97_repair_candidate_feasible"]) is True
    assert bool(claim_map["v97_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v97_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v97_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v97 eighth one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v97 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v97 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v97: Eighth One-Swap Repair Candidate" in notebook
    assert "next required experiment is v98" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v98_post_v97_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v98_status.json")

    assert status["phase"] == "v98_post_v97_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.98"
    assert status["summary_rows_v98"] == 1
    assert status["stage_summary_rows_v98"] == 6
    assert status["candidate_pair_rows_v98"] == 6783
    assert status["top_candidate_rows_v98"] == 200
    assert status["claim_blocker_rows_v98"] == 3
    assert status["selected_rows_v98"] == 171
    assert status["candidate_add_rows_v98"] == 276698
    assert status["total_pair_rows_screened_v98"] == 47315358
    assert status["return_improving_pair_rows_v98"] == 2055524
    assert status["budget_return_feasible_pair_rows_v98"] == 1240100
    assert status["source_prefilter_pair_rows_v98"] == 7862
    assert status["source_exact_pair_rows_v98"] == 6783
    assert status["cvar_feasible_pair_rows_v98"] == 6783
    assert status["one_swap_improving_rows_v98"] == 6783
    assert status["best_one_swap_return_delta_v98"] == pytest.approx(146.68859776698605)
    assert status["best_one_swap_cvar90_after_v98"] == pytest.approx(92333.23436577116)
    assert status["post_repair_one_swap_local_optimality_cleared_v98"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v98"] is False
    assert status["paper1_promotion_allowed_v98"] is False
    assert status["paper4_working_champion_changed_v98"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v98_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v98",
        "dropped_loan_id_v98",
        "return_delta_v98",
        "objective_return_after_swap_v98",
        "budget_swap_feasible_v98",
        "source_swap_feasible_v98",
        "source_cap_violations_after_swap_v98",
        "cvar_swap_feasible_v98",
        "one_swap_improves_return_v98",
        "claim_boundary_v98",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v98"]
    assert probe["return_delta_v98"].gt(0).all()
    assert probe["budget_swap_feasible_v98"].astype(bool).all()
    assert probe["source_swap_feasible_v98"].astype(bool).all()
    assert probe["cvar_swap_feasible_v98"].astype(bool).all()
    assert probe["one_swap_improves_return_v98"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v98"].sum()) == 0
    assert probe["return_delta_v98"].max() == pytest.approx(
        status["best_one_swap_return_delta_v98"]
    )
    assert probe["claim_boundary_v98"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v98_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v98"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v98"]) == "128253688"
    assert str(best["dropped_loan_id_v98"]) == "127051964"
    assert float(best["return_delta_v98"]) == pytest.approx(
        status["best_one_swap_return_delta_v98"]
    )
    assert float(best["exposure_after_swap_v98"]) == pytest.approx(842650.0)
    assert bool(best["one_swap_improves_return_v98"]) is True

    summary = _read_csv("paper4_v98_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v98"]) == status["one_swap_improving_rows_v98"]
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v98"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v98"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v98"])

    stage_summary = _read_csv("paper4_v98_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v98", "pair_rows_v98", "claim_boundary_v98"}.issubset(stage_summary.columns)
    stage_map = dict(zip(stage_summary["stage_v98"], stage_summary["pair_rows_v98"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v98"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v98"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v98"]

    blockers = _read_csv("paper4_v98_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v98"], blockers["blocking_v98"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v98"], blockers["evidence_count_v98"], strict=False)
    )
    assert bool(blocker_map["post_v97_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v97_one_swap_improvement_found"]) == 6783
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v98_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v98_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v98_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v98_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v98_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v98 post-v97 one-swap pricing screen." in set(current_boundaries["claim"])
    assert "v98 proves the v97 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v98 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v98: Post-v97 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v99_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v99_status.json")

    assert status["phase"] == "v99_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.99"
    assert status["allocation_rows_v99"] == 171
    assert status["summary_rows_v99"] == 1
    assert status["action_rows_v99"] == 1
    assert status["source_summary_rows_v99"] == 51
    assert status["claim_blocker_rows_v99"] == 4
    assert status["added_loan_id_v99"] == "128253688"
    assert status["dropped_loan_id_v99"] == "127051964"
    assert status["selected_rows_v99"] == 171
    assert status["portfolio_exposure_v99"] == pytest.approx(842650.0)
    assert status["objective_return_v99"] == pytest.approx(-2495.13961740746)
    assert status["scenario_loss_cvar90_v99"] == pytest.approx(92333.23436577116)
    assert status["source_cap_violations_v99"] == 0
    assert status["delta_return_vs_v97_v99"] == pytest.approx(146.68859776698446)
    assert status["delta_cvar90_vs_v97_v99"] == pytest.approx(111.43752673536073)
    assert status["delta_exposure_vs_v97_v99"] == pytest.approx(200.0)
    assert status["budget_feasible_v99"] is True
    assert status["source_feasible_v99"] is True
    assert status["cvar_feasible_v99"] is True
    assert status["repair_candidate_feasible_v99"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v99"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v99"] is False
    assert status["paper1_promotion_allowed_v99"] is False
    assert status["paper4_working_champion_changed_v99"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v99_next_one_swap_repair_allocations.parquet")
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v99",
        "selected_v99",
        "portfolio_label_v99",
        "repair_action_v99",
        "claim_boundary_v99",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v99"]
    assert int(allocations["selected_v99"].sum()) == status["selected_rows_v99"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v99"])
    assert "128253688" in set(allocations["loan_id"].astype(str))
    assert "127051964" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v99"]) == {
        "added_from_v98_best_swap",
        "kept_from_v97",
    }
    assert allocations["claim_boundary_v99"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v99_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v99"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v99"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v99"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v99"])

    action = _read_csv("paper4_v99_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v99"]) == status["added_loan_id_v99"]
    assert str(action_row["dropped_loan_id_v99"]) == status["dropped_loan_id_v99"]
    assert float(action_row["return_delta_v99"]) == pytest.approx(146.68859776698605)
    assert int(action_row["source_cap_violations_after_repair_v99"]) == 0

    source_summary = _read_csv("paper4_v99_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v99",
        "source_slack_v99",
        "source_cap_violated_v99",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v99"]
    assert not source_summary["source_cap_violated_v99"].astype(bool).any()

    blockers = _read_csv("paper4_v99_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v99"], blockers["blocking_v99"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v99"], blockers["evidence_count_v99"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v99_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v99_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v99_repair_candidate_feasible"]) is True
    assert bool(claim_map["v99_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v99_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v99_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v99 ninth one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v99 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v99 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v99: Ninth One-Swap Repair Candidate" in notebook
    assert "next required experiment is v100" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v100_post_v99_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v100_status.json")

    assert status["phase"] == "v100_post_v99_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.100"
    assert status["summary_rows_v100"] == 1
    assert status["stage_summary_rows_v100"] == 6
    assert status["candidate_pair_rows_v100"] == 10946
    assert status["top_candidate_rows_v100"] == 200
    assert status["claim_blocker_rows_v100"] == 3
    assert status["selected_rows_v100"] == 171
    assert status["candidate_add_rows_v100"] == 276698
    assert status["total_pair_rows_screened_v100"] == 47315358
    assert status["return_improving_pair_rows_v100"] == 2046327
    assert status["budget_return_feasible_pair_rows_v100"] == 1234402
    assert status["source_prefilter_pair_rows_v100"] == 18046
    assert status["source_exact_pair_rows_v100"] == 10946
    assert status["cvar_feasible_pair_rows_v100"] == 10946
    assert status["one_swap_improving_rows_v100"] == 10946
    assert status["best_one_swap_return_delta_v100"] == pytest.approx(191.1881191206193)
    assert status["best_one_swap_cvar90_after_v100"] == pytest.approx(92311.43781816204)
    assert status["post_repair_one_swap_local_optimality_cleared_v100"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v100"] is False
    assert status["paper1_promotion_allowed_v100"] is False
    assert status["paper4_working_champion_changed_v100"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v100_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v100",
        "dropped_loan_id_v100",
        "return_delta_v100",
        "objective_return_after_swap_v100",
        "budget_swap_feasible_v100",
        "source_swap_feasible_v100",
        "source_cap_violations_after_swap_v100",
        "cvar_swap_feasible_v100",
        "one_swap_improves_return_v100",
        "claim_boundary_v100",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v100"]
    assert probe["return_delta_v100"].gt(0).all()
    assert probe["budget_swap_feasible_v100"].astype(bool).all()
    assert probe["source_swap_feasible_v100"].astype(bool).all()
    assert probe["cvar_swap_feasible_v100"].astype(bool).all()
    assert probe["one_swap_improves_return_v100"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v100"].sum()) == 0
    assert probe["return_delta_v100"].max() == pytest.approx(
        status["best_one_swap_return_delta_v100"]
    )
    assert probe["claim_boundary_v100"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v100_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v100"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v100"]) == "142000768"
    assert str(best["dropped_loan_id_v100"]) == "127061180"
    assert float(best["return_delta_v100"]) == pytest.approx(
        status["best_one_swap_return_delta_v100"]
    )
    assert float(best["exposure_after_swap_v100"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v100"]) is True

    summary = _read_csv("paper4_v100_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v100"]) == status["one_swap_improving_rows_v100"]
    assert float(row["current_exposure_v100"]) == pytest.approx(842650.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v100"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v100"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v100"])

    stage_summary = _read_csv("paper4_v100_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v100", "pair_rows_v100", "claim_boundary_v100"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v100"], stage_summary["pair_rows_v100"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v100"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v100"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v100"]

    blockers = _read_csv("paper4_v100_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v100"], blockers["blocking_v100"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v100"], blockers["evidence_count_v100"], strict=False)
    )
    assert bool(blocker_map["post_v99_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v99_one_swap_improvement_found"]) == 10946
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v100_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v100_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v100_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v100_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v100_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v100 post-v99 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v100 proves the v99 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v100 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v100: Post-v99 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v101_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v101_status.json")

    assert status["phase"] == "v101_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.101"
    assert status["allocation_rows_v101"] == 171
    assert status["summary_rows_v101"] == 1
    assert status["action_rows_v101"] == 1
    assert status["source_summary_rows_v101"] == 51
    assert status["claim_blocker_rows_v101"] == 4
    assert status["added_loan_id_v101"] == "142000768"
    assert status["dropped_loan_id_v101"] == "127061180"
    assert status["selected_rows_v101"] == 171
    assert status["portfolio_exposure_v101"] == pytest.approx(842450.0)
    assert status["objective_return_v101"] == pytest.approx(-2303.9514982868386)
    assert status["scenario_loss_cvar90_v101"] == pytest.approx(92311.43781816206)
    assert status["source_cap_violations_v101"] == 0
    assert status["delta_return_vs_v99_v101"] == pytest.approx(191.1881191206212)
    assert status["delta_cvar90_vs_v99_v101"] == pytest.approx(-21.79654760910489)
    assert status["delta_exposure_vs_v99_v101"] == pytest.approx(-200.0)
    assert status["budget_feasible_v101"] is True
    assert status["source_feasible_v101"] is True
    assert status["cvar_feasible_v101"] is True
    assert status["repair_candidate_feasible_v101"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v101"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v101"] is False
    assert status["paper1_promotion_allowed_v101"] is False
    assert status["paper4_working_champion_changed_v101"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v101_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v101",
        "selected_v101",
        "portfolio_label_v101",
        "repair_action_v101",
        "claim_boundary_v101",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v101"]
    assert int(allocations["selected_v101"].sum()) == status["selected_rows_v101"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v101"])
    assert "142000768" in set(allocations["loan_id"].astype(str))
    assert "127061180" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v101"]) == {
        "added_from_v100_best_swap",
        "kept_from_v99",
    }
    assert allocations["claim_boundary_v101"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v101_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v101"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v101"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v101"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v101"])

    action = _read_csv("paper4_v101_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v101"]) == status["added_loan_id_v101"]
    assert str(action_row["dropped_loan_id_v101"]) == status["dropped_loan_id_v101"]
    assert float(action_row["return_delta_v101"]) == pytest.approx(191.1881191206193)
    assert int(action_row["source_cap_violations_after_repair_v101"]) == 0

    source_summary = _read_csv("paper4_v101_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v101",
        "source_slack_v101",
        "source_cap_violated_v101",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v101"]
    assert not source_summary["source_cap_violated_v101"].astype(bool).any()

    blockers = _read_csv("paper4_v101_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v101"], blockers["blocking_v101"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v101"], blockers["evidence_count_v101"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v101_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v101_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v101_repair_candidate_feasible"]) is True
    assert bool(claim_map["v101_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v101_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v101_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v101 tenth one-swap repair candidate." in set(current_boundaries["claim"])
    assert "v101 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v101 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v101: Tenth One-Swap Repair Candidate" in notebook
    assert "v102 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v102_post_v101_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v102_status.json")

    assert status["phase"] == "v102_post_v101_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.102"
    assert status["summary_rows_v102"] == 1
    assert status["stage_summary_rows_v102"] == 6
    assert status["candidate_pair_rows_v102"] == 6481
    assert status["top_candidate_rows_v102"] == 200
    assert status["claim_blocker_rows_v102"] == 3
    assert status["selected_rows_v102"] == 171
    assert status["candidate_add_rows_v102"] == 276698
    assert status["total_pair_rows_screened_v102"] == 47315358
    assert status["return_improving_pair_rows_v102"] == 2032532
    assert status["budget_return_feasible_pair_rows_v102"] == 1229892
    assert status["source_prefilter_pair_rows_v102"] == 7612
    assert status["source_exact_pair_rows_v102"] == 6481
    assert status["cvar_feasible_pair_rows_v102"] == 6481
    assert status["one_swap_improving_rows_v102"] == 6481
    assert status["best_one_swap_return_delta_v102"] == pytest.approx(146.6809005878472)
    assert status["best_one_swap_cvar90_after_v102"] == pytest.approx(92438.43605767138)
    assert status["post_repair_one_swap_local_optimality_cleared_v102"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v102"] is False
    assert status["paper1_promotion_allowed_v102"] is False
    assert status["paper4_working_champion_changed_v102"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v102_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v102",
        "dropped_loan_id_v102",
        "return_delta_v102",
        "objective_return_after_swap_v102",
        "budget_swap_feasible_v102",
        "source_swap_feasible_v102",
        "source_cap_violations_after_swap_v102",
        "cvar_swap_feasible_v102",
        "one_swap_improves_return_v102",
        "claim_boundary_v102",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v102"]
    assert probe["return_delta_v102"].gt(0).all()
    assert probe["budget_swap_feasible_v102"].astype(bool).all()
    assert probe["source_swap_feasible_v102"].astype(bool).all()
    assert probe["cvar_swap_feasible_v102"].astype(bool).all()
    assert probe["one_swap_improves_return_v102"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v102"].sum()) == 0
    assert probe["return_delta_v102"].max() == pytest.approx(
        status["best_one_swap_return_delta_v102"]
    )
    assert probe["claim_boundary_v102"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v102_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v102"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v102"]) == "159720541"
    assert str(best["dropped_loan_id_v102"]) == "128000272"
    assert float(best["return_delta_v102"]) == pytest.approx(
        status["best_one_swap_return_delta_v102"]
    )
    assert float(best["exposure_after_swap_v102"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v102"]) is True

    summary = _read_csv("paper4_v102_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v102"]) == status["one_swap_improving_rows_v102"]
    assert float(row["current_exposure_v102"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v102"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v102"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v102"])

    stage_summary = _read_csv("paper4_v102_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v102", "pair_rows_v102", "claim_boundary_v102"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v102"], stage_summary["pair_rows_v102"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v102"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v102"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v102"]

    blockers = _read_csv("paper4_v102_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v102"], blockers["blocking_v102"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v102"], blockers["evidence_count_v102"], strict=False)
    )
    assert bool(blocker_map["post_v101_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v101_one_swap_improvement_found"]) == 6481
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v102_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v102_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v102_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v102_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v102_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v102 post-v101 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v102 proves the v101 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v102 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v102: Post-v101 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v103_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v103_status.json")

    assert status["phase"] == "v103_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.103"
    assert status["allocation_rows_v103"] == 171
    assert status["summary_rows_v103"] == 1
    assert status["action_rows_v103"] == 1
    assert status["source_summary_rows_v103"] == 51
    assert status["claim_blocker_rows_v103"] == 4
    assert status["added_loan_id_v103"] == "159720541"
    assert status["dropped_loan_id_v103"] == "128000272"
    assert status["selected_rows_v103"] == 171
    assert status["portfolio_exposure_v103"] == pytest.approx(842450.0)
    assert status["objective_return_v103"] == pytest.approx(-2157.2705976989946)
    assert status["scenario_loss_cvar90_v103"] == pytest.approx(92438.43605767141)
    assert status["source_cap_violations_v103"] == 0
    assert status["delta_return_vs_v101_v103"] == pytest.approx(146.680900587844)
    assert status["delta_cvar90_vs_v101_v103"] == pytest.approx(126.99823950935388)
    assert status["delta_exposure_vs_v101_v103"] == pytest.approx(0.0)
    assert status["budget_feasible_v103"] is True
    assert status["source_feasible_v103"] is True
    assert status["cvar_feasible_v103"] is True
    assert status["repair_candidate_feasible_v103"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v103"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v103"] is False
    assert status["paper1_promotion_allowed_v103"] is False
    assert status["paper4_working_champion_changed_v103"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v103_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v103",
        "selected_v103",
        "portfolio_label_v103",
        "repair_action_v103",
        "claim_boundary_v103",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v103"]
    assert int(allocations["selected_v103"].sum()) == status["selected_rows_v103"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v103"])
    assert "159720541" in set(allocations["loan_id"].astype(str))
    assert "128000272" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v103"]) == {
        "added_from_v102_best_swap",
        "kept_from_v101",
    }
    assert allocations["claim_boundary_v103"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v103_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v103"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v103"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v103"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v103"])

    action = _read_csv("paper4_v103_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v103"]) == status["added_loan_id_v103"]
    assert str(action_row["dropped_loan_id_v103"]) == status["dropped_loan_id_v103"]
    assert float(action_row["return_delta_v103"]) == pytest.approx(146.6809005878472)
    assert int(action_row["source_cap_violations_after_repair_v103"]) == 0

    source_summary = _read_csv("paper4_v103_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v103",
        "source_slack_v103",
        "source_cap_violated_v103",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v103"]
    assert not source_summary["source_cap_violated_v103"].astype(bool).any()

    blockers = _read_csv("paper4_v103_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v103"], blockers["blocking_v103"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v103"], blockers["evidence_count_v103"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v103_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v103_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v103_repair_candidate_feasible"]) is True
    assert bool(claim_map["v103_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v103_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v103_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v103 eleventh one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v103 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v103 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v103: Eleventh One-Swap Repair Candidate" in notebook
    assert "v104 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v104_post_v103_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v104_status.json")

    assert status["phase"] == "v104_post_v103_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.104"
    assert status["summary_rows_v104"] == 1
    assert status["stage_summary_rows_v104"] == 6
    assert status["candidate_pair_rows_v104"] == 6398
    assert status["top_candidate_rows_v104"] == 200
    assert status["claim_blocker_rows_v104"] == 3
    assert status["selected_rows_v104"] == 171
    assert status["candidate_add_rows_v104"] == 276698
    assert status["total_pair_rows_screened_v104"] == 47315358
    assert status["return_improving_pair_rows_v104"] == 2022487
    assert status["budget_return_feasible_pair_rows_v104"] == 1225519
    assert status["source_prefilter_pair_rows_v104"] == 7524
    assert status["source_exact_pair_rows_v104"] == 6398
    assert status["cvar_feasible_pair_rows_v104"] == 6398
    assert status["one_swap_improving_rows_v104"] == 6398
    assert status["best_one_swap_return_delta_v104"] == pytest.approx(145.24803479210811)
    assert status["best_one_swap_cvar90_after_v104"] == pytest.approx(92582.02702682154)
    assert status["post_repair_one_swap_local_optimality_cleared_v104"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v104"] is False
    assert status["paper1_promotion_allowed_v104"] is False
    assert status["paper4_working_champion_changed_v104"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v104_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v104",
        "dropped_loan_id_v104",
        "return_delta_v104",
        "objective_return_after_swap_v104",
        "budget_swap_feasible_v104",
        "source_swap_feasible_v104",
        "source_cap_violations_after_swap_v104",
        "cvar_swap_feasible_v104",
        "one_swap_improves_return_v104",
        "claim_boundary_v104",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v104"]
    assert probe["return_delta_v104"].gt(0).all()
    assert probe["budget_swap_feasible_v104"].astype(bool).all()
    assert probe["source_swap_feasible_v104"].astype(bool).all()
    assert probe["cvar_swap_feasible_v104"].astype(bool).all()
    assert probe["one_swap_improves_return_v104"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v104"].sum()) == 0
    assert probe["return_delta_v104"].max() == pytest.approx(
        status["best_one_swap_return_delta_v104"]
    )
    assert probe["claim_boundary_v104"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v104_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v104"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v104"]) == "156935428"
    assert str(best["dropped_loan_id_v104"]) == "126821354"
    assert float(best["return_delta_v104"]) == pytest.approx(
        status["best_one_swap_return_delta_v104"]
    )
    assert float(best["exposure_after_swap_v104"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v104"]) is True

    summary = _read_csv("paper4_v104_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v104"]) == status["one_swap_improving_rows_v104"]
    assert float(row["current_exposure_v104"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v104"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v104"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v104"])

    stage_summary = _read_csv("paper4_v104_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v104", "pair_rows_v104", "claim_boundary_v104"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v104"], stage_summary["pair_rows_v104"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v104"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v104"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v104"]

    blockers = _read_csv("paper4_v104_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v104"], blockers["blocking_v104"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v104"], blockers["evidence_count_v104"], strict=False)
    )
    assert bool(blocker_map["post_v103_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v103_one_swap_improvement_found"]) == 6398
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v104_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v104_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v104_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v104_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v104_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v104 post-v103 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v104 proves the v103 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v104 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v104: Post-v103 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v105_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v105_status.json")

    assert status["phase"] == "v105_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.105"
    assert status["allocation_rows_v105"] == 171
    assert status["summary_rows_v105"] == 1
    assert status["action_rows_v105"] == 1
    assert status["source_summary_rows_v105"] == 51
    assert status["claim_blocker_rows_v105"] == 4
    assert status["added_loan_id_v105"] == "156935428"
    assert status["dropped_loan_id_v105"] == "126821354"
    assert status["selected_rows_v105"] == 171
    assert status["portfolio_exposure_v105"] == pytest.approx(842450.0)
    assert status["objective_return_v105"] == pytest.approx(-2012.022562906881)
    assert status["scenario_loss_cvar90_v105"] == pytest.approx(92582.02702682154)
    assert status["source_cap_violations_v105"] == 0
    assert status["delta_return_vs_v103_v105"] == pytest.approx(145.24803479211369)
    assert status["delta_cvar90_vs_v103_v105"] == pytest.approx(143.5909691501438)
    assert status["delta_exposure_vs_v103_v105"] == pytest.approx(0.0)
    assert status["budget_feasible_v105"] is True
    assert status["source_feasible_v105"] is True
    assert status["cvar_feasible_v105"] is True
    assert status["repair_candidate_feasible_v105"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v105"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v105"] is False
    assert status["paper1_promotion_allowed_v105"] is False
    assert status["paper4_working_champion_changed_v105"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v105_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v105",
        "selected_v105",
        "portfolio_label_v105",
        "repair_action_v105",
        "claim_boundary_v105",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v105"]
    assert int(allocations["selected_v105"].sum()) == status["selected_rows_v105"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v105"])
    assert "156935428" in set(allocations["loan_id"].astype(str))
    assert "126821354" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v105"]) == {
        "added_from_v104_best_swap",
        "kept_from_v103",
    }
    assert allocations["claim_boundary_v105"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v105_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v105"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v105"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v105"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v105"])

    action = _read_csv("paper4_v105_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v105"]) == status["added_loan_id_v105"]
    assert str(action_row["dropped_loan_id_v105"]) == status["dropped_loan_id_v105"]
    assert float(action_row["return_delta_v105"]) == pytest.approx(145.24803479210811)
    assert int(action_row["source_cap_violations_after_repair_v105"]) == 0

    source_summary = _read_csv("paper4_v105_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v105",
        "source_slack_v105",
        "source_cap_violated_v105",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v105"]
    assert not source_summary["source_cap_violated_v105"].astype(bool).any()

    blockers = _read_csv("paper4_v105_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v105"], blockers["blocking_v105"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v105"], blockers["evidence_count_v105"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v105_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v105_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v105_repair_candidate_feasible"]) is True
    assert bool(claim_map["v105_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v105_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v105_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v105 twelfth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v105 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v105 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v105: Twelfth One-Swap Repair Candidate" in notebook
    assert "v106 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v106_post_v105_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v106_status.json")

    assert status["phase"] == "v106_post_v105_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.106"
    assert status["summary_rows_v106"] == 1
    assert status["stage_summary_rows_v106"] == 6
    assert status["candidate_pair_rows_v106"] == 6317
    assert status["top_candidate_rows_v106"] == 200
    assert status["claim_blocker_rows_v106"] == 3
    assert status["selected_rows_v106"] == 171
    assert status["candidate_add_rows_v106"] == 276698
    assert status["total_pair_rows_screened_v106"] == 47315358
    assert status["return_improving_pair_rows_v106"] == 2012547
    assert status["budget_return_feasible_pair_rows_v106"] == 1221190
    assert status["source_prefilter_pair_rows_v106"] == 7438
    assert status["source_exact_pair_rows_v106"] == 6317
    assert status["cvar_feasible_pair_rows_v106"] == 6317
    assert status["one_swap_improving_rows_v106"] == 6317
    assert status["best_one_swap_return_delta_v106"] == pytest.approx(142.09376124853478)
    assert status["best_one_swap_cvar90_after_v106"] == pytest.approx(92734.88809495818)
    assert status["post_repair_one_swap_local_optimality_cleared_v106"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v106"] is False
    assert status["paper1_promotion_allowed_v106"] is False
    assert status["paper4_working_champion_changed_v106"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v106_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v106",
        "dropped_loan_id_v106",
        "return_delta_v106",
        "objective_return_after_swap_v106",
        "budget_swap_feasible_v106",
        "source_swap_feasible_v106",
        "source_cap_violations_after_swap_v106",
        "cvar_swap_feasible_v106",
        "one_swap_improves_return_v106",
        "claim_boundary_v106",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v106"]
    assert probe["return_delta_v106"].gt(0).all()
    assert probe["budget_swap_feasible_v106"].astype(bool).all()
    assert probe["source_swap_feasible_v106"].astype(bool).all()
    assert probe["cvar_swap_feasible_v106"].astype(bool).all()
    assert probe["one_swap_improves_return_v106"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v106"].sum()) == 0
    assert probe["return_delta_v106"].max() == pytest.approx(
        status["best_one_swap_return_delta_v106"]
    )
    assert probe["claim_boundary_v106"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v106_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v106"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v106"]) == "155861848"
    assert str(best["dropped_loan_id_v106"]) == "126633227"
    assert float(best["return_delta_v106"]) == pytest.approx(
        status["best_one_swap_return_delta_v106"]
    )
    assert float(best["exposure_after_swap_v106"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v106"]) is True

    summary = _read_csv("paper4_v106_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v106"]) == status["one_swap_improving_rows_v106"]
    assert float(row["current_exposure_v106"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v106"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v106"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v106"])

    stage_summary = _read_csv("paper4_v106_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v106", "pair_rows_v106", "claim_boundary_v106"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v106"], stage_summary["pair_rows_v106"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v106"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v106"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v106"]

    blockers = _read_csv("paper4_v106_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v106"], blockers["blocking_v106"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v106"], blockers["evidence_count_v106"], strict=False)
    )
    assert bool(blocker_map["post_v105_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v105_one_swap_improvement_found"]) == 6317
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v106_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v106_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v106_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v106_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v106_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v106 post-v105 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v106 proves the v105 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v106 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v106: Post-v105 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v107_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v107_status.json")

    assert status["phase"] == "v107_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.107"
    assert status["allocation_rows_v107"] == 171
    assert status["summary_rows_v107"] == 1
    assert status["action_rows_v107"] == 1
    assert status["source_summary_rows_v107"] == 51
    assert status["claim_blocker_rows_v107"] == 4
    assert status["added_loan_id_v107"] == "155861848"
    assert status["dropped_loan_id_v107"] == "126633227"
    assert status["selected_rows_v107"] == 171
    assert status["portfolio_exposure_v107"] == pytest.approx(842450.0)
    assert status["objective_return_v107"] == pytest.approx(-1869.9288016583505)
    assert status["scenario_loss_cvar90_v107"] == pytest.approx(92734.88809495818)
    assert status["source_cap_violations_v107"] == 0
    assert status["delta_return_vs_v105_v107"] == pytest.approx(142.09376124853043)
    assert status["delta_cvar90_vs_v105_v107"] == pytest.approx(152.86106813664082)
    assert status["delta_exposure_vs_v105_v107"] == pytest.approx(0.0)
    assert status["budget_feasible_v107"] is True
    assert status["source_feasible_v107"] is True
    assert status["cvar_feasible_v107"] is True
    assert status["repair_candidate_feasible_v107"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v107"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v107"] is False
    assert status["paper1_promotion_allowed_v107"] is False
    assert status["paper4_working_champion_changed_v107"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v107_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v107",
        "selected_v107",
        "portfolio_label_v107",
        "repair_action_v107",
        "claim_boundary_v107",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v107"]
    assert int(allocations["selected_v107"].sum()) == status["selected_rows_v107"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v107"])
    assert "155861848" in set(allocations["loan_id"].astype(str))
    assert "126633227" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v107"]) == {
        "added_from_v106_best_swap",
        "kept_from_v105",
    }
    assert allocations["claim_boundary_v107"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v107_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v107"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v107"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v107"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v107"])

    action = _read_csv("paper4_v107_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v107"]) == status["added_loan_id_v107"]
    assert str(action_row["dropped_loan_id_v107"]) == status["dropped_loan_id_v107"]
    assert float(action_row["return_delta_v107"]) == pytest.approx(142.09376124853478)
    assert int(action_row["source_cap_violations_after_repair_v107"]) == 0

    source_summary = _read_csv("paper4_v107_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v107",
        "source_slack_v107",
        "source_cap_violated_v107",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v107"]
    assert not source_summary["source_cap_violated_v107"].astype(bool).any()

    blockers = _read_csv("paper4_v107_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v107"], blockers["blocking_v107"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v107"], blockers["evidence_count_v107"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v107_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v107_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v107_repair_candidate_feasible"]) is True
    assert bool(claim_map["v107_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v107_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v107_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v107 thirteenth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v107 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v107 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v107: Thirteenth One-Swap Repair Candidate" in notebook
    assert "v108 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v108_post_v107_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v108_status.json")

    assert status["phase"] == "v108_post_v107_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.108"
    assert status["summary_rows_v108"] == 1
    assert status["stage_summary_rows_v108"] == 6
    assert status["candidate_pair_rows_v108"] == 6165
    assert status["top_candidate_rows_v108"] == 200
    assert status["claim_blocker_rows_v108"] == 3
    assert status["selected_rows_v108"] == 171
    assert status["candidate_add_rows_v108"] == 276698
    assert status["total_pair_rows_screened_v108"] == 47315358
    assert status["return_improving_pair_rows_v108"] == 2002105
    assert status["budget_return_feasible_pair_rows_v108"] == 1215503
    assert status["source_prefilter_pair_rows_v108"] == 7259
    assert status["source_exact_pair_rows_v108"] == 6165
    assert status["cvar_feasible_pair_rows_v108"] == 6165
    assert status["one_swap_improving_rows_v108"] == 6165
    assert status["best_one_swap_return_delta_v108"] == pytest.approx(136.78708648947654)
    assert status["best_one_swap_cvar90_after_v108"] == pytest.approx(92923.29605141311)
    assert status["post_repair_one_swap_local_optimality_cleared_v108"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v108"] is False
    assert status["paper1_promotion_allowed_v108"] is False
    assert status["paper4_working_champion_changed_v108"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v108_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v108",
        "dropped_loan_id_v108",
        "return_delta_v108",
        "objective_return_after_swap_v108",
        "budget_swap_feasible_v108",
        "source_swap_feasible_v108",
        "source_cap_violations_after_swap_v108",
        "cvar_swap_feasible_v108",
        "one_swap_improves_return_v108",
        "claim_boundary_v108",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v108"]
    assert probe["return_delta_v108"].gt(0).all()
    assert probe["budget_swap_feasible_v108"].astype(bool).all()
    assert probe["source_swap_feasible_v108"].astype(bool).all()
    assert probe["cvar_swap_feasible_v108"].astype(bool).all()
    assert probe["one_swap_improves_return_v108"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v108"].sum()) == 0
    assert probe["return_delta_v108"].max() == pytest.approx(
        status["best_one_swap_return_delta_v108"]
    )
    assert probe["claim_boundary_v108"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v108_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v108"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v108"]) == "133746505"
    assert str(best["dropped_loan_id_v108"]) == "126053700"
    assert float(best["return_delta_v108"]) == pytest.approx(
        status["best_one_swap_return_delta_v108"]
    )
    assert float(best["exposure_after_swap_v108"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v108"]) is True

    summary = _read_csv("paper4_v108_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v108"]) == status["one_swap_improving_rows_v108"]
    assert float(row["current_exposure_v108"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v108"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v108"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v108"])

    stage_summary = _read_csv("paper4_v108_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v108", "pair_rows_v108", "claim_boundary_v108"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v108"], stage_summary["pair_rows_v108"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v108"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v108"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v108"]

    blockers = _read_csv("paper4_v108_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v108"], blockers["blocking_v108"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v108"], blockers["evidence_count_v108"], strict=False)
    )
    assert bool(blocker_map["post_v107_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v107_one_swap_improvement_found"]) == 6165
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v108_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v108_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v108_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v108_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v108_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v108 post-v107 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v108 proves the v107 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v108 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v108: Post-v107 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v109_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v109_status.json")

    assert status["phase"] == "v109_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.109"
    assert status["allocation_rows_v109"] == 171
    assert status["summary_rows_v109"] == 1
    assert status["action_rows_v109"] == 1
    assert status["source_summary_rows_v109"] == 51
    assert status["claim_blocker_rows_v109"] == 4
    assert status["added_loan_id_v109"] == "133746505"
    assert status["dropped_loan_id_v109"] == "126053700"
    assert status["selected_rows_v109"] == 171
    assert status["portfolio_exposure_v109"] == pytest.approx(842450.0)
    assert status["objective_return_v109"] == pytest.approx(-1733.1417151688747)
    assert status["scenario_loss_cvar90_v109"] == pytest.approx(92923.29605141311)
    assert status["source_cap_violations_v109"] == 0
    assert status["delta_return_vs_v107_v109"] == pytest.approx(136.78708648947577)
    assert status["delta_cvar90_vs_v107_v109"] == pytest.approx(188.407956454932)
    assert status["delta_exposure_vs_v107_v109"] == pytest.approx(0.0)
    assert status["budget_feasible_v109"] is True
    assert status["source_feasible_v109"] is True
    assert status["cvar_feasible_v109"] is True
    assert status["repair_candidate_feasible_v109"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v109"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v109"] is False
    assert status["paper1_promotion_allowed_v109"] is False
    assert status["paper4_working_champion_changed_v109"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v109_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v109",
        "selected_v109",
        "portfolio_label_v109",
        "repair_action_v109",
        "claim_boundary_v109",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v109"]
    assert int(allocations["selected_v109"].sum()) == status["selected_rows_v109"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v109"])
    assert "133746505" in set(allocations["loan_id"].astype(str))
    assert "126053700" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v109"]) == {
        "added_from_v108_best_swap",
        "kept_from_v107",
    }
    assert allocations["claim_boundary_v109"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v109_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v109"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v109"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v109"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v109"])

    action = _read_csv("paper4_v109_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v109"]) == status["added_loan_id_v109"]
    assert str(action_row["dropped_loan_id_v109"]) == status["dropped_loan_id_v109"]
    assert float(action_row["return_delta_v109"]) == pytest.approx(136.78708648947654)
    assert int(action_row["source_cap_violations_after_repair_v109"]) == 0

    source_summary = _read_csv("paper4_v109_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v109",
        "source_slack_v109",
        "source_cap_violated_v109",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v109"]
    assert not source_summary["source_cap_violated_v109"].astype(bool).any()

    blockers = _read_csv("paper4_v109_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v109"], blockers["blocking_v109"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v109"], blockers["evidence_count_v109"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v109_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v109_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v109_repair_candidate_feasible"]) is True
    assert bool(claim_map["v109_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v109_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v109_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v109 fourteenth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v109 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v109 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v109: Fourteenth One-Swap Repair Candidate" in notebook
    assert "v110 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v110_post_v109_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v110_status.json")

    assert status["phase"] == "v110_post_v109_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.110"
    assert status["summary_rows_v110"] == 1
    assert status["stage_summary_rows_v110"] == 6
    assert status["candidate_pair_rows_v110"] == 5991
    assert status["top_candidate_rows_v110"] == 200
    assert status["claim_blocker_rows_v110"] == 3
    assert status["selected_rows_v110"] == 171
    assert status["candidate_add_rows_v110"] == 276698
    assert status["total_pair_rows_screened_v110"] == 47315358
    assert status["return_improving_pair_rows_v110"] == 1991672
    assert status["budget_return_feasible_pair_rows_v110"] == 1210628
    assert status["source_prefilter_pair_rows_v110"] == 7030
    assert status["source_exact_pair_rows_v110"] == 5991
    assert status["cvar_feasible_pair_rows_v110"] == 5991
    assert status["one_swap_improving_rows_v110"] == 5991
    assert status["best_one_swap_return_delta_v110"] == pytest.approx(131.64172216618448)
    assert status["best_one_swap_cvar90_after_v110"] == pytest.approx(93308.72000762806)
    assert status["post_repair_one_swap_local_optimality_cleared_v110"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v110"] is False
    assert status["paper1_promotion_allowed_v110"] is False
    assert status["paper4_working_champion_changed_v110"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v110_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v110",
        "dropped_loan_id_v110",
        "return_delta_v110",
        "objective_return_after_swap_v110",
        "budget_swap_feasible_v110",
        "source_swap_feasible_v110",
        "source_cap_violations_after_swap_v110",
        "cvar_swap_feasible_v110",
        "one_swap_improves_return_v110",
        "claim_boundary_v110",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v110"]
    assert probe["return_delta_v110"].gt(0).all()
    assert probe["budget_swap_feasible_v110"].astype(bool).all()
    assert probe["source_swap_feasible_v110"].astype(bool).all()
    assert probe["cvar_swap_feasible_v110"].astype(bool).all()
    assert probe["one_swap_improves_return_v110"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v110"].sum()) == 0
    assert probe["return_delta_v110"].max() == pytest.approx(
        status["best_one_swap_return_delta_v110"]
    )
    assert probe["claim_boundary_v110"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v110_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v110"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v110"]) == "158940609"
    assert str(best["dropped_loan_id_v110"]) == "126736783"
    assert float(best["return_delta_v110"]) == pytest.approx(
        status["best_one_swap_return_delta_v110"]
    )
    assert float(best["exposure_after_swap_v110"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v110"]) is True

    summary = _read_csv("paper4_v110_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v110"]) == status["one_swap_improving_rows_v110"]
    assert float(row["current_exposure_v110"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v110"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v110"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v110"])

    stage_summary = _read_csv("paper4_v110_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v110", "pair_rows_v110", "claim_boundary_v110"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v110"], stage_summary["pair_rows_v110"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v110"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v110"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v110"]

    blockers = _read_csv("paper4_v110_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v110"], blockers["blocking_v110"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v110"], blockers["evidence_count_v110"], strict=False)
    )
    assert bool(blocker_map["post_v109_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v109_one_swap_improvement_found"]) == 5991
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v110_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v110_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v110_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v110_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v110_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v110 post-v109 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v110 proves the v109 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v110 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v110: Post-v109 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v111_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v111_status.json")

    assert status["phase"] == "v111_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.111"
    assert status["allocation_rows_v111"] == 171
    assert status["summary_rows_v111"] == 1
    assert status["action_rows_v111"] == 1
    assert status["source_summary_rows_v111"] == 51
    assert status["claim_blocker_rows_v111"] == 4
    assert status["added_loan_id_v111"] == "158940609"
    assert status["dropped_loan_id_v111"] == "126736783"
    assert status["selected_rows_v111"] == 171
    assert status["portfolio_exposure_v111"] == pytest.approx(842450.0)
    assert status["objective_return_v111"] == pytest.approx(-1601.4999930026916)
    assert status["scenario_loss_cvar90_v111"] == pytest.approx(93308.72000762806)
    assert status["source_cap_violations_v111"] == 0
    assert status["delta_return_vs_v109_v111"] == pytest.approx(131.64172216618317)
    assert status["delta_cvar90_vs_v109_v111"] == pytest.approx(385.4239562149305)
    assert status["delta_exposure_vs_v109_v111"] == pytest.approx(0.0)
    assert status["budget_feasible_v111"] is True
    assert status["source_feasible_v111"] is True
    assert status["cvar_feasible_v111"] is True
    assert status["repair_candidate_feasible_v111"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v111"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v111"] is False
    assert status["paper1_promotion_allowed_v111"] is False
    assert status["paper4_working_champion_changed_v111"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v111_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v111",
        "selected_v111",
        "portfolio_label_v111",
        "repair_action_v111",
        "claim_boundary_v111",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v111"]
    assert int(allocations["selected_v111"].sum()) == status["selected_rows_v111"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v111"])
    assert "158940609" in set(allocations["loan_id"].astype(str))
    assert "126736783" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v111"]) == {
        "added_from_v110_best_swap",
        "kept_from_v109",
    }
    assert allocations["claim_boundary_v111"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v111_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v111"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v111"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v111"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v111"])

    action = _read_csv("paper4_v111_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v111"]) == status["added_loan_id_v111"]
    assert str(action_row["dropped_loan_id_v111"]) == status["dropped_loan_id_v111"]
    assert float(action_row["return_delta_v111"]) == pytest.approx(131.64172216618448)
    assert int(action_row["source_cap_violations_after_repair_v111"]) == 0

    source_summary = _read_csv("paper4_v111_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v111",
        "source_slack_v111",
        "source_cap_violated_v111",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v111"]
    assert not source_summary["source_cap_violated_v111"].astype(bool).any()

    blockers = _read_csv("paper4_v111_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v111"], blockers["blocking_v111"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v111"], blockers["evidence_count_v111"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v111_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v111_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v111_repair_candidate_feasible"]) is True
    assert bool(claim_map["v111_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v111_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v111_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v111 fifteenth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v111 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v111 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v111: Fifteenth One-Swap Repair Candidate" in notebook
    assert "v112 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v112_post_v111_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v112_status.json")

    assert status["phase"] == "v112_post_v111_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.112"
    assert status["summary_rows_v112"] == 1
    assert status["stage_summary_rows_v112"] == 6
    assert status["candidate_pair_rows_v112"] == 5915
    assert status["top_candidate_rows_v112"] == 200
    assert status["claim_blocker_rows_v112"] == 3
    assert status["selected_rows_v112"] == 171
    assert status["candidate_add_rows_v112"] == 276698
    assert status["total_pair_rows_screened_v112"] == 47315358
    assert status["return_improving_pair_rows_v112"] == 1982862
    assert status["budget_return_feasible_pair_rows_v112"] == 1206782
    assert status["source_prefilter_pair_rows_v112"] == 6949
    assert status["source_exact_pair_rows_v112"] == 5915
    assert status["cvar_feasible_pair_rows_v112"] == 5915
    assert status["one_swap_improving_rows_v112"] == 5915
    assert status["best_one_swap_return_delta_v112"] == pytest.approx(128.01854187366177)
    assert status["best_one_swap_cvar90_after_v112"] == pytest.approx(93291.5115872918)
    assert status["post_repair_one_swap_local_optimality_cleared_v112"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v112"] is False
    assert status["paper1_promotion_allowed_v112"] is False
    assert status["paper4_working_champion_changed_v112"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v112_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v112",
        "dropped_loan_id_v112",
        "return_delta_v112",
        "objective_return_after_swap_v112",
        "budget_swap_feasible_v112",
        "source_swap_feasible_v112",
        "source_cap_violations_after_swap_v112",
        "cvar_swap_feasible_v112",
        "one_swap_improves_return_v112",
        "claim_boundary_v112",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v112"]
    assert probe["return_delta_v112"].gt(0).all()
    assert probe["budget_swap_feasible_v112"].astype(bool).all()
    assert probe["source_swap_feasible_v112"].astype(bool).all()
    assert probe["cvar_swap_feasible_v112"].astype(bool).all()
    assert probe["one_swap_improves_return_v112"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v112"].sum()) == 0
    assert probe["return_delta_v112"].max() == pytest.approx(
        status["best_one_swap_return_delta_v112"]
    )
    assert probe["claim_boundary_v112"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v112_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v112"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v112"]) == "136918234"
    assert str(best["dropped_loan_id_v112"]) == "127277178"
    assert float(best["return_delta_v112"]) == pytest.approx(
        status["best_one_swap_return_delta_v112"]
    )
    assert float(best["exposure_after_swap_v112"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v112"]) is True

    summary = _read_csv("paper4_v112_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v112"]) == status["one_swap_improving_rows_v112"]
    assert float(row["current_exposure_v112"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v112"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v112"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v112"])

    stage_summary = _read_csv("paper4_v112_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v112", "pair_rows_v112", "claim_boundary_v112"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v112"], stage_summary["pair_rows_v112"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v112"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v112"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v112"]

    blockers = _read_csv("paper4_v112_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v112"], blockers["blocking_v112"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v112"], blockers["evidence_count_v112"], strict=False)
    )
    assert bool(blocker_map["post_v111_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v111_one_swap_improvement_found"]) == 5915
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v112_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v112_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v112_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v112_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v112_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v112 post-v111 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v112 proves the v111 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v112 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v112: Post-v111 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v113_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v113_status.json")

    assert status["phase"] == "v113_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.113"
    assert status["allocation_rows_v113"] == 171
    assert status["summary_rows_v113"] == 1
    assert status["action_rows_v113"] == 1
    assert status["source_summary_rows_v113"] == 51
    assert status["claim_blocker_rows_v113"] == 4
    assert status["added_loan_id_v113"] == "136918234"
    assert status["dropped_loan_id_v113"] == "127277178"
    assert status["selected_rows_v113"] == 171
    assert status["portfolio_exposure_v113"] == pytest.approx(842450.0)
    assert status["objective_return_v113"] == pytest.approx(-1473.4814511290242)
    assert status["scenario_loss_cvar90_v113"] == pytest.approx(93291.5115872918)
    assert status["source_cap_violations_v113"] == 0
    assert status["delta_return_vs_v111_v113"] == pytest.approx(128.01854187366735)
    assert status["delta_cvar90_vs_v111_v113"] == pytest.approx(-17.208420336261042)
    assert status["delta_exposure_vs_v111_v113"] == pytest.approx(0.0)
    assert status["budget_feasible_v113"] is True
    assert status["source_feasible_v113"] is True
    assert status["cvar_feasible_v113"] is True
    assert status["repair_candidate_feasible_v113"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v113"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v113"] is False
    assert status["paper1_promotion_allowed_v113"] is False
    assert status["paper4_working_champion_changed_v113"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v113_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v113",
        "selected_v113",
        "portfolio_label_v113",
        "repair_action_v113",
        "claim_boundary_v113",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v113"]
    assert int(allocations["selected_v113"].sum()) == status["selected_rows_v113"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v113"])
    assert "136918234" in set(allocations["loan_id"].astype(str))
    assert "127277178" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v113"]) == {
        "added_from_v112_best_swap",
        "kept_from_v111",
    }
    assert allocations["claim_boundary_v113"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v113_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v113"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v113"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v113"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v113"])

    action = _read_csv("paper4_v113_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v113"]) == status["added_loan_id_v113"]
    assert str(action_row["dropped_loan_id_v113"]) == status["dropped_loan_id_v113"]
    assert float(action_row["return_delta_v113"]) == pytest.approx(128.01854187366177)
    assert int(action_row["source_cap_violations_after_repair_v113"]) == 0

    source_summary = _read_csv("paper4_v113_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v113",
        "source_slack_v113",
        "source_cap_violated_v113",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v113"]
    assert not source_summary["source_cap_violated_v113"].astype(bool).any()

    blockers = _read_csv("paper4_v113_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v113"], blockers["blocking_v113"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v113"], blockers["evidence_count_v113"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v113_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v113_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v113_repair_candidate_feasible"]) is True
    assert bool(claim_map["v113_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v113_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v113_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v113 sixteenth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v113 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v113 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v113: Sixteenth One-Swap Repair Candidate" in notebook
    assert "v114 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v114_post_v113_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v114_status.json")

    assert status["phase"] == "v114_post_v113_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.114"
    assert status["summary_rows_v114"] == 1
    assert status["stage_summary_rows_v114"] == 6
    assert status["candidate_pair_rows_v114"] == 5488
    assert status["top_candidate_rows_v114"] == 200
    assert status["claim_blocker_rows_v114"] == 3
    assert status["selected_rows_v114"] == 171
    assert status["candidate_add_rows_v114"] == 276698
    assert status["total_pair_rows_screened_v114"] == 47315358
    assert status["return_improving_pair_rows_v114"] == 1974003
    assert status["budget_return_feasible_pair_rows_v114"] == 1200847
    assert status["source_prefilter_pair_rows_v114"] == 6522
    assert status["source_exact_pair_rows_v114"] == 5488
    assert status["cvar_feasible_pair_rows_v114"] == 5488
    assert status["one_swap_improving_rows_v114"] == 5488
    assert status["best_one_swap_return_delta_v114"] == pytest.approx(122.87735193601367)
    assert status["best_one_swap_cvar90_after_v114"] == pytest.approx(93380.73432561096)
    assert status["post_repair_one_swap_local_optimality_cleared_v114"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v114"] is False
    assert status["paper1_promotion_allowed_v114"] is False
    assert status["paper4_working_champion_changed_v114"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v114_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v114",
        "dropped_loan_id_v114",
        "return_delta_v114",
        "objective_return_after_swap_v114",
        "budget_swap_feasible_v114",
        "source_swap_feasible_v114",
        "source_cap_violations_after_swap_v114",
        "cvar_swap_feasible_v114",
        "one_swap_improves_return_v114",
        "claim_boundary_v114",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v114"]
    assert probe["return_delta_v114"].gt(0).all()
    assert probe["budget_swap_feasible_v114"].astype(bool).all()
    assert probe["source_swap_feasible_v114"].astype(bool).all()
    assert probe["cvar_swap_feasible_v114"].astype(bool).all()
    assert probe["one_swap_improves_return_v114"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v114"].sum()) == 0
    assert probe["return_delta_v114"].max() == pytest.approx(
        status["best_one_swap_return_delta_v114"]
    )
    assert probe["claim_boundary_v114"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v114_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v114"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v114"]) == "157340979"
    assert str(best["dropped_loan_id_v114"]) == "126349193"
    assert float(best["return_delta_v114"]) == pytest.approx(
        status["best_one_swap_return_delta_v114"]
    )
    assert float(best["exposure_after_swap_v114"]) == pytest.approx(842550.0)
    assert bool(best["one_swap_improves_return_v114"]) is True

    summary = _read_csv("paper4_v114_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v114"]) == status["one_swap_improving_rows_v114"]
    assert float(row["current_exposure_v114"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v114"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v114"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v114"])

    stage_summary = _read_csv("paper4_v114_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v114", "pair_rows_v114", "claim_boundary_v114"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v114"], stage_summary["pair_rows_v114"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v114"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v114"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v114"]

    blockers = _read_csv("paper4_v114_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v114"], blockers["blocking_v114"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v114"], blockers["evidence_count_v114"], strict=False)
    )
    assert bool(blocker_map["post_v113_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v113_one_swap_improvement_found"]) == 5488
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v114_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v114_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v114_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v114_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v114_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v114 post-v113 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v114 proves the v113 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v114 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v114: Post-v113 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v115_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v115_status.json")

    assert status["phase"] == "v115_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.115"
    assert status["allocation_rows_v115"] == 171
    assert status["summary_rows_v115"] == 1
    assert status["action_rows_v115"] == 1
    assert status["source_summary_rows_v115"] == 51
    assert status["claim_blocker_rows_v115"] == 4
    assert status["added_loan_id_v115"] == "157340979"
    assert status["dropped_loan_id_v115"] == "126349193"
    assert status["selected_rows_v115"] == 171
    assert status["portfolio_exposure_v115"] == pytest.approx(842550.0)
    assert status["objective_return_v115"] == pytest.approx(-1350.6040991930186)
    assert status["scenario_loss_cvar90_v115"] == pytest.approx(93380.73432561096)
    assert status["source_cap_violations_v115"] == 0
    assert status["delta_return_vs_v113_v115"] == pytest.approx(122.87735193600565)
    assert status["delta_cvar90_vs_v113_v115"] == pytest.approx(89.22273831916391)
    assert status["delta_exposure_vs_v113_v115"] == pytest.approx(100.0)
    assert status["budget_feasible_v115"] is True
    assert status["source_feasible_v115"] is True
    assert status["cvar_feasible_v115"] is True
    assert status["repair_candidate_feasible_v115"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v115"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v115"] is False
    assert status["paper1_promotion_allowed_v115"] is False
    assert status["paper4_working_champion_changed_v115"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v115_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v115",
        "selected_v115",
        "portfolio_label_v115",
        "repair_action_v115",
        "claim_boundary_v115",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v115"]
    assert int(allocations["selected_v115"].sum()) == status["selected_rows_v115"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v115"])
    assert "157340979" in set(allocations["loan_id"].astype(str))
    assert "126349193" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v115"]) == {
        "added_from_v114_best_swap",
        "kept_from_v113",
    }
    assert allocations["claim_boundary_v115"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v115_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v115"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v115"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v115"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v115"])

    action = _read_csv("paper4_v115_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v115"]) == status["added_loan_id_v115"]
    assert str(action_row["dropped_loan_id_v115"]) == status["dropped_loan_id_v115"]
    assert float(action_row["return_delta_v115"]) == pytest.approx(122.87735193601367)
    assert int(action_row["source_cap_violations_after_repair_v115"]) == 0

    source_summary = _read_csv("paper4_v115_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v115",
        "source_slack_v115",
        "source_cap_violated_v115",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v115"]
    assert not source_summary["source_cap_violated_v115"].astype(bool).any()

    blockers = _read_csv("paper4_v115_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v115"], blockers["blocking_v115"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v115"], blockers["evidence_count_v115"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v115_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v115_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v115_repair_candidate_feasible"]) is True
    assert bool(claim_map["v115_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v115_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v115_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v115 seventeenth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v115 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v115 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v115: Seventeenth One-Swap Repair Candidate" in notebook
    assert "v116 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v116_post_v115_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v116_status.json")

    assert status["phase"] == "v116_post_v115_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.116"
    assert status["summary_rows_v116"] == 1
    assert status["stage_summary_rows_v116"] == 6
    assert status["candidate_pair_rows_v116"] == 6511
    assert status["top_candidate_rows_v116"] == 200
    assert status["claim_blocker_rows_v116"] == 3
    assert status["selected_rows_v116"] == 171
    assert status["candidate_add_rows_v116"] == 276698
    assert status["total_pair_rows_screened_v116"] == 47315358
    assert status["return_improving_pair_rows_v116"] == 1964553
    assert status["budget_return_feasible_pair_rows_v116"] == 1185383
    assert status["source_prefilter_pair_rows_v116"] == 7752
    assert status["source_exact_pair_rows_v116"] == 6511
    assert status["cvar_feasible_pair_rows_v116"] == 6511
    assert status["one_swap_improving_rows_v116"] == 6511
    assert status["best_one_swap_return_delta_v116"] == pytest.approx(146.50898236741978)
    assert status["best_one_swap_cvar90_after_v116"] == pytest.approx(93254.73943831668)
    assert status["post_repair_one_swap_local_optimality_cleared_v116"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v116"] is False
    assert status["paper1_promotion_allowed_v116"] is False
    assert status["paper4_working_champion_changed_v116"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v116_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v116",
        "dropped_loan_id_v116",
        "return_delta_v116",
        "objective_return_after_swap_v116",
        "budget_swap_feasible_v116",
        "source_swap_feasible_v116",
        "source_cap_violations_after_swap_v116",
        "cvar_swap_feasible_v116",
        "one_swap_improves_return_v116",
        "claim_boundary_v116",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v116"]
    assert probe["return_delta_v116"].gt(0).all()
    assert probe["budget_swap_feasible_v116"].astype(bool).all()
    assert probe["source_swap_feasible_v116"].astype(bool).all()
    assert probe["cvar_swap_feasible_v116"].astype(bool).all()
    assert probe["one_swap_improves_return_v116"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v116"].sum()) == 0
    assert probe["return_delta_v116"].max() == pytest.approx(
        status["best_one_swap_return_delta_v116"]
    )
    assert probe["claim_boundary_v116"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v116_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v116"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v116"]) == "145935771"
    assert str(best["dropped_loan_id_v116"]) == "127794335"
    assert float(best["return_delta_v116"]) == pytest.approx(
        status["best_one_swap_return_delta_v116"]
    )
    assert float(best["exposure_after_swap_v116"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v116"]) is True

    summary = _read_csv("paper4_v116_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v116"]) == status["one_swap_improving_rows_v116"]
    assert float(row["current_exposure_v116"]) == pytest.approx(842550.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v116"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v116"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v116"])

    stage_summary = _read_csv("paper4_v116_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v116", "pair_rows_v116", "claim_boundary_v116"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v116"], stage_summary["pair_rows_v116"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v116"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v116"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v116"]

    blockers = _read_csv("paper4_v116_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v116"], blockers["blocking_v116"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v116"], blockers["evidence_count_v116"], strict=False)
    )
    assert bool(blocker_map["post_v115_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v115_one_swap_improvement_found"]) == 6511
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v116_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v116_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v116_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v116_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v116_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v116 post-v115 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v116 proves the v115 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v116 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v116: Post-v115 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v117_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v117_status.json")

    assert status["phase"] == "v117_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.117"
    assert status["allocation_rows_v117"] == 171
    assert status["summary_rows_v117"] == 1
    assert status["action_rows_v117"] == 1
    assert status["source_summary_rows_v117"] == 51
    assert status["claim_blocker_rows_v117"] == 4
    assert status["added_loan_id_v117"] == "145935771"
    assert status["dropped_loan_id_v117"] == "127794335"
    assert status["selected_rows_v117"] == 171
    assert status["portfolio_exposure_v117"] == pytest.approx(842450.0)
    assert status["objective_return_v117"] == pytest.approx(-1204.0951168255942)
    assert status["scenario_loss_cvar90_v117"] == pytest.approx(93254.73943831668)
    assert status["source_cap_violations_v117"] == 0
    assert status["delta_return_vs_v115_v117"] == pytest.approx(146.50898236742432)
    assert status["delta_cvar90_vs_v115_v117"] == pytest.approx(-125.99488729427685)
    assert status["delta_exposure_vs_v115_v117"] == pytest.approx(-100.0)
    assert status["budget_feasible_v117"] is True
    assert status["source_feasible_v117"] is True
    assert status["cvar_feasible_v117"] is True
    assert status["repair_candidate_feasible_v117"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v117"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v117"] is False
    assert status["paper1_promotion_allowed_v117"] is False
    assert status["paper4_working_champion_changed_v117"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v117_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v117",
        "selected_v117",
        "portfolio_label_v117",
        "repair_action_v117",
        "claim_boundary_v117",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v117"]
    assert int(allocations["selected_v117"].sum()) == status["selected_rows_v117"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v117"])
    assert "145935771" in set(allocations["loan_id"].astype(str))
    assert "127794335" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v117"]) == {
        "added_from_v116_best_swap",
        "kept_from_v115",
    }
    assert allocations["claim_boundary_v117"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v117_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v117"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v117"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v117"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v117"])

    action = _read_csv("paper4_v117_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v117"]) == status["added_loan_id_v117"]
    assert str(action_row["dropped_loan_id_v117"]) == status["dropped_loan_id_v117"]
    assert float(action_row["return_delta_v117"]) == pytest.approx(146.50898236741978)
    assert int(action_row["source_cap_violations_after_repair_v117"]) == 0

    source_summary = _read_csv("paper4_v117_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v117",
        "source_slack_v117",
        "source_cap_violated_v117",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v117"]
    assert not source_summary["source_cap_violated_v117"].astype(bool).any()

    blockers = _read_csv("paper4_v117_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v117"], blockers["blocking_v117"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v117"], blockers["evidence_count_v117"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v117_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v117_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v117_repair_candidate_feasible"]) is True
    assert bool(claim_map["v117_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v117_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v117_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v117 eighteenth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v117 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v117 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v117: Eighteenth One-Swap Repair Candidate" in notebook
    assert "v118 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v118_post_v117_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v118_status.json")

    assert status["phase"] == "v118_post_v117_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.118"
    assert status["summary_rows_v118"] == 1
    assert status["stage_summary_rows_v118"] == 6
    assert status["candidate_pair_rows_v118"] == 5239
    assert status["top_candidate_rows_v118"] == 200
    assert status["claim_blocker_rows_v118"] == 3
    assert status["selected_rows_v118"] == 171
    assert status["candidate_add_rows_v118"] == 276698
    assert status["total_pair_rows_screened_v118"] == 47315358
    assert status["return_improving_pair_rows_v118"] == 1953470
    assert status["budget_return_feasible_pair_rows_v118"] == 1190731
    assert status["source_prefilter_pair_rows_v118"] == 6060
    assert status["source_exact_pair_rows_v118"] == 5239
    assert status["cvar_feasible_pair_rows_v118"] == 5239
    assert status["one_swap_improving_rows_v118"] == 5239
    assert status["best_one_swap_return_delta_v118"] == pytest.approx(121.23652794012801)
    assert status["best_one_swap_cvar90_after_v118"] == pytest.approx(93372.68065506514)
    assert status["post_repair_one_swap_local_optimality_cleared_v118"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v118"] is False
    assert status["paper1_promotion_allowed_v118"] is False
    assert status["paper4_working_champion_changed_v118"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v118_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v118",
        "dropped_loan_id_v118",
        "return_delta_v118",
        "objective_return_after_swap_v118",
        "budget_swap_feasible_v118",
        "source_swap_feasible_v118",
        "source_cap_violations_after_swap_v118",
        "cvar_swap_feasible_v118",
        "one_swap_improves_return_v118",
        "claim_boundary_v118",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v118"]
    assert probe["return_delta_v118"].gt(0).all()
    assert probe["budget_swap_feasible_v118"].astype(bool).all()
    assert probe["source_swap_feasible_v118"].astype(bool).all()
    assert probe["cvar_swap_feasible_v118"].astype(bool).all()
    assert probe["one_swap_improves_return_v118"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v118"].sum()) == 0
    assert probe["return_delta_v118"].max() == pytest.approx(
        status["best_one_swap_return_delta_v118"]
    )
    assert probe["claim_boundary_v118"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v118_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v118"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v118"]) == "163964785"
    assert str(best["dropped_loan_id_v118"]) == "127742550"
    assert float(best["return_delta_v118"]) == pytest.approx(
        status["best_one_swap_return_delta_v118"]
    )
    assert float(best["exposure_after_swap_v118"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v118"]) is True

    summary = _read_csv("paper4_v118_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v118"]) == status["one_swap_improving_rows_v118"]
    assert float(row["current_exposure_v118"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v118"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v118"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v118"])

    stage_summary = _read_csv("paper4_v118_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v118", "pair_rows_v118", "claim_boundary_v118"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v118"], stage_summary["pair_rows_v118"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v118"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v118"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v118"]

    blockers = _read_csv("paper4_v118_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v118"], blockers["blocking_v118"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v118"], blockers["evidence_count_v118"], strict=False)
    )
    assert bool(blocker_map["post_v117_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v117_one_swap_improvement_found"]) == 5239
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v118_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v118_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v118_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v118_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v118_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v118 post-v117 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v118 proves the v117 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v118 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v118: Post-v117 One-Swap Repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v119_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v119_status.json")

    assert status["phase"] == "v119_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.119"
    assert status["allocation_rows_v119"] == 171
    assert status["summary_rows_v119"] == 1
    assert status["action_rows_v119"] == 1
    assert status["source_summary_rows_v119"] == 51
    assert status["claim_blocker_rows_v119"] == 4
    assert status["added_loan_id_v119"] == "163964785"
    assert status["dropped_loan_id_v119"] == "127742550"
    assert status["selected_rows_v119"] == 171
    assert status["portfolio_exposure_v119"] == pytest.approx(842450.0)
    assert status["objective_return_v119"] == pytest.approx(-1082.8585888854686)
    assert status["scenario_loss_cvar90_v119"] == pytest.approx(93372.68065506514)
    assert status["source_cap_violations_v119"] == 0
    assert status["delta_return_vs_v117_v119"] == pytest.approx(121.2365279401256)
    assert status["delta_cvar90_vs_v117_v119"] == pytest.approx(117.94121674845519)
    assert status["delta_exposure_vs_v117_v119"] == pytest.approx(0.0)
    assert status["budget_feasible_v119"] is True
    assert status["source_feasible_v119"] is True
    assert status["cvar_feasible_v119"] is True
    assert status["repair_candidate_feasible_v119"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v119"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v119"] is False
    assert status["paper1_promotion_allowed_v119"] is False
    assert status["paper4_working_champion_changed_v119"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v119_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v119",
        "selected_v119",
        "portfolio_label_v119",
        "repair_action_v119",
        "claim_boundary_v119",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v119"]
    assert int(allocations["selected_v119"].sum()) == status["selected_rows_v119"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v119"])
    assert "163964785" in set(allocations["loan_id"].astype(str))
    assert "127742550" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v119"]) == {
        "added_from_v118_best_swap",
        "kept_from_v117",
    }
    assert allocations["claim_boundary_v119"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v119_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v119"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v119"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v119"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v119"])

    action = _read_csv("paper4_v119_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v119"]) == status["added_loan_id_v119"]
    assert str(action_row["dropped_loan_id_v119"]) == status["dropped_loan_id_v119"]
    assert float(action_row["return_delta_v119"]) == pytest.approx(121.23652794012801)
    assert int(action_row["source_cap_violations_after_repair_v119"]) == 0

    source_summary = _read_csv("paper4_v119_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v119",
        "source_slack_v119",
        "source_cap_violated_v119",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v119"]
    assert not source_summary["source_cap_violated_v119"].astype(bool).any()

    blockers = _read_csv("paper4_v119_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v119"], blockers["blocking_v119"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v119"], blockers["evidence_count_v119"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v119_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v119_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v119_repair_candidate_feasible"]) is True
    assert bool(claim_map["v119_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v119_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v119_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v119 nineteenth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v119 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v119 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v119: Nineteenth One-Swap Repair Candidate" in notebook
    assert "v120 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v120_post_v119_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v120_status.json")

    assert status["phase"] == "v120_post_v119_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.120"
    assert status["summary_rows_v120"] == 1
    assert status["stage_summary_rows_v120"] == 6
    assert status["candidate_pair_rows_v120"] == 4835
    assert status["top_candidate_rows_v120"] == 200
    assert status["claim_blocker_rows_v120"] == 3
    assert status["selected_rows_v120"] == 171
    assert status["candidate_add_rows_v120"] == 276698
    assert status["total_pair_rows_screened_v120"] == 47315358
    assert status["return_improving_pair_rows_v120"] == 1945037
    assert status["budget_return_feasible_pair_rows_v120"] == 1185131
    assert status["source_prefilter_pair_rows_v120"] == 5656
    assert status["source_exact_pair_rows_v120"] == 4835
    assert status["cvar_feasible_pair_rows_v120"] == 4835
    assert status["one_swap_improving_rows_v120"] == 4835
    assert status["best_one_swap_return_delta_v120"] == pytest.approx(118.8179334966386)
    assert status["best_one_swap_cvar90_after_v120"] == pytest.approx(93390.96271634115)
    assert status["post_repair_one_swap_local_optimality_cleared_v120"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v120"] is False
    assert status["paper1_promotion_allowed_v120"] is False
    assert status["paper4_working_champion_changed_v120"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v120_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v120",
        "dropped_loan_id_v120",
        "return_delta_v120",
        "objective_return_after_swap_v120",
        "budget_swap_feasible_v120",
        "source_swap_feasible_v120",
        "source_cap_violations_after_swap_v120",
        "cvar_swap_feasible_v120",
        "one_swap_improves_return_v120",
        "claim_boundary_v120",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v120"]
    assert probe["return_delta_v120"].gt(0).all()
    assert probe["budget_swap_feasible_v120"].astype(bool).all()
    assert probe["source_swap_feasible_v120"].astype(bool).all()
    assert probe["cvar_swap_feasible_v120"].astype(bool).all()
    assert probe["one_swap_improves_return_v120"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v120"].sum()) == 0
    assert probe["return_delta_v120"].max() == pytest.approx(
        status["best_one_swap_return_delta_v120"]
    )
    assert probe["claim_boundary_v120"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v120_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v120"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v120"]) == "160560314"
    assert str(best["dropped_loan_id_v120"]) == "126864348"
    assert float(best["return_delta_v120"]) == pytest.approx(
        status["best_one_swap_return_delta_v120"]
    )
    assert float(best["exposure_after_swap_v120"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v120"]) is True

    summary = _read_csv("paper4_v120_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v120"]) == status["one_swap_improving_rows_v120"]
    assert float(row["current_exposure_v120"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v120"]) == pytest.approx(-1082.8585888854686)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v120"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v120"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v120"])

    stage_summary = _read_csv("paper4_v120_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v120", "pair_rows_v120", "claim_boundary_v120"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v120"], stage_summary["pair_rows_v120"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v120"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v120"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v120"]

    blockers = _read_csv("paper4_v120_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v120"], blockers["blocking_v120"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v120"], blockers["evidence_count_v120"], strict=False)
    )
    assert bool(blocker_map["post_v119_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v119_one_swap_improvement_found"]) == 4835
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v120_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v120_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v120_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v120_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v120_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v120 post-v119 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v120 proves the v119 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v120 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v120: Post-v119 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4835`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v121_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v121_status.json")

    assert status["phase"] == "v121_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.121"
    assert status["allocation_rows_v121"] == 171
    assert status["summary_rows_v121"] == 1
    assert status["action_rows_v121"] == 1
    assert status["source_summary_rows_v121"] == 51
    assert status["claim_blocker_rows_v121"] == 4
    assert status["added_loan_id_v121"] == "160560314"
    assert status["dropped_loan_id_v121"] == "126864348"
    assert status["selected_rows_v121"] == 171
    assert status["portfolio_exposure_v121"] == pytest.approx(842450.0)
    assert status["objective_return_v121"] == pytest.approx(-964.0406553888279)
    assert status["scenario_loss_cvar90_v121"] == pytest.approx(93390.96271634116)
    assert status["source_cap_violations_v121"] == 0
    assert status["delta_return_vs_v119_v121"] == pytest.approx(118.81793349664076)
    assert status["delta_cvar90_vs_v119_v121"] == pytest.approx(18.28206127602607)
    assert status["delta_exposure_vs_v119_v121"] == pytest.approx(0.0)
    assert status["budget_feasible_v121"] is True
    assert status["source_feasible_v121"] is True
    assert status["cvar_feasible_v121"] is True
    assert status["repair_candidate_feasible_v121"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v121"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v121"] is False
    assert status["paper1_promotion_allowed_v121"] is False
    assert status["paper4_working_champion_changed_v121"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v121_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v121",
        "selected_v121",
        "portfolio_label_v121",
        "repair_action_v121",
        "claim_boundary_v121",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v121"]
    assert int(allocations["selected_v121"].sum()) == status["selected_rows_v121"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v121"])
    assert "160560314" in set(allocations["loan_id"].astype(str))
    assert "126864348" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v121"]) == {
        "added_from_v120_best_swap",
        "kept_from_v119",
    }
    assert allocations["claim_boundary_v121"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v121_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v121"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v121"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v121"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v121"])

    action = _read_csv("paper4_v121_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v121"]) == status["added_loan_id_v121"]
    assert str(action_row["dropped_loan_id_v121"]) == status["dropped_loan_id_v121"]
    assert float(action_row["return_delta_v121"]) == pytest.approx(118.8179334966386)
    assert int(action_row["source_cap_violations_after_repair_v121"]) == 0

    source_summary = _read_csv("paper4_v121_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v121",
        "source_slack_v121",
        "source_cap_violated_v121",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v121"]
    assert not source_summary["source_cap_violated_v121"].astype(bool).any()

    blockers = _read_csv("paper4_v121_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v121"], blockers["blocking_v121"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v121"], blockers["evidence_count_v121"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v121_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v121_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v121_repair_candidate_feasible"]) is True
    assert bool(claim_map["v121_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v121_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v121_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v121 twentieth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v121 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v121 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v121: Twentieth One-Swap Repair Candidate" in notebook
    assert "v122 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v122_post_v121_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v122_status.json")

    assert status["phase"] == "v122_post_v121_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.122"
    assert status["summary_rows_v122"] == 1
    assert status["stage_summary_rows_v122"] == 6
    assert status["candidate_pair_rows_v122"] == 4755
    assert status["top_candidate_rows_v122"] == 200
    assert status["claim_blocker_rows_v122"] == 3
    assert status["selected_rows_v122"] == 171
    assert status["candidate_add_rows_v122"] == 276698
    assert status["total_pair_rows_screened_v122"] == 47315358
    assert status["return_improving_pair_rows_v122"] == 1937078
    assert status["budget_return_feasible_pair_rows_v122"] == 1181596
    assert status["source_prefilter_pair_rows_v122"] == 5554
    assert status["source_exact_pair_rows_v122"] == 4755
    assert status["cvar_feasible_pair_rows_v122"] == 4755
    assert status["one_swap_improving_rows_v122"] == 4755
    assert status["best_one_swap_return_delta_v122"] == pytest.approx(108.53567836025547)
    assert status["best_one_swap_cvar90_after_v122"] == pytest.approx(93424.6751064591)
    assert status["post_repair_one_swap_local_optimality_cleared_v122"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v122"] is False
    assert status["paper1_promotion_allowed_v122"] is False
    assert status["paper4_working_champion_changed_v122"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v122_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v122",
        "dropped_loan_id_v122",
        "return_delta_v122",
        "objective_return_after_swap_v122",
        "budget_swap_feasible_v122",
        "source_swap_feasible_v122",
        "source_cap_violations_after_swap_v122",
        "cvar_swap_feasible_v122",
        "one_swap_improves_return_v122",
        "claim_boundary_v122",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v122"]
    assert probe["return_delta_v122"].gt(0).all()
    assert probe["budget_swap_feasible_v122"].astype(bool).all()
    assert probe["source_swap_feasible_v122"].astype(bool).all()
    assert probe["cvar_swap_feasible_v122"].astype(bool).all()
    assert probe["one_swap_improves_return_v122"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v122"].sum()) == 0
    assert probe["return_delta_v122"].max() == pytest.approx(
        status["best_one_swap_return_delta_v122"]
    )
    assert probe["claim_boundary_v122"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v122_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v122"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v122"]) == "147944552"
    assert str(best["dropped_loan_id_v122"]) == "127232386"
    assert float(best["return_delta_v122"]) == pytest.approx(
        status["best_one_swap_return_delta_v122"]
    )
    assert float(best["exposure_after_swap_v122"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v122"]) is True

    summary = _read_csv("paper4_v122_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v122"]) == status["one_swap_improving_rows_v122"]
    assert float(row["current_exposure_v122"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v122"]) == pytest.approx(-964.040655388828)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v122"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v122"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v122"])

    stage_summary = _read_csv("paper4_v122_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v122", "pair_rows_v122", "claim_boundary_v122"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v122"], stage_summary["pair_rows_v122"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v122"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v122"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v122"]

    blockers = _read_csv("paper4_v122_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v122"], blockers["blocking_v122"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v122"], blockers["evidence_count_v122"], strict=False)
    )
    assert bool(blocker_map["post_v121_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v121_one_swap_improvement_found"]) == 4755
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v122_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v122_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v122_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v122_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v122_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v122 post-v121 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v122 proves the v121 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v122 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v122: Post-v121 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4755`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v123_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v123_status.json")

    assert status["phase"] == "v123_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.123"
    assert status["allocation_rows_v123"] == 171
    assert status["summary_rows_v123"] == 1
    assert status["action_rows_v123"] == 1
    assert status["source_summary_rows_v123"] == 51
    assert status["claim_blocker_rows_v123"] == 4
    assert status["added_loan_id_v123"] == "147944552"
    assert status["dropped_loan_id_v123"] == "127232386"
    assert status["selected_rows_v123"] == 171
    assert status["portfolio_exposure_v123"] == pytest.approx(842450.0)
    assert status["objective_return_v123"] == pytest.approx(-855.5049770285732)
    assert status["scenario_loss_cvar90_v123"] == pytest.approx(93424.67510645912)
    assert status["source_cap_violations_v123"] == 0
    assert status["delta_return_vs_v121_v123"] == pytest.approx(108.53567836025479)
    assert status["delta_cvar90_vs_v121_v123"] == pytest.approx(33.71239011795842)
    assert status["delta_exposure_vs_v121_v123"] == pytest.approx(0.0)
    assert status["budget_feasible_v123"] is True
    assert status["source_feasible_v123"] is True
    assert status["cvar_feasible_v123"] is True
    assert status["repair_candidate_feasible_v123"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v123"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v123"] is False
    assert status["paper1_promotion_allowed_v123"] is False
    assert status["paper4_working_champion_changed_v123"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v123_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v123",
        "selected_v123",
        "portfolio_label_v123",
        "repair_action_v123",
        "claim_boundary_v123",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v123"]
    assert int(allocations["selected_v123"].sum()) == status["selected_rows_v123"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v123"])
    assert "147944552" in set(allocations["loan_id"].astype(str))
    assert "127232386" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v123"]) == {
        "added_from_v122_best_swap",
        "kept_from_v121",
    }
    assert allocations["claim_boundary_v123"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v123_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v123"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v123"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v123"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v123"])

    action = _read_csv("paper4_v123_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v123"]) == status["added_loan_id_v123"]
    assert str(action_row["dropped_loan_id_v123"]) == status["dropped_loan_id_v123"]
    assert float(action_row["return_delta_v123"]) == pytest.approx(108.53567836025547)
    assert int(action_row["source_cap_violations_after_repair_v123"]) == 0

    source_summary = _read_csv("paper4_v123_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v123",
        "source_slack_v123",
        "source_cap_violated_v123",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v123"]
    assert not source_summary["source_cap_violated_v123"].astype(bool).any()

    blockers = _read_csv("paper4_v123_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v123"], blockers["blocking_v123"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v123"], blockers["evidence_count_v123"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v123_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v123_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v123_repair_candidate_feasible"]) is True
    assert bool(claim_map["v123_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v123_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v123_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v123 twenty-first one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v123 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v123 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v123: Twenty-First One-Swap Repair Candidate" in notebook
    assert "v124 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v124_post_v123_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v124_status.json")

    assert status["phase"] == "v124_post_v123_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.124"
    assert status["summary_rows_v124"] == 1
    assert status["stage_summary_rows_v124"] == 6
    assert status["candidate_pair_rows_v124"] == 4377
    assert status["top_candidate_rows_v124"] == 200
    assert status["claim_blocker_rows_v124"] == 3
    assert status["selected_rows_v124"] == 171
    assert status["candidate_add_rows_v124"] == 276698
    assert status["total_pair_rows_screened_v124"] == 47315358
    assert status["return_improving_pair_rows_v124"] == 1929510
    assert status["budget_return_feasible_pair_rows_v124"] == 1176556
    assert status["source_prefilter_pair_rows_v124"] == 5176
    assert status["source_exact_pair_rows_v124"] == 4377
    assert status["cvar_feasible_pair_rows_v124"] == 4377
    assert status["one_swap_improving_rows_v124"] == 4377
    assert status["best_one_swap_return_delta_v124"] == pytest.approx(105.32830814768198)
    assert status["best_one_swap_cvar90_after_v124"] == pytest.approx(93519.99529357199)
    assert status["post_repair_one_swap_local_optimality_cleared_v124"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v124"] is False
    assert status["paper1_promotion_allowed_v124"] is False
    assert status["paper4_working_champion_changed_v124"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v124_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v124",
        "dropped_loan_id_v124",
        "return_delta_v124",
        "objective_return_after_swap_v124",
        "budget_swap_feasible_v124",
        "source_swap_feasible_v124",
        "source_cap_violations_after_swap_v124",
        "cvar_swap_feasible_v124",
        "one_swap_improves_return_v124",
        "claim_boundary_v124",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v124"]
    assert probe["return_delta_v124"].gt(0).all()
    assert probe["budget_swap_feasible_v124"].astype(bool).all()
    assert probe["source_swap_feasible_v124"].astype(bool).all()
    assert probe["cvar_swap_feasible_v124"].astype(bool).all()
    assert probe["one_swap_improves_return_v124"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v124"].sum()) == 0
    assert probe["return_delta_v124"].max() == pytest.approx(
        status["best_one_swap_return_delta_v124"]
    )
    assert probe["claim_boundary_v124"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v124_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v124"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v124"]) == "130095750"
    assert str(best["dropped_loan_id_v124"]) == "127867847"
    assert float(best["return_delta_v124"]) == pytest.approx(
        status["best_one_swap_return_delta_v124"]
    )
    assert float(best["exposure_after_swap_v124"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v124"]) is True

    summary = _read_csv("paper4_v124_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v124"]) == status["one_swap_improving_rows_v124"]
    assert float(row["current_exposure_v124"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v124"]) == pytest.approx(-855.5049770285732)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v124"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v124"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v124"])

    stage_summary = _read_csv("paper4_v124_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v124", "pair_rows_v124", "claim_boundary_v124"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v124"], stage_summary["pair_rows_v124"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v124"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v124"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v124"]

    blockers = _read_csv("paper4_v124_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v124"], blockers["blocking_v124"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v124"], blockers["evidence_count_v124"], strict=False)
    )
    assert bool(blocker_map["post_v123_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v123_one_swap_improvement_found"]) == 4377
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v124_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v124_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v124_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v124_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v124_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v124 post-v123 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v124 proves the v123 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v124 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v124: Post-v123 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4377`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v125_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v125_status.json")

    assert status["phase"] == "v125_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.125"
    assert status["allocation_rows_v125"] == 171
    assert status["summary_rows_v125"] == 1
    assert status["action_rows_v125"] == 1
    assert status["source_summary_rows_v125"] == 51
    assert status["claim_blocker_rows_v125"] == 4
    assert status["added_loan_id_v125"] == "130095750"
    assert status["dropped_loan_id_v125"] == "127867847"
    assert status["selected_rows_v125"] == 171
    assert status["portfolio_exposure_v125"] == pytest.approx(842450.0)
    assert status["objective_return_v125"] == pytest.approx(-750.1766688808912)
    assert status["scenario_loss_cvar90_v125"] == pytest.approx(93519.99529357199)
    assert status["source_cap_violations_v125"] == 0
    assert status["delta_return_vs_v123_v125"] == pytest.approx(105.32830814768204)
    assert status["delta_cvar90_vs_v123_v125"] == pytest.approx(95.32018711286946)
    assert status["delta_exposure_vs_v123_v125"] == pytest.approx(0.0)
    assert status["budget_feasible_v125"] is True
    assert status["source_feasible_v125"] is True
    assert status["cvar_feasible_v125"] is True
    assert status["repair_candidate_feasible_v125"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v125"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v125"] is False
    assert status["paper1_promotion_allowed_v125"] is False
    assert status["paper4_working_champion_changed_v125"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v125_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v125",
        "selected_v125",
        "portfolio_label_v125",
        "repair_action_v125",
        "claim_boundary_v125",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v125"]
    assert int(allocations["selected_v125"].sum()) == status["selected_rows_v125"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v125"])
    assert "130095750" in set(allocations["loan_id"].astype(str))
    assert "127867847" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v125"]) == {
        "added_from_v124_best_swap",
        "kept_from_v123",
    }
    assert allocations["claim_boundary_v125"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v125_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v125"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v125"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v125"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v125"])

    action = _read_csv("paper4_v125_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v125"]) == status["added_loan_id_v125"]
    assert str(action_row["dropped_loan_id_v125"]) == status["dropped_loan_id_v125"]
    assert float(action_row["return_delta_v125"]) == pytest.approx(105.32830814768198)
    assert int(action_row["source_cap_violations_after_repair_v125"]) == 0

    source_summary = _read_csv("paper4_v125_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v125",
        "source_slack_v125",
        "source_cap_violated_v125",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v125"]
    assert not source_summary["source_cap_violated_v125"].astype(bool).any()

    blockers = _read_csv("paper4_v125_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v125"], blockers["blocking_v125"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v125"], blockers["evidence_count_v125"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v125_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v125_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v125_repair_candidate_feasible"]) is True
    assert bool(claim_map["v125_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v125_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v125_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v125 twenty-second one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v125 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v125 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v125: Twenty-Second One-Swap Repair Candidate" in notebook
    assert "v126 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v126_post_v125_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v126_status.json")

    assert status["phase"] == "v126_post_v125_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.126"
    assert status["summary_rows_v126"] == 1
    assert status["stage_summary_rows_v126"] == 6
    assert status["candidate_pair_rows_v126"] == 4364
    assert status["top_candidate_rows_v126"] == 200
    assert status["claim_blocker_rows_v126"] == 3
    assert status["selected_rows_v126"] == 171
    assert status["candidate_add_rows_v126"] == 276698
    assert status["total_pair_rows_screened_v126"] == 47315358
    assert status["return_improving_pair_rows_v126"] == 1922871
    assert status["budget_return_feasible_pair_rows_v126"] == 1173209
    assert status["source_prefilter_pair_rows_v126"] == 5163
    assert status["source_exact_pair_rows_v126"] == 4364
    assert status["cvar_feasible_pair_rows_v126"] == 4364
    assert status["one_swap_improving_rows_v126"] == 4364
    assert status["best_one_swap_return_delta_v126"] == pytest.approx(101.1474560355366)
    assert status["best_one_swap_cvar90_after_v126"] == pytest.approx(93507.96054728286)
    assert status["post_repair_one_swap_local_optimality_cleared_v126"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v126"] is False
    assert status["paper1_promotion_allowed_v126"] is False
    assert status["paper4_working_champion_changed_v126"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v126_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v126",
        "dropped_loan_id_v126",
        "return_delta_v126",
        "objective_return_after_swap_v126",
        "budget_swap_feasible_v126",
        "source_swap_feasible_v126",
        "source_cap_violations_after_swap_v126",
        "cvar_swap_feasible_v126",
        "one_swap_improves_return_v126",
        "claim_boundary_v126",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v126"]
    assert probe["return_delta_v126"].gt(0).all()
    assert probe["budget_swap_feasible_v126"].astype(bool).all()
    assert probe["source_swap_feasible_v126"].astype(bool).all()
    assert probe["cvar_swap_feasible_v126"].astype(bool).all()
    assert probe["one_swap_improves_return_v126"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v126"].sum()) == 0
    assert probe["return_delta_v126"].max() == pytest.approx(
        status["best_one_swap_return_delta_v126"]
    )
    assert probe["claim_boundary_v126"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v126_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v126"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v126"]) == "129751891"
    assert str(best["dropped_loan_id_v126"]) == "126608904"
    assert float(best["return_delta_v126"]) == pytest.approx(
        status["best_one_swap_return_delta_v126"]
    )
    assert float(best["exposure_after_swap_v126"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v126"]) is True

    summary = _read_csv("paper4_v126_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v126"]) == status["one_swap_improving_rows_v126"]
    assert float(row["current_exposure_v126"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v126"]) == pytest.approx(-750.1766688808912)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v126"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v126"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v126"])

    stage_summary = _read_csv("paper4_v126_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v126", "pair_rows_v126", "claim_boundary_v126"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v126"], stage_summary["pair_rows_v126"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v126"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v126"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v126"]

    blockers = _read_csv("paper4_v126_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v126"], blockers["blocking_v126"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v126"], blockers["evidence_count_v126"], strict=False)
    )
    assert bool(blocker_map["post_v125_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v125_one_swap_improvement_found"]) == 4364
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v126_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v126_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v126_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v126_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v126_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v126 post-v125 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v126 proves the v125 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v126 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v126: Post-v125 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4364`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v127_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v127_status.json")

    assert status["phase"] == "v127_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.127"
    assert status["allocation_rows_v127"] == 171
    assert status["summary_rows_v127"] == 1
    assert status["action_rows_v127"] == 1
    assert status["source_summary_rows_v127"] == 51
    assert status["claim_blocker_rows_v127"] == 4
    assert status["added_loan_id_v127"] == "129751891"
    assert status["dropped_loan_id_v127"] == "126608904"
    assert status["selected_rows_v127"] == 171
    assert status["portfolio_exposure_v127"] == pytest.approx(842450.0)
    assert status["objective_return_v127"] == pytest.approx(-649.029212845353)
    assert status["scenario_loss_cvar90_v127"] == pytest.approx(93507.96054728286)
    assert status["source_cap_violations_v127"] == 0
    assert status["delta_return_vs_v125_v127"] == pytest.approx(101.1474560355382)
    assert status["delta_cvar90_vs_v125_v127"] == pytest.approx(-12.034746289151371)
    assert status["delta_exposure_vs_v125_v127"] == pytest.approx(0.0)
    assert status["budget_feasible_v127"] is True
    assert status["source_feasible_v127"] is True
    assert status["cvar_feasible_v127"] is True
    assert status["repair_candidate_feasible_v127"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v127"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v127"] is False
    assert status["paper1_promotion_allowed_v127"] is False
    assert status["paper4_working_champion_changed_v127"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v127_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v127",
        "selected_v127",
        "portfolio_label_v127",
        "repair_action_v127",
        "claim_boundary_v127",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v127"]
    assert int(allocations["selected_v127"].sum()) == status["selected_rows_v127"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v127"])
    assert "129751891" in set(allocations["loan_id"].astype(str))
    assert "126608904" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v127"]) == {
        "added_from_v126_best_swap",
        "kept_from_v125",
    }
    assert allocations["claim_boundary_v127"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v127_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v127"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v127"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v127"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v127"])

    action = _read_csv("paper4_v127_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v127"]) == status["added_loan_id_v127"]
    assert str(action_row["dropped_loan_id_v127"]) == status["dropped_loan_id_v127"]
    assert float(action_row["return_delta_v127"]) == pytest.approx(101.1474560355366)
    assert int(action_row["source_cap_violations_after_repair_v127"]) == 0

    source_summary = _read_csv("paper4_v127_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v127",
        "source_slack_v127",
        "source_cap_violated_v127",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v127"]
    assert not source_summary["source_cap_violated_v127"].astype(bool).any()

    blockers = _read_csv("paper4_v127_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v127"], blockers["blocking_v127"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v127"], blockers["evidence_count_v127"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v127_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v127_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v127_repair_candidate_feasible"]) is True
    assert bool(claim_map["v127_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v127_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v127_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v127 twenty-third one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v127 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v127 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v127: Twenty-Third One-Swap Repair Candidate" in notebook
    assert "v128 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v128_post_v127_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v128_status.json")

    assert status["phase"] == "v128_post_v127_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.128"
    assert status["summary_rows_v128"] == 1
    assert status["stage_summary_rows_v128"] == 6
    assert status["candidate_pair_rows_v128"] == 4326
    assert status["top_candidate_rows_v128"] == 200
    assert status["claim_blocker_rows_v128"] == 3
    assert status["selected_rows_v128"] == 171
    assert status["candidate_add_rows_v128"] == 276698
    assert status["total_pair_rows_screened_v128"] == 47315358
    assert status["return_improving_pair_rows_v128"] == 1915852
    assert status["budget_return_feasible_pair_rows_v128"] == 1168819
    assert status["source_prefilter_pair_rows_v128"] == 5117
    assert status["source_exact_pair_rows_v128"] == 4326
    assert status["cvar_feasible_pair_rows_v128"] == 4326
    assert status["one_swap_improving_rows_v128"] == 4326
    assert status["best_one_swap_return_delta_v128"] == pytest.approx(101.13466517860724)
    assert status["best_one_swap_cvar90_after_v128"] == pytest.approx(93600.69280130665)
    assert status["post_repair_one_swap_local_optimality_cleared_v128"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v128"] is False
    assert status["paper1_promotion_allowed_v128"] is False
    assert status["paper4_working_champion_changed_v128"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v128_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v128",
        "dropped_loan_id_v128",
        "return_delta_v128",
        "objective_return_after_swap_v128",
        "budget_swap_feasible_v128",
        "source_swap_feasible_v128",
        "source_cap_violations_after_swap_v128",
        "cvar_swap_feasible_v128",
        "one_swap_improves_return_v128",
        "claim_boundary_v128",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v128"]
    assert probe["return_delta_v128"].gt(0).all()
    assert probe["budget_swap_feasible_v128"].astype(bool).all()
    assert probe["source_swap_feasible_v128"].astype(bool).all()
    assert probe["cvar_swap_feasible_v128"].astype(bool).all()
    assert probe["one_swap_improves_return_v128"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v128"].sum()) == 0
    assert probe["return_delta_v128"].max() == pytest.approx(
        status["best_one_swap_return_delta_v128"]
    )
    assert probe["claim_boundary_v128"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v128_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v128"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v128"]) == "140960427"
    assert str(best["dropped_loan_id_v128"]) == "127108843"
    assert float(best["return_delta_v128"]) == pytest.approx(
        status["best_one_swap_return_delta_v128"]
    )
    assert float(best["exposure_after_swap_v128"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v128"]) is True

    summary = _read_csv("paper4_v128_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v128"]) == status["one_swap_improving_rows_v128"]
    assert float(row["current_exposure_v128"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v128"]) == pytest.approx(-649.029212845353)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v128"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v128"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v128"])

    stage_summary = _read_csv("paper4_v128_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v128", "pair_rows_v128", "claim_boundary_v128"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v128"], stage_summary["pair_rows_v128"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v128"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v128"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v128"]

    blockers = _read_csv("paper4_v128_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v128"], blockers["blocking_v128"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v128"], blockers["evidence_count_v128"], strict=False)
    )
    assert bool(blocker_map["post_v127_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v127_one_swap_improvement_found"]) == 4326
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v128_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v128_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v128_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v128_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v128_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v128 post-v127 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v128 proves the v127 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v128 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v128: Post-v127 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4326`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v129_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v129_status.json")

    assert status["phase"] == "v129_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.129"
    assert status["allocation_rows_v129"] == 171
    assert status["summary_rows_v129"] == 1
    assert status["action_rows_v129"] == 1
    assert status["source_summary_rows_v129"] == 51
    assert status["claim_blocker_rows_v129"] == 4
    assert status["added_loan_id_v129"] == "140960427"
    assert status["dropped_loan_id_v129"] == "127108843"
    assert status["selected_rows_v129"] == 171
    assert status["portfolio_exposure_v129"] == pytest.approx(842450.0)
    assert status["objective_return_v129"] == pytest.approx(-547.894547666745)
    assert status["scenario_loss_cvar90_v129"] == pytest.approx(93600.69280130665)
    assert status["source_cap_violations_v129"] == 0
    assert status["delta_return_vs_v127_v129"] == pytest.approx(101.13466517860797)
    assert status["delta_cvar90_vs_v127_v129"] == pytest.approx(92.73225402379467)
    assert status["delta_exposure_vs_v127_v129"] == pytest.approx(0.0)
    assert status["budget_feasible_v129"] is True
    assert status["source_feasible_v129"] is True
    assert status["cvar_feasible_v129"] is True
    assert status["repair_candidate_feasible_v129"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v129"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v129"] is False
    assert status["paper1_promotion_allowed_v129"] is False
    assert status["paper4_working_champion_changed_v129"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v129_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v129",
        "selected_v129",
        "portfolio_label_v129",
        "repair_action_v129",
        "claim_boundary_v129",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v129"]
    assert int(allocations["selected_v129"].sum()) == status["selected_rows_v129"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v129"])
    assert "140960427" in set(allocations["loan_id"].astype(str))
    assert "127108843" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v129"]) == {
        "added_from_v128_best_swap",
        "kept_from_v127",
    }
    assert allocations["claim_boundary_v129"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v129_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v129"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v129"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v129"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v129"])

    action = _read_csv("paper4_v129_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v129"]) == status["added_loan_id_v129"]
    assert str(action_row["dropped_loan_id_v129"]) == status["dropped_loan_id_v129"]
    assert float(action_row["return_delta_v129"]) == pytest.approx(101.13466517860724)
    assert int(action_row["source_cap_violations_after_repair_v129"]) == 0

    source_summary = _read_csv("paper4_v129_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v129",
        "source_slack_v129",
        "source_cap_violated_v129",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v129"]
    assert not source_summary["source_cap_violated_v129"].astype(bool).any()

    blockers = _read_csv("paper4_v129_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v129"], blockers["blocking_v129"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v129"], blockers["evidence_count_v129"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v129_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v129_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v129_repair_candidate_feasible"]) is True
    assert bool(claim_map["v129_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v129_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v129_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v129 twenty-fourth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v129 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v129 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v129: Twenty-Fourth One-Swap Repair Candidate" in notebook
    assert "v130 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v130_post_v129_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v130_status.json")

    assert status["phase"] == "v130_post_v129_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.130"
    assert status["summary_rows_v130"] == 1
    assert status["stage_summary_rows_v130"] == 6
    assert status["candidate_pair_rows_v130"] == 4266
    assert status["top_candidate_rows_v130"] == 200
    assert status["claim_blocker_rows_v130"] == 3
    assert status["selected_rows_v130"] == 171
    assert status["candidate_add_rows_v130"] == 276698
    assert status["total_pair_rows_screened_v130"] == 47315358
    assert status["return_improving_pair_rows_v130"] == 1907873
    assert status["budget_return_feasible_pair_rows_v130"] == 1165150
    assert status["source_prefilter_pair_rows_v130"] == 5041
    assert status["source_exact_pair_rows_v130"] == 4266
    assert status["cvar_feasible_pair_rows_v130"] == 4266
    assert status["one_swap_improving_rows_v130"] == 4266
    assert status["best_one_swap_return_delta_v130"] == pytest.approx(99.93508283527802)
    assert status["best_one_swap_cvar90_after_v130"] == pytest.approx(93640.91093453715)
    assert status["post_repair_one_swap_local_optimality_cleared_v130"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v130"] is False
    assert status["paper1_promotion_allowed_v130"] is False
    assert status["paper4_working_champion_changed_v130"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v130_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v130",
        "dropped_loan_id_v130",
        "return_delta_v130",
        "objective_return_after_swap_v130",
        "budget_swap_feasible_v130",
        "source_swap_feasible_v130",
        "source_cap_violations_after_swap_v130",
        "cvar_swap_feasible_v130",
        "one_swap_improves_return_v130",
        "claim_boundary_v130",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v130"]
    assert probe["return_delta_v130"].gt(0).all()
    assert probe["budget_swap_feasible_v130"].astype(bool).all()
    assert probe["source_swap_feasible_v130"].astype(bool).all()
    assert probe["cvar_swap_feasible_v130"].astype(bool).all()
    assert probe["one_swap_improves_return_v130"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v130"].sum()) == 0
    assert probe["return_delta_v130"].max() == pytest.approx(
        status["best_one_swap_return_delta_v130"]
    )
    assert probe["claim_boundary_v130"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v130_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v130"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v130"]) == "137925780"
    assert str(best["dropped_loan_id_v130"]) == "127561240"
    assert float(best["return_delta_v130"]) == pytest.approx(
        status["best_one_swap_return_delta_v130"]
    )
    assert float(best["exposure_after_swap_v130"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v130"]) is True

    summary = _read_csv("paper4_v130_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v130"]) == status["one_swap_improving_rows_v130"]
    assert float(row["current_exposure_v130"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v130"]) == pytest.approx(-547.894547666745)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v130"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v130"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v130"])

    stage_summary = _read_csv("paper4_v130_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v130", "pair_rows_v130", "claim_boundary_v130"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v130"], stage_summary["pair_rows_v130"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v130"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v130"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v130"]

    blockers = _read_csv("paper4_v130_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v130"], blockers["blocking_v130"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v130"], blockers["evidence_count_v130"], strict=False)
    )
    assert bool(blocker_map["post_v129_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v129_one_swap_improvement_found"]) == 4266
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v130_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v130_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v130_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v130_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v130_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v130 post-v129 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v130 proves the v129 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v130 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v130: Post-v129 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4266`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v131_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v131_status.json")

    assert status["phase"] == "v131_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.131"
    assert status["allocation_rows_v131"] == 171
    assert status["summary_rows_v131"] == 1
    assert status["action_rows_v131"] == 1
    assert status["source_summary_rows_v131"] == 51
    assert status["claim_blocker_rows_v131"] == 4
    assert status["added_loan_id_v131"] == "137925780"
    assert status["dropped_loan_id_v131"] == "127561240"
    assert status["selected_rows_v131"] == 171
    assert status["portfolio_exposure_v131"] == pytest.approx(842450.0)
    assert status["objective_return_v131"] == pytest.approx(-447.95946483147054)
    assert status["scenario_loss_cvar90_v131"] == pytest.approx(93640.91093453715)
    assert status["source_cap_violations_v131"] == 0
    assert status["delta_return_vs_v129_v131"] == pytest.approx(99.93508283527444)
    assert status["delta_cvar90_vs_v129_v131"] == pytest.approx(40.218133230519015)
    assert status["delta_exposure_vs_v129_v131"] == pytest.approx(0.0)
    assert status["budget_feasible_v131"] is True
    assert status["source_feasible_v131"] is True
    assert status["cvar_feasible_v131"] is True
    assert status["repair_candidate_feasible_v131"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v131"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v131"] is False
    assert status["paper1_promotion_allowed_v131"] is False
    assert status["paper4_working_champion_changed_v131"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v131_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v131",
        "selected_v131",
        "portfolio_label_v131",
        "repair_action_v131",
        "claim_boundary_v131",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v131"]
    assert int(allocations["selected_v131"].sum()) == status["selected_rows_v131"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v131"])
    assert "137925780" in set(allocations["loan_id"].astype(str))
    assert "127561240" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v131"]) == {
        "added_from_v130_best_swap",
        "kept_from_v129",
    }
    assert allocations["claim_boundary_v131"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v131_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v131"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v131"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v131"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v131"])

    action = _read_csv("paper4_v131_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v131"]) == status["added_loan_id_v131"]
    assert str(action_row["dropped_loan_id_v131"]) == status["dropped_loan_id_v131"]
    assert float(action_row["return_delta_v131"]) == pytest.approx(99.93508283527802)
    assert int(action_row["source_cap_violations_after_repair_v131"]) == 0

    source_summary = _read_csv("paper4_v131_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v131",
        "source_slack_v131",
        "source_cap_violated_v131",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v131"]
    assert not source_summary["source_cap_violated_v131"].astype(bool).any()

    blockers = _read_csv("paper4_v131_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v131"], blockers["blocking_v131"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v131"], blockers["evidence_count_v131"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v131_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v131_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v131_repair_candidate_feasible"]) is True
    assert bool(claim_map["v131_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v131_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v131_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v131 twenty-fifth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v131 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v131 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v131: Twenty-Fifth One-Swap Repair Candidate" in notebook
    assert "v132 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v132_post_v131_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v132_status.json")

    assert status["phase"] == "v132_post_v131_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.132"
    assert status["summary_rows_v132"] == 1
    assert status["stage_summary_rows_v132"] == 6
    assert status["candidate_pair_rows_v132"] == 4261
    assert status["top_candidate_rows_v132"] == 200
    assert status["claim_blocker_rows_v132"] == 3
    assert status["selected_rows_v132"] == 171
    assert status["candidate_add_rows_v132"] == 276698
    assert status["total_pair_rows_screened_v132"] == 47315358
    assert status["return_improving_pair_rows_v132"] == 1900428
    assert status["budget_return_feasible_pair_rows_v132"] == 1161895
    assert status["source_prefilter_pair_rows_v132"] == 5034
    assert status["source_exact_pair_rows_v132"] == 4261
    assert status["cvar_feasible_pair_rows_v132"] == 4261
    assert status["one_swap_improving_rows_v132"] == 4261
    assert status["best_one_swap_return_delta_v132"] == pytest.approx(93.11511711504707)
    assert status["best_one_swap_cvar90_after_v132"] == pytest.approx(93628.98132751856)
    assert status["post_repair_one_swap_local_optimality_cleared_v132"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v132"] is False
    assert status["paper1_promotion_allowed_v132"] is False
    assert status["paper4_working_champion_changed_v132"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v132_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v132",
        "dropped_loan_id_v132",
        "return_delta_v132",
        "objective_return_after_swap_v132",
        "budget_swap_feasible_v132",
        "source_swap_feasible_v132",
        "source_cap_violations_after_swap_v132",
        "cvar_swap_feasible_v132",
        "one_swap_improves_return_v132",
        "claim_boundary_v132",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v132"]
    assert probe["return_delta_v132"].gt(0).all()
    assert probe["budget_swap_feasible_v132"].astype(bool).all()
    assert probe["source_swap_feasible_v132"].astype(bool).all()
    assert probe["cvar_swap_feasible_v132"].astype(bool).all()
    assert probe["one_swap_improves_return_v132"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v132"].sum()) == 0
    assert probe["return_delta_v132"].max() == pytest.approx(
        status["best_one_swap_return_delta_v132"]
    )
    assert probe["claim_boundary_v132"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v132_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v132"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v132"]) == "164191748"
    assert str(best["dropped_loan_id_v132"]) == "127190641"
    assert float(best["return_delta_v132"]) == pytest.approx(
        status["best_one_swap_return_delta_v132"]
    )
    assert float(best["exposure_after_swap_v132"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v132"]) is True

    summary = _read_csv("paper4_v132_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v132"]) == status["one_swap_improving_rows_v132"]
    assert float(row["current_exposure_v132"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v132"]) == pytest.approx(-447.95946483147054)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v132"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v132"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v132"])

    stage_summary = _read_csv("paper4_v132_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v132", "pair_rows_v132", "claim_boundary_v132"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v132"], stage_summary["pair_rows_v132"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v132"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v132"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v132"]

    blockers = _read_csv("paper4_v132_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v132"], blockers["blocking_v132"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v132"], blockers["evidence_count_v132"], strict=False)
    )
    assert bool(blocker_map["post_v131_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v131_one_swap_improvement_found"]) == 4261
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v132_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v132_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v132_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v132_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v132_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v132 post-v131 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v132 proves the v131 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v132 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v132: Post-v131 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4261`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v133_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v133_status.json")

    assert status["phase"] == "v133_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.133"
    assert status["allocation_rows_v133"] == 171
    assert status["summary_rows_v133"] == 1
    assert status["action_rows_v133"] == 1
    assert status["source_summary_rows_v133"] == 51
    assert status["claim_blocker_rows_v133"] == 4
    assert status["added_loan_id_v133"] == "164191748"
    assert status["dropped_loan_id_v133"] == "127190641"
    assert status["selected_rows_v133"] == 171
    assert status["portfolio_exposure_v133"] == pytest.approx(842450.0)
    assert status["objective_return_v133"] == pytest.approx(-354.8443477164192)
    assert status["scenario_loss_cvar90_v133"] == pytest.approx(93628.98132751856)
    assert status["source_cap_violations_v133"] == 0
    assert status["delta_return_vs_v131_v133"] == pytest.approx(93.11511711505136)
    assert status["delta_cvar90_vs_v131_v133"] == pytest.approx(-11.929607018595561)
    assert status["delta_exposure_vs_v131_v133"] == pytest.approx(0.0)
    assert status["budget_feasible_v133"] is True
    assert status["source_feasible_v133"] is True
    assert status["cvar_feasible_v133"] is True
    assert status["repair_candidate_feasible_v133"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v133"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v133"] is False
    assert status["paper1_promotion_allowed_v133"] is False
    assert status["paper4_working_champion_changed_v133"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v133_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v133",
        "selected_v133",
        "portfolio_label_v133",
        "repair_action_v133",
        "claim_boundary_v133",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v133"]
    assert int(allocations["selected_v133"].sum()) == status["selected_rows_v133"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v133"])
    assert "164191748" in set(allocations["loan_id"].astype(str))
    assert "127190641" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v133"]) == {
        "added_from_v132_best_swap",
        "kept_from_v131",
    }
    assert allocations["claim_boundary_v133"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v133_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v133"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v133"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v133"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v133"])

    action = _read_csv("paper4_v133_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v133"]) == status["added_loan_id_v133"]
    assert str(action_row["dropped_loan_id_v133"]) == status["dropped_loan_id_v133"]
    assert float(action_row["return_delta_v133"]) == pytest.approx(93.11511711504708)
    assert int(action_row["source_cap_violations_after_repair_v133"]) == 0

    source_summary = _read_csv("paper4_v133_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v133",
        "source_slack_v133",
        "source_cap_violated_v133",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v133"]
    assert not source_summary["source_cap_violated_v133"].astype(bool).any()

    blockers = _read_csv("paper4_v133_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v133"], blockers["blocking_v133"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v133"], blockers["evidence_count_v133"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v133_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v133_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v133_repair_candidate_feasible"]) is True
    assert bool(claim_map["v133_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v133_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v133_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v133 twenty-sixth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v133 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v133 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v133: Twenty-Sixth One-Swap Repair Candidate" in notebook
    assert "v134 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v134_post_v133_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v134_status.json")

    assert status["phase"] == "v134_post_v133_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.134"
    assert status["summary_rows_v134"] == 1
    assert status["stage_summary_rows_v134"] == 6
    assert status["candidate_pair_rows_v134"] == 3929
    assert status["top_candidate_rows_v134"] == 200
    assert status["claim_blocker_rows_v134"] == 3
    assert status["selected_rows_v134"] == 171
    assert status["candidate_add_rows_v134"] == 276698
    assert status["total_pair_rows_screened_v134"] == 47315358
    assert status["return_improving_pair_rows_v134"] == 1893951
    assert status["budget_return_feasible_pair_rows_v134"] == 1157656
    assert status["source_prefilter_pair_rows_v134"] == 4702
    assert status["source_exact_pair_rows_v134"] == 3929
    assert status["cvar_feasible_pair_rows_v134"] == 3929
    assert status["one_swap_improving_rows_v134"] == 3929
    assert status["best_one_swap_return_delta_v134"] == pytest.approx(92.97979012658101)
    assert status["best_one_swap_cvar90_after_v134"] == pytest.approx(93837.71761179007)
    assert status["post_repair_one_swap_local_optimality_cleared_v134"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v134"] is False
    assert status["paper1_promotion_allowed_v134"] is False
    assert status["paper4_working_champion_changed_v134"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v134_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v134",
        "dropped_loan_id_v134",
        "return_delta_v134",
        "objective_return_after_swap_v134",
        "budget_swap_feasible_v134",
        "source_swap_feasible_v134",
        "source_cap_violations_after_swap_v134",
        "cvar_swap_feasible_v134",
        "one_swap_improves_return_v134",
        "claim_boundary_v134",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v134"]
    assert probe["return_delta_v134"].gt(0).all()
    assert probe["budget_swap_feasible_v134"].astype(bool).all()
    assert probe["source_swap_feasible_v134"].astype(bool).all()
    assert probe["cvar_swap_feasible_v134"].astype(bool).all()
    assert probe["one_swap_improves_return_v134"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v134"].sum()) == 0
    assert probe["return_delta_v134"].max() == pytest.approx(
        status["best_one_swap_return_delta_v134"]
    )
    assert probe["claim_boundary_v134"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v134_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v134"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v134"]) == "152337011"
    assert str(best["dropped_loan_id_v134"]) == "127875030"
    assert float(best["return_delta_v134"]) == pytest.approx(
        status["best_one_swap_return_delta_v134"]
    )
    assert float(best["exposure_after_swap_v134"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v134"]) is True

    summary = _read_csv("paper4_v134_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v134"]) == status["one_swap_improving_rows_v134"]
    assert float(row["current_exposure_v134"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v134"]) == pytest.approx(-354.8443477164192)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v134"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v134"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v134"])

    stage_summary = _read_csv("paper4_v134_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v134", "pair_rows_v134", "claim_boundary_v134"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v134"], stage_summary["pair_rows_v134"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v134"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v134"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v134"]

    blockers = _read_csv("paper4_v134_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v134"], blockers["blocking_v134"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v134"], blockers["evidence_count_v134"], strict=False)
    )
    assert bool(blocker_map["post_v133_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v133_one_swap_improvement_found"]) == 3929
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v134_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v134_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v134_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v134_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v134_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v134 post-v133 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v134 proves the v133 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v134 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v134: Post-v133 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3929`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v135_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v135_status.json")

    assert status["phase"] == "v135_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.135"
    assert status["allocation_rows_v135"] == 171
    assert status["summary_rows_v135"] == 1
    assert status["action_rows_v135"] == 1
    assert status["source_summary_rows_v135"] == 51
    assert status["claim_blocker_rows_v135"] == 4
    assert status["added_loan_id_v135"] == "152337011"
    assert status["dropped_loan_id_v135"] == "127875030"
    assert status["selected_rows_v135"] == 171
    assert status["portfolio_exposure_v135"] == pytest.approx(842450.0)
    assert status["objective_return_v135"] == pytest.approx(-261.864557589839)
    assert status["scenario_loss_cvar90_v135"] == pytest.approx(93837.71761179007)
    assert status["source_cap_violations_v135"] == 0
    assert status["delta_return_vs_v133_v135"] == pytest.approx(92.97979012658016)
    assert status["delta_cvar90_vs_v133_v135"] == pytest.approx(208.73628427150834)
    assert status["delta_exposure_vs_v133_v135"] == pytest.approx(0.0)
    assert status["budget_feasible_v135"] is True
    assert status["source_feasible_v135"] is True
    assert status["cvar_feasible_v135"] is True
    assert status["repair_candidate_feasible_v135"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v135"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v135"] is False
    assert status["paper1_promotion_allowed_v135"] is False
    assert status["paper4_working_champion_changed_v135"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v135_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v135",
        "selected_v135",
        "portfolio_label_v135",
        "repair_action_v135",
        "claim_boundary_v135",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v135"]
    assert int(allocations["selected_v135"].sum()) == status["selected_rows_v135"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v135"])
    assert "152337011" in set(allocations["loan_id"].astype(str))
    assert "127875030" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v135"]) == {
        "added_from_v134_best_swap",
        "kept_from_v133",
    }
    assert allocations["claim_boundary_v135"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v135_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v135"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v135"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v135"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v135"])

    action = _read_csv("paper4_v135_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v135"]) == status["added_loan_id_v135"]
    assert str(action_row["dropped_loan_id_v135"]) == status["dropped_loan_id_v135"]
    assert float(action_row["return_delta_v135"]) == pytest.approx(92.979790126581)
    assert int(action_row["source_cap_violations_after_repair_v135"]) == 0

    source_summary = _read_csv("paper4_v135_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v135",
        "source_slack_v135",
        "source_cap_violated_v135",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v135"]
    assert not source_summary["source_cap_violated_v135"].astype(bool).any()

    blockers = _read_csv("paper4_v135_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v135"], blockers["blocking_v135"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v135"], blockers["evidence_count_v135"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v135_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v135_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v135_repair_candidate_feasible"]) is True
    assert bool(claim_map["v135_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v135_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v135_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v135 twenty-seventh one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v135 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v135 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v135: Twenty-Seventh One-Swap Repair Candidate" in notebook
    assert "v136 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v136_post_v135_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v136_status.json")

    assert status["phase"] == "v136_post_v135_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.136"
    assert status["summary_rows_v136"] == 1
    assert status["stage_summary_rows_v136"] == 6
    assert status["candidate_pair_rows_v136"] == 3807
    assert status["top_candidate_rows_v136"] == 200
    assert status["claim_blocker_rows_v136"] == 3
    assert status["selected_rows_v136"] == 171
    assert status["candidate_add_rows_v136"] == 276698
    assert status["total_pair_rows_screened_v136"] == 47315358
    assert status["return_improving_pair_rows_v136"] == 1886684
    assert status["budget_return_feasible_pair_rows_v136"] == 1154382
    assert status["source_prefilter_pair_rows_v136"] == 4542
    assert status["source_exact_pair_rows_v136"] == 3807
    assert status["cvar_feasible_pair_rows_v136"] == 3807
    assert status["one_swap_improving_rows_v136"] == 3807
    assert status["best_one_swap_return_delta_v136"] == pytest.approx(92.38723628164689)
    assert status["best_one_swap_cvar90_after_v136"] == pytest.approx(93943.3075895581)
    assert status["post_repair_one_swap_local_optimality_cleared_v136"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v136"] is False
    assert status["paper1_promotion_allowed_v136"] is False
    assert status["paper4_working_champion_changed_v136"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v136_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v136",
        "dropped_loan_id_v136",
        "return_delta_v136",
        "objective_return_after_swap_v136",
        "budget_swap_feasible_v136",
        "source_swap_feasible_v136",
        "source_cap_violations_after_swap_v136",
        "cvar_swap_feasible_v136",
        "one_swap_improves_return_v136",
        "claim_boundary_v136",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v136"]
    assert probe["return_delta_v136"].gt(0).all()
    assert probe["budget_swap_feasible_v136"].astype(bool).all()
    assert probe["source_swap_feasible_v136"].astype(bool).all()
    assert probe["cvar_swap_feasible_v136"].astype(bool).all()
    assert probe["one_swap_improves_return_v136"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v136"].sum()) == 0
    assert probe["return_delta_v136"].max() == pytest.approx(
        status["best_one_swap_return_delta_v136"]
    )
    assert probe["claim_boundary_v136"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v136_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v136"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v136"]) == "143784342"
    assert str(best["dropped_loan_id_v136"]) == "126425436"
    assert float(best["return_delta_v136"]) == pytest.approx(
        status["best_one_swap_return_delta_v136"]
    )
    assert float(best["exposure_after_swap_v136"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v136"]) is True

    summary = _read_csv("paper4_v136_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v136"]) == status["one_swap_improving_rows_v136"]
    assert float(row["current_exposure_v136"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v136"]) == pytest.approx(-261.864557589839)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v136"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v136"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v136"])

    stage_summary = _read_csv("paper4_v136_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v136", "pair_rows_v136", "claim_boundary_v136"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v136"], stage_summary["pair_rows_v136"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v136"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v136"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v136"]

    blockers = _read_csv("paper4_v136_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v136"], blockers["blocking_v136"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v136"], blockers["evidence_count_v136"], strict=False)
    )
    assert bool(blocker_map["post_v135_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v135_one_swap_improvement_found"]) == 3807
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v136_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v136_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v136_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v136_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v136_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v136 post-v135 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v136 proves the v135 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v136 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v136: Post-v135 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3807`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v137_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v137_status.json")

    assert status["phase"] == "v137_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.137"
    assert status["allocation_rows_v137"] == 171
    assert status["summary_rows_v137"] == 1
    assert status["action_rows_v137"] == 1
    assert status["source_summary_rows_v137"] == 51
    assert status["claim_blocker_rows_v137"] == 4
    assert status["added_loan_id_v137"] == "143784342"
    assert status["dropped_loan_id_v137"] == "126425436"
    assert status["selected_rows_v137"] == 171
    assert status["portfolio_exposure_v137"] == pytest.approx(842450.0)
    assert status["objective_return_v137"] == pytest.approx(-169.47732130819077)
    assert status["scenario_loss_cvar90_v137"] == pytest.approx(93943.3075895581)
    assert status["source_cap_violations_v137"] == 0
    assert status["delta_return_vs_v135_v137"] == pytest.approx(92.38723628164826)
    assert status["delta_cvar90_vs_v135_v137"] == pytest.approx(105.58997776801698)
    assert status["delta_exposure_vs_v135_v137"] == pytest.approx(0.0)
    assert status["budget_feasible_v137"] is True
    assert status["source_feasible_v137"] is True
    assert status["cvar_feasible_v137"] is True
    assert status["repair_candidate_feasible_v137"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v137"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v137"] is False
    assert status["paper1_promotion_allowed_v137"] is False
    assert status["paper4_working_champion_changed_v137"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v137_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v137",
        "selected_v137",
        "portfolio_label_v137",
        "repair_action_v137",
        "claim_boundary_v137",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v137"]
    assert int(allocations["selected_v137"].sum()) == status["selected_rows_v137"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v137"])
    assert "143784342" in set(allocations["loan_id"].astype(str))
    assert "126425436" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v137"]) == {
        "added_from_v136_best_swap",
        "kept_from_v135",
    }
    assert allocations["claim_boundary_v137"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v137_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v137"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v137"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v137"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v137"])

    action = _read_csv("paper4_v137_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v137"]) == status["added_loan_id_v137"]
    assert str(action_row["dropped_loan_id_v137"]) == status["dropped_loan_id_v137"]
    assert float(action_row["return_delta_v137"]) == pytest.approx(92.38723628164688)
    assert int(action_row["source_cap_violations_after_repair_v137"]) == 0

    source_summary = _read_csv("paper4_v137_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v137",
        "source_slack_v137",
        "source_cap_violated_v137",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v137"]
    assert not source_summary["source_cap_violated_v137"].astype(bool).any()

    blockers = _read_csv("paper4_v137_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v137"], blockers["blocking_v137"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v137"], blockers["evidence_count_v137"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v137_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v137_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v137_repair_candidate_feasible"]) is True
    assert bool(claim_map["v137_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v137_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v137_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v137 twenty-eighth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v137 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v137 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v137: Twenty-Eighth One-Swap Repair Candidate" in notebook
    assert "v138 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v138_post_v137_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v138_status.json")

    assert status["phase"] == "v138_post_v137_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.138"
    assert status["summary_rows_v138"] == 1
    assert status["stage_summary_rows_v138"] == 6
    assert status["candidate_pair_rows_v138"] == 3482
    assert status["top_candidate_rows_v138"] == 200
    assert status["claim_blocker_rows_v138"] == 3
    assert status["selected_rows_v138"] == 171
    assert status["candidate_add_rows_v138"] == 276698
    assert status["total_pair_rows_screened_v138"] == 47315358
    assert status["return_improving_pair_rows_v138"] == 1880271
    assert status["budget_return_feasible_pair_rows_v138"] == 1150188
    assert status["source_prefilter_pair_rows_v138"] == 4217
    assert status["source_exact_pair_rows_v138"] == 3482
    assert status["cvar_feasible_pair_rows_v138"] == 3482
    assert status["one_swap_improving_rows_v138"] == 3482
    assert status["best_one_swap_return_delta_v138"] == pytest.approx(89.71082424435784)
    assert status["best_one_swap_cvar90_after_v138"] == pytest.approx(94148.70501264239)
    assert status["post_repair_one_swap_local_optimality_cleared_v138"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v138"] is False
    assert status["paper1_promotion_allowed_v138"] is False
    assert status["paper4_working_champion_changed_v138"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v138_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v138",
        "dropped_loan_id_v138",
        "return_delta_v138",
        "objective_return_after_swap_v138",
        "budget_swap_feasible_v138",
        "source_swap_feasible_v138",
        "source_cap_violations_after_swap_v138",
        "cvar_swap_feasible_v138",
        "one_swap_improves_return_v138",
        "claim_boundary_v138",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v138"]
    assert probe["return_delta_v138"].gt(0).all()
    assert probe["budget_swap_feasible_v138"].astype(bool).all()
    assert probe["source_swap_feasible_v138"].astype(bool).all()
    assert probe["cvar_swap_feasible_v138"].astype(bool).all()
    assert probe["one_swap_improves_return_v138"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v138"].sum()) == 0
    assert probe["return_delta_v138"].max() == pytest.approx(
        status["best_one_swap_return_delta_v138"]
    )
    assert probe["claim_boundary_v138"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v138_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v138"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v138"]) == "143127376"
    assert str(best["dropped_loan_id_v138"]) == "127409242"
    assert float(best["return_delta_v138"]) == pytest.approx(
        status["best_one_swap_return_delta_v138"]
    )
    assert float(best["exposure_after_swap_v138"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v138"]) is True

    summary = _read_csv("paper4_v138_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v138"]) == status["one_swap_improving_rows_v138"]
    assert float(row["current_exposure_v138"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v138"]) == pytest.approx(-169.47732130819077)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v138"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v138"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v138"])

    stage_summary = _read_csv("paper4_v138_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v138", "pair_rows_v138", "claim_boundary_v138"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v138"], stage_summary["pair_rows_v138"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v138"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v138"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v138"]

    blockers = _read_csv("paper4_v138_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v138"], blockers["blocking_v138"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v138"], blockers["evidence_count_v138"], strict=False)
    )
    assert bool(blocker_map["post_v137_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v137_one_swap_improvement_found"]) == 3482
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v138_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v138_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v138_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v138_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v138_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v138 post-v137 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v138 proves the v137 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v138 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v138: Post-v137 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3482`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v139_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v139_status.json")

    assert status["phase"] == "v139_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.139"
    assert status["allocation_rows_v139"] == 171
    assert status["summary_rows_v139"] == 1
    assert status["action_rows_v139"] == 1
    assert status["source_summary_rows_v139"] == 51
    assert status["claim_blocker_rows_v139"] == 4
    assert status["added_loan_id_v139"] == "143127376"
    assert status["dropped_loan_id_v139"] == "127409242"
    assert status["selected_rows_v139"] == 171
    assert status["portfolio_exposure_v139"] == pytest.approx(842450.0)
    assert status["objective_return_v139"] == pytest.approx(-79.76649706383432)
    assert status["scenario_loss_cvar90_v139"] == pytest.approx(94148.70501264239)
    assert status["source_cap_violations_v139"] == 0
    assert status["delta_return_vs_v137_v139"] == pytest.approx(89.71082424435644)
    assert status["delta_cvar90_vs_v137_v139"] == pytest.approx(205.39742308428686)
    assert status["delta_exposure_vs_v137_v139"] == pytest.approx(0.0)
    assert status["budget_feasible_v139"] is True
    assert status["source_feasible_v139"] is True
    assert status["cvar_feasible_v139"] is True
    assert status["repair_candidate_feasible_v139"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v139"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v139"] is False
    assert status["paper1_promotion_allowed_v139"] is False
    assert status["paper4_working_champion_changed_v139"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v139_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v139",
        "selected_v139",
        "portfolio_label_v139",
        "repair_action_v139",
        "claim_boundary_v139",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v139"]
    assert int(allocations["selected_v139"].sum()) == status["selected_rows_v139"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v139"])
    assert "143127376" in set(allocations["loan_id"].astype(str))
    assert "127409242" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v139"]) == {
        "added_from_v138_best_swap",
        "kept_from_v137",
    }
    assert allocations["claim_boundary_v139"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v139_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v139"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v139"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v139"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v139"])

    action = _read_csv("paper4_v139_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v139"]) == status["added_loan_id_v139"]
    assert str(action_row["dropped_loan_id_v139"]) == status["dropped_loan_id_v139"]
    assert float(action_row["return_delta_v139"]) == pytest.approx(89.71082424435784)
    assert int(action_row["source_cap_violations_after_repair_v139"]) == 0

    source_summary = _read_csv("paper4_v139_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v139",
        "source_slack_v139",
        "source_cap_violated_v139",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v139"]
    assert not source_summary["source_cap_violated_v139"].astype(bool).any()

    blockers = _read_csv("paper4_v139_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v139"], blockers["blocking_v139"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v139"], blockers["evidence_count_v139"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v139_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v139_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v139_repair_candidate_feasible"]) is True
    assert bool(claim_map["v139_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v139_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v139_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v139 twenty-ninth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v139 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v139 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v139: Twenty-Ninth One-Swap Repair Candidate" in notebook
    assert "v140 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v140_post_v139_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v140_status.json")

    assert status["phase"] == "v140_post_v139_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.140"
    assert status["summary_rows_v140"] == 1
    assert status["stage_summary_rows_v140"] == 6
    assert status["candidate_pair_rows_v140"] == 3181
    assert status["top_candidate_rows_v140"] == 200
    assert status["claim_blocker_rows_v140"] == 3
    assert status["selected_rows_v140"] == 171
    assert status["candidate_add_rows_v140"] == 276698
    assert status["total_pair_rows_screened_v140"] == 47315358
    assert status["return_improving_pair_rows_v140"] == 1874065
    assert status["budget_return_feasible_pair_rows_v140"] == 1146139
    assert status["source_prefilter_pair_rows_v140"] == 3916
    assert status["source_exact_pair_rows_v140"] == 3181
    assert status["cvar_feasible_pair_rows_v140"] == 3181
    assert status["one_swap_improving_rows_v140"] == 3181
    assert status["best_one_swap_return_delta_v140"] == pytest.approx(87.47095956261964)
    assert status["best_one_swap_cvar90_after_v140"] == pytest.approx(94116.39079886669)
    assert status["post_repair_one_swap_local_optimality_cleared_v140"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v140"] is False
    assert status["paper1_promotion_allowed_v140"] is False
    assert status["paper4_working_champion_changed_v140"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v140_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v140",
        "dropped_loan_id_v140",
        "return_delta_v140",
        "objective_return_after_swap_v140",
        "budget_swap_feasible_v140",
        "source_swap_feasible_v140",
        "source_cap_violations_after_swap_v140",
        "cvar_swap_feasible_v140",
        "one_swap_improves_return_v140",
        "claim_boundary_v140",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v140"]
    assert probe["return_delta_v140"].gt(0).all()
    assert probe["budget_swap_feasible_v140"].astype(bool).all()
    assert probe["source_swap_feasible_v140"].astype(bool).all()
    assert probe["cvar_swap_feasible_v140"].astype(bool).all()
    assert probe["one_swap_improves_return_v140"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v140"].sum()) == 0
    assert probe["return_delta_v140"].max() == pytest.approx(
        status["best_one_swap_return_delta_v140"]
    )
    assert probe["claim_boundary_v140"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v140_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v140"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v140"]) == "139102238"
    assert str(best["dropped_loan_id_v140"]) == "126788147"
    assert float(best["return_delta_v140"]) == pytest.approx(
        status["best_one_swap_return_delta_v140"]
    )
    assert float(best["exposure_after_swap_v140"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v140"]) is True

    summary = _read_csv("paper4_v140_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v140"]) == status["one_swap_improving_rows_v140"]
    assert float(row["current_exposure_v140"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v140"]) == pytest.approx(-79.76649706383432)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v140"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v140"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v140"])

    stage_summary = _read_csv("paper4_v140_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v140", "pair_rows_v140", "claim_boundary_v140"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v140"], stage_summary["pair_rows_v140"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v140"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v140"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v140"]

    blockers = _read_csv("paper4_v140_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v140"], blockers["blocking_v140"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v140"], blockers["evidence_count_v140"], strict=False)
    )
    assert bool(blocker_map["post_v139_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v139_one_swap_improvement_found"]) == 3181
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v140_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v140_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v140_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v140_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v140_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v140 post-v139 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v140 proves the v139 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v140 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v140: Post-v139 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3181`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v141_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v141_status.json")

    assert status["phase"] == "v141_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.141"
    assert status["allocation_rows_v141"] == 171
    assert status["summary_rows_v141"] == 1
    assert status["action_rows_v141"] == 1
    assert status["source_summary_rows_v141"] == 51
    assert status["claim_blocker_rows_v141"] == 4
    assert status["added_loan_id_v141"] == "139102238"
    assert status["dropped_loan_id_v141"] == "126788147"
    assert status["selected_rows_v141"] == 171
    assert status["portfolio_exposure_v141"] == pytest.approx(842450.0)
    assert status["objective_return_v141"] == pytest.approx(7.704462498784778)
    assert status["scenario_loss_cvar90_v141"] == pytest.approx(94116.39079886672)
    assert status["source_cap_violations_v141"] == 0
    assert status["delta_return_vs_v139_v141"] == pytest.approx(87.4709595626191)
    assert status["delta_cvar90_vs_v139_v141"] == pytest.approx(-32.3142137756804)
    assert status["delta_exposure_vs_v139_v141"] == pytest.approx(0.0)
    assert status["budget_feasible_v141"] is True
    assert status["source_feasible_v141"] is True
    assert status["cvar_feasible_v141"] is True
    assert status["repair_candidate_feasible_v141"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v141"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v141"] is False
    assert status["paper1_promotion_allowed_v141"] is False
    assert status["paper4_working_champion_changed_v141"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v141_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v141",
        "selected_v141",
        "portfolio_label_v141",
        "repair_action_v141",
        "claim_boundary_v141",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v141"]
    assert int(allocations["selected_v141"].sum()) == status["selected_rows_v141"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v141"])
    assert "139102238" in set(allocations["loan_id"].astype(str))
    assert "126788147" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v141"]) == {
        "added_from_v140_best_swap",
        "kept_from_v139",
    }
    assert allocations["claim_boundary_v141"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v141_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v141"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v141"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v141"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v141"])

    action = _read_csv("paper4_v141_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v141"]) == status["added_loan_id_v141"]
    assert str(action_row["dropped_loan_id_v141"]) == status["dropped_loan_id_v141"]
    assert float(action_row["return_delta_v141"]) == pytest.approx(87.47095956261964)
    assert int(action_row["source_cap_violations_after_repair_v141"]) == 0

    source_summary = _read_csv("paper4_v141_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v141",
        "source_slack_v141",
        "source_cap_violated_v141",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v141"]
    assert not source_summary["source_cap_violated_v141"].astype(bool).any()

    blockers = _read_csv("paper4_v141_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v141"], blockers["blocking_v141"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v141"], blockers["evidence_count_v141"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v141_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v141_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v141_repair_candidate_feasible"]) is True
    assert bool(claim_map["v141_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v141_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v141_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v141 thirtieth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v141 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v141 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v141: Thirtieth One-Swap Repair Candidate" in notebook
    assert "v142 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v142_post_v141_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v142_status.json")

    assert status["phase"] == "v142_post_v141_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.142"
    assert status["summary_rows_v142"] == 1
    assert status["stage_summary_rows_v142"] == 6
    assert status["candidate_pair_rows_v142"] == 3177
    assert status["top_candidate_rows_v142"] == 200
    assert status["claim_blocker_rows_v142"] == 3
    assert status["selected_rows_v142"] == 171
    assert status["candidate_add_rows_v142"] == 276698
    assert status["total_pair_rows_screened_v142"] == 47315358
    assert status["return_improving_pair_rows_v142"] == 1867449
    assert status["budget_return_feasible_pair_rows_v142"] == 1142750
    assert status["source_prefilter_pair_rows_v142"] == 3912
    assert status["source_exact_pair_rows_v142"] == 3177
    assert status["cvar_feasible_pair_rows_v142"] == 3177
    assert status["one_swap_improving_rows_v142"] == 3177
    assert status["best_one_swap_return_delta_v142"] == pytest.approx(84.91258805462799)
    assert status["best_one_swap_cvar90_after_v142"] == pytest.approx(94043.11004841489)
    assert status["post_repair_one_swap_local_optimality_cleared_v142"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v142"] is False
    assert status["paper1_promotion_allowed_v142"] is False
    assert status["paper4_working_champion_changed_v142"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v142_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v142",
        "dropped_loan_id_v142",
        "return_delta_v142",
        "objective_return_after_swap_v142",
        "budget_swap_feasible_v142",
        "source_swap_feasible_v142",
        "source_cap_violations_after_swap_v142",
        "cvar_swap_feasible_v142",
        "one_swap_improves_return_v142",
        "claim_boundary_v142",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v142"]
    assert probe["return_delta_v142"].gt(0).all()
    assert probe["budget_swap_feasible_v142"].astype(bool).all()
    assert probe["source_swap_feasible_v142"].astype(bool).all()
    assert probe["cvar_swap_feasible_v142"].astype(bool).all()
    assert probe["one_swap_improves_return_v142"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v142"].sum()) == 0
    assert probe["return_delta_v142"].max() == pytest.approx(
        status["best_one_swap_return_delta_v142"]
    )
    assert probe["claim_boundary_v142"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v142_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v142"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v142"]) == "141895475"
    assert str(best["dropped_loan_id_v142"]) == "126844634"
    assert float(best["return_delta_v142"]) == pytest.approx(
        status["best_one_swap_return_delta_v142"]
    )
    assert float(best["exposure_after_swap_v142"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v142"]) is True

    summary = _read_csv("paper4_v142_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v142"]) == status["one_swap_improving_rows_v142"]
    assert float(row["current_exposure_v142"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v142"]) == pytest.approx(7.704462498784778)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v142"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v142"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v142"])

    stage_summary = _read_csv("paper4_v142_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v142", "pair_rows_v142", "claim_boundary_v142"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v142"], stage_summary["pair_rows_v142"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v142"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v142"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v142"]

    blockers = _read_csv("paper4_v142_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v142"], blockers["blocking_v142"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v142"], blockers["evidence_count_v142"], strict=False)
    )
    assert bool(blocker_map["post_v141_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v141_one_swap_improvement_found"]) == 3177
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v142_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v142_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v142_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v142_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v142_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v142 post-v141 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v142 proves the v141 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v142 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v142: Post-v141 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3177`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v143_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v143_status.json")

    assert status["phase"] == "v143_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.143"
    assert status["allocation_rows_v143"] == 171
    assert status["summary_rows_v143"] == 1
    assert status["action_rows_v143"] == 1
    assert status["source_summary_rows_v143"] == 51
    assert status["claim_blocker_rows_v143"] == 4
    assert status["added_loan_id_v143"] == "141895475"
    assert status["dropped_loan_id_v143"] == "126844634"
    assert status["selected_rows_v143"] == 171
    assert status["portfolio_exposure_v143"] == pytest.approx(842450.0)
    assert status["objective_return_v143"] == pytest.approx(92.61705055341372)
    assert status["scenario_loss_cvar90_v143"] == pytest.approx(94043.11004841488)
    assert status["source_cap_violations_v143"] == 0
    assert status["delta_return_vs_v141_v143"] == pytest.approx(84.91258805462894)
    assert status["delta_cvar90_vs_v141_v143"] == pytest.approx(-73.28075045184232)
    assert status["delta_exposure_vs_v141_v143"] == pytest.approx(0.0)
    assert status["budget_feasible_v143"] is True
    assert status["source_feasible_v143"] is True
    assert status["cvar_feasible_v143"] is True
    assert status["repair_candidate_feasible_v143"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v143"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v143"] is False
    assert status["paper1_promotion_allowed_v143"] is False
    assert status["paper4_working_champion_changed_v143"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v143_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v143",
        "selected_v143",
        "portfolio_label_v143",
        "repair_action_v143",
        "claim_boundary_v143",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v143"]
    assert int(allocations["selected_v143"].sum()) == status["selected_rows_v143"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v143"])
    assert "141895475" in set(allocations["loan_id"].astype(str))
    assert "126844634" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v143"]) == {
        "added_from_v142_best_swap",
        "kept_from_v141",
    }
    assert allocations["claim_boundary_v143"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v143_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v143"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v143"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v143"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v143"])

    action = _read_csv("paper4_v143_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v143"]) == status["added_loan_id_v143"]
    assert str(action_row["dropped_loan_id_v143"]) == status["dropped_loan_id_v143"]
    assert float(action_row["return_delta_v143"]) == pytest.approx(84.91258805462799)
    assert int(action_row["source_cap_violations_after_repair_v143"]) == 0

    source_summary = _read_csv("paper4_v143_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v143",
        "source_slack_v143",
        "source_cap_violated_v143",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v143"]
    assert not source_summary["source_cap_violated_v143"].astype(bool).any()

    blockers = _read_csv("paper4_v143_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v143"], blockers["blocking_v143"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v143"], blockers["evidence_count_v143"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v143_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v143_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v143_repair_candidate_feasible"]) is True
    assert bool(claim_map["v143_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v143_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v143_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v143 thirty-first one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v143 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v143 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v143: Thirty-First One-Swap Repair Candidate" in notebook
    assert "v144 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v144_post_v143_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v144_status.json")

    assert status["phase"] == "v144_post_v143_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.144"
    assert status["summary_rows_v144"] == 1
    assert status["stage_summary_rows_v144"] == 6
    assert status["candidate_pair_rows_v144"] == 3076
    assert status["top_candidate_rows_v144"] == 200
    assert status["claim_blocker_rows_v144"] == 3
    assert status["selected_rows_v144"] == 171
    assert status["candidate_add_rows_v144"] == 276698
    assert status["total_pair_rows_screened_v144"] == 47315358
    assert status["return_improving_pair_rows_v144"] == 1861729
    assert status["budget_return_feasible_pair_rows_v144"] == 1138759
    assert status["source_prefilter_pair_rows_v144"] == 3811
    assert status["source_exact_pair_rows_v144"] == 3076
    assert status["cvar_feasible_pair_rows_v144"] == 3076
    assert status["one_swap_improving_rows_v144"] == 3076
    assert status["best_one_swap_return_delta_v144"] == pytest.approx(82.33186597435457)
    assert status["best_one_swap_cvar90_after_v144"] == pytest.approx(94205.09531368258)
    assert status["post_repair_one_swap_local_optimality_cleared_v144"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v144"] is False
    assert status["paper1_promotion_allowed_v144"] is False
    assert status["paper4_working_champion_changed_v144"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v144_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v144",
        "dropped_loan_id_v144",
        "return_delta_v144",
        "objective_return_after_swap_v144",
        "budget_swap_feasible_v144",
        "source_swap_feasible_v144",
        "source_cap_violations_after_swap_v144",
        "cvar_swap_feasible_v144",
        "one_swap_improves_return_v144",
        "claim_boundary_v144",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v144"]
    assert probe["return_delta_v144"].gt(0).all()
    assert probe["budget_swap_feasible_v144"].astype(bool).all()
    assert probe["source_swap_feasible_v144"].astype(bool).all()
    assert probe["cvar_swap_feasible_v144"].astype(bool).all()
    assert probe["one_swap_improves_return_v144"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v144"].sum()) == 0
    assert probe["return_delta_v144"].max() == pytest.approx(
        status["best_one_swap_return_delta_v144"]
    )
    assert probe["claim_boundary_v144"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v144_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v144"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v144"]) == "162406873"
    assert str(best["dropped_loan_id_v144"]) == "127178217"
    assert float(best["return_delta_v144"]) == pytest.approx(
        status["best_one_swap_return_delta_v144"]
    )
    assert float(best["exposure_after_swap_v144"]) == pytest.approx(842550.0)
    assert bool(best["one_swap_improves_return_v144"]) is True

    summary = _read_csv("paper4_v144_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v144"]) == status["one_swap_improving_rows_v144"]
    assert float(row["current_exposure_v144"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v144"]) == pytest.approx(92.61705055341372)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v144"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v144"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v144"])

    stage_summary = _read_csv("paper4_v144_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v144", "pair_rows_v144", "claim_boundary_v144"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v144"], stage_summary["pair_rows_v144"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v144"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v144"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v144"]

    blockers = _read_csv("paper4_v144_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v144"], blockers["blocking_v144"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v144"], blockers["evidence_count_v144"], strict=False)
    )
    assert bool(blocker_map["post_v143_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v143_one_swap_improvement_found"]) == 3076
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v144_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v144_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v144_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v144_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v144_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v144 post-v143 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v144 proves the v143 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v144 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v144: Post-v143 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3076`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v145_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v145_status.json")

    assert status["phase"] == "v145_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.145"
    assert status["allocation_rows_v145"] == 171
    assert status["summary_rows_v145"] == 1
    assert status["action_rows_v145"] == 1
    assert status["source_summary_rows_v145"] == 51
    assert status["claim_blocker_rows_v145"] == 4
    assert status["added_loan_id_v145"] == "162406873"
    assert status["dropped_loan_id_v145"] == "127178217"
    assert status["selected_rows_v145"] == 171
    assert status["portfolio_exposure_v145"] == pytest.approx(842550.0)
    assert status["objective_return_v145"] == pytest.approx(174.94891652776732)
    assert status["scenario_loss_cvar90_v145"] == pytest.approx(94205.09531368258)
    assert status["source_cap_violations_v145"] == 0
    assert status["delta_return_vs_v143_v145"] == pytest.approx(82.3318659743536)
    assert status["delta_cvar90_vs_v143_v145"] == pytest.approx(161.9852652677073)
    assert status["delta_exposure_vs_v143_v145"] == pytest.approx(100.0)
    assert status["budget_feasible_v145"] is True
    assert status["source_feasible_v145"] is True
    assert status["cvar_feasible_v145"] is True
    assert status["repair_candidate_feasible_v145"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v145"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v145"] is False
    assert status["paper1_promotion_allowed_v145"] is False
    assert status["paper4_working_champion_changed_v145"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v145_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v145",
        "selected_v145",
        "portfolio_label_v145",
        "repair_action_v145",
        "claim_boundary_v145",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v145"]
    assert int(allocations["selected_v145"].sum()) == status["selected_rows_v145"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v145"])
    assert "162406873" in set(allocations["loan_id"].astype(str))
    assert "127178217" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v145"]) == {
        "added_from_v144_best_swap",
        "kept_from_v143",
    }
    assert allocations["claim_boundary_v145"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v145_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v145"]) is True
    assert float(row["min_source_slack_v145"]) == pytest.approx(0.00011275295234702831)
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v145"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v145"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v145"])

    action = _read_csv("paper4_v145_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v145"]) == status["added_loan_id_v145"]
    assert str(action_row["dropped_loan_id_v145"]) == status["dropped_loan_id_v145"]
    assert float(action_row["return_delta_v145"]) == pytest.approx(82.33186597435457)
    assert float(action_row["exposure_after_repair_v145"]) == pytest.approx(842550.0)
    assert int(action_row["source_cap_violations_after_repair_v145"]) == 0

    source_summary = _read_csv("paper4_v145_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v145",
        "source_slack_v145",
        "source_cap_violated_v145",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v145"]
    assert not source_summary["source_cap_violated_v145"].astype(bool).any()

    blockers = _read_csv("paper4_v145_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v145"], blockers["blocking_v145"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v145"], blockers["evidence_count_v145"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v145_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v145_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v145_repair_candidate_feasible"]) is True
    assert bool(claim_map["v145_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v145_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v145_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v145 thirty-second one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v145 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v145 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v145: Thirty-Second One-Swap Repair Candidate" in notebook
    assert "v146 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v146_post_v145_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v146_status.json")

    assert status["phase"] == "v146_post_v145_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.146"
    assert status["summary_rows_v146"] == 1
    assert status["stage_summary_rows_v146"] == 6
    assert status["candidate_pair_rows_v146"] == 3529
    assert status["top_candidate_rows_v146"] == 200
    assert status["claim_blocker_rows_v146"] == 3
    assert status["selected_rows_v146"] == 171
    assert status["candidate_add_rows_v146"] == 276698
    assert status["total_pair_rows_screened_v146"] == 47315358
    assert status["return_improving_pair_rows_v146"] == 1855901
    assert status["budget_return_feasible_pair_rows_v146"] == 1124085
    assert status["source_prefilter_pair_rows_v146"] == 4157
    assert status["source_exact_pair_rows_v146"] == 3529
    assert status["cvar_feasible_pair_rows_v146"] == 3529
    assert status["one_swap_improving_rows_v146"] == 3529
    assert status["best_one_swap_return_delta_v146"] == pytest.approx(136.7713455210524)
    assert status["best_one_swap_cvar90_after_v146"] == pytest.approx(94370.5136893005)
    assert status["post_repair_one_swap_local_optimality_cleared_v146"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v146"] is False
    assert status["paper1_promotion_allowed_v146"] is False
    assert status["paper4_working_champion_changed_v146"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v146_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v146",
        "dropped_loan_id_v146",
        "return_delta_v146",
        "objective_return_after_swap_v146",
        "budget_swap_feasible_v146",
        "source_swap_feasible_v146",
        "source_cap_violations_after_swap_v146",
        "cvar_swap_feasible_v146",
        "one_swap_improves_return_v146",
        "claim_boundary_v146",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v146"]
    assert probe["return_delta_v146"].gt(0).all()
    assert probe["budget_swap_feasible_v146"].astype(bool).all()
    assert probe["source_swap_feasible_v146"].astype(bool).all()
    assert probe["cvar_swap_feasible_v146"].astype(bool).all()
    assert probe["one_swap_improves_return_v146"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v146"].sum()) == 0
    assert probe["return_delta_v146"].max() == pytest.approx(
        status["best_one_swap_return_delta_v146"]
    )
    assert probe["claim_boundary_v146"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v146_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v146"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v146"]) == "151432552"
    assert str(best["dropped_loan_id_v146"]) == "127146282"
    assert float(best["return_delta_v146"]) == pytest.approx(
        status["best_one_swap_return_delta_v146"]
    )
    assert float(best["exposure_after_swap_v146"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v146"]) is True

    summary = _read_csv("paper4_v146_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v146"]) == status["one_swap_improving_rows_v146"]
    assert float(row["current_exposure_v146"]) == pytest.approx(842550.0)
    assert float(row["current_objective_return_v146"]) == pytest.approx(174.94891652776732)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v146"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v146"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v146"])

    stage_summary = _read_csv("paper4_v146_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v146", "pair_rows_v146", "claim_boundary_v146"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v146"], stage_summary["pair_rows_v146"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v146"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v146"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v146"]

    blockers = _read_csv("paper4_v146_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v146"], blockers["blocking_v146"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v146"], blockers["evidence_count_v146"], strict=False)
    )
    assert bool(blocker_map["post_v145_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v145_one_swap_improvement_found"]) == 3529
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v146_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v146_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v146_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v146_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v146_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v146 post-v145 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v146 proves the v145 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v146 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v146: Post-v145 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3529`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v147_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v147_status.json")

    assert status["phase"] == "v147_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.147"
    assert status["allocation_rows_v147"] == 171
    assert status["summary_rows_v147"] == 1
    assert status["action_rows_v147"] == 1
    assert status["source_summary_rows_v147"] == 51
    assert status["claim_blocker_rows_v147"] == 4
    assert status["added_loan_id_v147"] == "151432552"
    assert status["dropped_loan_id_v147"] == "127146282"
    assert status["selected_rows_v147"] == 171
    assert status["portfolio_exposure_v147"] == pytest.approx(842450.0)
    assert status["objective_return_v147"] == pytest.approx(311.7202620488197)
    assert status["scenario_loss_cvar90_v147"] == pytest.approx(94370.5136893005)
    assert status["source_cap_violations_v147"] == 0
    assert status["delta_return_vs_v145_v147"] == pytest.approx(136.77134552105235)
    assert status["delta_cvar90_vs_v145_v147"] == pytest.approx(165.4183756179118)
    assert status["delta_exposure_vs_v145_v147"] == pytest.approx(-100.0)
    assert status["budget_feasible_v147"] is True
    assert status["source_feasible_v147"] is True
    assert status["cvar_feasible_v147"] is True
    assert status["repair_candidate_feasible_v147"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v147"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v147"] is False
    assert status["paper1_promotion_allowed_v147"] is False
    assert status["paper4_working_champion_changed_v147"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v147_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v147",
        "selected_v147",
        "portfolio_label_v147",
        "repair_action_v147",
        "claim_boundary_v147",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v147"]
    assert int(allocations["selected_v147"].sum()) == status["selected_rows_v147"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v147"])
    assert "151432552" in set(allocations["loan_id"].astype(str))
    assert "127146282" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v147"]) == {
        "added_from_v146_best_swap",
        "kept_from_v145",
    }
    assert allocations["claim_boundary_v147"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v147_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v147"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v147"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v147"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v147"])

    action = _read_csv("paper4_v147_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v147"]) == status["added_loan_id_v147"]
    assert str(action_row["dropped_loan_id_v147"]) == status["dropped_loan_id_v147"]
    assert float(action_row["return_delta_v147"]) == pytest.approx(136.7713455210524)
    assert float(action_row["exposure_after_repair_v147"]) == pytest.approx(842450.0)
    assert int(action_row["source_cap_violations_after_repair_v147"]) == 0

    source_summary = _read_csv("paper4_v147_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v147",
        "source_slack_v147",
        "source_cap_violated_v147",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v147"]
    assert not source_summary["source_cap_violated_v147"].astype(bool).any()

    blockers = _read_csv("paper4_v147_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v147"], blockers["blocking_v147"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v147"], blockers["evidence_count_v147"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v147_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v147_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v147_repair_candidate_feasible"]) is True
    assert bool(claim_map["v147_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v147_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v147_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v147 thirty-third one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v147 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v147 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v147: Thirty-Third One-Swap Repair Candidate" in notebook
    assert "v148 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v148_post_v147_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v148_status.json")

    assert status["phase"] == "v148_post_v147_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.148"
    assert status["summary_rows_v148"] == 1
    assert status["stage_summary_rows_v148"] == 6
    assert status["candidate_pair_rows_v148"] == 2944
    assert status["top_candidate_rows_v148"] == 200
    assert status["claim_blocker_rows_v148"] == 3
    assert status["selected_rows_v148"] == 171
    assert status["candidate_add_rows_v148"] == 276698
    assert status["total_pair_rows_screened_v148"] == 47315358
    assert status["return_improving_pair_rows_v148"] == 1845508
    assert status["budget_return_feasible_pair_rows_v148"] == 1129352
    assert status["source_prefilter_pair_rows_v148"] == 3120
    assert status["source_exact_pair_rows_v148"] == 2944
    assert status["cvar_feasible_pair_rows_v148"] == 2944
    assert status["one_swap_improving_rows_v148"] == 2944
    assert status["best_one_swap_return_delta_v148"] == pytest.approx(82.05129953037954)
    assert status["best_one_swap_cvar90_after_v148"] == pytest.approx(94543.63943776375)
    assert status["post_repair_one_swap_local_optimality_cleared_v148"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v148"] is False
    assert status["paper1_promotion_allowed_v148"] is False
    assert status["paper4_working_champion_changed_v148"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v148_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v148",
        "dropped_loan_id_v148",
        "return_delta_v148",
        "objective_return_after_swap_v148",
        "budget_swap_feasible_v148",
        "source_swap_feasible_v148",
        "source_cap_violations_after_swap_v148",
        "cvar_swap_feasible_v148",
        "one_swap_improves_return_v148",
        "claim_boundary_v148",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v148"]
    assert probe["return_delta_v148"].gt(0).all()
    assert probe["budget_swap_feasible_v148"].astype(bool).all()
    assert probe["source_swap_feasible_v148"].astype(bool).all()
    assert probe["cvar_swap_feasible_v148"].astype(bool).all()
    assert probe["one_swap_improves_return_v148"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v148"].sum()) == 0
    assert probe["return_delta_v148"].max() == pytest.approx(
        status["best_one_swap_return_delta_v148"]
    )
    assert probe["claim_boundary_v148"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v148_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v148"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v148"]) == "144913322"
    assert str(best["dropped_loan_id_v148"]) == "169243494"
    assert float(best["return_delta_v148"]) == pytest.approx(
        status["best_one_swap_return_delta_v148"]
    )
    assert float(best["exposure_after_swap_v148"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v148"]) is True

    summary = _read_csv("paper4_v148_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v148"]) == status["one_swap_improving_rows_v148"]
    assert float(row["current_exposure_v148"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v148"]) == pytest.approx(311.7202620488197)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v148"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v148"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v148"])

    stage_summary = _read_csv("paper4_v148_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v148", "pair_rows_v148", "claim_boundary_v148"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v148"], stage_summary["pair_rows_v148"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v148"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v148"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v148"]

    blockers = _read_csv("paper4_v148_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v148"], blockers["blocking_v148"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v148"], blockers["evidence_count_v148"], strict=False)
    )
    assert bool(blocker_map["post_v147_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v147_one_swap_improvement_found"]) == 2944
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v148_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v148_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v148_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v148_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v148_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v148 post-v147 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v148 proves the v147 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v148 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v148: Post-v147 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `2944`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v149_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v149_status.json")

    assert status["phase"] == "v149_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.149"
    assert status["allocation_rows_v149"] == 171
    assert status["summary_rows_v149"] == 1
    assert status["action_rows_v149"] == 1
    assert status["source_summary_rows_v149"] == 51
    assert status["claim_blocker_rows_v149"] == 4
    assert status["added_loan_id_v149"] == "144913322"
    assert status["dropped_loan_id_v149"] == "169243494"
    assert status["selected_rows_v149"] == 171
    assert status["portfolio_exposure_v149"] == pytest.approx(842450.0)
    assert status["objective_return_v149"] == pytest.approx(393.7715615791999)
    assert status["scenario_loss_cvar90_v149"] == pytest.approx(94543.63943776378)
    assert status["source_cap_violations_v149"] == 0
    assert status["delta_return_vs_v147_v149"] == pytest.approx(82.05129953038022)
    assert status["delta_cvar90_vs_v147_v149"] == pytest.approx(173.12574846328062)
    assert status["delta_exposure_vs_v147_v149"] == pytest.approx(0.0)
    assert status["budget_feasible_v149"] is True
    assert status["source_feasible_v149"] is True
    assert status["cvar_feasible_v149"] is True
    assert status["repair_candidate_feasible_v149"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v149"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v149"] is False
    assert status["paper1_promotion_allowed_v149"] is False
    assert status["paper4_working_champion_changed_v149"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v149_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v149",
        "selected_v149",
        "portfolio_label_v149",
        "repair_action_v149",
        "claim_boundary_v149",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v149"]
    assert int(allocations["selected_v149"].sum()) == status["selected_rows_v149"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v149"])
    assert "144913322" in set(allocations["loan_id"].astype(str))
    assert "169243494" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v149"]) == {
        "added_from_v148_best_swap",
        "kept_from_v147",
    }
    assert allocations["claim_boundary_v149"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v149_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v149"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v149"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v149"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v149"])

    action = _read_csv("paper4_v149_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v149"]) == status["added_loan_id_v149"]
    assert str(action_row["dropped_loan_id_v149"]) == status["dropped_loan_id_v149"]
    assert float(action_row["return_delta_v149"]) == pytest.approx(82.05129953037954)
    assert float(action_row["exposure_after_repair_v149"]) == pytest.approx(842450.0)
    assert int(action_row["source_cap_violations_after_repair_v149"]) == 0

    source_summary = _read_csv("paper4_v149_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v149",
        "source_slack_v149",
        "source_cap_violated_v149",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v149"]
    assert not source_summary["source_cap_violated_v149"].astype(bool).any()

    blockers = _read_csv("paper4_v149_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v149"], blockers["blocking_v149"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v149"], blockers["evidence_count_v149"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v149_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v149_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v149_repair_candidate_feasible"]) is True
    assert bool(claim_map["v149_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v149_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v149_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v149 thirty-fourth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v149 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v149 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v149: Thirty-Fourth One-Swap Repair Candidate" in notebook
    assert "v150 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v150_post_v149_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v150_status.json")

    assert status["phase"] == "v150_post_v149_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.150"
    assert status["summary_rows_v150"] == 1
    assert status["stage_summary_rows_v150"] == 6
    assert status["candidate_pair_rows_v150"] == 2651
    assert status["top_candidate_rows_v150"] == 200
    assert status["claim_blocker_rows_v150"] == 3
    assert status["selected_rows_v150"] == 171
    assert status["candidate_add_rows_v150"] == 276698
    assert status["total_pair_rows_screened_v150"] == 47315358
    assert status["return_improving_pair_rows_v150"] == 1839789
    assert status["budget_return_feasible_pair_rows_v150"] == 1125644
    assert status["source_prefilter_pair_rows_v150"] == 2827
    assert status["source_exact_pair_rows_v150"] == 2651
    assert status["cvar_feasible_pair_rows_v150"] == 2651
    assert status["one_swap_improving_rows_v150"] == 2651
    assert status["best_one_swap_return_delta_v150"] == pytest.approx(81.51578091889951)
    assert status["best_one_swap_cvar90_after_v150"] == pytest.approx(94755.28809544674)
    assert status["post_repair_one_swap_local_optimality_cleared_v150"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v150"] is False
    assert status["paper1_promotion_allowed_v150"] is False
    assert status["paper4_working_champion_changed_v150"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v150_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v150",
        "dropped_loan_id_v150",
        "return_delta_v150",
        "objective_return_after_swap_v150",
        "budget_swap_feasible_v150",
        "source_swap_feasible_v150",
        "source_cap_violations_after_swap_v150",
        "cvar_swap_feasible_v150",
        "one_swap_improves_return_v150",
        "claim_boundary_v150",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v150"]
    assert probe["return_delta_v150"].gt(0).all()
    assert probe["budget_swap_feasible_v150"].astype(bool).all()
    assert probe["source_swap_feasible_v150"].astype(bool).all()
    assert probe["cvar_swap_feasible_v150"].astype(bool).all()
    assert probe["one_swap_improves_return_v150"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v150"].sum()) == 0
    assert probe["return_delta_v150"].max() == pytest.approx(
        status["best_one_swap_return_delta_v150"]
    )
    assert probe["claim_boundary_v150"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v150_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v150"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v150"]) == "158509118"
    assert str(best["dropped_loan_id_v150"]) == "126502150"
    assert float(best["return_delta_v150"]) == pytest.approx(
        status["best_one_swap_return_delta_v150"]
    )
    assert float(best["exposure_after_swap_v150"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v150"]) is True

    summary = _read_csv("paper4_v150_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v150"]) == status["one_swap_improving_rows_v150"]
    assert float(row["current_exposure_v150"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v150"]) == pytest.approx(393.7715615791999)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v150"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v150"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v150"])

    stage_summary = _read_csv("paper4_v150_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v150", "pair_rows_v150", "claim_boundary_v150"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v150"], stage_summary["pair_rows_v150"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v150"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v150"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v150"]

    blockers = _read_csv("paper4_v150_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v150"], blockers["blocking_v150"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v150"], blockers["evidence_count_v150"], strict=False)
    )
    assert bool(blocker_map["post_v149_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v149_one_swap_improvement_found"]) == 2651
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v150_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v150_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v150_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v150_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v150_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v150 post-v149 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v150 proves the v149 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v150 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v150: Post-v149 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `2651`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v151_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v151_status.json")

    assert status["phase"] == "v151_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.151"
    assert status["allocation_rows_v151"] == 171
    assert status["summary_rows_v151"] == 1
    assert status["action_rows_v151"] == 1
    assert status["source_summary_rows_v151"] == 51
    assert status["claim_blocker_rows_v151"] == 4
    assert status["added_loan_id_v151"] == "158509118"
    assert status["dropped_loan_id_v151"] == "126502150"
    assert status["selected_rows_v151"] == 171
    assert status["portfolio_exposure_v151"] == pytest.approx(842450.0)
    assert status["objective_return_v151"] == pytest.approx(475.28734249809986)
    assert status["scenario_loss_cvar90_v151"] == pytest.approx(94755.28809544674)
    assert status["source_cap_violations_v151"] == 0
    assert status["delta_return_vs_v149_v151"] == pytest.approx(81.51578091889996)
    assert status["delta_cvar90_vs_v149_v151"] == pytest.approx(211.64865768296295)
    assert status["delta_exposure_vs_v149_v151"] == pytest.approx(0.0)
    assert status["budget_feasible_v151"] is True
    assert status["source_feasible_v151"] is True
    assert status["cvar_feasible_v151"] is True
    assert status["repair_candidate_feasible_v151"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v151"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v151"] is False
    assert status["paper1_promotion_allowed_v151"] is False
    assert status["paper4_working_champion_changed_v151"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v151_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v151",
        "selected_v151",
        "portfolio_label_v151",
        "repair_action_v151",
        "claim_boundary_v151",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v151"]
    assert int(allocations["selected_v151"].sum()) == status["selected_rows_v151"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v151"])
    assert "158509118" in set(allocations["loan_id"].astype(str))
    assert "126502150" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v151"]) == {
        "added_from_v150_best_swap",
        "kept_from_v149",
    }
    assert allocations["claim_boundary_v151"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v151_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v151"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v151"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v151"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v151"])

    action = _read_csv("paper4_v151_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v151"]) == status["added_loan_id_v151"]
    assert str(action_row["dropped_loan_id_v151"]) == status["dropped_loan_id_v151"]
    assert float(action_row["return_delta_v151"]) == pytest.approx(81.51578091889951)
    assert float(action_row["exposure_after_repair_v151"]) == pytest.approx(842450.0)
    assert int(action_row["source_cap_violations_after_repair_v151"]) == 0

    source_summary = _read_csv("paper4_v151_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v151",
        "source_slack_v151",
        "source_cap_violated_v151",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v151"]
    assert not source_summary["source_cap_violated_v151"].astype(bool).any()

    blockers = _read_csv("paper4_v151_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v151"], blockers["blocking_v151"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v151"], blockers["evidence_count_v151"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v151_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v151_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v151_repair_candidate_feasible"]) is True
    assert bool(claim_map["v151_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v151_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v151_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v151 thirty-fifth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v151 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v151 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v151: Thirty-Fifth One-Swap Repair Candidate" in notebook
    assert "v152 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v152_post_v151_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v152_status.json")

    assert status["phase"] == "v152_post_v151_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.152"
    assert status["summary_rows_v152"] == 1
    assert status["stage_summary_rows_v152"] == 6
    assert status["candidate_pair_rows_v152"] == 2566
    assert status["top_candidate_rows_v152"] == 200
    assert status["claim_blocker_rows_v152"] == 3
    assert status["selected_rows_v152"] == 171
    assert status["candidate_add_rows_v152"] == 276698
    assert status["total_pair_rows_screened_v152"] == 47315358
    assert status["return_improving_pair_rows_v152"] == 1833292
    assert status["budget_return_feasible_pair_rows_v152"] == 1122764
    assert status["source_prefilter_pair_rows_v152"] == 2714
    assert status["source_exact_pair_rows_v152"] == 2566
    assert status["cvar_feasible_pair_rows_v152"] == 2566
    assert status["one_swap_improving_rows_v152"] == 2566
    assert status["best_one_swap_return_delta_v152"] == pytest.approx(81.277115861391)
    assert status["best_one_swap_cvar90_after_v152"] == pytest.approx(95299.83323500236)
    assert status["post_repair_one_swap_local_optimality_cleared_v152"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v152"] is False
    assert status["paper1_promotion_allowed_v152"] is False
    assert status["paper4_working_champion_changed_v152"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v152_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v152",
        "dropped_loan_id_v152",
        "return_delta_v152",
        "objective_return_after_swap_v152",
        "budget_swap_feasible_v152",
        "source_swap_feasible_v152",
        "source_cap_violations_after_swap_v152",
        "cvar_swap_feasible_v152",
        "one_swap_improves_return_v152",
        "claim_boundary_v152",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v152"]
    assert probe["return_delta_v152"].gt(0).all()
    assert probe["budget_swap_feasible_v152"].astype(bool).all()
    assert probe["source_swap_feasible_v152"].astype(bool).all()
    assert probe["cvar_swap_feasible_v152"].astype(bool).all()
    assert probe["one_swap_improves_return_v152"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v152"].sum()) == 0
    assert probe["return_delta_v152"].max() == pytest.approx(
        status["best_one_swap_return_delta_v152"]
    )
    assert probe["claim_boundary_v152"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v152_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v152"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v152"]) == "137741627"
    assert str(best["dropped_loan_id_v152"]) == "127550201"
    assert float(best["return_delta_v152"]) == pytest.approx(
        status["best_one_swap_return_delta_v152"]
    )
    assert float(best["exposure_after_swap_v152"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v152"]) is True

    summary = _read_csv("paper4_v152_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v152"]) == status["one_swap_improving_rows_v152"]
    assert float(row["current_exposure_v152"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v152"]) == pytest.approx(475.2873424980999)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v152"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v152"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v152"])

    stage_summary = _read_csv("paper4_v152_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v152", "pair_rows_v152", "claim_boundary_v152"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v152"], stage_summary["pair_rows_v152"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v152"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v152"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v152"]

    blockers = _read_csv("paper4_v152_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v152"], blockers["blocking_v152"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v152"], blockers["evidence_count_v152"], strict=False)
    )
    assert bool(blocker_map["post_v151_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v151_one_swap_improvement_found"]) == 2566
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v152_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v152_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v152_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v152_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v152_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v152 post-v151 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v152 proves the v151 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v152 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v152: Post-v151 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `2566`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v153_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v153_status.json")

    assert status["phase"] == "v153_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.153"
    assert status["allocation_rows_v153"] == 171
    assert status["summary_rows_v153"] == 1
    assert status["action_rows_v153"] == 1
    assert status["source_summary_rows_v153"] == 51
    assert status["claim_blocker_rows_v153"] == 4
    assert status["added_loan_id_v153"] == "137741627"
    assert status["dropped_loan_id_v153"] == "127550201"
    assert status["selected_rows_v153"] == 171
    assert status["portfolio_exposure_v153"] == pytest.approx(842450.0)
    assert status["objective_return_v153"] == pytest.approx(556.5644583594903)
    assert status["scenario_loss_cvar90_v153"] == pytest.approx(95299.83323500236)
    assert status["source_cap_violations_v153"] == 0
    assert status["delta_return_vs_v151_v153"] == pytest.approx(81.27711586139043)
    assert status["delta_cvar90_vs_v151_v153"] == pytest.approx(544.5451395556156)
    assert status["delta_exposure_vs_v151_v153"] == pytest.approx(0.0)
    assert status["budget_feasible_v153"] is True
    assert status["source_feasible_v153"] is True
    assert status["cvar_feasible_v153"] is True
    assert status["repair_candidate_feasible_v153"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v153"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v153"] is False
    assert status["paper1_promotion_allowed_v153"] is False
    assert status["paper4_working_champion_changed_v153"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v153_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v153",
        "selected_v153",
        "portfolio_label_v153",
        "repair_action_v153",
        "claim_boundary_v153",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v153"]
    assert int(allocations["selected_v153"].sum()) == status["selected_rows_v153"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v153"])
    assert "137741627" in set(allocations["loan_id"].astype(str))
    assert "127550201" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v153"]) == {
        "added_from_v152_best_swap",
        "kept_from_v151",
    }
    assert allocations["claim_boundary_v153"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v153_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v153"]) is True
    assert float(row["min_source_slack_v153"]) == pytest.approx(5.754096345278459e-05)
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v153"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v153"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v153"])

    action = _read_csv("paper4_v153_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v153"]) == status["added_loan_id_v153"]
    assert str(action_row["dropped_loan_id_v153"]) == status["dropped_loan_id_v153"]
    assert float(action_row["return_delta_v153"]) == pytest.approx(81.277115861391)
    assert float(action_row["exposure_after_repair_v153"]) == pytest.approx(842450.0)
    assert int(action_row["source_cap_violations_after_repair_v153"]) == 0

    source_summary = _read_csv("paper4_v153_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v153",
        "source_slack_v153",
        "source_cap_violated_v153",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v153"]
    assert not source_summary["source_cap_violated_v153"].astype(bool).any()

    blockers = _read_csv("paper4_v153_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v153"], blockers["blocking_v153"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v153"], blockers["evidence_count_v153"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v153_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v153_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v153_repair_candidate_feasible"]) is True
    assert bool(claim_map["v153_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v153_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v153_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v153 thirty-sixth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v153 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v153 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v153: Thirty-Sixth One-Swap Repair Candidate" in notebook
    assert "v154 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v154_post_v153_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v154_status.json")

    assert status["phase"] == "v154_post_v153_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.154"
    assert status["summary_rows_v154"] == 1
    assert status["stage_summary_rows_v154"] == 6
    assert status["candidate_pair_rows_v154"] == 5703
    assert status["top_candidate_rows_v154"] == 200
    assert status["claim_blocker_rows_v154"] == 3
    assert status["selected_rows_v154"] == 171
    assert status["candidate_add_rows_v154"] == 276698
    assert status["total_pair_rows_screened_v154"] == 47315358
    assert status["return_improving_pair_rows_v154"] == 1828018
    assert status["budget_return_feasible_pair_rows_v154"] == 1119986
    assert status["source_prefilter_pair_rows_v154"] == 5881
    assert status["source_exact_pair_rows_v154"] == 5703
    assert status["cvar_feasible_pair_rows_v154"] == 5703
    assert status["one_swap_improving_rows_v154"] == 5703
    assert status["best_one_swap_return_delta_v154"] == pytest.approx(161.87871667935158)
    assert status["best_one_swap_cvar90_after_v154"] == pytest.approx(95257.34349079213)
    assert status["post_repair_one_swap_local_optimality_cleared_v154"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v154"] is False
    assert status["paper1_promotion_allowed_v154"] is False
    assert status["paper4_working_champion_changed_v154"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v154_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v154",
        "dropped_loan_id_v154",
        "return_delta_v154",
        "objective_return_after_swap_v154",
        "budget_swap_feasible_v154",
        "source_swap_feasible_v154",
        "source_cap_violations_after_swap_v154",
        "cvar_swap_feasible_v154",
        "one_swap_improves_return_v154",
        "claim_boundary_v154",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v154"]
    assert probe["return_delta_v154"].gt(0).all()
    assert probe["budget_swap_feasible_v154"].astype(bool).all()
    assert probe["source_swap_feasible_v154"].astype(bool).all()
    assert probe["cvar_swap_feasible_v154"].astype(bool).all()
    assert probe["one_swap_improves_return_v154"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v154"].sum()) == 0
    assert probe["return_delta_v154"].max() == pytest.approx(
        status["best_one_swap_return_delta_v154"]
    )
    assert probe["claim_boundary_v154"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v154_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v154"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v154"]) == "132331246"
    assert str(best["dropped_loan_id_v154"]) == "126484483"
    assert float(best["return_delta_v154"]) == pytest.approx(
        status["best_one_swap_return_delta_v154"]
    )
    assert float(best["exposure_after_swap_v154"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v154"]) is True

    summary = _read_csv("paper4_v154_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v154"]) == status["one_swap_improving_rows_v154"]
    assert float(row["current_exposure_v154"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v154"]) == pytest.approx(556.5644583594903)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v154"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v154"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v154"])

    stage_summary = _read_csv("paper4_v154_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v154", "pair_rows_v154", "claim_boundary_v154"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v154"], stage_summary["pair_rows_v154"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v154"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v154"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v154"]

    blockers = _read_csv("paper4_v154_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v154"], blockers["blocking_v154"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v154"], blockers["evidence_count_v154"], strict=False)
    )
    assert bool(blocker_map["post_v153_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v153_one_swap_improvement_found"]) == 5703
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v154_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v154_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v154_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v154_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v154_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v154 post-v153 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v154 proves the v153 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v154 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v154: Post-v153 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `5703`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v155_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v155_status.json")

    assert status["phase"] == "v155_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.155"
    assert status["allocation_rows_v155"] == 171
    assert status["summary_rows_v155"] == 1
    assert status["action_rows_v155"] == 1
    assert status["source_summary_rows_v155"] == 51
    assert status["claim_blocker_rows_v155"] == 4
    assert status["added_loan_id_v155"] == "132331246"
    assert status["dropped_loan_id_v155"] == "126484483"
    assert status["selected_rows_v155"] == 171
    assert status["portfolio_exposure_v155"] == pytest.approx(842450.0)
    assert status["objective_return_v155"] == pytest.approx(718.4431750388414)
    assert status["scenario_loss_cvar90_v155"] == pytest.approx(95257.34349079213)
    assert status["source_cap_violations_v155"] == 0
    assert status["delta_return_vs_v153_v155"] == pytest.approx(161.8787166793511)
    assert status["delta_cvar90_vs_v153_v155"] == pytest.approx(-42.48974421022285)
    assert status["delta_exposure_vs_v153_v155"] == pytest.approx(0.0)
    assert status["budget_feasible_v155"] is True
    assert status["source_feasible_v155"] is True
    assert status["cvar_feasible_v155"] is True
    assert status["repair_candidate_feasible_v155"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v155"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v155"] is False
    assert status["paper1_promotion_allowed_v155"] is False
    assert status["paper4_working_champion_changed_v155"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v155_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v155",
        "selected_v155",
        "portfolio_label_v155",
        "repair_action_v155",
        "claim_boundary_v155",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v155"]
    assert int(allocations["selected_v155"].sum()) == status["selected_rows_v155"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v155"])
    assert "132331246" in set(allocations["loan_id"].astype(str))
    assert "126484483" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v155"]) == {
        "added_from_v154_best_swap",
        "kept_from_v153",
    }
    assert allocations["claim_boundary_v155"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v155_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v155"]) is True
    assert float(row["min_source_slack_v155"]) == pytest.approx(5.754096345278459e-05)
    assert float(row["delta_cvar90_vs_v153_v155"]) < 0
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v155"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v155"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v155"])

    action = _read_csv("paper4_v155_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v155"]) == status["added_loan_id_v155"]
    assert str(action_row["dropped_loan_id_v155"]) == status["dropped_loan_id_v155"]
    assert float(action_row["return_delta_v155"]) == pytest.approx(161.87871667935158)
    assert float(action_row["cvar90_after_repair_v155"]) == pytest.approx(
        status["scenario_loss_cvar90_v155"]
    )
    assert float(action_row["exposure_after_repair_v155"]) == pytest.approx(842450.0)
    assert int(action_row["source_cap_violations_after_repair_v155"]) == 0

    source_summary = _read_csv("paper4_v155_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v155",
        "source_slack_v155",
        "source_cap_violated_v155",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v155"]
    assert not source_summary["source_cap_violated_v155"].astype(bool).any()

    blockers = _read_csv("paper4_v155_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v155"], blockers["blocking_v155"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v155"], blockers["evidence_count_v155"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v155_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v155_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v155_repair_candidate_feasible"]) is True
    assert bool(claim_map["v155_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v155_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v155_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v155 thirty-seventh one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v155 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v155 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v155: Thirty-Seventh One-Swap Repair Candidate" in notebook
    assert "v156 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v156_post_v155_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v156_status.json")

    assert status["phase"] == "v156_post_v155_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.156"
    assert status["summary_rows_v156"] == 1
    assert status["stage_summary_rows_v156"] == 6
    assert status["candidate_pair_rows_v156"] == 2599
    assert status["top_candidate_rows_v156"] == 200
    assert status["claim_blocker_rows_v156"] == 3
    assert status["selected_rows_v156"] == 171
    assert status["candidate_add_rows_v156"] == 276698
    assert status["total_pair_rows_screened_v156"] == 47315358
    assert status["return_improving_pair_rows_v156"] == 1816027
    assert status["budget_return_feasible_pair_rows_v156"] == 1114637
    assert status["source_prefilter_pair_rows_v156"] == 2702
    assert status["source_exact_pair_rows_v156"] == 2599
    assert status["cvar_feasible_pair_rows_v156"] == 2599
    assert status["one_swap_improving_rows_v156"] == 2599
    assert status["best_one_swap_return_delta_v156"] == pytest.approx(106.89852390570778)
    assert status["best_one_swap_cvar90_after_v156"] == pytest.approx(95115.13590797938)
    assert status["post_repair_one_swap_local_optimality_cleared_v156"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v156"] is False
    assert status["paper1_promotion_allowed_v156"] is False
    assert status["paper4_working_champion_changed_v156"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v156_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v156",
        "dropped_loan_id_v156",
        "return_delta_v156",
        "objective_return_after_swap_v156",
        "budget_swap_feasible_v156",
        "source_swap_feasible_v156",
        "source_cap_violations_after_swap_v156",
        "cvar_swap_feasible_v156",
        "one_swap_improves_return_v156",
        "claim_boundary_v156",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v156"]
    assert probe["return_delta_v156"].gt(0).all()
    assert probe["budget_swap_feasible_v156"].astype(bool).all()
    assert probe["source_swap_feasible_v156"].astype(bool).all()
    assert probe["cvar_swap_feasible_v156"].astype(bool).all()
    assert probe["one_swap_improves_return_v156"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v156"].sum()) == 0
    assert probe["return_delta_v156"].max() == pytest.approx(
        status["best_one_swap_return_delta_v156"]
    )
    assert probe["claim_boundary_v156"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v156_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v156"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v156"]) == "158296387"
    assert str(best["dropped_loan_id_v156"]) == "126414619"
    assert float(best["return_delta_v156"]) == pytest.approx(
        status["best_one_swap_return_delta_v156"]
    )
    assert float(best["exposure_after_swap_v156"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v156"]) is True

    summary = _read_csv("paper4_v156_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v156"]) == status["one_swap_improving_rows_v156"]
    assert float(row["current_exposure_v156"]) == pytest.approx(842450.0)
    assert float(row["current_objective_return_v156"]) == pytest.approx(718.4431750388414)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v156"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v156"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v156"])

    stage_summary = _read_csv("paper4_v156_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v156", "pair_rows_v156", "claim_boundary_v156"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v156"], stage_summary["pair_rows_v156"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v156"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v156"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v156"]

    blockers = _read_csv("paper4_v156_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v156"], blockers["blocking_v156"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v156"], blockers["evidence_count_v156"], strict=False)
    )
    assert bool(blocker_map["post_v155_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v155_one_swap_improvement_found"]) == 2599
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v156_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v156_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v156_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v156_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v156_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v156 post-v155 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v156 proves the v155 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v156 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v156: Post-v155 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `2599`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v157_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v157_status.json")

    assert status["phase"] == "v157_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.157"
    assert status["allocation_rows_v157"] == 171
    assert status["summary_rows_v157"] == 1
    assert status["action_rows_v157"] == 1
    assert status["source_summary_rows_v157"] == 51
    assert status["claim_blocker_rows_v157"] == 4
    assert status["added_loan_id_v157"] == "158296387"
    assert status["dropped_loan_id_v157"] == "126414619"
    assert status["selected_rows_v157"] == 171
    assert status["portfolio_exposure_v157"] == pytest.approx(842400.0)
    assert status["objective_return_v157"] == pytest.approx(825.3416989445486)
    assert status["scenario_loss_cvar90_v157"] == pytest.approx(95115.13590797938)
    assert status["source_cap_violations_v157"] == 0
    assert status["delta_return_vs_v155_v157"] == pytest.approx(106.89852390570718)
    assert status["delta_cvar90_vs_v155_v157"] == pytest.approx(-142.20758281274175)
    assert status["delta_exposure_vs_v155_v157"] == pytest.approx(-50.0)
    assert status["budget_feasible_v157"] is True
    assert status["source_feasible_v157"] is True
    assert status["cvar_feasible_v157"] is True
    assert status["repair_candidate_feasible_v157"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v157"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v157"] is False
    assert status["paper1_promotion_allowed_v157"] is False
    assert status["paper4_working_champion_changed_v157"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v157_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v157",
        "selected_v157",
        "portfolio_label_v157",
        "repair_action_v157",
        "claim_boundary_v157",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v157"]
    assert int(allocations["selected_v157"].sum()) == status["selected_rows_v157"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v157"])
    assert "158296387" in set(allocations["loan_id"].astype(str))
    assert "126414619" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v157"]) == {
        "added_from_v156_best_swap",
        "kept_from_v155",
    }
    assert allocations["claim_boundary_v157"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v157_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v157"]) is True
    assert float(row["min_source_slack_v157"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v155_v157"]) < 0
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v157"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v157"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v157"])

    action = _read_csv("paper4_v157_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v157"]) == status["added_loan_id_v157"]
    assert str(action_row["dropped_loan_id_v157"]) == status["dropped_loan_id_v157"]
    assert float(action_row["return_delta_v157"]) == pytest.approx(106.89852390570778)
    assert float(action_row["cvar90_after_repair_v157"]) == pytest.approx(
        status["scenario_loss_cvar90_v157"]
    )
    assert float(action_row["exposure_after_repair_v157"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v157"]) == 0

    source_summary = _read_csv("paper4_v157_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v157",
        "source_slack_v157",
        "source_cap_violated_v157",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v157"]
    assert not source_summary["source_cap_violated_v157"].astype(bool).any()

    blockers = _read_csv("paper4_v157_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v157"], blockers["blocking_v157"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v157"], blockers["evidence_count_v157"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v157_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v157_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v157_repair_candidate_feasible"]) is True
    assert bool(claim_map["v157_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v157_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v157_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v157 thirty-eighth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v157 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v157 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v157: Thirty-Eighth One-Swap Repair Candidate" in notebook
    assert "v158 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v158_post_v157_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v158_status.json")

    assert status["phase"] == "v158_post_v157_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.158"
    assert status["summary_rows_v158"] == 1
    assert status["stage_summary_rows_v158"] == 6
    assert status["candidate_pair_rows_v158"] == 2475
    assert status["top_candidate_rows_v158"] == 200
    assert status["claim_blocker_rows_v158"] == 3
    assert status["selected_rows_v158"] == 171
    assert status["candidate_add_rows_v158"] == 276698
    assert status["total_pair_rows_screened_v158"] == 47315358
    assert status["return_improving_pair_rows_v158"] == 1807648
    assert status["budget_return_feasible_pair_rows_v158"] == 1116751
    assert status["source_prefilter_pair_rows_v158"] == 2567
    assert status["source_exact_pair_rows_v158"] == 2475
    assert status["cvar_feasible_pair_rows_v158"] == 2475
    assert status["one_swap_improving_rows_v158"] == 2475
    assert status["best_one_swap_return_delta_v158"] == pytest.approx(75.50848095113494)
    assert status["best_one_swap_cvar90_after_v158"] == pytest.approx(95230.69350127784)
    assert status["post_repair_one_swap_local_optimality_cleared_v158"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v158"] is False
    assert status["paper1_promotion_allowed_v158"] is False
    assert status["paper4_working_champion_changed_v158"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v158_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v158",
        "dropped_loan_id_v158",
        "return_delta_v158",
        "objective_return_after_swap_v158",
        "budget_swap_feasible_v158",
        "source_swap_feasible_v158",
        "source_cap_violations_after_swap_v158",
        "cvar_swap_feasible_v158",
        "one_swap_improves_return_v158",
        "claim_boundary_v158",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v158"]
    assert probe["return_delta_v158"].gt(0).all()
    assert probe["budget_swap_feasible_v158"].astype(bool).all()
    assert probe["source_swap_feasible_v158"].astype(bool).all()
    assert probe["cvar_swap_feasible_v158"].astype(bool).all()
    assert probe["one_swap_improves_return_v158"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v158"].sum()) == 0
    assert probe["return_delta_v158"].max() == pytest.approx(
        status["best_one_swap_return_delta_v158"]
    )
    assert probe["claim_boundary_v158"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v158_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v158"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v158"]) == "161455148"
    assert str(best["dropped_loan_id_v158"]) == "126460194"
    assert float(best["return_delta_v158"]) == pytest.approx(
        status["best_one_swap_return_delta_v158"]
    )
    assert float(best["exposure_after_swap_v158"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v158"]) is True

    summary = _read_csv("paper4_v158_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v158"]) == status["one_swap_improving_rows_v158"]
    assert float(row["current_exposure_v158"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v158"]) == pytest.approx(825.3416989445486)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v158"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v158"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v158"])

    stage_summary = _read_csv("paper4_v158_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v158", "pair_rows_v158", "claim_boundary_v158"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v158"], stage_summary["pair_rows_v158"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v158"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v158"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v158"]

    blockers = _read_csv("paper4_v158_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v158"], blockers["blocking_v158"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v158"], blockers["evidence_count_v158"], strict=False)
    )
    assert bool(blocker_map["post_v157_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v157_one_swap_improvement_found"]) == 2475
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v158_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v158_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v158_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v158_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v158_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v158 post-v157 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v158 proves the v157 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v158 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v158: Post-v157 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `2475`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v159_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v159_status.json")

    assert status["phase"] == "v159_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.159"
    assert status["allocation_rows_v159"] == 171
    assert status["summary_rows_v159"] == 1
    assert status["action_rows_v159"] == 1
    assert status["source_summary_rows_v159"] == 51
    assert status["claim_blocker_rows_v159"] == 4
    assert status["added_loan_id_v159"] == "161455148"
    assert status["dropped_loan_id_v159"] == "126460194"
    assert status["selected_rows_v159"] == 171
    assert status["portfolio_exposure_v159"] == pytest.approx(842400.0)
    assert status["objective_return_v159"] == pytest.approx(900.8501798956847)
    assert status["scenario_loss_cvar90_v159"] == pytest.approx(95230.69350127784)
    assert status["source_cap_violations_v159"] == 0
    assert status["delta_return_vs_v157_v159"] == pytest.approx(75.50848095113611)
    assert status["delta_cvar90_vs_v157_v159"] == pytest.approx(115.55759329846478)
    assert status["delta_exposure_vs_v157_v159"] == pytest.approx(0.0)
    assert status["budget_feasible_v159"] is True
    assert status["source_feasible_v159"] is True
    assert status["cvar_feasible_v159"] is True
    assert status["repair_candidate_feasible_v159"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v159"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v159"] is False
    assert status["paper1_promotion_allowed_v159"] is False
    assert status["paper4_working_champion_changed_v159"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v159_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v159",
        "selected_v159",
        "portfolio_label_v159",
        "repair_action_v159",
        "claim_boundary_v159",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v159"]
    assert int(allocations["selected_v159"].sum()) == status["selected_rows_v159"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v159"])
    assert "161455148" in set(allocations["loan_id"].astype(str))
    assert "126460194" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v159"]) == {
        "added_from_v158_best_swap",
        "kept_from_v157",
    }
    assert allocations["claim_boundary_v159"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v159_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v159"]) is True
    assert float(row["min_source_slack_v159"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v157_v159"]) > 0
    assert float(row["scenario_loss_cvar90_v159"]) <= float(row["cvar_cap_v159"])
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v159"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v159"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v159"])

    action = _read_csv("paper4_v159_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v159"]) == status["added_loan_id_v159"]
    assert str(action_row["dropped_loan_id_v159"]) == status["dropped_loan_id_v159"]
    assert float(action_row["return_delta_v159"]) == pytest.approx(75.50848095113494)
    assert float(action_row["cvar90_after_repair_v159"]) == pytest.approx(
        status["scenario_loss_cvar90_v159"]
    )
    assert float(action_row["exposure_after_repair_v159"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v159"]) == 0

    source_summary = _read_csv("paper4_v159_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v159",
        "source_slack_v159",
        "source_cap_violated_v159",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v159"]
    assert not source_summary["source_cap_violated_v159"].astype(bool).any()

    blockers = _read_csv("paper4_v159_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v159"], blockers["blocking_v159"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v159"], blockers["evidence_count_v159"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v159_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v159_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v159_repair_candidate_feasible"]) is True
    assert bool(claim_map["v159_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v159_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v159_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v159 thirty-ninth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v159 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v159 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v159: Thirty-Ninth One-Swap Repair Candidate" in notebook
    assert "v160 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v160_post_v159_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v160_status.json")

    assert status["phase"] == "v160_post_v159_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.160"
    assert status["summary_rows_v160"] == 1
    assert status["stage_summary_rows_v160"] == 6
    assert status["candidate_pair_rows_v160"] == 2211
    assert status["top_candidate_rows_v160"] == 200
    assert status["claim_blocker_rows_v160"] == 3
    assert status["selected_rows_v160"] == 171
    assert status["candidate_add_rows_v160"] == 276698
    assert status["total_pair_rows_screened_v160"] == 47315358
    assert status["return_improving_pair_rows_v160"] == 1802388
    assert status["budget_return_feasible_pair_rows_v160"] == 1113346
    assert status["source_prefilter_pair_rows_v160"] == 2303
    assert status["source_exact_pair_rows_v160"] == 2211
    assert status["cvar_feasible_pair_rows_v160"] == 2211
    assert status["one_swap_improving_rows_v160"] == 2211
    assert status["best_one_swap_return_delta_v160"] == pytest.approx(73.56756536196579)
    assert status["best_one_swap_cvar90_after_v160"] == pytest.approx(95213.35540496596)
    assert status["post_repair_one_swap_local_optimality_cleared_v160"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v160"] is False
    assert status["paper1_promotion_allowed_v160"] is False
    assert status["paper4_working_champion_changed_v160"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v160_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v160",
        "dropped_loan_id_v160",
        "return_delta_v160",
        "objective_return_after_swap_v160",
        "budget_swap_feasible_v160",
        "source_swap_feasible_v160",
        "source_cap_violations_after_swap_v160",
        "cvar_swap_feasible_v160",
        "one_swap_improves_return_v160",
        "claim_boundary_v160",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v160"]
    assert probe["return_delta_v160"].gt(0).all()
    assert probe["budget_swap_feasible_v160"].astype(bool).all()
    assert probe["source_swap_feasible_v160"].astype(bool).all()
    assert probe["cvar_swap_feasible_v160"].astype(bool).all()
    assert probe["one_swap_improves_return_v160"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v160"].sum()) == 0
    assert probe["return_delta_v160"].max() == pytest.approx(
        status["best_one_swap_return_delta_v160"]
    )
    assert probe["claim_boundary_v160"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v160_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v160"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v160"]) == "162228701"
    assert str(best["dropped_loan_id_v160"]) == "167046951"
    assert float(best["return_delta_v160"]) == pytest.approx(
        status["best_one_swap_return_delta_v160"]
    )
    assert float(best["exposure_after_swap_v160"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v160"]) is True

    summary = _read_csv("paper4_v160_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v160"]) == status["one_swap_improving_rows_v160"]
    assert float(row["current_exposure_v160"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v160"]) == pytest.approx(900.8501798956848)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v160"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v160"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v160"])

    stage_summary = _read_csv("paper4_v160_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v160", "pair_rows_v160", "claim_boundary_v160"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v160"], stage_summary["pair_rows_v160"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v160"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v160"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v160"]

    blockers = _read_csv("paper4_v160_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v160"], blockers["blocking_v160"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v160"], blockers["evidence_count_v160"], strict=False)
    )
    assert bool(blocker_map["post_v159_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v159_one_swap_improvement_found"]) == 2211
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v160_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v160_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v160_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v160_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v160_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v160 post-v159 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v160 proves the v159 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v160 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v160: Post-v159 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `2211`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v161_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v161_status.json")

    assert status["phase"] == "v161_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.161"
    assert status["allocation_rows_v161"] == 171
    assert status["summary_rows_v161"] == 1
    assert status["action_rows_v161"] == 1
    assert status["source_summary_rows_v161"] == 51
    assert status["claim_blocker_rows_v161"] == 4
    assert status["added_loan_id_v161"] == "162228701"
    assert status["dropped_loan_id_v161"] == "167046951"
    assert status["selected_rows_v161"] == 171
    assert status["portfolio_exposure_v161"] == pytest.approx(842400.0)
    assert status["objective_return_v161"] == pytest.approx(974.4177452576505)
    assert status["scenario_loss_cvar90_v161"] == pytest.approx(95213.35540496597)
    assert status["source_cap_violations_v161"] == 0
    assert status["delta_return_vs_v159_v161"] == pytest.approx(73.56756536196565)
    assert status["delta_cvar90_vs_v159_v161"] == pytest.approx(-17.338096311868867)
    assert status["delta_exposure_vs_v159_v161"] == pytest.approx(0.0)
    assert status["budget_feasible_v161"] is True
    assert status["source_feasible_v161"] is True
    assert status["cvar_feasible_v161"] is True
    assert status["repair_candidate_feasible_v161"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v161"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v161"] is False
    assert status["paper1_promotion_allowed_v161"] is False
    assert status["paper4_working_champion_changed_v161"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v161_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v161",
        "selected_v161",
        "portfolio_label_v161",
        "repair_action_v161",
        "claim_boundary_v161",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v161"]
    assert int(allocations["selected_v161"].sum()) == status["selected_rows_v161"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v161"])
    assert "162228701" in set(allocations["loan_id"].astype(str))
    assert "167046951" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v161"]) == {
        "added_from_v160_best_swap",
        "kept_from_v159",
    }
    assert allocations["claim_boundary_v161"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v161_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v161"]) is True
    assert float(row["min_source_slack_v161"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v159_v161"]) < 0
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v161"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v161"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v161"])

    action = _read_csv("paper4_v161_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v161"]) == status["added_loan_id_v161"]
    assert str(action_row["dropped_loan_id_v161"]) == status["dropped_loan_id_v161"]
    assert float(action_row["return_delta_v161"]) == pytest.approx(73.56756536196579)
    assert float(action_row["cvar90_after_repair_v161"]) == pytest.approx(
        status["scenario_loss_cvar90_v161"]
    )
    assert float(action_row["exposure_after_repair_v161"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v161"]) == 0

    source_summary = _read_csv("paper4_v161_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v161",
        "source_slack_v161",
        "source_cap_violated_v161",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v161"]
    assert not source_summary["source_cap_violated_v161"].astype(bool).any()

    blockers = _read_csv("paper4_v161_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v161"], blockers["blocking_v161"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v161"], blockers["evidence_count_v161"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v161_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v161_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v161_repair_candidate_feasible"]) is True
    assert bool(claim_map["v161_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v161_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v161_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v161 fortieth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v161 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v161 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v161: Fortieth One-Swap Repair Candidate" in notebook
    assert "v162 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v162_post_v161_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v162_status.json")

    assert status["phase"] == "v162_post_v161_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.162"
    assert status["summary_rows_v162"] == 1
    assert status["stage_summary_rows_v162"] == 6
    assert status["candidate_pair_rows_v162"] == 1963
    assert status["top_candidate_rows_v162"] == 200
    assert status["claim_blocker_rows_v162"] == 3
    assert status["selected_rows_v162"] == 171
    assert status["candidate_add_rows_v162"] == 276698
    assert status["total_pair_rows_screened_v162"] == 47315358
    assert status["return_improving_pair_rows_v162"] == 1797235
    assert status["budget_return_feasible_pair_rows_v162"] == 1110011
    assert status["source_prefilter_pair_rows_v162"] == 2055
    assert status["source_exact_pair_rows_v162"] == 1963
    assert status["cvar_feasible_pair_rows_v162"] == 1963
    assert status["one_swap_improving_rows_v162"] == 1963
    assert status["best_one_swap_return_delta_v162"] == pytest.approx(73.47581065600826)
    assert status["best_one_swap_cvar90_after_v162"] == pytest.approx(95396.44025571868)
    assert status["post_repair_one_swap_local_optimality_cleared_v162"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v162"] is False
    assert status["paper1_promotion_allowed_v162"] is False
    assert status["paper4_working_champion_changed_v162"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v162_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v162",
        "dropped_loan_id_v162",
        "return_delta_v162",
        "objective_return_after_swap_v162",
        "budget_swap_feasible_v162",
        "source_swap_feasible_v162",
        "source_cap_violations_after_swap_v162",
        "cvar_swap_feasible_v162",
        "one_swap_improves_return_v162",
        "claim_boundary_v162",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v162"]
    assert probe["return_delta_v162"].gt(0).all()
    assert probe["budget_swap_feasible_v162"].astype(bool).all()
    assert probe["source_swap_feasible_v162"].astype(bool).all()
    assert probe["cvar_swap_feasible_v162"].astype(bool).all()
    assert probe["one_swap_improves_return_v162"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v162"].sum()) == 0
    assert probe["return_delta_v162"].max() == pytest.approx(
        status["best_one_swap_return_delta_v162"]
    )
    assert probe["claim_boundary_v162"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v162_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v162"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v162"]) == "148025050"
    assert str(best["dropped_loan_id_v162"]) == "169005860"
    assert float(best["return_delta_v162"]) == pytest.approx(
        status["best_one_swap_return_delta_v162"]
    )
    assert float(best["exposure_after_swap_v162"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v162"]) is True

    summary = _read_csv("paper4_v162_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v162"]) == status["one_swap_improving_rows_v162"]
    assert float(row["current_exposure_v162"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v162"]) == pytest.approx(974.4177452576504)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v162"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v162"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v162"])

    stage_summary = _read_csv("paper4_v162_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v162", "pair_rows_v162", "claim_boundary_v162"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v162"], stage_summary["pair_rows_v162"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v162"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v162"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v162"]

    blockers = _read_csv("paper4_v162_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v162"], blockers["blocking_v162"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v162"], blockers["evidence_count_v162"], strict=False)
    )
    assert bool(blocker_map["post_v161_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v161_one_swap_improvement_found"]) == 1963
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v162_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v162_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v162_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v162_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v162_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v162 post-v161 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v162 proves the v161 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v162 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v162: Post-v161 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1963`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v163_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v163_status.json")

    assert status["phase"] == "v163_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.163"
    assert status["allocation_rows_v163"] == 171
    assert status["summary_rows_v163"] == 1
    assert status["action_rows_v163"] == 1
    assert status["source_summary_rows_v163"] == 51
    assert status["claim_blocker_rows_v163"] == 4
    assert status["added_loan_id_v163"] == "148025050"
    assert status["dropped_loan_id_v163"] == "169005860"
    assert status["selected_rows_v163"] == 171
    assert status["portfolio_exposure_v163"] == pytest.approx(842400.0)
    assert status["objective_return_v163"] == pytest.approx(1047.8935559136587)
    assert status["scenario_loss_cvar90_v163"] == pytest.approx(95396.44025571868)
    assert status["source_cap_violations_v163"] == 0
    assert status["delta_return_vs_v161_v163"] == pytest.approx(73.47581065600832)
    assert status["delta_cvar90_vs_v161_v163"] == pytest.approx(183.0848507527262)
    assert status["delta_exposure_vs_v161_v163"] == pytest.approx(0.0)
    assert status["budget_feasible_v163"] is True
    assert status["source_feasible_v163"] is True
    assert status["cvar_feasible_v163"] is True
    assert status["repair_candidate_feasible_v163"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v163"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v163"] is False
    assert status["paper1_promotion_allowed_v163"] is False
    assert status["paper4_working_champion_changed_v163"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v163_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v163",
        "selected_v163",
        "portfolio_label_v163",
        "repair_action_v163",
        "claim_boundary_v163",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v163"]
    assert int(allocations["selected_v163"].sum()) == status["selected_rows_v163"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v163"])
    assert "148025050" in set(allocations["loan_id"].astype(str))
    assert "169005860" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v163"]) == {
        "added_from_v162_best_swap",
        "kept_from_v161",
    }
    assert allocations["claim_boundary_v163"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v163_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v163"]) is True
    assert float(row["min_source_slack_v163"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v161_v163"]) > 0
    assert float(row["scenario_loss_cvar90_v163"]) <= float(row["cvar_cap_v163"])
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v163"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v163"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v163"])

    action = _read_csv("paper4_v163_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v163"]) == status["added_loan_id_v163"]
    assert str(action_row["dropped_loan_id_v163"]) == status["dropped_loan_id_v163"]
    assert float(action_row["return_delta_v163"]) == pytest.approx(73.47581065600826)
    assert float(action_row["cvar90_after_repair_v163"]) == pytest.approx(
        status["scenario_loss_cvar90_v163"]
    )
    assert float(action_row["exposure_after_repair_v163"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v163"]) == 0

    source_summary = _read_csv("paper4_v163_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v163",
        "source_slack_v163",
        "source_cap_violated_v163",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v163"]
    assert not source_summary["source_cap_violated_v163"].astype(bool).any()

    blockers = _read_csv("paper4_v163_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v163"], blockers["blocking_v163"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v163"], blockers["evidence_count_v163"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v163_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v163_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v163_repair_candidate_feasible"]) is True
    assert bool(claim_map["v163_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v163_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v163_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v163 forty-first one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v163 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v163 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v163: Forty-First One-Swap Repair Candidate" in notebook
    assert "v164 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v164_post_v163_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v164_status.json")

    assert status["phase"] == "v164_post_v163_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.164"
    assert status["summary_rows_v164"] == 1
    assert status["stage_summary_rows_v164"] == 6
    assert status["candidate_pair_rows_v164"] == 1939
    assert status["top_candidate_rows_v164"] == 200
    assert status["claim_blocker_rows_v164"] == 3
    assert status["selected_rows_v164"] == 171
    assert status["candidate_add_rows_v164"] == 276698
    assert status["total_pair_rows_screened_v164"] == 47315358
    assert status["return_improving_pair_rows_v164"] == 1792253
    assert status["budget_return_feasible_pair_rows_v164"] == 1107774
    assert status["source_prefilter_pair_rows_v164"] == 2028
    assert status["source_exact_pair_rows_v164"] == 1939
    assert status["cvar_feasible_pair_rows_v164"] == 1939
    assert status["one_swap_improving_rows_v164"] == 1939
    assert status["best_one_swap_return_delta_v164"] == pytest.approx(71.68345973431857)
    assert status["best_one_swap_cvar90_after_v164"] == pytest.approx(95372.98132877337)
    assert status["post_repair_one_swap_local_optimality_cleared_v164"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v164"] is False
    assert status["paper1_promotion_allowed_v164"] is False
    assert status["paper4_working_champion_changed_v164"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v164_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v164",
        "dropped_loan_id_v164",
        "return_delta_v164",
        "objective_return_after_swap_v164",
        "budget_swap_feasible_v164",
        "source_swap_feasible_v164",
        "source_cap_violations_after_swap_v164",
        "cvar_swap_feasible_v164",
        "one_swap_improves_return_v164",
        "claim_boundary_v164",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v164"]
    assert probe["return_delta_v164"].gt(0).all()
    assert probe["budget_swap_feasible_v164"].astype(bool).all()
    assert probe["source_swap_feasible_v164"].astype(bool).all()
    assert probe["cvar_swap_feasible_v164"].astype(bool).all()
    assert probe["one_swap_improves_return_v164"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v164"].sum()) == 0
    assert probe["return_delta_v164"].max() == pytest.approx(
        status["best_one_swap_return_delta_v164"]
    )
    assert probe["claim_boundary_v164"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v164_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v164"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v164"]) == "161494495"
    assert str(best["dropped_loan_id_v164"]) == "126707528"
    assert float(best["return_delta_v164"]) == pytest.approx(
        status["best_one_swap_return_delta_v164"]
    )
    assert float(best["exposure_after_swap_v164"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v164"]) is True

    summary = _read_csv("paper4_v164_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v164"]) == status["one_swap_improving_rows_v164"]
    assert float(row["current_exposure_v164"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v164"]) == pytest.approx(1047.8935559136587)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v164"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v164"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v164"])

    stage_summary = _read_csv("paper4_v164_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v164", "pair_rows_v164", "claim_boundary_v164"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v164"], stage_summary["pair_rows_v164"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v164"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v164"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v164"]

    blockers = _read_csv("paper4_v164_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v164"], blockers["blocking_v164"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v164"], blockers["evidence_count_v164"], strict=False)
    )
    assert bool(blocker_map["post_v163_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v163_one_swap_improvement_found"]) == 1939
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v164_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v164_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v164_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v164_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v164_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v164 post-v163 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v164 proves the v163 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v164 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v164: Post-v163 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1939`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v165_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v165_status.json")

    assert status["phase"] == "v165_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.165"
    assert status["allocation_rows_v165"] == 171
    assert status["summary_rows_v165"] == 1
    assert status["action_rows_v165"] == 1
    assert status["source_summary_rows_v165"] == 51
    assert status["claim_blocker_rows_v165"] == 4
    assert status["added_loan_id_v165"] == "161494495"
    assert status["dropped_loan_id_v165"] == "126707528"
    assert status["selected_rows_v165"] == 171
    assert status["portfolio_exposure_v165"] == pytest.approx(842400.0)
    assert status["objective_return_v165"] == pytest.approx(1119.5770156479769)
    assert status["scenario_loss_cvar90_v165"] == pytest.approx(95372.98132877337)
    assert status["source_cap_violations_v165"] == 0
    assert status["delta_return_vs_v163_v165"] == pytest.approx(71.68345973431815)
    assert status["delta_cvar90_vs_v163_v165"] == pytest.approx(-23.45892694531358)
    assert status["delta_exposure_vs_v163_v165"] == pytest.approx(0.0)
    assert status["budget_feasible_v165"] is True
    assert status["source_feasible_v165"] is True
    assert status["cvar_feasible_v165"] is True
    assert status["repair_candidate_feasible_v165"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v165"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v165"] is False
    assert status["paper1_promotion_allowed_v165"] is False
    assert status["paper4_working_champion_changed_v165"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v165_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v165",
        "selected_v165",
        "portfolio_label_v165",
        "repair_action_v165",
        "claim_boundary_v165",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v165"]
    assert int(allocations["selected_v165"].sum()) == status["selected_rows_v165"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v165"])
    assert "161494495" in set(allocations["loan_id"].astype(str))
    assert "126707528" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v165"]) == {
        "added_from_v164_best_swap",
        "kept_from_v163",
    }
    assert allocations["claim_boundary_v165"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v165_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v165"]) is True
    assert float(row["min_source_slack_v165"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v163_v165"]) < 0
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v165"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v165"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v165"])

    action = _read_csv("paper4_v165_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v165"]) == status["added_loan_id_v165"]
    assert str(action_row["dropped_loan_id_v165"]) == status["dropped_loan_id_v165"]
    assert float(action_row["return_delta_v165"]) == pytest.approx(71.68345973431857)
    assert float(action_row["cvar90_after_repair_v165"]) == pytest.approx(
        status["scenario_loss_cvar90_v165"]
    )
    assert float(action_row["exposure_after_repair_v165"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v165"]) == 0

    source_summary = _read_csv("paper4_v165_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v165",
        "source_slack_v165",
        "source_cap_violated_v165",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v165"]
    assert not source_summary["source_cap_violated_v165"].astype(bool).any()

    blockers = _read_csv("paper4_v165_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v165"], blockers["blocking_v165"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v165"], blockers["evidence_count_v165"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v165_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v165_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v165_repair_candidate_feasible"]) is True
    assert bool(claim_map["v165_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v165_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v165_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v165 forty-second one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v165 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v165 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v165: Forty-Second One-Swap Repair Candidate" in notebook
    assert "v166 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v166_post_v165_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v166_status.json")

    assert status["phase"] == "v166_post_v165_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.166"
    assert status["summary_rows_v166"] == 1
    assert status["stage_summary_rows_v166"] == 6
    assert status["candidate_pair_rows_v166"] == 1695
    assert status["top_candidate_rows_v166"] == 200
    assert status["claim_blocker_rows_v166"] == 3
    assert status["selected_rows_v166"] == 171
    assert status["candidate_add_rows_v166"] == 276698
    assert status["total_pair_rows_screened_v166"] == 47315358
    assert status["return_improving_pair_rows_v166"] == 1787214
    assert status["budget_return_feasible_pair_rows_v166"] == 1104516
    assert status["source_prefilter_pair_rows_v166"] == 1784
    assert status["source_exact_pair_rows_v166"] == 1695
    assert status["cvar_feasible_pair_rows_v166"] == 1695
    assert status["one_swap_improving_rows_v166"] == 1695
    assert status["best_one_swap_return_delta_v166"] == pytest.approx(70.20781030153404)
    assert status["best_one_swap_cvar90_after_v166"] == pytest.approx(95856.03208682848)
    assert status["post_repair_one_swap_local_optimality_cleared_v166"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v166"] is False
    assert status["paper1_promotion_allowed_v166"] is False
    assert status["paper4_working_champion_changed_v166"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v166_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v166",
        "dropped_loan_id_v166",
        "return_delta_v166",
        "objective_return_after_swap_v166",
        "budget_swap_feasible_v166",
        "source_swap_feasible_v166",
        "source_cap_violations_after_swap_v166",
        "cvar_swap_feasible_v166",
        "one_swap_improves_return_v166",
        "claim_boundary_v166",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v166"]
    assert probe["return_delta_v166"].gt(0).all()
    assert probe["budget_swap_feasible_v166"].astype(bool).all()
    assert probe["source_swap_feasible_v166"].astype(bool).all()
    assert probe["cvar_swap_feasible_v166"].astype(bool).all()
    assert probe["one_swap_improves_return_v166"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v166"].sum()) == 0
    assert probe["return_delta_v166"].max() == pytest.approx(
        status["best_one_swap_return_delta_v166"]
    )
    assert probe["claim_boundary_v166"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v166_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v166"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v166"]) == "140623777"
    assert str(best["dropped_loan_id_v166"]) == "167137941"
    assert float(best["return_delta_v166"]) == pytest.approx(
        status["best_one_swap_return_delta_v166"]
    )
    assert float(best["exposure_after_swap_v166"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v166"]) is True

    summary = _read_csv("paper4_v166_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v166"]) == status["one_swap_improving_rows_v166"]
    assert float(row["current_exposure_v166"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v166"]) == pytest.approx(1119.5770156479769)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v166"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v166"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v166"])

    stage_summary = _read_csv("paper4_v166_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v166", "pair_rows_v166", "claim_boundary_v166"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v166"], stage_summary["pair_rows_v166"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v166"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v166"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v166"]

    blockers = _read_csv("paper4_v166_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v166"], blockers["blocking_v166"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v166"], blockers["evidence_count_v166"], strict=False)
    )
    assert bool(blocker_map["post_v165_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v165_one_swap_improvement_found"]) == 1695
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v166_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v166_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v166_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v166_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v166_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v166 post-v165 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v166 proves the v165 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v166 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v166: Post-v165 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1695`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v167_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v167_status.json")

    assert status["phase"] == "v167_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.167"
    assert status["allocation_rows_v167"] == 171
    assert status["summary_rows_v167"] == 1
    assert status["action_rows_v167"] == 1
    assert status["source_summary_rows_v167"] == 51
    assert status["claim_blocker_rows_v167"] == 4
    assert status["added_loan_id_v167"] == "140623777"
    assert status["dropped_loan_id_v167"] == "167137941"
    assert status["selected_rows_v167"] == 171
    assert status["portfolio_exposure_v167"] == pytest.approx(842400.0)
    assert status["objective_return_v167"] == pytest.approx(1189.7848259495108)
    assert status["scenario_loss_cvar90_v167"] == pytest.approx(95856.03208682849)
    assert status["source_cap_violations_v167"] == 0
    assert status["delta_return_vs_v165_v167"] == pytest.approx(70.20781030153398)
    assert status["delta_cvar90_vs_v165_v167"] == pytest.approx(483.0507580551348)
    assert status["delta_exposure_vs_v165_v167"] == pytest.approx(0.0)
    assert status["budget_feasible_v167"] is True
    assert status["source_feasible_v167"] is True
    assert status["cvar_feasible_v167"] is True
    assert status["repair_candidate_feasible_v167"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v167"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v167"] is False
    assert status["paper1_promotion_allowed_v167"] is False
    assert status["paper4_working_champion_changed_v167"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v167_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v167",
        "selected_v167",
        "portfolio_label_v167",
        "repair_action_v167",
        "claim_boundary_v167",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v167"]
    assert int(allocations["selected_v167"].sum()) == status["selected_rows_v167"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v167"])
    assert "140623777" in set(allocations["loan_id"].astype(str))
    assert "167137941" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v167"]) == {
        "added_from_v166_best_swap",
        "kept_from_v165",
    }
    assert allocations["claim_boundary_v167"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v167_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v167"]) is True
    assert float(row["max_source_share_v167"]) == pytest.approx(0.8956552706552706)
    assert float(row["min_source_slack_v167"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v165_v167"]) > 0
    assert float(row["scenario_loss_cvar90_v167"]) <= float(row["cvar_cap_v167"])
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v167"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v167"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v167"])

    action = _read_csv("paper4_v167_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v167"]) == status["added_loan_id_v167"]
    assert str(action_row["dropped_loan_id_v167"]) == status["dropped_loan_id_v167"]
    assert float(action_row["return_delta_v167"]) == pytest.approx(70.20781030153404)
    assert float(action_row["cvar90_after_repair_v167"]) == pytest.approx(
        status["scenario_loss_cvar90_v167"]
    )
    assert float(action_row["exposure_after_repair_v167"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v167"]) == 0

    source_summary = _read_csv("paper4_v167_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v167",
        "source_slack_v167",
        "source_cap_violated_v167",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v167"]
    assert not source_summary["source_cap_violated_v167"].astype(bool).any()

    blockers = _read_csv("paper4_v167_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v167"], blockers["blocking_v167"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v167"], blockers["evidence_count_v167"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v167_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v167_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v167_repair_candidate_feasible"]) is True
    assert bool(claim_map["v167_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v167_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v167_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v167 forty-third one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v167 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v167 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v167: Forty-Third One-Swap Repair Candidate" in notebook
    assert "v168 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v168_post_v167_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v168_status.json")

    assert status["phase"] == "v168_post_v167_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.168"
    assert status["summary_rows_v168"] == 1
    assert status["stage_summary_rows_v168"] == 6
    assert status["candidate_pair_rows_v168"] == 3688
    assert status["top_candidate_rows_v168"] == 200
    assert status["claim_blocker_rows_v168"] == 3
    assert status["selected_rows_v168"] == 171
    assert status["candidate_add_rows_v168"] == 276698
    assert status["total_pair_rows_screened_v168"] == 47315358
    assert status["return_improving_pair_rows_v168"] == 1782772
    assert status["budget_return_feasible_pair_rows_v168"] == 1102270
    assert status["source_prefilter_pair_rows_v168"] == 3840
    assert status["source_exact_pair_rows_v168"] == 3688
    assert status["cvar_feasible_pair_rows_v168"] == 3688
    assert status["one_swap_improving_rows_v168"] == 3688
    assert status["best_one_swap_return_delta_v168"] == pytest.approx(119.49376223708978)
    assert status["best_one_swap_cvar90_after_v168"] == pytest.approx(95616.12937825698)
    assert status["post_repair_one_swap_local_optimality_cleared_v168"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v168"] is False
    assert status["paper1_promotion_allowed_v168"] is False
    assert status["paper4_working_champion_changed_v168"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v168_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v168",
        "dropped_loan_id_v168",
        "return_delta_v168",
        "objective_return_after_swap_v168",
        "budget_swap_feasible_v168",
        "source_swap_feasible_v168",
        "source_cap_violations_after_swap_v168",
        "cvar_swap_feasible_v168",
        "one_swap_improves_return_v168",
        "claim_boundary_v168",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v168"]
    assert probe["return_delta_v168"].gt(0).all()
    assert probe["budget_swap_feasible_v168"].astype(bool).all()
    assert probe["source_swap_feasible_v168"].astype(bool).all()
    assert probe["cvar_swap_feasible_v168"].astype(bool).all()
    assert probe["one_swap_improves_return_v168"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v168"].sum()) == 0
    assert probe["return_delta_v168"].max() == pytest.approx(
        status["best_one_swap_return_delta_v168"]
    )
    assert probe["claim_boundary_v168"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v168_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v168"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v168"]) == "145117824"
    assert str(best["dropped_loan_id_v168"]) == "167713836"
    assert float(best["return_delta_v168"]) == pytest.approx(
        status["best_one_swap_return_delta_v168"]
    )
    assert float(best["exposure_after_swap_v168"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v168"]) is True

    summary = _read_csv("paper4_v168_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v168"]) == status["one_swap_improving_rows_v168"]
    assert float(row["current_exposure_v168"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v168"]) == pytest.approx(1189.7848259495108)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v168"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v168"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v168"])

    stage_summary = _read_csv("paper4_v168_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v168", "pair_rows_v168", "claim_boundary_v168"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v168"], stage_summary["pair_rows_v168"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v168"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v168"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v168"]

    blockers = _read_csv("paper4_v168_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v168"], blockers["blocking_v168"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v168"], blockers["evidence_count_v168"], strict=False)
    )
    assert bool(blocker_map["post_v167_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v167_one_swap_improvement_found"]) == 3688
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v168_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v168_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v168_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v168_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v168_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v168 post-v167 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v168 proves the v167 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v168 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v168: Post-v167 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3688`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v169_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v169_status.json")

    assert status["phase"] == "v169_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.169"
    assert status["allocation_rows_v169"] == 171
    assert status["summary_rows_v169"] == 1
    assert status["action_rows_v169"] == 1
    assert status["source_summary_rows_v169"] == 51
    assert status["claim_blocker_rows_v169"] == 4
    assert status["added_loan_id_v169"] == "145117824"
    assert status["dropped_loan_id_v169"] == "167713836"
    assert status["selected_rows_v169"] == 171
    assert status["portfolio_exposure_v169"] == pytest.approx(842400.0)
    assert status["objective_return_v169"] == pytest.approx(1309.2785881866005)
    assert status["scenario_loss_cvar90_v169"] == pytest.approx(95616.12937825698)
    assert status["source_cap_violations_v169"] == 0
    assert status["delta_return_vs_v167_v169"] == pytest.approx(119.49376223708964)
    assert status["delta_cvar90_vs_v167_v169"] == pytest.approx(-239.90270857149153)
    assert status["delta_exposure_vs_v167_v169"] == pytest.approx(0.0)
    assert status["budget_feasible_v169"] is True
    assert status["source_feasible_v169"] is True
    assert status["cvar_feasible_v169"] is True
    assert status["repair_candidate_feasible_v169"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v169"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v169"] is False
    assert status["paper1_promotion_allowed_v169"] is False
    assert status["paper4_working_champion_changed_v169"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v169_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v169",
        "selected_v169",
        "portfolio_label_v169",
        "repair_action_v169",
        "claim_boundary_v169",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v169"]
    assert int(allocations["selected_v169"].sum()) == status["selected_rows_v169"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v169"])
    assert "145117824" in set(allocations["loan_id"].astype(str))
    assert "167713836" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v169"]) == {
        "added_from_v168_best_swap",
        "kept_from_v167",
    }
    assert allocations["claim_boundary_v169"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v169_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v169"]) is True
    assert float(row["max_source_share_v169"]) == pytest.approx(0.8974358974358975)
    assert float(row["min_source_slack_v169"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v167_v169"]) < 0
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v169"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v169"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v169"])

    action = _read_csv("paper4_v169_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v169"]) == status["added_loan_id_v169"]
    assert str(action_row["dropped_loan_id_v169"]) == status["dropped_loan_id_v169"]
    assert float(action_row["return_delta_v169"]) == pytest.approx(119.49376223708978)
    assert float(action_row["cvar90_after_repair_v169"]) == pytest.approx(
        status["scenario_loss_cvar90_v169"]
    )
    assert float(action_row["exposure_after_repair_v169"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v169"]) == 0

    source_summary = _read_csv("paper4_v169_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v169",
        "source_slack_v169",
        "source_cap_violated_v169",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v169"]
    assert not source_summary["source_cap_violated_v169"].astype(bool).any()

    blockers = _read_csv("paper4_v169_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v169"], blockers["blocking_v169"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v169"], blockers["evidence_count_v169"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v169_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v169_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v169_repair_candidate_feasible"]) is True
    assert bool(claim_map["v169_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v169_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v169_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v169 forty-fourth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v169 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v169 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v169: Forty-Fourth One-Swap Repair Candidate" in notebook
    assert "v170 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v170_post_v169_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v170_status.json")

    assert status["phase"] == "v170_post_v169_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.170"
    assert status["summary_rows_v170"] == 1
    assert status["stage_summary_rows_v170"] == 6
    assert status["candidate_pair_rows_v170"] == 3572
    assert status["top_candidate_rows_v170"] == 200
    assert status["claim_blocker_rows_v170"] == 3
    assert status["selected_rows_v170"] == 171
    assert status["candidate_add_rows_v170"] == 276698
    assert status["total_pair_rows_screened_v170"] == 47315358
    assert status["return_improving_pair_rows_v170"] == 1773459
    assert status["budget_return_feasible_pair_rows_v170"] == 1097847
    assert status["source_prefilter_pair_rows_v170"] == 3693
    assert status["source_exact_pair_rows_v170"] == 3572
    assert status["cvar_feasible_pair_rows_v170"] == 3572
    assert status["one_swap_improving_rows_v170"] == 3572
    assert status["best_one_swap_return_delta_v170"] == pytest.approx(115.9846875108)
    assert status["best_one_swap_cvar90_after_v170"] == pytest.approx(95525.96161168531)
    assert status["post_repair_one_swap_local_optimality_cleared_v170"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v170"] is False
    assert status["paper1_promotion_allowed_v170"] is False
    assert status["paper4_working_champion_changed_v170"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v170_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v170",
        "dropped_loan_id_v170",
        "return_delta_v170",
        "objective_return_after_swap_v170",
        "budget_swap_feasible_v170",
        "source_swap_feasible_v170",
        "source_cap_violations_after_swap_v170",
        "cvar_swap_feasible_v170",
        "one_swap_improves_return_v170",
        "claim_boundary_v170",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v170"]
    assert probe["return_delta_v170"].gt(0).all()
    assert probe["budget_swap_feasible_v170"].astype(bool).all()
    assert probe["source_swap_feasible_v170"].astype(bool).all()
    assert probe["cvar_swap_feasible_v170"].astype(bool).all()
    assert probe["one_swap_improves_return_v170"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v170"].sum()) == 0
    assert probe["return_delta_v170"].max() == pytest.approx(
        status["best_one_swap_return_delta_v170"]
    )
    assert probe["claim_boundary_v170"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v170_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v170"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v170"]) == "144399667"
    assert str(best["dropped_loan_id_v170"]) == "126620912"
    assert float(best["return_delta_v170"]) == pytest.approx(
        status["best_one_swap_return_delta_v170"]
    )
    assert float(best["objective_return_after_swap_v170"]) == pytest.approx(1425.2632756974006)
    assert float(best["exposure_after_swap_v170"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v170"]) is True

    summary = _read_csv("paper4_v170_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v170"]) == status["one_swap_improving_rows_v170"]
    assert float(row["current_exposure_v170"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v170"]) == pytest.approx(1309.2785881866005)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v170"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v170"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v170"])

    stage_summary = _read_csv("paper4_v170_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v170", "pair_rows_v170", "claim_boundary_v170"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v170"], stage_summary["pair_rows_v170"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v170"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v170"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v170"]

    blockers = _read_csv("paper4_v170_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v170"], blockers["blocking_v170"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v170"], blockers["evidence_count_v170"], strict=False)
    )
    assert bool(blocker_map["post_v169_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v169_one_swap_improvement_found"]) == 3572
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v170_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v170_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v170_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v170_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v170_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v170 post-v169 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v170 proves the v169 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v170 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v170: Post-v169 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `3572`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v171_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v171_status.json")

    assert status["phase"] == "v171_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.171"
    assert status["allocation_rows_v171"] == 171
    assert status["summary_rows_v171"] == 1
    assert status["action_rows_v171"] == 1
    assert status["source_summary_rows_v171"] == 51
    assert status["claim_blocker_rows_v171"] == 4
    assert status["added_loan_id_v171"] == "144399667"
    assert status["dropped_loan_id_v171"] == "126620912"
    assert status["selected_rows_v171"] == 171
    assert status["portfolio_exposure_v171"] == pytest.approx(842400.0)
    assert status["objective_return_v171"] == pytest.approx(1425.2632756974012)
    assert status["scenario_loss_cvar90_v171"] == pytest.approx(95525.96161168528)
    assert status["source_cap_violations_v171"] == 0
    assert status["delta_return_vs_v169_v171"] == pytest.approx(115.98468751080077)
    assert status["delta_cvar90_vs_v169_v171"] == pytest.approx(-90.16776657169976)
    assert status["delta_exposure_vs_v169_v171"] == pytest.approx(0.0)
    assert status["budget_feasible_v171"] is True
    assert status["source_feasible_v171"] is True
    assert status["cvar_feasible_v171"] is True
    assert status["repair_candidate_feasible_v171"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v171"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v171"] is False
    assert status["paper1_promotion_allowed_v171"] is False
    assert status["paper4_working_champion_changed_v171"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v171_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v171",
        "selected_v171",
        "portfolio_label_v171",
        "repair_action_v171",
        "claim_boundary_v171",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v171"]
    assert int(allocations["selected_v171"].sum()) == status["selected_rows_v171"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v171"])
    assert "144399667" in set(allocations["loan_id"].astype(str))
    assert "126620912" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v171"]) == {
        "added_from_v170_best_swap",
        "kept_from_v169",
    }
    assert allocations["claim_boundary_v171"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v171_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v171"]) is True
    assert float(row["max_source_share_v171"]) == pytest.approx(0.8992165242165242)
    assert float(row["min_source_slack_v171"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v169_v171"]) < 0
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v171"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v171"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v171"])

    action = _read_csv("paper4_v171_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v171"]) == status["added_loan_id_v171"]
    assert str(action_row["dropped_loan_id_v171"]) == status["dropped_loan_id_v171"]
    assert float(action_row["return_delta_v171"]) == pytest.approx(115.9846875108)
    assert float(action_row["cvar90_after_repair_v171"]) == pytest.approx(
        status["scenario_loss_cvar90_v171"]
    )
    assert float(action_row["exposure_after_repair_v171"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v171"]) == 0

    source_summary = _read_csv("paper4_v171_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v171",
        "source_slack_v171",
        "source_cap_violated_v171",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v171"]
    assert not source_summary["source_cap_violated_v171"].astype(bool).any()

    blockers = _read_csv("paper4_v171_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v171"], blockers["blocking_v171"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v171"], blockers["evidence_count_v171"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v171_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v171_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v171_repair_candidate_feasible"]) is True
    assert bool(claim_map["v171_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v171_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v171_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v171 forty-fifth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v171 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v171 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v171: Forty-Fifth One-Swap Repair Candidate" in notebook
    assert "v172 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v172_post_v171_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v172_status.json")

    assert status["phase"] == "v172_post_v171_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.172"
    assert status["summary_rows_v172"] == 1
    assert status["stage_summary_rows_v172"] == 6
    assert status["candidate_pair_rows_v172"] == 1598
    assert status["top_candidate_rows_v172"] == 200
    assert status["claim_blocker_rows_v172"] == 3
    assert status["selected_rows_v172"] == 171
    assert status["candidate_add_rows_v172"] == 276698
    assert status["total_pair_rows_screened_v172"] == 47315358
    assert status["return_improving_pair_rows_v172"] == 1764415
    assert status["budget_return_feasible_pair_rows_v172"] == 1093546
    assert status["source_prefilter_pair_rows_v172"] == 1646
    assert status["source_exact_pair_rows_v172"] == 1598
    assert status["cvar_feasible_pair_rows_v172"] == 1598
    assert status["one_swap_improving_rows_v172"] == 1598
    assert status["best_one_swap_return_delta_v172"] == pytest.approx(69.08822213844525)
    assert status["best_one_swap_cvar90_after_v172"] == pytest.approx(95664.47349143549)
    assert status["post_repair_one_swap_local_optimality_cleared_v172"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v172"] is False
    assert status["paper1_promotion_allowed_v172"] is False
    assert status["paper4_working_champion_changed_v172"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v172_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v172",
        "dropped_loan_id_v172",
        "return_delta_v172",
        "objective_return_after_swap_v172",
        "budget_swap_feasible_v172",
        "source_swap_feasible_v172",
        "source_cap_violations_after_swap_v172",
        "cvar_swap_feasible_v172",
        "one_swap_improves_return_v172",
        "claim_boundary_v172",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v172"]
    assert probe["return_delta_v172"].gt(0).all()
    assert probe["budget_swap_feasible_v172"].astype(bool).all()
    assert probe["source_swap_feasible_v172"].astype(bool).all()
    assert probe["cvar_swap_feasible_v172"].astype(bool).all()
    assert probe["one_swap_improves_return_v172"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v172"].sum()) == 0
    assert probe["return_delta_v172"].max() == pytest.approx(
        status["best_one_swap_return_delta_v172"]
    )
    assert probe["claim_boundary_v172"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v172_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v172"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v172"]) == "144038628"
    assert str(best["dropped_loan_id_v172"]) == "126409482"
    assert float(best["return_delta_v172"]) == pytest.approx(
        status["best_one_swap_return_delta_v172"]
    )
    assert float(best["objective_return_after_swap_v172"]) == pytest.approx(1494.3514978358464)
    assert float(best["exposure_after_swap_v172"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v172"]) is True

    summary = _read_csv("paper4_v172_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v172"]) == status["one_swap_improving_rows_v172"]
    assert float(row["current_exposure_v172"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v172"]) == pytest.approx(1425.2632756974012)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v172"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v172"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v172"])

    stage_summary = _read_csv("paper4_v172_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v172", "pair_rows_v172", "claim_boundary_v172"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v172"], stage_summary["pair_rows_v172"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v172"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v172"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v172"]

    blockers = _read_csv("paper4_v172_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v172"], blockers["blocking_v172"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v172"], blockers["evidence_count_v172"], strict=False)
    )
    assert bool(blocker_map["post_v171_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v171_one_swap_improvement_found"]) == 1598
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v172_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v172_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v172_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v172_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v172_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v172 post-v171 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v172 proves the v171 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v172 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v172: Post-v171 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1598`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v173_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v173_status.json")

    assert status["phase"] == "v173_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.173"
    assert status["allocation_rows_v173"] == 171
    assert status["summary_rows_v173"] == 1
    assert status["action_rows_v173"] == 1
    assert status["source_summary_rows_v173"] == 51
    assert status["claim_blocker_rows_v173"] == 4
    assert status["added_loan_id_v173"] == "144038628"
    assert status["dropped_loan_id_v173"] == "126409482"
    assert status["selected_rows_v173"] == 171
    assert status["portfolio_exposure_v173"] == pytest.approx(842400.0)
    assert status["objective_return_v173"] == pytest.approx(1494.3514978358467)
    assert status["scenario_loss_cvar90_v173"] == pytest.approx(95664.47349143549)
    assert status["source_cap_violations_v173"] == 0
    assert status["delta_return_vs_v171_v173"] == pytest.approx(69.08822213844542)
    assert status["delta_cvar90_vs_v171_v173"] == pytest.approx(138.5118797502073)
    assert status["delta_exposure_vs_v171_v173"] == pytest.approx(0.0)
    assert status["budget_feasible_v173"] is True
    assert status["source_feasible_v173"] is True
    assert status["cvar_feasible_v173"] is True
    assert status["repair_candidate_feasible_v173"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v173"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v173"] is False
    assert status["paper1_promotion_allowed_v173"] is False
    assert status["paper4_working_champion_changed_v173"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v173_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v173",
        "selected_v173",
        "portfolio_label_v173",
        "repair_action_v173",
        "claim_boundary_v173",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v173"]
    assert int(allocations["selected_v173"].sum()) == status["selected_rows_v173"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v173"])
    assert "144038628" in set(allocations["loan_id"].astype(str))
    assert "126409482" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v173"]) == {
        "added_from_v172_best_swap",
        "kept_from_v171",
    }
    assert allocations["claim_boundary_v173"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v173_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v173"]) is True
    assert float(row["max_source_share_v173"]) == pytest.approx(0.8992165242165242)
    assert float(row["min_source_slack_v173"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v171_v173"]) > 0
    assert float(row["scenario_loss_cvar90_v173"]) <= float(row["cvar_cap_v173"])
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v173"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v173"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v173"])

    action = _read_csv("paper4_v173_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v173"]) == status["added_loan_id_v173"]
    assert str(action_row["dropped_loan_id_v173"]) == status["dropped_loan_id_v173"]
    assert float(action_row["return_delta_v173"]) == pytest.approx(69.08822213844525)
    assert float(action_row["cvar90_after_repair_v173"]) == pytest.approx(
        status["scenario_loss_cvar90_v173"]
    )
    assert float(action_row["exposure_after_repair_v173"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v173"]) == 0

    source_summary = _read_csv("paper4_v173_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v173",
        "source_slack_v173",
        "source_cap_violated_v173",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v173"]
    assert not source_summary["source_cap_violated_v173"].astype(bool).any()

    blockers = _read_csv("paper4_v173_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v173"], blockers["blocking_v173"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v173"], blockers["evidence_count_v173"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v173_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v173_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v173_repair_candidate_feasible"]) is True
    assert bool(claim_map["v173_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v173_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v173_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v173 forty-sixth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v173 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v173 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v173: Forty-Sixth One-Swap Repair Candidate" in notebook
    assert "v174 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v174_post_v173_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v174_status.json")

    assert status["phase"] == "v174_post_v173_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.174"
    assert status["summary_rows_v174"] == 1
    assert status["stage_summary_rows_v174"] == 6
    assert status["candidate_pair_rows_v174"] == 1372
    assert status["top_candidate_rows_v174"] == 200
    assert status["claim_blocker_rows_v174"] == 3
    assert status["selected_rows_v174"] == 171
    assert status["candidate_add_rows_v174"] == 276698
    assert status["total_pair_rows_screened_v174"] == 47315358
    assert status["return_improving_pair_rows_v174"] == 1759563
    assert status["budget_return_feasible_pair_rows_v174"] == 1090420
    assert status["source_prefilter_pair_rows_v174"] == 1420
    assert status["source_exact_pair_rows_v174"] == 1372
    assert status["cvar_feasible_pair_rows_v174"] == 1372
    assert status["one_swap_improving_rows_v174"] == 1372
    assert status["best_one_swap_return_delta_v174"] == pytest.approx(65.77828181265099)
    assert status["best_one_swap_cvar90_after_v174"] == pytest.approx(95640.33111916838)
    assert status["post_repair_one_swap_local_optimality_cleared_v174"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v174"] is False
    assert status["paper1_promotion_allowed_v174"] is False
    assert status["paper4_working_champion_changed_v174"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v174_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v174",
        "dropped_loan_id_v174",
        "return_delta_v174",
        "objective_return_after_swap_v174",
        "budget_swap_feasible_v174",
        "source_swap_feasible_v174",
        "source_cap_violations_after_swap_v174",
        "cvar_swap_feasible_v174",
        "one_swap_improves_return_v174",
        "claim_boundary_v174",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v174"]
    assert probe["return_delta_v174"].gt(0).all()
    assert probe["budget_swap_feasible_v174"].astype(bool).all()
    assert probe["source_swap_feasible_v174"].astype(bool).all()
    assert probe["cvar_swap_feasible_v174"].astype(bool).all()
    assert probe["one_swap_improves_return_v174"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v174"].sum()) == 0
    assert probe["return_delta_v174"].max() == pytest.approx(
        status["best_one_swap_return_delta_v174"]
    )
    assert probe["claim_boundary_v174"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v174_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v174"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v174"]) == "159706782"
    assert str(best["dropped_loan_id_v174"]) == "169887690"
    assert float(best["return_delta_v174"]) == pytest.approx(
        status["best_one_swap_return_delta_v174"]
    )
    assert float(best["objective_return_after_swap_v174"]) == pytest.approx(1560.1297796484978)
    assert float(best["exposure_after_swap_v174"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v174"]) is True

    summary = _read_csv("paper4_v174_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v174"]) == status["one_swap_improving_rows_v174"]
    assert float(row["current_exposure_v174"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v174"]) == pytest.approx(1494.3514978358469)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v174"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v174"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v174"])

    stage_summary = _read_csv("paper4_v174_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v174", "pair_rows_v174", "claim_boundary_v174"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v174"], stage_summary["pair_rows_v174"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v174"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v174"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v174"]

    blockers = _read_csv("paper4_v174_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v174"], blockers["blocking_v174"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v174"], blockers["evidence_count_v174"], strict=False)
    )
    assert bool(blocker_map["post_v173_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v173_one_swap_improvement_found"]) == 1372
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v174_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v174_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v174_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v174_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v174_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v174 post-v173 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v174 proves the v173 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v174 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v174: Post-v173 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1372`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v175_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v175_status.json")

    assert status["phase"] == "v175_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.175"
    assert status["allocation_rows_v175"] == 171
    assert status["summary_rows_v175"] == 1
    assert status["action_rows_v175"] == 1
    assert status["source_summary_rows_v175"] == 51
    assert status["claim_blocker_rows_v175"] == 4
    assert status["added_loan_id_v175"] == "159706782"
    assert status["dropped_loan_id_v175"] == "169887690"
    assert status["selected_rows_v175"] == 171
    assert status["portfolio_exposure_v175"] == pytest.approx(842400.0)
    assert status["objective_return_v175"] == pytest.approx(1560.1297796484969)
    assert status["scenario_loss_cvar90_v175"] == pytest.approx(95640.33111916838)
    assert status["source_cap_violations_v175"] == 0
    assert status["delta_return_vs_v173_v175"] == pytest.approx(65.77828181265)
    assert status["delta_cvar90_vs_v173_v175"] == pytest.approx(-24.142372267102473)
    assert status["delta_exposure_vs_v173_v175"] == pytest.approx(0.0)
    assert status["budget_feasible_v175"] is True
    assert status["source_feasible_v175"] is True
    assert status["cvar_feasible_v175"] is True
    assert status["repair_candidate_feasible_v175"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v175"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v175"] is False
    assert status["paper1_promotion_allowed_v175"] is False
    assert status["paper4_working_champion_changed_v175"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v175_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v175",
        "selected_v175",
        "portfolio_label_v175",
        "repair_action_v175",
        "claim_boundary_v175",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v175"]
    assert int(allocations["selected_v175"].sum()) == status["selected_rows_v175"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v175"])
    assert "159706782" in set(allocations["loan_id"].astype(str))
    assert "169887690" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v175"]) == {
        "added_from_v174_best_swap",
        "kept_from_v173",
    }
    assert allocations["claim_boundary_v175"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v175_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v175"]) is True
    assert float(row["max_source_share_v175"]) == pytest.approx(0.8992165242165242)
    assert float(row["min_source_slack_v175"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v173_v175"]) < 0
    assert float(row["scenario_loss_cvar90_v175"]) <= float(row["cvar_cap_v175"])
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v175"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v175"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v175"])

    action = _read_csv("paper4_v175_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v175"]) == status["added_loan_id_v175"]
    assert str(action_row["dropped_loan_id_v175"]) == status["dropped_loan_id_v175"]
    assert float(action_row["return_delta_v175"]) == pytest.approx(65.77828181265099)
    assert float(action_row["cvar90_after_repair_v175"]) == pytest.approx(
        status["scenario_loss_cvar90_v175"]
    )
    assert float(action_row["exposure_after_repair_v175"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v175"]) == 0

    source_summary = _read_csv("paper4_v175_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v175",
        "source_slack_v175",
        "source_cap_violated_v175",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v175"]
    assert not source_summary["source_cap_violated_v175"].astype(bool).any()

    blockers = _read_csv("paper4_v175_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v175"], blockers["blocking_v175"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v175"], blockers["evidence_count_v175"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v175_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v175_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v175_repair_candidate_feasible"]) is True
    assert bool(claim_map["v175_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v175_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v175_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v175 forty-seventh one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v175 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v175 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v175: Forty-Seventh One-Swap Repair Candidate" in notebook
    assert "v176 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v176_post_v175_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v176_status.json")

    assert status["phase"] == "v176_post_v175_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.176"
    assert status["summary_rows_v176"] == 1
    assert status["stage_summary_rows_v176"] == 6
    assert status["candidate_pair_rows_v176"] == 1183
    assert status["top_candidate_rows_v176"] == 200
    assert status["claim_blocker_rows_v176"] == 3
    assert status["selected_rows_v176"] == 171
    assert status["candidate_add_rows_v176"] == 276698
    assert status["total_pair_rows_screened_v176"] == 47315358
    assert status["return_improving_pair_rows_v176"] == 1754943
    assert status["budget_return_feasible_pair_rows_v176"] == 1087449
    assert status["source_prefilter_pair_rows_v176"] == 1231
    assert status["source_exact_pair_rows_v176"] == 1183
    assert status["cvar_feasible_pair_rows_v176"] == 1183
    assert status["one_swap_improving_rows_v176"] == 1183
    assert status["best_one_swap_return_delta_v176"] == pytest.approx(63.85703958187739)
    assert status["best_one_swap_cvar90_after_v176"] == pytest.approx(95613.25967645532)
    assert status["post_repair_one_swap_local_optimality_cleared_v176"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v176"] is False
    assert status["paper1_promotion_allowed_v176"] is False
    assert status["paper4_working_champion_changed_v176"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v176_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v176",
        "dropped_loan_id_v176",
        "return_delta_v176",
        "objective_return_after_swap_v176",
        "budget_swap_feasible_v176",
        "source_swap_feasible_v176",
        "source_cap_violations_after_swap_v176",
        "cvar_swap_feasible_v176",
        "one_swap_improves_return_v176",
        "claim_boundary_v176",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v176"]
    assert probe["return_delta_v176"].gt(0).all()
    assert probe["budget_swap_feasible_v176"].astype(bool).all()
    assert probe["source_swap_feasible_v176"].astype(bool).all()
    assert probe["cvar_swap_feasible_v176"].astype(bool).all()
    assert probe["one_swap_improves_return_v176"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v176"].sum()) == 0
    assert probe["return_delta_v176"].max() == pytest.approx(
        status["best_one_swap_return_delta_v176"]
    )
    assert probe["claim_boundary_v176"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v176_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v176"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v176"]) == "128448205"
    assert str(best["dropped_loan_id_v176"]) == "127676286"
    assert float(best["return_delta_v176"]) == pytest.approx(
        status["best_one_swap_return_delta_v176"]
    )
    assert float(best["objective_return_after_swap_v176"]) == pytest.approx(1623.9868192303743)
    assert float(best["exposure_after_swap_v176"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v176"]) is True

    summary = _read_csv("paper4_v176_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v176"]) == status["one_swap_improving_rows_v176"]
    assert float(row["current_exposure_v176"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v176"]) == pytest.approx(1560.1297796484969)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v176"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v176"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v176"])

    stage_summary = _read_csv("paper4_v176_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v176", "pair_rows_v176", "claim_boundary_v176"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v176"], stage_summary["pair_rows_v176"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v176"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v176"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v176"]

    blockers = _read_csv("paper4_v176_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v176"], blockers["blocking_v176"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v176"], blockers["evidence_count_v176"], strict=False)
    )
    assert bool(blocker_map["post_v175_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v175_one_swap_improvement_found"]) == 1183
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v176_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v176_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v176_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v176_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v176_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v176 post-v175 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v176 proves the v175 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v176 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v176: Post-v175 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1183`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v177_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v177_status.json")

    assert status["phase"] == "v177_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.177"
    assert status["allocation_rows_v177"] == 171
    assert status["summary_rows_v177"] == 1
    assert status["action_rows_v177"] == 1
    assert status["source_summary_rows_v177"] == 51
    assert status["claim_blocker_rows_v177"] == 4
    assert status["added_loan_id_v177"] == "128448205"
    assert status["dropped_loan_id_v177"] == "127676286"
    assert status["selected_rows_v177"] == 171
    assert status["portfolio_exposure_v177"] == pytest.approx(842400.0)
    assert status["objective_return_v177"] == pytest.approx(1623.9868192303743)
    assert status["scenario_loss_cvar90_v177"] == pytest.approx(95613.25967645532)
    assert status["source_cap_violations_v177"] == 0
    assert status["delta_return_vs_v175_v177"] == pytest.approx(63.857039581877416)
    assert status["delta_cvar90_vs_v175_v177"] == pytest.approx(-27.071442713058786)
    assert status["delta_exposure_vs_v175_v177"] == pytest.approx(0.0)
    assert status["budget_feasible_v177"] is True
    assert status["source_feasible_v177"] is True
    assert status["cvar_feasible_v177"] is True
    assert status["repair_candidate_feasible_v177"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v177"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v177"] is False
    assert status["paper1_promotion_allowed_v177"] is False
    assert status["paper4_working_champion_changed_v177"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v177_next_one_swap_repair_allocations.parquet"
    )
    assert {
        "loan_id",
        "loan_amnt",
        "mean_return_v177",
        "selected_v177",
        "portfolio_label_v177",
        "repair_action_v177",
        "claim_boundary_v177",
    }.issubset(allocations.columns)
    assert len(allocations) == status["allocation_rows_v177"]
    assert int(allocations["selected_v177"].sum()) == status["selected_rows_v177"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v177"])
    assert "128448205" in set(allocations["loan_id"].astype(str))
    assert "127676286" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v177"]) == {
        "added_from_v176_best_swap",
        "kept_from_v175",
    }
    assert allocations["claim_boundary_v177"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v177_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v177"]) is True
    assert float(row["max_source_share_v177"]) == pytest.approx(0.8992165242165242)
    assert float(row["min_source_slack_v177"]) == pytest.approx(6.907218559226713e-06)
    assert float(row["delta_cvar90_vs_v175_v177"]) < 0
    assert float(row["scenario_loss_cvar90_v177"]) <= float(row["cvar_cap_v177"])
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v177"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v177"]) is False
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v177"])

    action = _read_csv("paper4_v177_next_one_swap_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v177"]) == status["added_loan_id_v177"]
    assert str(action_row["dropped_loan_id_v177"]) == status["dropped_loan_id_v177"]
    assert float(action_row["return_delta_v177"]) == pytest.approx(63.85703958187739)
    assert float(action_row["cvar90_after_repair_v177"]) == pytest.approx(
        status["scenario_loss_cvar90_v177"]
    )
    assert float(action_row["exposure_after_repair_v177"]) == pytest.approx(842400.0)
    assert int(action_row["source_cap_violations_after_repair_v177"]) == 0

    source_summary = _read_csv("paper4_v177_next_one_swap_repair_source_summary.csv")
    assert {
        "source_family",
        "source_id",
        "source_share_v177",
        "source_slack_v177",
        "source_cap_violated_v177",
    }.issubset(source_summary.columns)
    assert len(source_summary) == status["source_summary_rows_v177"]
    assert not source_summary["source_cap_violated_v177"].astype(bool).any()

    blockers = _read_csv("paper4_v177_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v177"], blockers["blocking_v177"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v177"], blockers["evidence_count_v177"], strict=False)
    )
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert int(evidence_map["next_one_swap_repair_candidate_created"]) == 1
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v177_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v177_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v177_repair_candidate_feasible"]) is True
    assert bool(claim_map["v177_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v177_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v177_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v177 forty-eighth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v177 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v177 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v177: Forty-Eighth One-Swap Repair Candidate" in notebook
    assert "v178 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v178_post_v177_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v178_status.json")

    assert status["phase"] == "v178_post_v177_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.178"
    assert status["summary_rows_v178"] == 1
    assert status["stage_summary_rows_v178"] == 6
    assert status["candidate_pair_rows_v178"] == 1174
    assert status["top_candidate_rows_v178"] == 200
    assert status["claim_blocker_rows_v178"] == 3
    assert status["selected_rows_v178"] == 171
    assert status["candidate_add_rows_v178"] == 276698
    assert status["total_pair_rows_screened_v178"] == 47315358
    assert status["return_improving_pair_rows_v178"] == 1749921
    assert status["budget_return_feasible_pair_rows_v178"] == 1085094
    assert status["source_prefilter_pair_rows_v178"] == 1213
    assert status["source_exact_pair_rows_v178"] == 1174
    assert status["cvar_feasible_pair_rows_v178"] == 1174
    assert status["one_swap_improving_rows_v178"] == 1174
    assert status["best_one_swap_return_delta_v178"] == pytest.approx(58.355442286591966)
    assert status["best_one_swap_cvar90_after_v178"] == pytest.approx(95618.15631921861)
    assert status["post_repair_one_swap_local_optimality_cleared_v178"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v178"] is False
    assert status["paper1_promotion_allowed_v178"] is False
    assert status["paper4_working_champion_changed_v178"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v178_post_repair_one_swap_reprice.csv")
    assert {
        "added_loan_id_v178",
        "dropped_loan_id_v178",
        "return_delta_v178",
        "objective_return_after_swap_v178",
        "budget_swap_feasible_v178",
        "source_swap_feasible_v178",
        "source_cap_violations_after_swap_v178",
        "cvar_swap_feasible_v178",
        "one_swap_improves_return_v178",
        "claim_boundary_v178",
    }.issubset(probe.columns)
    assert len(probe) == status["candidate_pair_rows_v178"]
    assert probe["return_delta_v178"].gt(0).all()
    assert probe["budget_swap_feasible_v178"].astype(bool).all()
    assert probe["source_swap_feasible_v178"].astype(bool).all()
    assert probe["cvar_swap_feasible_v178"].astype(bool).all()
    assert probe["one_swap_improves_return_v178"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v178"].sum()) == 0
    assert probe["return_delta_v178"].max() == pytest.approx(
        status["best_one_swap_return_delta_v178"]
    )
    assert probe["claim_boundary_v178"].str.contains("not multi-swap or global proof").all()

    top_candidates = _read_csv("paper4_v178_post_repair_one_swap_top_candidates.csv")
    assert len(top_candidates) == status["top_candidate_rows_v178"]
    best = top_candidates.iloc[0]
    assert str(best["added_loan_id_v178"]) == "142970101"
    assert str(best["dropped_loan_id_v178"]) == "126416707"
    assert float(best["return_delta_v178"]) == pytest.approx(
        status["best_one_swap_return_delta_v178"]
    )
    assert float(best["objective_return_after_swap_v178"]) == pytest.approx(1682.3422615169663)
    assert float(best["exposure_after_swap_v178"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v178"]) is True

    summary = _read_csv("paper4_v178_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v178"]) == status["one_swap_improving_rows_v178"]
    assert float(row["current_exposure_v178"]) == pytest.approx(842400.0)
    assert float(row["current_objective_return_v178"]) == pytest.approx(1623.9868192303743)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v178"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v178"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v178"])

    stage_summary = _read_csv("paper4_v178_post_repair_one_swap_stage_summary.csv")
    assert {"stage_v178", "pair_rows_v178", "claim_boundary_v178"}.issubset(stage_summary.columns)
    stage_map = dict(
        zip(stage_summary["stage_v178"], stage_summary["pair_rows_v178"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v178"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v178"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v178"]

    blockers = _read_csv("paper4_v178_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v178"], blockers["blocking_v178"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v178"], blockers["evidence_count_v178"], strict=False)
    )
    assert bool(blocker_map["post_v177_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v177_one_swap_improvement_found"]) == 1174
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v178_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v178_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v178_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v178_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v178_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v178 post-v177 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v178 proves the v177 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v178 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v178: Post-v177 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1174`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v179_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v179_status.json")

    assert status["phase"] == "v179_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.179"
    assert status["allocation_rows_v179"] == 171
    assert status["summary_rows_v179"] == 1
    assert status["action_rows_v179"] == 1
    assert status["source_summary_rows_v179"] == 51
    assert status["claim_blocker_rows_v179"] == 4
    assert status["added_loan_id_v179"] == "142970101"
    assert status["dropped_loan_id_v179"] == "126416707"
    assert status["selected_rows_v179"] == 171
    assert status["portfolio_exposure_v179"] == pytest.approx(842400.0)
    assert status["objective_return_v179"] == pytest.approx(1682.342261516967)
    assert status["scenario_loss_cvar90_v179"] == pytest.approx(95618.15631921861)
    assert status["source_cap_violations_v179"] == 0
    assert status["delta_return_vs_v177_v179"] == pytest.approx(58.355442286592734)
    assert status["delta_cvar90_vs_v177_v179"] == pytest.approx(4.896642763298587)
    assert status["delta_exposure_vs_v177_v179"] == pytest.approx(0.0)
    assert status["budget_feasible_v179"] is True
    assert status["source_feasible_v179"] is True
    assert status["cvar_feasible_v179"] is True
    assert status["repair_candidate_feasible_v179"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v179"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v179"] is False
    assert status["paper1_promotion_allowed_v179"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v179_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v179"]
    assert int(allocations["selected_v179"].sum()) == status["selected_rows_v179"]
    assert "142970101" in set(allocations["loan_id"].astype(str))
    assert "126416707" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v179"]) == {
        "added_from_v178_best_swap",
        "kept_from_v177",
    }
    assert allocations["claim_boundary_v179"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v179_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v179"]) is True
    assert float(row["delta_cvar90_vs_v177_v179"]) > 0
    assert float(row["scenario_loss_cvar90_v179"]) <= float(row["cvar_cap_v179"])
    assert float(row["max_source_share_v179"]) == pytest.approx(0.8992165242165242)
    assert float(row["min_source_slack_v179"]) == pytest.approx(6.907218559226713e-06)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v179"])

    action = _read_csv("paper4_v179_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v179"]) == status["added_loan_id_v179"]
    assert str(action["dropped_loan_id_v179"]) == status["dropped_loan_id_v179"]
    assert float(action["return_delta_v179"]) == pytest.approx(58.355442286591966)
    assert float(action["cvar90_after_repair_v179"]) == pytest.approx(
        status["scenario_loss_cvar90_v179"]
    )
    assert int(action["source_cap_violations_after_repair_v179"]) == 0

    source_summary = _read_csv("paper4_v179_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v179"]
    assert not source_summary["source_cap_violated_v179"].astype(bool).any()

    blockers = _read_csv("paper4_v179_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v179"], blockers["blocking_v179"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v179_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v179_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v179_repair_candidate_feasible"]) is True
    assert bool(claim_map["v179_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v179_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v179_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v179 forty-ninth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v179 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v179 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v179: Forty-Ninth One-Swap Repair Candidate" in notebook
    assert "v180 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v180_post_v179_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v180_status.json")

    assert status["phase"] == "v180_post_v179_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.180"
    assert status["summary_rows_v180"] == 1
    assert status["stage_summary_rows_v180"] == 6
    assert status["candidate_pair_rows_v180"] == 1090
    assert status["top_candidate_rows_v180"] == 200
    assert status["claim_blocker_rows_v180"] == 3
    assert status["selected_rows_v180"] == 171
    assert status["candidate_add_rows_v180"] == 276698
    assert status["total_pair_rows_screened_v180"] == 47315358
    assert status["return_improving_pair_rows_v180"] == 1745905
    assert status["budget_return_feasible_pair_rows_v180"] == 1082333
    assert status["source_prefilter_pair_rows_v180"] == 1129
    assert status["source_exact_pair_rows_v180"] == 1090
    assert status["cvar_feasible_pair_rows_v180"] == 1090
    assert status["one_swap_improving_rows_v180"] == 1090
    assert status["best_one_swap_return_delta_v180"] == pytest.approx(55.90855706519778)
    assert status["best_one_swap_cvar90_after_v180"] == pytest.approx(95709.4215407301)
    assert status["post_repair_one_swap_local_optimality_cleared_v180"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v180"] is False
    assert status["paper1_promotion_allowed_v180"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v180_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v180"]
    assert probe["return_delta_v180"].gt(0).all()
    assert probe["budget_swap_feasible_v180"].astype(bool).all()
    assert probe["source_swap_feasible_v180"].astype(bool).all()
    assert probe["cvar_swap_feasible_v180"].astype(bool).all()
    assert probe["one_swap_improves_return_v180"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v180"].sum()) == 0
    assert probe["return_delta_v180"].max() == pytest.approx(
        status["best_one_swap_return_delta_v180"]
    )
    assert probe["claim_boundary_v180"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v180_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v180"]) == "143080063"
    assert str(best["dropped_loan_id_v180"]) == "166121358"
    assert float(best["objective_return_after_swap_v180"]) == pytest.approx(1738.2508185821648)
    assert float(best["exposure_after_swap_v180"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v180"]) is True

    summary = _read_csv("paper4_v180_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v180"]) == status["one_swap_improving_rows_v180"]
    assert float(row["current_objective_return_v180"]) == pytest.approx(1682.342261516967)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v180"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v180"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v180"])

    stage_summary = _read_csv("paper4_v180_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v180"], stage_summary["pair_rows_v180"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v180"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v180"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v180"]

    blockers = _read_csv("paper4_v180_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v180"], blockers["blocking_v180"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v180"], blockers["evidence_count_v180"], strict=False)
    )
    assert bool(blocker_map["post_v179_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v179_one_swap_improvement_found"]) == 1090
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v180_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v180_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v180_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v180_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v180_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v180 post-v179 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v180 proves the v179 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v180 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v180: Post-v179 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1090`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v181_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v181_status.json")

    assert status["phase"] == "v181_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.181"
    assert status["allocation_rows_v181"] == 171
    assert status["summary_rows_v181"] == 1
    assert status["action_rows_v181"] == 1
    assert status["source_summary_rows_v181"] == 51
    assert status["claim_blocker_rows_v181"] == 4
    assert status["added_loan_id_v181"] == "143080063"
    assert status["dropped_loan_id_v181"] == "166121358"
    assert status["selected_rows_v181"] == 171
    assert status["portfolio_exposure_v181"] == pytest.approx(842400.0)
    assert status["objective_return_v181"] == pytest.approx(1738.250818582164)
    assert status["scenario_loss_cvar90_v181"] == pytest.approx(95709.4215407301)
    assert status["source_cap_violations_v181"] == 0
    assert status["delta_return_vs_v179_v181"] == pytest.approx(55.908557065196874)
    assert status["delta_cvar90_vs_v179_v181"] == pytest.approx(91.26522151149402)
    assert status["delta_exposure_vs_v179_v181"] == pytest.approx(0.0)
    assert status["budget_feasible_v181"] is True
    assert status["source_feasible_v181"] is True
    assert status["cvar_feasible_v181"] is True
    assert status["repair_candidate_feasible_v181"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v181"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v181"] is False
    assert status["paper1_promotion_allowed_v181"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v181_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v181"]
    assert int(allocations["selected_v181"].sum()) == status["selected_rows_v181"]
    assert "143080063" in set(allocations["loan_id"].astype(str))
    assert "166121358" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v181"]) == {
        "added_from_v180_best_swap",
        "kept_from_v179",
    }
    assert allocations["claim_boundary_v181"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v181_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v181"]) is True
    assert float(row["delta_cvar90_vs_v179_v181"]) > 0
    assert float(row["scenario_loss_cvar90_v181"]) <= float(row["cvar_cap_v181"])
    assert float(row["max_source_share_v181"]) == pytest.approx(0.8992165242165242)
    assert float(row["min_source_slack_v181"]) == pytest.approx(6.907218559226713e-06)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v181"])

    action = _read_csv("paper4_v181_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v181"]) == status["added_loan_id_v181"]
    assert str(action["dropped_loan_id_v181"]) == status["dropped_loan_id_v181"]
    assert float(action["return_delta_v181"]) == pytest.approx(55.90855706519778)
    assert float(action["cvar90_after_repair_v181"]) == pytest.approx(
        status["scenario_loss_cvar90_v181"]
    )
    assert int(action["source_cap_violations_after_repair_v181"]) == 0

    source_summary = _read_csv("paper4_v181_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v181"]
    assert not source_summary["source_cap_violated_v181"].astype(bool).any()

    blockers = _read_csv("paper4_v181_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v181"], blockers["blocking_v181"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v181_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v181_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v181_repair_candidate_feasible"]) is True
    assert bool(claim_map["v181_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v181_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v181_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v181 fiftieth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v181 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v181 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v181: Fiftieth One-Swap Repair Candidate" in notebook
    assert "v182 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v182_post_v181_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v182_status.json")

    assert status["phase"] == "v182_post_v181_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.182"
    assert status["summary_rows_v182"] == 1
    assert status["stage_summary_rows_v182"] == 6
    assert status["candidate_pair_rows_v182"] == 964
    assert status["top_candidate_rows_v182"] == 200
    assert status["claim_blocker_rows_v182"] == 3
    assert status["selected_rows_v182"] == 171
    assert status["candidate_add_rows_v182"] == 276698
    assert status["total_pair_rows_screened_v182"] == 47315358
    assert status["return_improving_pair_rows_v182"] == 1742090
    assert status["budget_return_feasible_pair_rows_v182"] == 1079840
    assert status["source_prefilter_pair_rows_v182"] == 1003
    assert status["source_exact_pair_rows_v182"] == 964
    assert status["cvar_feasible_pair_rows_v182"] == 964
    assert status["one_swap_improving_rows_v182"] == 964
    assert status["best_one_swap_return_delta_v182"] == pytest.approx(54.87416599855558)
    assert status["best_one_swap_cvar90_after_v182"] == pytest.approx(95872.02755685386)
    assert status["post_repair_one_swap_local_optimality_cleared_v182"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v182"] is False
    assert status["paper1_promotion_allowed_v182"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v182_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v182"]
    assert probe["return_delta_v182"].gt(0).all()
    assert probe["budget_swap_feasible_v182"].astype(bool).all()
    assert probe["source_swap_feasible_v182"].astype(bool).all()
    assert probe["cvar_swap_feasible_v182"].astype(bool).all()
    assert probe["one_swap_improves_return_v182"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v182"].sum()) == 0
    assert probe["return_delta_v182"].max() == pytest.approx(
        status["best_one_swap_return_delta_v182"]
    )
    assert probe["claim_boundary_v182"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v182_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v182"]) == "137380434"
    assert str(best["dropped_loan_id_v182"]) == "127475725"
    assert float(best["objective_return_after_swap_v182"]) == pytest.approx(1793.1249845807195)
    assert float(best["exposure_after_swap_v182"]) == pytest.approx(842400.0)
    assert bool(best["one_swap_improves_return_v182"]) is True

    summary = _read_csv("paper4_v182_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v182"]) == status["one_swap_improving_rows_v182"]
    assert float(row["current_objective_return_v182"]) == pytest.approx(1738.250818582164)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v182"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v182"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v182"])

    stage_summary = _read_csv("paper4_v182_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v182"], stage_summary["pair_rows_v182"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v182"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v182"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v182"]

    blockers = _read_csv("paper4_v182_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v182"], blockers["blocking_v182"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v182"], blockers["evidence_count_v182"], strict=False)
    )
    assert bool(blocker_map["post_v181_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v181_one_swap_improvement_found"]) == 964
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v182_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v182_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v182_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v182_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v182_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v182 post-v181 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v182 proves the v181 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v182 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v182: Post-v181 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `964`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v183_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v183_status.json")

    assert status["phase"] == "v183_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.183"
    assert status["allocation_rows_v183"] == 171
    assert status["summary_rows_v183"] == 1
    assert status["action_rows_v183"] == 1
    assert status["source_summary_rows_v183"] == 51
    assert status["claim_blocker_rows_v183"] == 4
    assert status["added_loan_id_v183"] == "137380434"
    assert status["dropped_loan_id_v183"] == "127475725"
    assert status["selected_rows_v183"] == 171
    assert status["portfolio_exposure_v183"] == pytest.approx(842400.0)
    assert status["objective_return_v183"] == pytest.approx(1793.1249845807192)
    assert status["scenario_loss_cvar90_v183"] == pytest.approx(95872.02755685386)
    assert status["source_cap_violations_v183"] == 0
    assert status["delta_return_vs_v181_v183"] == pytest.approx(54.87416599855533)
    assert status["delta_cvar90_vs_v181_v183"] == pytest.approx(162.60601612376922)
    assert status["delta_exposure_vs_v181_v183"] == pytest.approx(0.0)
    assert status["budget_feasible_v183"] is True
    assert status["source_feasible_v183"] is True
    assert status["cvar_feasible_v183"] is True
    assert status["repair_candidate_feasible_v183"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v183"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v183"] is False
    assert status["paper1_promotion_allowed_v183"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v183_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v183"]
    assert int(allocations["selected_v183"].sum()) == status["selected_rows_v183"]
    assert "137380434" in set(allocations["loan_id"].astype(str))
    assert "127475725" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v183"]) == {
        "added_from_v182_best_swap",
        "kept_from_v181",
    }
    assert allocations["claim_boundary_v183"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v183_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v183"]) is True
    assert float(row["delta_cvar90_vs_v181_v183"]) > 0
    assert float(row["scenario_loss_cvar90_v183"]) <= float(row["cvar_cap_v183"])
    assert float(row["max_source_share_v183"]) == pytest.approx(0.8992165242165242)
    assert float(row["min_source_slack_v183"]) == pytest.approx(6.907218559226713e-06)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v183"])

    action = _read_csv("paper4_v183_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v183"]) == status["added_loan_id_v183"]
    assert str(action["dropped_loan_id_v183"]) == status["dropped_loan_id_v183"]
    assert float(action["return_delta_v183"]) == pytest.approx(54.87416599855558)
    assert float(action["cvar90_after_repair_v183"]) == pytest.approx(
        status["scenario_loss_cvar90_v183"]
    )
    assert int(action["source_cap_violations_after_repair_v183"]) == 0

    source_summary = _read_csv("paper4_v183_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v183"]
    assert not source_summary["source_cap_violated_v183"].astype(bool).any()

    blockers = _read_csv("paper4_v183_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v183"], blockers["blocking_v183"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v183_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v183_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v183_repair_candidate_feasible"]) is True
    assert bool(claim_map["v183_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v183_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v183_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v183 fifty-first one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v183 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v183 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v183: Fifty-First One-Swap Repair Candidate" in notebook
    assert "v184 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v184_post_v183_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v184_status.json")

    assert status["phase"] == "v184_post_v183_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.184"
    assert status["summary_rows_v184"] == 1
    assert status["stage_summary_rows_v184"] == 6
    assert status["candidate_pair_rows_v184"] == 840
    assert status["top_candidate_rows_v184"] == 200
    assert status["claim_blocker_rows_v184"] == 3
    assert status["selected_rows_v184"] == 171
    assert status["candidate_add_rows_v184"] == 276698
    assert status["total_pair_rows_screened_v184"] == 47315358
    assert status["return_improving_pair_rows_v184"] == 1738343
    assert status["budget_return_feasible_pair_rows_v184"] == 1077375
    assert status["source_prefilter_pair_rows_v184"] == 879
    assert status["source_exact_pair_rows_v184"] == 840
    assert status["cvar_feasible_pair_rows_v184"] == 840
    assert status["one_swap_improving_rows_v184"] == 840
    assert status["best_one_swap_return_delta_v184"] == pytest.approx(50.16637163365709)
    assert status["best_one_swap_cvar90_after_v184"] == pytest.approx(96008.23617755131)
    assert status["post_repair_one_swap_local_optimality_cleared_v184"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v184"] is False
    assert status["paper1_promotion_allowed_v184"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v184_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v184"]
    assert probe["return_delta_v184"].gt(0).all()
    assert probe["budget_swap_feasible_v184"].astype(bool).all()
    assert probe["source_swap_feasible_v184"].astype(bool).all()
    assert probe["cvar_swap_feasible_v184"].astype(bool).all()
    assert probe["one_swap_improves_return_v184"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v184"].sum()) == 0
    assert probe["return_delta_v184"].max() == pytest.approx(
        status["best_one_swap_return_delta_v184"]
    )
    assert probe["claim_boundary_v184"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v184_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v184"]) == "154756272"
    assert str(best["dropped_loan_id_v184"]) == "127011905"
    assert float(best["objective_return_after_swap_v184"]) == pytest.approx(1843.2913562143763)
    assert float(best["exposure_after_swap_v184"]) == pytest.approx(842500.0)
    assert float(best["source_min_slack_after_swap_v184"]) == pytest.approx(0.00010816869840624133)
    assert bool(best["one_swap_improves_return_v184"]) is True

    summary = _read_csv("paper4_v184_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v184"]) == status["one_swap_improving_rows_v184"]
    assert float(row["current_objective_return_v184"]) == pytest.approx(1793.1249845807192)
    assert float(row["current_exposure_v184"]) == pytest.approx(842400.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v184"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v184"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v184"])

    stage_summary = _read_csv("paper4_v184_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v184"], stage_summary["pair_rows_v184"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v184"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v184"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v184"]

    blockers = _read_csv("paper4_v184_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v184"], blockers["blocking_v184"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v184"], blockers["evidence_count_v184"], strict=False)
    )
    assert bool(blocker_map["post_v183_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v183_one_swap_improvement_found"]) == 840
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v184_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v184_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v184_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v184_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v184_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v184 post-v183 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v184 proves the v183 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v184 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v184: Post-v183 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `840`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v185_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v185_status.json")

    assert status["phase"] == "v185_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.185"
    assert status["allocation_rows_v185"] == 171
    assert status["summary_rows_v185"] == 1
    assert status["action_rows_v185"] == 1
    assert status["source_summary_rows_v185"] == 51
    assert status["claim_blocker_rows_v185"] == 4
    assert status["added_loan_id_v185"] == "154756272"
    assert status["dropped_loan_id_v185"] == "127011905"
    assert status["selected_rows_v185"] == 171
    assert status["portfolio_exposure_v185"] == pytest.approx(842500.0)
    assert status["objective_return_v185"] == pytest.approx(1843.2913562143763)
    assert status["scenario_loss_cvar90_v185"] == pytest.approx(96008.23617755131)
    assert status["source_cap_violations_v185"] == 0
    assert status["delta_return_vs_v183_v185"] == pytest.approx(50.166371633657036)
    assert status["delta_cvar90_vs_v183_v185"] == pytest.approx(136.20862069744908)
    assert status["delta_exposure_vs_v183_v185"] == pytest.approx(100.0)
    assert status["budget_feasible_v185"] is True
    assert status["source_feasible_v185"] is True
    assert status["cvar_feasible_v185"] is True
    assert status["repair_candidate_feasible_v185"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v185"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v185"] is False
    assert status["paper1_promotion_allowed_v185"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v185_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v185"]
    assert int(allocations["selected_v185"].sum()) == status["selected_rows_v185"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v185"])
    assert "154756272" in set(allocations["loan_id"].astype(str))
    assert "127011905" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v185"]) == {
        "added_from_v184_best_swap",
        "kept_from_v183",
    }
    assert allocations["claim_boundary_v185"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v185_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v185"]) is True
    assert float(row["delta_exposure_vs_v183_v185"]) == pytest.approx(100.0)
    assert float(row["delta_cvar90_vs_v183_v185"]) > 0
    assert float(row["scenario_loss_cvar90_v185"]) <= float(row["cvar_cap_v185"])
    assert float(row["max_source_share_v185"]) == pytest.approx(0.8991097922848664)
    assert float(row["min_source_slack_v185"]) == pytest.approx(0.00010816869840624133)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v185"])

    action = _read_csv("paper4_v185_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v185"]) == status["added_loan_id_v185"]
    assert str(action["dropped_loan_id_v185"]) == status["dropped_loan_id_v185"]
    assert float(action["return_delta_v185"]) == pytest.approx(50.16637163365709)
    assert float(action["exposure_after_repair_v185"]) == pytest.approx(842500.0)
    assert int(action["source_cap_violations_after_repair_v185"]) == 0

    source_summary = _read_csv("paper4_v185_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v185"]
    assert not source_summary["source_cap_violated_v185"].astype(bool).any()

    blockers = _read_csv("paper4_v185_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v185"], blockers["blocking_v185"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v185_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v185_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v185_repair_candidate_feasible"]) is True
    assert bool(claim_map["v185_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v185_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v185_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v185 fifty-second one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v185 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v185 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v185: Fifty-Second One-Swap Repair Candidate" in notebook
    assert "v186 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v186_post_v185_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v186_status.json")

    assert status["phase"] == "v186_post_v185_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.186"
    assert status["summary_rows_v186"] == 1
    assert status["stage_summary_rows_v186"] == 6
    assert status["candidate_pair_rows_v186"] == 921
    assert status["top_candidate_rows_v186"] == 200
    assert status["claim_blocker_rows_v186"] == 3
    assert status["selected_rows_v186"] == 171
    assert status["candidate_add_rows_v186"] == 276698
    assert status["total_pair_rows_screened_v186"] == 47315358
    assert status["return_improving_pair_rows_v186"] == 1734194
    assert status["budget_return_feasible_pair_rows_v186"] == 1075121
    assert status["source_prefilter_pair_rows_v186"] == 1262
    assert status["source_exact_pair_rows_v186"] == 921
    assert status["cvar_feasible_pair_rows_v186"] == 921
    assert status["one_swap_improving_rows_v186"] == 921
    assert status["best_one_swap_return_delta_v186"] == pytest.approx(48.89384843829143)
    assert status["best_one_swap_cvar90_after_v186"] == pytest.approx(96196.78673348864)
    assert status["post_repair_one_swap_local_optimality_cleared_v186"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v186"] is False
    assert status["paper1_promotion_allowed_v186"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v186_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v186"]
    assert probe["return_delta_v186"].gt(0).all()
    assert probe["budget_swap_feasible_v186"].astype(bool).all()
    assert probe["source_swap_feasible_v186"].astype(bool).all()
    assert probe["cvar_swap_feasible_v186"].astype(bool).all()
    assert probe["one_swap_improves_return_v186"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v186"].sum()) == 0
    assert probe["return_delta_v186"].max() == pytest.approx(
        status["best_one_swap_return_delta_v186"]
    )
    assert probe["claim_boundary_v186"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v186_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v186"]) == "146047525"
    assert str(best["dropped_loan_id_v186"]) == "126652025"
    assert float(best["objective_return_after_swap_v186"]) == pytest.approx(1892.1852046526678)
    assert float(best["exposure_after_swap_v186"]) == pytest.approx(842500.0)
    assert bool(best["one_swap_improves_return_v186"]) is True

    summary = _read_csv("paper4_v186_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v186"]) == status["one_swap_improving_rows_v186"]
    assert float(row["current_objective_return_v186"]) == pytest.approx(1843.2913562143765)
    assert float(row["current_exposure_v186"]) == pytest.approx(842500.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v186"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v186"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v186"])

    stage_summary = _read_csv("paper4_v186_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v186"], stage_summary["pair_rows_v186"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v186"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v186"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v186"]

    blockers = _read_csv("paper4_v186_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v186"], blockers["blocking_v186"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v186"], blockers["evidence_count_v186"], strict=False)
    )
    assert bool(blocker_map["post_v185_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v185_one_swap_improvement_found"]) == 921
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v186_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v186_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v186_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v186_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v186_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v186 post-v185 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v186 proves the v185 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v186 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v186: Post-v185 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `921`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v187_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v187_status.json")

    assert status["phase"] == "v187_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.187"
    assert status["allocation_rows_v187"] == 171
    assert status["summary_rows_v187"] == 1
    assert status["action_rows_v187"] == 1
    assert status["source_summary_rows_v187"] == 51
    assert status["claim_blocker_rows_v187"] == 4
    assert status["added_loan_id_v187"] == "146047525"
    assert status["dropped_loan_id_v187"] == "126652025"
    assert status["selected_rows_v187"] == 171
    assert status["portfolio_exposure_v187"] == pytest.approx(842500.0)
    assert status["objective_return_v187"] == pytest.approx(1892.1852046526683)
    assert status["scenario_loss_cvar90_v187"] == pytest.approx(96196.78673348865)
    assert status["source_cap_violations_v187"] == 0
    assert status["delta_return_vs_v185_v187"] == pytest.approx(48.8938484382918)
    assert status["delta_cvar90_vs_v185_v187"] == pytest.approx(188.55055593732686)
    assert status["delta_exposure_vs_v185_v187"] == pytest.approx(0.0)
    assert status["budget_feasible_v187"] is True
    assert status["source_feasible_v187"] is True
    assert status["cvar_feasible_v187"] is True
    assert status["repair_candidate_feasible_v187"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v187"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v187"] is False
    assert status["paper1_promotion_allowed_v187"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v187_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v187"]
    assert int(allocations["selected_v187"].sum()) == status["selected_rows_v187"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v187"])
    assert "146047525" in set(allocations["loan_id"].astype(str))
    assert "126652025" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v187"]) == {
        "added_from_v186_best_swap",
        "kept_from_v185",
    }
    assert allocations["claim_boundary_v187"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v187_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v187"]) is True
    assert float(row["delta_cvar90_vs_v185_v187"]) > 0
    assert float(row["scenario_loss_cvar90_v187"]) <= float(row["cvar_cap_v187"])
    assert float(row["max_source_share_v187"]) == pytest.approx(0.8991097922848664)
    assert float(row["min_source_slack_v187"]) == pytest.approx(0.00010816869840624133)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v187"])

    action = _read_csv("paper4_v187_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v187"]) == status["added_loan_id_v187"]
    assert str(action["dropped_loan_id_v187"]) == status["dropped_loan_id_v187"]
    assert float(action["return_delta_v187"]) == pytest.approx(48.89384843829143)
    assert float(action["exposure_after_repair_v187"]) == pytest.approx(842500.0)
    assert int(action["source_cap_violations_after_repair_v187"]) == 0

    source_summary = _read_csv("paper4_v187_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v187"]
    assert not source_summary["source_cap_violated_v187"].astype(bool).any()

    blockers = _read_csv("paper4_v187_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v187"], blockers["blocking_v187"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v187_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v187_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v187_repair_candidate_feasible"]) is True
    assert bool(claim_map["v187_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v187_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v187_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v187 fifty-third one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v187 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v187 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v187: Fifty-Third One-Swap Repair Candidate" in notebook
    assert "v188 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v188_post_v187_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v188_status.json")

    assert status["phase"] == "v188_post_v187_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.188"
    assert status["summary_rows_v188"] == 1
    assert status["stage_summary_rows_v188"] == 6
    assert status["candidate_pair_rows_v188"] == 812
    assert status["top_candidate_rows_v188"] == 200
    assert status["claim_blocker_rows_v188"] == 3
    assert status["selected_rows_v188"] == 171
    assert status["candidate_add_rows_v188"] == 276698
    assert status["total_pair_rows_screened_v188"] == 47315358
    assert status["return_improving_pair_rows_v188"] == 1730883
    assert status["budget_return_feasible_pair_rows_v188"] == 1072932
    assert status["source_prefilter_pair_rows_v188"] == 1148
    assert status["source_exact_pair_rows_v188"] == 812
    assert status["cvar_feasible_pair_rows_v188"] == 812
    assert status["one_swap_improving_rows_v188"] == 812
    assert status["best_one_swap_return_delta_v188"] == pytest.approx(46.968822503693275)
    assert status["best_one_swap_cvar90_after_v188"] == pytest.approx(96263.49359700759)
    assert status["post_repair_one_swap_local_optimality_cleared_v188"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v188"] is False
    assert status["paper1_promotion_allowed_v188"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v188_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v188"]
    assert probe["return_delta_v188"].gt(0).all()
    assert probe["budget_swap_feasible_v188"].astype(bool).all()
    assert probe["source_swap_feasible_v188"].astype(bool).all()
    assert probe["cvar_swap_feasible_v188"].astype(bool).all()
    assert probe["one_swap_improves_return_v188"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v188"].sum()) == 0
    assert probe["return_delta_v188"].max() == pytest.approx(
        status["best_one_swap_return_delta_v188"]
    )
    assert probe["claim_boundary_v188"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v188_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v188"]) == "153939446"
    assert str(best["dropped_loan_id_v188"]) == "127582382"
    assert float(best["objective_return_after_swap_v188"]) == pytest.approx(1939.1540271563615)
    assert float(best["exposure_after_swap_v188"]) == pytest.approx(842500.0)
    assert bool(best["one_swap_improves_return_v188"]) is True

    summary = _read_csv("paper4_v188_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v188"]) == status["one_swap_improving_rows_v188"]
    assert float(row["current_objective_return_v188"]) == pytest.approx(1892.1852046526683)
    assert float(row["current_exposure_v188"]) == pytest.approx(842500.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v188"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v188"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v188"])

    stage_summary = _read_csv("paper4_v188_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v188"], stage_summary["pair_rows_v188"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v188"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v188"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v188"]

    blockers = _read_csv("paper4_v188_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v188"], blockers["blocking_v188"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v188"], blockers["evidence_count_v188"], strict=False)
    )
    assert bool(blocker_map["post_v187_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v187_one_swap_improvement_found"]) == 812
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v188_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v188_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v188_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v188_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v188_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v188 post-v187 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v188 proves the v187 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v188 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v188: Post-v187 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `812`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v189_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v189_status.json")

    assert status["phase"] == "v189_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.189"
    assert status["allocation_rows_v189"] == 171
    assert status["summary_rows_v189"] == 1
    assert status["action_rows_v189"] == 1
    assert status["source_summary_rows_v189"] == 51
    assert status["claim_blocker_rows_v189"] == 4
    assert status["added_loan_id_v189"] == "153939446"
    assert status["dropped_loan_id_v189"] == "127582382"
    assert status["selected_rows_v189"] == 171
    assert status["portfolio_exposure_v189"] == pytest.approx(842500.0)
    assert status["objective_return_v189"] == pytest.approx(1939.1540271563617)
    assert status["scenario_loss_cvar90_v189"] == pytest.approx(96263.49359700759)
    assert status["source_cap_violations_v189"] == 0
    assert status["delta_return_vs_v187_v189"] == pytest.approx(46.968822503693445)
    assert status["delta_cvar90_vs_v187_v189"] == pytest.approx(66.70686351894983)
    assert status["delta_exposure_vs_v187_v189"] == pytest.approx(0.0)
    assert status["budget_feasible_v189"] is True
    assert status["source_feasible_v189"] is True
    assert status["cvar_feasible_v189"] is True
    assert status["repair_candidate_feasible_v189"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v189"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v189"] is False
    assert status["paper1_promotion_allowed_v189"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v189_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v189"]
    assert int(allocations["selected_v189"].sum()) == status["selected_rows_v189"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v189"])
    assert "153939446" in set(allocations["loan_id"].astype(str))
    assert "127582382" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v189"]) == {
        "added_from_v188_best_swap",
        "kept_from_v187",
    }
    assert allocations["claim_boundary_v189"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v189_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v189"]) is True
    assert float(row["delta_cvar90_vs_v187_v189"]) > 0
    assert float(row["scenario_loss_cvar90_v189"]) <= float(row["cvar_cap_v189"])
    assert float(row["max_source_share_v189"]) == pytest.approx(0.8991097922848664)
    assert float(row["min_source_slack_v189"]) == pytest.approx(0.00010816869840624133)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v189"])

    action = _read_csv("paper4_v189_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v189"]) == status["added_loan_id_v189"]
    assert str(action["dropped_loan_id_v189"]) == status["dropped_loan_id_v189"]
    assert float(action["return_delta_v189"]) == pytest.approx(46.96882250369328)
    assert float(action["exposure_after_repair_v189"]) == pytest.approx(842500.0)
    assert int(action["source_cap_violations_after_repair_v189"]) == 0

    source_summary = _read_csv("paper4_v189_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v189"]
    assert not source_summary["source_cap_violated_v189"].astype(bool).any()

    blockers = _read_csv("paper4_v189_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v189"], blockers["blocking_v189"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v189_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v189_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v189_repair_candidate_feasible"]) is True
    assert bool(claim_map["v189_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v189_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v189_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v189 fifty-fourth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v189 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v189 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v189: Fifty-Fourth One-Swap Repair Candidate" in notebook
    assert "v190 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v190_post_v189_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v190_status.json")

    assert status["phase"] == "v190_post_v189_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.190"
    assert status["summary_rows_v190"] == 1
    assert status["stage_summary_rows_v190"] == 6
    assert status["candidate_pair_rows_v190"] == 713
    assert status["top_candidate_rows_v190"] == 200
    assert status["claim_blocker_rows_v190"] == 3
    assert status["selected_rows_v190"] == 171
    assert status["candidate_add_rows_v190"] == 276698
    assert status["total_pair_rows_screened_v190"] == 47315358
    assert status["return_improving_pair_rows_v190"] == 1727672
    assert status["budget_return_feasible_pair_rows_v190"] == 1070808
    assert status["source_prefilter_pair_rows_v190"] == 1044
    assert status["source_exact_pair_rows_v190"] == 713
    assert status["cvar_feasible_pair_rows_v190"] == 713
    assert status["one_swap_improving_rows_v190"] == 713
    assert status["best_one_swap_return_delta_v190"] == pytest.approx(46.67475777771685)
    assert status["best_one_swap_cvar90_after_v190"] == pytest.approx(96277.62665487477)
    assert status["post_repair_one_swap_local_optimality_cleared_v190"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v190"] is False
    assert status["paper1_promotion_allowed_v190"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v190_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v190"]
    assert probe["return_delta_v190"].gt(0).all()
    assert probe["budget_swap_feasible_v190"].astype(bool).all()
    assert probe["source_swap_feasible_v190"].astype(bool).all()
    assert probe["cvar_swap_feasible_v190"].astype(bool).all()
    assert probe["one_swap_improves_return_v190"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v190"].sum()) == 0
    assert probe["return_delta_v190"].max() == pytest.approx(
        status["best_one_swap_return_delta_v190"]
    )
    assert probe["claim_boundary_v190"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v190_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v190"]) == "158269673"
    assert str(best["dropped_loan_id_v190"]) == "127874931"
    assert float(best["objective_return_after_swap_v190"]) == pytest.approx(1985.8287849340784)
    assert float(best["exposure_after_swap_v190"]) == pytest.approx(842500.0)
    assert bool(best["one_swap_improves_return_v190"]) is True

    summary = _read_csv("paper4_v190_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v190"]) == status["one_swap_improving_rows_v190"]
    assert float(row["current_objective_return_v190"]) == pytest.approx(1939.1540271563615)
    assert float(row["current_exposure_v190"]) == pytest.approx(842500.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v190"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v190"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v190"])

    stage_summary = _read_csv("paper4_v190_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v190"], stage_summary["pair_rows_v190"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v190"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v190"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v190"]

    blockers = _read_csv("paper4_v190_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v190"], blockers["blocking_v190"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v190"], blockers["evidence_count_v190"], strict=False)
    )
    assert bool(blocker_map["post_v189_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v189_one_swap_improvement_found"]) == 713
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v190_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v190_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v190_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v190_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v190_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v190 post-v189 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v190 proves the v189 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v190 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v190: Post-v189 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `713`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v191_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v191_status.json")

    assert status["phase"] == "v191_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.191"
    assert status["allocation_rows_v191"] == 171
    assert status["summary_rows_v191"] == 1
    assert status["action_rows_v191"] == 1
    assert status["source_summary_rows_v191"] == 51
    assert status["claim_blocker_rows_v191"] == 4
    assert status["added_loan_id_v191"] == "158269673"
    assert status["dropped_loan_id_v191"] == "127874931"
    assert status["selected_rows_v191"] == 171
    assert status["portfolio_exposure_v191"] == pytest.approx(842500.0)
    assert status["objective_return_v191"] == pytest.approx(1985.8287849340777)
    assert status["scenario_loss_cvar90_v191"] == pytest.approx(96277.62665487477)
    assert status["source_cap_violations_v191"] == 0
    assert status["delta_return_vs_v189_v191"] == pytest.approx(46.674757777716195)
    assert status["delta_cvar90_vs_v189_v191"] == pytest.approx(14.133057867162279)
    assert status["delta_exposure_vs_v189_v191"] == pytest.approx(0.0)
    assert status["budget_feasible_v191"] is True
    assert status["source_feasible_v191"] is True
    assert status["cvar_feasible_v191"] is True
    assert status["repair_candidate_feasible_v191"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v191"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v191"] is False
    assert status["paper1_promotion_allowed_v191"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v191_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v191"]
    assert int(allocations["selected_v191"].sum()) == status["selected_rows_v191"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v191"])
    assert "158269673" in set(allocations["loan_id"].astype(str))
    assert "127874931" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v191"]) == {
        "added_from_v190_best_swap",
        "kept_from_v189",
    }
    assert allocations["claim_boundary_v191"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v191_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v191"]) is True
    assert float(row["delta_cvar90_vs_v189_v191"]) > 0
    assert float(row["scenario_loss_cvar90_v191"]) <= float(row["cvar_cap_v191"])
    assert float(row["max_source_share_v191"]) == pytest.approx(0.8991097922848664)
    assert float(row["min_source_slack_v191"]) == pytest.approx(0.00010816869840624133)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v191"])

    action = _read_csv("paper4_v191_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v191"]) == status["added_loan_id_v191"]
    assert str(action["dropped_loan_id_v191"]) == status["dropped_loan_id_v191"]
    assert float(action["return_delta_v191"]) == pytest.approx(46.67475777771685)
    assert float(action["exposure_after_repair_v191"]) == pytest.approx(842500.0)
    assert int(action["source_cap_violations_after_repair_v191"]) == 0

    source_summary = _read_csv("paper4_v191_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v191"]
    assert not source_summary["source_cap_violated_v191"].astype(bool).any()

    blockers = _read_csv("paper4_v191_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v191"], blockers["blocking_v191"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v191_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v191_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v191_repair_candidate_feasible"]) is True
    assert bool(claim_map["v191_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v191_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v191_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v191 fifty-fifth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v191 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v191 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v191: Fifty-Fifth One-Swap Repair Candidate" in notebook
    assert "v192 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v192_post_v191_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v192_status.json")

    assert status["phase"] == "v192_post_v191_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.192"
    assert status["summary_rows_v192"] == 1
    assert status["stage_summary_rows_v192"] == 6
    assert status["candidate_pair_rows_v192"] == 636
    assert status["top_candidate_rows_v192"] == 200
    assert status["claim_blocker_rows_v192"] == 3
    assert status["selected_rows_v192"] == 171
    assert status["candidate_add_rows_v192"] == 276698
    assert status["total_pair_rows_screened_v192"] == 47315358
    assert status["return_improving_pair_rows_v192"] == 1724471
    assert status["budget_return_feasible_pair_rows_v192"] == 1068651
    assert status["source_prefilter_pair_rows_v192"] == 967
    assert status["source_exact_pair_rows_v192"] == 636
    assert status["cvar_feasible_pair_rows_v192"] == 636
    assert status["one_swap_improving_rows_v192"] == 636
    assert status["best_one_swap_return_delta_v192"] == pytest.approx(45.91268799738384)
    assert status["best_one_swap_cvar90_after_v192"] == pytest.approx(96535.10023934311)
    assert status["post_repair_one_swap_local_optimality_cleared_v192"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v192"] is False
    assert status["paper1_promotion_allowed_v192"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v192_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v192"]
    assert probe["return_delta_v192"].gt(0).all()
    assert probe["budget_swap_feasible_v192"].astype(bool).all()
    assert probe["source_swap_feasible_v192"].astype(bool).all()
    assert probe["cvar_swap_feasible_v192"].astype(bool).all()
    assert probe["one_swap_improves_return_v192"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v192"].sum()) == 0
    assert probe["return_delta_v192"].max() == pytest.approx(
        status["best_one_swap_return_delta_v192"]
    )
    assert probe["claim_boundary_v192"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v192_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v192"]) == "146107119"
    assert str(best["dropped_loan_id_v192"]) == "126737687"
    assert float(best["objective_return_after_swap_v192"]) == pytest.approx(2031.7414729314614)
    assert float(best["exposure_after_swap_v192"]) == pytest.approx(842825.0)
    assert float(best["source_min_slack_after_swap_v192"]) == pytest.approx(0.0004371025571850984)
    assert bool(best["one_swap_improves_return_v192"]) is True

    summary = _read_csv("paper4_v192_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v192"]) == status["one_swap_improving_rows_v192"]
    assert float(row["current_objective_return_v192"]) == pytest.approx(1985.8287849340777)
    assert float(row["current_exposure_v192"]) == pytest.approx(842500.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v192"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v192"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v192"])

    stage_summary = _read_csv("paper4_v192_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v192"], stage_summary["pair_rows_v192"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v192"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v192"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v192"]

    blockers = _read_csv("paper4_v192_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v192"], blockers["blocking_v192"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v192"], blockers["evidence_count_v192"], strict=False)
    )
    assert bool(blocker_map["post_v191_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v191_one_swap_improvement_found"]) == 636
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v192_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v192_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v192_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v192_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v192_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v192 post-v191 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v192 proves the v191 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v192 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v192: Post-v191 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `636`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v193_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v193_status.json")

    assert status["phase"] == "v193_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.193"
    assert status["allocation_rows_v193"] == 171
    assert status["summary_rows_v193"] == 1
    assert status["action_rows_v193"] == 1
    assert status["source_summary_rows_v193"] == 51
    assert status["claim_blocker_rows_v193"] == 4
    assert status["added_loan_id_v193"] == "146107119"
    assert status["dropped_loan_id_v193"] == "126737687"
    assert status["selected_rows_v193"] == 171
    assert status["portfolio_exposure_v193"] == pytest.approx(842825.0)
    assert status["objective_return_v193"] == pytest.approx(2031.7414729314623)
    assert status["scenario_loss_cvar90_v193"] == pytest.approx(96535.10023934311)
    assert status["source_cap_violations_v193"] == 0
    assert status["delta_return_vs_v191_v193"] == pytest.approx(45.91268799738464)
    assert status["delta_cvar90_vs_v191_v193"] == pytest.approx(257.4735844683455)
    assert status["delta_exposure_vs_v191_v193"] == pytest.approx(325.0)
    assert status["budget_feasible_v193"] is True
    assert status["source_feasible_v193"] is True
    assert status["cvar_feasible_v193"] is True
    assert status["repair_candidate_feasible_v193"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v193"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v193"] is False
    assert status["paper1_promotion_allowed_v193"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v193_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v193"]
    assert int(allocations["selected_v193"].sum()) == status["selected_rows_v193"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v193"])
    assert "146107119" in set(allocations["loan_id"].astype(str))
    assert "126737687" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v193"]) == {
        "added_from_v192_best_swap",
        "kept_from_v191",
    }
    assert allocations["claim_boundary_v193"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v193_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v193"]) is True
    assert float(row["delta_exposure_vs_v191_v193"]) == pytest.approx(325.0)
    assert float(row["delta_cvar90_vs_v191_v193"]) > 0
    assert float(row["scenario_loss_cvar90_v193"]) <= float(row["cvar_cap_v193"])
    assert float(row["max_source_share_v193"]) == pytest.approx(0.8991486963485895)
    assert float(row["min_source_slack_v193"]) == pytest.approx(0.0004371025571850984)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v193"])

    action = _read_csv("paper4_v193_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v193"]) == status["added_loan_id_v193"]
    assert str(action["dropped_loan_id_v193"]) == status["dropped_loan_id_v193"]
    assert float(action["return_delta_v193"]) == pytest.approx(45.91268799738384)
    assert float(action["exposure_after_repair_v193"]) == pytest.approx(842825.0)
    assert int(action["source_cap_violations_after_repair_v193"]) == 0

    source_summary = _read_csv("paper4_v193_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v193"]
    assert not source_summary["source_cap_violated_v193"].astype(bool).any()

    blockers = _read_csv("paper4_v193_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v193"], blockers["blocking_v193"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v193_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v193_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v193_repair_candidate_feasible"]) is True
    assert bool(claim_map["v193_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v193_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v193_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v193 fifty-sixth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v193 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v193 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v193: Fifty-Sixth One-Swap Repair Candidate" in notebook
    assert "v194 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v194_post_v193_reprice_reopens_improvement_front() -> None:
    status = _read_json("paper4_v194_status.json")

    assert status["phase"] == "v194_post_v193_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.194"
    assert status["summary_rows_v194"] == 1
    assert status["stage_summary_rows_v194"] == 6
    assert status["candidate_pair_rows_v194"] == 4354
    assert status["top_candidate_rows_v194"] == 200
    assert status["claim_blocker_rows_v194"] == 3
    assert status["selected_rows_v194"] == 171
    assert status["candidate_add_rows_v194"] == 276698
    assert status["total_pair_rows_screened_v194"] == 47315358
    assert status["return_improving_pair_rows_v194"] == 1721602
    assert status["budget_return_feasible_pair_rows_v194"] == 1067424
    assert status["source_prefilter_pair_rows_v194"] == 8196
    assert status["source_exact_pair_rows_v194"] == 4354
    assert status["cvar_feasible_pair_rows_v194"] == 4354
    assert status["one_swap_improving_rows_v194"] == 4354
    assert status["best_one_swap_return_delta_v194"] == pytest.approx(92.41760413748665)
    assert status["best_one_swap_cvar90_after_v194"] == pytest.approx(96204.95119771129)
    assert status["post_repair_one_swap_local_optimality_cleared_v194"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v194"] is False
    assert status["paper1_promotion_allowed_v194"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v194_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v194"]
    assert probe["return_delta_v194"].gt(0).all()
    assert probe["budget_swap_feasible_v194"].astype(bool).all()
    assert probe["source_swap_feasible_v194"].astype(bool).all()
    assert probe["cvar_swap_feasible_v194"].astype(bool).all()
    assert probe["one_swap_improves_return_v194"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v194"].sum()) == 0
    assert probe["return_delta_v194"].max() == pytest.approx(
        status["best_one_swap_return_delta_v194"]
    )
    assert probe["claim_boundary_v194"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v194_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v194"]) == "140230494"
    assert str(best["dropped_loan_id_v194"]) == "158509118"
    assert float(best["objective_return_after_swap_v194"]) == pytest.approx(2124.159077068949)
    assert float(best["exposure_after_swap_v194"]) == pytest.approx(842450.0)
    assert float(best["source_min_slack_after_swap_v194"]) == pytest.approx(5.754096345278459e-05)
    assert bool(best["one_swap_improves_return_v194"]) is True

    summary = _read_csv("paper4_v194_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v194"]) == status["one_swap_improving_rows_v194"]
    assert float(row["current_objective_return_v194"]) == pytest.approx(2031.7414729314623)
    assert float(row["current_exposure_v194"]) == pytest.approx(842825.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v194"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v194"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v194"])

    stage_summary = _read_csv("paper4_v194_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v194"], stage_summary["pair_rows_v194"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v194"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v194"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v194"]

    blockers = _read_csv("paper4_v194_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v194"], blockers["blocking_v194"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v194"], blockers["evidence_count_v194"], strict=False)
    )
    assert bool(blocker_map["post_v193_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v193_one_swap_improvement_found"]) == 4354
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v194_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v194_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v194_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v194_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v194_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v194 post-v193 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v194 proves the v193 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v194 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v194: Post-v193 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4354`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v195_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v195_status.json")

    assert status["phase"] == "v195_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.195"
    assert status["allocation_rows_v195"] == 171
    assert status["summary_rows_v195"] == 1
    assert status["action_rows_v195"] == 1
    assert status["source_summary_rows_v195"] == 51
    assert status["claim_blocker_rows_v195"] == 4
    assert status["added_loan_id_v195"] == "140230494"
    assert status["dropped_loan_id_v195"] == "158509118"
    assert status["selected_rows_v195"] == 171
    assert status["portfolio_exposure_v195"] == pytest.approx(842450.0)
    assert status["objective_return_v195"] == pytest.approx(2124.15907706895)
    assert status["scenario_loss_cvar90_v195"] == pytest.approx(96204.95119771129)
    assert status["source_cap_violations_v195"] == 0
    assert status["delta_return_vs_v193_v195"] == pytest.approx(92.41760413748761)
    assert status["delta_cvar90_vs_v193_v195"] == pytest.approx(-330.1490416318411)
    assert status["delta_exposure_vs_v193_v195"] == pytest.approx(-375.0)
    assert status["budget_feasible_v195"] is True
    assert status["source_feasible_v195"] is True
    assert status["cvar_feasible_v195"] is True
    assert status["repair_candidate_feasible_v195"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v195"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v195"] is False
    assert status["paper1_promotion_allowed_v195"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v195_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v195"]
    assert int(allocations["selected_v195"].sum()) == status["selected_rows_v195"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v195"])
    assert "140230494" in set(allocations["loan_id"].astype(str))
    assert "158509118" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v195"]) == {
        "added_from_v194_best_swap",
        "kept_from_v193",
    }
    assert allocations["claim_boundary_v195"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v195_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v195"]) is True
    assert float(row["delta_exposure_vs_v193_v195"]) == pytest.approx(-375.0)
    assert float(row["delta_cvar90_vs_v193_v195"]) < 0
    assert float(row["scenario_loss_cvar90_v195"]) <= float(row["cvar_cap_v195"])
    assert float(row["max_source_share_v195"]) == pytest.approx(0.8995489346548756)
    assert float(row["min_source_slack_v195"]) == pytest.approx(5.754096345278459e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v195"])

    action = _read_csv("paper4_v195_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v195"]) == status["added_loan_id_v195"]
    assert str(action["dropped_loan_id_v195"]) == status["dropped_loan_id_v195"]
    assert float(action["return_delta_v195"]) == pytest.approx(92.41760413748663)
    assert float(action["exposure_after_repair_v195"]) == pytest.approx(842450.0)
    assert int(action["source_cap_violations_after_repair_v195"]) == 0

    source_summary = _read_csv("paper4_v195_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v195"]
    assert not source_summary["source_cap_violated_v195"].astype(bool).any()

    blockers = _read_csv("paper4_v195_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v195"], blockers["blocking_v195"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v195_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v195_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v195_repair_candidate_feasible"]) is True
    assert bool(claim_map["v195_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v195_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v195_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v195 fifty-seventh one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v195 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v195 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v195: Fifty-Seventh One-Swap Repair Candidate" in notebook
    assert "v196 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v196_post_v195_reprice_contracts_reopened_front() -> None:
    status = _read_json("paper4_v196_status.json")

    assert status["phase"] == "v196_post_v195_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.196"
    assert status["summary_rows_v196"] == 1
    assert status["stage_summary_rows_v196"] == 6
    assert status["candidate_pair_rows_v196"] == 539
    assert status["top_candidate_rows_v196"] == 200
    assert status["claim_blocker_rows_v196"] == 3
    assert status["selected_rows_v196"] == 171
    assert status["candidate_add_rows_v196"] == 276698
    assert status["total_pair_rows_screened_v196"] == 47315358
    assert status["return_improving_pair_rows_v196"] == 1715011
    assert status["budget_return_feasible_pair_rows_v196"] == 1058372
    assert status["source_prefilter_pair_rows_v196"] == 563
    assert status["source_exact_pair_rows_v196"] == 539
    assert status["cvar_feasible_pair_rows_v196"] == 539
    assert status["one_swap_improving_rows_v196"] == 539
    assert status["best_one_swap_return_delta_v196"] == pytest.approx(41.98245890639751)
    assert status["best_one_swap_cvar90_after_v196"] == pytest.approx(96285.39668408083)
    assert status["post_repair_one_swap_local_optimality_cleared_v196"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v196"] is False
    assert status["paper1_promotion_allowed_v196"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v196_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v196"]
    assert probe["return_delta_v196"].gt(0).all()
    assert probe["budget_swap_feasible_v196"].astype(bool).all()
    assert probe["source_swap_feasible_v196"].astype(bool).all()
    assert probe["cvar_swap_feasible_v196"].astype(bool).all()
    assert probe["one_swap_improves_return_v196"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v196"].sum()) == 0
    assert probe["return_delta_v196"].max() == pytest.approx(
        status["best_one_swap_return_delta_v196"]
    )
    assert probe["claim_boundary_v196"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v196_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v196"]) == "136288831"
    assert str(best["dropped_loan_id_v196"]) == "126820122"
    assert float(best["objective_return_after_swap_v196"]) == pytest.approx(2166.1415359753473)
    assert float(best["exposure_after_swap_v196"]) == pytest.approx(842450.0)
    assert bool(best["one_swap_improves_return_v196"]) is True

    summary = _read_csv("paper4_v196_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v196"]) == status["one_swap_improving_rows_v196"]
    assert float(row["current_objective_return_v196"]) == pytest.approx(2124.15907706895)
    assert float(row["current_exposure_v196"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v196"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v196"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v196"])

    stage_summary = _read_csv("paper4_v196_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v196"], stage_summary["pair_rows_v196"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v196"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v196"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v196"]

    blockers = _read_csv("paper4_v196_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v196"], blockers["blocking_v196"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v196"], blockers["evidence_count_v196"], strict=False)
    )
    assert bool(blocker_map["post_v195_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v195_one_swap_improvement_found"]) == 539
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v196_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v196_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v196_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v196_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v196_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v196 post-v195 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v196 proves the v195 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v196 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v196: Post-v195 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `539`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v197_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v197_status.json")

    assert status["phase"] == "v197_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.197"
    assert status["allocation_rows_v197"] == 171
    assert status["summary_rows_v197"] == 1
    assert status["action_rows_v197"] == 1
    assert status["source_summary_rows_v197"] == 51
    assert status["claim_blocker_rows_v197"] == 4
    assert status["added_loan_id_v197"] == "136288831"
    assert status["dropped_loan_id_v197"] == "126820122"
    assert status["selected_rows_v197"] == 171
    assert status["portfolio_exposure_v197"] == pytest.approx(842450.0)
    assert status["objective_return_v197"] == pytest.approx(2166.1415359753464)
    assert status["scenario_loss_cvar90_v197"] == pytest.approx(96285.39668408083)
    assert status["source_cap_violations_v197"] == 0
    assert status["delta_return_vs_v195_v197"] == pytest.approx(41.98245890639646)
    assert status["delta_cvar90_vs_v195_v197"] == pytest.approx(80.44548636954278)
    assert status["delta_exposure_vs_v195_v197"] == pytest.approx(0.0)
    assert status["budget_feasible_v197"] is True
    assert status["source_feasible_v197"] is True
    assert status["cvar_feasible_v197"] is True
    assert status["repair_candidate_feasible_v197"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v197"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v197"] is False
    assert status["paper1_promotion_allowed_v197"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v197_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v197"]
    assert int(allocations["selected_v197"].sum()) == status["selected_rows_v197"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v197"])
    assert "136288831" in set(allocations["loan_id"].astype(str))
    assert "126820122" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v197"]) == {
        "added_from_v196_best_swap",
        "kept_from_v195",
    }
    assert allocations["claim_boundary_v197"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v197_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v197"]) is True
    assert float(row["delta_cvar90_vs_v195_v197"]) > 0
    assert float(row["scenario_loss_cvar90_v197"]) <= float(row["cvar_cap_v197"])
    assert float(row["max_source_share_v197"]) == pytest.approx(0.8995489346548756)
    assert float(row["min_source_slack_v197"]) == pytest.approx(5.754096345278459e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v197"])

    action = _read_csv("paper4_v197_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v197"]) == status["added_loan_id_v197"]
    assert str(action["dropped_loan_id_v197"]) == status["dropped_loan_id_v197"]
    assert float(action["return_delta_v197"]) == pytest.approx(41.98245890639751)
    assert float(action["exposure_after_repair_v197"]) == pytest.approx(842450.0)
    assert int(action["source_cap_violations_after_repair_v197"]) == 0

    source_summary = _read_csv("paper4_v197_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v197"]
    assert not source_summary["source_cap_violated_v197"].astype(bool).any()

    blockers = _read_csv("paper4_v197_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v197"], blockers["blocking_v197"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v197_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v197_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v197_repair_candidate_feasible"]) is True
    assert bool(claim_map["v197_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v197_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v197_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v197 fifty-eighth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v197 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v197 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v197: Fifty-Eighth One-Swap Repair Candidate" in notebook
    assert "v198 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v198_post_v197_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v198_status.json")

    assert status["phase"] == "v198_post_v197_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.198"
    assert status["summary_rows_v198"] == 1
    assert status["stage_summary_rows_v198"] == 6
    assert status["candidate_pair_rows_v198"] == 459
    assert status["top_candidate_rows_v198"] == 200
    assert status["claim_blocker_rows_v198"] == 3
    assert status["selected_rows_v198"] == 171
    assert status["candidate_add_rows_v198"] == 276698
    assert status["total_pair_rows_screened_v198"] == 47315358
    assert status["return_improving_pair_rows_v198"] == 1712123
    assert status["budget_return_feasible_pair_rows_v198"] == 1056449
    assert status["source_prefilter_pair_rows_v198"] == 482
    assert status["source_exact_pair_rows_v198"] == 459
    assert status["cvar_feasible_pair_rows_v198"] == 459
    assert status["one_swap_improving_rows_v198"] == 459
    assert status["best_one_swap_return_delta_v198"] == pytest.approx(41.769759661746264)
    assert status["best_one_swap_cvar90_after_v198"] == pytest.approx(97026.14714488928)
    assert status["post_repair_one_swap_local_optimality_cleared_v198"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v198"] is False
    assert status["paper1_promotion_allowed_v198"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v198_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v198"]
    assert probe["return_delta_v198"].gt(0).all()
    assert probe["budget_swap_feasible_v198"].astype(bool).all()
    assert probe["source_swap_feasible_v198"].astype(bool).all()
    assert probe["cvar_swap_feasible_v198"].astype(bool).all()
    assert probe["one_swap_improves_return_v198"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v198"].sum()) == 0
    assert probe["return_delta_v198"].max() == pytest.approx(
        status["best_one_swap_return_delta_v198"]
    )
    assert probe["claim_boundary_v198"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v198_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v198"]) == "164456389"
    assert str(best["dropped_loan_id_v198"]) == "127640797"
    assert float(best["objective_return_after_swap_v198"]) == pytest.approx(2207.9112956370927)
    assert float(best["exposure_after_swap_v198"]) == pytest.approx(842425.0)
    assert bool(best["one_swap_improves_return_v198"]) is True

    summary = _read_csv("paper4_v198_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v198"]) == status["one_swap_improving_rows_v198"]
    assert float(row["current_objective_return_v198"]) == pytest.approx(2166.1415359753464)
    assert float(row["current_exposure_v198"]) == pytest.approx(842450.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v198"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v198"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v198"])

    stage_summary = _read_csv("paper4_v198_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v198"], stage_summary["pair_rows_v198"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v198"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v198"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v198"]

    blockers = _read_csv("paper4_v198_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v198"], blockers["blocking_v198"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v198"], blockers["evidence_count_v198"], strict=False)
    )
    assert bool(blocker_map["post_v197_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v197_one_swap_improvement_found"]) == 459
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v198_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v198_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v198_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v198_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v198_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v198 post-v197 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v198 proves the v197 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v198 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v198: Post-v197 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `459`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v199_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v199_status.json")

    assert status["phase"] == "v199_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.199"
    assert status["allocation_rows_v199"] == 171
    assert status["summary_rows_v199"] == 1
    assert status["action_rows_v199"] == 1
    assert status["source_summary_rows_v199"] == 51
    assert status["claim_blocker_rows_v199"] == 4
    assert status["added_loan_id_v199"] == "164456389"
    assert status["dropped_loan_id_v199"] == "127640797"
    assert status["selected_rows_v199"] == 171
    assert status["portfolio_exposure_v199"] == pytest.approx(842425.0)
    assert status["objective_return_v199"] == pytest.approx(2207.9112956370927)
    assert status["scenario_loss_cvar90_v199"] == pytest.approx(97026.14714488928)
    assert status["source_cap_violations_v199"] == 0
    assert status["delta_return_vs_v197_v199"] == pytest.approx(41.76975966174632)
    assert status["delta_cvar90_vs_v197_v199"] == pytest.approx(740.7504608084419)
    assert status["delta_exposure_vs_v197_v199"] == pytest.approx(-25.0)
    assert status["budget_feasible_v199"] is True
    assert status["source_feasible_v199"] is True
    assert status["cvar_feasible_v199"] is True
    assert status["repair_candidate_feasible_v199"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v199"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v199"] is False
    assert status["paper1_promotion_allowed_v199"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v199_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v199"]
    assert int(allocations["selected_v199"].sum()) == status["selected_rows_v199"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v199"])
    assert "164456389" in set(allocations["loan_id"].astype(str))
    assert "127640797" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v199"]) == {
        "added_from_v198_best_swap",
        "kept_from_v197",
    }
    assert allocations["claim_boundary_v199"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v199_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v199"]) is True
    assert float(row["delta_cvar90_vs_v197_v199"]) > 0
    assert float(row["scenario_loss_cvar90_v199"]) <= float(row["cvar_cap_v199"])
    assert float(row["max_source_share_v199"]) == pytest.approx(0.8959848057690596)
    assert float(row["min_source_slack_v199"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v199"])

    action = _read_csv("paper4_v199_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v199"]) == status["added_loan_id_v199"]
    assert str(action["dropped_loan_id_v199"]) == status["dropped_loan_id_v199"]
    assert float(action["return_delta_v199"]) == pytest.approx(41.76975966174626)
    assert float(action["exposure_after_repair_v199"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v199"]) == 0

    source_summary = _read_csv("paper4_v199_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v199"]
    assert not source_summary["source_cap_violated_v199"].astype(bool).any()

    blockers = _read_csv("paper4_v199_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v199"], blockers["blocking_v199"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v199_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v199_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v199_repair_candidate_feasible"]) is True
    assert bool(claim_map["v199_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v199_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v199_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v199 fifty-ninth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v199 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v199 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v199: Fifty-Ninth One-Swap Repair Candidate" in notebook
    assert "v200 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v200_post_v199_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v200_status.json")

    assert status["phase"] == "v200_post_v199_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.200"
    assert status["summary_rows_v200"] == 1
    assert status["stage_summary_rows_v200"] == 6
    assert status["candidate_pair_rows_v200"] == 1907
    assert status["top_candidate_rows_v200"] == 200
    assert status["claim_blocker_rows_v200"] == 3
    assert status["selected_rows_v200"] == 171
    assert status["candidate_add_rows_v200"] == 276698
    assert status["total_pair_rows_screened_v200"] == 47315358
    assert status["return_improving_pair_rows_v200"] == 1709481
    assert status["budget_return_feasible_pair_rows_v200"] == 1055011
    assert status["source_prefilter_pair_rows_v200"] == 1984
    assert status["source_exact_pair_rows_v200"] == 1907
    assert status["cvar_feasible_pair_rows_v200"] == 1907
    assert status["one_swap_improving_rows_v200"] == 1907
    assert status["best_one_swap_return_delta_v200"] == pytest.approx(115.73891280112039)
    assert status["best_one_swap_cvar90_after_v200"] == pytest.approx(96869.65824564731)
    assert status["post_repair_one_swap_local_optimality_cleared_v200"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v200"] is False
    assert status["paper1_promotion_allowed_v200"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v200_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v200"]
    assert probe["return_delta_v200"].gt(0).all()
    assert probe["budget_swap_feasible_v200"].astype(bool).all()
    assert probe["source_swap_feasible_v200"].astype(bool).all()
    assert probe["cvar_swap_feasible_v200"].astype(bool).all()
    assert probe["one_swap_improves_return_v200"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v200"].sum()) == 0
    assert probe["return_delta_v200"].max() == pytest.approx(
        status["best_one_swap_return_delta_v200"]
    )
    assert probe["claim_boundary_v200"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v200_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v200"]) == "143738130"
    assert str(best["dropped_loan_id_v200"]) == "126523487"
    assert float(best["objective_return_after_swap_v200"]) == pytest.approx(2323.650208438213)
    assert float(best["exposure_after_swap_v200"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v200"]) == pytest.approx(96869.65824564733)
    assert bool(best["one_swap_improves_return_v200"]) is True

    summary = _read_csv("paper4_v200_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v200"]) == status["one_swap_improving_rows_v200"]
    assert float(row["current_objective_return_v200"]) == pytest.approx(2207.9112956370927)
    assert float(row["current_exposure_v200"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v200"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v200"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v200"])

    stage_summary = _read_csv("paper4_v200_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v200"], stage_summary["pair_rows_v200"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v200"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v200"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v200"]

    blockers = _read_csv("paper4_v200_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v200"], blockers["blocking_v200"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v200"], blockers["evidence_count_v200"], strict=False)
    )
    assert bool(blocker_map["post_v199_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v199_one_swap_improvement_found"]) == 1907
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v200_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v200_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v200_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v200_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v200_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v200 post-v199 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v200 proves the v199 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v200 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v200: Post-v199 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1907`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v201_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v201_status.json")

    assert status["phase"] == "v201_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.201"
    assert status["allocation_rows_v201"] == 171
    assert status["summary_rows_v201"] == 1
    assert status["action_rows_v201"] == 1
    assert status["source_summary_rows_v201"] == 51
    assert status["claim_blocker_rows_v201"] == 4
    assert status["added_loan_id_v201"] == "143738130"
    assert status["dropped_loan_id_v201"] == "126523487"
    assert status["selected_rows_v201"] == 171
    assert status["portfolio_exposure_v201"] == pytest.approx(842425.0)
    assert status["objective_return_v201"] == pytest.approx(2323.650208438211)
    assert status["scenario_loss_cvar90_v201"] == pytest.approx(96869.65824564731)
    assert status["source_cap_violations_v201"] == 0
    assert status["delta_return_vs_v199_v201"] == pytest.approx(115.7389128011182)
    assert status["delta_cvar90_vs_v199_v201"] == pytest.approx(-156.48889924197283)
    assert status["delta_exposure_vs_v199_v201"] == pytest.approx(0.0)
    assert status["budget_feasible_v201"] is True
    assert status["source_feasible_v201"] is True
    assert status["cvar_feasible_v201"] is True
    assert status["repair_candidate_feasible_v201"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v201"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v201"] is False
    assert status["paper1_promotion_allowed_v201"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v201_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v201"]
    assert int(allocations["selected_v201"].sum()) == status["selected_rows_v201"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v201"])
    assert "143738130" in set(allocations["loan_id"].astype(str))
    assert "126523487" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v201"]) == {
        "added_from_v200_best_swap",
        "kept_from_v199",
    }
    assert allocations["claim_boundary_v201"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v201_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v201"]) is True
    assert float(row["delta_cvar90_vs_v199_v201"]) < 0
    assert float(row["scenario_loss_cvar90_v201"]) <= float(row["cvar_cap_v201"])
    assert float(row["max_source_share_v201"]) == pytest.approx(0.8983589043535033)
    assert float(row["min_source_slack_v201"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v201"])

    action = _read_csv("paper4_v201_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v201"]) == status["added_loan_id_v201"]
    assert str(action["dropped_loan_id_v201"]) == status["dropped_loan_id_v201"]
    assert float(action["return_delta_v201"]) == pytest.approx(115.7389128011204)
    assert float(action["exposure_after_repair_v201"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v201"]) == 0

    source_summary = _read_csv("paper4_v201_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v201"]
    assert not source_summary["source_cap_violated_v201"].astype(bool).any()

    blockers = _read_csv("paper4_v201_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v201"], blockers["blocking_v201"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v201_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v201_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v201_repair_candidate_feasible"]) is True
    assert bool(claim_map["v201_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v201_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v201_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v201 sixtieth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v201 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v201 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v201: Sixtieth One-Swap Repair Candidate" in notebook
    assert "v202 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v202_post_v201_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v202_status.json")

    assert status["phase"] == "v202_post_v201_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.202"
    assert status["summary_rows_v202"] == 1
    assert status["stage_summary_rows_v202"] == 6
    assert status["candidate_pair_rows_v202"] == 1566
    assert status["top_candidate_rows_v202"] == 200
    assert status["claim_blocker_rows_v202"] == 3
    assert status["selected_rows_v202"] == 171
    assert status["candidate_add_rows_v202"] == 276698
    assert status["total_pair_rows_screened_v202"] == 47315358
    assert status["return_improving_pair_rows_v202"] == 1700512
    assert status["budget_return_feasible_pair_rows_v202"] == 1051023
    assert status["source_prefilter_pair_rows_v202"] == 1603
    assert status["source_exact_pair_rows_v202"] == 1566
    assert status["cvar_feasible_pair_rows_v202"] == 1566
    assert status["one_swap_improving_rows_v202"] == 1566
    assert status["best_one_swap_return_delta_v202"] == pytest.approx(53.11880047344977)
    assert status["best_one_swap_cvar90_after_v202"] == pytest.approx(96788.7798001177)
    assert status["post_repair_one_swap_local_optimality_cleared_v202"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v202"] is False
    assert status["paper1_promotion_allowed_v202"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v202_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v202"]
    assert probe["return_delta_v202"].gt(0).all()
    assert probe["budget_swap_feasible_v202"].astype(bool).all()
    assert probe["source_swap_feasible_v202"].astype(bool).all()
    assert probe["cvar_swap_feasible_v202"].astype(bool).all()
    assert probe["one_swap_improves_return_v202"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v202"].sum()) == 0
    assert probe["return_delta_v202"].max() == pytest.approx(
        status["best_one_swap_return_delta_v202"]
    )
    assert probe["claim_boundary_v202"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v202_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v202"]) == "134420497"
    assert str(best["dropped_loan_id_v202"]) == "126419064"
    assert float(best["objective_return_after_swap_v202"]) == pytest.approx(2376.769008911661)
    assert float(best["exposure_after_swap_v202"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v202"]) == pytest.approx(96788.7798001177)
    assert bool(best["one_swap_improves_return_v202"]) is True

    summary = _read_csv("paper4_v202_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v202"]) == status["one_swap_improving_rows_v202"]
    assert float(row["current_objective_return_v202"]) == pytest.approx(2323.650208438211)
    assert float(row["current_exposure_v202"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v202"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v202"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v202"])

    stage_summary = _read_csv("paper4_v202_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v202"], stage_summary["pair_rows_v202"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v202"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v202"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v202"]

    blockers = _read_csv("paper4_v202_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v202"], blockers["blocking_v202"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v202"], blockers["evidence_count_v202"], strict=False)
    )
    assert bool(blocker_map["post_v201_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v201_one_swap_improvement_found"]) == 1566
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v202_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v202_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v202_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v202_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v202_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v202 post-v201 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v202 proves the v201 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v202 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v202: Post-v201 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `1566`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v203_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v203_status.json")

    assert status["phase"] == "v203_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.203"
    assert status["allocation_rows_v203"] == 171
    assert status["summary_rows_v203"] == 1
    assert status["action_rows_v203"] == 1
    assert status["source_summary_rows_v203"] == 51
    assert status["claim_blocker_rows_v203"] == 4
    assert status["added_loan_id_v203"] == "134420497"
    assert status["dropped_loan_id_v203"] == "126419064"
    assert status["selected_rows_v203"] == 171
    assert status["portfolio_exposure_v203"] == pytest.approx(842425.0)
    assert status["objective_return_v203"] == pytest.approx(2376.7690089116622)
    assert status["scenario_loss_cvar90_v203"] == pytest.approx(96788.7798001177)
    assert status["source_cap_violations_v203"] == 0
    assert status["delta_return_vs_v201_v203"] == pytest.approx(53.1188004734513)
    assert status["delta_cvar90_vs_v201_v203"] == pytest.approx(-80.87844552962633)
    assert status["delta_exposure_vs_v201_v203"] == pytest.approx(0.0)
    assert status["budget_feasible_v203"] is True
    assert status["source_feasible_v203"] is True
    assert status["cvar_feasible_v203"] is True
    assert status["repair_candidate_feasible_v203"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v203"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v203"] is False
    assert status["paper1_promotion_allowed_v203"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v203_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v203"]
    assert int(allocations["selected_v203"].sum()) == status["selected_rows_v203"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v203"])
    assert "134420497" in set(allocations["loan_id"].astype(str))
    assert "126419064" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v203"]) == {
        "added_from_v202_best_swap",
        "kept_from_v201",
    }
    assert allocations["claim_boundary_v203"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v203_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v203"]) is True
    assert float(row["delta_cvar90_vs_v201_v203"]) < 0
    assert float(row["scenario_loss_cvar90_v203"]) <= float(row["cvar_cap_v203"])
    assert float(row["max_source_share_v203"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v203"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v203"])

    action = _read_csv("paper4_v203_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v203"]) == status["added_loan_id_v203"]
    assert str(action["dropped_loan_id_v203"]) == status["dropped_loan_id_v203"]
    assert float(action["return_delta_v203"]) == pytest.approx(53.11880047344977)
    assert float(action["exposure_after_repair_v203"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v203"]) == 0

    source_summary = _read_csv("paper4_v203_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v203"]
    assert not source_summary["source_cap_violated_v203"].astype(bool).any()

    blockers = _read_csv("paper4_v203_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v203"], blockers["blocking_v203"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v203_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v203_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v203_repair_candidate_feasible"]) is True
    assert bool(claim_map["v203_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v203_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v203_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v203 sixty-first one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v203 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v203 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v203: Sixty-First One-Swap Repair Candidate" in notebook
    assert "v204 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v204_post_v203_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v204_status.json")

    assert status["phase"] == "v204_post_v203_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.204"
    assert status["summary_rows_v204"] == 1
    assert status["stage_summary_rows_v204"] == 6
    assert status["candidate_pair_rows_v204"] == 360
    assert status["top_candidate_rows_v204"] == 200
    assert status["claim_blocker_rows_v204"] == 3
    assert status["selected_rows_v204"] == 171
    assert status["candidate_add_rows_v204"] == 276698
    assert status["total_pair_rows_screened_v204"] == 47315358
    assert status["return_improving_pair_rows_v204"] == 1696907
    assert status["budget_return_feasible_pair_rows_v204"] == 1048605
    assert status["source_prefilter_pair_rows_v204"] == 380
    assert status["source_exact_pair_rows_v204"] == 360
    assert status["cvar_feasible_pair_rows_v204"] == 360
    assert status["one_swap_improving_rows_v204"] == 360
    assert status["best_one_swap_return_delta_v204"] == pytest.approx(39.18466238758043)
    assert status["best_one_swap_cvar90_after_v204"] == pytest.approx(96865.79533821657)
    assert status["post_repair_one_swap_local_optimality_cleared_v204"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v204"] is False
    assert status["paper1_promotion_allowed_v204"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v204_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v204"]
    assert probe["return_delta_v204"].gt(0).all()
    assert probe["budget_swap_feasible_v204"].astype(bool).all()
    assert probe["source_swap_feasible_v204"].astype(bool).all()
    assert probe["cvar_swap_feasible_v204"].astype(bool).all()
    assert probe["one_swap_improves_return_v204"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v204"].sum()) == 0
    assert probe["return_delta_v204"].max() == pytest.approx(
        status["best_one_swap_return_delta_v204"]
    )
    assert probe["claim_boundary_v204"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v204_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v204"]) == "140155571"
    assert str(best["dropped_loan_id_v204"]) == "126483031"
    assert float(best["objective_return_after_swap_v204"]) == pytest.approx(2415.9536712992426)
    assert float(best["exposure_after_swap_v204"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v204"]) == pytest.approx(96865.79533821656)
    assert bool(best["one_swap_improves_return_v204"]) is True

    summary = _read_csv("paper4_v204_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v204"]) == status["one_swap_improving_rows_v204"]
    assert float(row["current_objective_return_v204"]) == pytest.approx(2376.7690089116622)
    assert float(row["current_exposure_v204"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v204"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v204"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v204"])

    stage_summary = _read_csv("paper4_v204_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v204"], stage_summary["pair_rows_v204"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v204"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v204"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v204"]

    blockers = _read_csv("paper4_v204_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v204"], blockers["blocking_v204"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v204"], blockers["evidence_count_v204"], strict=False)
    )
    assert bool(blocker_map["post_v203_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v203_one_swap_improvement_found"]) == 360
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v204_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v204_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v204_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v204_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v204_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v204 post-v203 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v204 proves the v203 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v204 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v204: Post-v203 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `360`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v205_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v205_status.json")

    assert status["phase"] == "v205_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.205"
    assert status["allocation_rows_v205"] == 171
    assert status["summary_rows_v205"] == 1
    assert status["action_rows_v205"] == 1
    assert status["source_summary_rows_v205"] == 51
    assert status["claim_blocker_rows_v205"] == 4
    assert status["added_loan_id_v205"] == "140155571"
    assert status["dropped_loan_id_v205"] == "126483031"
    assert status["selected_rows_v205"] == 171
    assert status["portfolio_exposure_v205"] == pytest.approx(842425.0)
    assert status["objective_return_v205"] == pytest.approx(2415.953671299242)
    assert status["scenario_loss_cvar90_v205"] == pytest.approx(96865.79533821656)
    assert status["source_cap_violations_v205"] == 0
    assert status["delta_return_vs_v203_v205"] == pytest.approx(39.18466238757992)
    assert status["delta_cvar90_vs_v203_v205"] == pytest.approx(77.01553809885809)
    assert status["delta_exposure_vs_v203_v205"] == pytest.approx(0.0)
    assert status["budget_feasible_v205"] is True
    assert status["source_feasible_v205"] is True
    assert status["cvar_feasible_v205"] is True
    assert status["repair_candidate_feasible_v205"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v205"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v205"] is False
    assert status["paper1_promotion_allowed_v205"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v205_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v205"]
    assert int(allocations["selected_v205"].sum()) == status["selected_rows_v205"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v205"])
    assert "140155571" in set(allocations["loan_id"].astype(str))
    assert "126483031" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v205"]) == {
        "added_from_v204_best_swap",
        "kept_from_v203",
    }
    assert allocations["claim_boundary_v205"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v205_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v205"]) is True
    assert float(row["delta_cvar90_vs_v203_v205"]) > 0
    assert float(row["scenario_loss_cvar90_v205"]) <= float(row["cvar_cap_v205"])
    assert float(row["max_source_share_v205"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v205"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v205"])

    action = _read_csv("paper4_v205_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v205"]) == status["added_loan_id_v205"]
    assert str(action["dropped_loan_id_v205"]) == status["dropped_loan_id_v205"]
    assert float(action["return_delta_v205"]) == pytest.approx(39.18466238758043)
    assert float(action["exposure_after_repair_v205"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v205"]) == 0

    source_summary = _read_csv("paper4_v205_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v205"]
    assert not source_summary["source_cap_violated_v205"].astype(bool).any()

    blockers = _read_csv("paper4_v205_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v205"], blockers["blocking_v205"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v205_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v205_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v205_repair_candidate_feasible"]) is True
    assert bool(claim_map["v205_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v205_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v205_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v205 sixty-second one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v205 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v205 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v205: Sixty-Second One-Swap Repair Candidate" in notebook
    assert "v206 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v206_post_v205_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v206_status.json")

    assert status["phase"] == "v206_post_v205_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.206"
    assert status["summary_rows_v206"] == 1
    assert status["stage_summary_rows_v206"] == 6
    assert status["candidate_pair_rows_v206"] == 344
    assert status["top_candidate_rows_v206"] == 200
    assert status["claim_blocker_rows_v206"] == 3
    assert status["selected_rows_v206"] == 171
    assert status["candidate_add_rows_v206"] == 276698
    assert status["total_pair_rows_screened_v206"] == 47315358
    assert status["return_improving_pair_rows_v206"] == 1694213
    assert status["budget_return_feasible_pair_rows_v206"] == 1046907
    assert status["source_prefilter_pair_rows_v206"] == 359
    assert status["source_exact_pair_rows_v206"] == 344
    assert status["cvar_feasible_pair_rows_v206"] == 344
    assert status["one_swap_improving_rows_v206"] == 344
    assert status["best_one_swap_return_delta_v206"] == pytest.approx(35.385628875693044)
    assert status["best_one_swap_cvar90_after_v206"] == pytest.approx(97050.39226781993)
    assert status["post_repair_one_swap_local_optimality_cleared_v206"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v206"] is False
    assert status["paper1_promotion_allowed_v206"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v206_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v206"]
    assert probe["return_delta_v206"].gt(0).all()
    assert probe["budget_swap_feasible_v206"].astype(bool).all()
    assert probe["source_swap_feasible_v206"].astype(bool).all()
    assert probe["cvar_swap_feasible_v206"].astype(bool).all()
    assert probe["one_swap_improves_return_v206"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v206"].sum()) == 0
    assert probe["return_delta_v206"].max() == pytest.approx(
        status["best_one_swap_return_delta_v206"]
    )
    assert probe["claim_boundary_v206"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v206_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v206"]) == "162605401"
    assert str(best["dropped_loan_id_v206"]) == "127672145"
    assert float(best["objective_return_after_swap_v206"]) == pytest.approx(2451.339300174935)
    assert float(best["exposure_after_swap_v206"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v206"]) == pytest.approx(97050.39226781992)
    assert bool(best["one_swap_improves_return_v206"]) is True

    summary = _read_csv("paper4_v206_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v206"]) == status["one_swap_improving_rows_v206"]
    assert float(row["current_objective_return_v206"]) == pytest.approx(2415.953671299242)
    assert float(row["current_exposure_v206"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v206"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v206"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v206"])

    stage_summary = _read_csv("paper4_v206_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v206"], stage_summary["pair_rows_v206"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v206"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v206"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v206"]

    blockers = _read_csv("paper4_v206_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v206"], blockers["blocking_v206"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v206"], blockers["evidence_count_v206"], strict=False)
    )
    assert bool(blocker_map["post_v205_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v205_one_swap_improvement_found"]) == 344
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v206_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v206_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v206_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v206_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v206_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v206 post-v205 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v206 proves the v205 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v206 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v206: Post-v205 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `344`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v207_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v207_status.json")

    assert status["phase"] == "v207_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.207"
    assert status["allocation_rows_v207"] == 171
    assert status["summary_rows_v207"] == 1
    assert status["action_rows_v207"] == 1
    assert status["source_summary_rows_v207"] == 51
    assert status["claim_blocker_rows_v207"] == 4
    assert status["added_loan_id_v207"] == "162605401"
    assert status["dropped_loan_id_v207"] == "127672145"
    assert status["selected_rows_v207"] == 171
    assert status["portfolio_exposure_v207"] == pytest.approx(842425.0)
    assert status["objective_return_v207"] == pytest.approx(2451.3393001749364)
    assert status["scenario_loss_cvar90_v207"] == pytest.approx(97050.39226781993)
    assert status["source_cap_violations_v207"] == 0
    assert status["delta_return_vs_v205_v207"] == pytest.approx(35.38562887569424)
    assert status["delta_cvar90_vs_v205_v207"] == pytest.approx(184.5969296033727)
    assert status["delta_exposure_vs_v205_v207"] == pytest.approx(0.0)
    assert status["budget_feasible_v207"] is True
    assert status["source_feasible_v207"] is True
    assert status["cvar_feasible_v207"] is True
    assert status["repair_candidate_feasible_v207"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v207"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v207"] is False
    assert status["paper1_promotion_allowed_v207"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v207_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v207"]
    assert int(allocations["selected_v207"].sum()) == status["selected_rows_v207"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v207"])
    assert "162605401" in set(allocations["loan_id"].astype(str))
    assert "127672145" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v207"]) == {
        "added_from_v206_best_swap",
        "kept_from_v205",
    }
    assert allocations["claim_boundary_v207"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v207_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v207"]) is True
    assert float(row["delta_cvar90_vs_v205_v207"]) > 0
    assert float(row["scenario_loss_cvar90_v207"]) <= float(row["cvar_cap_v207"])
    assert float(row["max_source_share_v207"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v207"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v207"])

    action = _read_csv("paper4_v207_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v207"]) == status["added_loan_id_v207"]
    assert str(action["dropped_loan_id_v207"]) == status["dropped_loan_id_v207"]
    assert float(action["return_delta_v207"]) == pytest.approx(35.385628875693044)
    assert float(action["exposure_after_repair_v207"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v207"]) == 0

    source_summary = _read_csv("paper4_v207_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v207"]
    assert not source_summary["source_cap_violated_v207"].astype(bool).any()

    blockers = _read_csv("paper4_v207_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v207"], blockers["blocking_v207"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v207_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v207_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v207_repair_candidate_feasible"]) is True
    assert bool(claim_map["v207_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v207_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v207_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v207 sixty-third one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v207 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v207 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v207: Sixty-Third One-Swap Repair Candidate" in notebook
    assert "v208 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v208_post_v207_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v208_status.json")

    assert status["phase"] == "v208_post_v207_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.208"
    assert status["summary_rows_v208"] == 1
    assert status["stage_summary_rows_v208"] == 6
    assert status["candidate_pair_rows_v208"] == 279
    assert status["top_candidate_rows_v208"] == 200
    assert status["claim_blocker_rows_v208"] == 3
    assert status["selected_rows_v208"] == 171
    assert status["candidate_add_rows_v208"] == 276698
    assert status["total_pair_rows_screened_v208"] == 47315358
    assert status["return_improving_pair_rows_v208"] == 1691811
    assert status["budget_return_feasible_pair_rows_v208"] == 1045321
    assert status["source_prefilter_pair_rows_v208"] == 293
    assert status["source_exact_pair_rows_v208"] == 279
    assert status["cvar_feasible_pair_rows_v208"] == 279
    assert status["one_swap_improving_rows_v208"] == 279
    assert status["best_one_swap_return_delta_v208"] == pytest.approx(29.883111271598693)
    assert status["best_one_swap_cvar90_after_v208"] == pytest.approx(97049.98884939894)
    assert status["post_repair_one_swap_local_optimality_cleared_v208"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v208"] is False
    assert status["paper1_promotion_allowed_v208"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v208_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v208"]
    assert probe["return_delta_v208"].gt(0).all()
    assert probe["budget_swap_feasible_v208"].astype(bool).all()
    assert probe["source_swap_feasible_v208"].astype(bool).all()
    assert probe["cvar_swap_feasible_v208"].astype(bool).all()
    assert probe["one_swap_improves_return_v208"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v208"].sum()) == 0
    assert probe["return_delta_v208"].max() == pytest.approx(
        status["best_one_swap_return_delta_v208"]
    )
    assert probe["claim_boundary_v208"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v208_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v208"]) == "131574675"
    assert str(best["dropped_loan_id_v208"]) == "126733849"
    assert float(best["objective_return_after_swap_v208"]) == pytest.approx(2481.2224114465357)
    assert float(best["exposure_after_swap_v208"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v208"]) == pytest.approx(97049.98884939894)
    assert bool(best["one_swap_improves_return_v208"]) is True

    summary = _read_csv("paper4_v208_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v208"]) == status["one_swap_improving_rows_v208"]
    assert float(row["current_objective_return_v208"]) == pytest.approx(2451.3393001749364)
    assert float(row["current_exposure_v208"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v208"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v208"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v208"])

    stage_summary = _read_csv("paper4_v208_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v208"], stage_summary["pair_rows_v208"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v208"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v208"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v208"]

    blockers = _read_csv("paper4_v208_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v208"], blockers["blocking_v208"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v208"], blockers["evidence_count_v208"], strict=False)
    )
    assert bool(blocker_map["post_v207_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v207_one_swap_improvement_found"]) == 279
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v208_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v208_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v208_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v208_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v208_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v208 post-v207 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v208 proves the v207 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v208 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v208: Post-v207 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `279`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v209_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v209_status.json")

    assert status["phase"] == "v209_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.209"
    assert status["allocation_rows_v209"] == 171
    assert status["summary_rows_v209"] == 1
    assert status["action_rows_v209"] == 1
    assert status["source_summary_rows_v209"] == 51
    assert status["claim_blocker_rows_v209"] == 4
    assert status["added_loan_id_v209"] == "131574675"
    assert status["dropped_loan_id_v209"] == "126733849"
    assert status["selected_rows_v209"] == 171
    assert status["portfolio_exposure_v209"] == pytest.approx(842425.0)
    assert status["objective_return_v209"] == pytest.approx(2481.2224114465334)
    assert status["scenario_loss_cvar90_v209"] == pytest.approx(97049.98884939894)
    assert status["source_cap_violations_v209"] == 0
    assert status["delta_return_vs_v207_v209"] == pytest.approx(29.883111271597045)
    assert status["delta_cvar90_vs_v207_v209"] == pytest.approx(-0.4034184209740488)
    assert status["delta_exposure_vs_v207_v209"] == pytest.approx(0.0)
    assert status["budget_feasible_v209"] is True
    assert status["source_feasible_v209"] is True
    assert status["cvar_feasible_v209"] is True
    assert status["repair_candidate_feasible_v209"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v209"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v209"] is False
    assert status["paper1_promotion_allowed_v209"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v209_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v209"]
    assert int(allocations["selected_v209"].sum()) == status["selected_rows_v209"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v209"])
    assert "131574675" in set(allocations["loan_id"].astype(str))
    assert "126733849" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v209"]) == {
        "added_from_v208_best_swap",
        "kept_from_v207",
    }
    assert allocations["claim_boundary_v209"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v209_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v209"]) is True
    assert float(row["delta_cvar90_vs_v207_v209"]) < 0
    assert float(row["scenario_loss_cvar90_v209"]) <= float(row["cvar_cap_v209"])
    assert float(row["max_source_share_v209"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v209"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v209"])

    action = _read_csv("paper4_v209_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v209"]) == status["added_loan_id_v209"]
    assert str(action["dropped_loan_id_v209"]) == status["dropped_loan_id_v209"]
    assert float(action["return_delta_v209"]) == pytest.approx(29.883111271598693)
    assert float(action["exposure_after_repair_v209"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v209"]) == 0

    source_summary = _read_csv("paper4_v209_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v209"]
    assert not source_summary["source_cap_violated_v209"].astype(bool).any()

    blockers = _read_csv("paper4_v209_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v209"], blockers["blocking_v209"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v209_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v209_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v209_repair_candidate_feasible"]) is True
    assert bool(claim_map["v209_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v209_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v209_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v209 sixty-fourth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v209 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v209 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v209: Sixty-Fourth One-Swap Repair Candidate" in notebook
    assert "v210 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v210_post_v209_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v210_status.json")

    assert status["phase"] == "v210_post_v209_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.210"
    assert status["summary_rows_v210"] == 1
    assert status["stage_summary_rows_v210"] == 6
    assert status["candidate_pair_rows_v210"] == 209
    assert status["top_candidate_rows_v210"] == 200
    assert status["claim_blocker_rows_v210"] == 3
    assert status["selected_rows_v210"] == 171
    assert status["candidate_add_rows_v210"] == 276698
    assert status["total_pair_rows_screened_v210"] == 47315358
    assert status["return_improving_pair_rows_v210"] == 1689749
    assert status["budget_return_feasible_pair_rows_v210"] == 1043949
    assert status["source_prefilter_pair_rows_v210"] == 223
    assert status["source_exact_pair_rows_v210"] == 209
    assert status["cvar_feasible_pair_rows_v210"] == 209
    assert status["one_swap_improving_rows_v210"] == 209
    assert status["best_one_swap_return_delta_v210"] == pytest.approx(29.835030943521673)
    assert status["best_one_swap_cvar90_after_v210"] == pytest.approx(97133.88367335375)
    assert status["post_repair_one_swap_local_optimality_cleared_v210"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v210"] is False
    assert status["paper1_promotion_allowed_v210"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v210_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v210"]
    assert probe["return_delta_v210"].gt(0).all()
    assert probe["budget_swap_feasible_v210"].astype(bool).all()
    assert probe["source_swap_feasible_v210"].astype(bool).all()
    assert probe["cvar_swap_feasible_v210"].astype(bool).all()
    assert probe["one_swap_improves_return_v210"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v210"].sum()) == 0
    assert probe["return_delta_v210"].max() == pytest.approx(
        status["best_one_swap_return_delta_v210"]
    )
    assert probe["claim_boundary_v210"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v210_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v210"]) == "158316010"
    assert str(best["dropped_loan_id_v210"]) == "126760355"
    assert float(best["objective_return_after_swap_v210"]) == pytest.approx(2511.057442390055)
    assert float(best["exposure_after_swap_v210"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v210"]) == pytest.approx(97133.88367335376)
    assert bool(best["one_swap_improves_return_v210"]) is True

    summary = _read_csv("paper4_v210_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v210"]) == status["one_swap_improving_rows_v210"]
    assert float(row["current_objective_return_v210"]) == pytest.approx(2481.222411446533)
    assert float(row["current_exposure_v210"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v210"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v210"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v210"])

    stage_summary = _read_csv("paper4_v210_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v210"], stage_summary["pair_rows_v210"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v210"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v210"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v210"]

    blockers = _read_csv("paper4_v210_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v210"], blockers["blocking_v210"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v210"], blockers["evidence_count_v210"], strict=False)
    )
    assert bool(blocker_map["post_v209_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v209_one_swap_improvement_found"]) == 209
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v210_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v210_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v210_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v210_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v210_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v210 post-v209 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v210 proves the v209 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v210 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v210: Post-v209 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `209`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v211_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v211_status.json")

    assert status["phase"] == "v211_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.211"
    assert status["allocation_rows_v211"] == 171
    assert status["summary_rows_v211"] == 1
    assert status["action_rows_v211"] == 1
    assert status["source_summary_rows_v211"] == 51
    assert status["claim_blocker_rows_v211"] == 4
    assert status["added_loan_id_v211"] == "158316010"
    assert status["dropped_loan_id_v211"] == "126760355"
    assert status["selected_rows_v211"] == 171
    assert status["portfolio_exposure_v211"] == pytest.approx(842425.0)
    assert status["objective_return_v211"] == pytest.approx(2511.057442390058)
    assert status["scenario_loss_cvar90_v211"] == pytest.approx(97133.88367335375)
    assert status["source_cap_violations_v211"] == 0
    assert status["delta_return_vs_v209_v211"] == pytest.approx(29.835030943525)
    assert status["delta_cvar90_vs_v209_v211"] == pytest.approx(83.89482395480445)
    assert status["delta_exposure_vs_v209_v211"] == pytest.approx(0.0)
    assert status["budget_feasible_v211"] is True
    assert status["source_feasible_v211"] is True
    assert status["cvar_feasible_v211"] is True
    assert status["repair_candidate_feasible_v211"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v211"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v211"] is False
    assert status["paper1_promotion_allowed_v211"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v211_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v211"]
    assert int(allocations["selected_v211"].sum()) == status["selected_rows_v211"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v211"])
    assert "158316010" in set(allocations["loan_id"].astype(str))
    assert "126760355" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v211"]) == {
        "added_from_v210_best_swap",
        "kept_from_v209",
    }
    assert allocations["claim_boundary_v211"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v211_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v211"]) is True
    assert float(row["delta_cvar90_vs_v209_v211"]) > 0
    assert float(row["scenario_loss_cvar90_v211"]) <= float(row["cvar_cap_v211"])
    assert float(row["max_source_share_v211"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v211"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v211"])

    action = _read_csv("paper4_v211_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v211"]) == status["added_loan_id_v211"]
    assert str(action["dropped_loan_id_v211"]) == status["dropped_loan_id_v211"]
    assert float(action["return_delta_v211"]) == pytest.approx(29.83503094352168)
    assert float(action["exposure_after_repair_v211"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v211"]) == 0

    source_summary = _read_csv("paper4_v211_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v211"]
    assert not source_summary["source_cap_violated_v211"].astype(bool).any()

    blockers = _read_csv("paper4_v211_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v211"], blockers["blocking_v211"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v211_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v211_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v211_repair_candidate_feasible"]) is True
    assert bool(claim_map["v211_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v211_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v211_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v211 sixty-fifth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v211 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v211 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v211: Sixty-Fifth One-Swap Repair Candidate" in notebook
    assert "v212 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v212_post_v211_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v212_status.json")

    assert status["phase"] == "v212_post_v211_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.212"
    assert status["summary_rows_v212"] == 1
    assert status["stage_summary_rows_v212"] == 6
    assert status["candidate_pair_rows_v212"] == 194
    assert status["top_candidate_rows_v212"] == 194
    assert status["claim_blocker_rows_v212"] == 3
    assert status["selected_rows_v212"] == 171
    assert status["candidate_add_rows_v212"] == 276698
    assert status["total_pair_rows_screened_v212"] == 47315358
    assert status["return_improving_pair_rows_v212"] == 1687545
    assert status["budget_return_feasible_pair_rows_v212"] == 1042793
    assert status["source_prefilter_pair_rows_v212"] == 207
    assert status["source_exact_pair_rows_v212"] == 194
    assert status["cvar_feasible_pair_rows_v212"] == 194
    assert status["one_swap_improving_rows_v212"] == 194
    assert status["best_one_swap_return_delta_v212"] == pytest.approx(27.39452777347819)
    assert status["best_one_swap_cvar90_after_v212"] == pytest.approx(97159.84142778009)
    assert status["post_repair_one_swap_local_optimality_cleared_v212"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v212"] is False
    assert status["paper1_promotion_allowed_v212"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v212_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v212"]
    assert probe["return_delta_v212"].gt(0).all()
    assert probe["budget_swap_feasible_v212"].astype(bool).all()
    assert probe["source_swap_feasible_v212"].astype(bool).all()
    assert probe["cvar_swap_feasible_v212"].astype(bool).all()
    assert probe["one_swap_improves_return_v212"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v212"].sum()) == 0
    assert probe["return_delta_v212"].max() == pytest.approx(
        status["best_one_swap_return_delta_v212"]
    )
    assert probe["claim_boundary_v212"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v212_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v212"]) == "158776308"
    assert str(best["dropped_loan_id_v212"]) == "160806330"
    assert float(best["objective_return_after_swap_v212"]) == pytest.approx(2538.4519701635363)
    assert float(best["exposure_after_swap_v212"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v212"]) == pytest.approx(97159.84142778008)
    assert bool(best["one_swap_improves_return_v212"]) is True

    summary = _read_csv("paper4_v212_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v212"]) == status["one_swap_improving_rows_v212"]
    assert float(row["current_objective_return_v212"]) == pytest.approx(2511.057442390058)
    assert float(row["current_exposure_v212"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v212"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v212"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v212"])

    stage_summary = _read_csv("paper4_v212_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v212"], stage_summary["pair_rows_v212"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v212"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v212"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v212"]

    blockers = _read_csv("paper4_v212_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v212"], blockers["blocking_v212"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v212"], blockers["evidence_count_v212"], strict=False)
    )
    assert bool(blocker_map["post_v211_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v211_one_swap_improvement_found"]) == 194
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v212_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v212_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v212_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v212_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v212_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v212 post-v211 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v212 proves the v211 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v212 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v212: Post-v211 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `194`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v213_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v213_status.json")

    assert status["phase"] == "v213_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.213"
    assert status["allocation_rows_v213"] == 171
    assert status["summary_rows_v213"] == 1
    assert status["action_rows_v213"] == 1
    assert status["source_summary_rows_v213"] == 51
    assert status["claim_blocker_rows_v213"] == 4
    assert status["added_loan_id_v213"] == "158776308"
    assert status["dropped_loan_id_v213"] == "160806330"
    assert status["selected_rows_v213"] == 171
    assert status["portfolio_exposure_v213"] == pytest.approx(842425.0)
    assert status["objective_return_v213"] == pytest.approx(2538.4519701635336)
    assert status["scenario_loss_cvar90_v213"] == pytest.approx(97159.84142778009)
    assert status["source_cap_violations_v213"] == 0
    assert status["delta_return_vs_v211_v213"] == pytest.approx(27.394527773475602)
    assert status["delta_cvar90_vs_v211_v213"] == pytest.approx(25.95775442633021)
    assert status["delta_exposure_vs_v211_v213"] == pytest.approx(0.0)
    assert status["budget_feasible_v213"] is True
    assert status["source_feasible_v213"] is True
    assert status["cvar_feasible_v213"] is True
    assert status["repair_candidate_feasible_v213"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v213"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v213"] is False
    assert status["paper1_promotion_allowed_v213"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v213_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v213"]
    assert int(allocations["selected_v213"].sum()) == status["selected_rows_v213"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v213"])
    assert "158776308" in set(allocations["loan_id"].astype(str))
    assert "160806330" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v213"]) == {
        "added_from_v212_best_swap",
        "kept_from_v211",
    }
    assert allocations["claim_boundary_v213"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v213_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v213"]) is True
    assert float(row["delta_cvar90_vs_v211_v213"]) > 0
    assert float(row["scenario_loss_cvar90_v213"]) <= float(row["cvar_cap_v213"])
    assert float(row["max_source_share_v213"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v213"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v213"])

    action = _read_csv("paper4_v213_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v213"]) == status["added_loan_id_v213"]
    assert str(action["dropped_loan_id_v213"]) == status["dropped_loan_id_v213"]
    assert float(action["return_delta_v213"]) == pytest.approx(27.39452777347819)
    assert float(action["exposure_after_repair_v213"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v213"]) == 0

    source_summary = _read_csv("paper4_v213_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v213"]
    assert not source_summary["source_cap_violated_v213"].astype(bool).any()

    blockers = _read_csv("paper4_v213_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v213"], blockers["blocking_v213"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v213_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v213_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v213_repair_candidate_feasible"]) is True
    assert bool(claim_map["v213_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v213_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v213_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v213 sixty-sixth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v213 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v213 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v213: Sixty-Sixth One-Swap Repair Candidate" in notebook
    assert "v214 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v214_post_v213_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v214_status.json")

    assert status["phase"] == "v214_post_v213_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.214"
    assert status["summary_rows_v214"] == 1
    assert status["stage_summary_rows_v214"] == 6
    assert status["candidate_pair_rows_v214"] == 185
    assert status["top_candidate_rows_v214"] == 185
    assert status["claim_blocker_rows_v214"] == 3
    assert status["selected_rows_v214"] == 171
    assert status["candidate_add_rows_v214"] == 276698
    assert status["total_pair_rows_screened_v214"] == 47315358
    assert status["return_improving_pair_rows_v214"] == 1685669
    assert status["budget_return_feasible_pair_rows_v214"] == 1041660
    assert status["source_prefilter_pair_rows_v214"] == 196
    assert status["source_exact_pair_rows_v214"] == 185
    assert status["cvar_feasible_pair_rows_v214"] == 185
    assert status["one_swap_improving_rows_v214"] == 185
    assert status["best_one_swap_return_delta_v214"] == pytest.approx(22.59521891679944)
    assert status["best_one_swap_cvar90_after_v214"] == pytest.approx(97175.17451391011)
    assert status["post_repair_one_swap_local_optimality_cleared_v214"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v214"] is False
    assert status["paper1_promotion_allowed_v214"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v214_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v214"]
    assert probe["return_delta_v214"].gt(0).all()
    assert probe["budget_swap_feasible_v214"].astype(bool).all()
    assert probe["source_swap_feasible_v214"].astype(bool).all()
    assert probe["cvar_swap_feasible_v214"].astype(bool).all()
    assert probe["one_swap_improves_return_v214"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v214"].sum()) == 0
    assert probe["return_delta_v214"].max() == pytest.approx(
        status["best_one_swap_return_delta_v214"]
    )
    assert probe["claim_boundary_v214"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v214_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v214"]) == "141017853"
    assert str(best["dropped_loan_id_v214"]) == "156823754"
    assert float(best["objective_return_after_swap_v214"]) == pytest.approx(2561.047189080333)
    assert float(best["exposure_after_swap_v214"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v214"]) == pytest.approx(97175.17451391013)
    assert bool(best["one_swap_improves_return_v214"]) is True

    summary = _read_csv("paper4_v214_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v214"]) == status["one_swap_improving_rows_v214"]
    assert float(row["current_objective_return_v214"]) == pytest.approx(2538.4519701635336)
    assert float(row["current_exposure_v214"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v214"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v214"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v214"])

    stage_summary = _read_csv("paper4_v214_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v214"], stage_summary["pair_rows_v214"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v214"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v214"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v214"]

    blockers = _read_csv("paper4_v214_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v214"], blockers["blocking_v214"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v214"], blockers["evidence_count_v214"], strict=False)
    )
    assert bool(blocker_map["post_v213_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v213_one_swap_improvement_found"]) == 185
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v214_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v214_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v214_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v214_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v214_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v214 post-v213 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v214 proves the v213 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v214 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v214: Post-v213 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `185`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v215_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v215_status.json")

    assert status["phase"] == "v215_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.215"
    assert status["allocation_rows_v215"] == 171
    assert status["summary_rows_v215"] == 1
    assert status["action_rows_v215"] == 1
    assert status["source_summary_rows_v215"] == 51
    assert status["claim_blocker_rows_v215"] == 4
    assert status["added_loan_id_v215"] == "141017853"
    assert status["dropped_loan_id_v215"] == "156823754"
    assert status["selected_rows_v215"] == 171
    assert status["portfolio_exposure_v215"] == pytest.approx(842425.0)
    assert status["objective_return_v215"] == pytest.approx(2561.047189080333)
    assert status["scenario_loss_cvar90_v215"] == pytest.approx(97175.17451391011)
    assert status["source_cap_violations_v215"] == 0
    assert status["delta_return_vs_v213_v215"] == pytest.approx(22.59521891679924)
    assert status["delta_cvar90_vs_v213_v215"] == pytest.approx(15.333086130034644)
    assert status["delta_exposure_vs_v213_v215"] == pytest.approx(0.0)
    assert status["budget_feasible_v215"] is True
    assert status["source_feasible_v215"] is True
    assert status["cvar_feasible_v215"] is True
    assert status["repair_candidate_feasible_v215"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v215"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v215"] is False
    assert status["paper1_promotion_allowed_v215"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v215_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v215"]
    assert int(allocations["selected_v215"].sum()) == status["selected_rows_v215"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v215"])
    assert "141017853" in set(allocations["loan_id"].astype(str))
    assert "156823754" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v215"]) == {
        "added_from_v214_best_swap",
        "kept_from_v213",
    }
    assert allocations["claim_boundary_v215"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v215_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v215"]) is True
    assert float(row["delta_cvar90_vs_v213_v215"]) > 0
    assert float(row["scenario_loss_cvar90_v215"]) <= float(row["cvar_cap_v215"])
    assert float(row["max_source_share_v215"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v215"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v215"])

    action = _read_csv("paper4_v215_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v215"]) == status["added_loan_id_v215"]
    assert str(action["dropped_loan_id_v215"]) == status["dropped_loan_id_v215"]
    assert float(action["return_delta_v215"]) == pytest.approx(22.59521891679944)
    assert float(action["exposure_after_repair_v215"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v215"]) == 0

    source_summary = _read_csv("paper4_v215_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v215"]
    assert not source_summary["source_cap_violated_v215"].astype(bool).any()

    blockers = _read_csv("paper4_v215_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v215"], blockers["blocking_v215"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v215_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v215_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v215_repair_candidate_feasible"]) is True
    assert bool(claim_map["v215_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v215_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v215_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v215 sixty-seventh one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v215 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v215 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v215: Sixty-Seventh One-Swap Repair Candidate" in notebook
    assert "v216 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v216_post_v215_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v216_status.json")

    assert status["phase"] == "v216_post_v215_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.216"
    assert status["summary_rows_v216"] == 1
    assert status["stage_summary_rows_v216"] == 6
    assert status["candidate_pair_rows_v216"] == 152
    assert status["top_candidate_rows_v216"] == 152
    assert status["claim_blocker_rows_v216"] == 3
    assert status["selected_rows_v216"] == 171
    assert status["candidate_add_rows_v216"] == 276698
    assert status["total_pair_rows_screened_v216"] == 47315358
    assert status["return_improving_pair_rows_v216"] == 1684155
    assert status["budget_return_feasible_pair_rows_v216"] == 1040659
    assert status["source_prefilter_pair_rows_v216"] == 162
    assert status["source_exact_pair_rows_v216"] == 152
    assert status["cvar_feasible_pair_rows_v216"] == 152
    assert status["one_swap_improving_rows_v216"] == 152
    assert status["best_one_swap_return_delta_v216"] == pytest.approx(22.471414615300972)
    assert status["best_one_swap_cvar90_after_v216"] == pytest.approx(97265.71570645458)
    assert status["post_repair_one_swap_local_optimality_cleared_v216"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v216"] is False
    assert status["paper1_promotion_allowed_v216"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v216_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v216"]
    assert probe["return_delta_v216"].gt(0).all()
    assert probe["budget_swap_feasible_v216"].astype(bool).all()
    assert probe["source_swap_feasible_v216"].astype(bool).all()
    assert probe["cvar_swap_feasible_v216"].astype(bool).all()
    assert probe["one_swap_improves_return_v216"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v216"].sum()) == 0
    assert probe["return_delta_v216"].max() == pytest.approx(
        status["best_one_swap_return_delta_v216"]
    )
    assert probe["claim_boundary_v216"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v216_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v216"]) == "141184113"
    assert str(best["dropped_loan_id_v216"]) == "127281432"
    assert float(best["objective_return_after_swap_v216"]) == pytest.approx(2583.518603695634)
    assert float(best["exposure_after_swap_v216"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v216"]) == pytest.approx(97265.71570645458)
    assert bool(best["one_swap_improves_return_v216"]) is True

    summary = _read_csv("paper4_v216_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v216"]) == status["one_swap_improving_rows_v216"]
    assert float(row["current_objective_return_v216"]) == pytest.approx(2561.047189080333)
    assert float(row["current_exposure_v216"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v216"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v216"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v216"])

    stage_summary = _read_csv("paper4_v216_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v216"], stage_summary["pair_rows_v216"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v216"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v216"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v216"]

    blockers = _read_csv("paper4_v216_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v216"], blockers["blocking_v216"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v216"], blockers["evidence_count_v216"], strict=False)
    )
    assert bool(blocker_map["post_v215_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v215_one_swap_improvement_found"]) == 152
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v216_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v216_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v216_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v216_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v216_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v216 post-v215 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v216 proves the v215 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v216 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v216: Post-v215 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `152`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v217_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v217_status.json")

    assert status["phase"] == "v217_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.217"
    assert status["allocation_rows_v217"] == 171
    assert status["summary_rows_v217"] == 1
    assert status["action_rows_v217"] == 1
    assert status["source_summary_rows_v217"] == 51
    assert status["claim_blocker_rows_v217"] == 4
    assert status["added_loan_id_v217"] == "141184113"
    assert status["dropped_loan_id_v217"] == "127281432"
    assert status["selected_rows_v217"] == 171
    assert status["portfolio_exposure_v217"] == pytest.approx(842425.0)
    assert status["objective_return_v217"] == pytest.approx(2583.5186036956366)
    assert status["scenario_loss_cvar90_v217"] == pytest.approx(97265.71570645458)
    assert status["source_cap_violations_v217"] == 0
    assert status["delta_return_vs_v215_v217"] == pytest.approx(22.47141461530373)
    assert status["delta_cvar90_vs_v215_v217"] == pytest.approx(90.54119254444959)
    assert status["delta_exposure_vs_v215_v217"] == pytest.approx(0.0)
    assert status["budget_feasible_v217"] is True
    assert status["source_feasible_v217"] is True
    assert status["cvar_feasible_v217"] is True
    assert status["repair_candidate_feasible_v217"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v217"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v217"] is False
    assert status["paper1_promotion_allowed_v217"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v217_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v217"]
    assert int(allocations["selected_v217"].sum()) == status["selected_rows_v217"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v217"])
    assert "141184113" in set(allocations["loan_id"].astype(str))
    assert "127281432" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v217"]) == {
        "added_from_v216_best_swap",
        "kept_from_v215",
    }
    assert allocations["claim_boundary_v217"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v217_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v217"]) is True
    assert float(row["delta_cvar90_vs_v215_v217"]) > 0
    assert float(row["scenario_loss_cvar90_v217"]) <= float(row["cvar_cap_v217"])
    assert float(row["max_source_share_v217"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v217"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v217"])

    action = _read_csv("paper4_v217_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v217"]) == status["added_loan_id_v217"]
    assert str(action["dropped_loan_id_v217"]) == status["dropped_loan_id_v217"]
    assert float(action["return_delta_v217"]) == pytest.approx(22.47141461530097)
    assert float(action["exposure_after_repair_v217"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v217"]) == 0

    source_summary = _read_csv("paper4_v217_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v217"]
    assert not source_summary["source_cap_violated_v217"].astype(bool).any()

    blockers = _read_csv("paper4_v217_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v217"], blockers["blocking_v217"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v217_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v217_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v217_repair_candidate_feasible"]) is True
    assert bool(claim_map["v217_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v217_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v217_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v217 sixty-eighth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v217 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v217 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v217: Sixty-Eighth One-Swap Repair Candidate" in notebook
    assert "v218 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v218_post_v217_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v218_status.json")

    assert status["phase"] == "v218_post_v217_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.218"
    assert status["summary_rows_v218"] == 1
    assert status["stage_summary_rows_v218"] == 6
    assert status["candidate_pair_rows_v218"] == 151
    assert status["top_candidate_rows_v218"] == 151
    assert status["claim_blocker_rows_v218"] == 3
    assert status["selected_rows_v218"] == 171
    assert status["candidate_add_rows_v218"] == 276698
    assert status["total_pair_rows_screened_v218"] == 47315358
    assert status["return_improving_pair_rows_v218"] == 1682320
    assert status["budget_return_feasible_pair_rows_v218"] == 1039801
    assert status["source_prefilter_pair_rows_v218"] == 161
    assert status["source_exact_pair_rows_v218"] == 151
    assert status["cvar_feasible_pair_rows_v218"] == 151
    assert status["one_swap_improving_rows_v218"] == 151
    assert status["best_one_swap_return_delta_v218"] == pytest.approx(19.98801524717925)
    assert status["best_one_swap_cvar90_after_v218"] == pytest.approx(97415.83243297288)
    assert status["post_repair_one_swap_local_optimality_cleared_v218"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v218"] is False
    assert status["paper1_promotion_allowed_v218"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v218_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v218"]
    assert probe["return_delta_v218"].gt(0).all()
    assert probe["budget_swap_feasible_v218"].astype(bool).all()
    assert probe["source_swap_feasible_v218"].astype(bool).all()
    assert probe["cvar_swap_feasible_v218"].astype(bool).all()
    assert probe["one_swap_improves_return_v218"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v218"].sum()) == 0
    assert probe["return_delta_v218"].max() == pytest.approx(
        status["best_one_swap_return_delta_v218"]
    )
    assert probe["claim_boundary_v218"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v218_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v218"]) == "162528420"
    assert str(best["dropped_loan_id_v218"]) == "165985906"
    assert float(best["objective_return_after_swap_v218"]) == pytest.approx(2603.506618942816)
    assert float(best["exposure_after_swap_v218"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v218"]) == pytest.approx(97415.83243297288)
    assert bool(best["one_swap_improves_return_v218"]) is True

    summary = _read_csv("paper4_v218_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v218"]) == status["one_swap_improving_rows_v218"]
    assert float(row["current_objective_return_v218"]) == pytest.approx(2583.518603695637)
    assert float(row["current_exposure_v218"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v218"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v218"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v218"])

    stage_summary = _read_csv("paper4_v218_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v218"], stage_summary["pair_rows_v218"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v218"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v218"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v218"]

    blockers = _read_csv("paper4_v218_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v218"], blockers["blocking_v218"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v218"], blockers["evidence_count_v218"], strict=False)
    )
    assert bool(blocker_map["post_v217_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v217_one_swap_improvement_found"]) == 151
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v218_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v218_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v218_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v218_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v218_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v218 post-v217 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v218 proves the v217 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v218 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v218: Post-v217 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `151`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v219_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v219_status.json")

    assert status["phase"] == "v219_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.219"
    assert status["allocation_rows_v219"] == 171
    assert status["summary_rows_v219"] == 1
    assert status["action_rows_v219"] == 1
    assert status["source_summary_rows_v219"] == 51
    assert status["claim_blocker_rows_v219"] == 4
    assert status["added_loan_id_v219"] == "162528420"
    assert status["dropped_loan_id_v219"] == "165985906"
    assert status["selected_rows_v219"] == 171
    assert status["portfolio_exposure_v219"] == pytest.approx(842425.0)
    assert status["objective_return_v219"] == pytest.approx(2603.5066189428144)
    assert status["scenario_loss_cvar90_v219"] == pytest.approx(97415.8324329729)
    assert status["source_cap_violations_v219"] == 0
    assert status["delta_return_vs_v217_v219"] == pytest.approx(19.988015247177373)
    assert status["delta_cvar90_vs_v217_v219"] == pytest.approx(150.1167265183176)
    assert status["delta_exposure_vs_v217_v219"] == pytest.approx(0.0)
    assert status["budget_feasible_v219"] is True
    assert status["source_feasible_v219"] is True
    assert status["cvar_feasible_v219"] is True
    assert status["repair_candidate_feasible_v219"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v219"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v219"] is False
    assert status["paper1_promotion_allowed_v219"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v219_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v219"]
    assert int(allocations["selected_v219"].sum()) == status["selected_rows_v219"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v219"])
    assert "162528420" in set(allocations["loan_id"].astype(str))
    assert "165985906" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v219"]) == {
        "added_from_v218_best_swap",
        "kept_from_v217",
    }
    assert allocations["claim_boundary_v219"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v219_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v219"]) is True
    assert float(row["delta_cvar90_vs_v217_v219"]) > 0
    assert float(row["scenario_loss_cvar90_v219"]) <= float(row["cvar_cap_v219"])
    assert float(row["max_source_share_v219"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v219"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v219"])

    action = _read_csv("paper4_v219_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v219"]) == status["added_loan_id_v219"]
    assert str(action["dropped_loan_id_v219"]) == status["dropped_loan_id_v219"]
    assert float(action["return_delta_v219"]) == pytest.approx(19.98801524717925)
    assert float(action["exposure_after_repair_v219"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v219"]) == 0

    source_summary = _read_csv("paper4_v219_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v219"]
    assert not source_summary["source_cap_violated_v219"].astype(bool).any()

    blockers = _read_csv("paper4_v219_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v219"], blockers["blocking_v219"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v219_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v219_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v219_repair_candidate_feasible"]) is True
    assert bool(claim_map["v219_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v219_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v219_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v219 sixty-ninth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v219 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v219 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v219: Sixty-Ninth One-Swap Repair Candidate" in notebook
    assert "v220 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v220_post_v219_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v220_status.json")

    assert status["phase"] == "v220_post_v219_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.220"
    assert status["summary_rows_v220"] == 1
    assert status["stage_summary_rows_v220"] == 6
    assert status["candidate_pair_rows_v220"] == 147
    assert status["top_candidate_rows_v220"] == 147
    assert status["claim_blocker_rows_v220"] == 3
    assert status["selected_rows_v220"] == 171
    assert status["candidate_add_rows_v220"] == 276698
    assert status["total_pair_rows_screened_v220"] == 47315358
    assert status["return_improving_pair_rows_v220"] == 1680953
    assert status["budget_return_feasible_pair_rows_v220"] == 1038959
    assert status["source_prefilter_pair_rows_v220"] == 155
    assert status["source_exact_pair_rows_v220"] == 147
    assert status["cvar_feasible_pair_rows_v220"] == 147
    assert status["one_swap_improving_rows_v220"] == 147
    assert status["best_one_swap_return_delta_v220"] == pytest.approx(19.2900500175584)
    assert status["best_one_swap_cvar90_after_v220"] == pytest.approx(97487.03061156688)
    assert status["post_repair_one_swap_local_optimality_cleared_v220"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v220"] is False
    assert status["paper1_promotion_allowed_v220"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v220_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v220"]
    assert probe["return_delta_v220"].gt(0).all()
    assert probe["budget_swap_feasible_v220"].astype(bool).all()
    assert probe["source_swap_feasible_v220"].astype(bool).all()
    assert probe["cvar_swap_feasible_v220"].astype(bool).all()
    assert probe["one_swap_improves_return_v220"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v220"].sum()) == 0
    assert probe["return_delta_v220"].max() == pytest.approx(
        status["best_one_swap_return_delta_v220"]
    )
    assert probe["claim_boundary_v220"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v220_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v220"]) == "143278940"
    assert str(best["dropped_loan_id_v220"]) == "126430923"
    assert float(best["objective_return_after_swap_v220"]) == pytest.approx(2622.7966689603727)
    assert float(best["exposure_after_swap_v220"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v220"]) == pytest.approx(97487.03061156688)
    assert bool(best["one_swap_improves_return_v220"]) is True

    summary = _read_csv("paper4_v220_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v220"]) == status["one_swap_improving_rows_v220"]
    assert float(row["current_objective_return_v220"]) == pytest.approx(2603.5066189428144)
    assert float(row["current_exposure_v220"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v220"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v220"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v220"])

    stage_summary = _read_csv("paper4_v220_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v220"], stage_summary["pair_rows_v220"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v220"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v220"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v220"]

    blockers = _read_csv("paper4_v220_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v220"], blockers["blocking_v220"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v220"], blockers["evidence_count_v220"], strict=False)
    )
    assert bool(blocker_map["post_v219_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v219_one_swap_improvement_found"]) == 147
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v220_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v220_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v220_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v220_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v220_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v220 post-v219 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v220 proves the v219 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v220 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v220: Post-v219 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `147`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v221_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v221_status.json")

    assert status["phase"] == "v221_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.221"
    assert status["allocation_rows_v221"] == 171
    assert status["summary_rows_v221"] == 1
    assert status["action_rows_v221"] == 1
    assert status["source_summary_rows_v221"] == 51
    assert status["claim_blocker_rows_v221"] == 4
    assert status["added_loan_id_v221"] == "143278940"
    assert status["dropped_loan_id_v221"] == "126430923"
    assert status["selected_rows_v221"] == 171
    assert status["portfolio_exposure_v221"] == pytest.approx(842425.0)
    assert status["objective_return_v221"] == pytest.approx(2622.7966689603727)
    assert status["scenario_loss_cvar90_v221"] == pytest.approx(97487.03061156688)
    assert status["source_cap_violations_v221"] == 0
    assert status["delta_return_vs_v219_v221"] == pytest.approx(19.290050017558315)
    assert status["delta_cvar90_vs_v219_v221"] == pytest.approx(71.19817859398609)
    assert status["delta_exposure_vs_v219_v221"] == pytest.approx(0.0)
    assert status["budget_feasible_v221"] is True
    assert status["source_feasible_v221"] is True
    assert status["cvar_feasible_v221"] is True
    assert status["repair_candidate_feasible_v221"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v221"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v221"] is False
    assert status["paper1_promotion_allowed_v221"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v221_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v221"]
    assert int(allocations["selected_v221"].sum()) == status["selected_rows_v221"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v221"])
    assert "143278940" in set(allocations["loan_id"].astype(str))
    assert "126430923" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v221"]) == {
        "added_from_v220_best_swap",
        "kept_from_v219",
    }
    assert allocations["claim_boundary_v221"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v221_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v221"]) is True
    assert float(row["delta_cvar90_vs_v219_v221"]) > 0
    assert float(row["scenario_loss_cvar90_v221"]) <= float(row["cvar_cap_v221"])
    assert float(row["max_source_share_v221"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v221"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v221"])

    action = _read_csv("paper4_v221_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v221"]) == status["added_loan_id_v221"]
    assert str(action["dropped_loan_id_v221"]) == status["dropped_loan_id_v221"]
    assert float(action["return_delta_v221"]) == pytest.approx(19.2900500175584)
    assert float(action["exposure_after_repair_v221"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v221"]) == 0

    source_summary = _read_csv("paper4_v221_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v221"]
    assert not source_summary["source_cap_violated_v221"].astype(bool).any()

    blockers = _read_csv("paper4_v221_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v221"], blockers["blocking_v221"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v221_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v221_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v221_repair_candidate_feasible"]) is True
    assert bool(claim_map["v221_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v221_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v221_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v221 seventieth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v221 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v221 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v221: Seventieth One-Swap Repair Candidate" in notebook
    assert "v222 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v222_post_v221_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v222_status.json")

    assert status["phase"] == "v222_post_v221_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.222"
    assert status["summary_rows_v222"] == 1
    assert status["stage_summary_rows_v222"] == 6
    assert status["candidate_pair_rows_v222"] == 138
    assert status["top_candidate_rows_v222"] == 138
    assert status["claim_blocker_rows_v222"] == 3
    assert status["selected_rows_v222"] == 171
    assert status["candidate_add_rows_v222"] == 276698
    assert status["total_pair_rows_screened_v222"] == 47315358
    assert status["return_improving_pair_rows_v222"] == 1679577
    assert status["budget_return_feasible_pair_rows_v222"] == 1038223
    assert status["source_prefilter_pair_rows_v222"] == 145
    assert status["source_exact_pair_rows_v222"] == 138
    assert status["cvar_feasible_pair_rows_v222"] == 138
    assert status["one_swap_improving_rows_v222"] == 138
    assert status["best_one_swap_return_delta_v222"] == pytest.approx(18.638613690714152)
    assert status["best_one_swap_cvar90_after_v222"] == pytest.approx(97472.43193372485)
    assert status["post_repair_one_swap_local_optimality_cleared_v222"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v222"] is False
    assert status["paper1_promotion_allowed_v222"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v222_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v222"]
    assert probe["return_delta_v222"].gt(0).all()
    assert probe["budget_swap_feasible_v222"].astype(bool).all()
    assert probe["source_swap_feasible_v222"].astype(bool).all()
    assert probe["cvar_swap_feasible_v222"].astype(bool).all()
    assert probe["one_swap_improves_return_v222"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v222"].sum()) == 0
    assert probe["return_delta_v222"].max() == pytest.approx(
        status["best_one_swap_return_delta_v222"]
    )
    assert probe["claim_boundary_v222"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v222_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v222"]) == "162590887"
    assert str(best["dropped_loan_id_v222"]) == "168678809"
    assert float(best["objective_return_after_swap_v222"]) == pytest.approx(2641.4352826510867)
    assert float(best["exposure_after_swap_v222"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v222"]) == pytest.approx(97472.43193372485)
    assert bool(best["one_swap_improves_return_v222"]) is True

    summary = _read_csv("paper4_v222_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v222"]) == status["one_swap_improving_rows_v222"]
    assert float(row["current_objective_return_v222"]) == pytest.approx(2622.7966689603727)
    assert float(row["current_exposure_v222"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v222"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v222"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v222"])

    stage_summary = _read_csv("paper4_v222_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v222"], stage_summary["pair_rows_v222"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v222"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v222"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v222"]

    blockers = _read_csv("paper4_v222_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v222"], blockers["blocking_v222"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v222"], blockers["evidence_count_v222"], strict=False)
    )
    assert bool(blocker_map["post_v221_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v221_one_swap_improvement_found"]) == 138
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v222_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v222_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v222_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v222_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v222_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v222 post-v221 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v222 proves the v221 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v222 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v222: Post-v221 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `138`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v223_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v223_status.json")

    assert status["phase"] == "v223_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.223"
    assert status["allocation_rows_v223"] == 171
    assert status["summary_rows_v223"] == 1
    assert status["action_rows_v223"] == 1
    assert status["source_summary_rows_v223"] == 51
    assert status["claim_blocker_rows_v223"] == 4
    assert status["added_loan_id_v223"] == "162590887"
    assert status["dropped_loan_id_v223"] == "168678809"
    assert status["selected_rows_v223"] == 171
    assert status["portfolio_exposure_v223"] == pytest.approx(842425.0)
    assert status["objective_return_v223"] == pytest.approx(2641.4352826510876)
    assert status["scenario_loss_cvar90_v223"] == pytest.approx(97472.43193372485)
    assert status["source_cap_violations_v223"] == 0
    assert status["delta_return_vs_v221_v223"] == pytest.approx(18.63861369071492)
    assert status["delta_cvar90_vs_v221_v223"] == pytest.approx(-14.598677842033794)
    assert status["delta_exposure_vs_v221_v223"] == pytest.approx(0.0)
    assert status["budget_feasible_v223"] is True
    assert status["source_feasible_v223"] is True
    assert status["cvar_feasible_v223"] is True
    assert status["repair_candidate_feasible_v223"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v223"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v223"] is False
    assert status["paper1_promotion_allowed_v223"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v223_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v223"]
    assert int(allocations["selected_v223"].sum()) == status["selected_rows_v223"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v223"])
    assert "162590887" in set(allocations["loan_id"].astype(str))
    assert "168678809" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v223"]) == {
        "added_from_v222_best_swap",
        "kept_from_v221",
    }
    assert allocations["claim_boundary_v223"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v223_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v223"]) is True
    assert float(row["delta_cvar90_vs_v221_v223"]) < 0
    assert float(row["scenario_loss_cvar90_v223"]) <= float(row["cvar_cap_v223"])
    assert float(row["max_source_share_v223"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v223"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v223"])

    action = _read_csv("paper4_v223_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v223"]) == status["added_loan_id_v223"]
    assert str(action["dropped_loan_id_v223"]) == status["dropped_loan_id_v223"]
    assert float(action["return_delta_v223"]) == pytest.approx(18.638613690714152)
    assert float(action["exposure_after_repair_v223"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v223"]) == 0

    source_summary = _read_csv("paper4_v223_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v223"]
    assert not source_summary["source_cap_violated_v223"].astype(bool).any()

    blockers = _read_csv("paper4_v223_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v223"], blockers["blocking_v223"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v223_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v223_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v223_repair_candidate_feasible"]) is True
    assert bool(claim_map["v223_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v223_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v223_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v223 seventy-first one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v223 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v223 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v223: Seventy-First One-Swap Repair Candidate" in notebook
    assert "v224 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v224_post_v223_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v224_status.json")

    assert status["phase"] == "v224_post_v223_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.224"
    assert status["summary_rows_v224"] == 1
    assert status["stage_summary_rows_v224"] == 6
    assert status["candidate_pair_rows_v224"] == 124
    assert status["top_candidate_rows_v224"] == 124
    assert status["claim_blocker_rows_v224"] == 3
    assert status["selected_rows_v224"] == 171
    assert status["candidate_add_rows_v224"] == 276698
    assert status["total_pair_rows_screened_v224"] == 47315358
    assert status["return_improving_pair_rows_v224"] == 1678150
    assert status["budget_return_feasible_pair_rows_v224"] == 1037227
    assert status["source_prefilter_pair_rows_v224"] == 131
    assert status["source_exact_pair_rows_v224"] == 124
    assert status["cvar_feasible_pair_rows_v224"] == 124
    assert status["one_swap_improving_rows_v224"] == 124
    assert status["best_one_swap_return_delta_v224"] == pytest.approx(16.610320726818998)
    assert status["best_one_swap_cvar90_after_v224"] == pytest.approx(97563.38874971557)
    assert status["post_repair_one_swap_local_optimality_cleared_v224"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v224"] is False
    assert status["paper1_promotion_allowed_v224"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v224_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v224"]
    assert probe["return_delta_v224"].gt(0).all()
    assert probe["budget_swap_feasible_v224"].astype(bool).all()
    assert probe["source_swap_feasible_v224"].astype(bool).all()
    assert probe["cvar_swap_feasible_v224"].astype(bool).all()
    assert probe["one_swap_improves_return_v224"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v224"].sum()) == 0
    assert probe["return_delta_v224"].max() == pytest.approx(
        status["best_one_swap_return_delta_v224"]
    )
    assert probe["claim_boundary_v224"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v224_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v224"]) == "140620222"
    assert str(best["dropped_loan_id_v224"]) == "168701788"
    assert float(best["objective_return_after_swap_v224"]) == pytest.approx(2658.045603377907)
    assert float(best["exposure_after_swap_v224"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v224"]) == pytest.approx(97563.38874971557)
    assert bool(best["one_swap_improves_return_v224"]) is True

    summary = _read_csv("paper4_v224_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v224"]) == status["one_swap_improving_rows_v224"]
    assert float(row["current_objective_return_v224"]) == pytest.approx(2641.435282651088)
    assert float(row["current_exposure_v224"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v224"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v224"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v224"])

    stage_summary = _read_csv("paper4_v224_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v224"], stage_summary["pair_rows_v224"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v224"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v224"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v224"]

    blockers = _read_csv("paper4_v224_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v224"], blockers["blocking_v224"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v224"], blockers["evidence_count_v224"], strict=False)
    )
    assert bool(blocker_map["post_v223_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v223_one_swap_improvement_found"]) == 124
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v224_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v224_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v224_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v224_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v224_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v224 post-v223 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v224 proves the v223 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v224 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v224: Post-v223 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `124`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v225_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v225_status.json")

    assert status["phase"] == "v225_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.225"
    assert status["allocation_rows_v225"] == 171
    assert status["summary_rows_v225"] == 1
    assert status["action_rows_v225"] == 1
    assert status["source_summary_rows_v225"] == 51
    assert status["claim_blocker_rows_v225"] == 4
    assert status["added_loan_id_v225"] == "140620222"
    assert status["dropped_loan_id_v225"] == "168701788"
    assert status["selected_rows_v225"] == 171
    assert status["portfolio_exposure_v225"] == pytest.approx(842425.0)
    assert status["objective_return_v225"] == pytest.approx(2658.0456033779064)
    assert status["scenario_loss_cvar90_v225"] == pytest.approx(97563.38874971557)
    assert status["source_cap_violations_v225"] == 0
    assert status["delta_return_vs_v223_v225"] == pytest.approx(16.610320726818372)
    assert status["delta_cvar90_vs_v223_v225"] == pytest.approx(90.95681599072122)
    assert status["delta_exposure_vs_v223_v225"] == pytest.approx(0.0)
    assert status["budget_feasible_v225"] is True
    assert status["source_feasible_v225"] is True
    assert status["cvar_feasible_v225"] is True
    assert status["repair_candidate_feasible_v225"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v225"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v225"] is False
    assert status["paper1_promotion_allowed_v225"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v225_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v225"]
    assert int(allocations["selected_v225"].sum()) == status["selected_rows_v225"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v225"])
    assert "140620222" in set(allocations["loan_id"].astype(str))
    assert "168701788" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v225"]) == {
        "added_from_v224_best_swap",
        "kept_from_v223",
    }
    assert allocations["claim_boundary_v225"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v225_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v225"]) is True
    assert float(row["delta_cvar90_vs_v223_v225"]) > 0
    assert float(row["scenario_loss_cvar90_v225"]) <= float(row["cvar_cap_v225"])
    assert float(row["max_source_share_v225"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v225"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v225"])

    action = _read_csv("paper4_v225_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v225"]) == status["added_loan_id_v225"]
    assert str(action["dropped_loan_id_v225"]) == status["dropped_loan_id_v225"]
    assert float(action["return_delta_v225"]) == pytest.approx(16.610320726818998)
    assert float(action["exposure_after_repair_v225"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v225"]) == 0

    source_summary = _read_csv("paper4_v225_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v225"]
    assert not source_summary["source_cap_violated_v225"].astype(bool).any()

    blockers = _read_csv("paper4_v225_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v225"], blockers["blocking_v225"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v225_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v225_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v225_repair_candidate_feasible"]) is True
    assert bool(claim_map["v225_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v225_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v225_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v225 seventy-second one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v225 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v225 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v225: Seventy-Second One-Swap Repair Candidate" in notebook
    assert "v226 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v226_post_v225_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v226_status.json")

    assert status["phase"] == "v226_post_v225_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.226"
    assert status["summary_rows_v226"] == 1
    assert status["stage_summary_rows_v226"] == 6
    assert status["candidate_pair_rows_v226"] == 104
    assert status["top_candidate_rows_v226"] == 104
    assert status["claim_blocker_rows_v226"] == 3
    assert status["selected_rows_v226"] == 171
    assert status["candidate_add_rows_v226"] == 276698
    assert status["total_pair_rows_screened_v226"] == 47315358
    assert status["return_improving_pair_rows_v226"] == 1677008
    assert status["budget_return_feasible_pair_rows_v226"] == 1036470
    assert status["source_prefilter_pair_rows_v226"] == 110
    assert status["source_exact_pair_rows_v226"] == 104
    assert status["cvar_feasible_pair_rows_v226"] == 104
    assert status["one_swap_improving_rows_v226"] == 104
    assert status["best_one_swap_return_delta_v226"] == pytest.approx(15.270031838802083)
    assert status["best_one_swap_cvar90_after_v226"] == pytest.approx(97765.57828246354)
    assert status["post_repair_one_swap_local_optimality_cleared_v226"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v226"] is False
    assert status["paper1_promotion_allowed_v226"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v226_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v226"]
    assert probe["return_delta_v226"].gt(0).all()
    assert probe["budget_swap_feasible_v226"].astype(bool).all()
    assert probe["source_swap_feasible_v226"].astype(bool).all()
    assert probe["cvar_swap_feasible_v226"].astype(bool).all()
    assert probe["one_swap_improves_return_v226"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v226"].sum()) == 0
    assert probe["return_delta_v226"].max() == pytest.approx(
        status["best_one_swap_return_delta_v226"]
    )
    assert probe["claim_boundary_v226"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v226_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v226"]) == "146058451"
    assert str(best["dropped_loan_id_v226"]) == "169358871"
    assert float(best["objective_return_after_swap_v226"]) == pytest.approx(2673.3156352167084)
    assert float(best["exposure_after_swap_v226"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v226"]) == pytest.approx(97765.57828246354)
    assert bool(best["one_swap_improves_return_v226"]) is True

    summary = _read_csv("paper4_v226_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v226"]) == status["one_swap_improving_rows_v226"]
    assert float(row["current_objective_return_v226"]) == pytest.approx(2658.0456033779064)
    assert float(row["current_exposure_v226"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v226"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v226"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v226"])

    stage_summary = _read_csv("paper4_v226_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v226"], stage_summary["pair_rows_v226"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v226"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v226"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v226"]

    blockers = _read_csv("paper4_v226_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v226"], blockers["blocking_v226"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v226"], blockers["evidence_count_v226"], strict=False)
    )
    assert bool(blocker_map["post_v225_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v225_one_swap_improvement_found"]) == 104
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v226_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v226_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v226_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v226_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v226_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v226 post-v225 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v226 proves the v225 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v226 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v226: Post-v225 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `104`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v227_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v227_status.json")

    assert status["phase"] == "v227_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.227"
    assert status["allocation_rows_v227"] == 171
    assert status["summary_rows_v227"] == 1
    assert status["action_rows_v227"] == 1
    assert status["source_summary_rows_v227"] == 51
    assert status["claim_blocker_rows_v227"] == 4
    assert status["added_loan_id_v227"] == "146058451"
    assert status["dropped_loan_id_v227"] == "169358871"
    assert status["selected_rows_v227"] == 171
    assert status["portfolio_exposure_v227"] == pytest.approx(842425.0)
    assert status["objective_return_v227"] == pytest.approx(2673.3156352167043)
    assert status["scenario_loss_cvar90_v227"] == pytest.approx(97765.57828246354)
    assert status["source_cap_violations_v227"] == 0
    assert status["delta_return_vs_v225_v227"] == pytest.approx(15.270031838797877)
    assert status["delta_cvar90_vs_v225_v227"] == pytest.approx(202.1895327479724)
    assert status["delta_exposure_vs_v225_v227"] == pytest.approx(0.0)
    assert status["budget_feasible_v227"] is True
    assert status["source_feasible_v227"] is True
    assert status["cvar_feasible_v227"] is True
    assert status["repair_candidate_feasible_v227"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v227"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v227"] is False
    assert status["paper1_promotion_allowed_v227"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v227_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v227"]
    assert int(allocations["selected_v227"].sum()) == status["selected_rows_v227"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v227"])
    assert "146058451" in set(allocations["loan_id"].astype(str))
    assert "169358871" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v227"]) == {
        "added_from_v226_best_swap",
        "kept_from_v225",
    }
    assert allocations["claim_boundary_v227"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v227_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v227"]) is True
    assert float(row["delta_cvar90_vs_v225_v227"]) > 0
    assert float(row["scenario_loss_cvar90_v227"]) <= float(row["cvar_cap_v227"])
    assert float(row["max_source_share_v227"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v227"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v227"])

    action = _read_csv("paper4_v227_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v227"]) == status["added_loan_id_v227"]
    assert str(action["dropped_loan_id_v227"]) == status["dropped_loan_id_v227"]
    assert float(action["return_delta_v227"]) == pytest.approx(15.270031838802083)
    assert float(action["exposure_after_repair_v227"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v227"]) == 0

    source_summary = _read_csv("paper4_v227_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v227"]
    assert not source_summary["source_cap_violated_v227"].astype(bool).any()

    blockers = _read_csv("paper4_v227_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v227"], blockers["blocking_v227"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v227_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v227_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v227_repair_candidate_feasible"]) is True
    assert bool(claim_map["v227_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v227_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v227_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v227 seventy-third one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v227 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v227 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v227: Seventy-Third One-Swap Repair Candidate" in notebook
    assert "v228 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v228_post_v227_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v228_status.json")

    assert status["phase"] == "v228_post_v227_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.228"
    assert status["summary_rows_v228"] == 1
    assert status["stage_summary_rows_v228"] == 6
    assert status["candidate_pair_rows_v228"] == 86
    assert status["top_candidate_rows_v228"] == 86
    assert status["claim_blocker_rows_v228"] == 3
    assert status["selected_rows_v228"] == 171
    assert status["candidate_add_rows_v228"] == 276698
    assert status["total_pair_rows_screened_v228"] == 47315358
    assert status["return_improving_pair_rows_v228"] == 1675941
    assert status["budget_return_feasible_pair_rows_v228"] == 1035761
    assert status["source_prefilter_pair_rows_v228"] == 91
    assert status["source_exact_pair_rows_v228"] == 86
    assert status["cvar_feasible_pair_rows_v228"] == 86
    assert status["one_swap_improving_rows_v228"] == 86
    assert status["best_one_swap_return_delta_v228"] == pytest.approx(14.17516298781652)
    assert status["best_one_swap_cvar90_after_v228"] == pytest.approx(97748.72687062543)
    assert status["post_repair_one_swap_local_optimality_cleared_v228"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v228"] is False
    assert status["paper1_promotion_allowed_v228"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v228_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v228"]
    assert probe["return_delta_v228"].gt(0).all()
    assert probe["budget_swap_feasible_v228"].astype(bool).all()
    assert probe["source_swap_feasible_v228"].astype(bool).all()
    assert probe["cvar_swap_feasible_v228"].astype(bool).all()
    assert probe["one_swap_improves_return_v228"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v228"].sum()) == 0
    assert probe["return_delta_v228"].max() == pytest.approx(
        status["best_one_swap_return_delta_v228"]
    )
    assert probe["claim_boundary_v228"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v228_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v228"]) == "163383289"
    assert str(best["dropped_loan_id_v228"]) == "170390171"
    assert float(best["objective_return_after_swap_v228"]) == pytest.approx(2687.4907982045206)
    assert float(best["exposure_after_swap_v228"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v228"]) == pytest.approx(97748.72687062543)
    assert bool(best["one_swap_improves_return_v228"]) is True

    summary = _read_csv("paper4_v228_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v228"]) == status["one_swap_improving_rows_v228"]
    assert float(row["current_objective_return_v228"]) == pytest.approx(2673.3156352167043)
    assert float(row["current_exposure_v228"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v228"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v228"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v228"])

    stage_summary = _read_csv("paper4_v228_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v228"], stage_summary["pair_rows_v228"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v228"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v228"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v228"]

    blockers = _read_csv("paper4_v228_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v228"], blockers["blocking_v228"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v228"], blockers["evidence_count_v228"], strict=False)
    )
    assert bool(blocker_map["post_v227_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v227_one_swap_improvement_found"]) == 86
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v228_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v228_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v228_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v228_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v228_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v228 post-v227 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v228 proves the v227 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v228 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v228: Post-v227 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `86`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v229_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v229_status.json")

    assert status["phase"] == "v229_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.229"
    assert status["allocation_rows_v229"] == 171
    assert status["summary_rows_v229"] == 1
    assert status["action_rows_v229"] == 1
    assert status["source_summary_rows_v229"] == 51
    assert status["claim_blocker_rows_v229"] == 4
    assert status["added_loan_id_v229"] == "163383289"
    assert status["dropped_loan_id_v229"] == "170390171"
    assert status["selected_rows_v229"] == 171
    assert status["portfolio_exposure_v229"] == pytest.approx(842425.0)
    assert status["objective_return_v229"] == pytest.approx(2687.4907982045243)
    assert status["scenario_loss_cvar90_v229"] == pytest.approx(97748.72687062543)
    assert status["source_cap_violations_v229"] == 0
    assert status["delta_return_vs_v227_v229"] == pytest.approx(14.175162987819931)
    assert status["delta_cvar90_vs_v227_v229"] == pytest.approx(-16.851411838113563)
    assert status["delta_exposure_vs_v227_v229"] == pytest.approx(0.0)
    assert status["budget_feasible_v229"] is True
    assert status["source_feasible_v229"] is True
    assert status["cvar_feasible_v229"] is True
    assert status["repair_candidate_feasible_v229"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v229"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v229"] is False
    assert status["paper1_promotion_allowed_v229"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v229_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v229"]
    assert int(allocations["selected_v229"].sum()) == status["selected_rows_v229"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v229"])
    assert "163383289" in set(allocations["loan_id"].astype(str))
    assert "170390171" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v229"]) == {
        "added_from_v228_best_swap",
        "kept_from_v227",
    }
    assert allocations["claim_boundary_v229"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v229_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v229"]) is True
    assert float(row["delta_cvar90_vs_v227_v229"]) < 0
    assert float(row["scenario_loss_cvar90_v229"]) <= float(row["cvar_cap_v229"])
    assert float(row["max_source_share_v229"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v229"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v229"])

    action = _read_csv("paper4_v229_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v229"]) == status["added_loan_id_v229"]
    assert str(action["dropped_loan_id_v229"]) == status["dropped_loan_id_v229"]
    assert float(action["return_delta_v229"]) == pytest.approx(14.17516298781652)
    assert float(action["exposure_after_repair_v229"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v229"]) == 0

    source_summary = _read_csv("paper4_v229_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v229"]
    assert not source_summary["source_cap_violated_v229"].astype(bool).any()

    blockers = _read_csv("paper4_v229_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v229"], blockers["blocking_v229"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v229_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v229_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v229_repair_candidate_feasible"]) is True
    assert bool(claim_map["v229_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v229_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v229_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v229 seventy-fourth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v229 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v229 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v229: Seventy-Fourth One-Swap Repair Candidate" in notebook
    assert "v230 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v230_post_v229_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v230_status.json")

    assert status["phase"] == "v230_post_v229_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.230"
    assert status["summary_rows_v230"] == 1
    assert status["stage_summary_rows_v230"] == 6
    assert status["candidate_pair_rows_v230"] == 75
    assert status["top_candidate_rows_v230"] == 75
    assert status["claim_blocker_rows_v230"] == 3
    assert status["selected_rows_v230"] == 171
    assert status["candidate_add_rows_v230"] == 276698
    assert status["total_pair_rows_screened_v230"] == 47315358
    assert status["return_improving_pair_rows_v230"] == 1674872
    assert status["budget_return_feasible_pair_rows_v230"] == 1035017
    assert status["source_prefilter_pair_rows_v230"] == 80
    assert status["source_exact_pair_rows_v230"] == 75
    assert status["cvar_feasible_pair_rows_v230"] == 75
    assert status["one_swap_improving_rows_v230"] == 75
    assert status["best_one_swap_return_delta_v230"] == pytest.approx(12.340194846796805)
    assert status["best_one_swap_cvar90_after_v230"] == pytest.approx(97694.8328083569)
    assert status["post_repair_one_swap_local_optimality_cleared_v230"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v230"] is False
    assert status["paper1_promotion_allowed_v230"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v230_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v230"]
    assert probe["return_delta_v230"].gt(0).all()
    assert probe["budget_swap_feasible_v230"].astype(bool).all()
    assert probe["source_swap_feasible_v230"].astype(bool).all()
    assert probe["cvar_swap_feasible_v230"].astype(bool).all()
    assert probe["one_swap_improves_return_v230"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v230"].sum()) == 0
    assert probe["return_delta_v230"].max() == pytest.approx(
        status["best_one_swap_return_delta_v230"]
    )
    assert probe["claim_boundary_v230"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v230_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v230"]) == "139684635"
    assert str(best["dropped_loan_id_v230"]) == "168803640"
    assert float(best["objective_return_after_swap_v230"]) == pytest.approx(2699.830993051321)
    assert float(best["exposure_after_swap_v230"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v230"]) == pytest.approx(97694.8328083569)
    assert bool(best["one_swap_improves_return_v230"]) is True

    summary = _read_csv("paper4_v230_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v230"]) == status["one_swap_improving_rows_v230"]
    assert float(row["current_objective_return_v230"]) == pytest.approx(2687.4907982045243)
    assert float(row["current_exposure_v230"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v230"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v230"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v230"])

    stage_summary = _read_csv("paper4_v230_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v230"], stage_summary["pair_rows_v230"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v230"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v230"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v230"]

    blockers = _read_csv("paper4_v230_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v230"], blockers["blocking_v230"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v230"], blockers["evidence_count_v230"], strict=False)
    )
    assert bool(blocker_map["post_v229_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v229_one_swap_improvement_found"]) == 75
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v230_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v230_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v230_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v230_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v230_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v230 post-v229 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v230 proves the v229 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v230 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v230: Post-v229 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `75`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v231_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v231_status.json")

    assert status["phase"] == "v231_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.231"
    assert status["allocation_rows_v231"] == 171
    assert status["summary_rows_v231"] == 1
    assert status["action_rows_v231"] == 1
    assert status["source_summary_rows_v231"] == 51
    assert status["claim_blocker_rows_v231"] == 4
    assert status["added_loan_id_v231"] == "139684635"
    assert status["dropped_loan_id_v231"] == "168803640"
    assert status["selected_rows_v231"] == 171
    assert status["portfolio_exposure_v231"] == pytest.approx(842425.0)
    assert status["objective_return_v231"] == pytest.approx(2699.8309930513187)
    assert status["scenario_loss_cvar90_v231"] == pytest.approx(97694.8328083569)
    assert status["source_cap_violations_v231"] == 0
    assert status["delta_return_vs_v229_v231"] == pytest.approx(12.340194846794475)
    assert status["delta_cvar90_vs_v229_v231"] == pytest.approx(-53.89406226853316)
    assert status["delta_exposure_vs_v229_v231"] == pytest.approx(0.0)
    assert status["budget_feasible_v231"] is True
    assert status["source_feasible_v231"] is True
    assert status["cvar_feasible_v231"] is True
    assert status["repair_candidate_feasible_v231"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v231"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v231"] is False
    assert status["paper1_promotion_allowed_v231"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v231_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v231"]
    assert int(allocations["selected_v231"].sum()) == status["selected_rows_v231"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v231"])
    assert "139684635" in set(allocations["loan_id"].astype(str))
    assert "168803640" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v231"]) == {
        "added_from_v230_best_swap",
        "kept_from_v229",
    }
    assert allocations["claim_boundary_v231"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v231_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v231"]) is True
    assert float(row["delta_cvar90_vs_v229_v231"]) < 0
    assert float(row["scenario_loss_cvar90_v231"]) <= float(row["cvar_cap_v231"])
    assert float(row["max_source_share_v231"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v231"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v231"])

    action = _read_csv("paper4_v231_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v231"]) == status["added_loan_id_v231"]
    assert str(action["dropped_loan_id_v231"]) == status["dropped_loan_id_v231"]
    assert float(action["return_delta_v231"]) == pytest.approx(12.340194846796804)
    assert float(action["exposure_after_repair_v231"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v231"]) == 0

    source_summary = _read_csv("paper4_v231_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v231"]
    assert not source_summary["source_cap_violated_v231"].astype(bool).any()

    blockers = _read_csv("paper4_v231_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v231"], blockers["blocking_v231"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v231_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v231_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v231_repair_candidate_feasible"]) is True
    assert bool(claim_map["v231_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v231_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v231_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v231 seventy-fifth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v231 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v231 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v231: Seventy-Fifth One-Swap Repair Candidate" in notebook
    assert "v232 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v232_post_v231_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v232_status.json")

    assert status["phase"] == "v232_post_v231_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.232"
    assert status["summary_rows_v232"] == 1
    assert status["stage_summary_rows_v232"] == 6
    assert status["candidate_pair_rows_v232"] == 71
    assert status["top_candidate_rows_v232"] == 71
    assert status["claim_blocker_rows_v232"] == 3
    assert status["selected_rows_v232"] == 171
    assert status["candidate_add_rows_v232"] == 276698
    assert status["total_pair_rows_screened_v232"] == 47315358
    assert status["return_improving_pair_rows_v232"] == 1673936
    assert status["budget_return_feasible_pair_rows_v232"] == 1034416
    assert status["source_prefilter_pair_rows_v232"] == 76
    assert status["source_exact_pair_rows_v232"] == 71
    assert status["cvar_feasible_pair_rows_v232"] == 71
    assert status["one_swap_improving_rows_v232"] == 71
    assert status["best_one_swap_return_delta_v232"] == pytest.approx(11.978763967603207)
    assert status["best_one_swap_cvar90_after_v232"] == pytest.approx(97634.57015361603)
    assert status["post_repair_one_swap_local_optimality_cleared_v232"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v232"] is False
    assert status["paper1_promotion_allowed_v232"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v232_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v232"]
    assert probe["return_delta_v232"].gt(0).all()
    assert probe["budget_swap_feasible_v232"].astype(bool).all()
    assert probe["source_swap_feasible_v232"].astype(bool).all()
    assert probe["cvar_swap_feasible_v232"].astype(bool).all()
    assert probe["one_swap_improves_return_v232"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v232"].sum()) == 0
    assert probe["return_delta_v232"].max() == pytest.approx(
        status["best_one_swap_return_delta_v232"]
    )
    assert probe["claim_boundary_v232"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v232_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v232"]) == "143126474"
    assert str(best["dropped_loan_id_v232"]) == "169450190"
    assert float(best["objective_return_after_swap_v232"]) == pytest.approx(2711.809757018922)
    assert float(best["exposure_after_swap_v232"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v232"]) == pytest.approx(97634.57015361603)
    assert bool(best["one_swap_improves_return_v232"]) is True

    summary = _read_csv("paper4_v232_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v232"]) == status["one_swap_improving_rows_v232"]
    assert float(row["current_objective_return_v232"]) == pytest.approx(2699.8309930513187)
    assert float(row["current_exposure_v232"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v232"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v232"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v232"])

    stage_summary = _read_csv("paper4_v232_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v232"], stage_summary["pair_rows_v232"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v232"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v232"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v232"]

    blockers = _read_csv("paper4_v232_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v232"], blockers["blocking_v232"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v232"], blockers["evidence_count_v232"], strict=False)
    )
    assert bool(blocker_map["post_v231_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v231_one_swap_improvement_found"]) == 71
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v232_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v232_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v232_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v232_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v232_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v232 post-v231 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v232 proves the v231 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v232 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v232: Post-v231 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `71`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v233_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v233_status.json")

    assert status["phase"] == "v233_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.233"
    assert status["allocation_rows_v233"] == 171
    assert status["summary_rows_v233"] == 1
    assert status["action_rows_v233"] == 1
    assert status["source_summary_rows_v233"] == 51
    assert status["claim_blocker_rows_v233"] == 4
    assert status["added_loan_id_v233"] == "143126474"
    assert status["dropped_loan_id_v233"] == "169450190"
    assert status["selected_rows_v233"] == 171
    assert status["portfolio_exposure_v233"] == pytest.approx(842425.0)
    assert status["objective_return_v233"] == pytest.approx(2711.8097570189257)
    assert status["scenario_loss_cvar90_v233"] == pytest.approx(97634.57015361603)
    assert status["source_cap_violations_v233"] == 0
    assert status["delta_return_vs_v231_v233"] == pytest.approx(11.978763967606938)
    assert status["delta_cvar90_vs_v231_v233"] == pytest.approx(-60.26265474087268)
    assert status["delta_exposure_vs_v231_v233"] == pytest.approx(0.0)
    assert status["budget_feasible_v233"] is True
    assert status["source_feasible_v233"] is True
    assert status["cvar_feasible_v233"] is True
    assert status["repair_candidate_feasible_v233"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v233"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v233"] is False
    assert status["paper1_promotion_allowed_v233"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v233_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v233"]
    assert int(allocations["selected_v233"].sum()) == status["selected_rows_v233"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v233"])
    assert "143126474" in set(allocations["loan_id"].astype(str))
    assert "169450190" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v233"]) == {
        "added_from_v232_best_swap",
        "kept_from_v231",
    }
    assert allocations["claim_boundary_v233"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v233_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v233"]) is True
    assert float(row["delta_cvar90_vs_v231_v233"]) < 0
    assert float(row["scenario_loss_cvar90_v233"]) <= float(row["cvar_cap_v233"])
    assert float(row["max_source_share_v233"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v233"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v233"])

    action = _read_csv("paper4_v233_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v233"]) == status["added_loan_id_v233"]
    assert str(action["dropped_loan_id_v233"]) == status["dropped_loan_id_v233"]
    assert float(action["return_delta_v233"]) == pytest.approx(11.978763967603207)
    assert float(action["exposure_after_repair_v233"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v233"]) == 0

    source_summary = _read_csv("paper4_v233_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v233"]
    assert not source_summary["source_cap_violated_v233"].astype(bool).any()

    blockers = _read_csv("paper4_v233_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v233"], blockers["blocking_v233"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v233_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v233_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v233_repair_candidate_feasible"]) is True
    assert bool(claim_map["v233_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v233_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v233_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v233 seventy-sixth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v233 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v233 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v233: Seventy-Sixth One-Swap Repair Candidate" in notebook
    assert "v234 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v234_post_v233_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v234_status.json")

    assert status["phase"] == "v234_post_v233_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.234"
    assert status["summary_rows_v234"] == 1
    assert status["stage_summary_rows_v234"] == 6
    assert status["candidate_pair_rows_v234"] == 63
    assert status["top_candidate_rows_v234"] == 63
    assert status["claim_blocker_rows_v234"] == 3
    assert status["selected_rows_v234"] == 171
    assert status["candidate_add_rows_v234"] == 276698
    assert status["total_pair_rows_screened_v234"] == 47315358
    assert status["return_improving_pair_rows_v234"] == 1673021
    assert status["budget_return_feasible_pair_rows_v234"] == 1033771
    assert status["source_prefilter_pair_rows_v234"] == 68
    assert status["source_exact_pair_rows_v234"] == 63
    assert status["cvar_feasible_pair_rows_v234"] == 63
    assert status["one_swap_improving_rows_v234"] == 63
    assert status["best_one_swap_return_delta_v234"] == pytest.approx(11.570785823818994)
    assert status["best_one_swap_cvar90_after_v234"] == pytest.approx(97496.90245125577)
    assert status["post_repair_one_swap_local_optimality_cleared_v234"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v234"] is False
    assert status["paper1_promotion_allowed_v234"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v234_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v234"]
    assert probe["return_delta_v234"].gt(0).all()
    assert probe["budget_swap_feasible_v234"].astype(bool).all()
    assert probe["source_swap_feasible_v234"].astype(bool).all()
    assert probe["cvar_swap_feasible_v234"].astype(bool).all()
    assert probe["one_swap_improves_return_v234"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v234"].sum()) == 0
    assert probe["return_delta_v234"].max() == pytest.approx(
        status["best_one_swap_return_delta_v234"]
    )
    assert probe["claim_boundary_v234"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v234_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v234"]) == "138239745"
    assert str(best["dropped_loan_id_v234"]) == "139506195"
    assert float(best["objective_return_after_swap_v234"]) == pytest.approx(2723.3805428427445)
    assert float(best["exposure_after_swap_v234"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v234"]) == pytest.approx(97496.90245125577)
    assert bool(best["one_swap_improves_return_v234"]) is True

    summary = _read_csv("paper4_v234_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v234"]) == status["one_swap_improving_rows_v234"]
    assert float(row["current_objective_return_v234"]) == pytest.approx(2711.8097570189257)
    assert float(row["current_exposure_v234"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v234"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v234"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v234"])

    stage_summary = _read_csv("paper4_v234_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v234"], stage_summary["pair_rows_v234"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v234"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v234"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v234"]

    blockers = _read_csv("paper4_v234_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v234"], blockers["blocking_v234"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v234"], blockers["evidence_count_v234"], strict=False)
    )
    assert bool(blocker_map["post_v233_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v233_one_swap_improvement_found"]) == 63
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v234_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v234_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v234_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v234_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v234_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v234 post-v233 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v234 proves the v233 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v234 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v234: Post-v233 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `63`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v235_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v235_status.json")

    assert status["phase"] == "v235_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.235"
    assert status["allocation_rows_v235"] == 171
    assert status["summary_rows_v235"] == 1
    assert status["action_rows_v235"] == 1
    assert status["source_summary_rows_v235"] == 51
    assert status["claim_blocker_rows_v235"] == 4
    assert status["added_loan_id_v235"] == "138239745"
    assert status["dropped_loan_id_v235"] == "139506195"
    assert status["selected_rows_v235"] == 171
    assert status["portfolio_exposure_v235"] == pytest.approx(842425.0)
    assert status["objective_return_v235"] == pytest.approx(2723.380542842742)
    assert status["scenario_loss_cvar90_v235"] == pytest.approx(97496.90245125574)
    assert status["source_cap_violations_v235"] == 0
    assert status["delta_return_vs_v233_v235"] == pytest.approx(11.57078582381655)
    assert status["delta_cvar90_vs_v233_v235"] == pytest.approx(-137.66770236029697)
    assert status["delta_exposure_vs_v233_v235"] == pytest.approx(0.0)
    assert status["budget_feasible_v235"] is True
    assert status["source_feasible_v235"] is True
    assert status["cvar_feasible_v235"] is True
    assert status["repair_candidate_feasible_v235"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v235"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v235"] is False
    assert status["paper1_promotion_allowed_v235"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v235_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v235"]
    assert int(allocations["selected_v235"].sum()) == status["selected_rows_v235"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v235"])
    assert "138239745" in set(allocations["loan_id"].astype(str))
    assert "139506195" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v235"]) == {
        "added_from_v234_best_swap",
        "kept_from_v233",
    }
    assert allocations["claim_boundary_v235"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v235_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v235"]) is True
    assert float(row["delta_cvar90_vs_v233_v235"]) < 0
    assert float(row["scenario_loss_cvar90_v235"]) <= float(row["cvar_cap_v235"])
    assert float(row["max_source_share_v235"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v235"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v235"])

    action = _read_csv("paper4_v235_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v235"]) == status["added_loan_id_v235"]
    assert str(action["dropped_loan_id_v235"]) == status["dropped_loan_id_v235"]
    assert float(action["return_delta_v235"]) == pytest.approx(11.570785823818994)
    assert float(action["exposure_after_repair_v235"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v235"]) == 0

    source_summary = _read_csv("paper4_v235_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v235"]
    assert not source_summary["source_cap_violated_v235"].astype(bool).any()

    blockers = _read_csv("paper4_v235_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v235"], blockers["blocking_v235"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v235_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v235_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v235_repair_candidate_feasible"]) is True
    assert bool(claim_map["v235_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v235_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v235_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v235 seventy-seventh one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v235 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v235 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v235: Seventy-Seventh One-Swap Repair Candidate" in notebook
    assert "v236 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v236_post_v235_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v236_status.json")

    assert status["phase"] == "v236_post_v235_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.236"
    assert status["summary_rows_v236"] == 1
    assert status["stage_summary_rows_v236"] == 6
    assert status["candidate_pair_rows_v236"] == 47
    assert status["top_candidate_rows_v236"] == 47
    assert status["claim_blocker_rows_v236"] == 3
    assert status["selected_rows_v236"] == 171
    assert status["candidate_add_rows_v236"] == 276698
    assert status["total_pair_rows_screened_v236"] == 47315358
    assert status["return_improving_pair_rows_v236"] == 1672236
    assert status["budget_return_feasible_pair_rows_v236"] == 1033251
    assert status["source_prefilter_pair_rows_v236"] == 52
    assert status["source_exact_pair_rows_v236"] == 47
    assert status["cvar_feasible_pair_rows_v236"] == 47
    assert status["one_swap_improving_rows_v236"] == 47
    assert status["best_one_swap_return_delta_v236"] == pytest.approx(10.547617016004295)
    assert status["best_one_swap_cvar90_after_v236"] == pytest.approx(97568.93047152687)
    assert status["post_repair_one_swap_local_optimality_cleared_v236"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v236"] is False
    assert status["paper1_promotion_allowed_v236"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v236_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v236"]
    assert probe["return_delta_v236"].gt(0).all()
    assert probe["budget_swap_feasible_v236"].astype(bool).all()
    assert probe["source_swap_feasible_v236"].astype(bool).all()
    assert probe["cvar_swap_feasible_v236"].astype(bool).all()
    assert probe["one_swap_improves_return_v236"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v236"].sum()) == 0
    assert probe["return_delta_v236"].max() == pytest.approx(
        status["best_one_swap_return_delta_v236"]
    )
    assert probe["claim_boundary_v236"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v236_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v236"]) == "140352540"
    assert str(best["dropped_loan_id_v236"]) == "127695160"
    assert float(best["objective_return_after_swap_v236"]) == pytest.approx(2733.9281598587463)
    assert float(best["exposure_after_swap_v236"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v236"]) == pytest.approx(97568.93047152687)
    assert bool(best["one_swap_improves_return_v236"]) is True

    summary = _read_csv("paper4_v236_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v236"]) == status["one_swap_improving_rows_v236"]
    assert float(row["current_objective_return_v236"]) == pytest.approx(2723.380542842742)
    assert float(row["current_exposure_v236"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v236"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v236"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v236"])

    stage_summary = _read_csv("paper4_v236_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v236"], stage_summary["pair_rows_v236"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v236"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v236"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v236"]

    blockers = _read_csv("paper4_v236_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v236"], blockers["blocking_v236"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v236"], blockers["evidence_count_v236"], strict=False)
    )
    assert bool(blocker_map["post_v235_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v235_one_swap_improvement_found"]) == 47
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v236_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v236_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v236_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v236_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v236_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v236 post-v235 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v236 proves the v235 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v236 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v236: Post-v235 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `47`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v237_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v237_status.json")

    assert status["phase"] == "v237_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.237"
    assert status["allocation_rows_v237"] == 171
    assert status["summary_rows_v237"] == 1
    assert status["action_rows_v237"] == 1
    assert status["source_summary_rows_v237"] == 51
    assert status["claim_blocker_rows_v237"] == 4
    assert status["added_loan_id_v237"] == "140352540"
    assert status["dropped_loan_id_v237"] == "127695160"
    assert status["selected_rows_v237"] == 171
    assert status["portfolio_exposure_v237"] == pytest.approx(842425.0)
    assert status["objective_return_v237"] == pytest.approx(2733.9281598587477)
    assert status["scenario_loss_cvar90_v237"] == pytest.approx(97568.93047152687)
    assert status["source_cap_violations_v237"] == 0
    assert status["delta_return_vs_v235_v237"] == pytest.approx(10.54761701600546)
    assert status["delta_cvar90_vs_v235_v237"] == pytest.approx(72.02802027112921)
    assert status["delta_exposure_vs_v235_v237"] == pytest.approx(0.0)
    assert status["budget_feasible_v237"] is True
    assert status["source_feasible_v237"] is True
    assert status["cvar_feasible_v237"] is True
    assert status["repair_candidate_feasible_v237"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v237"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v237"] is False
    assert status["paper1_promotion_allowed_v237"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v237_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v237"]
    assert int(allocations["selected_v237"].sum()) == status["selected_rows_v237"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v237"])
    assert "140352540" in set(allocations["loan_id"].astype(str))
    assert "127695160" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v237"]) == {
        "added_from_v236_best_swap",
        "kept_from_v235",
    }
    assert allocations["claim_boundary_v237"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v237_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v237"]) is True
    assert float(row["delta_cvar90_vs_v235_v237"]) > 0
    assert float(row["scenario_loss_cvar90_v237"]) <= float(row["cvar_cap_v237"])
    assert float(row["max_source_share_v237"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v237"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v237"])

    action = _read_csv("paper4_v237_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v237"]) == status["added_loan_id_v237"]
    assert str(action["dropped_loan_id_v237"]) == status["dropped_loan_id_v237"]
    assert float(action["return_delta_v237"]) == pytest.approx(10.547617016004295)
    assert float(action["exposure_after_repair_v237"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v237"]) == 0

    source_summary = _read_csv("paper4_v237_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v237"]
    assert not source_summary["source_cap_violated_v237"].astype(bool).any()

    blockers = _read_csv("paper4_v237_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v237"], blockers["blocking_v237"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v237_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v237_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v237_repair_candidate_feasible"]) is True
    assert bool(claim_map["v237_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v237_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v237_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v237 seventy-eighth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v237 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v237 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v237: Seventy-Eighth One-Swap Repair Candidate" in notebook
    assert "v238 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v238_post_v237_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v238_status.json")

    assert status["phase"] == "v238_post_v237_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.238"
    assert status["summary_rows_v238"] == 1
    assert status["stage_summary_rows_v238"] == 6
    assert status["candidate_pair_rows_v238"] == 42
    assert status["top_candidate_rows_v238"] == 42
    assert status["claim_blocker_rows_v238"] == 3
    assert status["selected_rows_v238"] == 171
    assert status["candidate_add_rows_v238"] == 276698
    assert status["total_pair_rows_screened_v238"] == 47315358
    assert status["return_improving_pair_rows_v238"] == 1671490
    assert status["budget_return_feasible_pair_rows_v238"] == 1032842
    assert status["source_prefilter_pair_rows_v238"] == 47
    assert status["source_exact_pair_rows_v238"] == 42
    assert status["cvar_feasible_pair_rows_v238"] == 42
    assert status["one_swap_improving_rows_v238"] == 42
    assert status["best_one_swap_return_delta_v238"] == pytest.approx(10.19443425863426)
    assert status["best_one_swap_cvar90_after_v238"] == pytest.approx(97618.67176360867)
    assert status["post_repair_one_swap_local_optimality_cleared_v238"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v238"] is False
    assert status["paper1_promotion_allowed_v238"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v238_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v238"]
    assert probe["return_delta_v238"].gt(0).all()
    assert probe["budget_swap_feasible_v238"].astype(bool).all()
    assert probe["source_swap_feasible_v238"].astype(bool).all()
    assert probe["cvar_swap_feasible_v238"].astype(bool).all()
    assert probe["one_swap_improves_return_v238"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v238"].sum()) == 0
    assert probe["return_delta_v238"].max() == pytest.approx(
        status["best_one_swap_return_delta_v238"]
    )
    assert probe["claim_boundary_v238"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v238_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v238"]) == "137378784"
    assert str(best["dropped_loan_id_v238"]) == "126468092"
    assert float(best["objective_return_after_swap_v238"]) == pytest.approx(2744.1225941173825)
    assert float(best["exposure_after_swap_v238"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v238"]) == pytest.approx(97618.67176360867)
    assert bool(best["one_swap_improves_return_v238"]) is True

    summary = _read_csv("paper4_v238_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v238"]) == status["one_swap_improving_rows_v238"]
    assert float(row["current_objective_return_v238"]) == pytest.approx(2733.928159858748)
    assert float(row["current_exposure_v238"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v238"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v238"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v238"])

    stage_summary = _read_csv("paper4_v238_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v238"], stage_summary["pair_rows_v238"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v238"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v238"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v238"]

    blockers = _read_csv("paper4_v238_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v238"], blockers["blocking_v238"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v238"], blockers["evidence_count_v238"], strict=False)
    )
    assert bool(blocker_map["post_v237_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v237_one_swap_improvement_found"]) == 42
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v238_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v238_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v238_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v238_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v238_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v238 post-v237 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v238 proves the v237 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v238 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v238: Post-v237 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `42`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v239_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v239_status.json")

    assert status["phase"] == "v239_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.239"
    assert status["allocation_rows_v239"] == 171
    assert status["summary_rows_v239"] == 1
    assert status["action_rows_v239"] == 1
    assert status["source_summary_rows_v239"] == 51
    assert status["claim_blocker_rows_v239"] == 4
    assert status["added_loan_id_v239"] == "137378784"
    assert status["dropped_loan_id_v239"] == "126468092"
    assert status["selected_rows_v239"] == 171
    assert status["portfolio_exposure_v239"] == pytest.approx(842425.0)
    assert status["objective_return_v239"] == pytest.approx(2744.12259411738)
    assert status["scenario_loss_cvar90_v239"] == pytest.approx(97618.67176360867)
    assert status["source_cap_violations_v239"] == 0
    assert status["delta_return_vs_v237_v239"] == pytest.approx(10.194434258631645)
    assert status["delta_cvar90_vs_v237_v239"] == pytest.approx(49.74129208178783)
    assert status["delta_exposure_vs_v237_v239"] == pytest.approx(0.0)
    assert status["budget_feasible_v239"] is True
    assert status["source_feasible_v239"] is True
    assert status["cvar_feasible_v239"] is True
    assert status["repair_candidate_feasible_v239"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v239"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v239"] is False
    assert status["paper1_promotion_allowed_v239"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v239_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v239"]
    assert int(allocations["selected_v239"].sum()) == status["selected_rows_v239"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v239"])
    assert "137378784" in set(allocations["loan_id"].astype(str))
    assert "126468092" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v239"]) == {
        "added_from_v238_best_swap",
        "kept_from_v237",
    }
    assert allocations["claim_boundary_v239"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v239_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v239"]) is True
    assert float(row["delta_cvar90_vs_v237_v239"]) > 0
    assert float(row["scenario_loss_cvar90_v239"]) <= float(row["cvar_cap_v239"])
    assert float(row["max_source_share_v239"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v239"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v239"])

    action = _read_csv("paper4_v239_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v239"]) == status["added_loan_id_v239"]
    assert str(action["dropped_loan_id_v239"]) == status["dropped_loan_id_v239"]
    assert float(action["return_delta_v239"]) == pytest.approx(10.19443425863426)
    assert float(action["exposure_after_repair_v239"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v239"]) == 0

    source_summary = _read_csv("paper4_v239_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v239"]
    assert not source_summary["source_cap_violated_v239"].astype(bool).any()

    blockers = _read_csv("paper4_v239_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v239"], blockers["blocking_v239"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v239_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v239_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v239_repair_candidate_feasible"]) is True
    assert bool(claim_map["v239_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v239_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v239_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v239 seventy-ninth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v239 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v239 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v239: Seventy-Ninth One-Swap Repair Candidate" in notebook
    assert "v240 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v240_post_v239_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v240_status.json")

    assert status["phase"] == "v240_post_v239_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.240"
    assert status["summary_rows_v240"] == 1
    assert status["stage_summary_rows_v240"] == 6
    assert status["candidate_pair_rows_v240"] == 26
    assert status["top_candidate_rows_v240"] == 26
    assert status["claim_blocker_rows_v240"] == 3
    assert status["selected_rows_v240"] == 171
    assert status["candidate_add_rows_v240"] == 276698
    assert status["total_pair_rows_screened_v240"] == 47315358
    assert status["return_improving_pair_rows_v240"] == 1670765
    assert status["budget_return_feasible_pair_rows_v240"] == 1032355
    assert status["source_prefilter_pair_rows_v240"] == 31
    assert status["source_exact_pair_rows_v240"] == 26
    assert status["cvar_feasible_pair_rows_v240"] == 26
    assert status["one_swap_improving_rows_v240"] == 26
    assert status["best_one_swap_return_delta_v240"] == pytest.approx(7.381885540755661)
    assert status["best_one_swap_cvar90_after_v240"] == pytest.approx(97568.81252848775)
    assert status["post_repair_one_swap_local_optimality_cleared_v240"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v240"] is False
    assert status["paper1_promotion_allowed_v240"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v240_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v240"]
    assert probe["return_delta_v240"].gt(0).all()
    assert probe["budget_swap_feasible_v240"].astype(bool).all()
    assert probe["source_swap_feasible_v240"].astype(bool).all()
    assert probe["cvar_swap_feasible_v240"].astype(bool).all()
    assert probe["one_swap_improves_return_v240"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v240"].sum()) == 0
    assert probe["return_delta_v240"].max() == pytest.approx(
        status["best_one_swap_return_delta_v240"]
    )
    assert probe["claim_boundary_v240"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v240_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v240"]) == "162083390"
    assert str(best["dropped_loan_id_v240"]) == "156807668"
    assert float(best["objective_return_after_swap_v240"]) == pytest.approx(2751.5044796581356)
    assert float(best["exposure_after_swap_v240"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v240"]) == pytest.approx(97568.81252848775)
    assert bool(best["one_swap_improves_return_v240"]) is True

    summary = _read_csv("paper4_v240_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v240"]) == status["one_swap_improving_rows_v240"]
    assert float(row["current_objective_return_v240"]) == pytest.approx(2744.12259411738)
    assert float(row["current_exposure_v240"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v240"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v240"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v240"])

    stage_summary = _read_csv("paper4_v240_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v240"], stage_summary["pair_rows_v240"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v240"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v240"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v240"]

    blockers = _read_csv("paper4_v240_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v240"], blockers["blocking_v240"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v240"], blockers["evidence_count_v240"], strict=False)
    )
    assert bool(blocker_map["post_v239_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v239_one_swap_improvement_found"]) == 26
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v240_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v240_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v240_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v240_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v240_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v240 post-v239 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v240 proves the v239 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v240 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v240: Post-v239 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `26`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v241_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v241_status.json")

    assert status["phase"] == "v241_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.241"
    assert status["allocation_rows_v241"] == 171
    assert status["summary_rows_v241"] == 1
    assert status["action_rows_v241"] == 1
    assert status["source_summary_rows_v241"] == 51
    assert status["claim_blocker_rows_v241"] == 4
    assert status["added_loan_id_v241"] == "162083390"
    assert status["dropped_loan_id_v241"] == "156807668"
    assert status["selected_rows_v241"] == 171
    assert status["portfolio_exposure_v241"] == pytest.approx(842425.0)
    assert status["objective_return_v241"] == pytest.approx(2751.504479658137)
    assert status["scenario_loss_cvar90_v241"] == pytest.approx(97568.81252848776)
    assert status["source_cap_violations_v241"] == 0
    assert status["delta_return_vs_v239_v241"] == pytest.approx(7.381885540757139)
    assert status["delta_cvar90_vs_v239_v241"] == pytest.approx(-49.859235120922676)
    assert status["delta_exposure_vs_v239_v241"] == pytest.approx(0.0)
    assert status["budget_feasible_v241"] is True
    assert status["source_feasible_v241"] is True
    assert status["cvar_feasible_v241"] is True
    assert status["repair_candidate_feasible_v241"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v241"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v241"] is False
    assert status["paper1_promotion_allowed_v241"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v241_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v241"]
    assert int(allocations["selected_v241"].sum()) == status["selected_rows_v241"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v241"])
    assert "162083390" in set(allocations["loan_id"].astype(str))
    assert "156807668" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v241"]) == {
        "added_from_v240_best_swap",
        "kept_from_v239",
    }
    assert allocations["claim_boundary_v241"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v241_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v241"]) is True
    assert float(row["delta_cvar90_vs_v239_v241"]) < 0
    assert float(row["scenario_loss_cvar90_v241"]) <= float(row["cvar_cap_v241"])
    assert float(row["max_source_share_v241"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v241"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v241"])

    action = _read_csv("paper4_v241_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v241"]) == status["added_loan_id_v241"]
    assert str(action["dropped_loan_id_v241"]) == status["dropped_loan_id_v241"]
    assert float(action["return_delta_v241"]) == pytest.approx(7.381885540755661)
    assert float(action["exposure_after_repair_v241"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v241"]) == 0

    source_summary = _read_csv("paper4_v241_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v241"]
    assert not source_summary["source_cap_violated_v241"].astype(bool).any()

    blockers = _read_csv("paper4_v241_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v241"], blockers["blocking_v241"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v241_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v241_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v241_repair_candidate_feasible"]) is True
    assert bool(claim_map["v241_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v241_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v241_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v241 eightieth one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v241 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v241 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v241: Eightieth One-Swap Repair Candidate" in notebook
    assert "v242 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v242_post_v241_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v242_status.json")

    assert status["phase"] == "v242_post_v241_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.242"
    assert status["summary_rows_v242"] == 1
    assert status["stage_summary_rows_v242"] == 6
    assert status["candidate_pair_rows_v242"] == 14
    assert status["top_candidate_rows_v242"] == 14
    assert status["claim_blocker_rows_v242"] == 3
    assert status["selected_rows_v242"] == 171
    assert status["candidate_add_rows_v242"] == 276698
    assert status["total_pair_rows_screened_v242"] == 47315358
    assert status["return_improving_pair_rows_v242"] == 1670269
    assert status["budget_return_feasible_pair_rows_v242"] == 1032010
    assert status["source_prefilter_pair_rows_v242"] == 19
    assert status["source_exact_pair_rows_v242"] == 14
    assert status["cvar_feasible_pair_rows_v242"] == 14
    assert status["one_swap_improving_rows_v242"] == 14
    assert status["best_one_swap_return_delta_v242"] == pytest.approx(7.096478885443332)
    assert status["best_one_swap_cvar90_after_v242"] == pytest.approx(97614.66532619043)
    assert status["post_repair_one_swap_local_optimality_cleared_v242"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v242"] is False
    assert status["paper1_promotion_allowed_v242"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v242_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v242"]
    assert probe["return_delta_v242"].gt(0).all()
    assert probe["budget_swap_feasible_v242"].astype(bool).all()
    assert probe["source_swap_feasible_v242"].astype(bool).all()
    assert probe["cvar_swap_feasible_v242"].astype(bool).all()
    assert probe["one_swap_improves_return_v242"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v242"].sum()) == 0
    assert probe["return_delta_v242"].max() == pytest.approx(
        status["best_one_swap_return_delta_v242"]
    )
    assert probe["claim_boundary_v242"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v242_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v242"]) == "138631135"
    assert str(best["dropped_loan_id_v242"]) == "126827839"
    assert float(best["objective_return_after_swap_v242"]) == pytest.approx(2758.6009585435804)
    assert float(best["exposure_after_swap_v242"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v242"]) == pytest.approx(97614.66532619043)
    assert bool(best["one_swap_improves_return_v242"]) is True

    summary = _read_csv("paper4_v242_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v242"]) == status["one_swap_improving_rows_v242"]
    assert float(row["current_objective_return_v242"]) == pytest.approx(2751.504479658137)
    assert float(row["current_exposure_v242"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v242"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v242"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v242"])

    stage_summary = _read_csv("paper4_v242_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v242"], stage_summary["pair_rows_v242"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v242"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v242"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v242"]

    blockers = _read_csv("paper4_v242_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v242"], blockers["blocking_v242"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v242"], blockers["evidence_count_v242"], strict=False)
    )
    assert bool(blocker_map["post_v241_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v241_one_swap_improvement_found"]) == 14
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v242_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v242_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v242_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v242_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v242_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v242 post-v241 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v242 proves the v241 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v242 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v242: Post-v241 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `14`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v243_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v243_status.json")

    assert status["phase"] == "v243_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.243"
    assert status["allocation_rows_v243"] == 171
    assert status["summary_rows_v243"] == 1
    assert status["action_rows_v243"] == 1
    assert status["source_summary_rows_v243"] == 51
    assert status["claim_blocker_rows_v243"] == 4
    assert status["added_loan_id_v243"] == "138631135"
    assert status["dropped_loan_id_v243"] == "126827839"
    assert status["selected_rows_v243"] == 171
    assert status["portfolio_exposure_v243"] == pytest.approx(842425.0)
    assert status["objective_return_v243"] == pytest.approx(2758.6009585435822)
    assert status["scenario_loss_cvar90_v243"] == pytest.approx(97614.66532619043)
    assert status["source_cap_violations_v243"] == 0
    assert status["delta_return_vs_v241_v243"] == pytest.approx(7.096478885445322)
    assert status["delta_cvar90_vs_v241_v243"] == pytest.approx(45.852797702667885)
    assert status["delta_exposure_vs_v241_v243"] == pytest.approx(0.0)
    assert status["budget_feasible_v243"] is True
    assert status["source_feasible_v243"] is True
    assert status["cvar_feasible_v243"] is True
    assert status["repair_candidate_feasible_v243"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v243"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v243"] is False
    assert status["paper1_promotion_allowed_v243"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v243_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v243"]
    assert int(allocations["selected_v243"].sum()) == status["selected_rows_v243"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v243"])
    assert "138631135" in set(allocations["loan_id"].astype(str))
    assert "126827839" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v243"]) == {
        "added_from_v242_best_swap",
        "kept_from_v241",
    }
    assert allocations["claim_boundary_v243"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v243_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v243"]) is True
    assert float(row["delta_cvar90_vs_v241_v243"]) > 0
    assert float(row["scenario_loss_cvar90_v243"]) <= float(row["cvar_cap_v243"])
    assert float(row["max_source_share_v243"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v243"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v243"])

    action = _read_csv("paper4_v243_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v243"]) == status["added_loan_id_v243"]
    assert str(action["dropped_loan_id_v243"]) == status["dropped_loan_id_v243"]
    assert float(action["return_delta_v243"]) == pytest.approx(7.096478885443332)
    assert float(action["exposure_after_repair_v243"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v243"]) == 0

    source_summary = _read_csv("paper4_v243_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v243"]
    assert not source_summary["source_cap_violated_v243"].astype(bool).any()

    blockers = _read_csv("paper4_v243_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v243"], blockers["blocking_v243"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v243_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v243_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v243_repair_candidate_feasible"]) is True
    assert bool(claim_map["v243_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v243_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v243_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v243 eighty-first one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v243 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v243 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v243: Eighty-First One-Swap Repair Candidate" in notebook
    assert "v244 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v244_post_v243_reprice_still_finds_improvements() -> None:
    status = _read_json("paper4_v244_status.json")

    assert status["phase"] == "v244_post_v243_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.244"
    assert status["summary_rows_v244"] == 1
    assert status["stage_summary_rows_v244"] == 6
    assert status["candidate_pair_rows_v244"] == 4
    assert status["top_candidate_rows_v244"] == 4
    assert status["claim_blocker_rows_v244"] == 3
    assert status["selected_rows_v244"] == 171
    assert status["candidate_add_rows_v244"] == 276698
    assert status["total_pair_rows_screened_v244"] == 47315358
    assert status["return_improving_pair_rows_v244"] == 1669795
    assert status["budget_return_feasible_pair_rows_v244"] == 1031683
    assert status["source_prefilter_pair_rows_v244"] == 9
    assert status["source_exact_pair_rows_v244"] == 4
    assert status["cvar_feasible_pair_rows_v244"] == 4
    assert status["one_swap_improving_rows_v244"] == 4
    assert status["best_one_swap_return_delta_v244"] == pytest.approx(2.068708550310282)
    assert status["best_one_swap_cvar90_after_v244"] == pytest.approx(97585.69476728079)
    assert status["post_repair_one_swap_local_optimality_cleared_v244"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v244"] is False
    assert status["paper1_promotion_allowed_v244"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v244_post_repair_one_swap_reprice.csv")
    assert len(probe) == status["candidate_pair_rows_v244"]
    assert probe["return_delta_v244"].gt(0).all()
    assert probe["budget_swap_feasible_v244"].astype(bool).all()
    assert probe["source_swap_feasible_v244"].astype(bool).all()
    assert probe["cvar_swap_feasible_v244"].astype(bool).all()
    assert probe["one_swap_improves_return_v244"].astype(bool).all()
    assert int(probe["source_cap_violations_after_swap_v244"].sum()) == 0
    assert probe["return_delta_v244"].max() == pytest.approx(
        status["best_one_swap_return_delta_v244"]
    )
    assert probe["claim_boundary_v244"].str.contains("not multi-swap or global proof").all()

    best = _read_csv("paper4_v244_post_repair_one_swap_top_candidates.csv").iloc[0]
    assert str(best["added_loan_id_v244"]) == "152762134"
    assert str(best["dropped_loan_id_v244"]) == "168841753"
    assert float(best["objective_return_after_swap_v244"]) == pytest.approx(2760.6696670938927)
    assert float(best["exposure_after_swap_v244"]) == pytest.approx(842425.0)
    assert float(best["cvar90_after_swap_v244"]) == pytest.approx(97585.69476728079)
    assert bool(best["one_swap_improves_return_v244"]) is True

    summary = _read_csv("paper4_v244_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v244"]) == status["one_swap_improving_rows_v244"]
    assert float(row["current_objective_return_v244"]) == pytest.approx(2758.6009585435822)
    assert float(row["current_exposure_v244"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v244"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v244"]) is False
    assert "repeat repair/repricing" in str(row["claim_boundary_v244"])

    stage_summary = _read_csv("paper4_v244_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v244"], stage_summary["pair_rows_v244"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v244"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v244"]
    assert int(stage_map["cvar_feasible_improving"]) == status["one_swap_improving_rows_v244"]

    blockers = _read_csv("paper4_v244_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v244"], blockers["blocking_v244"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v244"], blockers["evidence_count_v244"], strict=False)
    )
    assert bool(blocker_map["post_v243_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v243_one_swap_improvement_found"]) == 4
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v244_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v244_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v244_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v244_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v244_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v244 post-v243 one-swap pricing screen." in set(
        current_boundaries["claim"]
    )
    assert "v244 proves the v243 repaired portfolio is locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v244 proves full-universe integer optimality." in set(current_boundaries["claim"])

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v244: Post-v243 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `4`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v245_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v245_status.json")

    assert status["phase"] == "v245_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.245"
    assert status["allocation_rows_v245"] == 171
    assert status["summary_rows_v245"] == 1
    assert status["action_rows_v245"] == 1
    assert status["source_summary_rows_v245"] == 51
    assert status["claim_blocker_rows_v245"] == 4
    assert status["added_loan_id_v245"] == "152762134"
    assert status["dropped_loan_id_v245"] == "168841753"
    assert status["selected_rows_v245"] == 171
    assert status["portfolio_exposure_v245"] == pytest.approx(842425.0)
    assert status["objective_return_v245"] == pytest.approx(2760.669667093891)
    assert status["scenario_loss_cvar90_v245"] == pytest.approx(97585.69476728079)
    assert status["source_cap_violations_v245"] == 0
    assert status["delta_return_vs_v243_v245"] == pytest.approx(2.0687085503086564)
    assert status["delta_cvar90_vs_v243_v245"] == pytest.approx(-28.970558909655665)
    assert status["delta_exposure_vs_v243_v245"] == pytest.approx(0.0)
    assert status["budget_feasible_v245"] is True
    assert status["source_feasible_v245"] is True
    assert status["cvar_feasible_v245"] is True
    assert status["repair_candidate_feasible_v245"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v245"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v245"] is False
    assert status["paper1_promotion_allowed_v245"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v245_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v245"]
    assert int(allocations["selected_v245"].sum()) == status["selected_rows_v245"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v245"])
    assert "152762134" in set(allocations["loan_id"].astype(str))
    assert "168841753" not in set(allocations["loan_id"].astype(str))
    assert set(allocations["repair_action_v245"]) == {
        "added_from_v244_best_swap",
        "kept_from_v243",
    }
    assert allocations["claim_boundary_v245"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v245_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert bool(row["repair_candidate_feasible_v245"]) is True
    assert float(row["delta_cvar90_vs_v243_v245"]) < 0
    assert float(row["scenario_loss_cvar90_v245"]) <= float(row["cvar_cap_v245"])
    assert float(row["max_source_share_v245"]) == pytest.approx(0.8995459536457251)
    assert float(row["min_source_slack_v245"]) == pytest.approx(3.222484231535372e-05)
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v245"])

    action = _read_csv("paper4_v245_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v245"]) == status["added_loan_id_v245"]
    assert str(action["dropped_loan_id_v245"]) == status["dropped_loan_id_v245"]
    assert float(action["return_delta_v245"]) == pytest.approx(2.068708550310282)
    assert float(action["exposure_after_repair_v245"]) == pytest.approx(842425.0)
    assert int(action["source_cap_violations_after_repair_v245"]) == 0

    source_summary = _read_csv("paper4_v245_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v245"]
    assert not source_summary["source_cap_violated_v245"].astype(bool).any()

    blockers = _read_csv("paper4_v245_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v245"], blockers["blocking_v245"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v245_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v245_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v245_repair_candidate_feasible"]) is True
    assert bool(claim_map["v245_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v245_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v245_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    assert "Paper 4 has a v245 eighty-second one-swap repair candidate." in set(
        current_boundaries["claim"]
    )
    assert "v245 repaired portfolio is post-repair locally optimal." in set(
        current_boundaries["claim"]
    )
    assert "v245 replaces Paper Estrella or proves full-universe integer optimality." in set(
        current_boundaries["claim"]
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v245: Eighty-Second One-Swap Repair Candidate" in notebook
    assert "v246 post-repair one-swap pricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v246_post_v245_reprice_clears_one_swap_local_screen() -> None:
    status = _read_json("paper4_v246_status.json")

    assert status["phase"] == "v246_post_v245_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.246"
    assert status["summary_rows_v246"] == 1
    assert status["stage_summary_rows_v246"] == 6
    assert status["candidate_pair_rows_v246"] == 0
    assert status["top_candidate_rows_v246"] == 0
    assert status["claim_blocker_rows_v246"] == 3
    assert status["selected_rows_v246"] == 171
    assert status["candidate_add_rows_v246"] == 276698
    assert status["total_pair_rows_screened_v246"] == 47315358
    assert status["return_improving_pair_rows_v246"] == 1669643
    assert status["budget_return_feasible_pair_rows_v246"] == 1031582
    assert status["source_prefilter_pair_rows_v246"] == 5
    assert status["source_exact_pair_rows_v246"] == 0
    assert status["cvar_feasible_pair_rows_v246"] == 0
    assert status["one_swap_improving_rows_v246"] == 0
    assert status["best_one_swap_return_delta_v246"] is None
    assert status["best_one_swap_cvar90_after_v246"] is None
    assert status["post_repair_one_swap_local_optimality_cleared_v246"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v246"] is False
    assert status["paper1_promotion_allowed_v246"] is False
    assert status["paper4_final_promotion_created"] is False

    probe = _read_csv("paper4_v246_post_repair_one_swap_reprice.csv")
    assert probe.empty
    assert {
        "policy_id",
        "return_delta_v246",
        "cvar90_after_swap_v246",
        "one_swap_improves_return_v246",
        "claim_boundary_v246",
    }.issubset(probe.columns)

    top_candidates = _read_csv("paper4_v246_post_repair_one_swap_top_candidates.csv")
    assert top_candidates.empty
    assert "return_delta_v246" in top_candidates.columns

    summary = _read_csv("paper4_v246_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert int(row["one_swap_improving_rows_v246"]) == 0
    assert int(row["source_exact_pair_rows_v246"]) == 0
    assert pd.isna(row["best_one_swap_return_delta_v246"])
    assert pd.isna(row["best_one_swap_cvar90_after_v246"])
    assert float(row["current_objective_return_v246"]) == pytest.approx(2760.669667093891)
    assert float(row["current_exposure_v246"]) == pytest.approx(842425.0)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v246"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v246"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v246"])
    assert "multi-swap/global proof still missing" in str(row["claim_boundary_v246"])

    stage_summary = _read_csv("paper4_v246_post_repair_one_swap_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v246"], stage_summary["pair_rows_v246"], strict=False)
    )
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v246"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v246"]
    assert int(stage_map["source_exact_feasible"]) == 0
    assert int(stage_map["cvar_feasible_improving"]) == 0

    blockers = _read_csv("paper4_v246_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v246"], blockers["blocking_v246"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v246"], blockers["evidence_count_v246"], strict=False)
    )
    next_artifact_map = dict(
        zip(
            blockers["blocker_id_v246"],
            blockers["required_next_artifact_v246"],
            strict=False,
        )
    )
    assert bool(blocker_map["post_v245_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v245_one_swap_improvement_found"]) == 0
    assert (
        next_artifact_map["post_v245_one_swap_improvement_found"]
        == "paper4_v246_one_swap_local_optimality_evidence.csv"
    )
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v246_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v246_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v246_post_repair_one_swap_local_optimality"]) is True
    assert bool(claim_map["v246_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v246_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v246 post-v245 one-swap pricing screen."]) is True
    assert bool(boundary_map["v246 proves the v245 repaired portfolio is locally optimal."]) is True
    assert bool(boundary_map["v246 proves full-universe integer optimality."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v246_rows = backlog.loc[backlog["last_wave"].eq("v246")]
    assert len(v246_rows) == 1
    backlog_row = v246_rows.iloc[0]
    assert backlog_row["status"] == "one_swap_local_optimality_cleared"
    assert backlog_row["next_artifact"] == "paper4_v246_multi_swap_or_global_gap_protocol.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v246: Post-v245 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert "`not applicable; no feasible improving one-swaps`" in notebook
    assert "Post-v245 local optimality cleared:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v247_one_swap_loop_synthesis_preserves_boundaries() -> None:
    status = _read_json("paper4_v247_status.json")

    assert status["phase"] == "v247_one_swap_loop_synthesis"
    assert status["schema_version"] == "2026-05-15.247"
    assert status["trajectory_rows_v247"] == 165
    assert status["repair_wave_rows_v247"] == 82
    assert status["pricing_wave_rows_v247"] == 83
    assert status["first_pricing_version_v247"] == 82
    assert status["first_repair_version_v247"] == 83
    assert status["final_repair_version_v247"] == 245
    assert status["terminal_reprice_version_v247"] == 246
    assert status["final_objective_return_v247"] == pytest.approx(2760.669667093891)
    assert status["final_scenario_loss_cvar90_v247"] == pytest.approx(97585.69476728079)
    assert status["return_gain_vs_v80_v247"] == pytest.approx(6816.565198406637)
    assert status["cvar_delta_vs_v80_v247"] == pytest.approx(6191.6784202539275)
    assert status["terminal_one_swap_improving_rows_v247"] == 0
    assert status["one_swap_local_optimality_claim_allowed_v247"] is True
    assert status["multi_swap_integer_optimality_claim_allowed_v247"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v247"] is False
    assert status["paper1_promotion_allowed_v247"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v247"] == 4
    assert status["claim_matrix_rows_v247"] == 4

    synthesis = _read_csv("paper4_v247_one_swap_loop_synthesis.csv")
    assert len(synthesis) == 1
    row = synthesis.iloc[0]
    assert row["synthesis_label_v247"] == "one_swap_loop_v82_v246"
    assert int(row["trajectory_rows_v247"]) == status["trajectory_rows_v247"]
    assert bool(row["one_swap_local_optimality_claim_allowed_v247"]) is True
    assert bool(row["multi_swap_integer_optimality_claim_allowed_v247"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v247"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "multi-swap/global/dynamic promotion claims remain blocked" in str(
        row["claim_boundary_v247"]
    )

    trajectory = _read_csv("paper4_v247_one_swap_loop_trajectory.csv")
    assert len(trajectory) == status["trajectory_rows_v247"]
    assert set(trajectory["wave_type_v247"]) == {"pricing", "repair"}
    assert int(trajectory["wave_version_v247"].min()) == 82
    assert int(trajectory["wave_version_v247"].max()) == 246
    assert int(trajectory["wave_type_v247"].eq("repair").sum()) == 82
    assert int(trajectory["wave_type_v247"].eq("pricing").sum()) == 83

    first = trajectory.loc[trajectory["wave_version_v247"].eq(82)].iloc[0]
    assert first["phase_v247"] == "v82_one_swap_integer_pricing_probe"
    assert int(first["one_swap_improving_rows_v247"]) == 8126
    assert float(first["best_one_swap_return_delta_v247"]) == pytest.approx(201.74349922313803)

    final_repair = trajectory.loc[trajectory["wave_version_v247"].eq(245)].iloc[0]
    assert final_repair["phase_v247"] == "v245_next_one_swap_repair"
    assert float(final_repair["objective_return_v247"]) == pytest.approx(
        status["final_objective_return_v247"]
    )
    assert str(final_repair["added_loan_id_v247"]) == "152762134"
    assert str(final_repair["dropped_loan_id_v247"]) == "168841753"

    terminal = trajectory.loc[trajectory["wave_version_v247"].eq(246)].iloc[0]
    assert terminal["phase_v247"] == "v246_post_v245_one_swap_reprice"
    assert int(terminal["candidate_pair_rows_v247"]) == 0
    assert int(terminal["one_swap_improving_rows_v247"]) == 0
    assert bool(terminal["one_swap_local_optimality_cleared_v247"]) is True

    blockers = _read_csv("paper4_v247_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v247"], blockers["blocking_v247"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v247"], blockers["evidence_count_v247"], strict=False)
    )
    assert bool(blocker_map["one_swap_local_screen_open"]) is False
    assert int(evidence_map["one_swap_local_screen_open"]) == 0
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v247_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v247_one_swap_loop_synthesis_executed"]) is True
    assert bool(claim_map["v247_v245_one_swap_local_optimality"]) is True
    assert bool(claim_map["v247_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v247_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v247 one-swap loop synthesis from v82-v246."]) is True
    assert (
        bool(boundary_map["v247 permits a one-swap local optimality claim for the v245 candidate."])
        is True
    )
    assert (
        bool(boundary_map["v247 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v247 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v247_rows = backlog.loc[backlog["last_wave"].eq("v247")]
    assert len(v247_rows) == 1
    backlog_row = v247_rows.iloc[0]
    assert backlog_row["status"] == "multi_swap_global_gap_pending"
    assert backlog_row["next_artifact"] == "paper4_v248_multi_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v247: One-Swap Loop Synthesis" in notebook
    assert "Trajectory rows: `165`" in notebook
    assert "Terminal one-swap improving rows:\n  `0`" in notebook
    assert "One-swap local optimality claim allowed:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v248_bounded_two_swap_probe_finds_improvements_without_promotion() -> None:
    status = _read_json("paper4_v248_status.json")

    assert status["phase"] == "v248_bounded_two_swap_source_relief_probe"
    assert status["schema_version"] == "2026-05-15.248"
    assert status["previous_repair_version_v248"] == 245
    assert status["terminal_reprice_version_v248"] == 246
    assert status["primary_frontier_rows_v248"] == 5
    assert status["base_two_swap_pair_rows_v248"] == 5411407
    assert status["source_prefilter_two_swap_pair_rows_v248"] == 4833
    assert status["source_exact_two_swap_pair_rows_v248"] == 625
    assert status["cvar_feasible_two_swap_pair_rows_v248"] == 625
    assert status["top_candidate_rows_v248"] == 200
    assert status["best_two_swap_return_delta_v248"] == pytest.approx(76.49772378746906)
    assert status["best_two_swap_cvar90_after_v248"] == pytest.approx(97872.31896744829)
    assert status["best_two_swap_objective_after_v248"] == pytest.approx(2837.16739088136)
    assert status["bounded_two_swap_improvement_found_v248"] is True
    assert status["multi_swap_integer_optimality_claim_allowed_v248"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v248"] is False
    assert status["paper1_promotion_allowed_v248"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v248"] == 4
    assert status["claim_matrix_rows_v248"] == 4

    primary = _read_csv("paper4_v248_bounded_two_swap_primary_frontier.csv")
    assert len(primary) == status["primary_frontier_rows_v248"]
    best_primary = primary.iloc[0]
    assert str(best_primary["primary_added_loan_id_v248"]) == "158701181"
    assert str(best_primary["primary_dropped_loan_id_v248"]) == "127392982"
    assert float(best_primary["primary_return_delta_v248"]) == pytest.approx(43.34182985926691)
    assert int(best_primary["primary_source_cap_violations_v248"]) == 1
    assert best_primary["primary_first_source_block_family_v248"] == "grade"
    assert str(best_primary["primary_first_source_block_id_v248"]) == "A"

    stage = _read_csv("paper4_v248_bounded_two_swap_stage_summary.csv")
    assert len(stage) == status["primary_frontier_rows_v248"]
    assert (
        int(stage["base_two_swap_pair_rows_v248"].sum()) == status["base_two_swap_pair_rows_v248"]
    )
    assert (
        int(stage["source_prefilter_two_swap_pair_rows_v248"].sum())
        == status["source_prefilter_two_swap_pair_rows_v248"]
    )
    assert (
        int(stage["source_exact_two_swap_pair_rows_v248"].sum())
        == status["source_exact_two_swap_pair_rows_v248"]
    )
    assert (
        int(stage["cvar_feasible_two_swap_pair_rows_v248"].sum())
        == status["cvar_feasible_two_swap_pair_rows_v248"]
    )
    stage_map = {int(row["primary_rank_v248"]): row for _, row in stage.iterrows()}
    assert int(stage_map[0]["base_two_swap_pair_rows_v248"]) == 1204022
    assert int(stage_map[0]["source_prefilter_two_swap_pair_rows_v248"]) == 3711
    assert int(stage_map[0]["source_exact_two_swap_pair_rows_v248"]) == 575
    assert int(stage_map[4]["source_exact_two_swap_pair_rows_v248"]) == 0

    candidates = _read_csv("paper4_v248_bounded_two_swap_candidates.csv")
    assert len(candidates) == status["cvar_feasible_two_swap_pair_rows_v248"]
    assert candidates["source_swap_feasible_v248"].astype(bool).all()
    assert candidates["cvar_swap_feasible_v248"].astype(bool).all()
    assert candidates["two_swap_improves_return_v248"].astype(bool).all()
    assert int(candidates["source_cap_violations_after_two_swap_v248"].sum()) == 0
    assert candidates["total_return_delta_v248"].max() == pytest.approx(
        status["best_two_swap_return_delta_v248"]
    )

    top = _read_csv("paper4_v248_bounded_two_swap_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v248"]
    best = top.iloc[0]
    assert str(best["primary_added_loan_id_v248"]) == "158701181"
    assert str(best["primary_dropped_loan_id_v248"]) == "127392982"
    assert str(best["relief_added_loan_id_v248"]) == "127281432"
    assert str(best["relief_dropped_loan_id_v248"]) == "160560314"
    assert float(best["total_return_delta_v248"]) == pytest.approx(
        status["best_two_swap_return_delta_v248"]
    )
    assert float(best["objective_return_after_two_swap_v248"]) == pytest.approx(
        status["best_two_swap_objective_after_v248"]
    )
    assert float(best["exposure_after_two_swap_v248"]) == pytest.approx(842525.0)
    assert float(best["cvar90_after_two_swap_v248"]) == pytest.approx(
        status["best_two_swap_cvar90_after_v248"]
    )
    assert float(best["source_min_slack_after_two_swap_v248"]) == pytest.approx(
        0.00013348031248983094
    )
    assert bool(best["source_swap_feasible_v248"]) is True
    assert bool(best["cvar_swap_feasible_v248"]) is True

    summary = _read_csv("paper4_v248_bounded_two_swap_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v248"] == "bounded_two_swap_source_relief_probe"
    assert bool(row["bounded_two_swap_improvement_found_v248"]) is True
    assert bool(row["multi_swap_integer_optimality_claim_allowed_v248"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v248"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "does not prove global optimality" in str(row["claim_boundary_v248"])

    blockers = _read_csv("paper4_v248_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v248"], blockers["blocking_v248"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v248"], blockers["evidence_count_v248"], strict=False)
    )
    assert bool(blocker_map["bounded_two_swap_improvement_found"]) is True
    assert int(evidence_map["bounded_two_swap_improvement_found"]) == 625
    assert bool(blocker_map["multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v248_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v248_bounded_two_swap_probe_executed"]) is True
    assert bool(claim_map["v248_bounded_two_swap_improvement_found"]) is True
    assert bool(claim_map["v248_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v248_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v248 bounded two-swap source-relief probe."]) is True
    assert (
        bool(boundary_map["v248 finds bounded two-swap improvements over the v245 candidate."])
        is True
    )
    assert (
        bool(boundary_map["v248 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v248 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v248_rows = backlog.loc[backlog["last_wave"].eq("v248")]
    assert len(v248_rows) == 1
    backlog_row = v248_rows.iloc[0]
    assert backlog_row["status"] == "bounded_two_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v249_apply_bounded_two_swap_repair.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v248: Bounded Two-Swap Source-Relief Probe" in notebook
    assert "CVaR-feasible improving two-swap rows: `625`" in notebook
    assert "Best bounded two-swap return delta:\n  `76.49772378746906`" in notebook
    assert "not multi-swap or global integer optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v249_bounded_two_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v249_status.json")

    assert status["phase"] == "v249_bounded_two_swap_repair"
    assert status["schema_version"] == "2026-05-15.249"
    assert status["allocation_rows_v249"] == 171
    assert status["summary_rows_v249"] == 1
    assert status["action_rows_v249"] == 1
    assert status["source_summary_rows_v249"] == 51
    assert status["claim_blocker_rows_v249"] == 4
    assert status["claim_matrix_rows_v249"] == 5
    assert status["primary_added_loan_id_v249"] == "158701181"
    assert status["primary_dropped_loan_id_v249"] == "127392982"
    assert status["relief_added_loan_id_v249"] == "127281432"
    assert status["relief_dropped_loan_id_v249"] == "160560314"
    assert status["selected_rows_v249"] == 171
    assert status["portfolio_exposure_v249"] == pytest.approx(842525.0)
    assert status["objective_return_v249"] == pytest.approx(2837.1673908813555)
    assert status["scenario_loss_cvar90_v249"] == pytest.approx(97872.31896744829)
    assert status["source_cap_violations_v249"] == 0
    assert status["delta_return_vs_v245_v249"] == pytest.approx(76.49772378746457)
    assert status["delta_cvar90_vs_v245_v249"] == pytest.approx(286.62420016748365)
    assert status["delta_exposure_vs_v245_v249"] == pytest.approx(100.0)
    assert status["budget_feasible_v249"] is True
    assert status["source_feasible_v249"] is True
    assert status["cvar_feasible_v249"] is True
    assert status["repair_candidate_feasible_v249"] is True
    assert status["post_repair_local_optimality_claim_allowed_v249"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v249"] is False
    assert status["paper1_promotion_allowed_v249"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v249_bounded_two_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v249"]
    assert int(allocations["selected_v249"].sum()) == status["selected_rows_v249"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v249"])
    loan_ids = set(allocations["loan_id"].astype(str))
    assert {"158701181", "127281432"}.issubset(loan_ids)
    assert {"127392982", "160560314"}.isdisjoint(loan_ids)
    assert set(allocations["repair_action_v249"]) == {
        "added_from_v248_best_bounded_two_swap",
        "kept_from_v245",
    }
    assert allocations["claim_boundary_v249"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v249_bounded_two_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v249"] == "bounded_two_swap_repair_candidate"
    assert bool(row["repair_candidate_feasible_v249"]) is True
    assert bool(row["post_repair_pricing_required_v249"]) is True
    assert float(row["max_source_share_v249"]) == pytest.approx(0.8988457315806653)
    assert float(row["min_source_slack_v249"]) == pytest.approx(0.00013348031248983094)
    assert float(row["scenario_loss_cvar90_v249"]) <= float(row["cvar_cap_v249"])
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v249"])

    action = _read_csv("paper4_v249_bounded_two_swap_repair_action.csv").iloc[0]
    assert str(action["primary_added_loan_id_v249"]) == status["primary_added_loan_id_v249"]
    assert str(action["primary_dropped_loan_id_v249"]) == status["primary_dropped_loan_id_v249"]
    assert str(action["relief_added_loan_id_v249"]) == status["relief_added_loan_id_v249"]
    assert str(action["relief_dropped_loan_id_v249"]) == status["relief_dropped_loan_id_v249"]
    assert float(action["primary_return_delta_v249"]) == pytest.approx(43.34182985926691)
    assert float(action["relief_return_delta_v249"]) == pytest.approx(33.15589392820215)
    assert float(action["total_return_delta_v249"]) == pytest.approx(76.49772378746906)
    assert float(action["exposure_after_repair_v249"]) == pytest.approx(842525.0)
    assert int(action["source_cap_violations_after_repair_v249"]) == 0

    source_summary = _read_csv("paper4_v249_bounded_two_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v249"]
    assert not source_summary["source_cap_violated_v249"].astype(bool).any()

    blockers = _read_csv("paper4_v249_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v249"], blockers["blocking_v249"], strict=False))
    assert bool(blocker_map["bounded_two_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v249_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v249_bounded_two_swap_repair_executed"]) is True
    assert bool(claim_map["v249_repair_candidate_feasible"]) is True
    assert bool(claim_map["v249_post_repair_local_optimality"]) is False
    assert bool(claim_map["v249_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v249_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v249 bounded two-swap repair candidate."]) is True
    assert bool(boundary_map["v249 repaired portfolio is post-repair locally optimal."]) is False
    assert (
        bool(
            boundary_map["v249 replaces Paper Estrella or proves full-universe integer optimality."]
        )
        is False
    )

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v249_rows = backlog.loc[backlog["last_wave"].eq("v249")]
    assert len(v249_rows) == 1
    backlog_row = v249_rows.iloc[0]
    assert backlog_row["status"] == "post_repair_pricing_required"
    assert (
        backlog_row["next_artifact"] == "paper4_v250_post_two_swap_reprice_or_multi_swap_probe.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v249: Bounded Two-Swap Repair Candidate" in notebook
    assert "Added loans: `158701181`" in notebook
    assert "Dropped loans: `127392982`" in notebook
    assert "Return delta vs v245:\n  `76.49772378746457`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v250_post_bounded_two_swap_reprice_finds_improvements() -> None:
    status = _read_json("paper4_v250_status.json")

    assert status["phase"] == "v250_post_v249_bounded_two_swap_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.250"
    assert status["summary_rows_v250"] == 1
    assert status["stage_summary_rows_v250"] == 6
    assert status["candidate_pair_rows_v250"] == 77
    assert status["top_candidate_rows_v250"] == 77
    assert status["claim_blocker_rows_v250"] == 4
    assert status["claim_matrix_rows_v250"] == 4
    assert status["selected_rows_v250"] == 171
    assert status["candidate_add_rows_v250"] == 276698
    assert status["total_pair_rows_screened_v250"] == 47315358
    assert status["return_improving_pair_rows_v250"] == 1664059
    assert status["budget_return_feasible_pair_rows_v250"] == 1020538
    assert status["source_prefilter_pair_rows_v250"] == 163
    assert status["source_exact_pair_rows_v250"] == 77
    assert status["cvar_feasible_pair_rows_v250"] == 77
    assert status["one_swap_improving_rows_v250"] == 77
    assert status["best_one_swap_return_delta_v250"] == pytest.approx(15.229636341461529)
    assert status["best_one_swap_cvar90_after_v250"] == pytest.approx(97975.3530968413)
    assert status["current_exposure_v250"] == pytest.approx(842525.0)
    assert status["current_objective_return_v250"] == pytest.approx(2837.1673908813555)
    assert status["post_bounded_two_swap_one_swap_local_optimality_cleared_v250"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v250"] is False
    assert status["paper1_promotion_allowed_v250"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v250_post_bounded_two_swap_one_swap_reprice.csv")
    assert len(candidates) == status["candidate_pair_rows_v250"]
    assert candidates["source_swap_feasible_v250"].astype(bool).all()
    assert candidates["cvar_swap_feasible_v250"].astype(bool).all()
    assert candidates["one_swap_improves_return_v250"].astype(bool).all()
    assert int(candidates["source_cap_violations_after_swap_v250"].sum()) == 0
    assert candidates["return_delta_v250"].max() == pytest.approx(
        status["best_one_swap_return_delta_v250"]
    )

    top = _read_csv("paper4_v250_post_bounded_two_swap_one_swap_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v250"]
    best = top.iloc[0]
    assert str(best["added_loan_id_v250"]) == "161626478"
    assert str(best["dropped_loan_id_v250"]) == "129934914"
    assert float(best["added_loan_amount_v250"]) == pytest.approx(2400.0)
    assert float(best["dropped_loan_amount_v250"]) == pytest.approx(1650.0)
    assert float(best["return_delta_v250"]) == pytest.approx(
        status["best_one_swap_return_delta_v250"]
    )
    assert float(best["objective_return_after_swap_v250"]) == pytest.approx(2852.397027222817)
    assert float(best["exposure_after_swap_v250"]) == pytest.approx(843275.0)
    assert float(best["cvar90_after_swap_v250"]) == pytest.approx(
        status["best_one_swap_cvar90_after_v250"]
    )
    assert float(best["source_min_slack_after_swap_v250"]) == pytest.approx(2.741284252372189e-06)
    assert float(best["max_source_share_after_swap_v250"]) == pytest.approx(0.8989356971332009)
    assert bool(best["source_swap_feasible_v250"]) is True
    assert bool(best["cvar_swap_feasible_v250"]) is True

    summary = _read_csv("paper4_v250_post_bounded_two_swap_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v250"] == "post_v249_bounded_two_swap_repair"
    assert int(row["one_swap_improving_rows_v250"]) == 77
    assert bool(row["post_bounded_two_swap_one_swap_local_optimality_cleared_v250"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v250"]) is False
    assert bool(row["paper1_promotion_allowed_v250"]) is False
    assert bool(row["paper4_final_promotion_created_v250"]) is False
    assert "improvements remain" in str(row["claim_boundary_v250"])

    stage = _read_csv("paper4_v250_post_bounded_two_swap_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v250"]
    stage_map = dict(zip(stage["stage_v250"], stage["pair_rows_v250"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v250"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v250"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v250"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v250"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v250"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v250"]

    blockers = _read_csv("paper4_v250_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v250"], blockers["blocking_v250"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v250"], blockers["evidence_count_v250"], strict=False)
    )
    assert bool(blocker_map["post_v249_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v249_one_swap_improvement_found"]) == 77
    assert bool(blocker_map["broader_multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v250_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v250_post_v249_bounded_two_swap_one_swap_reprice_executed"])
    assert bool(claim_map["v250_post_v249_one_swap_local_optimality"]) is False
    assert bool(claim_map["v250_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v250_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert (
        bool(boundary_map["Paper 4 has a v250 post-v249 bounded two-swap one-swap pricing screen."])
        is True
    )
    assert (
        bool(
            boundary_map[
                "v250 proves the v249 bounded two-swap repaired portfolio is "
                "one-swap locally optimal."
            ]
        )
        is False
    )
    assert (
        bool(boundary_map["v250 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v250 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v250_rows = backlog.loc[backlog["last_wave"].eq("v250")]
    assert len(v250_rows) == 1
    backlog_row = v250_rows.iloc[0]
    assert backlog_row["status"] == "iterate_if_improving_swaps_remain"
    assert (
        backlog_row["next_artifact"] == "paper4_v251_apply_next_one_swap_after_two_swap_repair.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v250: Post-v249 Bounded Two-Swap One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `77`" in notebook
    assert "Best post-v249 one-swap return delta:\n  `15.229636341461529`" in notebook
    assert "broader multi-swap/global integer optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v251_one_swap_after_two_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v251_status.json")

    assert status["phase"] == "v251_next_one_swap_after_two_swap_repair"
    assert status["schema_version"] == "2026-05-15.251"
    assert status["allocation_rows_v251"] == 171
    assert status["summary_rows_v251"] == 1
    assert status["action_rows_v251"] == 1
    assert status["source_summary_rows_v251"] == 51
    assert status["claim_blocker_rows_v251"] == 5
    assert status["claim_matrix_rows_v251"] == 5
    assert status["added_loan_id_v251"] == "161626478"
    assert status["dropped_loan_id_v251"] == "129934914"
    assert status["selected_rows_v251"] == 171
    assert status["portfolio_exposure_v251"] == pytest.approx(843275.0)
    assert status["objective_return_v251"] == pytest.approx(2852.39702722282)
    assert status["scenario_loss_cvar90_v251"] == pytest.approx(97975.3530968413)
    assert status["source_cap_violations_v251"] == 0
    assert status["delta_return_vs_v249_v251"] == pytest.approx(15.229636341464357)
    assert status["delta_cvar90_vs_v249_v251"] == pytest.approx(103.03412939301052)
    assert status["delta_exposure_vs_v249_v251"] == pytest.approx(750.0)
    assert status["budget_feasible_v251"] is True
    assert status["source_feasible_v251"] is True
    assert status["cvar_feasible_v251"] is True
    assert status["repair_candidate_feasible_v251"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v251"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v251"] is False
    assert status["paper1_promotion_allowed_v251"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v251_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v251"]
    assert int(allocations["selected_v251"].sum()) == status["selected_rows_v251"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v251"])
    loan_ids = set(allocations["loan_id"].astype(str))
    assert "161626478" in loan_ids
    assert "129934914" not in loan_ids
    assert set(allocations["repair_action_v251"]) == {
        "added_from_v250_best_post_two_swap_one_swap",
        "kept_from_v249",
    }
    assert allocations["claim_boundary_v251"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v251_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v251"] == "next_one_swap_repair_candidate"
    assert bool(row["repair_candidate_feasible_v251"]) is True
    assert bool(row["post_repair_pricing_required_v251"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v251"]) is False
    assert float(row["max_source_share_v251"]) == pytest.approx(0.8989356971332009)
    assert float(row["min_source_slack_v251"]) == pytest.approx(2.741284252372189e-06)
    assert float(row["scenario_loss_cvar90_v251"]) <= float(row["cvar_cap_v251"])
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v251"])

    action = _read_csv("paper4_v251_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v251"]) == status["added_loan_id_v251"]
    assert str(action["dropped_loan_id_v251"]) == status["dropped_loan_id_v251"]
    assert float(action["added_loan_amount_v251"]) == pytest.approx(2400.0)
    assert float(action["dropped_loan_amount_v251"]) == pytest.approx(1650.0)
    assert float(action["added_mean_return_v251"]) == pytest.approx(59.91771594001454)
    assert float(action["dropped_mean_return_v251"]) == pytest.approx(44.68807959855301)
    assert float(action["return_delta_v251"]) == pytest.approx(15.229636341461529)
    assert float(action["exposure_after_repair_v251"]) == pytest.approx(843275.0)
    assert int(action["source_cap_violations_after_repair_v251"]) == 0

    source_summary = _read_csv("paper4_v251_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v251"]
    assert not source_summary["source_cap_violated_v251"].astype(bool).any()

    blockers = _read_csv("paper4_v251_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v251"], blockers["blocking_v251"], strict=False))
    assert bool(blocker_map["post_two_swap_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["broader_multi_swap_search_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v251_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v251_post_two_swap_one_swap_repair_executed"]) is True
    assert bool(claim_map["v251_repair_candidate_feasible"]) is True
    assert bool(claim_map["v251_post_repair_local_optimality"]) is False
    assert bool(claim_map["v251_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v251_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v251 one-swap repair after the v249 two-swap."])
    assert bool(boundary_map["v251 repaired portfolio is post-repair locally optimal."]) is False
    assert (
        bool(boundary_map["v251 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v251 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v251_rows = backlog.loc[backlog["last_wave"].eq("v251")]
    assert len(v251_rows) == 1
    backlog_row = v251_rows.iloc[0]
    assert backlog_row["status"] == "post_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v252_post_repair_one_swap_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v251: One-Swap Repair After the Bounded Two-Swap" in notebook
    assert "Added loan: `161626478`" in notebook
    assert "Dropped loan: `129934914`" in notebook
    assert "Return delta vs v249:\n  `15.229636341464357`" in notebook
    assert "next required experiment is v252 post-repair repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v252_post_v251_one_swap_reprice_finds_remaining_improvements() -> None:
    status = _read_json("paper4_v252_status.json")

    assert status["phase"] == "v252_post_v251_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.252"
    assert status["summary_rows_v252"] == 1
    assert status["stage_summary_rows_v252"] == 6
    assert status["candidate_pair_rows_v252"] == 6
    assert status["top_candidate_rows_v252"] == 6
    assert status["claim_blocker_rows_v252"] == 3
    assert status["selected_rows_v252"] == 171
    assert status["candidate_add_rows_v252"] == 276698
    assert status["total_pair_rows_screened_v252"] == 47315358
    assert status["return_improving_pair_rows_v252"] == 1663631
    assert status["budget_return_feasible_pair_rows_v252"] == 1000627
    assert status["source_prefilter_pair_rows_v252"] == 6432
    assert status["source_exact_pair_rows_v252"] == 6
    assert status["cvar_feasible_pair_rows_v252"] == 6
    assert status["one_swap_improving_rows_v252"] == 6
    assert status["best_one_swap_return_delta_v252"] == pytest.approx(9.00573991339527)
    assert status["best_one_swap_cvar90_after_v252"] == pytest.approx(97941.4064742405)
    assert status["post_repair_one_swap_local_optimality_cleared_v252"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v252"] is False
    assert status["paper1_promotion_allowed_v252"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v252_post_repair_one_swap_reprice.csv")
    assert len(candidates) == status["candidate_pair_rows_v252"]
    assert candidates["source_swap_feasible_v252"].astype(bool).all()
    assert candidates["cvar_swap_feasible_v252"].astype(bool).all()
    assert candidates["one_swap_improves_return_v252"].astype(bool).all()
    assert int(candidates["source_cap_violations_after_swap_v252"].sum()) == 0
    assert candidates["return_delta_v252"].max() == pytest.approx(
        status["best_one_swap_return_delta_v252"]
    )

    top = _read_csv("paper4_v252_post_repair_one_swap_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v252"]
    best = top.iloc[0]
    assert str(best["added_loan_id_v252"]) == "159991596"
    assert str(best["dropped_loan_id_v252"]) == "139684635"
    assert float(best["added_loan_amount_v252"]) == pytest.approx(1200.0)
    assert float(best["dropped_loan_amount_v252"]) == pytest.approx(1500.0)
    assert float(best["return_delta_v252"]) == pytest.approx(
        status["best_one_swap_return_delta_v252"]
    )
    assert float(best["objective_return_after_swap_v252"]) == pytest.approx(2861.402767136215)
    assert float(best["exposure_after_swap_v252"]) == pytest.approx(842975.0)
    assert float(best["cvar90_after_swap_v252"]) == pytest.approx(
        status["best_one_swap_cvar90_after_v252"]
    )
    assert float(best["source_min_slack_after_swap_v252"]) == pytest.approx(5.5008978912751694e-05)
    assert float(best["max_source_share_after_swap_v252"]) == pytest.approx(0.8992556125626502)
    assert bool(best["source_swap_feasible_v252"]) is True
    assert bool(best["cvar_swap_feasible_v252"]) is True

    summary = _read_csv("paper4_v252_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v252"] == "post_v249_bounded_two_swap_one_swap_repair"
    assert int(row["one_swap_improving_rows_v252"]) == 6
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v252"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v252"]) is False
    assert "improvements remain" in str(row["claim_boundary_v252"])

    stage = _read_csv("paper4_v252_post_repair_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v252"]
    stage_map = dict(zip(stage["stage_v252"], stage["pair_rows_v252"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v252"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v252"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v252"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v252"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v252"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v252"]

    blockers = _read_csv("paper4_v252_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v252"], blockers["blocking_v252"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v252"], blockers["evidence_count_v252"], strict=False)
    )
    assert bool(blocker_map["post_v251_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v251_one_swap_improvement_found"]) == 6
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v252_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v252_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v252_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v252_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v252_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v252 post-v251 one-swap pricing screen."])
    assert (
        bool(boundary_map["v252 proves the v251 repaired portfolio is locally optimal."]) is False
    )
    assert bool(boundary_map["v252 proves full-universe integer optimality."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v252_rows = backlog.loc[backlog["last_wave"].eq("v252")]
    assert len(v252_rows) == 1
    backlog_row = v252_rows.iloc[0]
    assert backlog_row["status"] == "iterate_if_improving_swaps_remain"
    assert backlog_row["next_artifact"] == "paper4_v253_apply_next_swap_or_iterated_repair.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v252: Post-v251 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `6`" in notebook
    assert "Best post-v251 one-swap return delta:\n  `9.00573991339527`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v253_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v253_status.json")

    assert status["phase"] == "v253_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.253"
    assert status["allocation_rows_v253"] == 171
    assert status["summary_rows_v253"] == 1
    assert status["action_rows_v253"] == 1
    assert status["source_summary_rows_v253"] == 51
    assert status["claim_blocker_rows_v253"] == 4
    assert status["added_loan_id_v253"] == "159991596"
    assert status["dropped_loan_id_v253"] == "139684635"
    assert status["selected_rows_v253"] == 171
    assert status["portfolio_exposure_v253"] == pytest.approx(842975.0)
    assert status["objective_return_v253"] == pytest.approx(2861.4027671362146)
    assert status["scenario_loss_cvar90_v253"] == pytest.approx(97941.4064742405)
    assert status["source_cap_violations_v253"] == 0
    assert status["delta_return_vs_v251_v253"] == pytest.approx(9.005739913394791)
    assert status["delta_cvar90_vs_v251_v253"] == pytest.approx(-33.94662260079349)
    assert status["delta_exposure_vs_v251_v253"] == pytest.approx(-300.0)
    assert status["budget_feasible_v253"] is True
    assert status["source_feasible_v253"] is True
    assert status["cvar_feasible_v253"] is True
    assert status["repair_candidate_feasible_v253"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v253"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v253"] is False
    assert status["paper1_promotion_allowed_v253"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v253_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v253"]
    assert int(allocations["selected_v253"].sum()) == status["selected_rows_v253"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v253"])
    loan_ids = set(allocations["loan_id"].astype(str))
    assert "159991596" in loan_ids
    assert "139684635" not in loan_ids
    assert set(allocations["repair_action_v253"]) == {
        "added_from_v252_best_swap",
        "kept_from_v251",
    }
    assert allocations["claim_boundary_v253"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v253_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v253"] == "next_one_swap_repair_candidate"
    assert bool(row["repair_candidate_feasible_v253"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v253"]) is False
    assert float(row["max_source_share_v253"]) == pytest.approx(0.8992556125626502)
    assert float(row["min_source_slack_v253"]) == pytest.approx(5.5008978912751694e-05)
    assert float(row["scenario_loss_cvar90_v253"]) <= float(row["cvar_cap_v253"])
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v253"])

    action = _read_csv("paper4_v253_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v253"]) == status["added_loan_id_v253"]
    assert str(action["dropped_loan_id_v253"]) == status["dropped_loan_id_v253"]
    assert float(action["added_loan_amount_v253"]) == pytest.approx(1200.0)
    assert float(action["dropped_loan_amount_v253"]) == pytest.approx(1500.0)
    assert float(action["added_mean_return_v253"]) == pytest.approx(-9.152330203220732)
    assert float(action["dropped_mean_return_v253"]) == pytest.approx(-18.158070116616003)
    assert float(action["return_delta_v253"]) == pytest.approx(9.00573991339527)
    assert float(action["exposure_after_repair_v253"]) == pytest.approx(842975.0)
    assert int(action["source_cap_violations_after_repair_v253"]) == 0

    source_summary = _read_csv("paper4_v253_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v253"]
    assert not source_summary["source_cap_violated_v253"].astype(bool).any()

    blockers = _read_csv("paper4_v253_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v253"], blockers["blocking_v253"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v253_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v253_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v253_repair_candidate_feasible"]) is True
    assert bool(claim_map["v253_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v253_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v253_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v253 post-v251 one-swap repair candidate."])
    assert bool(boundary_map["v253 repaired portfolio is post-repair locally optimal."]) is False
    assert (
        bool(
            boundary_map["v253 replaces Paper Estrella or proves full-universe integer optimality."]
        )
        is False
    )

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v253_rows = backlog.loc[backlog["last_wave"].eq("v253")]
    assert len(v253_rows) == 1
    backlog_row = v253_rows.iloc[0]
    assert backlog_row["status"] == "post_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v254_post_repair_one_swap_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v253: Post-V251 One-Swap Repair Candidate" in notebook
    assert "Added loan: `159991596`" in notebook
    assert "Dropped loan: `139684635`" in notebook
    assert "Return delta vs v251:\n  `9.005739913394791`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v254_post_v253_one_swap_reprice_finds_remaining_improvements() -> None:
    status = _read_json("paper4_v254_status.json")

    assert status["phase"] == "v254_post_v253_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.254"
    assert status["summary_rows_v254"] == 1
    assert status["stage_summary_rows_v254"] == 6
    assert status["candidate_pair_rows_v254"] == 11
    assert status["top_candidate_rows_v254"] == 11
    assert status["claim_blocker_rows_v254"] == 3
    assert status["selected_rows_v254"] == 171
    assert status["candidate_add_rows_v254"] == 276698
    assert status["total_pair_rows_screened_v254"] == 47315358
    assert status["return_improving_pair_rows_v254"] == 1662962
    assert status["budget_return_feasible_pair_rows_v254"] == 1036823
    assert status["source_prefilter_pair_rows_v254"] == 4532
    assert status["source_exact_pair_rows_v254"] == 11
    assert status["cvar_feasible_pair_rows_v254"] == 11
    assert status["one_swap_improving_rows_v254"] == 11
    assert status["best_one_swap_return_delta_v254"] == pytest.approx(5.178027539349671)
    assert status["best_one_swap_cvar90_after_v254"] == pytest.approx(97919.14183635268)
    assert status["post_repair_one_swap_local_optimality_cleared_v254"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v254"] is False
    assert status["paper1_promotion_allowed_v254"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v254_post_repair_one_swap_reprice.csv")
    assert len(candidates) == status["candidate_pair_rows_v254"]
    assert candidates["source_swap_feasible_v254"].astype(bool).all()
    assert candidates["cvar_swap_feasible_v254"].astype(bool).all()
    assert candidates["one_swap_improves_return_v254"].astype(bool).all()
    assert int(candidates["source_cap_violations_after_swap_v254"].sum()) == 0
    assert candidates["return_delta_v254"].max() == pytest.approx(
        status["best_one_swap_return_delta_v254"]
    )

    top = _read_csv("paper4_v254_post_repair_one_swap_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v254"]
    best = top.iloc[0]
    assert str(best["added_loan_id_v254"]) == "129934914"
    assert str(best["dropped_loan_id_v254"]) == "152268188"
    assert float(best["added_loan_amount_v254"]) == pytest.approx(1650.0)
    assert float(best["dropped_loan_amount_v254"]) == pytest.approx(1500.0)
    assert float(best["return_delta_v254"]) == pytest.approx(
        status["best_one_swap_return_delta_v254"]
    )
    assert float(best["objective_return_after_swap_v254"]) == pytest.approx(2866.5807946755644)
    assert float(best["exposure_after_swap_v254"]) == pytest.approx(843125.0)
    assert float(best["cvar90_after_swap_v254"]) == pytest.approx(
        status["best_one_swap_cvar90_after_v254"]
    )
    assert float(best["source_min_slack_after_swap_v254"]) == pytest.approx(2.8870482121190477e-05)
    assert float(best["max_source_share_after_swap_v254"]) == pytest.approx(0.8992735359525574)
    assert bool(best["source_swap_feasible_v254"]) is True
    assert bool(best["cvar_swap_feasible_v254"]) is True

    summary = _read_csv("paper4_v254_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v254"] == "post_v249_bounded_two_swap_one_swap_repair"
    assert int(row["one_swap_improving_rows_v254"]) == 11
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v254"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v254"]) is False
    assert "improvements remain" in str(row["claim_boundary_v254"])

    stage = _read_csv("paper4_v254_post_repair_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v254"]
    stage_map = dict(zip(stage["stage_v254"], stage["pair_rows_v254"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v254"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v254"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v254"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v254"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v254"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v254"]

    blockers = _read_csv("paper4_v254_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v254"], blockers["blocking_v254"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v254"], blockers["evidence_count_v254"], strict=False)
    )
    assert bool(blocker_map["post_v253_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v253_one_swap_improvement_found"]) == 11
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v254_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v254_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v254_post_repair_one_swap_local_optimality"]) is False
    assert bool(claim_map["v254_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v254_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v254 post-v253 one-swap pricing screen."])
    assert (
        bool(boundary_map["v254 proves the v253 repaired portfolio is locally optimal."]) is False
    )
    assert bool(boundary_map["v254 proves full-universe integer optimality."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v254_rows = backlog.loc[backlog["last_wave"].eq("v254")]
    assert len(v254_rows) == 1
    backlog_row = v254_rows.iloc[0]
    assert backlog_row["status"] == "iterate_if_improving_swaps_remain"
    assert backlog_row["next_artifact"] == "paper4_v255_apply_next_swap_or_iterated_repair.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v254: Post-v253 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `11`" in notebook
    assert "Best post-v253 one-swap return delta:\n  `5.178027539349671`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v255_next_one_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v255_status.json")

    assert status["phase"] == "v255_next_one_swap_repair"
    assert status["schema_version"] == "2026-05-15.255"
    assert status["allocation_rows_v255"] == 171
    assert status["summary_rows_v255"] == 1
    assert status["action_rows_v255"] == 1
    assert status["source_summary_rows_v255"] == 51
    assert status["claim_blocker_rows_v255"] == 4
    assert status["added_loan_id_v255"] == "129934914"
    assert status["dropped_loan_id_v255"] == "152268188"
    assert status["selected_rows_v255"] == 171
    assert status["portfolio_exposure_v255"] == pytest.approx(843125.0)
    assert status["objective_return_v255"] == pytest.approx(2866.5807946755667)
    assert status["scenario_loss_cvar90_v255"] == pytest.approx(97919.14183635266)
    assert status["source_cap_violations_v255"] == 0
    assert status["delta_return_vs_v253_v255"] == pytest.approx(5.178027539352115)
    assert status["delta_cvar90_vs_v253_v255"] == pytest.approx(-22.264637887841673)
    assert status["delta_exposure_vs_v253_v255"] == pytest.approx(150.0)
    assert status["budget_feasible_v255"] is True
    assert status["source_feasible_v255"] is True
    assert status["cvar_feasible_v255"] is True
    assert status["repair_candidate_feasible_v255"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v255"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v255"] is False
    assert status["paper1_promotion_allowed_v255"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v255_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v255"]
    assert int(allocations["selected_v255"].sum()) == status["selected_rows_v255"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v255"])
    loan_ids = set(allocations["loan_id"].astype(str))
    assert "129934914" in loan_ids
    assert "152268188" not in loan_ids
    assert set(allocations["repair_action_v255"]) == {
        "added_from_v254_best_swap",
        "kept_from_v253",
    }
    assert allocations["claim_boundary_v255"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v255_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v255"] == "next_one_swap_repair_candidate"
    assert bool(row["repair_candidate_feasible_v255"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v255"]) is False
    assert float(row["max_source_share_v255"]) == pytest.approx(0.8992735359525574)
    assert float(row["min_source_slack_v255"]) == pytest.approx(2.8870482121190477e-05)
    assert float(row["scenario_loss_cvar90_v255"]) <= float(row["cvar_cap_v255"])
    assert "must rerun omitted-universe pricing" in str(row["claim_boundary_v255"])

    action = _read_csv("paper4_v255_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v255"]) == status["added_loan_id_v255"]
    assert str(action["dropped_loan_id_v255"]) == status["dropped_loan_id_v255"]
    assert float(action["added_loan_amount_v255"]) == pytest.approx(1650.0)
    assert float(action["dropped_loan_amount_v255"]) == pytest.approx(1500.0)
    assert float(action["added_mean_return_v255"]) == pytest.approx(44.68807959855301)
    assert float(action["dropped_mean_return_v255"]) == pytest.approx(39.51005205920334)
    assert float(action["return_delta_v255"]) == pytest.approx(5.178027539349671)
    assert float(action["exposure_after_repair_v255"]) == pytest.approx(843125.0)
    assert int(action["source_cap_violations_after_repair_v255"]) == 0

    source_summary = _read_csv("paper4_v255_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v255"]
    assert not source_summary["source_cap_violated_v255"].astype(bool).any()

    blockers = _read_csv("paper4_v255_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v255"], blockers["blocking_v255"], strict=False))
    assert bool(blocker_map["next_one_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v255_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v255_next_one_swap_repair_executed"]) is True
    assert bool(claim_map["v255_repair_candidate_feasible"]) is True
    assert bool(claim_map["v255_post_repair_one_swap_optimality"]) is False
    assert bool(claim_map["v255_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v255_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v255 post-v253 one-swap repair candidate."])
    assert bool(boundary_map["v255 repaired portfolio is post-repair locally optimal."]) is False
    assert (
        bool(
            boundary_map["v255 replaces Paper Estrella or proves full-universe integer optimality."]
        )
        is False
    )

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v255_rows = backlog.loc[backlog["last_wave"].eq("v255")]
    assert len(v255_rows) == 1
    backlog_row = v255_rows.iloc[0]
    assert backlog_row["status"] == "post_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v256_post_repair_one_swap_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v255: Post-V253 One-Swap Repair Candidate" in notebook
    assert "Added loan: `129934914`" in notebook
    assert "Dropped loan: `152268188`" in notebook
    assert "Return delta vs v253:\n  `5.178027539352115`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v256_post_v255_one_swap_reprice_clears_local_screen() -> None:
    status = _read_json("paper4_v256_status.json")

    assert status["phase"] == "v256_post_v255_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.256"
    assert status["summary_rows_v256"] == 1
    assert status["stage_summary_rows_v256"] == 6
    assert status["candidate_pair_rows_v256"] == 0
    assert status["top_candidate_rows_v256"] == 0
    assert status["claim_blocker_rows_v256"] == 3
    assert status["selected_rows_v256"] == 171
    assert status["candidate_add_rows_v256"] == 276698
    assert status["total_pair_rows_screened_v256"] == 47315358
    assert status["return_improving_pair_rows_v256"] == 1662798
    assert status["budget_return_feasible_pair_rows_v256"] == 1006426
    assert status["source_prefilter_pair_rows_v256"] == 5955
    assert status["source_exact_pair_rows_v256"] == 0
    assert status["cvar_feasible_pair_rows_v256"] == 0
    assert status["one_swap_improving_rows_v256"] == 0
    assert status["best_one_swap_return_delta_v256"] is None
    assert status["best_one_swap_cvar90_after_v256"] is None
    assert status["post_repair_one_swap_local_optimality_cleared_v256"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v256"] is False
    assert status["paper1_promotion_allowed_v256"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v256_post_repair_one_swap_reprice.csv")
    top = _read_csv("paper4_v256_post_repair_one_swap_top_candidates.csv")
    assert len(candidates) == 0
    assert len(top) == 0

    summary = _read_csv("paper4_v256_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v256"] == "post_v249_bounded_two_swap_one_swap_repair"
    assert int(row["one_swap_improving_rows_v256"]) == 0
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v256"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v256"]) is False
    assert "multi-swap/global proof still missing" in str(row["claim_boundary_v256"])

    stage = _read_csv("paper4_v256_post_repair_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v256"]
    stage_map = dict(zip(stage["stage_v256"], stage["pair_rows_v256"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v256"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v256"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v256"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v256"]
    assert int(stage_map["source_exact_feasible"]) == 0
    assert int(stage_map["cvar_feasible_improving"]) == 0

    blockers = _read_csv("paper4_v256_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v256"], blockers["blocking_v256"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v256"], blockers["evidence_count_v256"], strict=False)
    )
    assert bool(blocker_map["post_v255_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v255_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v256_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v256_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v256_post_repair_one_swap_local_optimality"]) is True
    assert bool(claim_map["v256_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v256_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v256 post-v255 one-swap pricing screen."])
    assert bool(boundary_map["v256 proves the v255 repaired portfolio is locally optimal."])
    assert bool(boundary_map["v256 proves full-universe integer optimality."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v256_rows = backlog.loc[backlog["last_wave"].eq("v256")]
    assert len(v256_rows) == 1
    backlog_row = v256_rows.iloc[0]
    assert backlog_row["status"] == "one_swap_local_optimality_cleared"
    assert backlog_row["next_artifact"] == "paper4_v256_multi_swap_or_global_gap_protocol.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v256: Post-v255 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best post-v255 one-swap return delta:\n  `not applicable; no feasible improving one-swaps`"
        in notebook
    )
    assert "Post-v255 local optimality cleared:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v257_bounded_two_swap_probe_finds_post_clearance_improvements() -> None:
    status = _read_json("paper4_v257_status.json")

    assert status["phase"] == "v257_bounded_two_swap_after_v256_local_clearance"
    assert status["schema_version"] == "2026-05-15.257"
    assert status["previous_repair_version_v257"] == 255
    assert status["terminal_reprice_version_v257"] == 256
    assert status["primary_frontier_limit_v257"] == 25
    assert status["primary_frontier_rows_v257"] == 25
    assert status["one_swap_return_improving_pair_rows_v257"] == 1662798
    assert status["one_swap_budget_return_feasible_pair_rows_v257"] == 1006426
    assert status["one_swap_source_prefilter_pair_rows_v257"] == 5955
    assert status["base_two_swap_pair_rows_v257"] == 37731910
    assert status["source_prefilter_two_swap_pair_rows_v257"] == 585378
    assert status["source_exact_two_swap_pair_rows_v257"] == 28
    assert status["cvar_feasible_two_swap_pair_rows_v257"] == 28
    assert status["top_candidate_rows_v257"] == 28
    assert status["best_two_swap_return_delta_v257"] == pytest.approx(22.614910791374086)
    assert status["best_two_swap_cvar90_after_v257"] == pytest.approx(97930.33594179785)
    assert status["best_two_swap_objective_after_v257"] == pytest.approx(2889.195705466941)
    assert status["bounded_two_swap_improvement_found_v257"] is True
    assert status["multi_swap_integer_optimality_claim_allowed_v257"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v257"] is False
    assert status["paper1_promotion_allowed_v257"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v257"] == 4
    assert status["claim_matrix_rows_v257"] == 4

    primary = _read_csv("paper4_v257_bounded_two_swap_primary_frontier.csv")
    assert len(primary) == status["primary_frontier_rows_v257"]
    best_primary = primary.iloc[0]
    assert str(best_primary["primary_added_loan_id_v257"]) == "153077076"
    assert str(best_primary["primary_dropped_loan_id_v257"]) == "148025050"
    assert float(best_primary["primary_return_delta_v257"]) == pytest.approx(133.52080418811724)
    assert int(best_primary["primary_source_cap_violations_v257"]) >= 1
    assert best_primary["claim_boundary_v257"].startswith("bounded primary one-swap")

    stage = _read_csv("paper4_v257_bounded_two_swap_stage_summary.csv")
    assert len(stage) == status["primary_frontier_rows_v257"]
    assert (
        int(stage["base_two_swap_pair_rows_v257"].sum()) == status["base_two_swap_pair_rows_v257"]
    )
    assert (
        int(stage["source_prefilter_two_swap_pair_rows_v257"].sum())
        == status["source_prefilter_two_swap_pair_rows_v257"]
    )
    assert (
        int(stage["source_exact_two_swap_pair_rows_v257"].sum())
        == status["source_exact_two_swap_pair_rows_v257"]
    )
    assert (
        int(stage["cvar_feasible_two_swap_pair_rows_v257"].sum())
        == status["cvar_feasible_two_swap_pair_rows_v257"]
    )
    stage_map = {int(row["primary_rank_v257"]): row for _, row in stage.iterrows()}
    assert int(stage_map[0]["base_two_swap_pair_rows_v257"]) == 1641639
    assert int(stage_map[0]["source_prefilter_two_swap_pair_rows_v257"]) == 42765
    assert int(stage_map[0]["source_exact_two_swap_pair_rows_v257"]) == 6
    assert int(stage_map[8]["source_exact_two_swap_pair_rows_v257"]) == 0

    candidates = _read_csv("paper4_v257_bounded_two_swap_candidates.csv")
    assert len(candidates) == status["source_exact_two_swap_pair_rows_v257"]
    assert candidates["source_swap_feasible_v257"].astype(bool).all()
    assert candidates["cvar_swap_feasible_v257"].astype(bool).all()
    assert candidates["two_swap_improves_return_v257"].astype(bool).all()
    assert int(candidates["source_cap_violations_after_two_swap_v257"].sum()) == 0
    assert candidates["total_return_delta_v257"].max() == pytest.approx(
        status["best_two_swap_return_delta_v257"]
    )

    top = _read_csv("paper4_v257_bounded_two_swap_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v257"]
    best = top.iloc[0]
    assert str(best["primary_added_loan_id_v257"]) == "153077076"
    assert str(best["primary_dropped_loan_id_v257"]) == "148025050"
    assert str(best["relief_added_loan_id_v257"]) == "158739818"
    assert str(best["relief_dropped_loan_id_v257"]) == "158940609"
    assert float(best["primary_return_delta_v257"]) == pytest.approx(133.52080418811724)
    assert float(best["relief_return_delta_v257"]) == pytest.approx(-110.90589339674318)
    assert float(best["total_return_delta_v257"]) == pytest.approx(
        status["best_two_swap_return_delta_v257"]
    )
    assert float(best["objective_return_after_two_swap_v257"]) == pytest.approx(
        status["best_two_swap_objective_after_v257"]
    )
    assert float(best["exposure_after_two_swap_v257"]) == pytest.approx(843325.0)
    assert float(best["cvar90_after_two_swap_v257"]) == pytest.approx(
        status["best_two_swap_cvar90_after_v257"]
    )
    assert float(best["source_min_slack_after_two_swap_v257"]) == pytest.approx(
        0.00023119011084038465
    )
    assert float(best["max_source_share_after_two_swap_v257"]) == pytest.approx(0.8990602673939466)
    assert bool(best["source_swap_feasible_v257"]) is True
    assert bool(best["cvar_swap_feasible_v257"]) is True

    summary = _read_csv("paper4_v257_bounded_two_swap_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v257"] == "bounded_two_swap_after_v256_local_clearance"
    assert bool(row["bounded_two_swap_improvement_found_v257"]) is True
    assert bool(row["multi_swap_integer_optimality_claim_allowed_v257"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v257"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "no exhaustive multi-swap/global proof" in str(row["claim_boundary_v257"])

    blockers = _read_csv("paper4_v257_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v257"], blockers["blocking_v257"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v257"], blockers["evidence_count_v257"], strict=False)
    )
    assert bool(blocker_map["bounded_two_swap_improvement_found"]) is True
    assert int(evidence_map["bounded_two_swap_improvement_found"]) == 28
    assert bool(blocker_map["multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v257_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v257_bounded_two_swap_probe_executed"]) is True
    assert bool(claim_map["v257_bounded_two_swap_improvement_found"]) is True
    assert bool(claim_map["v257_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v257_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert (
        bool(boundary_map["Paper 4 has a v257 bounded two-swap source-relief probe after v256."])
        is True
    )
    assert (
        bool(boundary_map["v257 finds bounded two-swap improvements over the v255 candidate."])
        is True
    )
    assert (
        bool(boundary_map["v257 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v257 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v257_rows = backlog.loc[backlog["last_wave"].eq("v257")]
    assert len(v257_rows) == 1
    backlog_row = v257_rows.iloc[0]
    assert backlog_row["status"] == "bounded_two_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v258_apply_bounded_two_swap_repair.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v257: Bounded Two-Swap Probe After v256 Local Clearance" in notebook
    assert "CVaR-feasible improving two-swap rows:\n  `28`" in notebook
    assert "Best bounded two-swap return delta:\n  `22.614910791374086`" in notebook
    assert "bounded multi-swap evidence after the one-swap loop terminates" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v258_bounded_two_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v258_status.json")

    assert status["phase"] == "v258_bounded_two_swap_repair"
    assert status["schema_version"] == "2026-05-15.258"
    assert status["allocation_rows_v258"] == 171
    assert status["summary_rows_v258"] == 1
    assert status["action_rows_v258"] == 1
    assert status["source_summary_rows_v258"] == 51
    assert status["claim_blocker_rows_v258"] == 4
    assert status["claim_matrix_rows_v258"] == 5
    assert status["primary_added_loan_id_v258"] == "153077076"
    assert status["primary_dropped_loan_id_v258"] == "148025050"
    assert status["relief_added_loan_id_v258"] == "158739818"
    assert status["relief_dropped_loan_id_v258"] == "158940609"
    assert status["selected_rows_v258"] == 171
    assert status["portfolio_exposure_v258"] == pytest.approx(843325.0)
    assert status["objective_return_v258"] == pytest.approx(2889.1957054669383)
    assert status["scenario_loss_cvar90_v258"] == pytest.approx(97930.33594179785)
    assert status["source_cap_violations_v258"] == 0
    assert status["delta_return_vs_v255_v258"] == pytest.approx(22.614910791371585)
    assert status["delta_cvar90_vs_v255_v258"] == pytest.approx(11.194105445189052)
    assert status["delta_exposure_vs_v255_v258"] == pytest.approx(200.0)
    assert status["budget_feasible_v258"] is True
    assert status["source_feasible_v258"] is True
    assert status["cvar_feasible_v258"] is True
    assert status["repair_candidate_feasible_v258"] is True
    assert status["post_repair_local_optimality_claim_allowed_v258"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v258"] is False
    assert status["paper1_promotion_allowed_v258"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v258_bounded_two_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v258"]
    assert int(allocations["selected_v258"].sum()) == status["selected_rows_v258"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v258"])
    loan_ids = set(allocations["loan_id"].astype(str))
    assert {"153077076", "158739818"}.issubset(loan_ids)
    assert {"148025050", "158940609"}.isdisjoint(loan_ids)
    assert set(allocations["repair_action_v258"]) == {
        "added_from_v257_best_bounded_two_swap",
        "kept_from_v255",
    }
    assert allocations["claim_boundary_v258"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v258_bounded_two_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v258"] == "bounded_two_swap_repair_candidate"
    assert bool(row["repair_candidate_feasible_v258"]) is True
    assert bool(row["post_repair_pricing_required_v258"]) is True
    assert float(row["max_source_share_v258"]) == pytest.approx(0.8990602673939466)
    assert float(row["min_source_slack_v258"]) == pytest.approx(0.00023119011084038465)
    assert float(row["scenario_loss_cvar90_v258"]) <= float(row["cvar_cap_v258"])
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v258"])

    action = _read_csv("paper4_v258_bounded_two_swap_repair_action.csv").iloc[0]
    assert str(action["primary_added_loan_id_v258"]) == status["primary_added_loan_id_v258"]
    assert str(action["primary_dropped_loan_id_v258"]) == status["primary_dropped_loan_id_v258"]
    assert str(action["relief_added_loan_id_v258"]) == status["relief_added_loan_id_v258"]
    assert str(action["relief_dropped_loan_id_v258"]) == status["relief_dropped_loan_id_v258"]
    assert float(action["primary_return_delta_v258"]) == pytest.approx(133.52080418811724)
    assert float(action["relief_return_delta_v258"]) == pytest.approx(-110.90589339674318)
    assert float(action["total_return_delta_v258"]) == pytest.approx(22.614910791374086)
    assert float(action["exposure_after_repair_v258"]) == pytest.approx(843325.0)
    assert int(action["source_cap_violations_after_repair_v258"]) == 0

    source_summary = _read_csv("paper4_v258_bounded_two_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v258"]
    assert not source_summary["source_cap_violated_v258"].astype(bool).any()

    blockers = _read_csv("paper4_v258_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v258"], blockers["blocking_v258"], strict=False))
    assert bool(blocker_map["bounded_two_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v258_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v258_bounded_two_swap_repair_executed"]) is True
    assert bool(claim_map["v258_repair_candidate_feasible"]) is True
    assert bool(claim_map["v258_post_repair_local_optimality"]) is False
    assert bool(claim_map["v258_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v258_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v258 bounded two-swap repair candidate."]) is True
    assert bool(boundary_map["v258 repaired portfolio is post-repair locally optimal."]) is False
    assert (
        bool(boundary_map["v258 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v258 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v258_rows = backlog.loc[backlog["last_wave"].eq("v258")]
    assert len(v258_rows) == 1
    backlog_row = v258_rows.iloc[0]
    assert backlog_row["status"] == "post_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v259_post_bounded_two_swap_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v258: Bounded Two-Swap Repair Candidate" in notebook
    assert "Added loans: `153077076`" in notebook
    assert "Dropped loans: `148025050`" in notebook
    assert "Return delta vs v255:\n  `22.614910791371585`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v259_post_bounded_two_swap_reprice_finds_improvements() -> None:
    status = _read_json("paper4_v259_status.json")

    assert status["phase"] == "v259_post_v258_bounded_two_swap_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.259"
    assert status["summary_rows_v259"] == 1
    assert status["stage_summary_rows_v259"] == 6
    assert status["candidate_pair_rows_v259"] == 604
    assert status["top_candidate_rows_v259"] == 200
    assert status["claim_blocker_rows_v259"] == 4
    assert status["claim_matrix_rows_v259"] == 4
    assert status["selected_rows_v259"] == 171
    assert status["candidate_add_rows_v259"] == 276698
    assert status["total_pair_rows_screened_v259"] == 47315358
    assert status["return_improving_pair_rows_v259"] == 1660068
    assert status["budget_return_feasible_pair_rows_v259"] == 1008750
    assert status["source_prefilter_pair_rows_v259"] == 12790
    assert status["source_exact_pair_rows_v259"] == 604
    assert status["cvar_feasible_pair_rows_v259"] == 604
    assert status["one_swap_improving_rows_v259"] == 604
    assert status["best_one_swap_return_delta_v259"] == pytest.approx(44.24089021272701)
    assert status["best_one_swap_cvar90_after_v259"] == pytest.approx(97875.45699152809)
    assert status["current_exposure_v259"] == pytest.approx(843325.0)
    assert status["current_objective_return_v259"] == pytest.approx(2889.1957054669383)
    assert status["post_bounded_two_swap_one_swap_local_optimality_cleared_v259"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v259"] is False
    assert status["paper1_promotion_allowed_v259"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v259_post_bounded_two_swap_one_swap_reprice.csv")
    assert len(candidates) == status["candidate_pair_rows_v259"]
    assert candidates["source_swap_feasible_v259"].astype(bool).all()
    assert candidates["cvar_swap_feasible_v259"].astype(bool).all()
    assert candidates["one_swap_improves_return_v259"].astype(bool).all()
    assert int(candidates["source_cap_violations_after_swap_v259"].sum()) == 0
    assert candidates["return_delta_v259"].max() == pytest.approx(
        status["best_one_swap_return_delta_v259"]
    )

    top = _read_csv("paper4_v259_post_bounded_two_swap_one_swap_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v259"]
    best = top.iloc[0]
    assert str(best["added_loan_id_v259"]) == "157560196"
    assert str(best["dropped_loan_id_v259"]) == "140352540"
    assert float(best["added_loan_amount_v259"]) == pytest.approx(1300.0)
    assert float(best["dropped_loan_amount_v259"]) == pytest.approx(1500.0)
    assert float(best["return_delta_v259"]) == pytest.approx(
        status["best_one_swap_return_delta_v259"]
    )
    assert float(best["objective_return_after_swap_v259"]) == pytest.approx(2933.4365956796655)
    assert float(best["exposure_after_swap_v259"]) == pytest.approx(843125.0)
    assert float(best["cvar90_after_swap_v259"]) == pytest.approx(
        status["best_one_swap_cvar90_after_v259"]
    )
    assert float(best["source_min_slack_after_swap_v259"]) == pytest.approx(2.8870482121190477e-05)
    assert float(best["max_source_share_after_swap_v259"]) == pytest.approx(0.8990363232023721)
    assert bool(best["source_swap_feasible_v259"]) is True
    assert bool(best["cvar_swap_feasible_v259"]) is True

    summary = _read_csv("paper4_v259_post_bounded_two_swap_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v259"] == "post_v258_bounded_two_swap_repair"
    assert int(row["one_swap_improving_rows_v259"]) == 604
    assert bool(row["post_bounded_two_swap_one_swap_local_optimality_cleared_v259"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v259"]) is False
    assert bool(row["paper1_promotion_allowed_v259"]) is False
    assert bool(row["paper4_final_promotion_created_v259"]) is False
    assert "improvements remain" in str(row["claim_boundary_v259"])

    stage = _read_csv("paper4_v259_post_bounded_two_swap_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v259"]
    stage_map = dict(zip(stage["stage_v259"], stage["pair_rows_v259"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v259"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v259"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v259"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v259"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v259"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v259"]

    blockers = _read_csv("paper4_v259_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v259"], blockers["blocking_v259"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v259"], blockers["evidence_count_v259"], strict=False)
    )
    assert bool(blocker_map["post_v258_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v258_one_swap_improvement_found"]) == 604
    assert bool(blocker_map["broader_multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v259_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v259_post_v258_bounded_two_swap_one_swap_reprice_executed"])
    assert bool(claim_map["v259_post_v258_one_swap_local_optimality"]) is False
    assert bool(claim_map["v259_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v259_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert (
        bool(boundary_map["Paper 4 has a v259 post-v258 bounded two-swap one-swap pricing screen."])
        is True
    )
    assert (
        bool(
            boundary_map[
                "v259 proves the v258 bounded two-swap repaired portfolio is "
                "one-swap locally optimal."
            ]
        )
        is False
    )
    assert (
        bool(boundary_map["v259 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v259 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v259_rows = backlog.loc[backlog["last_wave"].eq("v259")]
    assert len(v259_rows) == 1
    backlog_row = v259_rows.iloc[0]
    assert backlog_row["status"] == "iterate_if_improving_swaps_remain"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v260_apply_next_one_swap_after_bounded_two_swap_repair.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v259: Post-v258 Bounded Two-Swap One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `604`" in notebook
    assert "Best post-v258 one-swap return delta:\n  `44.24089021272701`" in notebook
    assert "broader multi-swap/global integer evidence" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v260_next_one_swap_after_bounded_two_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v260_status.json")

    assert status["phase"] == "v260_next_one_swap_after_bounded_two_swap_repair"
    assert status["schema_version"] == "2026-05-15.260"
    assert status["allocation_rows_v260"] == 171
    assert status["summary_rows_v260"] == 1
    assert status["action_rows_v260"] == 1
    assert status["source_summary_rows_v260"] == 51
    assert status["claim_blocker_rows_v260"] == 5
    assert status["claim_matrix_rows_v260"] == 5
    assert status["added_loan_id_v260"] == "157560196"
    assert status["dropped_loan_id_v260"] == "140352540"
    assert status["selected_rows_v260"] == 171
    assert status["portfolio_exposure_v260"] == pytest.approx(843125.0)
    assert status["objective_return_v260"] == pytest.approx(2933.4365956796664)
    assert status["scenario_loss_cvar90_v260"] == pytest.approx(97875.45699152809)
    assert status["source_cap_violations_v260"] == 0
    assert status["delta_return_vs_v258_v260"] == pytest.approx(44.24089021272812)
    assert status["delta_cvar90_vs_v258_v260"] == pytest.approx(-54.87895026974729)
    assert status["delta_exposure_vs_v258_v260"] == pytest.approx(-200.0)
    assert status["budget_feasible_v260"] is True
    assert status["source_feasible_v260"] is True
    assert status["cvar_feasible_v260"] is True
    assert status["repair_candidate_feasible_v260"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v260"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v260"] is False
    assert status["paper1_promotion_allowed_v260"] is False
    assert status["paper4_working_champion_changed_v260"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v260_next_one_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v260"]
    assert int(allocations["selected_v260"].sum()) == status["selected_rows_v260"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v260"])
    allocation_ids = set(allocations["loan_id"].astype(str))
    assert status["added_loan_id_v260"] in allocation_ids
    assert status["dropped_loan_id_v260"] not in allocation_ids
    assert set(allocations["repair_action_v260"]) == {
        "added_from_v259_best_post_bounded_two_swap_one_swap",
        "kept_from_v258",
    }
    assert allocations["claim_boundary_v260"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v260_next_one_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v260"] == "next_one_swap_repair_candidate"
    assert bool(row["repair_candidate_feasible_v260"]) is True
    assert bool(row["post_repair_pricing_required_v260"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v260"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v260"]) is False
    assert float(row["max_source_share_v260"]) == pytest.approx(0.8990363232023721)
    assert float(row["min_source_slack_v260"]) == pytest.approx(2.887048212119048e-05)
    assert float(row["scenario_loss_cvar90_v260"]) <= float(row["cvar_cap_v260"])
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v260"])

    action = _read_csv("paper4_v260_next_one_swap_repair_action.csv").iloc[0]
    assert str(action["added_loan_id_v260"]) == status["added_loan_id_v260"]
    assert str(action["dropped_loan_id_v260"]) == status["dropped_loan_id_v260"]
    assert float(action["added_loan_amount_v260"]) == pytest.approx(1300.0)
    assert float(action["dropped_loan_amount_v260"]) == pytest.approx(1500.0)
    assert float(action["added_mean_return_v260"]) == pytest.approx(-181.11178871668363)
    assert float(action["dropped_mean_return_v260"]) == pytest.approx(-225.35267892941064)
    assert float(action["return_delta_v260"]) == pytest.approx(44.24089021272701)
    assert float(action["exposure_after_repair_v260"]) == pytest.approx(843125.0)
    assert int(action["source_cap_violations_after_repair_v260"]) == 0

    source_summary = _read_csv("paper4_v260_next_one_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v260"]
    assert not source_summary["source_cap_violated_v260"].astype(bool).any()

    blockers = _read_csv("paper4_v260_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v260"], blockers["blocking_v260"], strict=False))
    assert bool(blocker_map["post_bounded_two_swap_one_swap_repair_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["broader_multi_swap_search_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v260_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v260_post_bounded_two_swap_one_swap_repair_executed"]) is True
    assert bool(claim_map["v260_repair_candidate_feasible"]) is True
    assert bool(claim_map["v260_post_repair_local_optimality"]) is False
    assert bool(claim_map["v260_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v260_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v260 one-swap repair after the v258 two-swap."])
    assert bool(boundary_map["v260 repaired portfolio is post-repair locally optimal."]) is False
    assert (
        bool(boundary_map["v260 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v260 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v260_rows = backlog.loc[backlog["last_wave"].eq("v260")]
    assert len(v260_rows) == 1
    backlog_row = v260_rows.iloc[0]
    assert backlog_row["status"] == "post_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v261_post_repair_one_swap_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v260: One-Swap Repair After the v258 Bounded Two-Swap" in notebook
    assert "Added loan: `157560196`" in notebook
    assert "Dropped loan: `140352540`" in notebook
    assert "Return delta vs v258:\n  `44.24089021272812`" in notebook
    assert "post-repair repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v261_post_v260_one_swap_reprice_clears_local_screen() -> None:
    status = _read_json("paper4_v261_status.json")

    assert status["phase"] == "v261_post_v260_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.261"
    assert status["summary_rows_v261"] == 1
    assert status["stage_summary_rows_v261"] == 6
    assert status["candidate_pair_rows_v261"] == 0
    assert status["top_candidate_rows_v261"] == 0
    assert status["claim_blocker_rows_v261"] == 3
    assert status["selected_rows_v261"] == 171
    assert status["candidate_add_rows_v261"] == 276698
    assert status["total_pair_rows_screened_v261"] == 47315358
    assert status["return_improving_pair_rows_v261"] == 1656897
    assert status["budget_return_feasible_pair_rows_v261"] == 1001719
    assert status["source_prefilter_pair_rows_v261"] == 5257
    assert status["source_exact_pair_rows_v261"] == 0
    assert status["cvar_feasible_pair_rows_v261"] == 0
    assert status["one_swap_improving_rows_v261"] == 0
    assert status["best_one_swap_return_delta_v261"] is None
    assert status["best_one_swap_cvar90_after_v261"] is None
    assert status["post_repair_one_swap_local_optimality_cleared_v261"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v261"] is False
    assert status["paper1_promotion_allowed_v261"] is False
    assert status["paper4_working_champion_changed_v261"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v261_post_repair_one_swap_reprice.csv")
    top = _read_csv("paper4_v261_post_repair_one_swap_top_candidates.csv")
    assert candidates.empty
    assert top.empty
    assert "added_loan_id_v261" in candidates.columns
    assert "return_delta_v261" in top.columns

    summary = _read_csv("paper4_v261_post_repair_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v261"] == "post_v258_bounded_two_swap_one_swap_repair"
    assert int(row["selected_rows_v261"]) == status["selected_rows_v261"]
    assert int(row["one_swap_improving_rows_v261"]) == 0
    assert float(row["current_exposure_v261"]) == pytest.approx(843125.0)
    assert float(row["current_objective_return_v261"]) == pytest.approx(2933.4365956796664)
    assert bool(row["post_repair_one_swap_local_optimality_cleared_v261"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v261"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v261"])

    stage = _read_csv("paper4_v261_post_repair_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v261"]
    stage_map = dict(zip(stage["stage_v261"], stage["pair_rows_v261"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v261"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v261"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v261"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v261"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v261"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v261"]

    blockers = _read_csv("paper4_v261_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v261"], blockers["blocking_v261"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v261"], blockers["evidence_count_v261"], strict=False)
    )
    assert bool(blocker_map["post_v260_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v260_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["multi_swap_integer_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True

    claim_delta = _read_csv("paper4_v261_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v261_post_repair_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v261_post_repair_one_swap_local_optimality"]) is True
    assert bool(claim_map["v261_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v261_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v261 post-v260 one-swap pricing screen."])
    assert bool(boundary_map["v261 proves the v260 repaired portfolio is locally optimal."])
    assert bool(boundary_map["v261 proves full-universe integer optimality."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v261_rows = backlog.loc[backlog["last_wave"].eq("v261")]
    assert len(v261_rows) == 1
    backlog_row = v261_rows.iloc[0]
    assert backlog_row["status"] == "one_swap_local_optimality_cleared"
    assert backlog_row["next_artifact"] == "paper4_v261_multi_swap_or_global_gap_protocol.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v261: Post-v260 One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best post-v260 one-swap return delta:\n  `not applicable; no feasible improving one-swaps`"
    ) in notebook
    assert "Post-v260 local optimality cleared:\n  `True`" in notebook
    assert "multi-swap/global integer evidence" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v262_bounded_two_swap_probe_finds_post_v260_improvements() -> None:
    status = _read_json("paper4_v262_status.json")

    assert status["phase"] == "v262_bounded_two_swap_after_v261_local_clearance"
    assert status["schema_version"] == "2026-05-15.262"
    assert status["previous_repair_version_v262"] == 260
    assert status["terminal_reprice_version_v262"] == 261
    assert status["primary_frontier_limit_v262"] == 25
    assert status["primary_frontier_rows_v262"] == 25
    assert status["one_swap_return_improving_pair_rows_v262"] == 1656897
    assert status["one_swap_budget_return_feasible_pair_rows_v262"] == 1001719
    assert status["one_swap_source_prefilter_pair_rows_v262"] == 5257
    assert status["base_two_swap_pair_rows_v262"] == 35465801
    assert status["source_prefilter_two_swap_pair_rows_v262"] == 465239
    assert status["source_exact_two_swap_pair_rows_v262"] == 264
    assert status["cvar_feasible_two_swap_pair_rows_v262"] == 264
    assert status["top_candidate_rows_v262"] == 200
    assert status["best_two_swap_return_delta_v262"] == pytest.approx(32.96097682572454)
    assert status["best_two_swap_cvar90_after_v262"] == pytest.approx(97990.63109107458)
    assert status["best_two_swap_objective_after_v262"] == pytest.approx(2966.397572505391)
    assert status["bounded_two_swap_improvement_found_v262"] is True
    assert status["multi_swap_integer_optimality_claim_allowed_v262"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v262"] is False
    assert status["paper1_promotion_allowed_v262"] is False
    assert status["paper4_working_champion_changed_v262"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v262"] == 4
    assert status["claim_matrix_rows_v262"] == 4

    primary = _read_csv("paper4_v262_bounded_two_swap_primary_frontier.csv")
    assert len(primary) == status["primary_frontier_rows_v262"]
    best_primary = primary.iloc[0]
    assert str(best_primary["primary_added_loan_id_v262"]) == "162604995"
    assert str(best_primary["primary_dropped_loan_id_v262"]) == "156935428"
    assert float(best_primary["primary_return_delta_v262"]) == pytest.approx(102.31039856619132)
    assert int(best_primary["primary_source_cap_violations_v262"]) == 1
    assert best_primary["primary_first_source_block_family_v262"] == "grade"
    assert best_primary["primary_first_source_block_id_v262"] == "A"
    assert float(best_primary["primary_source_min_slack_v262"]) == pytest.approx(
        -0.0007813688002913
    )
    assert best_primary["claim_boundary_v262"].startswith("bounded primary one-swap")

    stage = _read_csv("paper4_v262_bounded_two_swap_stage_summary.csv")
    assert len(stage) == status["primary_frontier_rows_v262"]
    assert (
        int(stage["base_two_swap_pair_rows_v262"].sum()) == status["base_two_swap_pair_rows_v262"]
    )
    assert (
        int(stage["source_prefilter_two_swap_pair_rows_v262"].sum())
        == status["source_prefilter_two_swap_pair_rows_v262"]
    )
    assert (
        int(stage["source_exact_two_swap_pair_rows_v262"].sum())
        == status["source_exact_two_swap_pair_rows_v262"]
    )
    assert (
        int(stage["cvar_feasible_two_swap_pair_rows_v262"].sum())
        == status["cvar_feasible_two_swap_pair_rows_v262"]
    )
    stage_map = {int(row["primary_rank_v262"]): row for _, row in stage.iterrows()}
    assert int(stage_map[11]["source_exact_two_swap_pair_rows_v262"]) == 47
    assert int(stage_map[11]["cvar_feasible_two_swap_pair_rows_v262"]) == 47

    candidates = _read_csv("paper4_v262_bounded_two_swap_candidates.csv")
    assert len(candidates) == status["source_exact_two_swap_pair_rows_v262"]
    assert candidates["source_swap_feasible_v262"].astype(bool).all()
    assert candidates["cvar_swap_feasible_v262"].astype(bool).all()
    assert candidates["two_swap_improves_return_v262"].astype(bool).all()
    assert int(candidates["source_cap_violations_after_two_swap_v262"].sum()) == 0
    assert candidates["total_return_delta_v262"].max() == pytest.approx(
        status["best_two_swap_return_delta_v262"]
    )

    top = _read_csv("paper4_v262_bounded_two_swap_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v262"]
    best = top.iloc[0]
    assert str(best["primary_added_loan_id_v262"]) == "140861718"
    assert str(best["primary_dropped_loan_id_v262"]) == "152337011"
    assert str(best["relief_added_loan_id_v262"]) == "158940609"
    assert str(best["relief_dropped_loan_id_v262"]) == "140804185"
    assert float(best["primary_return_delta_v262"]) == pytest.approx(88.97211276550695)
    assert float(best["relief_return_delta_v262"]) == pytest.approx(-56.011135939782406)
    assert float(best["total_return_delta_v262"]) == pytest.approx(
        status["best_two_swap_return_delta_v262"]
    )
    assert float(best["objective_return_after_two_swap_v262"]) == pytest.approx(
        status["best_two_swap_objective_after_v262"]
    )
    assert float(best["exposure_after_two_swap_v262"]) == pytest.approx(843125.0)
    assert float(best["cvar90_after_two_swap_v262"]) == pytest.approx(
        status["best_two_swap_cvar90_after_v262"]
    )
    assert float(best["source_min_slack_after_two_swap_v262"]) == pytest.approx(
        2.887048212119048e-05
    )
    assert float(best["max_source_share_after_two_swap_v262"]) == pytest.approx(0.8990363232023721)
    assert bool(best["source_swap_feasible_v262"]) is True
    assert bool(best["cvar_swap_feasible_v262"]) is True

    summary = _read_csv("paper4_v262_bounded_two_swap_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v262"] == "bounded_two_swap_after_v261_local_clearance"
    assert bool(row["bounded_two_swap_improvement_found_v262"]) is True
    assert bool(row["multi_swap_integer_optimality_claim_allowed_v262"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v262"]) is False
    assert bool(row["paper1_promotion_allowed_v262"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "no exhaustive multi-swap/global proof" in str(row["claim_boundary_v262"])

    blockers = _read_csv("paper4_v262_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v262"], blockers["blocking_v262"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v262"], blockers["evidence_count_v262"], strict=False)
    )
    assert bool(blocker_map["bounded_two_swap_improvement_found"]) is True
    assert int(evidence_map["bounded_two_swap_improvement_found"]) == 264
    assert bool(blocker_map["multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v262_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v262_bounded_two_swap_probe_executed"]) is True
    assert bool(claim_map["v262_bounded_two_swap_improvement_found"]) is True
    assert bool(claim_map["v262_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v262_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert (
        bool(boundary_map["Paper 4 has a v262 bounded two-swap source-relief probe after v261."])
        is True
    )
    assert bool(boundary_map["v262 finds bounded two-swap improvements over the v260 candidate."])
    assert (
        bool(boundary_map["v262 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v262 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v262_rows = backlog.loc[backlog["last_wave"].eq("v262")]
    assert len(v262_rows) == 1
    backlog_row = v262_rows.iloc[0]
    assert backlog_row["status"] == "bounded_two_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v263_apply_bounded_two_swap_repair.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v262: Bounded Two-Swap Probe After v261 Local Clearance" in notebook
    assert "CVaR-feasible improving two-swap rows:\n  `264`" in notebook
    assert "Best bounded two-swap return delta:\n  `32.96097682572454`" in notebook
    assert "not prove global optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v263_bounded_two_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v263_status.json")

    assert status["phase"] == "v263_bounded_two_swap_repair"
    assert status["schema_version"] == "2026-05-15.263"
    assert status["allocation_rows_v263"] == 171
    assert status["summary_rows_v263"] == 1
    assert status["action_rows_v263"] == 1
    assert status["source_summary_rows_v263"] == 51
    assert status["claim_blocker_rows_v263"] == 4
    assert status["claim_matrix_rows_v263"] == 5
    assert status["primary_added_loan_id_v263"] == "140861718"
    assert status["primary_dropped_loan_id_v263"] == "152337011"
    assert status["relief_added_loan_id_v263"] == "158940609"
    assert status["relief_dropped_loan_id_v263"] == "140804185"
    assert status["selected_rows_v263"] == 171
    assert status["portfolio_exposure_v263"] == pytest.approx(843125.0)
    assert status["objective_return_v263"] == pytest.approx(2966.3975725053933)
    assert status["scenario_loss_cvar90_v263"] == pytest.approx(97990.63109107458)
    assert status["source_cap_violations_v263"] == 0
    assert status["delta_return_vs_v260_v263"] == pytest.approx(32.9609768257269)
    assert status["delta_cvar90_vs_v260_v263"] == pytest.approx(115.17409954650793)
    assert status["delta_exposure_vs_v260_v263"] == pytest.approx(0.0)
    assert status["budget_feasible_v263"] is True
    assert status["source_feasible_v263"] is True
    assert status["cvar_feasible_v263"] is True
    assert status["repair_candidate_feasible_v263"] is True
    assert status["post_repair_local_optimality_claim_allowed_v263"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v263"] is False
    assert status["paper1_promotion_allowed_v263"] is False
    assert status["paper4_working_champion_changed_v263"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v263_bounded_two_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v263"]
    assert int(allocations["selected_v263"].sum()) == status["selected_rows_v263"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v263"])
    allocation_ids = set(allocations["loan_id"].astype(str))
    assert status["primary_added_loan_id_v263"] in allocation_ids
    assert status["relief_added_loan_id_v263"] in allocation_ids
    assert status["primary_dropped_loan_id_v263"] not in allocation_ids
    assert status["relief_dropped_loan_id_v263"] not in allocation_ids
    assert set(allocations["repair_action_v263"]) == {
        "added_from_v262_best_bounded_two_swap",
        "kept_from_v260",
    }
    assert allocations["claim_boundary_v263"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v263_bounded_two_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v263"] == "bounded_two_swap_repair_candidate"
    assert bool(row["repair_candidate_feasible_v263"]) is True
    assert bool(row["post_repair_pricing_required_v263"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v263"]) is False
    assert float(row["max_source_share_v263"]) == pytest.approx(0.8990363232023721)
    assert float(row["min_source_slack_v263"]) == pytest.approx(2.887048212119048e-05)
    assert float(row["scenario_loss_cvar90_v263"]) <= float(row["cvar_cap_v263"])
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v263"])

    action = _read_csv("paper4_v263_bounded_two_swap_repair_action.csv").iloc[0]
    assert str(action["primary_added_loan_id_v263"]) == status["primary_added_loan_id_v263"]
    assert str(action["primary_dropped_loan_id_v263"]) == status["primary_dropped_loan_id_v263"]
    assert str(action["relief_added_loan_id_v263"]) == status["relief_added_loan_id_v263"]
    assert str(action["relief_dropped_loan_id_v263"]) == status["relief_dropped_loan_id_v263"]
    assert float(action["primary_return_delta_v263"]) == pytest.approx(88.97211276550695)
    assert float(action["relief_return_delta_v263"]) == pytest.approx(-56.011135939782406)
    assert float(action["total_return_delta_v263"]) == pytest.approx(32.96097682572454)
    assert float(action["exposure_after_repair_v263"]) == pytest.approx(843125.0)
    assert int(action["source_cap_violations_after_repair_v263"]) == 0

    source_summary = _read_csv("paper4_v263_bounded_two_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v263"]
    assert not source_summary["source_cap_violated_v263"].astype(bool).any()

    blockers = _read_csv("paper4_v263_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v263"], blockers["blocking_v263"], strict=False))
    assert bool(blocker_map["bounded_two_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v263_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v263_bounded_two_swap_repair_executed"]) is True
    assert bool(claim_map["v263_repair_candidate_feasible"]) is True
    assert bool(claim_map["v263_post_repair_local_optimality"]) is False
    assert bool(claim_map["v263_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v263_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v263 bounded two-swap repair candidate."])
    assert bool(boundary_map["v263 repaired portfolio is post-repair locally optimal."]) is False
    assert (
        bool(boundary_map["v263 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v263 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v263_rows = backlog.loc[backlog["last_wave"].eq("v263")]
    assert len(v263_rows) == 1
    backlog_row = v263_rows.iloc[0]
    assert backlog_row["status"] == "post_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v264_post_bounded_two_swap_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v263: Bounded Two-Swap Repair Candidate" in notebook
    assert "Added loans: `140861718`" in notebook
    assert "Dropped loans: `152337011`" in notebook
    assert "Return delta vs v260:\n  `32.9609768257269`" in notebook
    assert "post-repair repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v264_post_bounded_two_swap_reprice_clears_local_screen() -> None:
    status = _read_json("paper4_v264_status.json")

    assert status["phase"] == "v264_post_v263_bounded_two_swap_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.264"
    assert status["summary_rows_v264"] == 1
    assert status["stage_summary_rows_v264"] == 6
    assert status["candidate_pair_rows_v264"] == 0
    assert status["top_candidate_rows_v264"] == 0
    assert status["claim_blocker_rows_v264"] == 4
    assert status["claim_matrix_rows_v264"] == 4
    assert status["selected_rows_v264"] == 171
    assert status["candidate_add_rows_v264"] == 276698
    assert status["total_pair_rows_screened_v264"] == 47315358
    assert status["return_improving_pair_rows_v264"] == 1655166
    assert status["budget_return_feasible_pair_rows_v264"] == 998571
    assert status["source_prefilter_pair_rows_v264"] == 4373
    assert status["source_exact_pair_rows_v264"] == 0
    assert status["cvar_feasible_pair_rows_v264"] == 0
    assert status["one_swap_improving_rows_v264"] == 0
    assert status["best_one_swap_return_delta_v264"] is None
    assert status["best_one_swap_cvar90_after_v264"] is None
    assert status["current_exposure_v264"] == pytest.approx(843125.0)
    assert status["current_objective_return_v264"] == pytest.approx(2966.3975725053933)
    assert status["post_bounded_two_swap_one_swap_local_optimality_cleared_v264"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v264"] is False
    assert status["paper1_promotion_allowed_v264"] is False
    assert status["paper4_working_champion_changed_v264"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v264_post_bounded_two_swap_one_swap_reprice.csv")
    top = _read_csv("paper4_v264_post_bounded_two_swap_one_swap_top_candidates.csv")
    assert candidates.empty
    assert top.empty
    assert "added_loan_id_v264" in candidates.columns
    assert "return_delta_v264" in top.columns

    summary = _read_csv("paper4_v264_post_bounded_two_swap_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v264"] == "post_v263_bounded_two_swap_repair"
    assert int(row["selected_rows_v264"]) == status["selected_rows_v264"]
    assert int(row["one_swap_improving_rows_v264"]) == 0
    assert bool(row["post_bounded_two_swap_one_swap_local_optimality_cleared_v264"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v264"]) is False
    assert bool(row["paper1_promotion_allowed_v264"]) is False
    assert bool(row["paper4_final_promotion_created_v264"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v264"])

    stage = _read_csv("paper4_v264_post_bounded_two_swap_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v264"]
    stage_map = dict(zip(stage["stage_v264"], stage["pair_rows_v264"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v264"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v264"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v264"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v264"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v264"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v264"]

    blockers = _read_csv("paper4_v264_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v264"], blockers["blocking_v264"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v264"], blockers["evidence_count_v264"], strict=False)
    )
    assert bool(blocker_map["post_v263_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v263_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["broader_multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v264_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v264_post_v263_bounded_two_swap_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v264_post_v263_one_swap_local_optimality"]) is True
    assert bool(claim_map["v264_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v264_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v264 post-v263 bounded two-swap one-swap pricing screen."]
    )
    assert bool(
        boundary_map[
            "v264 proves the v263 bounded two-swap repaired portfolio is one-swap locally optimal."
        ]
    )
    assert (
        bool(boundary_map["v264 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v264 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v264_rows = backlog.loc[backlog["last_wave"].eq("v264")]
    assert len(v264_rows) == 1
    backlog_row = v264_rows.iloc[0]
    assert (
        backlog_row["status"] == "one_swap_local_optimality_cleared_after_bounded_two_swap_repair"
    )
    assert backlog_row["next_artifact"] == "paper4_v265_broader_multi_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v264: Post-v263 Bounded Two-Swap One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best post-v263 one-swap return delta:\n  `not applicable; no feasible improving one-swaps`"
    ) in notebook
    assert "Post-v263 one-swap local optimality cleared:\n  `True`" in notebook
    assert "broader multi-swap/global integer evidence" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v265_bounded_two_swap_probe_finds_post_v263_improvements() -> None:
    status = _read_json("paper4_v265_status.json")

    assert status["phase"] == "v265_bounded_two_swap_after_v264_local_clearance"
    assert status["schema_version"] == "2026-05-15.265"
    assert status["previous_repair_version_v265"] == 263
    assert status["terminal_reprice_version_v265"] == 264
    assert status["primary_frontier_limit_v265"] == 25
    assert status["primary_frontier_rows_v265"] == 25
    assert status["one_swap_return_improving_pair_rows_v265"] == 1655166
    assert status["one_swap_budget_return_feasible_pair_rows_v265"] == 998571
    assert status["one_swap_source_prefilter_pair_rows_v265"] == 4373
    assert status["base_two_swap_pair_rows_v265"] == 35904290
    assert status["source_prefilter_two_swap_pair_rows_v265"] == 465580
    assert status["source_exact_two_swap_pair_rows_v265"] == 46
    assert status["cvar_feasible_two_swap_pair_rows_v265"] == 46
    assert status["top_candidate_rows_v265"] == 46
    assert status["best_two_swap_return_delta_v265"] == pytest.approx(21.911980076230094)
    assert status["best_two_swap_cvar90_after_v265"] == pytest.approx(97867.324781459)
    assert status["best_two_swap_objective_after_v265"] == pytest.approx(2988.3095525816234)
    assert status["bounded_two_swap_improvement_found_v265"] is True
    assert status["multi_swap_integer_optimality_claim_allowed_v265"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v265"] is False
    assert status["paper1_promotion_allowed_v265"] is False
    assert status["paper4_working_champion_changed_v265"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v265"] == 4
    assert status["claim_matrix_rows_v265"] == 4

    primary = _read_csv("paper4_v265_bounded_two_swap_primary_frontier.csv")
    assert len(primary) == status["primary_frontier_rows_v265"]
    best_primary = primary.iloc[0]
    assert str(best_primary["primary_added_loan_id_v265"]) == "162604995"
    assert str(best_primary["primary_dropped_loan_id_v265"]) == "158940609"
    assert float(best_primary["primary_return_delta_v265"]) == pytest.approx(114.1309605916256)
    assert int(best_primary["primary_source_cap_violations_v265"]) == 1
    assert best_primary["primary_first_source_block_family_v265"] == "grade"
    assert best_primary["primary_first_source_block_id_v265"] == "A"
    assert float(best_primary["primary_source_min_slack_v265"]) == pytest.approx(
        -0.0007813688002913
    )
    assert best_primary["claim_boundary_v265"].startswith("bounded primary one-swap")

    stage = _read_csv("paper4_v265_bounded_two_swap_stage_summary.csv")
    assert len(stage) == status["primary_frontier_rows_v265"]
    assert (
        int(stage["base_two_swap_pair_rows_v265"].sum()) == status["base_two_swap_pair_rows_v265"]
    )
    assert (
        int(stage["source_prefilter_two_swap_pair_rows_v265"].sum())
        == status["source_prefilter_two_swap_pair_rows_v265"]
    )
    assert (
        int(stage["source_exact_two_swap_pair_rows_v265"].sum())
        == status["source_exact_two_swap_pair_rows_v265"]
    )
    assert (
        int(stage["cvar_feasible_two_swap_pair_rows_v265"].sum())
        == status["cvar_feasible_two_swap_pair_rows_v265"]
    )
    stage_map = {int(row["primary_rank_v265"]): row for _, row in stage.iterrows()}
    assert int(stage_map[0]["source_exact_two_swap_pair_rows_v265"]) == 3
    assert int(stage_map[11]["source_exact_two_swap_pair_rows_v265"]) == 6

    candidates = _read_csv("paper4_v265_bounded_two_swap_candidates.csv")
    assert len(candidates) == status["source_exact_two_swap_pair_rows_v265"]
    assert candidates["source_swap_feasible_v265"].astype(bool).all()
    assert candidates["cvar_swap_feasible_v265"].astype(bool).all()
    assert candidates["two_swap_improves_return_v265"].astype(bool).all()
    assert int(candidates["source_cap_violations_after_two_swap_v265"].sum()) == 0
    assert candidates["total_return_delta_v265"].max() == pytest.approx(
        status["best_two_swap_return_delta_v265"]
    )

    top = _read_csv("paper4_v265_bounded_two_swap_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v265"]
    best = top.iloc[0]
    assert str(best["primary_added_loan_id_v265"]) == "143384974"
    assert str(best["primary_dropped_loan_id_v265"]) == "154756272"
    assert str(best["relief_added_loan_id_v265"]) == "145079878"
    assert str(best["relief_dropped_loan_id_v265"]) == "137378784"
    assert float(best["primary_return_delta_v265"]) == pytest.approx(91.09848276973648)
    assert float(best["relief_return_delta_v265"]) == pytest.approx(-69.18650269350638)
    assert float(best["total_return_delta_v265"]) == pytest.approx(
        status["best_two_swap_return_delta_v265"]
    )
    assert float(best["objective_return_after_two_swap_v265"]) == pytest.approx(
        status["best_two_swap_objective_after_v265"]
    )
    assert float(best["exposure_after_two_swap_v265"]) == pytest.approx(843125.0)
    assert float(best["cvar90_after_two_swap_v265"]) == pytest.approx(
        status["best_two_swap_cvar90_after_v265"]
    )
    assert float(best["source_min_slack_after_two_swap_v265"]) == pytest.approx(
        2.887048212119048e-05
    )
    assert float(best["max_source_share_after_two_swap_v265"]) == pytest.approx(0.8997479614529281)
    assert bool(best["source_swap_feasible_v265"]) is True
    assert bool(best["cvar_swap_feasible_v265"]) is True

    summary = _read_csv("paper4_v265_bounded_two_swap_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v265"] == "bounded_two_swap_after_v264_local_clearance"
    assert bool(row["bounded_two_swap_improvement_found_v265"]) is True
    assert bool(row["multi_swap_integer_optimality_claim_allowed_v265"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v265"]) is False
    assert bool(row["paper1_promotion_allowed_v265"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "no exhaustive multi-swap/global proof" in str(row["claim_boundary_v265"])

    blockers = _read_csv("paper4_v265_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v265"], blockers["blocking_v265"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v265"], blockers["evidence_count_v265"], strict=False)
    )
    assert bool(blocker_map["bounded_two_swap_improvement_found"]) is True
    assert int(evidence_map["bounded_two_swap_improvement_found"]) == 46
    assert bool(blocker_map["multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v265_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v265_bounded_two_swap_probe_executed"]) is True
    assert bool(claim_map["v265_bounded_two_swap_improvement_found"]) is True
    assert bool(claim_map["v265_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v265_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert (
        bool(boundary_map["Paper 4 has a v265 bounded two-swap source-relief probe after v264."])
        is True
    )
    assert bool(boundary_map["v265 finds bounded two-swap improvements over the v263 candidate."])
    assert (
        bool(boundary_map["v265 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v265 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v265_rows = backlog.loc[backlog["last_wave"].eq("v265")]
    assert len(v265_rows) == 1
    backlog_row = v265_rows.iloc[0]
    assert backlog_row["status"] == "bounded_two_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v266_apply_bounded_two_swap_repair.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v265: Bounded Two-Swap Probe After v264 Local Clearance" in notebook
    assert "CVaR-feasible improving two-swap rows:\n  `46`" in notebook
    assert "Best bounded two-swap return delta:\n  `21.911980076230094`" in notebook
    assert "not prove global optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v266_bounded_two_swap_repair_requires_repricing() -> None:
    status = _read_json("paper4_v266_status.json")

    assert status["phase"] == "v266_bounded_two_swap_repair"
    assert status["schema_version"] == "2026-05-15.266"
    assert status["allocation_rows_v266"] == 171
    assert status["summary_rows_v266"] == 1
    assert status["action_rows_v266"] == 1
    assert status["source_summary_rows_v266"] == 51
    assert status["claim_blocker_rows_v266"] == 4
    assert status["claim_matrix_rows_v266"] == 5
    assert status["primary_added_loan_id_v266"] == "143384974"
    assert status["primary_dropped_loan_id_v266"] == "154756272"
    assert status["relief_added_loan_id_v266"] == "145079878"
    assert status["relief_dropped_loan_id_v266"] == "137378784"
    assert status["selected_rows_v266"] == 171
    assert status["portfolio_exposure_v266"] == pytest.approx(843125.0)
    assert status["objective_return_v266"] == pytest.approx(2988.3095525816243)
    assert status["scenario_loss_cvar90_v266"] == pytest.approx(97867.324781459)
    assert status["source_cap_violations_v266"] == 0
    assert status["delta_return_vs_v263_v266"] == pytest.approx(21.911980076230975)
    assert status["delta_cvar90_vs_v263_v266"] == pytest.approx(-123.306309615582)
    assert status["delta_exposure_vs_v263_v266"] == pytest.approx(0.0)
    assert status["budget_feasible_v266"] is True
    assert status["source_feasible_v266"] is True
    assert status["cvar_feasible_v266"] is True
    assert status["repair_candidate_feasible_v266"] is True
    assert status["post_repair_local_optimality_claim_allowed_v266"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v266"] is False
    assert status["paper1_promotion_allowed_v266"] is False
    assert status["paper4_working_champion_changed_v266"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v266_bounded_two_swap_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v266"]
    assert int(allocations["selected_v266"].sum()) == status["selected_rows_v266"]
    assert allocations["loan_amnt"].sum() == pytest.approx(status["portfolio_exposure_v266"])
    allocation_ids = set(allocations["loan_id"].astype(str))
    assert status["primary_added_loan_id_v266"] in allocation_ids
    assert status["relief_added_loan_id_v266"] in allocation_ids
    assert status["primary_dropped_loan_id_v266"] not in allocation_ids
    assert status["relief_dropped_loan_id_v266"] not in allocation_ids
    assert set(allocations["repair_action_v266"]) == {
        "added_from_v265_best_bounded_two_swap",
        "kept_from_v263",
    }
    assert allocations["claim_boundary_v266"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v266_bounded_two_swap_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v266"] == "bounded_two_swap_repair_candidate"
    assert bool(row["repair_candidate_feasible_v266"]) is True
    assert bool(row["post_repair_pricing_required_v266"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v266"]) is False
    assert float(row["max_source_share_v266"]) == pytest.approx(0.8997479614529281)
    assert float(row["min_source_slack_v266"]) == pytest.approx(2.887048212119048e-05)
    assert float(row["scenario_loss_cvar90_v266"]) <= float(row["cvar_cap_v266"])
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v266"])

    action = _read_csv("paper4_v266_bounded_two_swap_repair_action.csv").iloc[0]
    assert str(action["primary_added_loan_id_v266"]) == status["primary_added_loan_id_v266"]
    assert str(action["primary_dropped_loan_id_v266"]) == status["primary_dropped_loan_id_v266"]
    assert str(action["relief_added_loan_id_v266"]) == status["relief_added_loan_id_v266"]
    assert str(action["relief_dropped_loan_id_v266"]) == status["relief_dropped_loan_id_v266"]
    assert float(action["primary_return_delta_v266"]) == pytest.approx(91.09848276973648)
    assert float(action["relief_return_delta_v266"]) == pytest.approx(-69.18650269350638)
    assert float(action["total_return_delta_v266"]) == pytest.approx(21.911980076230094)
    assert float(action["exposure_after_repair_v266"]) == pytest.approx(843125.0)
    assert int(action["source_cap_violations_after_repair_v266"]) == 0

    source_summary = _read_csv("paper4_v266_bounded_two_swap_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v266"]
    assert not source_summary["source_cap_violated_v266"].astype(bool).any()

    blockers = _read_csv("paper4_v266_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v266"], blockers["blocking_v266"], strict=False))
    assert bool(blocker_map["bounded_two_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v266_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v266_bounded_two_swap_repair_executed"]) is True
    assert bool(claim_map["v266_repair_candidate_feasible"]) is True
    assert bool(claim_map["v266_post_repair_local_optimality"]) is False
    assert bool(claim_map["v266_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v266_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v266 bounded two-swap repair candidate."])
    assert bool(boundary_map["v266 repaired portfolio is post-repair locally optimal."]) is False
    assert (
        bool(boundary_map["v266 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v266 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v266_rows = backlog.loc[backlog["last_wave"].eq("v266")]
    assert len(v266_rows) == 1
    backlog_row = v266_rows.iloc[0]
    assert backlog_row["status"] == "post_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v267_post_bounded_two_swap_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v266: Bounded Two-Swap Repair Candidate" in notebook
    assert "Added loans: `143384974`" in notebook
    assert "Dropped loans: `154756272`" in notebook
    assert "Return delta vs v263:\n  `21.911980076230975`" in notebook
    assert "post-repair repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v267_post_bounded_two_swap_reprice_clears_local_screen() -> None:
    status = _read_json("paper4_v267_status.json")

    assert status["phase"] == "v267_post_v266_bounded_two_swap_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.267"
    assert status["summary_rows_v267"] == 1
    assert status["stage_summary_rows_v267"] == 6
    assert status["candidate_pair_rows_v267"] == 0
    assert status["top_candidate_rows_v267"] == 0
    assert status["claim_blocker_rows_v267"] == 4
    assert status["claim_matrix_rows_v267"] == 4
    assert status["selected_rows_v267"] == 171
    assert status["candidate_add_rows_v267"] == 276698
    assert status["total_pair_rows_screened_v267"] == 47315358
    assert status["return_improving_pair_rows_v267"] == 1653502
    assert status["budget_return_feasible_pair_rows_v267"] == 997447
    assert status["source_prefilter_pair_rows_v267"] == 4038
    assert status["source_exact_pair_rows_v267"] == 0
    assert status["cvar_feasible_pair_rows_v267"] == 0
    assert status["one_swap_improving_rows_v267"] == 0
    assert status["best_one_swap_return_delta_v267"] is None
    assert status["best_one_swap_cvar90_after_v267"] is None
    assert status["current_exposure_v267"] == pytest.approx(843125.0)
    assert status["current_objective_return_v267"] == pytest.approx(2988.3095525816243)
    assert status["post_bounded_two_swap_one_swap_local_optimality_cleared_v267"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v267"] is False
    assert status["paper1_promotion_allowed_v267"] is False
    assert status["paper4_working_champion_changed_v267"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v267_post_bounded_two_swap_one_swap_reprice.csv")
    top = _read_csv("paper4_v267_post_bounded_two_swap_one_swap_top_candidates.csv")
    assert candidates.empty
    assert top.empty
    assert "added_loan_id_v267" in candidates.columns
    assert "return_delta_v267" in top.columns

    summary = _read_csv("paper4_v267_post_bounded_two_swap_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v267"] == "post_v266_bounded_two_swap_repair"
    assert int(row["selected_rows_v267"]) == status["selected_rows_v267"]
    assert int(row["one_swap_improving_rows_v267"]) == 0
    assert bool(row["post_bounded_two_swap_one_swap_local_optimality_cleared_v267"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v267"]) is False
    assert bool(row["paper1_promotion_allowed_v267"]) is False
    assert bool(row["paper4_final_promotion_created_v267"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v267"])

    stage = _read_csv("paper4_v267_post_bounded_two_swap_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v267"]
    stage_map = dict(zip(stage["stage_v267"], stage["pair_rows_v267"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v267"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v267"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v267"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v267"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v267"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v267"]

    blockers = _read_csv("paper4_v267_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v267"], blockers["blocking_v267"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v267"], blockers["evidence_count_v267"], strict=False)
    )
    assert bool(blocker_map["post_v266_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v266_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["broader_multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v267_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v267_post_v266_bounded_two_swap_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v267_post_v266_one_swap_local_optimality"]) is True
    assert bool(claim_map["v267_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v267_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v267 post-v266 bounded two-swap one-swap pricing screen."]
    )
    assert bool(
        boundary_map[
            "v267 proves the v266 bounded two-swap repaired portfolio is one-swap locally optimal."
        ]
    )
    assert (
        bool(boundary_map["v267 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v267 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v267_rows = backlog.loc[backlog["last_wave"].eq("v267")]
    assert len(v267_rows) == 1
    backlog_row = v267_rows.iloc[0]
    assert (
        backlog_row["status"] == "one_swap_local_optimality_cleared_after_bounded_two_swap_repair"
    )
    assert backlog_row["next_artifact"] == "paper4_v268_broader_multi_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v267: Post-v266 Bounded Two-Swap One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best post-v266 one-swap return delta:\n  `not applicable; no feasible improving one-swaps`"
    ) in notebook
    assert "Post-v266 one-swap local optimality cleared:\n  `True`" in notebook
    assert "broader multi-swap/global integer evidence" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v268_bounded_two_swap_probe_finds_no_post_v266_improvements() -> None:
    status = _read_json("paper4_v268_status.json")

    assert status["phase"] == "v268_bounded_two_swap_after_v267_local_clearance"
    assert status["schema_version"] == "2026-05-15.268"
    assert status["previous_repair_version_v268"] == 266
    assert status["terminal_reprice_version_v268"] == 267
    assert status["primary_frontier_limit_v268"] == 25
    assert status["primary_frontier_rows_v268"] == 25
    assert status["one_swap_return_improving_pair_rows_v268"] == 1653502
    assert status["one_swap_budget_return_feasible_pair_rows_v268"] == 997447
    assert status["one_swap_source_prefilter_pair_rows_v268"] == 4038
    assert status["base_two_swap_pair_rows_v268"] == 35846451
    assert status["source_prefilter_two_swap_pair_rows_v268"] == 397236
    assert status["source_exact_two_swap_pair_rows_v268"] == 0
    assert status["cvar_feasible_two_swap_pair_rows_v268"] == 0
    assert status["top_candidate_rows_v268"] == 0
    assert status["best_two_swap_return_delta_v268"] is None
    assert status["best_two_swap_cvar90_after_v268"] is None
    assert status["best_two_swap_objective_after_v268"] is None
    assert status["bounded_two_swap_improvement_found_v268"] is False
    assert status["multi_swap_integer_optimality_claim_allowed_v268"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v268"] is False
    assert status["paper1_promotion_allowed_v268"] is False
    assert status["paper4_working_champion_changed_v268"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v268"] == 4
    assert status["claim_matrix_rows_v268"] == 4

    primary = _read_csv("paper4_v268_bounded_two_swap_primary_frontier.csv")
    assert len(primary) == status["primary_frontier_rows_v268"]
    best_primary = primary.iloc[0]
    assert str(best_primary["primary_added_loan_id_v268"]) == "162604995"
    assert str(best_primary["primary_dropped_loan_id_v268"]) == "158940609"
    assert float(best_primary["primary_return_delta_v268"]) == pytest.approx(114.1309605916)
    assert int(best_primary["primary_source_cap_violations_v268"]) == 2
    assert best_primary["primary_first_source_block_family_v268"] == "grade"
    assert best_primary["primary_first_source_block_id_v268"] == "A"
    assert float(best_primary["primary_source_min_slack_v268"]) == pytest.approx(-0.0007813688)
    assert float(best_primary["primary_max_source_share_v268"]) == pytest.approx(0.900602499)
    assert best_primary["claim_boundary_v268"].startswith("bounded primary one-swap")

    stage = _read_csv("paper4_v268_bounded_two_swap_stage_summary.csv")
    assert len(stage) == status["primary_frontier_rows_v268"]
    assert (
        int(stage["base_two_swap_pair_rows_v268"].sum()) == status["base_two_swap_pair_rows_v268"]
    )
    assert (
        int(stage["source_prefilter_two_swap_pair_rows_v268"].sum())
        == status["source_prefilter_two_swap_pair_rows_v268"]
    )
    assert (
        int(stage["source_exact_two_swap_pair_rows_v268"].sum())
        == status["source_exact_two_swap_pair_rows_v268"]
    )
    assert (
        int(stage["cvar_feasible_two_swap_pair_rows_v268"].sum())
        == status["cvar_feasible_two_swap_pair_rows_v268"]
    )
    stage_map = {int(row["primary_rank_v268"]): row for _, row in stage.iterrows()}
    assert int(stage_map[0]["source_prefilter_two_swap_pair_rows_v268"]) == 28495
    assert int(stage_map[24]["source_prefilter_two_swap_pair_rows_v268"]) == 11484
    assert int(stage_map[0]["source_exact_two_swap_pair_rows_v268"]) == 0

    candidates = _read_csv("paper4_v268_bounded_two_swap_candidates.csv")
    top = _read_csv("paper4_v268_bounded_two_swap_top_candidates.csv")
    assert candidates.empty
    assert top.empty
    assert "primary_added_loan_id_v268" in candidates.columns
    assert "total_return_delta_v268" in top.columns

    summary = _read_csv("paper4_v268_bounded_two_swap_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v268"] == "bounded_two_swap_after_v267_local_clearance"
    assert bool(row["bounded_two_swap_improvement_found_v268"]) is False
    assert bool(row["multi_swap_integer_optimality_claim_allowed_v268"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v268"]) is False
    assert bool(row["paper1_promotion_allowed_v268"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "no exhaustive multi-swap/global proof" in str(row["claim_boundary_v268"])

    blockers = _read_csv("paper4_v268_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v268"], blockers["blocking_v268"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v268"], blockers["evidence_count_v268"], strict=False)
    )
    assert bool(blocker_map["bounded_two_swap_improvement_found"]) is False
    assert int(evidence_map["bounded_two_swap_improvement_found"]) == 0
    assert bool(blocker_map["multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v268_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v268_bounded_two_swap_probe_executed"]) is True
    assert bool(claim_map["v268_bounded_two_swap_improvement_found"]) is False
    assert bool(claim_map["v268_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v268_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v268 bounded two-swap source-relief probe after v267."])
    assert (
        bool(boundary_map["v268 finds bounded two-swap improvements over the v266 candidate."])
        is False
    )
    assert (
        bool(boundary_map["v268 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v268 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v268_rows = backlog.loc[backlog["last_wave"].eq("v268")]
    assert len(v268_rows) == 1
    backlog_row = v268_rows.iloc[0]
    assert backlog_row["status"] == "bounded_two_swap_no_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v269_broader_three_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v268: Bounded Two-Swap Probe After v267 Local Clearance" in notebook
    assert "CVaR-feasible improving two-swap rows:\n  `0`" in notebook
    assert (
        "Best bounded two-swap return delta:\n  `not applicable; no feasible bounded two-swap "
        "improvements`"
    ) in notebook
    assert "not prove global optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v269_restricted_pool_milp_finds_bounded_improvement() -> None:
    status = _read_json("paper4_v269_status.json")

    assert status["phase"] == "v269_restricted_pool_milp_gap_probe_after_v268"
    assert status["schema_version"] == "2026-05-15.269"
    assert status["base_repair_version_v269"] == 266
    assert status["terminal_reprice_version_v269"] == 267
    assert status["bounded_two_swap_version_v269"] == 268
    assert status["candidate_pool_limit_v269"] == 500
    assert status["pool_rows_v269"] == 685
    assert status["current_selected_rows_v269"] == 171
    assert status["omitted_candidate_rows_v269"] == 514
    assert status["selected_rows_v269"] == 171
    assert status["added_rows_v269"] == 8
    assert status["dropped_rows_v269"] == 8
    assert status["objective_return_v269"] == pytest.approx(3054.74591104893)
    assert status["objective_delta_vs_v266_v269"] == pytest.approx(66.43635846730558)
    assert status["portfolio_exposure_v269"] == pytest.approx(844900.0)
    assert status["scenario_loss_cvar90_v269"] == pytest.approx(98692.17278654788)
    assert status["cvar90_delta_vs_v266_v269"] == pytest.approx(824.8480050888757)
    assert status["source_cap_violations_v269"] == 0
    assert status["budget_feasible_v269"] is True
    assert status["cvar_feasible_v269"] is True
    assert status["source_feasible_v269"] is True
    assert status["restricted_pool_improvement_found_v269"] is True
    assert status["milp_success_v269"] is True
    assert status["milp_incumbent_available_v269"] is True
    assert status["milp_status_v269"] == 0
    assert status["milp_gap_v269"] == pytest.approx(0.0)
    assert status["milp_dual_bound_v269"] == pytest.approx(-3054.745911048933)
    assert status["constraint_rows_v269"] == 176
    assert status["restricted_pool_global_optimality_claim_allowed_v269"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v269"] is False
    assert status["paper1_promotion_allowed_v269"] is False
    assert status["paper4_working_champion_changed_v269"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v269"] == 4
    assert status["claim_matrix_rows_v269"] == 4

    summary = _read_csv("paper4_v269_restricted_pool_milp_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v269"] == "restricted_pool_milp_after_v268_no_two_swap_improvement"
    assert bool(row["restricted_pool_improvement_found_v269"]) is True
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v269"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v269"]) is False
    assert bool(row["paper1_promotion_allowed_v269"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "not full-universe global proof" in str(row["claim_boundary_v269"])

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v269_restricted_pool_milp_allocations.parquet"
    )
    assert len(allocations) == status["pool_rows_v269"]
    assert int(allocations["selected_v269"].sum()) == status["selected_rows_v269"]
    assert int(allocations["current_selected_v269"].sum()) == status["current_selected_rows_v269"]
    action_counts = allocations["milp_action_v269"].value_counts().to_dict()
    assert action_counts["added_by_restricted_pool_milp"] == status["added_rows_v269"]
    assert action_counts["dropped_by_restricted_pool_milp"] == status["dropped_rows_v269"]
    assert action_counts["kept_from_v266"] == 163
    assert action_counts["not_selected_candidate"] == 506
    added_ids = set(
        allocations.loc[
            allocations["milp_action_v269"].eq("added_by_restricted_pool_milp"), "loan_id"
        ].astype(str)
    )
    dropped_ids = set(
        allocations.loc[
            allocations["milp_action_v269"].eq("dropped_by_restricted_pool_milp"), "loan_id"
        ].astype(str)
    )
    assert added_ids == {
        "132864620",
        "142448559",
        "144223623",
        "144253758",
        "146255415",
        "154756272",
        "162604995",
        "163050494",
    }
    assert dropped_ids == {
        "131574675",
        "138240169",
        "143126474",
        "143278940",
        "143384974",
        "152762134",
        "158316010",
        "163383289",
    }

    source_summary = _read_csv("paper4_v269_restricted_pool_milp_source_summary.csv")
    assert len(source_summary) == 51
    assert not source_summary["source_cap_violated_v269"].astype(bool).any()
    assert float(source_summary["source_slack_v269"].min()) == pytest.approx(1.6162656218399185e-05)
    assert float(source_summary["source_share_v269"].max()) == pytest.approx(0.8998993963782697)

    constraints = _read_csv("paper4_v269_restricted_pool_milp_constraints.csv")
    assert len(constraints) == status["constraint_rows_v269"]
    constraint_counts = constraints["constraint_type_v269"].value_counts().to_dict()
    assert constraint_counts["cvar_path_excess"] == 128
    assert constraint_counts["source_share"] == 45
    assert constraint_counts["budget_range"] == 1
    assert constraint_counts["selected_row_cardinality"] == 1
    assert constraint_counts["cvar_cap"] == 1

    blockers = _read_csv("paper4_v269_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v269"], blockers["blocking_v269"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v269"], blockers["evidence_count_v269"], strict=False)
    )
    assert bool(blocker_map["restricted_pool_improvement_found"]) is True
    assert int(evidence_map["restricted_pool_improvement_found"]) == 1
    assert bool(blocker_map["restricted_pool_not_full_universe"]) is True
    assert int(evidence_map["restricted_pool_not_full_universe"]) == status["pool_rows_v269"]
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v269_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v269_restricted_pool_milp_probe_executed"]) is True
    assert bool(claim_map["v269_restricted_pool_improvement_found"]) is True
    assert bool(claim_map["v269_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v269_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v269 restricted-pool MILP/gap probe after v268."])
    assert bool(boundary_map["v269 finds a better restricted-pool MILP incumbent than v266."])
    assert bool(boundary_map["v269 proves global full-universe integer optimality."]) is False
    assert bool(boundary_map["v269 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v269_rows = backlog.loc[backlog["last_wave"].eq("v269")]
    assert len(v269_rows) == 1
    backlog_row = v269_rows.iloc[0]
    assert backlog_row["status"] == "restricted_pool_milp_improvement_found"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v270_apply_restricted_pool_candidate_or_decompose_swaps.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v269: Restricted-Pool MILP/Gap Probe After v268" in notebook
    assert "Objective delta vs v266:\n  `66.43635846730558`" in notebook
    assert "Better restricted-pool incumbent found:\n  `True`" in notebook
    assert "does not prove full-universe optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v270_restricted_pool_milp_repair_requires_repricing() -> None:
    status = _read_json("paper4_v270_status.json")

    assert status["phase"] == "v270_restricted_pool_milp_repair"
    assert status["schema_version"] == "2026-05-15.270"
    assert status["source_probe_version_v270"] == 269
    assert status["allocation_rows_v270"] == 171
    assert status["summary_rows_v270"] == 1
    assert status["action_rows_v270"] == 16
    assert status["source_summary_rows_v270"] == 51
    assert status["claim_blocker_rows_v270"] == 4
    assert status["claim_matrix_rows_v270"] == 5
    assert status["selected_rows_v270"] == 171
    assert status["added_rows_v270"] == 8
    assert status["dropped_rows_v270"] == 8
    assert status["portfolio_exposure_v270"] == pytest.approx(844900.0)
    assert status["objective_return_v270"] == pytest.approx(3054.745911048931)
    assert status["scenario_loss_cvar90_v270"] == pytest.approx(98692.17278654786)
    assert status["source_cap_violations_v270"] == 0
    assert status["delta_return_vs_v266_v270"] == pytest.approx(66.43635846730649)
    assert status["delta_cvar90_vs_v266_v270"] == pytest.approx(824.8480050888611)
    assert status["delta_exposure_vs_v266_v270"] == pytest.approx(1775.0)
    assert status["budget_feasible_v270"] is True
    assert status["source_feasible_v270"] is True
    assert status["cvar_feasible_v270"] is True
    assert status["repair_candidate_feasible_v270"] is True
    assert status["post_repair_local_optimality_claim_allowed_v270"] is False
    assert status["restricted_pool_global_optimality_claim_allowed_v270"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v270"] is False
    assert status["paper1_promotion_allowed_v270"] is False
    assert status["paper4_working_champion_changed_v270"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v270_restricted_pool_milp_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v270"]
    assert int(allocations["selected_v270"].sum()) == status["selected_rows_v270"]
    action_counts = allocations["repair_action_v270"].value_counts().to_dict()
    assert action_counts["added_by_restricted_pool_milp"] == status["added_rows_v270"]
    assert action_counts["kept_from_v266"] == 163
    assert allocations["claim_boundary_v270"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v270_restricted_pool_milp_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v270"] == "restricted_pool_milp_repair_candidate"
    assert int(row["source_probe_version_v270"]) == 269
    assert bool(row["repair_candidate_feasible_v270"]) is True
    assert bool(row["post_repair_pricing_required_v270"]) is True
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v270"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v270"]) is False
    assert float(row["max_source_share_v270"]) == pytest.approx(0.8998993963782697)
    assert float(row["min_source_slack_v270"]) == pytest.approx(1.6162656218399185e-05)
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v270"])

    action = _read_csv("paper4_v270_restricted_pool_milp_repair_action.csv")
    assert len(action) == status["action_rows_v270"]
    added_ids = set(
        action.loc[action["action_v270"].eq("added_by_restricted_pool_milp"), "loan_id"].astype(str)
    )
    dropped_ids = set(
        action.loc[action["action_v270"].eq("dropped_by_restricted_pool_milp"), "loan_id"].astype(
            str
        )
    )
    assert added_ids == {
        "132864620",
        "142448559",
        "144223623",
        "144253758",
        "146255415",
        "154756272",
        "162604995",
        "163050494",
    }
    assert dropped_ids == {
        "131574675",
        "138240169",
        "143126474",
        "143278940",
        "143384974",
        "152762134",
        "158316010",
        "163383289",
    }

    source_summary = _read_csv("paper4_v270_restricted_pool_milp_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v270"]
    assert not source_summary["source_cap_violated_v270"].astype(bool).any()

    blockers = _read_csv("paper4_v270_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v270"], blockers["blocking_v270"], strict=False))
    assert bool(blocker_map["restricted_pool_milp_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v270_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v270_restricted_pool_milp_repair_executed"]) is True
    assert bool(claim_map["v270_repair_candidate_feasible"]) is True
    assert bool(claim_map["v270_post_repair_local_optimality"]) is False
    assert bool(claim_map["v270_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v270_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v270 restricted-pool MILP repair candidate."])
    assert bool(boundary_map["v270 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v270 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v270 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v270_rows = backlog.loc[backlog["last_wave"].eq("v270")]
    assert len(v270_rows) == 1
    backlog_row = v270_rows.iloc[0]
    assert backlog_row["status"] == "post_restricted_pool_milp_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v271_post_restricted_pool_milp_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v270: Restricted-Pool MILP Repair Candidate" in notebook
    assert "Return delta vs v266:\n  `66.43635846730649`" in notebook
    assert "CVaR90 delta vs v266:\n  `824.8480050888611`" in notebook
    assert "does not make\nthe portfolio locally or globally optimal" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v271_post_restricted_pool_milp_reprice_clears_local_screen() -> None:
    status = _read_json("paper4_v271_status.json")

    assert status["phase"] == "v271_post_v270_restricted_pool_milp_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.271"
    assert status["summary_rows_v271"] == 1
    assert status["stage_summary_rows_v271"] == 6
    assert status["candidate_pair_rows_v271"] == 0
    assert status["top_candidate_rows_v271"] == 0
    assert status["claim_blocker_rows_v271"] == 4
    assert status["claim_matrix_rows_v271"] == 4
    assert status["selected_rows_v271"] == 171
    assert status["candidate_add_rows_v271"] == 276698
    assert status["total_pair_rows_screened_v271"] == 47315358
    assert status["return_improving_pair_rows_v271"] == 1643340
    assert status["budget_return_feasible_pair_rows_v271"] == 914115
    assert status["source_prefilter_pair_rows_v271"] == 36646
    assert status["source_exact_pair_rows_v271"] == 0
    assert status["cvar_feasible_pair_rows_v271"] == 0
    assert status["one_swap_improving_rows_v271"] == 0
    assert status["best_one_swap_return_delta_v271"] is None
    assert status["best_one_swap_cvar90_after_v271"] is None
    assert status["current_exposure_v271"] == pytest.approx(844900.0)
    assert status["current_objective_return_v271"] == pytest.approx(3054.745911048931)
    assert status["post_restricted_pool_milp_one_swap_local_optimality_cleared_v271"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v271"] is False
    assert status["paper1_promotion_allowed_v271"] is False
    assert status["paper4_working_champion_changed_v271"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v271_post_restricted_pool_milp_one_swap_reprice.csv")
    top = _read_csv("paper4_v271_post_restricted_pool_milp_one_swap_top_candidates.csv")
    assert candidates.empty
    assert top.empty
    assert "added_loan_id_v271" in candidates.columns
    assert "return_delta_v271" in top.columns

    summary = _read_csv("paper4_v271_post_restricted_pool_milp_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v271"] == "post_v270_restricted_pool_milp_repair"
    assert int(row["selected_rows_v271"]) == status["selected_rows_v271"]
    assert int(row["one_swap_improving_rows_v271"]) == 0
    assert bool(row["post_restricted_pool_milp_one_swap_local_optimality_cleared_v271"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v271"]) is False
    assert bool(row["paper1_promotion_allowed_v271"]) is False
    assert bool(row["paper4_final_promotion_created_v271"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v271"])

    stage = _read_csv("paper4_v271_post_restricted_pool_milp_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v271"]
    stage_map = dict(zip(stage["stage_v271"], stage["pair_rows_v271"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v271"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v271"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v271"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v271"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v271"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v271"]

    blockers = _read_csv("paper4_v271_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v271"], blockers["blocking_v271"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v271"], blockers["evidence_count_v271"], strict=False)
    )
    assert bool(blocker_map["post_v270_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v270_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["broader_multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v271_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v271_post_v270_restricted_pool_milp_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v271_post_v270_one_swap_local_optimality"]) is True
    assert bool(claim_map["v271_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v271_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v271 post-v270 restricted-pool MILP one-swap pricing screen."]
    )
    assert bool(
        boundary_map[
            "v271 proves the v270 restricted-pool MILP repaired portfolio is one-swap locally "
            "optimal."
        ]
    )
    assert (
        bool(boundary_map["v271 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v271 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v271_rows = backlog.loc[backlog["last_wave"].eq("v271")]
    assert len(v271_rows) == 1
    backlog_row = v271_rows.iloc[0]
    assert (
        backlog_row["status"]
        == "one_swap_local_optimality_cleared_after_restricted_pool_milp_repair"
    )
    assert backlog_row["next_artifact"] == "paper4_v272_broader_multi_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v271: Post-v270 Restricted-Pool MILP One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best post-v270 one-swap return delta:\n  `not applicable; no feasible improving one-swaps`"
    ) in notebook
    assert "Post-v270 one-swap local optimality cleared:\n  `True`" in notebook
    assert "broader multi-swap/global integer evidence" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v272_expanded_restricted_pool_milp_finds_bounded_improvement() -> None:
    status = _read_json("paper4_v272_status.json")

    assert status["phase"] == "v272_restricted_pool_milp_gap_probe_after_v271"
    assert status["schema_version"] == "2026-05-15.272"
    assert status["base_repair_version_v272"] == 270
    assert status["terminal_reprice_version_v272"] == 271
    assert status["bounded_two_swap_version_v272"] == 268
    assert status["candidate_pool_limit_v272"] == 1500
    assert status["pool_rows_v272"] == 1681
    assert status["current_selected_rows_v272"] == 171
    assert status["omitted_candidate_rows_v272"] == 1510
    assert status["selected_rows_v272"] == 171
    assert status["added_rows_v272"] == 7
    assert status["dropped_rows_v272"] == 7
    assert status["objective_return_v272"] == pytest.approx(3076.5834654043865)
    assert status["objective_delta_vs_v270_v272"] == pytest.approx(21.837554355455723)
    assert status["portfolio_exposure_v272"] == pytest.approx(843525.0)
    assert status["scenario_loss_cvar90_v272"] == pytest.approx(98480.4980299936)
    assert status["cvar90_delta_vs_v270_v272"] == pytest.approx(-211.67475655426097)
    assert status["source_cap_violations_v272"] == 0
    assert status["budget_feasible_v272"] is True
    assert status["cvar_feasible_v272"] is True
    assert status["source_feasible_v272"] is True
    assert status["restricted_pool_improvement_found_v272"] is True
    assert status["milp_success_v272"] is True
    assert status["milp_incumbent_available_v272"] is True
    assert status["milp_status_v272"] == 0
    assert status["milp_gap_v272"] == pytest.approx(0.0)
    assert status["milp_dual_bound_v272"] == pytest.approx(-3076.5834654044093)
    assert status["constraint_rows_v272"] == 176
    assert status["restricted_pool_global_optimality_claim_allowed_v272"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v272"] is False
    assert status["paper1_promotion_allowed_v272"] is False
    assert status["paper4_working_champion_changed_v272"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v272"] == 4
    assert status["claim_matrix_rows_v272"] == 4

    summary = _read_csv("paper4_v272_restricted_pool_milp_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v272"] == "restricted_pool_milp_after_v271_no_two_swap_improvement"
    assert bool(row["restricted_pool_improvement_found_v272"]) is True
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v272"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v272"]) is False
    assert bool(row["paper1_promotion_allowed_v272"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "not full-universe global proof" in str(row["claim_boundary_v272"])

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v272_restricted_pool_milp_allocations.parquet"
    )
    assert len(allocations) == status["pool_rows_v272"]
    assert int(allocations["selected_v272"].sum()) == status["selected_rows_v272"]
    assert int(allocations["current_selected_v272"].sum()) == status["current_selected_rows_v272"]
    action_counts = allocations["milp_action_v272"].value_counts().to_dict()
    assert action_counts["added_by_restricted_pool_milp"] == status["added_rows_v272"]
    assert action_counts["dropped_by_restricted_pool_milp"] == status["dropped_rows_v272"]
    assert action_counts["kept_from_v270"] == 164
    assert action_counts["not_selected_candidate"] == 1503
    added_ids = set(
        allocations.loc[
            allocations["milp_action_v272"].eq("added_by_restricted_pool_milp"), "loan_id"
        ].astype(str)
    )
    dropped_ids = set(
        allocations.loc[
            allocations["milp_action_v272"].eq("dropped_by_restricted_pool_milp"), "loan_id"
        ].astype(str)
    )
    assert added_ids == {
        "127475839",
        "138253214",
        "141665611",
        "145033022",
        "145359336",
        "151810745",
        "156192909",
    }
    assert dropped_ids == {
        "134420497",
        "138631135",
        "140861718",
        "144223623",
        "144253758",
        "162590887",
        "163050494",
    }

    source_summary = _read_csv("paper4_v272_restricted_pool_milp_source_summary.csv")
    assert len(source_summary) == 51
    assert not source_summary["source_cap_violated_v272"].astype(bool).any()
    assert float(source_summary["source_slack_v272"].min()) == pytest.approx(1.8488337880206984e-05)
    assert float(source_summary["source_share_v272"].max()) == pytest.approx(0.8999733262203254)

    constraints = _read_csv("paper4_v272_restricted_pool_milp_constraints.csv")
    assert len(constraints) == status["constraint_rows_v272"]
    constraint_counts = constraints["constraint_type_v272"].value_counts().to_dict()
    assert constraint_counts["cvar_path_excess"] == 128
    assert constraint_counts["source_share"] == 45
    assert constraint_counts["budget_range"] == 1
    assert constraint_counts["selected_row_cardinality"] == 1
    assert constraint_counts["cvar_cap"] == 1

    blockers = _read_csv("paper4_v272_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v272"], blockers["blocking_v272"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v272"], blockers["evidence_count_v272"], strict=False)
    )
    assert bool(blocker_map["restricted_pool_improvement_found"]) is True
    assert int(evidence_map["restricted_pool_improvement_found"]) == 1
    assert bool(blocker_map["restricted_pool_not_full_universe"]) is True
    assert int(evidence_map["restricted_pool_not_full_universe"]) == status["pool_rows_v272"]
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v272_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v272_restricted_pool_milp_probe_executed"]) is True
    assert bool(claim_map["v272_restricted_pool_improvement_found"]) is True
    assert bool(claim_map["v272_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v272_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v272 restricted-pool MILP/gap probe after v271."])
    assert bool(boundary_map["v272 finds a better restricted-pool MILP incumbent than v270."])
    assert bool(boundary_map["v272 proves global full-universe integer optimality."]) is False
    assert bool(boundary_map["v272 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v272_rows = backlog.loc[backlog["last_wave"].eq("v272")]
    assert len(v272_rows) == 1
    backlog_row = v272_rows.iloc[0]
    assert backlog_row["status"] == "restricted_pool_milp_improvement_found"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v273_apply_restricted_pool_candidate_or_decompose_swaps.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v272: Restricted-Pool MILP/Gap Probe After v271" in notebook
    assert "Objective delta vs v270:\n  `21.837554355455723`" in notebook
    assert "CVaR90 delta vs v270:\n  `-211.67475655426097`" in notebook
    assert "Better restricted-pool incumbent found:\n  `True`" in notebook
    assert "not a full-universe certificate" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v273_restricted_pool_milp_repair_requires_repricing() -> None:
    status = _read_json("paper4_v273_status.json")

    assert status["phase"] == "v273_restricted_pool_milp_repair"
    assert status["schema_version"] == "2026-05-15.273"
    assert status["source_probe_version_v273"] == 272
    assert status["allocation_rows_v273"] == 171
    assert status["summary_rows_v273"] == 1
    assert status["action_rows_v273"] == 14
    assert status["source_summary_rows_v273"] == 51
    assert status["claim_blocker_rows_v273"] == 4
    assert status["claim_matrix_rows_v273"] == 5
    assert status["selected_rows_v273"] == 171
    assert status["added_rows_v273"] == 7
    assert status["dropped_rows_v273"] == 7
    assert status["portfolio_exposure_v273"] == pytest.approx(843525.0)
    assert status["objective_return_v273"] == pytest.approx(3076.5834654043883)
    assert status["scenario_loss_cvar90_v273"] == pytest.approx(98480.49802999356)
    assert status["source_cap_violations_v273"] == 0
    assert status["delta_return_vs_v270_v273"] == pytest.approx(21.83755435545754)
    assert status["delta_cvar90_vs_v270_v273"] == pytest.approx(-211.67475655430462)
    assert status["delta_exposure_vs_v270_v273"] == pytest.approx(-1375.0)
    assert status["budget_feasible_v273"] is True
    assert status["source_feasible_v273"] is True
    assert status["cvar_feasible_v273"] is True
    assert status["repair_candidate_feasible_v273"] is True
    assert status["post_repair_local_optimality_claim_allowed_v273"] is False
    assert status["restricted_pool_global_optimality_claim_allowed_v273"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v273"] is False
    assert status["paper1_promotion_allowed_v273"] is False
    assert status["paper4_working_champion_changed_v273"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v273_restricted_pool_milp_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v273"]
    assert int(allocations["selected_v273"].sum()) == status["selected_rows_v273"]
    action_counts = allocations["repair_action_v273"].value_counts().to_dict()
    assert action_counts["added_by_restricted_pool_milp"] == status["added_rows_v273"]
    assert action_counts["kept_from_v270"] == 164
    assert allocations["claim_boundary_v273"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v273_restricted_pool_milp_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v273"] == "restricted_pool_milp_repair_candidate"
    assert int(row["source_probe_version_v273"]) == 272
    assert bool(row["repair_candidate_feasible_v273"]) is True
    assert bool(row["post_repair_pricing_required_v273"]) is True
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v273"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v273"]) is False
    assert float(row["max_source_share_v273"]) == pytest.approx(0.8999733262203254)
    assert float(row["min_source_slack_v273"]) == pytest.approx(1.8488337880206984e-05)
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v273"])

    action = _read_csv("paper4_v273_restricted_pool_milp_repair_action.csv")
    assert len(action) == status["action_rows_v273"]
    added_ids = set(
        action.loc[action["action_v273"].eq("added_by_restricted_pool_milp"), "loan_id"].astype(str)
    )
    dropped_ids = set(
        action.loc[action["action_v273"].eq("dropped_by_restricted_pool_milp"), "loan_id"].astype(
            str
        )
    )
    assert added_ids == {
        "127475839",
        "138253214",
        "141665611",
        "145033022",
        "145359336",
        "151810745",
        "156192909",
    }
    assert dropped_ids == {
        "134420497",
        "138631135",
        "140861718",
        "144223623",
        "144253758",
        "162590887",
        "163050494",
    }

    source_summary = _read_csv("paper4_v273_restricted_pool_milp_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v273"]
    assert not source_summary["source_cap_violated_v273"].astype(bool).any()

    blockers = _read_csv("paper4_v273_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v273"], blockers["blocking_v273"], strict=False))
    assert bool(blocker_map["restricted_pool_milp_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v273_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v273_restricted_pool_milp_repair_executed"]) is True
    assert bool(claim_map["v273_repair_candidate_feasible"]) is True
    assert bool(claim_map["v273_post_repair_local_optimality"]) is False
    assert bool(claim_map["v273_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v273_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v273 restricted-pool MILP repair candidate."])
    assert bool(boundary_map["v273 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v273 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v273 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v273_rows = backlog.loc[backlog["last_wave"].eq("v273")]
    assert len(v273_rows) == 1
    backlog_row = v273_rows.iloc[0]
    assert backlog_row["status"] == "post_restricted_pool_milp_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v274_post_restricted_pool_milp_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v273: Restricted-Pool MILP Repair Candidate" in notebook
    assert "Return delta vs v270:\n  `21.83755435545754`" in notebook
    assert "CVaR90 delta vs v270:\n  `-211.67475655430462`" in notebook
    assert "does not make\nthe portfolio locally or globally optimal" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v274_post_restricted_pool_milp_reprice_clears_local_screen() -> None:
    status = _read_json("paper4_v274_status.json")

    assert status["phase"] == "v274_post_v273_restricted_pool_milp_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.274"
    assert status["summary_rows_v274"] == 1
    assert status["stage_summary_rows_v274"] == 6
    assert status["candidate_pair_rows_v274"] == 0
    assert status["top_candidate_rows_v274"] == 0
    assert status["claim_blocker_rows_v274"] == 4
    assert status["claim_matrix_rows_v274"] == 4
    assert status["selected_rows_v274"] == 171
    assert status["candidate_add_rows_v274"] == 276698
    assert status["total_pair_rows_screened_v274"] == 47315358
    assert status["return_improving_pair_rows_v274"] == 1636704
    assert status["budget_return_feasible_pair_rows_v274"] == 980057
    assert status["source_prefilter_pair_rows_v274"] == 14210
    assert status["source_exact_pair_rows_v274"] == 0
    assert status["cvar_feasible_pair_rows_v274"] == 0
    assert status["one_swap_improving_rows_v274"] == 0
    assert status["best_one_swap_return_delta_v274"] is None
    assert status["best_one_swap_cvar90_after_v274"] is None
    assert status["current_exposure_v274"] == pytest.approx(843525.0)
    assert status["current_objective_return_v274"] == pytest.approx(3076.5834654043883)
    assert status["post_restricted_pool_milp_one_swap_local_optimality_cleared_v274"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v274"] is False
    assert status["paper1_promotion_allowed_v274"] is False
    assert status["paper4_working_champion_changed_v274"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v274_post_restricted_pool_milp_one_swap_reprice.csv")
    top = _read_csv("paper4_v274_post_restricted_pool_milp_one_swap_top_candidates.csv")
    assert candidates.empty
    assert top.empty
    assert "added_loan_id_v274" in candidates.columns
    assert "return_delta_v274" in top.columns

    summary = _read_csv("paper4_v274_post_restricted_pool_milp_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v274"] == "post_v273_restricted_pool_milp_repair"
    assert int(row["selected_rows_v274"]) == status["selected_rows_v274"]
    assert int(row["one_swap_improving_rows_v274"]) == 0
    assert bool(row["post_restricted_pool_milp_one_swap_local_optimality_cleared_v274"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v274"]) is False
    assert bool(row["paper1_promotion_allowed_v274"]) is False
    assert bool(row["paper4_final_promotion_created_v274"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v274"])

    stage = _read_csv("paper4_v274_post_restricted_pool_milp_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v274"]
    stage_map = dict(zip(stage["stage_v274"], stage["pair_rows_v274"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v274"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v274"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v274"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v274"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v274"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v274"]

    blockers = _read_csv("paper4_v274_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v274"], blockers["blocking_v274"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v274"], blockers["evidence_count_v274"], strict=False)
    )
    assert bool(blocker_map["post_v273_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v273_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["broader_multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v274_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v274_post_v273_restricted_pool_milp_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v274_post_v273_one_swap_local_optimality"]) is True
    assert bool(claim_map["v274_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v274_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v274 post-v273 restricted-pool MILP one-swap pricing screen."]
    )
    assert bool(
        boundary_map[
            "v274 proves the v273 restricted-pool MILP repaired portfolio is one-swap locally "
            "optimal."
        ]
    )
    assert (
        bool(boundary_map["v274 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v274 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v274_rows = backlog.loc[backlog["last_wave"].eq("v274")]
    assert len(v274_rows) == 1
    backlog_row = v274_rows.iloc[0]
    assert (
        backlog_row["status"]
        == "one_swap_local_optimality_cleared_after_restricted_pool_milp_repair"
    )
    assert backlog_row["next_artifact"] == "paper4_v275_broader_multi_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v274: Post-v273 Restricted-Pool MILP One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best post-v273 one-swap return delta:\n  `not applicable; no feasible improving one-swaps`"
    ) in notebook
    assert "Post-v273 one-swap local optimality cleared:\n  `True`" in notebook
    assert "broader multi-swap/global integer evidence" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v275_larger_restricted_pool_milp_finds_bounded_improvement() -> None:
    status = _read_json("paper4_v275_status.json")

    assert status["phase"] == "v275_restricted_pool_milp_gap_probe_after_v274"
    assert status["schema_version"] == "2026-05-15.275"
    assert status["base_repair_version_v275"] == 273
    assert status["terminal_reprice_version_v275"] == 274
    assert status["bounded_two_swap_version_v275"] == 268
    assert status["previous_restricted_pool_milp_version_v275"] == 272
    assert status["candidate_pool_limit_v275"] == 2500
    assert status["pool_rows_v275"] == 2683
    assert status["current_selected_rows_v275"] == 171
    assert status["omitted_candidate_rows_v275"] == 2512
    assert status["selected_rows_v275"] == 171
    assert status["added_rows_v275"] == 5
    assert status["dropped_rows_v275"] == 5
    assert status["objective_return_v275"] == pytest.approx(3093.7845184199846)
    assert status["objective_delta_vs_v273_v275"] == pytest.approx(17.201053015596244)
    assert status["portfolio_exposure_v275"] == pytest.approx(843525.0)
    assert status["scenario_loss_cvar90_v275"] == pytest.approx(98711.03721768621)
    assert status["cvar90_delta_vs_v273_v275"] == pytest.approx(230.53918769265874)
    assert status["source_cap_violations_v275"] == 0
    assert status["budget_feasible_v275"] is True
    assert status["cvar_feasible_v275"] is True
    assert status["source_feasible_v275"] is True
    assert status["restricted_pool_improvement_found_v275"] is True
    assert status["milp_success_v275"] is True
    assert status["milp_incumbent_available_v275"] is True
    assert status["milp_status_v275"] == 0
    assert status["milp_gap_v275"] == pytest.approx(0.0)
    assert status["milp_dual_bound_v275"] == pytest.approx(-3093.7845184200187)
    assert status["constraint_rows_v275"] == 176
    assert status["restricted_pool_global_optimality_claim_allowed_v275"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v275"] is False
    assert status["paper1_promotion_allowed_v275"] is False
    assert status["paper4_working_champion_changed_v275"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v275"] == 4
    assert status["claim_matrix_rows_v275"] == 4

    summary = _read_csv("paper4_v275_restricted_pool_milp_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v275"] == "restricted_pool_milp_after_v274_no_two_swap_improvement"
    assert bool(row["restricted_pool_improvement_found_v275"]) is True
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v275"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v275"]) is False
    assert bool(row["paper1_promotion_allowed_v275"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "not full-universe global proof" in str(row["claim_boundary_v275"])

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v275_restricted_pool_milp_allocations.parquet"
    )
    assert len(allocations) == status["pool_rows_v275"]
    assert int(allocations["selected_v275"].sum()) == status["selected_rows_v275"]
    assert int(allocations["current_selected_v275"].sum()) == status["current_selected_rows_v275"]
    action_counts = allocations["milp_action_v275"].value_counts().to_dict()
    assert action_counts["added_by_restricted_pool_milp"] == status["added_rows_v275"]
    assert action_counts["dropped_by_restricted_pool_milp"] == status["dropped_rows_v275"]
    assert action_counts["kept_from_v273"] == 166
    assert action_counts["not_selected_candidate"] == 2507
    added_ids = set(
        allocations.loc[
            allocations["milp_action_v275"].eq("added_by_restricted_pool_milp"), "loan_id"
        ].astype(str)
    )
    dropped_ids = set(
        allocations.loc[
            allocations["milp_action_v275"].eq("dropped_by_restricted_pool_milp"), "loan_id"
        ].astype(str)
    )
    assert added_ids == {
        "133763576",
        "143359641",
        "144534751",
        "156899870",
        "165985906",
    }
    assert dropped_ids == {
        "127475839",
        "132864620",
        "142448559",
        "151810745",
        "159991596",
    }

    source_summary = _read_csv("paper4_v275_restricted_pool_milp_source_summary.csv")
    assert len(source_summary) == 51
    assert not source_summary["source_cap_violated_v275"].astype(bool).any()
    assert float(source_summary["source_slack_v275"].min()) == pytest.approx(1.8488337880206984e-05)
    assert float(source_summary["source_share_v275"].max()) == pytest.approx(0.8999733262203254)

    constraints = _read_csv("paper4_v275_restricted_pool_milp_constraints.csv")
    assert len(constraints) == status["constraint_rows_v275"]
    constraint_counts = constraints["constraint_type_v275"].value_counts().to_dict()
    assert constraint_counts["cvar_path_excess"] == 128
    assert constraint_counts["source_share"] == 45
    assert constraint_counts["budget_range"] == 1
    assert constraint_counts["selected_row_cardinality"] == 1
    assert constraint_counts["cvar_cap"] == 1

    blockers = _read_csv("paper4_v275_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v275"], blockers["blocking_v275"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v275"], blockers["evidence_count_v275"], strict=False)
    )
    assert bool(blocker_map["restricted_pool_improvement_found"]) is True
    assert int(evidence_map["restricted_pool_improvement_found"]) == 1
    assert bool(blocker_map["restricted_pool_not_full_universe"]) is True
    assert int(evidence_map["restricted_pool_not_full_universe"]) == status["pool_rows_v275"]
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v275_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v275_restricted_pool_milp_probe_executed"]) is True
    assert bool(claim_map["v275_restricted_pool_improvement_found"]) is True
    assert bool(claim_map["v275_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v275_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v275 restricted-pool MILP/gap probe after v274."])
    assert bool(boundary_map["v275 finds a better restricted-pool MILP incumbent than v273."])
    assert bool(boundary_map["v275 proves global full-universe integer optimality."]) is False
    assert bool(boundary_map["v275 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v275_rows = backlog.loc[backlog["last_wave"].eq("v275")]
    assert len(v275_rows) == 1
    backlog_row = v275_rows.iloc[0]
    assert backlog_row["status"] == "restricted_pool_milp_improvement_found"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v276_apply_restricted_pool_candidate_or_decompose_swaps.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v275: Restricted-Pool MILP/Gap Probe After v274" in notebook
    assert "Objective delta vs v273:\n  `17.201053015596244`" in notebook
    assert "CVaR90 delta vs v273:\n  `230.53918769265874`" in notebook
    assert "Better restricted-pool incumbent found:\n  `True`" in notebook
    assert "does not prove full-universe optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v276_restricted_pool_milp_repair_requires_repricing() -> None:
    status = _read_json("paper4_v276_status.json")

    assert status["phase"] == "v276_restricted_pool_milp_repair"
    assert status["schema_version"] == "2026-05-15.276"
    assert status["source_probe_version_v276"] == 275
    assert status["allocation_rows_v276"] == 171
    assert status["summary_rows_v276"] == 1
    assert status["action_rows_v276"] == 10
    assert status["source_summary_rows_v276"] == 51
    assert status["claim_blocker_rows_v276"] == 4
    assert status["claim_matrix_rows_v276"] == 5
    assert status["selected_rows_v276"] == 171
    assert status["added_rows_v276"] == 5
    assert status["dropped_rows_v276"] == 5
    assert status["portfolio_exposure_v276"] == pytest.approx(843525.0)
    assert status["objective_return_v276"] == pytest.approx(3093.7845184199887)
    assert status["scenario_loss_cvar90_v276"] == pytest.approx(98711.03721768619)
    assert status["source_cap_violations_v276"] == 0
    assert status["delta_return_vs_v273_v276"] == pytest.approx(17.201053015600337)
    assert status["delta_cvar90_vs_v273_v276"] == pytest.approx(230.53918769262964)
    assert status["delta_exposure_vs_v273_v276"] == pytest.approx(0.0)
    assert status["budget_feasible_v276"] is True
    assert status["source_feasible_v276"] is True
    assert status["cvar_feasible_v276"] is True
    assert status["repair_candidate_feasible_v276"] is True
    assert status["post_repair_local_optimality_claim_allowed_v276"] is False
    assert status["restricted_pool_global_optimality_claim_allowed_v276"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v276"] is False
    assert status["paper1_promotion_allowed_v276"] is False
    assert status["paper4_working_champion_changed_v276"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v276_restricted_pool_milp_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v276"]
    assert int(allocations["selected_v276"].sum()) == status["selected_rows_v276"]
    action_counts = allocations["repair_action_v276"].value_counts().to_dict()
    assert action_counts["added_by_restricted_pool_milp"] == status["added_rows_v276"]
    assert action_counts["kept_from_v273"] == 166
    assert allocations["claim_boundary_v276"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v276_restricted_pool_milp_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v276"] == "restricted_pool_milp_repair_candidate"
    assert int(row["source_probe_version_v276"]) == 275
    assert bool(row["repair_candidate_feasible_v276"]) is True
    assert bool(row["post_repair_pricing_required_v276"]) is True
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v276"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v276"]) is False
    assert float(row["max_source_share_v276"]) == pytest.approx(0.8999733262203254)
    assert float(row["min_source_slack_v276"]) == pytest.approx(1.8488337880206984e-05)
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v276"])

    action = _read_csv("paper4_v276_restricted_pool_milp_repair_action.csv")
    assert len(action) == status["action_rows_v276"]
    added_ids = set(
        action.loc[action["action_v276"].eq("added_by_restricted_pool_milp"), "loan_id"].astype(str)
    )
    dropped_ids = set(
        action.loc[action["action_v276"].eq("dropped_by_restricted_pool_milp"), "loan_id"].astype(
            str
        )
    )
    assert added_ids == {
        "133763576",
        "143359641",
        "144534751",
        "156899870",
        "165985906",
    }
    assert dropped_ids == {
        "127475839",
        "132864620",
        "142448559",
        "151810745",
        "159991596",
    }

    source_summary = _read_csv("paper4_v276_restricted_pool_milp_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v276"]
    assert not source_summary["source_cap_violated_v276"].astype(bool).any()

    blockers = _read_csv("paper4_v276_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v276"], blockers["blocking_v276"], strict=False))
    assert bool(blocker_map["restricted_pool_milp_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v276_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v276_restricted_pool_milp_repair_executed"]) is True
    assert bool(claim_map["v276_repair_candidate_feasible"]) is True
    assert bool(claim_map["v276_post_repair_local_optimality"]) is False
    assert bool(claim_map["v276_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v276_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v276 restricted-pool MILP repair candidate."])
    assert bool(boundary_map["v276 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v276 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v276 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v276_rows = backlog.loc[backlog["last_wave"].eq("v276")]
    assert len(v276_rows) == 1
    backlog_row = v276_rows.iloc[0]
    assert backlog_row["status"] == "post_restricted_pool_milp_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v277_post_restricted_pool_milp_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v276: Restricted-Pool MILP Repair Candidate" in notebook
    assert "Return delta vs v273:\n  `17.201053015600337`" in notebook
    assert "CVaR90 delta vs v273:\n  `230.53918769262964`" in notebook
    assert "does not make\nthe portfolio locally or globally optimal" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v277_post_restricted_pool_milp_reprice_clears_local_screen() -> None:
    status = _read_json("paper4_v277_status.json")

    assert status["phase"] == "v277_post_v276_restricted_pool_milp_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.277"
    assert status["summary_rows_v277"] == 1
    assert status["stage_summary_rows_v277"] == 6
    assert status["candidate_pair_rows_v277"] == 0
    assert status["top_candidate_rows_v277"] == 0
    assert status["claim_blocker_rows_v277"] == 4
    assert status["claim_matrix_rows_v277"] == 4
    assert status["selected_rows_v277"] == 171
    assert status["candidate_add_rows_v277"] == 276698
    assert status["total_pair_rows_screened_v277"] == 47315358
    assert status["return_improving_pair_rows_v277"] == 1636685
    assert status["budget_return_feasible_pair_rows_v277"] == 979387
    assert status["source_prefilter_pair_rows_v277"] == 14237
    assert status["source_exact_pair_rows_v277"] == 0
    assert status["cvar_feasible_pair_rows_v277"] == 0
    assert status["one_swap_improving_rows_v277"] == 0
    assert status["best_one_swap_return_delta_v277"] is None
    assert status["best_one_swap_cvar90_after_v277"] is None
    assert status["current_exposure_v277"] == pytest.approx(843525.0)
    assert status["current_objective_return_v277"] == pytest.approx(3093.7845184199887)
    assert status["post_restricted_pool_milp_one_swap_local_optimality_cleared_v277"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v277"] is False
    assert status["paper1_promotion_allowed_v277"] is False
    assert status["paper4_working_champion_changed_v277"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v277_post_restricted_pool_milp_one_swap_reprice.csv")
    top = _read_csv("paper4_v277_post_restricted_pool_milp_one_swap_top_candidates.csv")
    assert candidates.empty
    assert top.empty
    assert "added_loan_id_v277" in candidates.columns
    assert "return_delta_v277" in top.columns

    summary = _read_csv("paper4_v277_post_restricted_pool_milp_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v277"] == "post_v276_restricted_pool_milp_repair"
    assert int(row["selected_rows_v277"]) == status["selected_rows_v277"]
    assert int(row["one_swap_improving_rows_v277"]) == 0
    assert bool(row["post_restricted_pool_milp_one_swap_local_optimality_cleared_v277"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v277"]) is False
    assert bool(row["paper1_promotion_allowed_v277"]) is False
    assert bool(row["paper4_final_promotion_created_v277"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v277"])

    stage = _read_csv("paper4_v277_post_restricted_pool_milp_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v277"]
    stage_map = dict(zip(stage["stage_v277"], stage["pair_rows_v277"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v277"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v277"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v277"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v277"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v277"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v277"]

    blockers = _read_csv("paper4_v277_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v277"], blockers["blocking_v277"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v277"], blockers["evidence_count_v277"], strict=False)
    )
    assert bool(blocker_map["post_v276_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v276_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["broader_multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v277_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v277_post_v276_restricted_pool_milp_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v277_post_v276_one_swap_local_optimality"]) is True
    assert bool(claim_map["v277_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v277_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v277 post-v276 restricted-pool MILP one-swap pricing screen."]
    )
    assert bool(
        boundary_map[
            "v277 proves the v276 restricted-pool MILP repaired portfolio is one-swap locally "
            "optimal."
        ]
    )
    assert (
        bool(boundary_map["v277 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v277 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v277_rows = backlog.loc[backlog["last_wave"].eq("v277")]
    assert len(v277_rows) == 1
    backlog_row = v277_rows.iloc[0]
    assert (
        backlog_row["status"]
        == "one_swap_local_optimality_cleared_after_restricted_pool_milp_repair"
    )
    assert backlog_row["next_artifact"] == "paper4_v278_broader_multi_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v277: Post-v276 Restricted-Pool MILP One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best post-v276 one-swap return delta:\n  `not applicable; no feasible improving one-swaps`"
    ) in notebook
    assert "Post-v276 one-swap local optimality cleared:\n  `True`" in notebook
    assert "broader multi-swap/global integer evidence" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v278_larger_restricted_pool_milp_finds_bounded_improvement() -> None:
    status = _read_json("paper4_v278_status.json")

    assert status["phase"] == "v278_restricted_pool_milp_gap_probe_after_v277"
    assert status["schema_version"] == "2026-05-15.278"
    assert status["base_repair_version_v278"] == 276
    assert status["terminal_reprice_version_v278"] == 277
    assert status["bounded_two_swap_version_v278"] == 268
    assert status["previous_restricted_pool_milp_version_v278"] == 275
    assert status["candidate_pool_limit_v278"] == 3500
    assert status["pool_rows_v278"] == 3684
    assert status["current_selected_rows_v278"] == 171
    assert status["omitted_candidate_rows_v278"] == 3513
    assert status["selected_rows_v278"] == 171
    assert status["added_rows_v278"] == 5
    assert status["dropped_rows_v278"] == 5
    assert status["objective_return_v278"] == pytest.approx(3102.542415185826)
    assert status["objective_delta_vs_v276_v278"] == pytest.approx(8.757896765837359)
    assert status["portfolio_exposure_v278"] == pytest.approx(843625.0)
    assert status["scenario_loss_cvar90_v278"] == pytest.approx(98435.12427343646)
    assert status["cvar90_delta_vs_v276_v278"] == pytest.approx(-275.91294424973603)
    assert status["source_cap_violations_v278"] == 0
    assert status["budget_feasible_v278"] is True
    assert status["cvar_feasible_v278"] is True
    assert status["source_feasible_v278"] is True
    assert status["restricted_pool_improvement_found_v278"] is True
    assert status["milp_success_v278"] is True
    assert status["milp_incumbent_available_v278"] is True
    assert status["milp_status_v278"] == 0
    assert status["milp_gap_v278"] == pytest.approx(0.0)
    assert status["milp_dual_bound_v278"] == pytest.approx(-3102.5424151858197)
    assert status["constraint_rows_v278"] == 176
    assert status["restricted_pool_global_optimality_claim_allowed_v278"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v278"] is False
    assert status["paper1_promotion_allowed_v278"] is False
    assert status["paper4_working_champion_changed_v278"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v278"] == 4
    assert status["claim_matrix_rows_v278"] == 4

    summary = _read_csv("paper4_v278_restricted_pool_milp_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v278"] == "restricted_pool_milp_after_v277_no_two_swap_improvement"
    assert bool(row["restricted_pool_improvement_found_v278"]) is True
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v278"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v278"]) is False
    assert bool(row["paper1_promotion_allowed_v278"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "not full-universe global proof" in str(row["claim_boundary_v278"])

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v278_restricted_pool_milp_allocations.parquet"
    )
    assert len(allocations) == status["pool_rows_v278"]
    assert int(allocations["selected_v278"].sum()) == status["selected_rows_v278"]
    assert int(allocations["current_selected_v278"].sum()) == status["current_selected_rows_v278"]
    action_counts = allocations["milp_action_v278"].value_counts().to_dict()
    assert action_counts["added_by_restricted_pool_milp"] == status["added_rows_v278"]
    assert action_counts["dropped_by_restricted_pool_milp"] == status["dropped_rows_v278"]
    assert action_counts["kept_from_v276"] == 166
    assert action_counts["not_selected_candidate"] == 3508
    added_ids = set(
        allocations.loc[
            allocations["milp_action_v278"].eq("added_by_restricted_pool_milp"), "loan_id"
        ].astype(str)
    )
    dropped_ids = set(
        allocations.loc[
            allocations["milp_action_v278"].eq("dropped_by_restricted_pool_milp"), "loan_id"
        ].astype(str)
    )
    assert added_ids == {
        "137182894",
        "138240169",
        "152268188",
        "159357211",
        "170387634",
    }
    assert dropped_ids == {
        "138138354",
        "143359641",
        "145359336",
        "156899870",
        "160508188",
    }

    source_summary = _read_csv("paper4_v278_restricted_pool_milp_source_summary.csv")
    assert len(source_summary) == 51
    assert not source_summary["source_cap_violated_v278"].astype(bool).any()
    assert float(source_summary["source_slack_v278"].min()) == pytest.approx(1.0773302159261533e-06)
    assert float(source_summary["source_share_v278"].max()) == pytest.approx(0.8999851829900726)

    constraints = _read_csv("paper4_v278_restricted_pool_milp_constraints.csv")
    assert len(constraints) == status["constraint_rows_v278"]
    constraint_counts = constraints["constraint_type_v278"].value_counts().to_dict()
    assert constraint_counts["cvar_path_excess"] == 128
    assert constraint_counts["source_share"] == 45
    assert constraint_counts["budget_range"] == 1
    assert constraint_counts["selected_row_cardinality"] == 1
    assert constraint_counts["cvar_cap"] == 1

    blockers = _read_csv("paper4_v278_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v278"], blockers["blocking_v278"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v278"], blockers["evidence_count_v278"], strict=False)
    )
    assert bool(blocker_map["restricted_pool_improvement_found"]) is True
    assert int(evidence_map["restricted_pool_improvement_found"]) == 1
    assert bool(blocker_map["restricted_pool_not_full_universe"]) is True
    assert int(evidence_map["restricted_pool_not_full_universe"]) == status["pool_rows_v278"]
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v278_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v278_restricted_pool_milp_probe_executed"]) is True
    assert bool(claim_map["v278_restricted_pool_improvement_found"]) is True
    assert bool(claim_map["v278_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v278_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v278 restricted-pool MILP/gap probe after v277."])
    assert bool(boundary_map["v278 finds a better restricted-pool MILP incumbent than v276."])
    assert bool(boundary_map["v278 proves global full-universe integer optimality."]) is False
    assert bool(boundary_map["v278 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v278_rows = backlog.loc[backlog["last_wave"].eq("v278")]
    assert len(v278_rows) == 1
    backlog_row = v278_rows.iloc[0]
    assert backlog_row["status"] == "restricted_pool_milp_improvement_found"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v279_apply_restricted_pool_candidate_or_decompose_swaps.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v278: Restricted-Pool MILP/Gap Probe After v277" in notebook
    assert "Objective delta vs v276:\n  `8.757896765837359`" in notebook
    assert "CVaR90 delta vs v276:\n  `-275.91294424973603`" in notebook
    assert "Better restricted-pool incumbent found:\n  `True`" in notebook
    assert "does not prove full-universe optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v279_restricted_pool_milp_repair_requires_repricing() -> None:
    status = _read_json("paper4_v279_status.json")

    assert status["phase"] == "v279_restricted_pool_milp_repair"
    assert status["schema_version"] == "2026-05-15.279"
    assert status["source_probe_version_v279"] == 278
    assert status["allocation_rows_v279"] == 171
    assert status["summary_rows_v279"] == 1
    assert status["action_rows_v279"] == 10
    assert status["source_summary_rows_v279"] == 51
    assert status["claim_blocker_rows_v279"] == 4
    assert status["claim_matrix_rows_v279"] == 5
    assert status["selected_rows_v279"] == 171
    assert status["added_rows_v279"] == 5
    assert status["dropped_rows_v279"] == 5
    assert status["portfolio_exposure_v279"] == pytest.approx(843625.0)
    assert status["objective_return_v279"] == pytest.approx(3102.542415185826)
    assert status["scenario_loss_cvar90_v279"] == pytest.approx(98435.12427343646)
    assert status["source_cap_violations_v279"] == 0
    assert status["delta_return_vs_v276_v279"] == pytest.approx(8.757896765837359)
    assert status["delta_cvar90_vs_v276_v279"] == pytest.approx(-275.91294424973603)
    assert status["delta_exposure_vs_v276_v279"] == pytest.approx(100.0)
    assert status["budget_feasible_v279"] is True
    assert status["source_feasible_v279"] is True
    assert status["cvar_feasible_v279"] is True
    assert status["repair_candidate_feasible_v279"] is True
    assert status["post_repair_local_optimality_claim_allowed_v279"] is False
    assert status["restricted_pool_global_optimality_claim_allowed_v279"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v279"] is False
    assert status["paper1_promotion_allowed_v279"] is False
    assert status["paper4_working_champion_changed_v279"] is False
    assert status["paper4_final_promotion_created"] is False

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v279_restricted_pool_milp_repair_allocations.parquet"
    )
    assert len(allocations) == status["allocation_rows_v279"]
    assert int(allocations["selected_v279"].sum()) == status["selected_rows_v279"]
    action_counts = allocations["repair_action_v279"].value_counts().to_dict()
    assert action_counts["added_by_restricted_pool_milp"] == status["added_rows_v279"]
    assert action_counts["kept_from_v276"] == 166
    assert allocations["claim_boundary_v279"].str.contains("requires post-repair repricing").all()

    summary = _read_csv("paper4_v279_restricted_pool_milp_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v279"] == "restricted_pool_milp_repair_candidate"
    assert int(row["source_probe_version_v279"]) == 278
    assert bool(row["repair_candidate_feasible_v279"]) is True
    assert bool(row["post_repair_pricing_required_v279"]) is True
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v279"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v279"]) is False
    assert float(row["max_source_share_v279"]) == pytest.approx(0.8999851829900726)
    assert float(row["min_source_slack_v279"]) == pytest.approx(1.0773302159261533e-06)
    assert "must rerun post-repair pricing" in str(row["claim_boundary_v279"])

    action = _read_csv("paper4_v279_restricted_pool_milp_repair_action.csv")
    assert len(action) == status["action_rows_v279"]
    added_ids = set(
        action.loc[action["action_v279"].eq("added_by_restricted_pool_milp"), "loan_id"].astype(str)
    )
    dropped_ids = set(
        action.loc[action["action_v279"].eq("dropped_by_restricted_pool_milp"), "loan_id"].astype(
            str
        )
    )
    assert added_ids == {
        "137182894",
        "138240169",
        "152268188",
        "159357211",
        "170387634",
    }
    assert dropped_ids == {
        "138138354",
        "143359641",
        "145359336",
        "156899870",
        "160508188",
    }

    source_summary = _read_csv("paper4_v279_restricted_pool_milp_repair_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v279"]
    assert not source_summary["source_cap_violated_v279"].astype(bool).any()

    blockers = _read_csv("paper4_v279_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v279"], blockers["blocking_v279"], strict=False))
    assert bool(blocker_map["restricted_pool_milp_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v279_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v279_restricted_pool_milp_repair_executed"]) is True
    assert bool(claim_map["v279_repair_candidate_feasible"]) is True
    assert bool(claim_map["v279_post_repair_local_optimality"]) is False
    assert bool(claim_map["v279_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v279_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v279 restricted-pool MILP repair candidate."])
    assert bool(boundary_map["v279 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v279 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v279 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v279_rows = backlog.loc[backlog["last_wave"].eq("v279")]
    assert len(v279_rows) == 1
    backlog_row = v279_rows.iloc[0]
    assert backlog_row["status"] == "post_restricted_pool_milp_repair_pricing_required"
    assert backlog_row["next_artifact"] == "paper4_v280_post_restricted_pool_milp_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v279: Restricted-Pool MILP Repair Candidate" in notebook
    assert "Return delta vs v276:\n  `8.757896765837359`" in notebook
    assert "CVaR90 delta vs v276:\n  `-275.91294424973603`" in notebook
    assert "does not make\nthe portfolio locally or globally optimal" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v280_post_restricted_pool_milp_reprice_clears_local_screen() -> None:
    status = _read_json("paper4_v280_status.json")

    assert status["phase"] == "v280_post_v279_restricted_pool_milp_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.280"
    assert status["summary_rows_v280"] == 1
    assert status["stage_summary_rows_v280"] == 6
    assert status["candidate_pair_rows_v280"] == 0
    assert status["top_candidate_rows_v280"] == 0
    assert status["claim_blocker_rows_v280"] == 4
    assert status["claim_matrix_rows_v280"] == 4
    assert status["selected_rows_v280"] == 171
    assert status["candidate_add_rows_v280"] == 276698
    assert status["total_pair_rows_screened_v280"] == 47315358
    assert status["return_improving_pair_rows_v280"] == 1642847
    assert status["budget_return_feasible_pair_rows_v280"] == 975412
    assert status["source_prefilter_pair_rows_v280"] == 14826
    assert status["source_exact_pair_rows_v280"] == 0
    assert status["cvar_feasible_pair_rows_v280"] == 0
    assert status["one_swap_improving_rows_v280"] == 0
    assert status["best_one_swap_return_delta_v280"] is None
    assert status["best_one_swap_cvar90_after_v280"] is None
    assert status["current_exposure_v280"] == pytest.approx(843625.0)
    assert status["current_objective_return_v280"] == pytest.approx(3102.542415185826)
    assert status["post_restricted_pool_milp_one_swap_local_optimality_cleared_v280"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v280"] is False
    assert status["paper1_promotion_allowed_v280"] is False
    assert status["paper4_working_champion_changed_v280"] is False
    assert status["paper4_final_promotion_created"] is False

    candidates = _read_csv("paper4_v280_post_restricted_pool_milp_one_swap_reprice.csv")
    top = _read_csv("paper4_v280_post_restricted_pool_milp_one_swap_top_candidates.csv")
    assert candidates.empty
    assert top.empty
    assert "added_loan_id_v280" in candidates.columns
    assert "return_delta_v280" in top.columns

    summary = _read_csv("paper4_v280_post_restricted_pool_milp_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["regime_v280"] == "post_v279_restricted_pool_milp_repair"
    assert int(row["selected_rows_v280"]) == status["selected_rows_v280"]
    assert int(row["one_swap_improving_rows_v280"]) == 0
    assert bool(row["post_restricted_pool_milp_one_swap_local_optimality_cleared_v280"]) is True
    assert bool(row["full_universe_integer_optimality_claim_allowed_v280"]) is False
    assert bool(row["paper1_promotion_allowed_v280"]) is False
    assert bool(row["paper4_final_promotion_created_v280"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v280"])

    stage = _read_csv("paper4_v280_post_restricted_pool_milp_one_swap_stage_summary.csv")
    assert len(stage) == status["stage_summary_rows_v280"]
    stage_map = dict(zip(stage["stage_v280"], stage["pair_rows_v280"], strict=False))
    assert int(stage_map["all_pairs"]) == status["total_pair_rows_screened_v280"]
    assert int(stage_map["return_improving"]) == status["return_improving_pair_rows_v280"]
    assert (
        int(stage_map["budget_return_feasible"]) == status["budget_return_feasible_pair_rows_v280"]
    )
    assert int(stage_map["source_prefilter_feasible"]) == status["source_prefilter_pair_rows_v280"]
    assert int(stage_map["source_exact_feasible"]) == status["source_exact_pair_rows_v280"]
    assert int(stage_map["cvar_feasible_improving"]) == status["cvar_feasible_pair_rows_v280"]

    blockers = _read_csv("paper4_v280_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v280"], blockers["blocking_v280"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v280"], blockers["evidence_count_v280"], strict=False)
    )
    assert bool(blocker_map["post_v279_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v279_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["broader_multi_swap_search_not_exhaustive"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v280_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v280_post_v279_restricted_pool_milp_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v280_post_v279_one_swap_local_optimality"]) is True
    assert bool(claim_map["v280_multi_swap_or_global_integer_optimality"]) is False
    assert bool(claim_map["v280_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v280 post-v279 restricted-pool MILP one-swap pricing screen."]
    )
    assert bool(
        boundary_map[
            "v280 proves the v279 restricted-pool MILP repaired portfolio is one-swap locally "
            "optimal."
        ]
    )
    assert (
        bool(boundary_map["v280 proves multi-swap or global full-universe integer optimality."])
        is False
    )
    assert bool(boundary_map["v280 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v280_rows = backlog.loc[backlog["last_wave"].eq("v280")]
    assert len(v280_rows) == 1
    backlog_row = v280_rows.iloc[0]
    assert (
        backlog_row["status"]
        == "one_swap_local_optimality_cleared_after_restricted_pool_milp_repair"
    )
    assert backlog_row["next_artifact"] == "paper4_v281_broader_multi_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v280: Post-v279 Restricted-Pool MILP One-Swap Repricing" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best post-v279 one-swap return delta:\n  `not applicable; no feasible improving one-swaps`"
    ) in notebook
    assert "Post-v279 one-swap local optimality cleared:\n  `True`" in notebook
    assert "broader multi-swap/global integer evidence" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v281_larger_restricted_pool_milp_records_no_improvement() -> None:
    status = _read_json("paper4_v281_status.json")

    assert status["phase"] == "v281_restricted_pool_milp_gap_probe_after_v280"
    assert status["schema_version"] == "2026-05-15.281"
    assert status["base_repair_version_v281"] == 279
    assert status["terminal_reprice_version_v281"] == 280
    assert status["bounded_two_swap_version_v281"] == 268
    assert status["previous_restricted_pool_milp_version_v281"] == 278
    assert status["candidate_pool_limit_v281"] == 5000
    assert status["pool_rows_v281"] == 5183
    assert status["current_selected_rows_v281"] == 171
    assert status["omitted_candidate_rows_v281"] == 5012
    assert status["selected_rows_v281"] == 171
    assert status["added_rows_v281"] == 0
    assert status["dropped_rows_v281"] == 0
    assert status["objective_return_v281"] == pytest.approx(3102.5424151858256)
    assert status["objective_delta_vs_v279_v281"] == pytest.approx(-4.547473508864641e-13)
    assert status["portfolio_exposure_v281"] == pytest.approx(843625.0)
    assert status["scenario_loss_cvar90_v281"] == pytest.approx(98435.12427343646)
    assert status["cvar90_delta_vs_v279_v281"] == pytest.approx(0.0)
    assert status["source_cap_violations_v281"] == 0
    assert status["budget_feasible_v281"] is True
    assert status["cvar_feasible_v281"] is True
    assert status["source_feasible_v281"] is True
    assert status["restricted_pool_improvement_found_v281"] is False
    assert status["milp_success_v281"] is True
    assert status["milp_incumbent_available_v281"] is True
    assert status["milp_status_v281"] == 0
    assert status["milp_gap_v281"] == pytest.approx(0.0)
    assert status["milp_dual_bound_v281"] == pytest.approx(-3102.5424151854977)
    assert status["constraint_rows_v281"] == 176
    assert status["restricted_pool_global_optimality_claim_allowed_v281"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v281"] is False
    assert status["paper1_promotion_allowed_v281"] is False
    assert status["paper4_working_champion_changed_v281"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v281"] == 4
    assert status["claim_matrix_rows_v281"] == 4

    summary = _read_csv("paper4_v281_restricted_pool_milp_summary.csv")
    row = summary.iloc[0]
    assert row["probe_label_v281"] == "restricted_pool_milp_after_v280_no_two_swap_improvement"
    assert bool(row["restricted_pool_improvement_found_v281"]) is False
    assert bool(row["restricted_pool_global_optimality_claim_allowed_v281"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v281"]) is False
    assert bool(row["paper1_promotion_allowed_v281"]) is False
    assert bool(row["paper4_final_promotion_created"]) is False
    assert "not full-universe global proof" in str(row["claim_boundary_v281"])

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v281_restricted_pool_milp_allocations.parquet"
    )
    assert len(allocations) == status["pool_rows_v281"]
    assert int(allocations["selected_v281"].sum()) == status["selected_rows_v281"]
    assert int(allocations["current_selected_v281"].sum()) == status["current_selected_rows_v281"]
    action_counts = allocations["milp_action_v281"].value_counts().to_dict()
    assert action_counts["kept_from_v279"] == 171
    assert action_counts["not_selected_candidate"] == 5012
    assert "added_by_restricted_pool_milp" not in action_counts
    assert "dropped_by_restricted_pool_milp" not in action_counts

    source_summary = _read_csv("paper4_v281_restricted_pool_milp_source_summary.csv")
    assert len(source_summary) == 51
    assert not source_summary["source_cap_violated_v281"].astype(bool).any()
    assert float(source_summary["source_slack_v281"].min()) == pytest.approx(1.0773302159261533e-06)
    assert float(source_summary["source_share_v281"].max()) == pytest.approx(0.8999851829900726)

    constraints = _read_csv("paper4_v281_restricted_pool_milp_constraints.csv")
    assert len(constraints) == status["constraint_rows_v281"]
    constraint_counts = constraints["constraint_type_v281"].value_counts().to_dict()
    assert constraint_counts["cvar_path_excess"] == 128
    assert constraint_counts["source_share"] == 45
    assert constraint_counts["budget_range"] == 1
    assert constraint_counts["selected_row_cardinality"] == 1
    assert constraint_counts["cvar_cap"] == 1

    blockers = _read_csv("paper4_v281_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v281"], blockers["blocking_v281"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v281"], blockers["evidence_count_v281"], strict=False)
    )
    assert bool(blocker_map["restricted_pool_improvement_found"]) is False
    assert int(evidence_map["restricted_pool_improvement_found"]) == 0
    assert bool(blocker_map["restricted_pool_not_full_universe"]) is True
    assert int(evidence_map["restricted_pool_not_full_universe"]) == status["pool_rows_v281"]
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v281_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v281_restricted_pool_milp_probe_executed"]) is True
    assert bool(claim_map["v281_restricted_pool_improvement_found"]) is False
    assert bool(claim_map["v281_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v281_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v281 restricted-pool MILP/gap probe after v280."])
    assert (
        bool(boundary_map["v281 finds a better restricted-pool MILP incumbent than v279."]) is False
    )
    assert bool(boundary_map["v281 proves global full-universe integer optimality."]) is False
    assert bool(boundary_map["v281 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v281_rows = backlog.loc[backlog["last_wave"].eq("v281")]
    assert len(v281_rows) == 1
    backlog_row = v281_rows.iloc[0]
    assert backlog_row["status"] == "restricted_pool_milp_gap_or_no_improvement_recorded"
    assert backlog_row["next_artifact"] == "paper4_v282_full_universe_gap_certificate_protocol.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v281: Restricted-Pool MILP/Gap Probe After v280" in notebook
    assert "Objective delta vs v279:\n  `-4.547473508864641e-13`" in notebook
    assert "Better restricted-pool incumbent found:\n  `False`" in notebook
    assert "does not prove full-universe optimality" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v282_full_universe_gap_certificate_protocol_blocks_global_claim() -> None:
    status = _read_json("paper4_v282_status.json")

    assert status["phase"] == "v282_full_universe_gap_certificate_protocol"
    assert status["schema_version"] == "2026-05-15.282"
    assert status["prior_restricted_pool_version_v282"] == 281
    assert status["incumbent_repair_version_v282"] == 279
    assert status["terminal_reprice_version_v282"] == 280
    assert status["universe_rows_v282"] == 276869
    assert status["selected_rows_v282"] == 171
    assert status["full_omitted_candidate_rows_v282"] == 276698
    assert status["restricted_pool_rows_v282"] == 5183
    assert status["restricted_omitted_candidate_rows_v282"] == 5012
    assert status["restricted_pool_coverage_share_v282"] == pytest.approx(0.018720044497578275)
    assert status["restricted_omitted_coverage_share_v282"] == pytest.approx(0.01811361122957159)
    assert status["prior_restricted_pool_gap_v282"] == pytest.approx(0.0)
    assert status["prior_restricted_pool_improvement_found_v282"] is False
    assert status["estimated_full_binary_variables_v282"] == 276869
    assert status["estimated_full_continuous_variables_v282"] == 129
    assert status["estimated_full_constraint_rows_v282"] == 176
    assert status["requirement_rows_v282"] == 6
    assert status["requirements_satisfied_v282"] == 2
    assert status["claim_blocker_rows_v282"] == 4
    assert status["claim_matrix_rows_v282"] == 5
    assert status["full_universe_model_built_v282"] is False
    assert status["full_universe_bound_probe_executed_v282"] is False
    assert status["full_universe_gap_certificate_available_v282"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v282"] is False
    assert status["paper1_promotion_allowed_v282"] is False
    assert status["paper4_working_champion_changed_v282"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v282"] == "paper4_v283_full_universe_integer_bound_probe.csv"

    protocol = _read_csv("paper4_v282_full_universe_gap_certificate_protocol.csv")
    row = protocol.iloc[0]
    assert row["protocol_id_v282"] == "full_v55_integer_gap_certificate_protocol"
    assert bool(row["terminal_one_swap_cleared_v282"]) is True
    assert bool(row["full_universe_model_built_v282"]) is False
    assert bool(row["full_universe_bound_probe_executed_v282"]) is False
    assert bool(row["full_universe_gap_certificate_available_v282"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v282"]) is False
    assert "not a full-v55 gap certificate" in str(row["claim_boundary_v282"])

    requirements = _read_csv("paper4_v282_gap_certificate_requirements.csv")
    assert len(requirements) == status["requirement_rows_v282"]
    requirement_map = dict(
        zip(
            requirements["requirement_id_v282"],
            requirements["satisfied_by_current_artifacts_v282"],
            strict=False,
        )
    )
    assert bool(requirement_map["incumbent_feasibility_audited"]) is True
    assert bool(requirement_map["restricted_pool_gap_zero"]) is True
    assert bool(requirement_map["complete_v55_candidate_coverage"]) is False
    assert bool(requirement_map["full_integer_model_or_branch_price_built"]) is False
    assert bool(requirement_map["valid_global_dual_bound_or_gap"]) is False
    assert bool(requirement_map["promotion_gate_and_dynamic_replay"]) is False
    missing = set(requirements["missing_artifact_v282"].dropna().astype(str))
    assert "paper4_v283_full_universe_integer_bound_probe.csv" in missing
    assert "future_dynamic_replay_and_promotion_gate" in missing

    blockers = _read_csv("paper4_v282_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v282"], blockers["blocking_v282"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v282"], blockers["evidence_count_v282"], strict=False)
    )
    assert bool(blocker_map["restricted_pool_not_full_universe"]) is True
    assert int(evidence_map["restricted_pool_not_full_universe"]) == 271686
    assert bool(blocker_map["full_universe_gap_certificate_missing"]) is True
    assert bool(blocker_map["global_integer_optimality_claim_blocked"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v282_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v282_full_universe_gap_protocol_executed"]) is True
    assert bool(claim_map["v282_v281_restricted_pool_no_improvement"]) is True
    assert bool(claim_map["v282_full_universe_gap_certificate"]) is False
    assert bool(claim_map["v282_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v282_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v282 full-universe gap-certificate protocol."])
    assert bool(
        boundary_map["v281/v282 prove no improvement inside the expanded top-5000 restricted pool."]
    )
    assert bool(boundary_map["v282 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v282 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v282_rows = backlog.loc[backlog["last_wave"].eq("v282")]
    assert len(v282_rows) == 1
    backlog_row = v282_rows.iloc[0]
    assert backlog_row["status"] == "full_universe_gap_certificate_protocol_created"
    assert backlog_row["next_artifact"] == "paper4_v283_full_universe_integer_bound_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v282: Full-Universe Gap-Certificate Protocol" in notebook
    assert "Full-universe certificate available:\n  `False`" in notebook
    assert "Exact full-universe claim allowed:\n  `False`" in notebook
    assert "does not cover most v55 candidates" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v283_full_universe_bound_probe_records_resource_guard() -> None:
    status = _read_json("paper4_v283_status.json")

    assert status["phase"] == "v283_full_universe_integer_bound_probe"
    assert status["schema_version"] == "2026-05-15.283"
    assert status["protocol_version_v283"] == 282
    assert status["incumbent_repair_version_v283"] == 279
    assert status["terminal_reprice_version_v283"] == 280
    assert status["restricted_pool_version_v283"] == 281
    assert status["full_binary_variables_v283"] == 276869
    assert status["full_continuous_variables_v283"] == 129
    assert status["full_constraint_rows_v283"] == 176
    assert status["full_omitted_candidate_rows_v283"] == 276698
    assert status["max_binary_vars_for_direct_mip_v283"] == 50000
    assert status["direct_full_mip_attempted_v283"] is False
    assert status["resource_guard_reason_v283"] == "binary_variable_count_exceeds_direct_mip_guard"
    assert (
        status["manifest_sha256_v283"]
        == "bb80bdd4f7cee9fbd00947361b13f263d90484a2ab9efd4dcca54ca945c794c3"
    )
    assert status["valid_full_universe_gap_certificate_v283"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v283"] is False
    assert status["paper1_promotion_allowed_v283"] is False
    assert status["paper4_working_champion_changed_v283"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v283"] == 4
    assert status["claim_matrix_rows_v283"] == 5
    assert status["next_artifact_v283"] == "paper4_v284_decomposition_or_branch_price_prototype.csv"

    probe = _read_csv("paper4_v283_full_universe_integer_bound_probe.csv")
    row = probe.iloc[0]
    assert row["probe_id_v283"] == "direct_full_v55_integer_mip_bound_probe"
    assert bool(row["direct_full_mip_attempted_v283"]) is False
    assert row["direct_full_mip_attempt_result_v283"] == "not_executed_resource_guard"
    assert row["resource_guard_reason_v283"] == status["resource_guard_reason_v283"]
    assert bool(row["valid_full_universe_gap_certificate_v283"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v283"]) is False
    assert "decomposition or branch-price required" in str(row["claim_boundary_v283"])

    manifest = _read_csv("paper4_v283_full_universe_model_manifest.csv")
    manifest_row = manifest.iloc[0]
    assert manifest_row["manifest_id_v283"] == "full_v55_integer_model_manifest"
    assert manifest_row["manifest_sha256_v283"] == status["manifest_sha256_v283"]
    assert int(manifest_row["binary_variables_v283"]) == status["full_binary_variables_v283"]
    assert (
        int(manifest_row["continuous_variables_v283"]) == status["full_continuous_variables_v283"]
    )
    assert int(manifest_row["constraint_rows_v283"]) == status["full_constraint_rows_v283"]
    assert "no solve or global bound certificate" in str(manifest_row["claim_boundary_v283"])

    blockers = _read_csv("paper4_v283_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v283"], blockers["blocking_v283"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v283"], blockers["evidence_count_v283"], strict=False)
    )
    assert bool(blocker_map["direct_full_v55_mip_resource_guarded"]) is True
    assert int(evidence_map["direct_full_v55_mip_resource_guarded"]) == 276869
    assert bool(blocker_map["global_integer_bound_missing"]) is True
    assert bool(blocker_map["decomposition_or_branch_price_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v283_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v283_full_universe_bound_probe_executed"]) is True
    assert bool(claim_map["v283_full_universe_model_manifest_created"]) is True
    assert bool(claim_map["v283_valid_full_universe_gap_certificate"]) is False
    assert bool(claim_map["v283_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v283_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v283 full-universe integer bound/resource probe."])
    assert bool(boundary_map["v283 proves full-universe global integer optimality."]) is False
    assert (
        bool(boundary_map["v283 authorizes Paper Estrella replacement or final Paper 4 promotion."])
        is False
    )

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v283_rows = backlog.loc[backlog["last_wave"].eq("v283")]
    assert len(v283_rows) == 1
    backlog_row = v283_rows.iloc[0]
    assert backlog_row["status"] == "full_universe_bound_probe_resource_guarded"
    assert backlog_row["next_artifact"] == "paper4_v284_decomposition_or_branch_price_prototype.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v283: Full-Universe Integer Bound/Resource Probe" in notebook
    assert "Direct full-v55 MIP attempted:\n  `False`" in notebook
    assert "Resource guard reason:\n  `binary_variable_count_exceeds_direct_mip_guard`" in notebook
    assert "Valid full-universe gap certificate available:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v284_decomposition_branch_price_prototype_identifies_tight_blocks() -> None:
    status = _read_json("paper4_v284_status.json")

    assert status["phase"] == "v284_decomposition_branch_price_prototype"
    assert status["schema_version"] == "2026-05-15.284"
    assert status["bound_probe_version_v284"] == 283
    assert status["incumbent_repair_version_v284"] == 279
    assert status["restricted_pool_version_v284"] == 281
    assert status["full_omitted_candidate_rows_v284"] == 276698
    assert status["tight_source_threshold_v284"] == pytest.approx(0.0001)
    assert status["tight_source_rows_v284"] == 2
    assert status["tight_source_candidate_rows_v284"] == 99878
    assert status["positive_return_tight_candidate_rows_v284"] == 8827
    assert status["direct_full_mip_attempted_v284"] is False
    assert status["decomposition_prototype_executed_v284"] is True
    assert status["pricing_screen_executed_v284"] is False
    assert status["valid_branch_price_bound_v284"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v284"] is False
    assert status["paper1_promotion_allowed_v284"] is False
    assert status["paper4_working_champion_changed_v284"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v284"] == 4
    assert status["claim_matrix_rows_v284"] == 5
    assert status["next_artifact_v284"] == "paper4_v285_source_tight_pricing_screen.csv"

    prototype = _read_csv("paper4_v284_decomposition_or_branch_price_prototype.csv")
    row = prototype.iloc[0]
    assert row["prototype_id_v284"] == "source_tight_decomposition_branch_price_prototype"
    assert bool(row["decomposition_prototype_executed_v284"]) is True
    assert bool(row["pricing_screen_executed_v284"]) is False
    assert bool(row["valid_branch_price_bound_v284"]) is False
    assert bool(row["full_universe_integer_optimality_claim_allowed_v284"]) is False
    assert "no source-tight pricing or global bound yet" in str(row["claim_boundary_v284"])

    blocks = _read_csv("paper4_v284_source_tight_pricing_blocks.csv")
    assert len(blocks) == status["tight_source_rows_v284"]
    block_map = dict(zip(blocks["pricing_block_id_v284"], blocks.index, strict=False))
    assert set(block_map) == {"grade=A", "score_decile=0"}
    grade_row = blocks.loc[block_map["grade=A"]]
    score_row = blocks.loc[block_map["score_decile=0"]]
    assert int(grade_row["candidate_rows_v284"]) == 72300
    assert int(grade_row["positive_return_candidate_rows_v284"]) == 4415
    assert int(score_row["candidate_rows_v284"]) == 27578
    assert int(score_row["positive_return_candidate_rows_v284"]) == 4412
    assert str(grade_row["top_candidate_loan_id_v284"]) == "165382274"
    assert str(score_row["top_candidate_loan_id_v284"]) == "165382274"
    assert float(grade_row["incumbent_source_slack_v284"]) == pytest.approx(1.0773302159261533e-06)
    assert float(score_row["incumbent_source_slack_v284"]) == pytest.approx(1.4817009927374687e-05)

    blockers = _read_csv("paper4_v284_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v284"], blockers["blocking_v284"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v284"], blockers["evidence_count_v284"], strict=False)
    )
    assert bool(blocker_map["source_tight_pricing_screen_missing"]) is True
    assert int(evidence_map["source_tight_pricing_screen_missing"]) == 2
    assert bool(blocker_map["branch_price_dual_bound_missing"]) is True
    assert bool(blocker_map["global_integer_optimality_claim_blocked"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v284_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v284_decomposition_branch_price_prototype_executed"]) is True
    assert bool(claim_map["v284_source_tight_pricing_blocks_identified"]) is True
    assert bool(claim_map["v284_valid_branch_price_bound"]) is False
    assert bool(claim_map["v284_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v284_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v284 decomposition/branch-price prototype."])
    assert bool(boundary_map["v284 proves a valid full-universe branch-price bound."]) is False
    assert bool(boundary_map["v284 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v284_rows = backlog.loc[backlog["last_wave"].eq("v284")]
    assert len(v284_rows) == 1
    backlog_row = v284_rows.iloc[0]
    assert backlog_row["status"] == "decomposition_branch_price_prototype_created"
    assert backlog_row["next_artifact"] == "paper4_v285_source_tight_pricing_screen.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v284: Decomposition/Branch-Price Prototype" in notebook
    assert "Tight source rows: `2`" in notebook
    assert "Valid branch-price bound produced:\n  `False`" in notebook
    assert "grade A and score decile 0" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v285_source_tight_pricing_screen_blocks_one_drop_entry() -> None:
    status = _read_json("paper4_v285_status.json")

    assert status["phase"] == "v285_source_tight_pricing_screen"
    assert status["schema_version"] == "2026-05-15.285"
    assert status["decomposition_version_v285"] == 284
    assert status["incumbent_repair_version_v285"] == 279
    assert status["terminal_reprice_version_v285"] == 280
    assert status["tight_candidate_rows_v285"] == 73048
    assert status["positive_tight_candidate_rows_v285"] == 4415
    assert status["positive_joint_grade_a_score0_candidate_rows_v285"] == 4412
    assert status["selected_rows_v285"] == 171
    assert status["total_pair_rows_screened_v285"] == 754965
    assert status["return_improving_pair_rows_v285"] == 456968
    assert status["budget_return_feasible_pair_rows_v285"] == 176697
    assert status["grade_source_feasible_pair_rows_v285"] == 105
    assert status["score_decile_source_feasible_pair_rows_v285"] == 5476
    assert status["joint_grade_score_source_feasible_pair_rows_v285"] == 0
    assert status["source_prefilter_pair_rows_v285"] == 0
    assert status["source_exact_pair_rows_v285"] == 0
    assert status["cvar_feasible_pair_rows_v285"] == 0
    assert status["one_drop_one_add_entering_columns_found_v285"] is False
    assert status["source_tight_pricing_screen_executed_v285"] is True
    assert status["valid_branch_price_bound_v285"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v285"] is False
    assert status["paper1_promotion_allowed_v285"] is False
    assert status["paper4_working_champion_changed_v285"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["candidate_diagnostic_rows_v285"] == 4415
    assert status["stage_summary_rows_v285"] == 17
    assert status["claim_blocker_rows_v285"] == 5
    assert status["claim_matrix_rows_v285"] == 5
    assert status["next_artifact_v285"] == "paper4_v286_joint_source_relief_pricing_protocol.csv"

    screen = _read_csv("paper4_v285_source_tight_pricing_screen.csv")
    row = screen.iloc[0]
    assert row["screen_id_v285"] == "source_tight_positive_candidate_one_drop_one_add_screen"
    assert bool(row["source_tight_pricing_screen_executed_v285"]) is True
    assert bool(row["one_drop_one_add_entering_columns_found_v285"]) is False
    assert bool(row["valid_branch_price_bound_v285"]) is False
    assert "no multi-source relief" in str(row["claim_boundary_v285"])

    stages = _read_csv("paper4_v285_source_tight_pair_stage_summary.csv")
    stage_map = dict(zip(stages["stage_v285"], stages["pair_rows_v285"], strict=False))
    assert int(stage_map["all_positive_tight_pairs"]) == 754965
    assert int(stage_map["return_improving"]) == 456968
    assert int(stage_map["budget_return_feasible"]) == 176697
    assert int(stage_map["grade_source_feasible_alone"]) == 105
    assert int(stage_map["score_decile_source_feasible_alone"]) == 5476
    assert int(stage_map["cumulative_source_prefilter_after_grade"]) == 105
    assert int(stage_map["cumulative_source_prefilter_after_score_decile"]) == 0
    assert int(stage_map["source_exact_feasible"]) == 0
    assert int(stage_map["cvar_feasible_entering_column"]) == 0

    diagnostics = _read_csv("paper4_v285_source_tight_candidate_diagnostics.csv")
    assert len(diagnostics) == status["candidate_diagnostic_rows_v285"]
    top = diagnostics.iloc[0]
    assert str(top["loan_id"]) == "165382274"
    assert top["mean_return_v285"] == pytest.approx(883.9433199290119)
    assert str(top["pricing_block_ids_v285"]) == "grade=A|score_decile=0"
    assert int(top["tight_block_count_v285"]) == 2
    assert int(top["return_improving_drop_rows_v285"]) == 169
    assert int(top["budget_return_drop_rows_v285"]) == 1
    assert int(top["joint_tight_source_feasible_drop_rows_v285"]) == 0
    assert int(top["all_source_prefilter_drop_rows_v285"]) == 0

    pricing_pairs = _read_csv("paper4_v285_source_tight_pricing_pairs.csv")
    assert pricing_pairs.empty

    blockers = _read_csv("paper4_v285_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v285"], blockers["blocking_v285"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v285"], blockers["evidence_count_v285"], strict=False)
    )
    assert bool(blocker_map["joint_tight_source_caps_block_one_drop_pricing"]) is True
    assert int(evidence_map["joint_tight_source_caps_block_one_drop_pricing"]) == 176697
    assert bool(blocker_map["source_exact_pricing_pairs_missing"]) is True
    assert bool(blocker_map["branch_price_dual_bound_missing"]) is True
    assert bool(blocker_map["global_integer_optimality_claim_blocked"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v285_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v285_source_tight_pricing_screen_executed"]) is True
    assert bool(claim_map["v285_no_one_drop_one_add_entering_column_in_tight_screen"]) is True
    assert bool(claim_map["v285_valid_branch_price_bound"]) is False
    assert bool(claim_map["v285_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v285_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v285 source-tight pricing screen."])
    assert bool(
        boundary_map[
            "v285 shows no one-drop/one-add entering column inside positive source-tight blocks."
        ]
    )
    assert bool(boundary_map["v285 proves a valid full-universe branch-price bound."]) is False
    assert bool(boundary_map["v285 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v285_rows = backlog.loc[backlog["last_wave"].eq("v285")]
    assert len(v285_rows) == 1
    backlog_row = v285_rows.iloc[0]
    assert backlog_row["status"] == "source_tight_pricing_screen_blocked_by_joint_caps"
    assert backlog_row["next_artifact"] == "paper4_v286_joint_source_relief_pricing_protocol.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v285: Source-Tight Pricing Screen" in notebook
    assert "Positive-return tight candidate rows: `4415`" in notebook
    assert "All-source prefilter pair rows: `0`" in notebook
    assert "joint tight source caps" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v286_joint_source_relief_protocol_finds_no_top200_entry() -> None:
    status = _read_json("paper4_v286_status.json")

    assert status["phase"] == "v286_exact_joint_source_relief_pricing_protocol"
    assert status["schema_version"] == "2026-05-15.286"
    assert status["source_tight_screen_version_v286"] == 285
    assert status["incumbent_repair_version_v286"] == 279
    assert status["v285_candidate_rows_available_v286"] == 4415
    assert status["candidate_screen_limit_v286"] == 200
    assert status["candidate_rows_screened_v286"] == 200
    assert status["unique_relief_milp_signatures_v286"] == 156
    assert status["relief_milp_success_rows_v286"] == 200
    assert status["source_violation_rows_v286"] == 0
    assert status["cvar_feasible_rows_v286"] == 200
    assert status["return_positive_exact_relief_rows_v286"] == 0
    assert status["exact_relief_entering_column_rows_v286"] == 0
    assert status["best_exact_relief_added_loan_id_v286"] == "161116709"
    assert status["best_exact_relief_return_delta_v286"] == pytest.approx(-4.890156830405999)
    assert status["best_exact_relief_drop_count_v286"] == 1
    assert status["exact_relief_entering_columns_found_v286"] is False
    assert status["valid_branch_price_bound_v286"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v286"] is False
    assert status["paper1_promotion_allowed_v286"] is False
    assert status["paper4_working_champion_changed_v286"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["drop_bundle_rows_v286"] == 724
    assert status["claim_blocker_rows_v286"] == 4
    assert status["claim_matrix_rows_v286"] == 5
    assert status["next_artifact_v286"] == (
        "paper4_v287_expand_exact_relief_or_multi_add_probe.csv"
    )

    protocol = _read_csv("paper4_v286_joint_source_relief_pricing_protocol.csv")
    row = protocol.iloc[0]
    assert row["protocol_id_v286"] == "top200_exact_joint_source_relief_pricing_protocol"
    assert int(row["candidate_rows_screened_v286"]) == status["candidate_rows_screened_v286"]
    assert int(row["return_positive_exact_relief_rows_v286"]) == 0
    assert bool(row["exact_relief_entering_columns_found_v286"]) is False
    assert "broader pricing and global bound evidence remain missing" in str(
        row["claim_boundary_v286"]
    )

    screen = _read_csv("paper4_v286_joint_source_relief_candidate_screen.csv")
    assert len(screen) == status["candidate_rows_screened_v286"]
    assert int(screen["relief_milp_success_v286"].sum()) == 200
    assert int(screen["source_cap_violations_after_exact_relief_v286"].sum()) == 0
    assert int(screen["budget_source_cvar_feasible_exact_relief_v286"].sum()) == 200
    assert int(screen["return_positive_exact_relief_v286"].sum()) == 0
    assert int(screen["exact_relief_entering_column_v286"].sum()) == 0
    top = screen.iloc[0]
    assert str(top["added_loan_id_v286"]) == "161116709"
    assert int(top["candidate_rank_v286"]) == 2
    assert int(top["drop_count_v286"]) == 1
    assert float(top["drop_exposure_v286"]) == pytest.approx(35000.0)
    assert float(top["drop_mean_return_v286"]) == pytest.approx(858.3018037609215)
    assert float(top["return_delta_after_exact_relief_v286"]) == pytest.approx(-4.890156830405999)
    assert float(top["cvar90_after_exact_relief_v286"]) == pytest.approx(99175.908784)
    assert bool(top["budget_source_cvar_feasible_exact_relief_v286"]) is True
    assert bool(top["return_positive_exact_relief_v286"]) is False
    assert bool(top["exact_relief_entering_column_v286"]) is False
    assert bool(top["cardinality_preserved_v286"]) is True

    drop_bundle = _read_csv("paper4_v286_joint_source_relief_drop_bundles.csv")
    assert len(drop_bundle) == status["drop_bundle_rows_v286"]
    best_drop = drop_bundle.loc[drop_bundle["candidate_rank_v286"].eq(2)].iloc[0]
    assert str(best_drop["added_loan_id_v286"]) == "161116709"
    assert str(best_drop["dropped_loan_id_v286"]) == "138240169"
    assert float(best_drop["dropped_loan_amount_v286"]) == pytest.approx(35000.0)

    blockers = _read_csv("paper4_v286_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v286"], blockers["blocking_v286"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v286"], blockers["evidence_count_v286"], strict=False)
    )
    assert bool(blocker_map["top200_exact_relief_no_return_positive_column"]) is True
    assert int(evidence_map["top200_exact_relief_no_return_positive_column"]) == 200
    assert bool(blocker_map["full_ranked_exact_relief_screen_missing"]) is True
    assert int(evidence_map["full_ranked_exact_relief_screen_missing"]) == 4215
    assert bool(blocker_map["branch_price_dual_bound_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v286_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v286_exact_joint_source_relief_protocol_executed"]) is True
    assert bool(claim_map["v286_no_top200_return_positive_exact_relief_column"]) is True
    assert bool(claim_map["v286_valid_branch_price_bound"]) is False
    assert bool(claim_map["v286_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v286_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v286 exact joint source-relief pricing protocol."])
    assert bool(
        boundary_map[
            "v286 finds no return-positive exact joint source-relief column "
            "inside the top-200 source-tight screen."
        ]
    )
    assert bool(boundary_map["v286 proves full-universe branch-price termination."]) is False
    assert bool(boundary_map["v286 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v286_rows = backlog.loc[backlog["last_wave"].eq("v286")]
    assert len(v286_rows) == 1
    backlog_row = v286_rows.iloc[0]
    assert backlog_row["status"] == "top200_exact_source_relief_screen_no_entering_column"
    assert backlog_row["next_artifact"] == "paper4_v287_expand_exact_relief_or_multi_add_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v286: Exact Joint Source-Relief Pricing Protocol" in notebook
    assert "Candidate rows screened: `200`" in notebook
    assert "Return-positive exact relief rows: `0`" in notebook
    assert "best top-200 delta is negative" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v287_multi_add_reinvestment_probe_blocks_second_adds() -> None:
    status = _read_json("paper4_v287_status.json")

    assert status["phase"] == "v287_multi_add_reinvestment_probe"
    assert status["schema_version"] == "2026-05-15.287"
    assert status["exact_relief_version_v287"] == 286
    assert status["incumbent_repair_version_v287"] == 279
    assert status["v286_relief_states_available_v287"] == 200
    assert status["reinvestment_state_limit_v287"] == 25
    assert status["reinvestment_states_screened_v287"] == 25
    assert status["budget_eligible_second_add_rows_v287"] == 1475633
    assert status["return_recovering_second_add_rows_v287"] == 13348
    assert status["source_feasible_second_add_rows_v287"] == 0
    assert status["cvar_feasible_second_add_rows_v287"] == 0
    assert status["reinvestment_entering_column_rows_v287"] == 0
    assert status["reinvestment_entering_columns_found_v287"] is False
    assert status["valid_branch_price_bound_v287"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v287"] is False
    assert status["paper1_promotion_allowed_v287"] is False
    assert status["paper4_working_champion_changed_v287"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["source_feasible_candidate_rows_v287"] == 0
    assert status["stage_summary_rows_v287"] == 4
    assert status["claim_blocker_rows_v287"] == 4
    assert status["claim_matrix_rows_v287"] == 5
    assert status["next_artifact_v287"] == (
        "paper4_v288_full_rank_exact_relief_resource_protocol.csv"
    )

    summary = _read_csv("paper4_v287_multi_add_reinvestment_probe.csv")
    row = summary.iloc[0]
    assert row["probe_id_v287"] == "top25_exact_relief_second_add_reinvestment_probe"
    assert int(row["budget_eligible_second_add_rows_v287"]) == 1475633
    assert int(row["return_recovering_second_add_rows_v287"]) == 13348
    assert int(row["source_feasible_second_add_rows_v287"]) == 0
    assert bool(row["reinvestment_entering_columns_found_v287"]) is False
    assert "broader exact relief and branch-price bounds remain missing" in str(
        row["claim_boundary_v287"]
    )

    states = _read_csv("paper4_v287_reinvestment_state_screen.csv")
    assert len(states) == status["reinvestment_states_screened_v287"]
    assert int(states["budget_eligible_second_add_rows_v287"].sum()) == 1475633
    assert int(states["return_recovering_second_add_rows_v287"].sum()) == 13348
    assert int(states["source_feasible_second_add_rows_v287"].sum()) == 0
    assert int(states["cvar_feasible_second_add_rows_v287"].sum()) == 0
    first = states.iloc[0]
    assert int(first["v286_candidate_rank_v287"]) == 2
    assert str(first["first_added_loan_id_v287"]) == "161116709"
    assert float(first["base_return_delta_v287"]) == pytest.approx(-4.890156830405999)
    assert float(first["budget_headroom_v287"]) == pytest.approx(6375.0)
    assert int(first["budget_eligible_second_add_rows_v287"]) == 58354
    assert int(first["return_recovering_second_add_rows_v287"]) == 1454
    assert str(first["first_source_blocker_v287"]) == "grade=A"
    assert bool(first["reinvestment_entering_column_found_v287"]) is False

    stage_summary = _read_csv("paper4_v287_reinvestment_stage_summary.csv")
    stage_map = dict(
        zip(stage_summary["stage_v287"], stage_summary["row_count_v287"], strict=False)
    )
    assert int(stage_map["budget_eligible_second_add"]) == 1475633
    assert int(stage_map["return_recovering_second_add"]) == 13348
    assert int(stage_map["source_feasible_second_add"]) == 0
    assert int(stage_map["cvar_feasible_second_add"]) == 0

    source_candidates = _read_csv("paper4_v287_source_feasible_second_add_candidates.csv")
    assert source_candidates.empty
    assert "second_added_loan_id_v287" in set(source_candidates.columns)

    blockers = _read_csv("paper4_v287_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v287"], blockers["blocking_v287"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v287"], blockers["evidence_count_v287"], strict=False)
    )
    assert bool(blocker_map["top25_second_add_blocked_by_source_caps"]) is True
    assert int(evidence_map["top25_second_add_blocked_by_source_caps"]) == 13348
    assert bool(blocker_map["full_rank_exact_relief_screen_missing"]) is True
    assert int(evidence_map["full_rank_exact_relief_screen_missing"]) == 175
    assert bool(blocker_map["branch_price_dual_bound_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v287_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v287_multi_add_reinvestment_probe_executed"]) is True
    assert bool(claim_map["v287_no_source_feasible_second_add_reinvestment"]) is True
    assert bool(claim_map["v287_valid_branch_price_bound"]) is False
    assert bool(claim_map["v287_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v287_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v287 multi-add reinvestment probe."])
    assert bool(
        boundary_map[
            "v287 finds no source-feasible second-add reinvestment column in "
            "the top-25 exact relief states."
        ]
    )
    assert bool(boundary_map["v287 proves full-universe branch-price termination."]) is False
    assert bool(boundary_map["v287 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v287_rows = backlog.loc[backlog["last_wave"].eq("v287")]
    assert len(v287_rows) == 1
    backlog_row = v287_rows.iloc[0]
    assert backlog_row["status"] == "top25_reinvestment_second_add_blocked_by_source_caps"
    assert (
        backlog_row["next_artifact"] == "paper4_v288_full_rank_exact_relief_resource_protocol.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v287: Multi-Add Reinvestment Probe" in notebook
    assert "Return-recovering second-add rows:\n  `13348`" in notebook
    assert "Source-feasible second-add rows:\n  `0`" in notebook
    assert "The blocker is therefore source capacity" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v288_exact_relief_rank_expansion_finds_repair_signal() -> None:
    status = _read_json("paper4_v288_status.json")

    assert status["phase"] == "v288_exact_relief_rank_expansion"
    assert status["schema_version"] == "2026-05-15.288"
    assert status["source_tight_screen_version_v288"] == 285
    assert status["previous_exact_relief_version_v288"] == 286
    assert status["incumbent_repair_version_v288"] == 279
    assert status["v285_candidate_rows_available_v288"] == 4415
    assert status["rank_start_v288"] == 201
    assert status["candidate_screen_limit_v288"] == 200
    assert status["candidate_rows_screened_v288"] == 200
    assert status["unique_relief_milp_signatures_v288"] == 190
    assert status["relief_milp_success_rows_v288"] == 200
    assert status["source_violation_rows_v288"] == 0
    assert status["cvar_feasible_rows_v288"] == 200
    assert status["return_positive_exact_relief_rows_v288"] == 1
    assert status["exact_relief_entering_column_rows_v288"] == 1
    assert status["best_exact_relief_added_loan_id_v288"] == "160508188"
    assert status["best_exact_relief_return_delta_v288"] == pytest.approx(0.7027606164280655)
    assert status["best_exact_relief_drop_count_v288"] == 4
    assert status["exact_relief_entering_columns_found_v288"] is True
    assert status["valid_branch_price_bound_v288"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v288"] is False
    assert status["paper1_promotion_allowed_v288"] is False
    assert status["paper4_working_champion_changed_v288"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["drop_bundle_rows_v288"] == 844
    assert status["claim_blocker_rows_v288"] == 4
    assert status["claim_matrix_rows_v288"] == 5
    assert status["next_artifact_v288"] == "paper4_v289_apply_exact_relief_candidate_or_reprice.csv"

    summary = _read_csv("paper4_v288_full_rank_exact_relief_resource_protocol.csv")
    row = summary.iloc[0]
    assert row["protocol_id_v288"] == "rank201_400_exact_source_relief_expansion"
    assert int(row["return_positive_exact_relief_rows_v288"]) == 1
    assert bool(row["exact_relief_entering_columns_found_v288"]) is True
    assert "lab-only repair signal" in str(row["claim_boundary_v288"])

    screen = _read_csv("paper4_v288_exact_relief_candidate_screen.csv")
    assert len(screen) == status["candidate_rows_screened_v288"]
    assert int(screen["relief_milp_success_v288"].sum()) == 200
    assert int(screen["source_cap_violations_after_exact_relief_v288"].sum()) == 0
    assert int(screen["budget_source_cvar_feasible_exact_relief_v288"].sum()) == 200
    assert int(screen["return_positive_exact_relief_v288"].sum()) == 1
    assert int(screen["exact_relief_entering_column_v288"].sum()) == 1
    top = screen.iloc[0]
    assert int(top["candidate_rank_v288"]) == 299
    assert str(top["added_loan_id_v288"]) == "160508188"
    assert int(top["drop_count_v288"]) == 4
    assert float(top["drop_exposure_v288"]) == pytest.approx(11350.0)
    assert float(top["drop_mean_return_v288"]) == pytest.approx(277.3334797251526)
    assert float(top["return_delta_after_exact_relief_v288"]) == pytest.approx(0.7027606164280655)
    assert float(top["cvar90_after_exact_relief_v288"]) == pytest.approx(98339.150736)
    assert bool(top["exact_relief_entering_column_v288"]) is True
    assert bool(top["cardinality_preserved_v288"]) is False

    drop_bundle = _read_csv("paper4_v288_exact_relief_drop_bundles.csv")
    assert len(drop_bundle) == status["drop_bundle_rows_v288"]
    best_drops = drop_bundle.loc[drop_bundle["candidate_rank_v288"].eq(299)]
    assert len(best_drops) == 4
    assert set(best_drops["dropped_loan_id_v288"].astype(str)) == {
        "159357211",
        "137182894",
        "170387634",
        "145033022",
    }

    blockers = _read_csv("paper4_v288_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v288"], blockers["blocking_v288"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v288"], blockers["evidence_count_v288"], strict=False)
    )
    assert bool(blocker_map["rank201_400_exact_relief_entering_column_requires_repair"]) is True
    assert int(evidence_map["rank201_400_exact_relief_entering_column_requires_repair"]) == 1
    assert bool(blocker_map["remaining_ranked_exact_relief_screen_missing"]) is True
    assert int(evidence_map["remaining_ranked_exact_relief_screen_missing"]) == 4015
    assert bool(blocker_map["branch_price_dual_bound_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v288_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v288_exact_relief_rank_expansion_executed"]) is True
    assert bool(claim_map["v288_rank201_400_return_positive_exact_relief_column_found"]) is True
    assert bool(claim_map["v288_valid_branch_price_bound"]) is False
    assert bool(claim_map["v288_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v288_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v288 exact-relief rank-expansion screen."])
    assert bool(
        boundary_map["v288 finds a return-positive exact relief column in v285 ranks 201-400."]
    )
    assert bool(boundary_map["v288 proves full-universe branch-price termination."]) is False
    assert bool(boundary_map["v288 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v288_rows = backlog.loc[backlog["last_wave"].eq("v288")]
    assert len(v288_rows) == 1
    backlog_row = v288_rows.iloc[0]
    assert backlog_row["status"] == "rank201_400_exact_relief_entering_column_found"
    assert backlog_row["next_artifact"] == "paper4_v289_apply_exact_relief_candidate_or_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v288: Exact-Relief Rank Expansion" in notebook
    assert "Return-positive exact relief rows:\n  `1`" in notebook
    assert "repair signal found in v285 ranks 201-400" in notebook
    assert "The next step is applying and repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v289_applies_exact_relief_repair_candidate_without_promotion() -> None:
    status = _read_json("paper4_v289_status.json")

    assert status["phase"] == "v289_apply_exact_relief_repair_candidate"
    assert status["schema_version"] == "2026-05-15.289"
    assert status["signal_version_v289"] == 288
    assert status["base_repair_version_v289"] == 279
    assert status["candidate_rank_v289"] == 299
    assert status["added_loan_id_v289"] == "160508188"
    assert status["dropped_rows_v289"] == 4
    assert status["selected_rows_v289"] == 168
    assert status["base_selected_rows_v289"] == 171
    assert status["cardinality_delta_vs_v279_v289"] == -3
    assert status["portfolio_exposure_v289"] == pytest.approx(843475.0)
    assert status["objective_return_v289"] == pytest.approx(3103.2451758022526)
    assert status["scenario_loss_cvar90_v289"] == pytest.approx(98339.15073561847)
    assert status["source_cap_violations_v289"] == 0
    assert status["delta_return_vs_v279_v289"] == pytest.approx(0.7027606164265308)
    assert status["delta_cvar90_vs_v279_v289"] == pytest.approx(-95.97353781799029)
    assert status["budget_feasible_v289"] is True
    assert status["cvar_feasible_v289"] is True
    assert status["source_feasible_v289"] is True
    assert status["repair_candidate_feasible_v289"] is True
    assert status["cardinality_changed_v289"] is True
    assert status["post_repair_pricing_required_v289"] is True
    assert status["working_champion_claim_allowed_v289"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v289"] is False
    assert status["paper1_promotion_allowed_v289"] is False
    assert status["paper4_working_champion_changed_v289"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v289"] == 4
    assert status["claim_matrix_rows_v289"] == 5
    assert status["next_artifact_v289"] == "paper4_v290_post_exact_relief_reprice.csv"

    summary = _read_csv("paper4_v289_exact_relief_repair_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v289"] == "exact_relief_repair_candidate"
    assert int(row["selected_rows_v289"]) == 168
    assert int(row["dropped_rows_v289"]) == 4
    assert bool(row["repair_candidate_feasible_v289"]) is True
    assert bool(row["working_champion_claim_allowed_v289"]) is False
    assert "repricing required" in str(row["claim_boundary_v289"])

    action = _read_csv("paper4_v289_exact_relief_repair_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v289"]) == "160508188"
    assert str(action_row["dropped_loan_ids_v289"]) == ("137182894|145033022|159357211|170387634")
    assert float(action_row["return_delta_v289"]) == pytest.approx(0.7027606164280655)

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v289_exact_relief_repair_allocations.parquet")
    assert len(allocations) == status["selected_rows_v289"]
    assert "160508188" in set(allocations["loan_id"].astype(str))
    assert not {
        "137182894",
        "145033022",
        "159357211",
        "170387634",
    }.intersection(set(allocations["loan_id"].astype(str)))

    source_summary = _read_csv("paper4_v289_exact_relief_repair_source_summary.csv")
    assert int(source_summary["source_cap_violated_v289"].sum()) == 0
    source_map = {
        (row.source_family, str(row.source_id)): row
        for row in source_summary.itertuples(index=False)
    }
    assert source_map[("grade", "A")].source_slack_v289 == pytest.approx(2.719538986206693e-05)
    assert source_map[("score_decile", "0")].source_slack_v289 == pytest.approx(
        3.2603218826920966e-05
    )

    blockers = _read_csv("paper4_v289_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v289"], blockers["blocking_v289"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v289"], blockers["evidence_count_v289"], strict=False)
    )
    assert bool(blocker_map["post_repair_pricing_missing"]) is True
    assert bool(blocker_map["cardinality_policy_changed"]) is True
    assert int(evidence_map["cardinality_policy_changed"]) == 3
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v289_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v289_exact_relief_repair_applied"]) is True
    assert bool(claim_map["v289_small_return_gain_confirmed"]) is True
    assert bool(claim_map["v289_working_champion"]) is False
    assert bool(claim_map["v289_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v289_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v289 applied exact-relief repair candidate."])
    assert bool(boundary_map["v289 is a new Paper 4 working champion."]) is False
    assert bool(boundary_map["v289 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v289_rows = backlog.loc[backlog["last_wave"].eq("v289")]
    assert len(v289_rows) == 1
    backlog_row = v289_rows.iloc[0]
    assert backlog_row["status"] == "exact_relief_repair_candidate_applied_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v290_post_exact_relief_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v289: Apply Exact-Relief Repair Candidate" in notebook
    assert "Delta return vs v279: `0.7027606164265308`" in notebook
    assert "Cardinality changed: `True`" in notebook
    assert "post-repair repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v290_post_exact_relief_reprice_clears_one_swap_scope() -> None:
    status = _read_json("paper4_v290_status.json")

    assert status["phase"] == "v290_post_v289_exact_relief_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.290"
    assert status["summary_rows_v290"] == 1
    assert status["stage_summary_rows_v290"] == 6
    assert status["candidate_pair_rows_v290"] == 0
    assert status["top_candidate_rows_v290"] == 0
    assert status["claim_blocker_rows_v290"] == 4
    assert status["claim_matrix_rows_v290"] == 5
    assert status["selected_rows_v290"] == 168
    assert status["base_selected_rows_v290"] == 171
    assert status["cardinality_changed_v290"] is True
    assert status["candidate_add_rows_v290"] == 276701
    assert status["total_pair_rows_screened_v290"] == 46485768
    assert status["return_improving_pair_rows_v290"] == 1635277
    assert status["budget_return_feasible_pair_rows_v290"] == 1025144
    assert status["source_prefilter_pair_rows_v290"] == 39316
    assert status["source_exact_pair_rows_v290"] == 0
    assert status["cvar_feasible_pair_rows_v290"] == 0
    assert status["one_swap_improving_rows_v290"] == 0
    assert status["best_one_swap_return_delta_v290"] is None
    assert status["best_one_swap_cvar90_after_v290"] is None
    assert status["current_exposure_v290"] == pytest.approx(843475.0)
    assert status["current_objective_return_v290"] == pytest.approx(3103.245175802253)
    assert status["post_exact_relief_one_swap_local_optimality_cleared_v290"] is True
    assert status["working_champion_claim_allowed_v290"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v290"] is False
    assert status["paper1_promotion_allowed_v290"] is False
    assert status["paper4_working_champion_changed_v290"] is False
    assert status["paper4_final_promotion_created"] is False

    summary = _read_csv("paper4_v290_post_exact_relief_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v289_exact_relief_repair_candidate"
    assert row["regime_v290"] == "post_v289_exact_relief_repair"
    assert bool(row["post_exact_relief_one_swap_local_optimality_cleared_v290"]) is True
    assert bool(row["working_champion_claim_allowed_v290"]) is False
    assert "cardinality/global evidence still missing" in str(row["claim_boundary_v290"])

    stages = _read_csv("paper4_v290_post_exact_relief_one_swap_stage_summary.csv")
    stage_map = dict(zip(stages["stage_v290"], stages["pair_rows_v290"], strict=False))
    assert int(stage_map["all_pairs"]) == 46485768
    assert int(stage_map["return_improving"]) == 1635277
    assert int(stage_map["budget_return_feasible"]) == 1025144
    assert int(stage_map["source_prefilter_feasible"]) == 39316
    assert int(stage_map["source_exact_feasible"]) == 0
    assert int(stage_map["cvar_feasible_improving"]) == 0

    reprice = _read_csv("paper4_v290_post_exact_relief_one_swap_reprice.csv")
    top = _read_csv("paper4_v290_post_exact_relief_one_swap_top_candidates.csv")
    assert reprice.empty
    assert top.empty
    assert "added_loan_id_v290" in set(reprice.columns)
    assert "dropped_loan_id_v290" in set(top.columns)

    blockers = _read_csv("paper4_v290_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v290"], blockers["blocking_v290"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v290"], blockers["evidence_count_v290"], strict=False)
    )
    assert bool(blocker_map["post_v289_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v289_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["cardinality_policy_changed"]) is True
    assert int(evidence_map["cardinality_policy_changed"]) == 3
    assert bool(blocker_map["broader_multi_swap_or_global_gap_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v290_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v290_post_v289_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v290_post_v289_one_swap_local_optimality"]) is True
    assert bool(claim_map["v290_working_champion"]) is False
    assert bool(claim_map["v290_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v290_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v290 post-v289 exact-relief one-swap repricing screen."]
    )
    assert bool(
        boundary_map["v290 clears post-v289 one-swap local optimality in the 168-row repair scope."]
    )
    assert bool(boundary_map["v290 authorizes a new Paper 4 working champion."]) is False
    assert bool(boundary_map["v290 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v290_rows = backlog.loc[backlog["last_wave"].eq("v290")]
    assert len(v290_rows) == 1
    backlog_row = v290_rows.iloc[0]
    assert backlog_row["status"] == "post_v289_one_swap_local_optimality_cleared"
    assert backlog_row["next_artifact"] == "paper4_v291_cardinality_or_multi_swap_protocol.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v290: Post-v289 Exact-Relief One-Swap Repricing" in notebook
    assert "Selected rows: `168`" in notebook
    assert "Exact source-feasible pairs: `0`" in notebook
    assert "Promotion remains blocked" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v291_cardinality_restoration_protocol_blocks_add_only_path() -> None:
    status = _read_json("paper4_v291_status.json")

    assert status["phase"] == "v291_cardinality_restoration_add_only_protocol"
    assert status["schema_version"] == "2026-05-15.291"
    assert status["post_reprice_version_v291"] == 290
    assert status["repair_version_v291"] == 289
    assert status["base_repair_version_v291"] == 279
    assert status["current_selected_rows_v291"] == 168
    assert status["target_selected_rows_v291"] == 171
    assert status["cardinality_deficit_v291"] == 3
    assert status["current_exposure_v291"] == pytest.approx(843475.0)
    assert status["budget_headroom_v291"] == pytest.approx(6525.0)
    assert status["candidate_rows_v291"] == 276701
    assert status["budget_eligible_add_rows_v291"] == 60821
    assert status["return_positive_budget_rows_v291"] == 1678
    assert status["source_feasible_budget_rows_v291"] == 42918
    assert status["return_positive_source_feasible_rows_v291"] == 0
    assert status["cvar_feasible_source_rows_v291"] == 0
    assert status["add_only_restoration_step_feasible_v291"] is False
    assert status["top_diagnostic_rows_v291"] == 500
    assert status["source_blocker_rows_v291"] == 2
    assert status["claim_blocker_rows_v291"] == 4
    assert status["claim_matrix_rows_v291"] == 5
    assert status["working_champion_claim_allowed_v291"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v291"] is False
    assert status["paper1_promotion_allowed_v291"] is False
    assert status["paper4_working_champion_changed_v291"] is False
    assert status["paper4_final_promotion_created"] is False
    assert (
        status["next_artifact_v291"] == "paper4_v292_cardinality_aware_multi_swap_milp_protocol.csv"
    )

    protocol = _read_csv("paper4_v291_cardinality_or_multi_swap_protocol.csv")
    row = protocol.iloc[0]
    assert row["protocol_id_v291"] == "add_only_cardinality_restoration_protocol"
    assert int(row["current_selected_rows_v291"]) == 168
    assert int(row["target_selected_rows_v291"]) == 171
    assert int(row["cardinality_deficit_v291"]) == 3
    assert int(row["return_positive_source_feasible_rows_v291"]) == 0
    assert int(row["cvar_feasible_source_rows_v291"]) == 0
    assert bool(row["add_only_restoration_step_feasible_v291"]) is False
    assert "multi-swap/MILP required" in str(row["claim_boundary_v291"])

    stages = _read_csv("paper4_v291_cardinality_restoration_stage_summary.csv")
    stage_map = dict(zip(stages["stage_v291"], stages["row_count_v291"], strict=False))
    assert int(stage_map["all_omitted_candidates"]) == 276701
    assert int(stage_map["budget_eligible_add"]) == 60821
    assert int(stage_map["return_positive_budget_eligible_add"]) == 1678
    assert int(stage_map["source_feasible_budget_eligible_add"]) == 42918
    assert int(stage_map["return_positive_source_feasible_add"]) == 0
    assert int(stage_map["cvar_feasible_source_add"]) == 0

    source_blockers = _read_csv("paper4_v291_add_only_source_blockers.csv")
    blocker_rows = {
        row.first_source_blocker_v291: row for row in source_blockers.itertuples(index=False)
    }
    assert int(blocker_rows["grade=A"].budget_blocked_rows_v291) == 17426
    assert int(blocker_rows["grade=A"].return_positive_blocked_rows_v291) == 1678
    assert int(blocker_rows["score_decile=0"].budget_blocked_rows_v291) == 477
    assert int(blocker_rows["score_decile=0"].return_positive_blocked_rows_v291) == 0

    diagnostics = _read_csv("paper4_v291_add_only_candidate_diagnostics_top.csv")
    assert len(diagnostics) == status["top_diagnostic_rows_v291"]
    top = diagnostics.iloc[0]
    assert bool(top["return_positive_v291"]) is True
    assert bool(top["source_feasible_v291"]) is False
    assert top["first_source_blocker_v291"] == "grade=A"

    blockers = _read_csv("paper4_v291_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v291"], blockers["blocking_v291"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v291"], blockers["evidence_count_v291"], strict=False)
    )
    assert bool(blocker_map["add_only_positive_restoration_blocked_by_source"]) is True
    assert int(evidence_map["add_only_positive_restoration_blocked_by_source"]) == 1678
    assert bool(blocker_map["add_only_cvar_restoration_blocked"]) is True
    assert int(evidence_map["add_only_cvar_restoration_blocked"]) == 42918
    assert bool(blocker_map["cardinality_deficit_requires_multi_swap"]) is True
    assert int(evidence_map["cardinality_deficit_requires_multi_swap"]) == 3
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v291_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v291_cardinality_restoration_protocol_executed"]) is True
    assert bool(claim_map["v291_add_only_restoration_feasible"]) is False
    assert bool(claim_map["v291_working_champion"]) is False
    assert bool(claim_map["v291_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v291_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v291 cardinality restoration protocol."])
    assert bool(boundary_map["v291 proves the 168-row v289 portfolio can be promoted."]) is False
    assert bool(boundary_map["v291 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v291_rows = backlog.loc[backlog["last_wave"].eq("v291")]
    assert len(v291_rows) == 1
    backlog_row = v291_rows.iloc[0]
    assert backlog_row["status"] == "add_only_cardinality_restoration_blocked"
    assert (
        backlog_row["next_artifact"] == "paper4_v292_cardinality_aware_multi_swap_milp_protocol.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v291: Cardinality Restoration Protocol" in notebook
    assert "Cardinality deficit: `3`" in notebook
    assert "Return-positive source-feasible rows:\n  `0`" in notebook
    assert "cardinality-aware multi-swap or MILP repair" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v292_cardinality_aware_milp_restores_rows_without_champion_claim() -> None:
    status = _read_json("paper4_v292_status.json")

    assert status["phase"] == "v292_cardinality_aware_multi_swap_milp_protocol"
    assert status["schema_version"] == "2026-05-15.292"
    assert status["previous_protocol_version_v292"] == 291
    assert status["repair_version_v292"] == 289
    assert status["base_repair_version_v292"] == 279
    assert status["candidate_pool_limit_v292"] == 15000
    assert status["pool_rows_v292"] == 15168
    assert status["selected_rows_v292"] == 171
    assert status["target_selected_rows_v292"] == 171
    assert status["cardinality_restored_v292"] is True
    assert status["kept_current_rows_v292"] == 167
    assert status["added_rows_v292"] == 4
    assert status["dropped_rows_v292"] == 1
    assert status["portfolio_exposure_v292"] == pytest.approx(843625.0)
    assert status["objective_return_v292"] == pytest.approx(3100.214040676401)
    assert status["delta_return_vs_v289_v292"] == pytest.approx(-3.0311351258519608)
    assert status["delta_return_vs_v279_v292"] == pytest.approx(-2.3283745094249753)
    assert status["scenario_loss_cvar90_v292"] == pytest.approx(98116.55123778542)
    assert status["delta_cvar90_vs_v289_v292"] == pytest.approx(-222.59949783305638)
    assert status["source_cap_violations_v292"] == 0
    assert status["budget_feasible_v292"] is True
    assert status["cvar_feasible_v292"] is True
    assert status["source_feasible_v292"] is True
    assert status["return_beats_v289_v292"] is False
    assert status["return_beats_v279_v292"] is False
    assert status["milp_success_v292"] is True
    assert status["milp_status_v292"] == 0
    assert status["milp_gap_v292"] == pytest.approx(0.0)
    assert status["milp_node_count_v292"] == 79
    assert status["milp_variable_count_v292"] == 15297
    assert status["milp_constraint_rows_v292"] == 177
    assert status["pool_summary_rows_v292"] == 2
    assert status["constraint_summary_rows_v292"] == 5
    assert status["source_summary_rows_v292"] == 51
    assert status["claim_blocker_rows_v292"] == 4
    assert status["claim_matrix_rows_v292"] == 7
    assert status["working_champion_claim_allowed_v292"] is False
    assert status["bounded_pool_optimality_claim_allowed_v292"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v292"] is False
    assert status["paper1_promotion_allowed_v292"] is False
    assert status["paper4_working_champion_changed_v292"] is False
    assert status["paper4_final_promotion_created"] is False
    assert (
        status["next_artifact_v292"]
        == "paper4_v293_cardinality_return_gap_decomposition_or_diverse_pool_probe.csv"
    )

    protocol = _read_csv("paper4_v292_cardinality_aware_multi_swap_milp_protocol.csv")
    row = protocol.iloc[0]
    assert row["protocol_id_v292"] == "bounded_top15000_cardinality_aware_milp"
    assert bool(row["cardinality_restored_v292"]) is True
    assert bool(row["return_beats_v289_v292"]) is False
    assert bool(row["working_champion_claim_allowed_v292"]) is False
    assert bool(row["bounded_pool_optimality_claim_allowed_v292"]) is True
    assert "trails v289/v279 return" in str(row["claim_boundary_v292"])

    action = _read_csv("paper4_v292_cardinality_milp_action.csv")
    action_row = action.iloc[0]
    assert action_row["added_loan_ids_v292"] == "145033022|152791402|158523975|159357211"
    assert str(action_row["dropped_loan_ids_v292"]) == "148029832"
    assert int(action_row["kept_current_rows_v292"]) == 167
    assert bool(action_row["cardinality_restored_v292"]) is True
    assert bool(action_row["return_beats_v289_v292"]) is False

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v292_cardinality_milp_allocations.parquet")
    assert len(allocations) == status["selected_rows_v292"]
    allocation_ids = set(allocations["loan_id"].astype(str))
    assert {"145033022", "152791402", "158523975", "159357211"}.issubset(allocation_ids)
    assert "148029832" not in allocation_ids
    assert allocations["selected_v292"].astype(bool).all()

    pool = _read_csv("paper4_v292_cardinality_milp_pool_summary.csv")
    pool_map = dict(zip(pool["pool_role_v292"], pool["selected_rows_v292"], strict=False))
    assert int(pool_map["current_v289_selected"]) == 167
    assert int(pool_map["top15000_omitted_by_mean_return"]) == 4

    constraints = _read_csv("paper4_v292_cardinality_milp_constraint_summary.csv")
    constraint_map = dict(
        zip(constraints["constraint_type_v292"], constraints["constraint_rows_v292"], strict=False)
    )
    assert int(constraint_map["budget_range"]) == 1
    assert int(constraint_map["selected_row_cardinality"]) == 1
    assert int(constraint_map["source_share"]) == 46
    assert int(constraint_map["cvar_cap"]) == 1
    assert int(constraint_map["cvar_path_excess"]) == 128

    source_summary = _read_csv("paper4_v292_cardinality_milp_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v292"]
    assert int(source_summary["source_cap_violated_v292"].sum()) == 0

    blockers = _read_csv("paper4_v292_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v292"], blockers["blocking_v292"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v292"], blockers["evidence_count_v292"], strict=False)
    )
    assert bool(blocker_map["cardinality_restored_but_return_gap_vs_v289"]) is True
    assert float(evidence_map["cardinality_restored_but_return_gap_vs_v289"]) == pytest.approx(
        3.0311351258519608
    )
    assert bool(blocker_map["bounded_pool_not_full_universe"]) is True
    assert int(evidence_map["bounded_pool_not_full_universe"]) == 261701
    assert bool(blocker_map["dynamic_replay_and_deployment_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v292_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v292_cardinality_aware_milp_protocol_executed"]) is True
    assert bool(claim_map["v292_cardinality_restored_in_bounded_pool"]) is True
    assert bool(claim_map["v292_bounded_pool_milp_optimality"]) is True
    assert bool(claim_map["v292_return_improves_vs_v289"]) is False
    assert bool(claim_map["v292_working_champion"]) is False
    assert bool(claim_map["v292_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v292_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v292 bounded cardinality-aware MILP restoration probe."]
    )
    assert bool(
        boundary_map["v292 restores 171-row cardinality inside the bounded top-15000 pool."]
    )
    assert bool(boundary_map["v292 is a new Paper 4 working champion."]) is False
    assert bool(boundary_map["v292 proves global full-universe integer optimality."]) is False
    assert bool(boundary_map["v292 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v292_rows = backlog.loc[backlog["last_wave"].eq("v292")]
    assert len(v292_rows) == 1
    backlog_row = v292_rows.iloc[0]
    assert backlog_row["status"] == "bounded_cardinality_restored_return_gap_remains"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v293_cardinality_return_gap_decomposition_or_diverse_pool_probe.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v292: Cardinality-Aware Multi-Swap MILP" in notebook
    assert "Selected rows: `171`" in notebook
    assert "Delta return vs v289: `-3.0311351258519608`" in notebook
    assert "cardinality restoration is feasible, but not yet economically dominant" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v293_diverse_pool_probe_finds_bounded_challenger_requiring_reprice() -> None:
    status = _read_json("paper4_v293_status.json")

    assert status["phase"] == "v293_diverse_pool_return_gap_probe"
    assert status["schema_version"] == "2026-05-15.293"
    assert status["previous_protocol_version_v293"] == 292
    assert status["repair_version_v293"] == 289
    assert status["base_repair_version_v293"] == 279
    assert status["top_return_limit_v293"] == 15000
    assert status["micro_relief_limit_v293"] == 10000
    assert status["pool_rows_v293"] == 25168
    assert status["selected_rows_v293"] == 171
    assert status["target_selected_rows_v293"] == 171
    assert status["cardinality_restored_v293"] is True
    assert status["kept_current_rows_v293"] == 154
    assert status["added_rows_v293"] == 17
    assert status["dropped_rows_v293"] == 14
    assert status["portfolio_exposure_v293"] == pytest.approx(842950.0)
    assert status["objective_return_v293"] == pytest.approx(3181.0395871735054)
    assert status["delta_return_vs_v289_v293"] == pytest.approx(77.79441137125241)
    assert status["delta_return_vs_v279_v293"] == pytest.approx(78.4971719876794)
    assert status["delta_return_vs_v292_v293"] == pytest.approx(80.82554649710437)
    assert status["scenario_loss_cvar90_v293"] == pytest.approx(97395.99143344731)
    assert status["delta_cvar90_vs_v289_v293"] == pytest.approx(-943.1593021711597)
    assert status["source_cap_violations_v293"] == 0
    assert status["budget_feasible_v293"] is True
    assert status["cvar_feasible_v293"] is True
    assert status["source_feasible_v293"] is True
    assert status["bounded_pool_challenger_found_v293"] is True
    assert status["post_repair_pricing_required_v293"] is True
    assert status["milp_success_v293"] is True
    assert status["milp_status_v293"] == 0
    assert status["milp_gap_v293"] == pytest.approx(0.0)
    assert status["milp_node_count_v293"] == 13
    assert status["milp_variable_count_v293"] == 25297
    assert status["milp_constraint_rows_v293"] == 182
    assert status["strategy_comparison_rows_v293"] == 2
    assert status["candidate_registry_rows_v293"] == 3
    assert status["pool_summary_rows_v293"] == 3
    assert status["constraint_summary_rows_v293"] == 5
    assert status["source_summary_rows_v293"] == 51
    assert status["return_contribution_rows_v293"] == 31
    assert status["claim_blocker_rows_v293"] == 5
    assert status["claim_matrix_rows_v293"] == 7
    assert status["working_champion_claim_allowed_v293"] is False
    assert status["bounded_pool_optimality_claim_allowed_v293"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v293"] is False
    assert status["paper1_promotion_allowed_v293"] is False
    assert status["paper4_working_champion_changed_v293"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v293"] == "paper4_v294_post_v293_diverse_pool_reprice.csv"

    protocol = _read_csv("paper4_v293_diverse_pool_return_gap_probe.csv")
    row = protocol.iloc[0]
    assert row["protocol_id_v293"] == "top15000_plus_micro_source_relief_gap_probe"
    assert bool(row["bounded_pool_challenger_found_v293"]) is True
    assert bool(row["post_repair_pricing_required_v293"]) is True
    assert bool(row["working_champion_claim_allowed_v293"]) is False
    assert "v294 repricing required" in str(row["claim_boundary_v293"])

    action = _read_csv("paper4_v293_diverse_pool_action.csv")
    action_row = action.iloc[0]
    expected_added = (
        "126788147|134420497|137151950|137182894|137721854|138138354|"
        "139317603|140352540|142448559|143278940|156433716|158316010|"
        "158523975|159357211|163435863|169720067|170387634"
    )
    expected_dropped = (
        "136288831|138239745|138240169|140620222|141017853|141665611|"
        "146058451|146255415|154756272|156192909|162083390|162605401|"
        "165985906|169095548"
    )
    assert action_row["added_loan_ids_v293"] == expected_added
    assert action_row["dropped_loan_ids_v293"] == expected_dropped
    assert int(action_row["kept_current_rows_v293"]) == 154
    assert bool(action_row["cardinality_restored_v293"]) is True
    assert bool(action_row["bounded_pool_challenger_found_v293"]) is True

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v293_diverse_pool_allocations.parquet")
    assert len(allocations) == status["selected_rows_v293"]
    allocation_ids = set(allocations["loan_id"].astype(str))
    assert set(expected_added.split("|")).issubset(allocation_ids)
    assert not set(expected_dropped.split("|")).intersection(allocation_ids)
    assert allocations["selected_v293"].astype(bool).all()

    strategy = _read_csv("paper4_v293_diverse_pool_strategy_comparison.csv")
    strategy_map = {row.strategy_id_v293: row for row in strategy.itertuples(index=False)}
    assert float(strategy_map["v292_top15000_baseline"].delta_return_vs_v289_v293) == pytest.approx(
        -3.0311351258519608
    )
    challenger = strategy_map["v293_top15000_plus_micro_source_relief"]
    assert float(challenger.delta_return_vs_v289_v293) == pytest.approx(77.79441137125241)
    assert float(challenger.delta_return_vs_v292_v293) == pytest.approx(80.82554649710437)
    assert bool(challenger.bounded_pool_challenger_found_v293) is True

    pool = _read_csv("paper4_v293_diverse_pool_pool_summary.csv")
    pool_map = dict(zip(pool["pool_role_v293"], pool["selected_rows_v293"], strict=False))
    assert int(pool_map["current_v289_selected"]) == 154
    assert int(pool_map["top15000_omitted_by_mean_return"]) == 9
    assert int(pool_map["micro_source_relief_outside_top15000"]) == 8

    constraints = _read_csv("paper4_v293_diverse_pool_constraint_summary.csv")
    constraint_map = dict(
        zip(constraints["constraint_type_v293"], constraints["constraint_rows_v293"], strict=False)
    )
    assert int(constraint_map["budget_range"]) == 1
    assert int(constraint_map["selected_row_cardinality"]) == 1
    assert int(constraint_map["source_share"]) == 51
    assert int(constraint_map["cvar_cap"]) == 1
    assert int(constraint_map["cvar_path_excess"]) == 128

    source_summary = _read_csv("paper4_v293_diverse_pool_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v293"]
    assert int(source_summary["source_cap_violated_v293"].sum()) == 0

    contributions = _read_csv("paper4_v293_return_gap_contributions.csv")
    assert len(contributions) == status["return_contribution_rows_v293"]
    assert contributions["signed_return_contribution_v293"].sum() == pytest.approx(
        status["delta_return_vs_v289_v293"]
    )

    blockers = _read_csv("paper4_v293_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v293"], blockers["blocking_v293"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v293"], blockers["evidence_count_v293"], strict=False)
    )
    assert bool(blocker_map["post_v293_repricing_missing"]) is True
    assert float(evidence_map["post_v293_repricing_missing"]) == pytest.approx(77.79441137125241)
    assert bool(blocker_map["bounded_pool_not_full_universe"]) is True
    assert int(evidence_map["bounded_pool_not_full_universe"]) == 251701
    assert bool(blocker_map["branch_price_dual_bound_missing"]) is True
    assert bool(blocker_map["dynamic_replay_and_deployment_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v293_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v293_diverse_pool_gap_probe_executed"]) is True
    assert bool(claim_map["v293_bounded_pool_challenger_found"]) is True
    assert bool(claim_map["v293_cardinality_restored"]) is True
    assert bool(claim_map["v293_bounded_pool_milp_optimality"]) is True
    assert bool(claim_map["v293_working_champion"]) is False
    assert bool(claim_map["v293_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v293_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v293 diverse source-relief cardinality MILP probe."])
    assert bool(
        boundary_map[
            "v293 finds a return-positive bounded-pool challenger that restores cardinality."
        ]
    )
    assert bool(boundary_map["v293 is a new Paper 4 working champion."]) is False
    assert bool(boundary_map["v293 proves global full-universe integer optimality."]) is False
    assert bool(boundary_map["v293 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v293_rows = backlog.loc[backlog["last_wave"].eq("v293")]
    assert len(v293_rows) == 1
    backlog_row = v293_rows.iloc[0]
    assert backlog_row["status"] == "diverse_pool_cardinality_challenger_found_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v294_post_v293_diverse_pool_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v293: Diverse Pool Return-Gap Probe" in notebook
    assert "Delta return vs v289: `77.79441137125241`" in notebook
    assert "bounded-pool challenger" in notebook
    assert "applying/repricing" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v294_post_v293_reprice_clears_one_swap_scope_without_promotion() -> None:
    status = _read_json("paper4_v294_status.json")

    assert status["phase"] == "v294_post_v293_diverse_pool_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.294"
    assert status["previous_challenger_version_v294"] == 293
    assert status["base_repair_version_v294"] == 279
    assert status["summary_rows_v294"] == 1
    assert status["stage_summary_rows_v294"] == 6
    assert status["candidate_pair_rows_v294"] == 0
    assert status["top_candidate_rows_v294"] == 0
    assert status["claim_blocker_rows_v294"] == 5
    assert status["claim_matrix_rows_v294"] == 5
    assert status["selected_rows_v294"] == 171
    assert status["base_selected_rows_v294"] == 171
    assert status["cardinality_restored_v294"] is True
    assert status["candidate_add_rows_v294"] == 276698
    assert status["total_pair_rows_screened_v294"] == 47315358
    assert status["return_improving_pair_rows_v294"] == 1615662
    assert status["budget_return_feasible_pair_rows_v294"] == 998656
    assert status["source_prefilter_pair_rows_v294"] == 3634
    assert status["source_exact_pair_rows_v294"] == 0
    assert status["cvar_feasible_pair_rows_v294"] == 0
    assert status["one_swap_improving_rows_v294"] == 0
    assert status["best_one_swap_return_delta_v294"] is None
    assert status["best_one_swap_cvar90_after_v294"] is None
    assert status["current_exposure_v294"] == pytest.approx(842950.0)
    assert status["current_objective_return_v294"] == pytest.approx(3181.0395871735054)
    assert status["cvar_cap_v294"] == pytest.approx(97395.99143344731)
    assert status["post_v293_one_swap_local_optimality_cleared_v294"] is True
    assert status["working_champion_claim_allowed_v294"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v294"] is False
    assert status["paper1_promotion_allowed_v294"] is False
    assert status["paper4_working_champion_changed_v294"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v294"] == "paper4_v295_broader_multi_swap_or_global_gap_probe.csv"

    summary = _read_csv("paper4_v294_post_v293_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v293_diverse_micro_relief_cardinality_milp"
    assert row["regime_v294"] == "post_v293_diverse_pool_challenger"
    assert bool(row["post_v293_one_swap_local_optimality_cleared_v294"]) is True
    assert bool(row["working_champion_claim_allowed_v294"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v294"])

    stages = _read_csv("paper4_v294_post_v293_one_swap_stage_summary.csv")
    stage_map = dict(zip(stages["stage_v294"], stages["pair_rows_v294"], strict=False))
    assert int(stage_map["all_pairs"]) == 47315358
    assert int(stage_map["return_improving"]) == 1615662
    assert int(stage_map["budget_return_feasible"]) == 998656
    assert int(stage_map["source_prefilter_feasible"]) == 3634
    assert int(stage_map["source_exact_feasible"]) == 0
    assert int(stage_map["cvar_feasible_improving"]) == 0

    reprice = _read_csv("paper4_v294_post_v293_one_swap_reprice.csv")
    top = _read_csv("paper4_v294_post_v293_one_swap_top_candidates.csv")
    assert reprice.empty
    assert top.empty
    assert "added_loan_id_v294" in set(reprice.columns)
    assert "dropped_loan_id_v294" in set(top.columns)

    blockers = _read_csv("paper4_v294_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v294"], blockers["blocking_v294"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v294"], blockers["evidence_count_v294"], strict=False)
    )
    assert bool(blocker_map["post_v293_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v293_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["bounded_challenger_not_working_champion"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["dynamic_replay_and_deployment_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v294_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v294_post_v293_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v294_post_v293_one_swap_local_optimality"]) is True
    assert bool(claim_map["v294_working_champion"]) is False
    assert bool(claim_map["v294_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v294_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v294 post-v293 one-swap repricing screen."])
    assert bool(boundary_map["v294 clears post-v293 one-swap local optimality."])
    assert bool(boundary_map["v294 authorizes a new Paper 4 working champion."]) is False
    assert bool(boundary_map["v294 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v294_rows = backlog.loc[backlog["last_wave"].eq("v294")]
    assert len(v294_rows) == 1
    backlog_row = v294_rows.iloc[0]
    assert backlog_row["status"] == "post_v293_one_swap_local_optimality_cleared"
    assert backlog_row["next_artifact"] == "paper4_v295_broader_multi_swap_or_global_gap_probe.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v294: Post-v293 One-Swap Repricing" in notebook
    assert "Pair rows screened: `47315358`" in notebook
    assert "Exact source-feasible pairs: `0`" in notebook
    assert "Post-v293 one-swap local optimality cleared:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v295_broader_multi_swap_probe_finds_bounded_challenger_only() -> None:
    status = _read_json("paper4_v295_status.json")

    assert status["phase"] == "v295_broader_multi_swap_or_global_gap_probe"
    assert status["schema_version"] == "2026-05-15.295"
    assert status["incumbent_version_v295"] == 293
    assert status["reprice_version_v295"] == 294
    assert status["base_repair_version_v295"] == 279
    assert status["top_return_limit_v295"] == 20000
    assert status["micro_relief_limit_v295"] == 15000
    assert status["pool_rows_v295"] == 35171
    assert status["outside_pool_rows_v295"] == 241698
    assert status["selected_rows_v295"] == 171
    assert status["target_selected_rows_v295"] == 171
    assert status["cardinality_restored_v295"] is True
    assert status["kept_current_rows_v295"] == 161
    assert status["added_rows_vs_v293_v295"] == 10
    assert status["dropped_rows_vs_v293_v295"] == 10
    assert status["portfolio_exposure_v295"] == pytest.approx(842775.0)
    assert status["objective_return_v295"] == pytest.approx(3249.9586479471764)
    assert status["incumbent_objective_return_v295"] == pytest.approx(3181.0395871735054)
    assert status["delta_return_vs_v293_v295"] == pytest.approx(68.91906077367094)
    assert status["scenario_loss_cvar90_v295"] == pytest.approx(97226.40959615848)
    assert status["incumbent_cvar90_v295"] == pytest.approx(97395.99143344731)
    assert status["delta_cvar90_vs_v293_v295"] == pytest.approx(-169.5818372888316)
    assert status["source_cap_violations_v295"] == 0
    assert status["budget_feasible_v295"] is True
    assert status["cvar_feasible_v295"] is True
    assert status["source_feasible_v295"] is True
    assert status["broader_pool_challenger_found_v295"] is True
    assert status["bounded_pool_optimality_claim_allowed_v295"] is True
    assert status["valid_full_universe_gap_certificate_v295"] is False
    assert status["milp_success_v295"] is True
    assert status["milp_incumbent_available_v295"] is True
    assert status["milp_fallback_to_v293_used_v295"] is False
    assert status["milp_status_v295"] == 0
    assert status["milp_gap_v295"] == pytest.approx(0.0)
    assert status["milp_node_count_v295"] == 1297
    assert status["milp_variable_count_v295"] == 35300
    assert status["milp_binary_variable_count_v295"] == 35171
    assert status["milp_constraint_rows_v295"] == 182
    assert status["strategy_comparison_rows_v295"] == 2
    assert status["candidate_registry_rows_v295"] == 4
    assert status["pool_summary_rows_v295"] == 3
    assert status["constraint_summary_rows_v295"] == 5
    assert status["source_summary_rows_v295"] == 51
    assert status["return_contribution_rows_v295"] == 20
    assert status["gap_diagnostic_rows_v295"] == 4
    assert status["outside_pool_top_candidate_rows_v295"] == 200
    assert status["claim_blocker_rows_v295"] == 5
    assert status["claim_matrix_rows_v295"] == 6
    assert status["working_champion_claim_allowed_v295"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v295"] is False
    assert status["paper1_promotion_allowed_v295"] is False
    assert status["paper4_working_champion_changed_v295"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v295"] == "paper4_v296_post_v295_broader_pool_reprice.csv"

    protocol = _read_csv("paper4_v295_broader_multi_swap_or_global_gap_probe.csv")
    row = protocol.iloc[0]
    assert row["protocol_id_v295"] == "broader_post_v293_multi_swap_or_global_gap_probe"
    assert bool(row["broader_pool_challenger_found_v295"]) is True
    assert bool(row["bounded_pool_optimality_claim_allowed_v295"]) is True
    assert bool(row["valid_full_universe_gap_certificate_v295"]) is False
    assert bool(row["working_champion_claim_allowed_v295"]) is False
    assert "no global or promotion claim" in str(row["claim_boundary_v295"])

    action = _read_csv("paper4_v295_broader_multi_swap_action.csv")
    action_row = action.iloc[0]
    expected_added = (
        "127640797|130791260|138767991|141085456|141674389|145660957|"
        "150078681|152791402|156899870|161613129"
    )
    expected_dropped = (
        "137151950|137721854|139317603|140352540|143278940|146047525|"
        "148029832|153939446|156707196|158316010"
    )
    assert action_row["added_loan_ids_v295"] == expected_added
    assert action_row["dropped_loan_ids_v295"] == expected_dropped
    assert int(action_row["kept_current_rows_v295"]) == 161
    assert bool(action_row["cardinality_restored_v295"]) is True
    assert bool(action_row["broader_pool_challenger_found_v295"]) is True

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v295_broader_multi_swap_allocations.parquet")
    assert len(allocations) == status["selected_rows_v295"]
    allocation_ids = set(allocations["loan_id"].astype(str))
    assert set(expected_added.split("|")).issubset(allocation_ids)
    assert not set(expected_dropped.split("|")).intersection(allocation_ids)
    assert allocations["selected_v295"].astype(bool).all()

    registry = _read_csv("paper4_v295_broader_candidate_registry.csv")
    registry_map = dict(
        zip(registry["pool_component_v295"], registry["candidate_rows_v295"], strict=False)
    )
    assert int(registry_map["current_v293_selected"]) == 171
    assert int(registry_map["top20000_post_v293_omitted_by_mean_return"]) == 20000
    assert int(registry_map["micro_source_relief_outside_top20000"]) == 15000
    assert int(registry_map["outside_v295_pool_not_solved"]) == 241698

    pool = _read_csv("paper4_v295_broader_pool_summary.csv")
    pool_map = dict(zip(pool["pool_role_v295"], pool["selected_rows_v295"], strict=False))
    assert int(pool_map["current_v293_selected"]) == 161
    assert int(pool_map["top20000_post_v293_omitted_by_mean_return"]) == 6
    assert int(pool_map["micro_source_relief_outside_top20000"]) == 4

    constraints = _read_csv("paper4_v295_broader_constraint_summary.csv")
    constraint_map = dict(
        zip(constraints["constraint_type_v295"], constraints["constraint_rows_v295"], strict=False)
    )
    assert int(constraint_map["budget_range"]) == 1
    assert int(constraint_map["selected_row_cardinality"]) == 1
    assert int(constraint_map["source_share"]) == 51
    assert int(constraint_map["cvar_cap"]) == 1
    assert int(constraint_map["cvar_path_excess"]) == 128

    source_summary = _read_csv("paper4_v295_broader_source_summary.csv")
    assert len(source_summary) == status["source_summary_rows_v295"]
    assert int(source_summary["source_cap_violated_v295"].sum()) == 0

    contributions = _read_csv("paper4_v295_return_delta_contributions.csv")
    assert len(contributions) == status["return_contribution_rows_v295"]
    assert contributions["signed_return_contribution_v295"].sum() == pytest.approx(
        status["delta_return_vs_v293_v295"]
    )

    gap = _read_csv("paper4_v295_global_gap_diagnostics.csv")
    gap_map = {row.diagnostic_id_v295: row for row in gap.itertuples(index=False)}
    assert int(gap_map["outside_v295_pool_rows"].value_v295) == 241698
    assert int(gap_map["outside_v295_pool_positive_return_rows"].value_v295) == 0
    assert int(gap_map["outside_v295_pool_source_tight_positive_rows"].value_v295) == 0
    assert float(
        gap_map["loose_top171_return_upper_bound_no_constraints"].delta_vs_incumbent_v295
    ) == pytest.approx(80621.37241261483)

    blockers = _read_csv("paper4_v295_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v295"], blockers["blocking_v295"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v295"], blockers["evidence_count_v295"], strict=False)
    )
    assert bool(blocker_map["broader_pool_improvement_found"]) is True
    assert float(evidence_map["broader_pool_improvement_found"]) == pytest.approx(68.91906077367094)
    assert bool(blocker_map["bounded_pool_not_full_universe"]) is True
    assert int(evidence_map["bounded_pool_not_full_universe"]) == 241698
    assert bool(blocker_map["valid_global_gap_certificate_missing"]) is True
    assert bool(blocker_map["dynamic_replay_and_deployment_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v295_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v295_broader_multi_swap_probe_executed"]) is True
    assert bool(claim_map["v295_bounded_pool_optimality"]) is True
    assert bool(claim_map["v295_broader_pool_improvement_over_v293"]) is True
    assert bool(claim_map["v295_full_universe_gap_certificate"]) is False
    assert bool(claim_map["v295_working_champion"]) is False
    assert bool(claim_map["v295_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v295 broader bounded multi-swap/global-gap probe."])
    assert bool(
        boundary_map["v295 proves bounded-pool optimality for the expanded post-v293 pool."]
    )
    assert bool(boundary_map["v295 finds a bounded-pool improvement over v293."])
    assert bool(boundary_map["v295 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v295 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v295 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v295_rows = backlog.loc[backlog["last_wave"].eq("v295")]
    assert len(v295_rows) == 1
    backlog_row = v295_rows.iloc[0]
    assert backlog_row["status"] == "broader_pool_improvement_found_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v296_post_v295_broader_pool_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v295: Broader Multi-Swap / Global-Gap Probe" in notebook
    assert "Delta return vs v293: `68.91906077367094`" in notebook
    assert "Valid full-universe gap certificate:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v296_post_v295_reprice_clears_one_swap_scope_without_promotion() -> None:
    status = _read_json("paper4_v296_status.json")

    assert status["phase"] == "v296_post_v295_broader_pool_one_swap_reprice"
    assert status["schema_version"] == "2026-05-15.296"
    assert status["previous_challenger_version_v296"] == 295
    assert status["base_repair_version_v296"] == 279
    assert status["summary_rows_v296"] == 1
    assert status["stage_summary_rows_v296"] == 6
    assert status["candidate_pair_rows_v296"] == 0
    assert status["top_candidate_rows_v296"] == 0
    assert status["claim_blocker_rows_v296"] == 5
    assert status["claim_matrix_rows_v296"] == 5
    assert status["selected_rows_v296"] == 171
    assert status["base_selected_rows_v296"] == 171
    assert status["cardinality_restored_v296"] is True
    assert status["candidate_add_rows_v296"] == 276698
    assert status["total_pair_rows_screened_v296"] == 47315358
    assert status["return_improving_pair_rows_v296"] == 1605020
    assert status["budget_return_feasible_pair_rows_v296"] == 964431
    assert status["source_prefilter_pair_rows_v296"] == 882
    assert status["source_exact_pair_rows_v296"] == 0
    assert status["cvar_feasible_pair_rows_v296"] == 0
    assert status["one_swap_improving_rows_v296"] == 0
    assert status["best_one_swap_return_delta_v296"] is None
    assert status["best_one_swap_cvar90_after_v296"] is None
    assert status["current_exposure_v296"] == pytest.approx(842775.0)
    assert status["current_objective_return_v296"] == pytest.approx(3249.9586479471764)
    assert status["cvar_cap_v296"] == pytest.approx(97226.40959615848)
    assert status["post_v295_one_swap_local_optimality_cleared_v296"] is True
    assert status["working_champion_claim_allowed_v296"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v296"] is False
    assert status["paper1_promotion_allowed_v296"] is False
    assert status["paper4_working_champion_changed_v296"] is False
    assert status["paper4_final_promotion_created"] is False
    assert (
        status["next_artifact_v296"]
        == "paper4_v297_full_universe_branch_price_bound_or_dynamic_replay.csv"
    )

    summary = _read_csv("paper4_v296_post_v295_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v295_broader_post_v293_multi_swap_milp"
    assert row["regime_v296"] == "post_v295_broader_pool_challenger"
    assert bool(row["post_v295_one_swap_local_optimality_cleared_v296"]) is True
    assert bool(row["working_champion_claim_allowed_v296"]) is False
    assert "one-swap screen cleared" in str(row["claim_boundary_v296"])

    stages = _read_csv("paper4_v296_post_v295_one_swap_stage_summary.csv")
    stage_map = dict(zip(stages["stage_v296"], stages["pair_rows_v296"], strict=False))
    assert int(stage_map["all_pairs"]) == 47315358
    assert int(stage_map["return_improving"]) == 1605020
    assert int(stage_map["budget_return_feasible"]) == 964431
    assert int(stage_map["source_prefilter_feasible"]) == 882
    assert int(stage_map["source_exact_feasible"]) == 0
    assert int(stage_map["cvar_feasible_improving"]) == 0

    reprice = _read_csv("paper4_v296_post_v295_one_swap_reprice.csv")
    top = _read_csv("paper4_v296_post_v295_one_swap_top_candidates.csv")
    assert reprice.empty
    assert top.empty
    assert "added_loan_id_v296" in set(reprice.columns)
    assert "dropped_loan_id_v296" in set(top.columns)

    blockers = _read_csv("paper4_v296_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v296"], blockers["blocking_v296"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v296"], blockers["evidence_count_v296"], strict=False)
    )
    assert bool(blocker_map["post_v295_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v295_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["bounded_challenger_not_working_champion"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["dynamic_replay_and_deployment_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v296_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v296_post_v295_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v296_post_v295_one_swap_local_optimality"]) is True
    assert bool(claim_map["v296_working_champion"]) is False
    assert bool(claim_map["v296_global_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v296_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v296 post-v295 one-swap repricing screen."])
    assert bool(boundary_map["v296 clears post-v295 one-swap local optimality."])
    assert bool(boundary_map["v296 authorizes a new Paper 4 working champion."]) is False
    assert bool(boundary_map["v296 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v296_rows = backlog.loc[backlog["last_wave"].eq("v296")]
    assert len(v296_rows) == 1
    backlog_row = v296_rows.iloc[0]
    assert backlog_row["status"] == "post_v295_one_swap_local_optimality_cleared"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v297_full_universe_branch_price_bound_or_dynamic_replay.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v296: Post-v295 One-Swap Repricing" in notebook
    assert "Pair rows screened: `47315358`" in notebook
    assert "Source prefilter pairs: `882`" in notebook
    assert "Post-v295 one-swap local optimality cleared:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v297_global_dynamic_proxy_gate_keeps_promotion_blocked() -> None:
    status = _read_json("paper4_v297_status.json")

    assert status["phase"] == "v297_full_universe_branch_price_bound_or_dynamic_replay"
    assert status["schema_version"] == "2026-05-15.297"
    assert status["incumbent_version_v297"] == 293
    assert status["challenger_version_v297"] == 295
    assert status["local_reprice_version_v297"] == 296
    assert status["full_binary_variables_v297"] == 276869
    assert status["direct_mip_binary_guard_v297"] == 50000
    assert status["direct_full_mip_attempted_v297"] is False
    assert status["valid_full_universe_gap_certificate_v297"] is False
    assert status["dynamic_proxy_replay_executed_v297"] is True
    assert status["dynamic_proxy_trace_rows_v297"] == 768
    assert status["dynamic_proxy_policy_count_v297"] == 2
    assert status["dynamic_proxy_period_count_v297"] == 3
    assert status["period_distribution_match_v297"] is True
    assert status["delta_return_v295_vs_v293_v297"] == pytest.approx(68.91906077367094)
    assert status["delta_cvar90_v295_vs_v293_v297"] == pytest.approx(-169.5818372888316)
    assert status["v295_dynamic_proxy_beats_v293_v297"] is True
    assert status["live_deployment_claim_allowed_v297"] is False
    assert status["working_champion_claim_allowed_v297"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v297"] is False
    assert status["paper1_promotion_allowed_v297"] is False
    assert status["paper4_working_champion_changed_v297"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["dynamic_proxy_summary_rows_v297"] == 2
    assert status["gate_requirement_rows_v297"] == 5
    assert status["claim_blocker_rows_v297"] == 5
    assert status["claim_matrix_rows_v297"] == 5
    assert status["next_artifact_v297"] == "paper4_v298_online_ifrs9_spo_dla_gate_expansion.csv"

    gate = _read_csv("paper4_v297_global_dynamic_gate_summary.csv")
    row = gate.iloc[0]
    assert row["gate_id_v297"] == "v297_global_bound_dynamic_proxy_gate"
    assert bool(row["dynamic_proxy_replay_executed_v297"]) is True
    assert bool(row["valid_full_universe_gap_certificate_v297"]) is False
    assert bool(row["live_deployment_claim_allowed_v297"]) is False
    assert bool(row["working_champion_claim_allowed_v297"]) is False
    assert "proxy replay is not deployment validation" in str(row["claim_boundary_v297"])

    proxy = _read_csv("paper4_v297_dynamic_proxy_summary.csv")
    proxy_map = {row.policy_id: row for row in proxy.itertuples(index=False)}
    v293 = proxy_map["v293_diverse_pool_after_v294_gate"]
    v295 = proxy_map["v295_broader_pool_after_v296_gate"]
    assert int(v293.trace_rows_v297) == 384
    assert int(v295.trace_rows_v297) == 384
    assert int(v293.period_count_v297) == 3
    assert int(v295.period_count_v297) == 3
    assert float(v293.final_objective_return_v297) == pytest.approx(3181.0395871735054)
    assert float(v295.final_objective_return_v297) == pytest.approx(3249.9586479471764)
    assert float(v293.final_loss_cvar90_v297) == pytest.approx(97395.99143344731)
    assert float(v295.final_loss_cvar90_v297) == pytest.approx(97226.40959615848)
    assert float(v295.worst_period_scenario_wealth_proxy_v297) > float(
        v293.worst_period_scenario_wealth_proxy_v297
    )

    trace = pd.read_parquet(TABLE_DIR / "paper4_v297_dynamic_proxy_trace.parquet")
    assert len(trace) == status["dynamic_proxy_trace_rows_v297"]
    assert set(trace["policy_id"]) == {
        "v293_diverse_pool_after_v294_gate",
        "v295_broader_pool_after_v296_gate",
    }
    assert set(trace["period_v297"].astype(str)) == {"2018", "2019", "2020"}
    assert trace["scenario_id_v297"].nunique() == 128

    requirements = _read_csv("paper4_v297_gate_requirements.csv")
    requirement_map = dict(
        zip(requirements["requirement_id_v297"], requirements["met_v297"], strict=False)
    )
    assert bool(requirement_map["valid_full_universe_gap_certificate"]) is False
    assert bool(requirement_map["post_v295_one_swap_gate_cleared"]) is True
    assert bool(requirement_map["dynamic_proxy_replay_executed"]) is True
    assert bool(requirement_map["online_holdout_rerun_for_v295"]) is False
    assert bool(requirement_map["ifrs9_proxy_rerun_for_v295"]) is False

    blockers = _read_csv("paper4_v297_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v297"], blockers["blocking_v297"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v297"], blockers["evidence_count_v297"], strict=False)
    )
    assert bool(blocker_map["valid_global_gap_certificate_missing"]) is True
    assert bool(blocker_map["dynamic_proxy_not_live_deployment_replay"]) is True
    assert int(evidence_map["dynamic_proxy_not_live_deployment_replay"]) == 768
    assert bool(blocker_map["online_holdout_not_rerun_for_v295"]) is True
    assert bool(blocker_map["ifrs9_proxy_not_rerun_for_v295"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v297_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v297_global_dynamic_gate_executed"]) is True
    assert bool(claim_map["v297_v295_dynamic_proxy_beats_v293"]) is True
    assert bool(claim_map["v297_full_universe_gap_certificate"]) is False
    assert bool(claim_map["v297_live_deployment_or_working_champion"]) is False
    assert bool(claim_map["v297_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v297 global-bound/dynamic proxy gate for v295."])
    assert bool(boundary_map["v297 dynamic proxy replay favors v295 over v293 on common paths."])
    assert bool(boundary_map["v297 proves full-universe global integer optimality."]) is False
    assert (
        bool(
            boundary_map["v297 authorizes live deployment or Paper 4 working champion replacement."]
        )
        is False
    )
    assert bool(boundary_map["v297 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v297_rows = backlog.loc[backlog["last_wave"].eq("v297")]
    assert len(v297_rows) == 1
    backlog_row = v297_rows.iloc[0]
    assert backlog_row["status"] == "dynamic_proxy_replay_executed_global_gap_still_open"
    assert backlog_row["next_artifact"] == "paper4_v298_online_ifrs9_spo_dla_gate_expansion.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v297: Global-Bound / Dynamic Proxy Gate" in notebook
    assert "Dynamic proxy trace rows: `768`" in notebook
    assert "Valid full-universe gap certificate:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v298_online_ifrs9_spo_dla_gate_expansion_blocks_transfer_claims() -> None:
    status = _read_json("paper4_v298_status.json")

    assert status["phase"] == "v298_online_ifrs9_spo_dla_gate_expansion"
    assert status["schema_version"] == "2026-05-15.298"
    assert status["source_candidate_version_v298"] == 295
    assert status["global_dynamic_gate_version_v298"] == 297
    assert status["online_transfer_rows_v298"] == 4
    assert status["online_internal_pass_rows_v298"] == 2
    assert status["online_external_live_pass_rows_v298"] == 0
    assert status["ifrs9_proxy_covered_loan_rows_v298"] == 95
    assert status["ifrs9_proxy_uncovered_loan_rows_v298"] == 76
    assert status["ifrs9_proxy_coverage_share_v298"] == pytest.approx(0.5555555555555556)
    assert status["spo_dla_audit_rows_v298"] == 2
    assert status["strict_live_deployability_claim_allowed_v298"] is False
    assert status["contractual_ifrs9_claim_allowed_v298"] is False
    assert status["formal_spo_dla_claim_allowed_v298"] is False
    assert status["working_champion_claim_allowed_v298"] is False
    assert status["paper1_promotion_allowed_v298"] is False
    assert status["paper4_working_champion_changed_v298"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v298"] == 5
    assert status["claim_matrix_rows_v298"] == 5
    assert status["next_artifact_v298"] == "paper4_v299_v295_cashflow_or_online_holdout_rerun.csv"

    summary = _read_csv("paper4_v298_online_ifrs9_spo_dla_gate_expansion.csv")
    row = summary.iloc[0]
    assert row["gate_id_v298"] == "v298_online_ifrs9_spo_dla_gate_expansion"
    assert int(row["online_internal_pass_rows_v298"]) == 2
    assert int(row["online_external_live_pass_rows_v298"]) == 0
    assert bool(row["strict_live_deployability_claim_allowed_v298"]) is False
    assert bool(row["contractual_ifrs9_claim_allowed_v298"]) is False
    assert bool(row["formal_spo_dla_claim_allowed_v298"]) is False
    assert "historical gates do not transfer" in str(row["claim_boundary_v298"])

    online = _read_csv("paper4_v298_online_gate_transfer_audit.csv")
    assert len(online) == status["online_transfer_rows_v298"]
    lane_map = online.groupby("gate_lane_v298")["gate_pass_v298"].sum().to_dict()
    live_map = online.groupby("gate_lane_v298")["strict_live_claim_allowed_v298"].sum().to_dict()
    assert int(lane_map["online_internal_margin_repair"]) == 2
    assert int(lane_map["online_external_holdout"]) == 0
    assert int(live_map["online_internal_margin_repair"]) == 0
    assert int(live_map["online_external_holdout"]) == 0

    ifrs9 = _read_csv("paper4_v298_ifrs9_v295_proxy_coverage.csv")
    ifrs9_row = ifrs9.iloc[0]
    assert int(ifrs9_row["selected_rows_v298"]) == 171
    assert int(ifrs9_row["ifrs9_proxy_covered_loan_rows_v298"]) == 95
    assert int(ifrs9_row["ifrs9_proxy_uncovered_loan_rows_v298"]) == 76
    assert float(ifrs9_row["ifrs9_proxy_coverage_share_v298"]) == pytest.approx(0.5555555555555556)
    assert int(ifrs9_row["ifrs9_proxy_panel_rows_v298"]) == 3420
    assert int(ifrs9_row["ifrs9_proxy_scenarios_v298"]) == 1
    assert int(ifrs9_row["ifrs9_proxy_months_v298"]) == 36
    assert bool(ifrs9_row["contractual_ifrs9_claim_allowed_v298"]) is False

    spo = _read_csv("paper4_v298_spo_dla_claim_audit.csv")
    spo_map = {row.gate_lane_v298: row for row in spo.itertuples(index=False)}
    assert int(spo_map["spo_dependency_audit"].evidence_rows_v298) == 8
    assert int(spo_map["dla_common_path_audit"].evidence_rows_v298) == 14
    assert bool(spo_map["spo_dependency_audit"].formal_claim_allowed_v298) is False
    assert bool(spo_map["dla_common_path_audit"].formal_claim_allowed_v298) is False

    blockers = _read_csv("paper4_v298_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v298"], blockers["blocking_v298"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v298"], blockers["evidence_count_v298"], strict=False)
    )
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert int(evidence_map["external_online_holdout_missing"]) == 0
    assert bool(blocker_map["ifrs9_proxy_partial_coverage_only"]) is True
    assert int(evidence_map["ifrs9_proxy_partial_coverage_only"]) == 76
    assert bool(blocker_map["formal_spo_dla_claims_missing"]) is True
    assert int(evidence_map["formal_spo_dla_claims_missing"]) == 2
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v298_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v298_gate_expansion_executed"]) is True
    assert bool(claim_map["v298_strict_live_online_deployability"]) is False
    assert bool(claim_map["v298_contractual_ifrs9"]) is False
    assert bool(claim_map["v298_formal_spo_dla"]) is False
    assert bool(claim_map["v298_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v298 online/IFRS9/SPO-DLA gate expansion for v295."])
    assert (
        bool(boundary_map["v298 transfers historical online gates into live deployability."])
        is False
    )
    assert bool(boundary_map["v298 implements contractual IFRS9 for v295."]) is False
    assert (
        bool(boundary_map["v298 proves formal differentiable SPO or exact Bellman DLA."]) is False
    )
    assert bool(boundary_map["v298 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v298_rows = backlog.loc[backlog["last_wave"].eq("v298")]
    assert len(v298_rows) == 1
    backlog_row = v298_rows.iloc[0]
    assert backlog_row["status"] == "gate_expansion_executed_claims_remain_blocked"
    assert backlog_row["next_artifact"] == "paper4_v299_v295_cashflow_or_online_holdout_rerun.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v298: Online / IFRS9 / SPO-DLA Gate Expansion" in notebook
    assert "IFRS9 proxy covered loans: `95`" in notebook
    assert "External online live pass rows: `0`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v299_v295_cashflow_online_rerun_keeps_live_claims_blocked() -> None:
    status = _read_json("paper4_v299_status.json")

    assert status["phase"] == "v299_v295_cashflow_or_online_holdout_rerun"
    assert status["schema_version"] == "2026-05-15.299"
    assert status["source_candidate_version_v299"] == 295
    assert status["gate_audit_version_v299"] == 298
    assert status["selected_rows_v299"] == 171
    assert status["cashflow_proxy_panel_rows_v299"] == 6156
    assert status["observed_proxy_loan_rows_v299"] == 95
    assert status["imputed_proxy_loan_rows_v299"] == 76
    assert status["post_imputation_coverage_share_v299"] == pytest.approx(1.0)
    assert status["observed_coverage_share_v299"] == pytest.approx(0.5555555555555556)
    assert status["cashflow_proxy_months_v299"] == 36
    assert status["cashflow_proxy_scenarios_v299"] == 1
    assert status["cashflow_proxy_source_rows_v299"] == 3
    assert status["online_temporal_cell_rows_v299"] == 6
    assert status["online_temporal_summary_rows_v299"] == 2
    assert status["online_internal_all_gate_family_rows_v299"] == 0
    assert status["strict_live_deployability_claim_allowed_v299"] is False
    assert status["contractual_ifrs9_claim_allowed_v299"] is False
    assert status["working_champion_claim_allowed_v299"] is False
    assert status["paper1_promotion_allowed_v299"] is False
    assert status["paper4_working_champion_changed_v299"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v299"] == 5
    assert status["claim_matrix_rows_v299"] == 5
    assert (
        status["next_artifact_v299"]
        == "paper4_v300_v295_source_governance_or_branch_price_protocol.csv"
    )

    cashflow = _read_csv("paper4_v299_v295_cashflow_proxy_summary.csv")
    row = cashflow.iloc[0]
    assert int(row["selected_rows_v299"]) == 171
    assert int(row["observed_proxy_loan_rows_v299"]) == 95
    assert int(row["imputed_proxy_loan_rows_v299"]) == 76
    assert int(row["cashflow_proxy_panel_rows_v299"]) == 6156
    assert float(row["post_imputation_coverage_share_v299"]) == pytest.approx(1.0)
    assert bool(row["contractual_ifrs9_claim_allowed_v299"]) is False
    assert "no contractual IFRS9 claim" in str(row["claim_boundary_v299"])

    panel = pd.read_parquet(TABLE_DIR / "paper4_v299_v295_cashflow_proxy_panel.parquet")
    assert len(panel) == status["cashflow_proxy_panel_rows_v299"]
    assert panel["loan_id"].astype(str).nunique() == status["selected_rows_v299"]
    assert panel["month_index"].nunique() == 36
    assert set(panel["scenario"].astype(str)) == {"baseline"}
    proxy_counts = panel["proxy_source_v299"].value_counts().to_dict()
    assert proxy_counts["observed_v47_proxy"] == 3420
    assert proxy_counts["imputed_grade_score_period"] == 2700
    assert proxy_counts["imputed_grade_score"] == 36
    assert panel["claim_boundary_v299"].str.contains("not contractual IFRS9").all()

    source_summary = _read_csv("paper4_v299_v295_cashflow_proxy_source_summary.csv")
    source_map = dict(
        zip(source_summary["proxy_source_v299"], source_summary["loan_rows_v299"], strict=False)
    )
    assert int(source_map["observed_v47_proxy"]) == 95
    assert int(source_map["imputed_grade_score_period"]) == 75
    assert int(source_map["imputed_grade_score"]) == 1

    online_cells = _read_csv("paper4_v299_v295_online_temporal_cells.csv")
    assert len(online_cells) == status["online_temporal_cell_rows_v299"]
    assert set(online_cells["source_family"]) == {"period", "term"}
    assert set(online_cells["holdout_period_v299"].astype(str)) == {"2018", "2019", "2020"}
    assert not online_cells["strict_live_claim_allowed_v299"].astype(bool).any()
    period_2019 = online_cells.loc[
        online_cells["source_family"].eq("period")
        & online_cells["holdout_period_v299"].astype(str).eq("2019")
    ].iloc[0]
    assert float(period_2019["coverage_v299"]) == pytest.approx(0.8857142857142857)
    assert bool(period_2019["policy_gate90_v299"]) is False

    online_summary = _read_csv("paper4_v299_v295_online_temporal_summary.csv")
    assert len(online_summary) == status["online_temporal_summary_rows_v299"]
    assert not online_summary["all_internal_gates_pass_v299"].astype(bool).any()
    assert not online_summary["strict_live_claim_allowed_v299"].astype(bool).any()
    assert online_summary["worst_policy_period_coverage_v299"].min() == pytest.approx(
        0.8857142857142857
    )

    blockers = _read_csv("paper4_v299_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v299"], blockers["blocking_v299"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v299"], blockers["evidence_count_v299"], strict=False)
    )
    assert bool(blocker_map["cashflow_proxy_imputation_required"]) is True
    assert int(evidence_map["cashflow_proxy_imputation_required"]) == 76
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert int(evidence_map["external_online_holdout_missing"]) == 0
    assert bool(blocker_map["strict_live_claim_forbidden"]) is True
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v299_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v299_v295_cashflow_proxy_panel_executed"]) is True
    assert bool(claim_map["v299_v295_online_temporal_rerun_executed"]) is True
    assert bool(claim_map["v299_contractual_ifrs9"]) is False
    assert bool(claim_map["v299_strict_live_online_deployability"]) is False
    assert bool(claim_map["v299_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v299 v295-specific cashflow proxy panel."])
    assert bool(boundary_map["Paper 4 has a v299 v295 temporal online rerun."])
    assert bool(boundary_map["v299 implements contractual IFRS9 for v295."]) is False
    assert bool(boundary_map["v299 authorizes live online deployability for v295."]) is False
    assert bool(boundary_map["v299 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v299_rows = backlog.loc[backlog["last_wave"].eq("v299")]
    assert len(v299_rows) == 1
    backlog_row = v299_rows.iloc[0]
    assert backlog_row["status"] == "v295_cashflow_proxy_and_temporal_online_rerun_executed"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v300_v295_source_governance_or_branch_price_protocol.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v299: v295 Cashflow Proxy / Online Temporal Rerun" in notebook
    assert "Cashflow proxy panel rows: `6156`" in notebook
    assert "Online internal all-gate family rows:\n  `0`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v300_source_governance_branch_price_protocol_keeps_claims_blocked() -> None:
    status = _read_json("paper4_v300_status.json")

    assert status["phase"] == "v300_v295_source_governance_or_branch_price_protocol"
    assert status["schema_version"] == "2026-05-15.300"
    assert status["source_candidate_version_v300"] == 295
    assert status["global_gate_version_v300"] == 297
    assert status["cashflow_proxy_version_v300"] == 299
    assert status["selected_rows_v300"] == 171
    assert status["source_summary_rows_v300"] == 51
    assert status["source_tight_rows_v300"] == 2
    assert status["source_cap_violations_v300"] == 0
    assert status["tightest_source_family_v300"] == "grade"
    assert status["tightest_source_id_v300"] == "A"
    assert status["tightest_source_slack_v300"] == pytest.approx(8.830577710172705e-07)
    assert status["second_tight_source_family_v300"] == "score_decile"
    assert status["second_tight_source_id_v300"] == "0"
    assert status["second_tight_source_slack_v300"] == pytest.approx(2.6697517130913617e-05)
    assert status["proxy_imputation_source_rows_v300"] == 27
    assert status["proxy_imputed_loan_rows_v300"] == 76
    assert status["proxy_observed_loan_rows_v300"] == 95
    assert status["proxy_imputed_loan_share_v300"] == pytest.approx(0.4444444444444444)
    assert status["full_binary_variables_v300"] == 276869
    assert status["direct_mip_binary_guard_v300"] == 50000
    assert status["direct_full_mip_guard_exceeded_v300"] is True
    assert status["requirement_rows_v300"] == 7
    assert status["unmet_requirement_rows_v300"] == 6
    assert status["valid_branch_price_bound_v300"] is False
    assert status["source_governance_protocol_executed_v300"] is True
    assert status["strict_live_deployability_claim_allowed_v300"] is False
    assert status["contractual_ifrs9_claim_allowed_v300"] is False
    assert status["working_champion_claim_allowed_v300"] is False
    assert status["paper1_promotion_allowed_v300"] is False
    assert status["paper4_working_champion_changed_v300"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v300"] == 6
    assert status["claim_matrix_rows_v300"] == 5
    assert (
        status["next_artifact_v300"]
        == "paper4_v301_source_tight_branch_price_pricing_or_imputation_repair.csv"
    )

    protocol = _read_csv("paper4_v300_v295_source_governance_or_branch_price_protocol.csv")
    row = protocol.iloc[0]
    assert row["protocol_id_v300"] == "v295_source_governance_branch_price_protocol"
    assert int(row["source_tight_rows_v300"]) == 2
    assert bool(row["direct_full_mip_guard_exceeded_v300"]) is True
    assert bool(row["valid_branch_price_bound_v300"]) is False
    assert bool(row["source_governance_protocol_executed_v300"]) is True
    assert "requirements remain open" in str(row["claim_boundary_v300"])

    hotspots = _read_csv("paper4_v300_v295_source_slack_hotspots.csv")
    assert len(hotspots) == status["source_summary_rows_v300"]
    assert int(hotspots["source_tight_flag_v300"].sum()) == 2
    assert int(hotspots["source_cap_violated_v295"].sum()) == 0
    first = hotspots.iloc[0]
    second = hotspots.iloc[1]
    assert first["source_family"] == "grade"
    assert str(first["source_id"]) == "A"
    assert float(first["source_slack_v295"]) == pytest.approx(8.830577710172705e-07)
    assert first["source_governance_severity_v300"] == "critical_tight"
    assert int(first["branch_price_priority_v300"]) == 1
    assert second["source_family"] == "score_decile"
    assert str(second["source_id"]) == "0"
    assert float(second["source_slack_v295"]) == pytest.approx(2.6697517130913617e-05)
    assert int(second["branch_price_priority_v300"]) == 2
    assert set(
        hotspots.loc[hotspots["source_governance_severity_v300"].eq("critical_tight")][
            "source_family"
        ]
    ) == {"grade", "score_decile"}

    proxy = _read_csv("paper4_v300_v295_proxy_imputation_by_source.csv")
    assert len(proxy) == status["proxy_imputation_source_rows_v300"]
    assert not proxy["contractual_ifrs9_claim_allowed_v300"].astype(bool).any()
    proxy_key = (
        proxy["source_family_v300"].astype(str)
        + "="
        + proxy["source_id_v300"].astype(str)
        + "|"
        + proxy["proxy_source_v299"].astype(str)
    )
    loan_map = dict(zip(proxy_key, proxy["loan_rows_v300"], strict=False))
    assert int(loan_map["grade=A|imputed_grade_score_period"]) == 67
    assert int(loan_map["grade=A|observed_v47_proxy"]) == 28
    assert int(loan_map["score_decile=0|imputed_grade_score_period"]) == 70
    assert int(loan_map["score_decile=0|observed_v47_proxy"]) == 53
    assert int(loan_map["period=2020|imputed_grade_score"]) == 1

    requirements = _read_csv("paper4_v300_branch_price_requirement_register.csv")
    assert len(requirements) == status["requirement_rows_v300"]
    requirement_map = dict(
        zip(requirements["requirement_id_v300"], requirements["met_v300"], strict=False)
    )
    assert bool(requirement_map["valid_full_universe_gap_certificate"]) is False
    assert bool(requirement_map["direct_full_mip_resource_guard"]) is False
    assert bool(requirement_map["observed_cashflow_for_imputed_v295_loans"]) is False
    assert bool(requirement_map["external_online_holdout_for_v295"]) is False
    assert bool(requirement_map["final_promotion_absence_guardrail"]) is True
    assert bool(requirement_map["source_tight_branch_price_grade_A"]) is False
    assert bool(requirement_map["source_tight_branch_price_score_decile_0"]) is False

    blockers = _read_csv("paper4_v300_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v300"], blockers["blocking_v300"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v300"], blockers["evidence_count_v300"], strict=False)
    )
    assert bool(blocker_map["source_tight_branch_prices_missing"]) is True
    assert int(evidence_map["source_tight_branch_prices_missing"]) == 2
    assert bool(blocker_map["full_universe_gap_certificate_missing"]) is True
    assert int(evidence_map["full_universe_gap_certificate_missing"]) == 276869
    assert bool(blocker_map["cashflow_proxy_imputation_required"]) is True
    assert int(evidence_map["cashflow_proxy_imputation_required"]) == 76
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v300_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v300_source_governance_branch_price_protocol_executed"]) is True
    assert bool(claim_map["v300_source_tight_hotspots_identified"]) is True
    assert bool(claim_map["v300_valid_branch_price_bound"]) is False
    assert bool(claim_map["v300_contractual_ifrs9_or_live_deployability"]) is False
    assert bool(claim_map["v300_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v300 v295 source-governance branch-price protocol."])
    assert bool(boundary_map["v300 identifies v295 source-tight branch-price hotspots."])
    assert bool(boundary_map["v300 proves a valid full-universe branch-price bound."]) is False
    assert (
        bool(boundary_map["v300 resolves contractual IFRS9 or live deployability for v295."])
        is False
    )
    assert bool(boundary_map["v300 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v300_rows = backlog.loc[backlog["last_wave"].eq("v300")]
    assert len(v300_rows) == 1
    backlog_row = v300_rows.iloc[0]
    assert backlog_row["status"] == "source_governance_branch_price_protocol_created"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v301_source_tight_branch_price_pricing_or_imputation_repair.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v300: Source Governance / Branch-Price Protocol" in notebook
    assert "Tight source rows: `2`" in notebook
    assert "Valid branch-price bound: `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v301_source_tight_imputation_repair_keeps_promotion_blocked() -> None:
    status = _read_json("paper4_v301_status.json")

    assert status["phase"] == "v301_source_tight_branch_price_pricing_or_imputation_repair"
    assert status["schema_version"] == "2026-05-15.301"
    assert status["source_candidate_version_v301"] == 295
    assert status["protocol_version_v301"] == 300
    assert status["observed_proxy_candidate_rows_v301"] == 1553
    assert status["imputed_selected_drop_rows_v301"] == 76
    assert status["total_pair_rows_v301"] == 118028
    assert status["budget_feasible_pair_rows_v301"] == 21916
    assert status["source_exact_pair_rows_v301"] == 12915
    assert status["cvar_feasible_repair_pair_rows_v301"] == 745
    assert status["return_improving_repair_pair_rows_v301"] == 0
    assert status["tight_relief_feasible_pair_rows_v301"] == 104
    assert status["return_improving_tight_relief_pair_rows_v301"] == 0
    assert status["top_candidate_rows_v301"] == 200
    assert status["repair_candidate_summary_rows_v301"] == 2
    assert status["repair_candidate_allocation_rows_v301"] == 342
    assert status["repair_source_summary_rows_v301"] == 102
    assert status["best_repair_added_loan_id_v301"] == "156707196"
    assert status["best_repair_dropped_loan_id_v301"] == "143080063"
    assert status["best_repair_return_delta_v301"] == pytest.approx(-2.334552360582819)
    assert status["best_repair_objective_return_after_v301"] == pytest.approx(3247.6240955865937)
    assert status["best_repair_cvar90_after_v301"] == pytest.approx(97216.88567393793)
    assert status["best_repair_imputed_proxy_loan_rows_after_v301"] == 75
    assert status["best_tight_relief_added_loan_id_v301"] == "139555921"
    assert status["best_tight_relief_dropped_loan_id_v301"] == "152791402"
    assert status["best_tight_relief_return_delta_v301"] == pytest.approx(-4.704560485038531)
    assert status["best_tight_relief_cvar90_after_v301"] == pytest.approx(97197.29074860565)
    assert status["best_tight_relief_grade_A_exposure_delta_v301"] == pytest.approx(-100.0)
    assert status["best_tight_relief_score0_exposure_delta_v301"] == pytest.approx(-100.0)
    assert status["feasible_imputation_repair_found_v301"] is True
    assert status["return_improving_imputation_repair_found_v301"] is False
    assert status["source_tight_relief_repair_found_v301"] is True
    assert status["valid_branch_price_bound_v301"] is False
    assert status["strict_live_deployability_claim_allowed_v301"] is False
    assert status["contractual_ifrs9_claim_allowed_v301"] is False
    assert status["working_champion_claim_allowed_v301"] is False
    assert status["paper1_promotion_allowed_v301"] is False
    assert status["paper4_working_champion_changed_v301"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v301"] == 8
    assert status["claim_matrix_rows_v301"] == 7
    assert (
        status["next_artifact_v301"]
        == "paper4_v302_apply_v301_repair_or_multi_swap_imputation_frontier.csv"
    )

    summary = _read_csv("paper4_v301_source_tight_branch_price_pricing_or_imputation_repair.csv")
    row = summary.iloc[0]
    assert row["screen_id_v301"] == "source_tight_branch_price_pricing_or_imputation_repair"
    assert int(row["cvar_feasible_repair_pair_rows_v301"]) == 745
    assert int(row["return_improving_repair_pair_rows_v301"]) == 0
    assert bool(row["valid_branch_price_bound_v301"]) is False
    assert bool(row["contractual_ifrs9_claim_allowed_v301"]) is False
    assert "bounded one-swap repair screen only" in str(row["claim_boundary_v301"])

    stages = _read_csv("paper4_v301_imputation_repair_stage_summary.csv")
    stage_map = dict(zip(stages["stage_v301"], stages["pair_rows_v301"], strict=False))
    assert int(stage_map["observed_proxy_candidate_rows"]) == 1553
    assert int(stage_map["imputed_selected_drop_rows"]) == 76
    assert int(stage_map["all_observed_drop_imputed_pair_rows"]) == 118028
    assert int(stage_map["budget_feasible_pair_rows"]) == 21916
    assert int(stage_map["source_exact_pair_rows"]) == 12915
    assert int(stage_map["cvar_feasible_repair_pair_rows"]) == 745
    assert int(stage_map["return_improving_repair_pair_rows"]) == 0
    assert int(stage_map["tight_relief_feasible_pair_rows"]) == 104
    assert int(stage_map["grade_source_feasible_pair_rows"]) == 14839
    assert int(stage_map["score_decile_source_feasible_pair_rows"]) == 13793

    profile = _read_csv("paper4_v301_tight_source_relief_profile.csv")
    profile_map = {row.repair_profile_v301: row for row in profile.itertuples(index=False)}
    assert int(profile_map["no_tight_relief"].pair_rows_v301) == 641
    assert int(profile_map["no_tight_relief"].return_improving_pair_rows_v301) == 0
    assert float(profile_map["no_tight_relief"].best_return_delta_v301) == pytest.approx(
        -2.334552360582819
    )
    assert int(profile_map["relieves_grade_A_and_score0"].pair_rows_v301) == 104
    assert int(profile_map["relieves_grade_A_and_score0"].return_improving_pair_rows_v301) == 0
    assert float(
        profile_map["relieves_grade_A_and_score0"].best_return_delta_v301
    ) == pytest.approx(-4.704560485038531)
    assert "relieves_score0_only" not in profile_map

    top = _read_csv("paper4_v301_observed_proxy_imputation_repair_top_candidates.csv")
    assert len(top) == status["top_candidate_rows_v301"]
    best = top.sort_values("return_delta_v301", ascending=False).iloc[0]
    assert str(best["added_loan_id_v301"]) == "156707196"
    assert str(best["dropped_loan_id_v301"]) == "143080063"
    assert float(best["return_delta_v301"]) == pytest.approx(-2.334552360582819)
    assert bool(best["return_improving_repair_v301"]) is False
    assert str(best["repair_profile_v301"]) == "no_tight_relief"
    tight = top.loc[top["repair_profile_v301"].eq("relieves_grade_A_and_score0")].iloc[0]
    assert str(tight["added_loan_id_v301"]) == "139555921"
    assert str(tight["dropped_loan_id_v301"]) == "152791402"
    assert float(tight["return_delta_v301"]) == pytest.approx(-4.704560485038531)

    candidates = _read_csv("paper4_v301_repair_candidate_summary.csv")
    assert set(candidates["repair_candidate_id_v301"]) == {
        "best_return_imputation_repair",
        "best_tight_source_relief_repair",
    }
    candidate_map = {
        row.repair_candidate_id_v301: row for row in candidates.itertuples(index=False)
    }
    assert candidate_map["best_return_imputation_repair"].added_loan_id_v301 == 156707196
    assert candidate_map["best_tight_source_relief_repair"].added_loan_id_v301 == 139555921

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v301_repair_candidate_allocations.parquet")
    assert len(allocations) == status["repair_candidate_allocation_rows_v301"]
    assert set(allocations["repair_candidate_id_v301"]) == {
        "best_return_imputation_repair",
        "best_tight_source_relief_repair",
    }
    assert allocations.groupby("repair_candidate_id_v301")["loan_id"].nunique().to_dict() == {
        "best_return_imputation_repair": 171,
        "best_tight_source_relief_repair": 171,
    }
    assert allocations["claim_boundary_v301"].str.contains("not a promoted portfolio").all()

    source = _read_csv("paper4_v301_repair_source_summary.csv")
    assert len(source) == status["repair_source_summary_rows_v301"]
    assert not source["source_cap_violated_v301"].astype(bool).any()

    blockers = _read_csv("paper4_v301_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v301"], blockers["blocking_v301"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v301"], blockers["evidence_count_v301"], strict=False)
    )
    assert bool(blocker_map["full_universe_gap_certificate_missing"]) is True
    assert int(evidence_map["full_universe_gap_certificate_missing"]) == 276869
    assert bool(blocker_map["branch_price_termination_missing"]) is True
    assert bool(blocker_map["residual_cashflow_imputation_after_best_repair"]) is True
    assert int(evidence_map["residual_cashflow_imputation_after_best_repair"]) == 75
    assert bool(blocker_map["return_improving_imputation_repair_missing"]) is True
    assert int(evidence_map["return_improving_imputation_repair_missing"]) == 0
    assert bool(blocker_map["return_improving_tight_relief_missing"]) is True
    assert int(evidence_map["return_improving_tight_relief_missing"]) == 0
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v301_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v301_observed_proxy_imputation_repair_screen_executed"]) is True
    assert bool(claim_map["v301_feasible_imputation_repair_found"]) is True
    assert bool(claim_map["v301_return_improving_imputation_repair_found"]) is False
    assert bool(claim_map["v301_feasible_tight_source_relief_found"]) is True
    assert bool(claim_map["v301_valid_branch_price_bound"]) is False
    assert bool(claim_map["v301_contractual_ifrs9_or_live_deployability"]) is False
    assert bool(claim_map["v301_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v301 observed-proxy imputation repair screen."])
    assert bool(boundary_map["v301 finds feasible one-swap imputation repair candidates."])
    assert (
        bool(boundary_map["v301 finds a return-improving one-swap imputation repair candidate."])
        is False
    )
    assert bool(boundary_map["v301 finds feasible tight-source relief candidates."])
    assert bool(boundary_map["v301 proves a valid full-universe branch-price bound."]) is False
    assert (
        bool(boundary_map["v301 resolves contractual IFRS9 or live deployability for v295."])
        is False
    )
    assert bool(boundary_map["v301 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v301_rows = backlog.loc[backlog["last_wave"].eq("v301")]
    assert len(v301_rows) == 1
    backlog_row = v301_rows.iloc[0]
    assert backlog_row["status"] == "observed_proxy_imputation_repair_screen_executed"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v302_apply_v301_repair_or_multi_swap_imputation_frontier.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v301: Source-Tight / Imputation Repair Pricing" in notebook
    assert "Return-improving repair rows: `0`" in notebook
    assert "Best repair return delta: `-2.334552360582819`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v302_greedy_imputation_frontier_keeps_optimality_claims_blocked() -> None:
    status = _read_json("paper4_v302_status.json")

    assert status["phase"] == "v302_apply_v301_repair_or_multi_swap_imputation_frontier"
    assert status["schema_version"] == "2026-05-15.302"
    assert status["source_candidate_version_v302"] == 295
    assert status["repair_screen_version_v302"] == 301
    assert status["max_greedy_steps_v302"] == 15
    assert status["greedy_steps_v302"] == 15
    assert status["initial_imputed_proxy_loan_rows_v302"] == 76
    assert status["final_imputed_proxy_loan_rows_v302"] == 61
    assert status["imputed_proxy_rows_reduced_v302"] == 15
    assert status["cumulative_return_delta_v302"] == pytest.approx(-129.5286141380224)
    assert status["final_objective_return_v302"] == pytest.approx(3120.4300338091534)
    assert status["v295_objective_return_v302"] == pytest.approx(3249.9586479471764)
    assert status["final_cvar90_v302"] == pytest.approx(97093.78314579456)
    assert status["v295_cvar90_cap_v302"] == pytest.approx(97226.40959615848)
    assert status["tight_relief_steps_v302"] == 2
    assert status["return_improving_steps_v302"] == 0
    assert status["frontier_rows_v302"] == 15
    assert status["final_allocation_rows_v302"] == 171
    assert status["final_source_summary_rows_v302"] == 51
    assert status["final_source_cap_violations_v302"] == 0
    assert status["valid_multi_swap_optimality_claim_v302"] is False
    assert status["strict_live_deployability_claim_allowed_v302"] is False
    assert status["contractual_ifrs9_claim_allowed_v302"] is False
    assert status["working_champion_claim_allowed_v302"] is False
    assert status["paper1_promotion_allowed_v302"] is False
    assert status["paper4_working_champion_changed_v302"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v302"] == 6
    assert status["claim_matrix_rows_v302"] == 5
    assert (
        status["next_artifact_v302"]
        == "paper4_v303_global_or_multiobjective_frontier_after_v302.csv"
    )

    summary = _read_csv("paper4_v302_apply_v301_repair_or_multi_swap_imputation_frontier.csv")
    row = summary.iloc[0]
    assert row["frontier_id_v302"] == "v302_greedy_multi_swap_imputation_frontier"
    assert int(row["greedy_steps_v302"]) == 15
    assert int(row["final_imputed_proxy_loan_rows_v302"]) == 61
    assert float(row["cumulative_return_delta_v302"]) == pytest.approx(-129.5286141380224)
    assert bool(row["valid_multi_swap_optimality_claim_v302"]) is False
    assert bool(row["contractual_ifrs9_claim_allowed_v302"]) is False
    assert "greedy bounded imputation frontier only" in str(row["claim_boundary_v302"])

    frontier = _read_csv("paper4_v302_greedy_imputation_frontier.csv")
    assert len(frontier) == status["frontier_rows_v302"]
    first = frontier.iloc[0]
    last = frontier.iloc[-1]
    assert int(first["frontier_step_v302"]) == 1
    assert str(first["added_loan_id_v302"]) == "156707196"
    assert str(first["dropped_loan_id_v302"]) == "143080063"
    assert float(first["return_delta_v302"]) == pytest.approx(-2.334552360582819)
    assert int(first["imputed_proxy_loan_rows_after_step_v302"]) == 75
    assert int(last["frontier_step_v302"]) == 15
    assert str(last["added_loan_id_v302"]) == "145636554"
    assert str(last["dropped_loan_id_v302"]) == "158700271"
    assert float(last["cumulative_return_delta_v302"]) == pytest.approx(-129.5286141380224)
    assert float(last["objective_return_after_step_v302"]) == pytest.approx(3120.4300338091534)
    assert float(last["cvar90_after_step_v302"]) == pytest.approx(97093.78314579456)
    assert int(last["imputed_proxy_loan_rows_after_step_v302"]) == 61
    assert not frontier["return_delta_v302"].gt(0).any()
    assert (
        int(
            frontier["repair_profile_v302"]
            .isin(["relieves_grade_A_and_score0", "relieves_grade_A_only", "relieves_score0_only"])
            .sum()
        )
        == 2
    )

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v302_greedy_frontier_final_allocations.parquet"
    )
    assert len(allocations) == status["final_allocation_rows_v302"]
    assert allocations["loan_id"].astype(str).nunique() == 171
    assert allocations["selected_v302"].astype(bool).all()
    assert allocations["claim_boundary_v302"].str.contains("not a promoted champion").all()

    source = _read_csv("paper4_v302_greedy_frontier_final_source_summary.csv")
    assert len(source) == status["final_source_summary_rows_v302"]
    assert not source["source_cap_violated_v302"].astype(bool).any()
    grade_a = source.loc[source["source_family"].eq("grade") & source["source_id"].eq("A")].iloc[0]
    assert float(grade_a["source_slack_v302"]) == pytest.approx(3.138572976535414e-05)

    blockers = _read_csv("paper4_v302_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v302"], blockers["blocking_v302"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v302"], blockers["evidence_count_v302"], strict=False)
    )
    assert bool(blocker_map["global_multi_swap_optimality_missing"]) is True
    assert int(evidence_map["global_multi_swap_optimality_missing"]) == 15
    assert bool(blocker_map["residual_cashflow_imputation_after_frontier"]) is True
    assert int(evidence_map["residual_cashflow_imputation_after_frontier"]) == 61
    assert bool(blocker_map["return_cost_frontier_not_champion_upgrade"]) is True
    assert int(evidence_map["return_cost_frontier_not_champion_upgrade"]) == 130
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v302_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v302_greedy_multi_swap_imputation_frontier_executed"]) is True
    assert bool(claim_map["v302_reduces_imputed_rows_under_constraints"]) is True
    assert bool(claim_map["v302_optimal_imputation_frontier"]) is False
    assert bool(claim_map["v302_contractual_ifrs9_or_live_deployability"]) is False
    assert bool(claim_map["v302_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v302 greedy multi-swap imputation frontier."])
    assert bool(
        boundary_map["v302 reduces v295 imputed proxy rows through 15 feasible greedy swaps."]
    )
    assert bool(boundary_map["v302 proves the optimal imputation-repair frontier."]) is False
    assert (
        bool(boundary_map["v302 resolves contractual IFRS9 or live deployability for v295."])
        is False
    )
    assert bool(boundary_map["v302 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v302_rows = backlog.loc[backlog["last_wave"].eq("v302")]
    assert len(v302_rows) == 1
    backlog_row = v302_rows.iloc[0]
    assert backlog_row["status"] == "greedy_multi_swap_imputation_frontier_executed"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v303_global_or_multiobjective_frontier_after_v302.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v302: Greedy Multi-Swap Imputation Frontier" in notebook
    assert "Final imputed proxy rows: `61`" in notebook
    assert "Cumulative return delta: `-129.5286141380224`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v303_multiobjective_frontier_audit_blocks_global_claims() -> None:
    status = _read_json("paper4_v303_status.json")

    assert status["phase"] == "v303_global_or_multiobjective_frontier_after_v302"
    assert status["schema_version"] == "2026-05-15.303"
    assert status["source_candidate_version_v303"] == 295
    assert status["frontier_version_v303"] == 302
    assert status["prefix_frontier_rows_v303"] == 16
    assert status["reward_grid_rows_v303"] == 17
    assert status["reward_envelope_rows_v303"] == 13
    assert status["reward_grid_unique_selected_steps_v303"] == 10
    assert status["minimum_reward_for_any_repair_v303"] == pytest.approx(1.9854510187119558)
    assert status["minimum_reward_for_full_frontier_v303"] == pytest.approx(19.288593262530867)
    assert status["best_average_cost_prefix_step_v303"] == 2
    assert status["best_average_return_cost_per_imputation_v303"] == pytest.approx(
        1.985451018711956
    )
    assert status["full_frontier_average_return_cost_per_imputation_v303"] == pytest.approx(
        8.635240942534827
    )
    assert status["full_frontier_final_imputed_rows_v303"] == 61
    assert status["full_frontier_cumulative_return_delta_v303"] == pytest.approx(-129.5286141380224)
    assert status["full_binary_variables_v303"] == 276869
    assert status["direct_mip_binary_guard_v303"] == 50000
    assert status["direct_full_mip_guard_exceeded_v303"] is True
    assert status["valid_global_multiobjective_claim_v303"] is False
    assert status["strict_live_deployability_claim_allowed_v303"] is False
    assert status["contractual_ifrs9_claim_allowed_v303"] is False
    assert status["working_champion_claim_allowed_v303"] is False
    assert status["paper1_promotion_allowed_v303"] is False
    assert status["paper4_working_champion_changed_v303"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v303"] == 6
    assert status["claim_matrix_rows_v303"] == 5
    assert (
        status["next_artifact_v303"]
        == "paper4_v304_bounded_multiobjective_milp_or_global_bound_probe.csv"
    )

    summary = _read_csv("paper4_v303_global_or_multiobjective_frontier_after_v302.csv")
    row = summary.iloc[0]
    assert row["audit_id_v303"] == "v303_global_or_multiobjective_frontier_after_v302"
    assert int(row["prefix_frontier_rows_v303"]) == 16
    assert int(row["reward_envelope_rows_v303"]) == 13
    assert float(row["minimum_reward_for_any_repair_v303"]) == pytest.approx(1.9854510187119558)
    assert bool(row["valid_global_multiobjective_claim_v303"]) is False
    assert bool(row["contractual_ifrs9_claim_allowed_v303"]) is False
    assert "multiobjective audit over greedy prefixes only" in str(row["claim_boundary_v303"])

    prefixes = _read_csv("paper4_v303_prefix_tradeoff_frontier.csv")
    assert len(prefixes) == status["prefix_frontier_rows_v303"]
    baseline = prefixes.loc[prefixes["frontier_step_v303"].eq(0)].iloc[0]
    step2 = prefixes.loc[prefixes["frontier_step_v303"].eq(2)].iloc[0]
    step15 = prefixes.loc[prefixes["frontier_step_v303"].eq(15)].iloc[0]
    assert int(baseline["imputed_proxy_loan_rows_v303"]) == 76
    assert float(baseline["objective_return_v303"]) == pytest.approx(3249.9586479471764)
    assert int(step2["imputed_proxy_loan_rows_v303"]) == 74
    assert float(step2["average_return_cost_per_imputation_v303"]) == pytest.approx(
        1.985451018711956
    )
    assert int(step15["imputed_proxy_loan_rows_v303"]) == 61
    assert float(step15["average_return_cost_per_imputation_v303"]) == pytest.approx(
        8.635240942534827
    )
    assert prefixes["pareto_nondominated_return_vs_imputation_v303"].astype(bool).all()
    assert prefixes["source_cap_feasible_v303"].astype(bool).all()
    assert prefixes["cvar_feasible_v303"].astype(bool).all()

    envelope = _read_csv("paper4_v303_reward_selection_envelope.csv")
    assert len(envelope) == status["reward_envelope_rows_v303"]
    envelope_steps = set(envelope["selected_frontier_step_v303"].astype(int))
    assert envelope_steps == {0, 2, 3, 4, 5, 6, 7, 10, 11, 12, 13, 14, 15}
    baseline_interval = envelope.loc[envelope["selected_frontier_step_v303"].eq(0)].iloc[0]
    full_interval = envelope.loc[envelope["selected_frontier_step_v303"].eq(15)].iloc[0]
    assert float(baseline_interval["reward_upper_bound_v303"]) == pytest.approx(1.9854510187119558)
    assert float(full_interval["reward_lower_bound_v303"]) == pytest.approx(19.288593262530867)
    assert np.isinf(float(full_interval["reward_upper_bound_v303"]))

    reward_grid = _read_csv("paper4_v303_multiobjective_reward_grid.csv")
    assert len(reward_grid) == status["reward_grid_rows_v303"]
    reward_map = dict(
        zip(
            reward_grid["reward_per_imputation_reduced_v303"],
            reward_grid["selected_frontier_step_v303"],
            strict=False,
        )
    )
    assert int(reward_map[0.0]) == 0
    assert int(reward_map[1.5]) == 0
    assert int(reward_map[2.0]) == 2
    assert int(reward_map[9.0]) == 10
    assert int(reward_map[20.0]) == 15
    reward_20 = reward_grid.loc[reward_grid["reward_per_imputation_reduced_v303"].eq(20.0)].iloc[0]
    assert int(reward_20["selected_imputed_rows_v303"]) == 61
    assert float(reward_20["selected_utility_v303"]) == pytest.approx(3420.430033809153)

    blockers = _read_csv("paper4_v303_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v303"], blockers["blocking_v303"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v303"], blockers["evidence_count_v303"], strict=False)
    )
    assert bool(blocker_map["full_universe_multiobjective_certificate_missing"]) is True
    assert int(evidence_map["full_universe_multiobjective_certificate_missing"]) == 276869
    assert bool(blocker_map["greedy_prefix_scope_only"]) is True
    assert int(evidence_map["greedy_prefix_scope_only"]) == 16
    assert bool(blocker_map["residual_cashflow_imputation_after_full_frontier"]) is True
    assert int(evidence_map["residual_cashflow_imputation_after_full_frontier"]) == 61
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v303_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v303_multiobjective_frontier_audit_executed"]) is True
    assert bool(claim_map["v303_reward_thresholds_quantified"]) is True
    assert bool(claim_map["v303_global_or_optimal_multiobjective_frontier"]) is False
    assert bool(claim_map["v303_contractual_ifrs9_or_live_deployability"]) is False
    assert bool(claim_map["v303_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v303 multiobjective audit of the v302 imputation frontier."]
    )
    assert bool(
        boundary_map["v303 quantifies the reward threshold for choosing imputation repair."]
    )
    assert bool(boundary_map["v303 proves a global or optimal multiobjective frontier."]) is False
    assert (
        bool(boundary_map["v303 resolves contractual IFRS9 or live deployability for v295."])
        is False
    )
    assert bool(boundary_map["v303 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v303_rows = backlog.loc[backlog["last_wave"].eq("v303")]
    assert len(v303_rows) == 1
    backlog_row = v303_rows.iloc[0]
    assert backlog_row["status"] == "multiobjective_frontier_audit_executed_global_claims_blocked"
    assert (
        backlog_row["next_artifact"]
        == "paper4_v304_bounded_multiobjective_milp_or_global_bound_probe.csv"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v303: Global / Multiobjective Frontier Audit" in notebook
    assert "Minimum reward for any repair:\n  `1.9854510187119558`" in notebook
    assert "Reward needed for full 15-step frontier:\n  `19.288593262530867`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v304_bounded_multiobjective_milp_keeps_global_claims_blocked() -> None:
    status = _read_json("paper4_v304_status.json")

    assert status["phase"] == "v304_bounded_multiobjective_milp_or_global_bound_probe"
    assert status["schema_version"] == "2026-05-15.304"
    assert status["source_candidate_version_v304"] == 295
    assert status["multiobjective_audit_version_v304"] == 303
    assert status["bounded_pool_rows_v304"] == 1724
    assert status["observed_outside_candidate_rows_v304"] == 1553
    assert status["outside_bounded_pool_rows_v304"] == 275145
    assert status["reward_solution_rows_v304"] == 5
    assert status["milp_success_rows_v304"] == 5
    assert status["best_return_reward_v304"] == pytest.approx(0.0)
    assert status["best_objective_return_v304"] == pytest.approx(4407.357278629816)
    assert status["best_delta_return_vs_v295_v304"] == pytest.approx(1157.39863068264)
    assert status["best_imputed_rows_v304"] == 68
    assert status["best_cvar90_v304"] == pytest.approx(97068.38549161276)
    assert status["lowest_imputed_reward_v304"] == pytest.approx(20.0)
    assert status["lowest_imputed_rows_v304"] == 51
    assert status["lowest_imputed_delta_return_vs_v295_v304"] == pytest.approx(964.3636621291053)
    assert status["lowest_imputed_cvar90_v304"] == pytest.approx(97070.87798038824)
    assert status["bounded_pool_optimality_claim_allowed_v304"] is True
    assert status["full_universe_global_claim_allowed_v304"] is False
    assert status["working_champion_claim_allowed_v304"] is False
    assert status["paper1_promotion_allowed_v304"] is False
    assert status["paper4_working_champion_changed_v304"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v304"] == 6
    assert status["claim_matrix_rows_v304"] == 5
    assert status["next_artifact_v304"] == "paper4_v305_post_v304_reprice_or_dynamic_gate.csv"

    summary = _read_csv("paper4_v304_bounded_multiobjective_milp_or_global_bound_probe.csv")
    row = summary.iloc[0]
    assert (
        row["probe_id_v304"] == "bounded_observed_proxy_multiobjective_milp_or_global_bound_probe"
    )
    assert int(row["bounded_pool_rows_v304"]) == 1724
    assert int(row["reward_solution_rows_v304"]) == 5
    assert bool(row["bounded_pool_optimality_claim_allowed_v304"]) is True
    assert bool(row["full_universe_global_claim_allowed_v304"]) is False
    assert bool(row["working_champion_claim_allowed_v304"]) is False
    assert "bounded observed-proxy MILP result only" in str(row["claim_boundary_v304"])

    pool = _read_csv("paper4_v304_bounded_pool_audit.csv")
    pool_row = pool.iloc[0]
    assert int(pool_row["full_universe_rows_v304"]) == 276869
    assert int(pool_row["bounded_pool_rows_v304"]) == 1724
    assert float(pool_row["bounded_pool_share_of_full_universe_v304"]) == pytest.approx(
        0.00622677150565791
    )
    assert int(pool_row["v295_selected_rows_v304"]) == 171
    assert int(pool_row["imputed_selected_rows_in_pool_v304"]) == 76
    assert int(pool_row["binary_variables_v304"]) == 1724
    assert int(pool_row["total_variables_v304"]) == 1853
    assert int(pool_row["constraint_rows_v304"]) == 182

    solutions = _read_csv("paper4_v304_bounded_reward_solutions.csv")
    assert len(solutions) == status["reward_solution_rows_v304"]
    assert solutions["milp_success_v304"].astype(bool).all()
    assert solutions["milp_gap_v304"].abs().max() == pytest.approx(0.0)
    assert not solutions["full_universe_global_claim_allowed_v304"].astype(bool).any()
    sol_map = {
        row.reward_per_imputation_penalty_v304: row for row in solutions.itertuples(index=False)
    }
    assert float(sol_map[0.0].objective_return_v304) == pytest.approx(4407.357278629816)
    assert int(sol_map[0.0].imputed_proxy_loan_rows_v304) == 68
    assert float(sol_map[0.0].delta_return_vs_v295_v304) == pytest.approx(1157.39863068264)
    assert float(sol_map[2.0].objective_return_v304) == pytest.approx(4405.684069517044)
    assert int(sol_map[2.0].imputed_proxy_loan_rows_v304) == 67
    assert float(sol_map[5.0].objective_return_v304) == pytest.approx(4405.684069517044)
    assert int(sol_map[10.0].imputed_proxy_loan_rows_v304) == 59
    assert float(sol_map[10.0].delta_return_vs_v295_v304) == pytest.approx(1091.8170193137648)
    assert int(sol_map[20.0].imputed_proxy_loan_rows_v304) == 51
    assert float(sol_map[20.0].delta_return_vs_v295_v304) == pytest.approx(964.3636621291053)

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v304_bounded_reward_allocations.parquet")
    assert len(allocations) == 855
    assert allocations.groupby("reward_per_imputation_penalty_v304")[
        "loan_id"
    ].nunique().to_dict() == {
        0.0: 171,
        2.0: 171,
        5.0: 171,
        10.0: 171,
        20.0: 171,
    }
    role_counts = allocations.groupby("reward_per_imputation_penalty_v304")[
        "pool_role_v304"
    ].value_counts()
    assert int(role_counts.loc[(0.0, "outside_observed_proxy")]) == 32
    assert int(role_counts.loc[(20.0, "outside_observed_proxy")]) == 43
    assert allocations["claim_boundary_v304"].str.contains("not full-universe promotion").all()

    source = _read_csv("paper4_v304_bounded_reward_source_summary.csv")
    assert len(source) == 255
    assert not source["source_cap_violated_v304"].astype(bool).any()
    grade_a_reward0 = source.loc[
        source["reward_per_imputation_penalty_v304"].eq(0.0)
        & source["source_family"].eq("grade")
        & source["source_id"].eq("A")
    ].iloc[0]
    assert float(grade_a_reward0["source_share_v304"]) == pytest.approx(0.8531348749298603)

    blockers = _read_csv("paper4_v304_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v304"], blockers["blocking_v304"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v304"], blockers["evidence_count_v304"], strict=False)
    )
    assert bool(blocker_map["full_universe_coverage_missing"]) is True
    assert int(evidence_map["full_universe_coverage_missing"]) == 275145
    assert bool(blocker_map["post_v304_reprice_missing"]) is True
    assert int(evidence_map["post_v304_reprice_missing"]) == 5
    assert bool(blocker_map["global_dynamic_gate_missing"]) is True
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v304_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v304_bounded_multiobjective_milp_executed"]) is True
    assert bool(claim_map["v304_bounded_pool_return_and_imputation_challengers"]) is True
    assert bool(claim_map["v304_full_universe_global_optimality"]) is False
    assert bool(claim_map["v304_working_champion_or_promotion"]) is False
    assert bool(claim_map["v304_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        boundary_map["Paper 4 has a v304 bounded observed-proxy multiobjective MILP probe."]
    )
    assert bool(
        boundary_map["v304 finds bounded-pool solutions that improve return and reduce imputation."]
    )
    assert (
        bool(boundary_map["v304 proves full-universe global or multiobjective optimality."])
        is False
    )
    assert (
        bool(
            boundary_map[
                "v304 authorizes a Paper 4 working champion or Paper Estrella replacement."
            ]
        )
        is False
    )
    assert bool(boundary_map["v304 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v304_rows = backlog.loc[backlog["last_wave"].eq("v304")]
    assert len(v304_rows) == 1
    backlog_row = v304_rows.iloc[0]
    assert backlog_row["status"] == "bounded_observed_proxy_multiobjective_milp_executed"
    assert backlog_row["next_artifact"] == "paper4_v305_post_v304_reprice_or_dynamic_gate.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v304: Bounded Multiobjective MILP / Global-Bound Probe" in notebook
    assert "Best objective return: `4407.357278629816`" in notebook
    assert "Lowest imputed rows: `51`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v305_post_v304_reprice_records_repair_signal_without_promotion() -> None:
    status = _read_json("paper4_v305_status.json")

    assert status["phase"] == "v305_post_v304_reprice_or_dynamic_gate"
    assert status["schema_version"] == "2026-05-16.305"
    assert status["bounded_milp_version_v305"] == 304
    assert status["baseline_version_v305"] == 295
    assert status["selected_reward_v305"] == pytest.approx(0.0)
    assert status["selected_rows_v305"] == 171
    assert status["candidate_add_rows_v305"] == 276698
    assert status["total_pair_rows_screened_v305"] == 47315358
    assert status["return_improving_pair_rows_v305"] == 1457384
    assert status["budget_return_feasible_pair_rows_v305"] == 590017
    assert status["source_prefilter_pair_rows_v305"] == 53163
    assert status["source_exact_pair_rows_v305"] == 33
    assert status["cvar_feasible_pair_rows_v305"] == 11
    assert status["one_swap_improving_rows_v305"] == 11
    assert status["best_one_swap_return_delta_v305"] == pytest.approx(5.4342866423982645)
    assert status["best_one_swap_cvar90_after_v305"] == pytest.approx(97069.07076112836)
    assert status["best_feasible_one_swap_return_delta_v305"] == pytest.approx(4.683130517167989)
    assert status["best_feasible_one_swap_cvar90_after_v305"] == pytest.approx(97055.15415607809)
    assert status["current_objective_return_v305"] == pytest.approx(4407.357278629816)
    assert status["current_cvar90_v305"] == pytest.approx(97068.38549161276)
    assert status["post_v304_one_swap_local_optimality_cleared_v305"] is False
    assert status["dynamic_gate_ready_v305"] is False
    assert status["working_champion_claim_allowed_v305"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v305"] is False
    assert status["paper1_promotion_allowed_v305"] is False
    assert status["paper4_working_champion_changed_v305"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v305"] == 5
    assert status["claim_matrix_rows_v305"] == 5
    assert status["next_artifact_v305"] == "paper4_v306_apply_next_post_v304_swap.csv"

    summary = _read_csv("paper4_v305_post_v304_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v304_bounded_observed_proxy_best_return_reward_0"
    assert int(row["candidate_add_rows_v305"]) == status["candidate_add_rows_v305"]
    assert int(row["source_exact_pair_rows_v305"]) == status["source_exact_pair_rows_v305"]
    assert int(row["one_swap_improving_rows_v305"]) == status["one_swap_improving_rows_v305"]
    assert bool(row["post_v304_one_swap_local_optimality_cleared_v305"]) is False
    assert bool(row["dynamic_gate_ready_v305"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v305"])

    stage = _read_csv("paper4_v305_post_v304_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v305"], stage["pair_rows_v305"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1457384,
        "budget_return_feasible": 590017,
        "source_prefilter_feasible": 53163,
        "source_exact_feasible": 33,
        "cvar_feasible_improving": 11,
    }

    top = _read_csv("paper4_v305_post_v304_one_swap_top_candidates.csv")
    assert len(top) == 33
    best_feasible = top.iloc[0]
    assert str(best_feasible["added_loan_id_v305"]) == "151825245"
    assert str(best_feasible["dropped_loan_id_v305"]) == "137867823"
    assert float(best_feasible["return_delta_v305"]) == pytest.approx(4.683130517167989)
    assert float(best_feasible["cvar90_after_swap_v305"]) == pytest.approx(97055.15415607809)
    assert bool(best_feasible["one_swap_improves_return_v305"]) is True

    best_source_exact = top.loc[top["return_delta_v305"].idxmax()]
    assert str(best_source_exact["added_loan_id_v305"]) == "157878957"
    assert str(best_source_exact["dropped_loan_id_v305"]) == "130167437"
    assert float(best_source_exact["return_delta_v305"]) == pytest.approx(5.4342866423982645)
    assert bool(best_source_exact["cvar_swap_feasible_v305"]) is False

    blockers = _read_csv("paper4_v305_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v305"], blockers["blocking_v305"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v305"], blockers["evidence_count_v305"], strict=False)
    )
    assert bool(blocker_map["post_v304_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v304_one_swap_improvement_found"]) == 11
    assert bool(blocker_map["bounded_solution_not_working_champion"]) is True
    assert bool(blocker_map["global_integer_gap_certificate_missing"]) is True
    assert bool(blocker_map["dynamic_online_ifrs9_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v305_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v305_post_v304_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v305_post_v304_one_swap_local_optimality"]) is False
    assert bool(claim_map["v305_dynamic_gate_ready"]) is False
    assert bool(claim_map["v305_working_champion"]) is False
    assert bool(claim_map["v305_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v305 post-v304 one-swap repricing gate."])
    assert bool(boundary_map["v305 clears post-v304 one-swap local optimality."]) is False
    assert bool(boundary_map["v305 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v305 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v305 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v305_rows = backlog.loc[backlog["last_wave"].eq("v305")]
    assert len(v305_rows) == 1
    backlog_row = v305_rows.iloc[0]
    assert backlog_row["status"] == "post_v304_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v306_apply_next_post_v304_swap.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v305: Post-v304 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `11`" in notebook
    assert "Dynamic/global gate ready:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v306_apply_post_v304_swap_requires_repricing_and_proxy_gate() -> None:
    status = _read_json("paper4_v306_status.json")

    assert status["phase"] == "v306_apply_next_post_v304_swap"
    assert status["schema_version"] == "2026-05-16.306"
    assert status["signal_version_v306"] == 305
    assert status["base_version_v306"] == 304
    assert status["baseline_version_v306"] == 295
    assert status["added_loan_id_v306"] == "151825245"
    assert status["dropped_loan_id_v306"] == "137867823"
    assert status["added_has_observed_proxy_v306"] is False
    assert status["dropped_had_observed_proxy_v306"] is True
    assert status["selected_rows_v306"] == 171
    assert status["base_selected_rows_v306"] == 171
    assert status["allocation_rows_v306"] == 171
    assert status["cardinality_restored_v306"] is True
    assert status["portfolio_exposure_v306"] == pytest.approx(846475.0)
    assert status["objective_return_v306"] == pytest.approx(4412.0404091469845)
    assert status["scenario_loss_mean_v306"] == pytest.approx(60508.724327858145)
    assert status["scenario_loss_cvar90_v306"] == pytest.approx(97055.15415607809)
    assert status["delta_return_vs_v304_v306"] == pytest.approx(4.683130517168138)
    assert status["delta_cvar90_vs_v304_v306"] == pytest.approx(-13.231335534670507)
    assert status["delta_exposure_vs_v304_v306"] == pytest.approx(-50.0)
    assert status["source_cap_violations_v306"] == 0
    assert status["max_source_share_v306"] == pytest.approx(0.8997312383708911)
    assert status["min_source_slack_v306"] == pytest.approx(8.6751011588726e-06)
    assert status["observed_v47_proxy_rows_v306"] == 102
    assert status["missing_v47_proxy_rows_v306"] == 69
    assert status["delta_missing_v47_proxy_rows_vs_v304_v306"] == 1
    assert status["budget_feasible_v306"] is True
    assert status["cvar_feasible_v306"] is True
    assert status["source_feasible_v306"] is True
    assert status["repair_candidate_feasible_v306"] is True
    assert status["post_repair_pricing_required_v306"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v306"] is False
    assert status["working_champion_claim_allowed_v306"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v306"] is False
    assert status["paper1_promotion_allowed_v306"] is False
    assert status["paper4_working_champion_changed_v306"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v306"] == 5
    assert status["claim_matrix_rows_v306"] == 5
    assert status["next_artifact_v306"] == "paper4_v307_post_v306_reprice.csv"

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v306_apply_next_post_v304_swap_allocations.parquet"
    )
    assert len(allocations) == 171
    assert allocations["loan_id"].astype(str).nunique() == 171
    assert "151825245" in set(allocations["loan_id"].astype(str))
    assert "137867823" not in set(allocations["loan_id"].astype(str))
    assert int((~allocations["observed_v47_proxy_v306"]).sum()) == 69

    summary = _read_csv("paper4_v306_apply_next_post_v304_swap_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v306"] == "post_v304_best_one_swap_repair_candidate"
    assert float(row["objective_return_v306"]) == pytest.approx(status["objective_return_v306"])
    assert float(row["scenario_loss_cvar90_v306"]) == pytest.approx(
        status["scenario_loss_cvar90_v306"]
    )
    assert bool(row["repair_candidate_feasible_v306"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v306"]) is False

    action = _read_csv("paper4_v306_apply_next_post_v304_swap_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v306"]) == "151825245"
    assert str(action_row["dropped_loan_id_v306"]) == "137867823"
    assert float(action_row["return_delta_v306"]) == pytest.approx(4.683130517167989)
    assert bool(action_row["added_has_observed_proxy_v306"]) is False
    assert bool(action_row["dropped_had_observed_proxy_v306"]) is True

    coverage = _read_csv("paper4_v306_proxy_coverage_delta.csv")
    coverage_row = coverage.iloc[0]
    assert int(coverage_row["before_observed_rows_v306"]) == 103
    assert int(coverage_row["after_observed_rows_v306"]) == 102
    assert int(coverage_row["before_missing_rows_v306"]) == 68
    assert int(coverage_row["after_missing_rows_v306"]) == 69
    assert int(coverage_row["delta_missing_rows_v306"]) == 1

    source = _read_csv("paper4_v306_apply_next_post_v304_swap_source_summary.csv")
    assert len(source) == 51
    assert not source["source_cap_violated_v306"].astype(bool).any()
    grade_a = source.loc[source["source_family"].eq("grade") & source["source_id"].eq("A")].iloc[0]
    assert float(grade_a["source_share_v306"]) == pytest.approx(0.8531261998287014)

    blockers = _read_csv("paper4_v306_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v306"], blockers["blocking_v306"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v306"], blockers["evidence_count_v306"], strict=False)
    )
    assert bool(blocker_map["best_v305_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["proxy_coverage_regression_or_gap"]) is True
    assert int(evidence_map["proxy_coverage_regression_or_gap"]) == 69
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v306_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v306_best_v305_swap_applied"]) is True
    assert bool(claim_map["v306_return_improves_and_cvar_lowers_vs_v304"]) is True
    assert bool(claim_map["v306_post_repair_local_optimality"]) is False
    assert bool(claim_map["v306_working_champion"]) is False
    assert bool(claim_map["v306_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v306 applied post-v304 one-swap repair candidate."])
    assert bool(boundary_map["v306 improves return and lowers CVaR versus v304."])
    assert bool(boundary_map["v306 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v306 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v306 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v306_rows = backlog.loc[backlog["last_wave"].eq("v306")]
    assert len(v306_rows) == 1
    backlog_row = v306_rows.iloc[0]
    assert backlog_row["status"] == "post_v304_best_one_swap_applied_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v307_post_v306_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v306: Apply Best Post-v304 One-Swap Repair" in notebook
    assert "Added loan: `151825245`" in notebook
    assert "Missing v47 proxy rows: `69`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v307_post_v306_reprice_records_next_repair_signal() -> None:
    status = _read_json("paper4_v307_status.json")

    assert status["phase"] == "v307_post_v306_reprice"
    assert status["schema_version"] == "2026-05-16.307"
    assert status["base_repair_version_v307"] == 306
    assert status["previous_reprice_version_v307"] == 305
    assert status["selected_rows_v307"] == 171
    assert status["base_selected_rows_v307"] == 171
    assert status["candidate_add_rows_v307"] == 276698
    assert status["total_pair_rows_screened_v307"] == 47315358
    assert status["return_improving_pair_rows_v307"] == 1457192
    assert status["budget_return_feasible_pair_rows_v307"] == 602428
    assert status["source_prefilter_pair_rows_v307"] == 53137
    assert status["source_exact_pair_rows_v307"] == 30
    assert status["cvar_feasible_pair_rows_v307"] == 7
    assert status["one_swap_improving_rows_v307"] == 7
    assert status["best_one_swap_return_delta_v307"] == pytest.approx(5.4342866423982645)
    assert status["best_one_swap_cvar90_after_v307"] == pytest.approx(97055.83942559364)
    assert status["best_feasible_one_swap_return_delta_v307"] == pytest.approx(2.825539642923383)
    assert status["best_feasible_one_swap_cvar90_after_v307"] == pytest.approx(97043.26964020556)
    assert status["current_missing_v47_proxy_rows_v307"] == 69
    assert status["best_feasible_delta_missing_v47_proxy_rows_v307"] == 1
    assert status["current_objective_return_v307"] == pytest.approx(4412.0404091469845)
    assert status["current_cvar90_v307"] == pytest.approx(97055.15415607808)
    assert status["post_v306_one_swap_local_optimality_cleared_v307"] is False
    assert status["dynamic_gate_ready_v307"] is False
    assert status["working_champion_claim_allowed_v307"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v307"] is False
    assert status["paper1_promotion_allowed_v307"] is False
    assert status["paper4_working_champion_changed_v307"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v307"] == "paper4_v308_apply_next_post_v306_swap.csv"

    summary = _read_csv("paper4_v307_post_v306_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v306_post_v304_best_one_swap_repair_candidate"
    assert int(row["source_exact_pair_rows_v307"]) == 30
    assert int(row["one_swap_improving_rows_v307"]) == 7
    assert bool(row["post_v306_one_swap_local_optimality_cleared_v307"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v307"])

    stage = _read_csv("paper4_v307_post_v306_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v307"], stage["pair_rows_v307"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1457192,
        "budget_return_feasible": 602428,
        "source_prefilter_feasible": 53137,
        "source_exact_feasible": 30,
        "cvar_feasible_improving": 7,
    }

    top = _read_csv("paper4_v307_post_v306_one_swap_top_candidates.csv")
    assert len(top) == 30
    best_feasible = top.iloc[0]
    assert str(best_feasible["added_loan_id_v307"]) == "127648167"
    assert str(best_feasible["dropped_loan_id_v307"]) == "130167437"
    assert float(best_feasible["return_delta_v307"]) == pytest.approx(2.825539642923383)
    assert float(best_feasible["cvar90_after_swap_v307"]) == pytest.approx(97043.26964020556)
    assert bool(best_feasible["one_swap_improves_return_v307"]) is True
    assert bool(best_feasible["added_observed_v47_proxy_v307"]) is False
    assert bool(best_feasible["dropped_observed_v47_proxy_v307"]) is True
    assert int(best_feasible["delta_missing_v47_proxy_rows_v307"]) == 1

    best_source_exact = top.loc[top["return_delta_v307"].idxmax()]
    assert str(best_source_exact["added_loan_id_v307"]) == "157878957"
    assert str(best_source_exact["dropped_loan_id_v307"]) == "130167437"
    assert float(best_source_exact["return_delta_v307"]) == pytest.approx(5.4342866423982645)
    assert bool(best_source_exact["cvar_swap_feasible_v307"]) is False

    blockers = _read_csv("paper4_v307_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v307"], blockers["blocking_v307"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v307"], blockers["evidence_count_v307"], strict=False)
    )
    assert bool(blocker_map["post_v306_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v306_one_swap_improvement_found"]) == 7
    assert bool(blocker_map["post_v306_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 69
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v307_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v307_post_v306_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v307_post_v306_one_swap_local_optimality"]) is False
    assert bool(claim_map["v307_dynamic_gate_ready"]) is False
    assert bool(claim_map["v307_working_champion"]) is False
    assert bool(claim_map["v307_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v307 post-v306 one-swap repricing gate."])
    assert bool(boundary_map["v307 clears post-v306 one-swap local optimality."]) is False
    assert bool(boundary_map["v307 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v307 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v307 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v307_rows = backlog.loc[backlog["last_wave"].eq("v307")]
    assert len(v307_rows) == 1
    backlog_row = v307_rows.iloc[0]
    assert backlog_row["status"] == "post_v306_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v308_apply_next_post_v306_swap.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v307: Post-v306 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `7`" in notebook
    assert "Best feasible delta missing proxy rows:\n  `1`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v308_apply_post_v306_swap_compounds_proxy_gap_without_promotion() -> None:
    status = _read_json("paper4_v308_status.json")

    assert status["phase"] == "v308_apply_next_post_v306_swap"
    assert status["schema_version"] == "2026-05-16.308"
    assert status["signal_version_v308"] == 307
    assert status["base_version_v308"] == 306
    assert status["baseline_version_v308"] == 295
    assert status["added_loan_id_v308"] == "127648167"
    assert status["dropped_loan_id_v308"] == "130167437"
    assert status["added_has_observed_proxy_v308"] is False
    assert status["dropped_had_observed_proxy_v308"] is True
    assert status["selected_rows_v308"] == 171
    assert status["base_selected_rows_v308"] == 171
    assert status["allocation_rows_v308"] == 171
    assert status["cardinality_restored_v308"] is True
    assert status["portfolio_exposure_v308"] == pytest.approx(846475.0)
    assert status["objective_return_v308"] == pytest.approx(4414.865948789908)
    assert status["scenario_loss_mean_v308"] == pytest.approx(60505.91783004272)
    assert status["scenario_loss_cvar90_v308"] == pytest.approx(97043.26964020557)
    assert status["delta_return_vs_v306_v308"] == pytest.approx(2.825539642923104)
    assert status["delta_cvar90_vs_v306_v308"] == pytest.approx(-11.884515872501652)
    assert status["delta_exposure_vs_v306_v308"] == pytest.approx(0.0)
    assert status["source_cap_violations_v308"] == 0
    assert status["observed_v47_proxy_rows_v308"] == 101
    assert status["missing_v47_proxy_rows_v308"] == 70
    assert status["delta_missing_v47_proxy_rows_vs_v306_v308"] == 1
    assert status["budget_feasible_v308"] is True
    assert status["cvar_feasible_v308"] is True
    assert status["source_feasible_v308"] is True
    assert status["repair_candidate_feasible_v308"] is True
    assert status["post_repair_pricing_required_v308"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v308"] is False
    assert status["working_champion_claim_allowed_v308"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v308"] is False
    assert status["paper1_promotion_allowed_v308"] is False
    assert status["paper4_working_champion_changed_v308"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v308"] == "paper4_v309_post_v308_reprice.csv"

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v308_apply_next_post_v306_swap_allocations.parquet"
    )
    assert len(allocations) == 171
    assert allocations["loan_id"].astype(str).nunique() == 171
    assert "127648167" in set(allocations["loan_id"].astype(str))
    assert "130167437" not in set(allocations["loan_id"].astype(str))
    assert int((~allocations["observed_v47_proxy_v308"]).sum()) == 70

    summary = _read_csv("paper4_v308_apply_next_post_v306_swap_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v308"] == "post_v306_best_one_swap_repair_candidate"
    assert float(row["objective_return_v308"]) == pytest.approx(status["objective_return_v308"])
    assert float(row["scenario_loss_cvar90_v308"]) == pytest.approx(
        status["scenario_loss_cvar90_v308"]
    )
    assert bool(row["repair_candidate_feasible_v308"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v308"]) is False

    action = _read_csv("paper4_v308_apply_next_post_v306_swap_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v308"]) == "127648167"
    assert str(action_row["dropped_loan_id_v308"]) == "130167437"
    assert float(action_row["return_delta_v308"]) == pytest.approx(2.825539642923383)
    assert bool(action_row["added_has_observed_proxy_v308"]) is False
    assert bool(action_row["dropped_had_observed_proxy_v308"]) is True

    coverage = _read_csv("paper4_v308_proxy_coverage_delta.csv")
    coverage_row = coverage.iloc[0]
    assert int(coverage_row["before_observed_rows_v308"]) == 102
    assert int(coverage_row["after_observed_rows_v308"]) == 101
    assert int(coverage_row["before_missing_rows_v308"]) == 69
    assert int(coverage_row["after_missing_rows_v308"]) == 70
    assert int(coverage_row["delta_missing_rows_v308"]) == 1

    source = _read_csv("paper4_v308_apply_next_post_v306_swap_source_summary.csv")
    assert len(source) == 51
    assert not source["source_cap_violated_v308"].astype(bool).any()
    grade_a = source.loc[source["source_family"].eq("grade") & source["source_id"].eq("A")].iloc[0]
    assert float(grade_a["source_share_v308"]) == pytest.approx(0.8531261998287014)

    blockers = _read_csv("paper4_v308_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v308"], blockers["blocking_v308"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v308"], blockers["evidence_count_v308"], strict=False)
    )
    assert bool(blocker_map["best_v307_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["proxy_coverage_regression_or_gap"]) is True
    assert int(evidence_map["proxy_coverage_regression_or_gap"]) == 70
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v308_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v308_best_v307_swap_applied"]) is True
    assert bool(claim_map["v308_return_improves_and_cvar_lowers_vs_v306"]) is True
    assert bool(claim_map["v308_post_repair_local_optimality"]) is False
    assert bool(claim_map["v308_working_champion"]) is False
    assert bool(claim_map["v308_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v308 applied post-v306 one-swap repair candidate."])
    assert bool(boundary_map["v308 improves return and lowers CVaR versus v306."])
    assert bool(boundary_map["v308 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v308 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v308 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v308_rows = backlog.loc[backlog["last_wave"].eq("v308")]
    assert len(v308_rows) == 1
    backlog_row = v308_rows.iloc[0]
    assert backlog_row["status"] == "post_v306_best_one_swap_applied_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v309_post_v308_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v308: Apply Best Post-v306 One-Swap Repair" in notebook
    assert "Added loan: `127648167`" in notebook
    assert "Missing v47 proxy rows: `70`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v309_post_v308_reprice_records_marginal_repair_signal() -> None:
    status = _read_json("paper4_v309_status.json")

    assert status["phase"] == "v309_post_v308_reprice"
    assert status["schema_version"] == "2026-05-16.309"
    assert status["base_repair_version_v309"] == 308
    assert status["previous_reprice_version_v309"] == 307
    assert status["selected_rows_v309"] == 171
    assert status["base_selected_rows_v309"] == 171
    assert status["candidate_add_rows_v309"] == 276698
    assert status["total_pair_rows_screened_v309"] == 47315358
    assert status["return_improving_pair_rows_v309"] == 1457070
    assert status["budget_return_feasible_pair_rows_v309"] == 602358
    assert status["source_prefilter_pair_rows_v309"] == 53129
    assert status["source_exact_pair_rows_v309"] == 22
    assert status["cvar_feasible_pair_rows_v309"] == 4
    assert status["one_swap_improving_rows_v309"] == 4
    assert status["best_one_swap_return_delta_v309"] == pytest.approx(5.376772278224863)
    assert status["best_one_swap_cvar90_after_v309"] == pytest.approx(97128.80137883892)
    assert status["best_feasible_one_swap_return_delta_v309"] == pytest.approx(1.2999666203700144)
    assert status["best_feasible_one_swap_cvar90_after_v309"] == pytest.approx(97032.42765604627)
    assert status["current_missing_v47_proxy_rows_v309"] == 70
    assert status["best_feasible_delta_missing_v47_proxy_rows_v309"] == 1
    assert status["current_objective_return_v309"] == pytest.approx(4414.865948789908)
    assert status["current_cvar90_v309"] == pytest.approx(97043.26964020556)
    assert status["post_v308_one_swap_local_optimality_cleared_v309"] is False
    assert status["dynamic_gate_ready_v309"] is False
    assert status["working_champion_claim_allowed_v309"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v309"] is False
    assert status["paper1_promotion_allowed_v309"] is False
    assert status["paper4_working_champion_changed_v309"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v309"] == "paper4_v310_apply_next_post_v308_swap.csv"

    summary = _read_csv("paper4_v309_post_v308_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v308_post_v306_best_one_swap_repair_candidate"
    assert int(row["source_exact_pair_rows_v309"]) == 22
    assert int(row["one_swap_improving_rows_v309"]) == 4
    assert bool(row["post_v308_one_swap_local_optimality_cleared_v309"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v309"])

    stage = _read_csv("paper4_v309_post_v308_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v309"], stage["pair_rows_v309"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1457070,
        "budget_return_feasible": 602358,
        "source_prefilter_feasible": 53129,
        "source_exact_feasible": 22,
        "cvar_feasible_improving": 4,
    }

    top = _read_csv("paper4_v309_post_v308_one_swap_top_candidates.csv")
    assert len(top) == 22
    best_feasible = top.iloc[0]
    assert str(best_feasible["added_loan_id_v309"]) == "157878957"
    assert str(best_feasible["dropped_loan_id_v309"]) == "148145784"
    assert float(best_feasible["return_delta_v309"]) == pytest.approx(1.2999666203700144)
    assert float(best_feasible["cvar90_after_swap_v309"]) == pytest.approx(97032.42765604627)
    assert bool(best_feasible["one_swap_improves_return_v309"]) is True
    assert bool(best_feasible["added_observed_v47_proxy_v309"]) is False
    assert bool(best_feasible["dropped_observed_v47_proxy_v309"]) is True
    assert int(best_feasible["delta_missing_v47_proxy_rows_v309"]) == 1

    best_source_exact = top.loc[top["return_delta_v309"].idxmax()]
    assert str(best_source_exact["added_loan_id_v309"]) == "145033022"
    assert str(best_source_exact["dropped_loan_id_v309"]) == "144603743"
    assert float(best_source_exact["return_delta_v309"]) == pytest.approx(5.376772278224863)
    assert bool(best_source_exact["cvar_swap_feasible_v309"]) is False

    blockers = _read_csv("paper4_v309_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v309"], blockers["blocking_v309"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v309"], blockers["evidence_count_v309"], strict=False)
    )
    assert bool(blocker_map["post_v308_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v308_one_swap_improvement_found"]) == 4
    assert bool(blocker_map["post_v308_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 70
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v309_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v309_post_v308_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v309_post_v308_one_swap_local_optimality"]) is False
    assert bool(claim_map["v309_dynamic_gate_ready"]) is False
    assert bool(claim_map["v309_working_champion"]) is False
    assert bool(claim_map["v309_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v309 post-v308 one-swap repricing gate."])
    assert bool(boundary_map["v309 clears post-v308 one-swap local optimality."]) is False
    assert bool(boundary_map["v309 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v309 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v309 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v309_rows = backlog.loc[backlog["last_wave"].eq("v309")]
    assert len(v309_rows) == 1
    backlog_row = v309_rows.iloc[0]
    assert backlog_row["status"] == "post_v308_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v310_apply_next_post_v308_swap.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v309: Post-v308 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `4`" in notebook
    assert "Best feasible delta missing proxy rows:\n  `1`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v310_apply_post_v308_swap_extends_repair_loop_without_promotion() -> None:
    status = _read_json("paper4_v310_status.json")

    assert status["phase"] == "v310_apply_next_post_v308_swap"
    assert status["schema_version"] == "2026-05-16.310"
    assert status["signal_version_v310"] == 309
    assert status["base_version_v310"] == 308
    assert status["baseline_version_v310"] == 295
    assert status["added_loan_id_v310"] == "157878957"
    assert status["dropped_loan_id_v310"] == "148145784"
    assert status["added_has_observed_proxy_v310"] is False
    assert status["dropped_had_observed_proxy_v310"] is True
    assert status["selected_rows_v310"] == 171
    assert status["base_selected_rows_v310"] == 171
    assert status["allocation_rows_v310"] == 171
    assert status["cardinality_restored_v310"] is True
    assert status["portfolio_exposure_v310"] == pytest.approx(846475.0)
    assert status["objective_return_v310"] == pytest.approx(4416.165915410278)
    assert status["scenario_loss_mean_v310"] == pytest.approx(60499.03715892069)
    assert status["scenario_loss_cvar90_v310"] == pytest.approx(97032.42765604627)
    assert status["delta_return_vs_v308_v310"] == pytest.approx(1.2999666203704692)
    assert status["delta_cvar90_vs_v308_v310"] == pytest.approx(-10.841984159284038)
    assert status["delta_exposure_vs_v308_v310"] == pytest.approx(0.0)
    assert status["source_cap_violations_v310"] == 0
    assert status["max_source_share_v310"] == pytest.approx(0.8997312383708911)
    assert status["min_source_slack_v310"] == pytest.approx(8.6751011588726e-06)
    assert status["observed_v47_proxy_rows_v310"] == 100
    assert status["missing_v47_proxy_rows_v310"] == 71
    assert status["delta_missing_v47_proxy_rows_vs_v308_v310"] == 1
    assert status["budget_feasible_v310"] is True
    assert status["cvar_feasible_v310"] is True
    assert status["source_feasible_v310"] is True
    assert status["repair_candidate_feasible_v310"] is True
    assert status["post_repair_pricing_required_v310"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v310"] is False
    assert status["working_champion_claim_allowed_v310"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v310"] is False
    assert status["paper1_promotion_allowed_v310"] is False
    assert status["paper4_working_champion_changed_v310"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v310"] == "paper4_v311_post_v310_reprice.csv"

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v310_apply_next_post_v308_swap_allocations.parquet"
    )
    assert len(allocations) == 171
    assert allocations["loan_id"].astype(str).nunique() == 171
    assert "157878957" in set(allocations["loan_id"].astype(str))
    assert "148145784" not in set(allocations["loan_id"].astype(str))
    assert int((~allocations["observed_v47_proxy_v310"]).sum()) == 71

    summary = _read_csv("paper4_v310_apply_next_post_v308_swap_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v310"] == "post_v308_best_one_swap_repair_candidate"
    assert float(row["objective_return_v310"]) == pytest.approx(status["objective_return_v310"])
    assert float(row["scenario_loss_cvar90_v310"]) == pytest.approx(
        status["scenario_loss_cvar90_v310"]
    )
    assert bool(row["repair_candidate_feasible_v310"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v310"]) is False

    action = _read_csv("paper4_v310_apply_next_post_v308_swap_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v310"]) == "157878957"
    assert str(action_row["dropped_loan_id_v310"]) == "148145784"
    assert float(action_row["return_delta_v310"]) == pytest.approx(1.2999666203700144)
    assert bool(action_row["added_has_observed_proxy_v310"]) is False
    assert bool(action_row["dropped_had_observed_proxy_v310"]) is True

    coverage = _read_csv("paper4_v310_proxy_coverage_delta.csv")
    coverage_row = coverage.iloc[0]
    assert int(coverage_row["before_observed_rows_v310"]) == 101
    assert int(coverage_row["after_observed_rows_v310"]) == 100
    assert int(coverage_row["before_missing_rows_v310"]) == 70
    assert int(coverage_row["after_missing_rows_v310"]) == 71
    assert int(coverage_row["delta_missing_rows_v310"]) == 1

    source = _read_csv("paper4_v310_apply_next_post_v308_swap_source_summary.csv")
    assert len(source) == 51
    assert not source["source_cap_violated_v310"].astype(bool).any()
    grade_a = source.loc[source["source_family"].eq("grade") & source["source_id"].eq("A")].iloc[0]
    assert float(grade_a["source_share_v310"]) == pytest.approx(0.8531261998287014)

    blockers = _read_csv("paper4_v310_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v310"], blockers["blocking_v310"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v310"], blockers["evidence_count_v310"], strict=False)
    )
    assert bool(blocker_map["best_v309_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["proxy_coverage_regression_or_gap"]) is True
    assert int(evidence_map["proxy_coverage_regression_or_gap"]) == 71
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v310_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v310_best_v309_swap_applied"]) is True
    assert bool(claim_map["v310_return_improves_and_cvar_lowers_vs_v308"]) is True
    assert bool(claim_map["v310_post_repair_local_optimality"]) is False
    assert bool(claim_map["v310_working_champion"]) is False
    assert bool(claim_map["v310_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v310 applied post-v308 one-swap repair candidate."])
    assert bool(boundary_map["v310 improves return and lowers CVaR versus v308."])
    assert bool(boundary_map["v310 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v310 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v310 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v310_rows = backlog.loc[backlog["last_wave"].eq("v310")]
    assert len(v310_rows) == 1
    backlog_row = v310_rows.iloc[0]
    assert backlog_row["status"] == "post_v308_best_one_swap_applied_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v311_post_v310_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v310: Apply Best Post-v308 One-Swap Repair" in notebook
    assert "Added loan: `157878957`" in notebook
    assert "Missing v47 proxy rows: `71`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v311_post_v310_reprice_records_remaining_repair_signal() -> None:
    status = _read_json("paper4_v311_status.json")

    assert status["phase"] == "v311_post_v310_reprice"
    assert status["schema_version"] == "2026-05-16.311"
    assert status["base_repair_version_v311"] == 310
    assert status["previous_reprice_version_v311"] == 309
    assert status["selected_rows_v311"] == 171
    assert status["base_selected_rows_v311"] == 171
    assert status["cardinality_restored_v311"] is True
    assert status["candidate_add_rows_v311"] == 276698
    assert status["total_pair_rows_screened_v311"] == 47315358
    assert status["return_improving_pair_rows_v311"] == 1457015
    assert status["budget_return_feasible_pair_rows_v311"] == 602329
    assert status["source_prefilter_pair_rows_v311"] == 53127
    assert status["source_exact_pair_rows_v311"] == 20
    assert status["cvar_feasible_pair_rows_v311"] == 2
    assert status["one_swap_improving_rows_v311"] == 2
    assert status["best_one_swap_return_delta_v311"] == pytest.approx(5.376772278224863)
    assert status["best_one_swap_cvar90_after_v311"] == pytest.approx(97117.95939467964)
    assert status["best_feasible_one_swap_return_delta_v311"] == pytest.approx(0.5559907472346772)
    assert status["best_feasible_one_swap_cvar90_after_v311"] == pytest.approx(97023.78575457056)
    assert status["current_missing_v47_proxy_rows_v311"] == 71
    assert status["best_feasible_delta_missing_v47_proxy_rows_v311"] == 1
    assert status["current_exposure_v311"] == pytest.approx(846475.0)
    assert status["current_objective_return_v311"] == pytest.approx(4416.165915410278)
    assert status["current_loss_mean_v311"] == pytest.approx(60499.03715892069)
    assert status["current_cvar90_v311"] == pytest.approx(97032.42765604627)
    assert status["post_v310_one_swap_local_optimality_cleared_v311"] is False
    assert status["dynamic_gate_ready_v311"] is False
    assert status["working_champion_claim_allowed_v311"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v311"] is False
    assert status["paper1_promotion_allowed_v311"] is False
    assert status["paper4_working_champion_changed_v311"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v311"] == "paper4_v312_apply_next_post_v310_swap.csv"

    summary = _read_csv("paper4_v311_post_v310_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v310_post_v308_best_one_swap_repair_candidate"
    assert int(row["source_exact_pair_rows_v311"]) == 20
    assert int(row["one_swap_improving_rows_v311"]) == 2
    assert bool(row["post_v310_one_swap_local_optimality_cleared_v311"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v311"])

    stage = _read_csv("paper4_v311_post_v310_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v311"], stage["pair_rows_v311"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1457015,
        "budget_return_feasible": 602329,
        "source_prefilter_feasible": 53127,
        "source_exact_feasible": 20,
        "cvar_feasible_improving": 2,
    }

    top = _read_csv("paper4_v311_post_v310_one_swap_top_candidates.csv")
    assert len(top) == 20
    best_feasible = top.iloc[0]
    assert str(best_feasible["added_loan_id_v311"]) == "160128011"
    assert str(best_feasible["dropped_loan_id_v311"]) == "141214885"
    assert float(best_feasible["return_delta_v311"]) == pytest.approx(0.5559907472346772)
    assert float(best_feasible["cvar90_after_swap_v311"]) == pytest.approx(97023.78575457056)
    assert bool(best_feasible["one_swap_improves_return_v311"]) is True
    assert bool(best_feasible["added_observed_v47_proxy_v311"]) is False
    assert bool(best_feasible["dropped_observed_v47_proxy_v311"]) is True
    assert int(best_feasible["delta_missing_v47_proxy_rows_v311"]) == 1

    second_feasible = top.iloc[1]
    assert str(second_feasible["added_loan_id_v311"]) == "129082051"
    assert str(second_feasible["dropped_loan_id_v311"]) == "144622469"
    assert float(second_feasible["return_delta_v311"]) == pytest.approx(0.4981193830302644)
    assert bool(second_feasible["one_swap_improves_return_v311"]) is True

    best_source_exact = top.loc[top["return_delta_v311"].idxmax()]
    assert str(best_source_exact["added_loan_id_v311"]) == "145033022"
    assert str(best_source_exact["dropped_loan_id_v311"]) == "144603743"
    assert float(best_source_exact["return_delta_v311"]) == pytest.approx(5.376772278224863)
    assert bool(best_source_exact["cvar_swap_feasible_v311"]) is False

    blockers = _read_csv("paper4_v311_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v311"], blockers["blocking_v311"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v311"], blockers["evidence_count_v311"], strict=False)
    )
    assert bool(blocker_map["post_v310_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v310_one_swap_improvement_found"]) == 2
    assert bool(blocker_map["post_v310_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 71
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v311_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v311_post_v310_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v311_post_v310_one_swap_local_optimality"]) is False
    assert bool(claim_map["v311_dynamic_gate_ready"]) is False
    assert bool(claim_map["v311_working_champion"]) is False
    assert bool(claim_map["v311_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v311 post-v310 one-swap repricing gate."])
    assert bool(boundary_map["v311 clears post-v310 one-swap local optimality."]) is False
    assert bool(boundary_map["v311 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v311 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v311 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v311_rows = backlog.loc[backlog["last_wave"].eq("v311")]
    assert len(v311_rows) == 1
    backlog_row = v311_rows.iloc[0]
    assert backlog_row["status"] == "post_v310_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v312_apply_next_post_v310_swap.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v311: Post-v310 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `2`" in notebook
    assert "Best feasible delta missing proxy rows:\n  `1`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v312_apply_post_v310_swap_compounds_proxy_gap_without_promotion() -> None:
    status = _read_json("paper4_v312_status.json")

    assert status["phase"] == "v312_apply_next_post_v310_swap"
    assert status["schema_version"] == "2026-05-16.312"
    assert status["signal_version_v312"] == 311
    assert status["base_version_v312"] == 310
    assert status["baseline_version_v312"] == 295
    assert status["added_loan_id_v312"] == "160128011"
    assert status["dropped_loan_id_v312"] == "141214885"
    assert status["added_has_observed_proxy_v312"] is False
    assert status["dropped_had_observed_proxy_v312"] is True
    assert status["selected_rows_v312"] == 171
    assert status["base_selected_rows_v312"] == 171
    assert status["allocation_rows_v312"] == 171
    assert status["cardinality_restored_v312"] is True
    assert status["portfolio_exposure_v312"] == pytest.approx(846275.0)
    assert status["objective_return_v312"] == pytest.approx(4416.721906157512)
    assert status["scenario_loss_mean_v312"] == pytest.approx(60491.26894448509)
    assert status["scenario_loss_cvar90_v312"] == pytest.approx(97023.78575457056)
    assert status["delta_return_vs_v310_v312"] == pytest.approx(0.5559907472343184)
    assert status["delta_cvar90_vs_v310_v312"] == pytest.approx(-8.641901475712075)
    assert status["delta_exposure_vs_v310_v312"] == pytest.approx(-200.0)
    assert status["source_cap_violations_v312"] == 0
    assert status["max_source_share_v312"] == pytest.approx(0.8997075418746862)
    assert status["min_source_slack_v312"] == pytest.approx(4.338575671913869e-05)
    assert status["observed_v47_proxy_rows_v312"] == 99
    assert status["missing_v47_proxy_rows_v312"] == 72
    assert status["delta_missing_v47_proxy_rows_vs_v310_v312"] == 1
    assert status["budget_feasible_v312"] is True
    assert status["cvar_feasible_v312"] is True
    assert status["source_feasible_v312"] is True
    assert status["repair_candidate_feasible_v312"] is True
    assert status["post_repair_pricing_required_v312"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v312"] is False
    assert status["working_champion_claim_allowed_v312"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v312"] is False
    assert status["paper1_promotion_allowed_v312"] is False
    assert status["paper4_working_champion_changed_v312"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v312"] == "paper4_v313_post_v312_reprice.csv"

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v312_apply_next_post_v310_swap_allocations.parquet"
    )
    assert len(allocations) == 171
    assert allocations["loan_id"].astype(str).nunique() == 171
    assert "160128011" in set(allocations["loan_id"].astype(str))
    assert "141214885" not in set(allocations["loan_id"].astype(str))
    assert int((~allocations["observed_v47_proxy_v312"]).sum()) == 72

    summary = _read_csv("paper4_v312_apply_next_post_v310_swap_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v312"] == "post_v310_best_one_swap_repair_candidate"
    assert float(row["objective_return_v312"]) == pytest.approx(status["objective_return_v312"])
    assert float(row["scenario_loss_cvar90_v312"]) == pytest.approx(
        status["scenario_loss_cvar90_v312"]
    )
    assert bool(row["repair_candidate_feasible_v312"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v312"]) is False

    action = _read_csv("paper4_v312_apply_next_post_v310_swap_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v312"]) == "160128011"
    assert str(action_row["dropped_loan_id_v312"]) == "141214885"
    assert float(action_row["return_delta_v312"]) == pytest.approx(0.5559907472346772)
    assert bool(action_row["added_has_observed_proxy_v312"]) is False
    assert bool(action_row["dropped_had_observed_proxy_v312"]) is True

    coverage = _read_csv("paper4_v312_proxy_coverage_delta.csv")
    coverage_row = coverage.iloc[0]
    assert int(coverage_row["before_observed_rows_v312"]) == 100
    assert int(coverage_row["after_observed_rows_v312"]) == 99
    assert int(coverage_row["before_missing_rows_v312"]) == 71
    assert int(coverage_row["after_missing_rows_v312"]) == 72
    assert int(coverage_row["delta_missing_rows_v312"]) == 1

    source = _read_csv("paper4_v312_apply_next_post_v310_swap_source_summary.csv")
    assert len(source) == 51
    assert not source["source_cap_violated_v312"].astype(bool).any()
    grade_a = source.loc[source["source_family"].eq("grade") & source["source_id"].eq("A")].iloc[0]
    assert float(grade_a["source_share_v312"]) == pytest.approx(0.8530914891731411)

    blockers = _read_csv("paper4_v312_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v312"], blockers["blocking_v312"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v312"], blockers["evidence_count_v312"], strict=False)
    )
    assert bool(blocker_map["best_v311_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["proxy_coverage_regression_or_gap"]) is True
    assert int(evidence_map["proxy_coverage_regression_or_gap"]) == 72
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v312_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v312_best_v311_swap_applied"]) is True
    assert bool(claim_map["v312_return_improves_and_cvar_lowers_vs_v310"]) is True
    assert bool(claim_map["v312_post_repair_local_optimality"]) is False
    assert bool(claim_map["v312_working_champion"]) is False
    assert bool(claim_map["v312_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v312 applied post-v310 one-swap repair candidate."])
    assert bool(boundary_map["v312 improves return and lowers CVaR versus v310."])
    assert bool(boundary_map["v312 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v312 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v312 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v312_rows = backlog.loc[backlog["last_wave"].eq("v312")]
    assert len(v312_rows) == 1
    backlog_row = v312_rows.iloc[0]
    assert backlog_row["status"] == "post_v310_best_one_swap_applied_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v313_post_v312_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v312: Apply Best Post-v310 One-Swap Repair" in notebook
    assert "Added loan: `160128011`" in notebook
    assert "Missing v47 proxy rows: `72`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v313_post_v312_reprice_records_nonmonotone_repair_signal() -> None:
    status = _read_json("paper4_v313_status.json")

    assert status["phase"] == "v313_post_v312_reprice"
    assert status["schema_version"] == "2026-05-16.313"
    assert status["base_repair_version_v313"] == 312
    assert status["previous_reprice_version_v313"] == 311
    assert status["selected_rows_v313"] == 171
    assert status["base_selected_rows_v313"] == 171
    assert status["cardinality_restored_v313"] is True
    assert status["candidate_add_rows_v313"] == 276698
    assert status["total_pair_rows_screened_v313"] == 47315358
    assert status["return_improving_pair_rows_v313"] == 1456983
    assert status["budget_return_feasible_pair_rows_v313"] == 610793
    assert status["source_prefilter_pair_rows_v313"] == 48887
    assert status["source_exact_pair_rows_v313"] == 78
    assert status["cvar_feasible_pair_rows_v313"] == 6
    assert status["one_swap_improving_rows_v313"] == 6
    assert status["best_one_swap_return_delta_v313"] == pytest.approx(7.477954283125548)
    assert status["best_one_swap_cvar90_after_v313"] == pytest.approx(97037.19363268014)
    assert status["best_feasible_one_swap_return_delta_v313"] == pytest.approx(2.537966044235489)
    assert status["best_feasible_one_swap_cvar90_after_v313"] == pytest.approx(97011.95781352991)
    assert status["current_missing_v47_proxy_rows_v313"] == 72
    assert status["best_feasible_delta_missing_v47_proxy_rows_v313"] == 0
    assert status["current_exposure_v313"] == pytest.approx(846275.0)
    assert status["current_objective_return_v313"] == pytest.approx(4416.721906157512)
    assert status["current_loss_mean_v313"] == pytest.approx(60491.26894448509)
    assert status["current_cvar90_v313"] == pytest.approx(97023.78575457056)
    assert status["post_v312_one_swap_local_optimality_cleared_v313"] is False
    assert status["dynamic_gate_ready_v313"] is False
    assert status["working_champion_claim_allowed_v313"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v313"] is False
    assert status["paper1_promotion_allowed_v313"] is False
    assert status["paper4_working_champion_changed_v313"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v313"] == "paper4_v314_apply_next_post_v312_swap.csv"

    summary = _read_csv("paper4_v313_post_v312_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v312_post_v310_best_one_swap_repair_candidate"
    assert int(row["source_exact_pair_rows_v313"]) == 78
    assert int(row["one_swap_improving_rows_v313"]) == 6
    assert bool(row["post_v312_one_swap_local_optimality_cleared_v313"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v313"])

    stage = _read_csv("paper4_v313_post_v312_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v313"], stage["pair_rows_v313"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1456983,
        "budget_return_feasible": 610793,
        "source_prefilter_feasible": 48887,
        "source_exact_feasible": 78,
        "cvar_feasible_improving": 6,
    }

    top = _read_csv("paper4_v313_post_v312_one_swap_top_candidates.csv")
    assert len(top) == 78
    best_feasible = top.iloc[0]
    assert str(best_feasible["added_loan_id_v313"]) == "131848660"
    assert str(best_feasible["dropped_loan_id_v313"]) == "138011934"
    assert float(best_feasible["return_delta_v313"]) == pytest.approx(2.537966044235489)
    assert float(best_feasible["cvar90_after_swap_v313"]) == pytest.approx(97011.95781352992)
    assert bool(best_feasible["one_swap_improves_return_v313"]) is True
    assert bool(best_feasible["added_observed_v47_proxy_v313"]) is True
    assert bool(best_feasible["dropped_observed_v47_proxy_v313"]) is True
    assert int(best_feasible["delta_missing_v47_proxy_rows_v313"]) == 0

    best_source_exact = top.loc[top["return_delta_v313"].idxmax()]
    assert str(best_source_exact["added_loan_id_v313"]) == "153611978"
    assert str(best_source_exact["dropped_loan_id_v313"]) == "143261301"
    assert float(best_source_exact["return_delta_v313"]) == pytest.approx(7.477954283125548)
    assert bool(best_source_exact["cvar_swap_feasible_v313"]) is False

    blockers = _read_csv("paper4_v313_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v313"], blockers["blocking_v313"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v313"], blockers["evidence_count_v313"], strict=False)
    )
    assert bool(blocker_map["post_v312_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v312_one_swap_improvement_found"]) == 6
    assert bool(blocker_map["post_v312_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 72
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v313_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v313_post_v312_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v313_post_v312_one_swap_local_optimality"]) is False
    assert bool(claim_map["v313_dynamic_gate_ready"]) is False
    assert bool(claim_map["v313_working_champion"]) is False
    assert bool(claim_map["v313_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v313 post-v312 one-swap repricing gate."])
    assert bool(boundary_map["v313 clears post-v312 one-swap local optimality."]) is False
    assert bool(boundary_map["v313 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v313 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v313 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v313_rows = backlog.loc[backlog["last_wave"].eq("v313")]
    assert len(v313_rows) == 1
    backlog_row = v313_rows.iloc[0]
    assert backlog_row["status"] == "post_v312_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v314_apply_next_post_v312_swap.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v313: Post-v312 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `6`" in notebook
    assert "Best feasible delta missing proxy rows:\n  `0`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v314_apply_post_v312_swap_improves_without_proxy_regression() -> None:
    status = _read_json("paper4_v314_status.json")

    assert status["phase"] == "v314_apply_next_post_v312_swap"
    assert status["schema_version"] == "2026-05-16.314"
    assert status["signal_version_v314"] == 313
    assert status["base_version_v314"] == 312
    assert status["baseline_version_v314"] == 295
    assert status["added_loan_id_v314"] == "131848660"
    assert status["dropped_loan_id_v314"] == "138011934"
    assert status["added_has_observed_proxy_v314"] is True
    assert status["dropped_had_observed_proxy_v314"] is True
    assert status["selected_rows_v314"] == 171
    assert status["base_selected_rows_v314"] == 171
    assert status["allocation_rows_v314"] == 171
    assert status["cardinality_restored_v314"] is True
    assert status["portfolio_exposure_v314"] == pytest.approx(846475.0)
    assert status["objective_return_v314"] == pytest.approx(4419.259872201747)
    assert status["scenario_loss_mean_v314"] == pytest.approx(60482.513167942845)
    assert status["scenario_loss_cvar90_v314"] == pytest.approx(97011.95781352991)
    assert status["delta_return_vs_v312_v314"] == pytest.approx(2.537966044234963)
    assert status["delta_cvar90_vs_v312_v314"] == pytest.approx(-11.827941040653968)
    assert status["delta_exposure_vs_v312_v314"] == pytest.approx(200.0)
    assert status["source_cap_violations_v314"] == 0
    assert status["max_source_share_v314"] == pytest.approx(0.8997312383708911)
    assert status["min_source_slack_v314"] == pytest.approx(8.6751011588726e-06)
    assert status["observed_v47_proxy_rows_v314"] == 99
    assert status["missing_v47_proxy_rows_v314"] == 72
    assert status["delta_missing_v47_proxy_rows_vs_v312_v314"] == 0
    assert status["budget_feasible_v314"] is True
    assert status["cvar_feasible_v314"] is True
    assert status["source_feasible_v314"] is True
    assert status["repair_candidate_feasible_v314"] is True
    assert status["post_repair_pricing_required_v314"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v314"] is False
    assert status["working_champion_claim_allowed_v314"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v314"] is False
    assert status["paper1_promotion_allowed_v314"] is False
    assert status["paper4_working_champion_changed_v314"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v314"] == "paper4_v315_post_v314_reprice.csv"

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v314_apply_next_post_v312_swap_allocations.parquet"
    )
    assert len(allocations) == 171
    assert allocations["loan_id"].astype(str).nunique() == 171
    assert "131848660" in set(allocations["loan_id"].astype(str))
    assert "138011934" not in set(allocations["loan_id"].astype(str))
    assert int((~allocations["observed_v47_proxy_v314"]).sum()) == 72

    summary = _read_csv("paper4_v314_apply_next_post_v312_swap_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v314"] == "post_v312_best_one_swap_repair_candidate"
    assert float(row["objective_return_v314"]) == pytest.approx(status["objective_return_v314"])
    assert float(row["scenario_loss_cvar90_v314"]) == pytest.approx(
        status["scenario_loss_cvar90_v314"]
    )
    assert bool(row["repair_candidate_feasible_v314"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v314"]) is False

    action = _read_csv("paper4_v314_apply_next_post_v312_swap_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v314"]) == "131848660"
    assert str(action_row["dropped_loan_id_v314"]) == "138011934"
    assert float(action_row["return_delta_v314"]) == pytest.approx(2.537966044235489)
    assert bool(action_row["added_has_observed_proxy_v314"]) is True
    assert bool(action_row["dropped_had_observed_proxy_v314"]) is True

    coverage = _read_csv("paper4_v314_proxy_coverage_delta.csv")
    coverage_row = coverage.iloc[0]
    assert int(coverage_row["before_observed_rows_v314"]) == 99
    assert int(coverage_row["after_observed_rows_v314"]) == 99
    assert int(coverage_row["before_missing_rows_v314"]) == 72
    assert int(coverage_row["after_missing_rows_v314"]) == 72
    assert int(coverage_row["delta_missing_rows_v314"]) == 0

    source = _read_csv("paper4_v314_apply_next_post_v312_swap_source_summary.csv")
    assert len(source) == 51
    assert not source["source_cap_violated_v314"].astype(bool).any()
    grade_a = source.loc[source["source_family"].eq("grade") & source["source_id"].eq("A")].iloc[0]
    assert float(grade_a["source_share_v314"]) == pytest.approx(0.8531261998287014)

    blockers = _read_csv("paper4_v314_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v314"], blockers["blocking_v314"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v314"], blockers["evidence_count_v314"], strict=False)
    )
    boundary_map = dict(
        zip(blockers["blocker_id_v314"], blockers["claim_boundary_v314"], strict=False)
    )
    assert bool(blocker_map["best_v313_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["proxy_coverage_regression_or_gap"]) is True
    assert int(evidence_map["proxy_coverage_regression_or_gap"]) == 72
    assert "does not add missing proxy rows" in boundary_map["proxy_coverage_regression_or_gap"]
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v314_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v314_best_v313_swap_applied"]) is True
    assert bool(claim_map["v314_return_improves_and_cvar_lowers_vs_v312"]) is True
    assert bool(claim_map["v314_post_repair_local_optimality"]) is False
    assert bool(claim_map["v314_working_champion"]) is False
    assert bool(claim_map["v314_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    current_boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(
        current_boundary_map["Paper 4 has a v314 applied post-v312 one-swap repair candidate."]
    )
    assert bool(current_boundary_map["v314 improves return and lowers CVaR versus v312."])
    assert (
        bool(current_boundary_map["v314 repaired portfolio is post-repair locally optimal."])
        is False
    )
    assert bool(current_boundary_map["v314 authorizes a Paper 4 working champion."]) is False
    assert bool(current_boundary_map["v314 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v314_rows = backlog.loc[backlog["last_wave"].eq("v314")]
    assert len(v314_rows) == 1
    backlog_row = v314_rows.iloc[0]
    assert backlog_row["status"] == "post_v312_best_one_swap_applied_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v315_post_v314_reprice.csv"
    assert backlog_row["execution_result"] == (
        "return_improving_cvar_lowering_swap_applied_without_proxy_regression"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v314: Apply Best Post-v312 One-Swap Repair" in notebook
    assert "Added loan: `131848660`" in notebook
    assert "Delta missing proxy rows vs v312:\n  `0`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v315_post_v314_reprice_records_single_remaining_repair_signal() -> None:
    status = _read_json("paper4_v315_status.json")

    assert status["phase"] == "v315_post_v314_reprice"
    assert status["schema_version"] == "2026-05-16.315"
    assert status["base_repair_version_v315"] == 314
    assert status["previous_reprice_version_v315"] == 313
    assert status["selected_rows_v315"] == 171
    assert status["base_selected_rows_v315"] == 171
    assert status["cardinality_restored_v315"] is True
    assert status["candidate_add_rows_v315"] == 276698
    assert status["total_pair_rows_screened_v315"] == 47315358
    assert status["return_improving_pair_rows_v315"] == 1456916
    assert status["budget_return_feasible_pair_rows_v315"] == 602291
    assert status["source_prefilter_pair_rows_v315"] == 53129
    assert status["source_exact_pair_rows_v315"] == 22
    assert status["cvar_feasible_pair_rows_v315"] == 1
    assert status["one_swap_improving_rows_v315"] == 1
    assert status["best_one_swap_return_delta_v315"] == pytest.approx(5.376772278224863)
    assert status["best_one_swap_cvar90_after_v315"] == pytest.approx(97097.48955216327)
    assert status["best_feasible_one_swap_return_delta_v315"] == pytest.approx(0.4981193830302644)
    assert status["best_feasible_one_swap_cvar90_after_v315"] == pytest.approx(96984.05075988935)
    assert status["current_missing_v47_proxy_rows_v315"] == 72
    assert status["best_feasible_delta_missing_v47_proxy_rows_v315"] == 1
    assert status["current_exposure_v315"] == pytest.approx(846475.0)
    assert status["current_objective_return_v315"] == pytest.approx(4419.259872201747)
    assert status["current_loss_mean_v315"] == pytest.approx(60482.513167942845)
    assert status["current_cvar90_v315"] == pytest.approx(97011.95781352992)
    assert status["post_v314_one_swap_local_optimality_cleared_v315"] is False
    assert status["dynamic_gate_ready_v315"] is False
    assert status["working_champion_claim_allowed_v315"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v315"] is False
    assert status["paper1_promotion_allowed_v315"] is False
    assert status["paper4_working_champion_changed_v315"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v315"] == "paper4_v316_apply_next_post_v314_swap.csv"

    summary = _read_csv("paper4_v315_post_v314_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v314_post_v312_best_one_swap_repair_candidate"
    assert int(row["source_exact_pair_rows_v315"]) == 22
    assert int(row["one_swap_improving_rows_v315"]) == 1
    assert bool(row["post_v314_one_swap_local_optimality_cleared_v315"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v315"])

    stage = _read_csv("paper4_v315_post_v314_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v315"], stage["pair_rows_v315"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1456916,
        "budget_return_feasible": 602291,
        "source_prefilter_feasible": 53129,
        "source_exact_feasible": 22,
        "cvar_feasible_improving": 1,
    }

    top = _read_csv("paper4_v315_post_v314_one_swap_top_candidates.csv")
    assert len(top) == 22
    best_feasible = top.iloc[0]
    assert str(best_feasible["added_loan_id_v315"]) == "129082051"
    assert str(best_feasible["dropped_loan_id_v315"]) == "144622469"
    assert float(best_feasible["return_delta_v315"]) == pytest.approx(0.4981193830302644)
    assert float(best_feasible["cvar90_after_swap_v315"]) == pytest.approx(96984.05075988935)
    assert bool(best_feasible["one_swap_improves_return_v315"]) is True
    assert bool(best_feasible["added_observed_v47_proxy_v315"]) is False
    assert bool(best_feasible["dropped_observed_v47_proxy_v315"]) is True
    assert int(best_feasible["delta_missing_v47_proxy_rows_v315"]) == 1

    best_source_exact = top.loc[top["return_delta_v315"].idxmax()]
    assert str(best_source_exact["added_loan_id_v315"]) == "145033022"
    assert str(best_source_exact["dropped_loan_id_v315"]) == "144603743"
    assert float(best_source_exact["return_delta_v315"]) == pytest.approx(5.376772278224863)
    assert bool(best_source_exact["cvar_swap_feasible_v315"]) is False

    blockers = _read_csv("paper4_v315_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v315"], blockers["blocking_v315"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v315"], blockers["evidence_count_v315"], strict=False)
    )
    assert bool(blocker_map["post_v314_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v314_one_swap_improvement_found"]) == 1
    assert bool(blocker_map["post_v314_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 72
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v315_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v315_post_v314_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v315_post_v314_one_swap_local_optimality"]) is False
    assert bool(claim_map["v315_dynamic_gate_ready"]) is False
    assert bool(claim_map["v315_working_champion"]) is False
    assert bool(claim_map["v315_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v315 post-v314 one-swap repricing gate."])
    assert bool(boundary_map["v315 clears post-v314 one-swap local optimality."]) is False
    assert bool(boundary_map["v315 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v315 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v315 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v315_rows = backlog.loc[backlog["last_wave"].eq("v315")]
    assert len(v315_rows) == 1
    backlog_row = v315_rows.iloc[0]
    assert backlog_row["status"] == "post_v314_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v316_apply_next_post_v314_swap.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v315: Post-v314 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `1`" in notebook
    assert "Best feasible delta missing proxy rows:\n  `1`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v316_apply_post_v314_swap_lowers_cvar_with_proxy_cost() -> None:
    status = _read_json("paper4_v316_status.json")

    assert status["phase"] == "v316_apply_next_post_v314_swap"
    assert status["schema_version"] == "2026-05-16.316"
    assert status["signal_version_v316"] == 315
    assert status["base_version_v316"] == 314
    assert status["baseline_version_v316"] == 295
    assert status["added_loan_id_v316"] == "129082051"
    assert status["dropped_loan_id_v316"] == "144622469"
    assert status["added_has_observed_proxy_v316"] is False
    assert status["dropped_had_observed_proxy_v316"] is True
    assert status["selected_rows_v316"] == 171
    assert status["base_selected_rows_v316"] == 171
    assert status["allocation_rows_v316"] == 171
    assert status["cardinality_restored_v316"] is True
    assert status["portfolio_exposure_v316"] == pytest.approx(846475.0)
    assert status["objective_return_v316"] == pytest.approx(4419.757991584778)
    assert status["scenario_loss_mean_v316"] == pytest.approx(60467.41219714489)
    assert status["scenario_loss_cvar90_v316"] == pytest.approx(96984.05075988933)
    assert status["delta_return_vs_v314_v316"] == pytest.approx(0.4981193830308257)
    assert status["delta_cvar90_vs_v314_v316"] == pytest.approx(-27.907053640592494)
    assert status["delta_exposure_vs_v314_v316"] == pytest.approx(0.0)
    assert status["source_cap_violations_v316"] == 0
    assert status["max_source_share_v316"] == pytest.approx(0.8997312383708911)
    assert status["min_source_slack_v316"] == pytest.approx(8.6751011588726e-06)
    assert status["observed_v47_proxy_rows_v316"] == 98
    assert status["missing_v47_proxy_rows_v316"] == 73
    assert status["delta_missing_v47_proxy_rows_vs_v314_v316"] == 1
    assert status["budget_feasible_v316"] is True
    assert status["cvar_feasible_v316"] is True
    assert status["source_feasible_v316"] is True
    assert status["repair_candidate_feasible_v316"] is True
    assert status["post_repair_pricing_required_v316"] is True
    assert status["post_repair_one_swap_optimality_claim_allowed_v316"] is False
    assert status["working_champion_claim_allowed_v316"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v316"] is False
    assert status["paper1_promotion_allowed_v316"] is False
    assert status["paper4_working_champion_changed_v316"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v316"] == "paper4_v317_post_v316_reprice.csv"

    allocations = pd.read_parquet(
        TABLE_DIR / "paper4_v316_apply_next_post_v314_swap_allocations.parquet"
    )
    assert len(allocations) == 171
    assert allocations["loan_id"].astype(str).nunique() == 171
    assert "129082051" in set(allocations["loan_id"].astype(str))
    assert "144622469" not in set(allocations["loan_id"].astype(str))
    assert int((~allocations["observed_v47_proxy_v316"]).sum()) == 73

    summary = _read_csv("paper4_v316_apply_next_post_v314_swap_summary.csv")
    row = summary.iloc[0]
    assert row["portfolio_label_v316"] == "post_v314_best_one_swap_repair_candidate"
    assert float(row["objective_return_v316"]) == pytest.approx(status["objective_return_v316"])
    assert float(row["scenario_loss_cvar90_v316"]) == pytest.approx(
        status["scenario_loss_cvar90_v316"]
    )
    assert bool(row["repair_candidate_feasible_v316"]) is True
    assert bool(row["post_repair_one_swap_optimality_claim_allowed_v316"]) is False

    action = _read_csv("paper4_v316_apply_next_post_v314_swap_action.csv")
    action_row = action.iloc[0]
    assert str(action_row["added_loan_id_v316"]) == "129082051"
    assert str(action_row["dropped_loan_id_v316"]) == "144622469"
    assert float(action_row["return_delta_v316"]) == pytest.approx(0.4981193830302644)
    assert bool(action_row["added_has_observed_proxy_v316"]) is False
    assert bool(action_row["dropped_had_observed_proxy_v316"]) is True

    coverage = _read_csv("paper4_v316_proxy_coverage_delta.csv")
    coverage_row = coverage.iloc[0]
    assert int(coverage_row["before_observed_rows_v316"]) == 99
    assert int(coverage_row["after_observed_rows_v316"]) == 98
    assert int(coverage_row["before_missing_rows_v316"]) == 72
    assert int(coverage_row["after_missing_rows_v316"]) == 73
    assert int(coverage_row["delta_missing_rows_v316"]) == 1

    source = _read_csv("paper4_v316_apply_next_post_v314_swap_source_summary.csv")
    assert len(source) == 51
    assert not source["source_cap_violated_v316"].astype(bool).any()
    grade_a = source.loc[source["source_family"].eq("grade") & source["source_id"].eq("A")].iloc[0]
    assert float(grade_a["source_share_v316"]) == pytest.approx(0.8531261998287014)

    blockers = _read_csv("paper4_v316_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v316"], blockers["blocking_v316"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v316"], blockers["evidence_count_v316"], strict=False)
    )
    assert bool(blocker_map["best_v315_swap_repair_candidate_created"]) is False
    assert bool(blocker_map["post_repair_one_swap_repricing_missing"]) is True
    assert bool(blocker_map["proxy_coverage_regression_or_gap"]) is True
    assert int(evidence_map["proxy_coverage_regression_or_gap"]) == 73
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v316_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v316_best_v315_swap_applied"]) is True
    assert bool(claim_map["v316_return_improves_and_cvar_lowers_vs_v314"]) is True
    assert bool(claim_map["v316_post_repair_local_optimality"]) is False
    assert bool(claim_map["v316_working_champion"]) is False
    assert bool(claim_map["v316_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v316 applied post-v314 one-swap repair candidate."])
    assert bool(boundary_map["v316 improves return and lowers CVaR versus v314."])
    assert bool(boundary_map["v316 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v316 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v316 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v316_rows = backlog.loc[backlog["last_wave"].eq("v316")]
    assert len(v316_rows) == 1
    backlog_row = v316_rows.iloc[0]
    assert backlog_row["status"] == "post_v314_best_one_swap_applied_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v317_post_v316_reprice.csv"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v316: Apply Best Post-v314 One-Swap Repair" in notebook
    assert "Added loan: `129082051`" in notebook
    assert "Missing v47 proxy rows: `73`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v317_post_v316_reprice_clears_one_swap_local_optimality() -> None:
    status = _read_json("paper4_v317_status.json")

    assert status["phase"] == "v317_post_v316_reprice"
    assert status["schema_version"] == "2026-05-16.317"
    assert status["base_repair_version_v317"] == 316
    assert status["previous_reprice_version_v317"] == 315
    assert status["selected_rows_v317"] == 171
    assert status["base_selected_rows_v317"] == 171
    assert status["cardinality_restored_v317"] is True
    assert status["candidate_add_rows_v317"] == 276698
    assert status["total_pair_rows_screened_v317"] == 47315358
    assert status["return_improving_pair_rows_v317"] == 1456904
    assert status["budget_return_feasible_pair_rows_v317"] == 602283
    assert status["source_prefilter_pair_rows_v317"] == 53128
    assert status["source_exact_pair_rows_v317"] == 21
    assert status["cvar_feasible_pair_rows_v317"] == 0
    assert status["one_swap_improving_rows_v317"] == 0
    assert status["best_one_swap_return_delta_v317"] == pytest.approx(5.376772278224863)
    assert status["best_one_swap_cvar90_after_v317"] == pytest.approx(97069.5824985227)
    assert status["best_feasible_one_swap_return_delta_v317"] is None
    assert status["best_feasible_one_swap_cvar90_after_v317"] is None
    assert status["current_missing_v47_proxy_rows_v317"] == 73
    assert status["best_feasible_delta_missing_v47_proxy_rows_v317"] == 0
    assert status["current_exposure_v317"] == pytest.approx(846475.0)
    assert status["current_objective_return_v317"] == pytest.approx(4419.757991584778)
    assert status["current_loss_mean_v317"] == pytest.approx(60467.41219714489)
    assert status["current_cvar90_v317"] == pytest.approx(96984.05075988932)
    assert status["post_v316_one_swap_local_optimality_cleared_v317"] is True
    assert status["dynamic_gate_ready_v317"] is True
    assert status["working_champion_claim_allowed_v317"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v317"] is False
    assert status["paper1_promotion_allowed_v317"] is False
    assert status["paper4_working_champion_changed_v317"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v317"] == (
        "paper4_v318_dynamic_proxy_or_global_bound_after_v316.csv"
    )

    summary = _read_csv("paper4_v317_post_v316_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v316_post_v314_best_one_swap_repair_candidate"
    assert int(row["source_exact_pair_rows_v317"]) == 21
    assert int(row["cvar_feasible_pair_rows_v317"]) == 0
    assert int(row["one_swap_improving_rows_v317"]) == 0
    assert pd.isna(row["best_feasible_one_swap_return_delta_v317"])
    assert pd.isna(row["best_feasible_one_swap_cvar90_after_v317"])
    assert bool(row["post_v316_one_swap_local_optimality_cleared_v317"]) is True
    assert bool(row["dynamic_gate_ready_v317"]) is True
    assert bool(row["working_champion_claim_allowed_v317"]) is False
    assert "post-v316 one-swap screen cleared" in str(row["claim_boundary_v317"])

    stage = _read_csv("paper4_v317_post_v316_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v317"], stage["pair_rows_v317"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1456904,
        "budget_return_feasible": 602283,
        "source_prefilter_feasible": 53128,
        "source_exact_feasible": 21,
        "cvar_feasible_improving": 0,
    }

    top = _read_csv("paper4_v317_post_v316_one_swap_top_candidates.csv")
    assert len(top) == 21
    assert not top["one_swap_improves_return_v317"].astype(bool).any()
    assert not top["cvar_swap_feasible_v317"].astype(bool).any()
    best_source_exact = top.loc[top["return_delta_v317"].idxmax()]
    assert str(best_source_exact["added_loan_id_v317"]) == "145033022"
    assert str(best_source_exact["dropped_loan_id_v317"]) == "144603743"
    assert float(best_source_exact["return_delta_v317"]) == pytest.approx(5.376772278224863)
    assert float(best_source_exact["cvar90_after_swap_v317"]) == pytest.approx(97069.5824985227)
    assert bool(best_source_exact["cvar_swap_feasible_v317"]) is False
    assert bool(best_source_exact["one_swap_improves_return_v317"]) is False
    assert bool(best_source_exact["added_observed_v47_proxy_v317"]) is False
    assert bool(best_source_exact["dropped_observed_v47_proxy_v317"]) is True
    assert int(best_source_exact["delta_missing_v47_proxy_rows_v317"]) == 1

    blockers = _read_csv("paper4_v317_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v317"], blockers["blocking_v317"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v317"], blockers["evidence_count_v317"], strict=False)
    )
    assert bool(blocker_map["post_v316_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v316_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["post_v316_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 73
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v317_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v317_post_v316_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v317_post_v316_one_swap_local_optimality"]) is True
    assert bool(claim_map["v317_dynamic_gate_ready"]) is True
    assert bool(claim_map["v317_working_champion"]) is False
    assert bool(claim_map["v317_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v317 post-v316 one-swap repricing gate."])
    assert bool(boundary_map["v317 clears post-v316 one-swap local optimality."])
    assert bool(boundary_map["v317 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v317 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v317 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v317_rows = backlog.loc[backlog["last_wave"].eq("v317")]
    assert len(v317_rows) == 1
    backlog_row = v317_rows.iloc[0]
    assert backlog_row["status"] == "post_v316_one_swap_local_optimality_cleared"
    assert backlog_row["next_artifact"] == (
        "paper4_v318_dynamic_proxy_or_global_bound_after_v316.csv"
    )
    assert backlog_row["execution_result"] == "post_v316_one_swap_reprice_cleared"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v317: Post-v316 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert (
        "Best CVaR-feasible return delta: `not applicable; no CVaR-feasible improving swaps`"
        in notebook
    )
    assert "Dynamic/global gate ready:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v318_dynamic_proxy_after_v316_blocks_promotion() -> None:
    status = _read_json("paper4_v318_status.json")

    assert status["phase"] == "v318_dynamic_proxy_or_global_bound_after_v316"
    assert status["schema_version"] == "2026-05-16.318"
    assert status["incumbent_version_v318"] == 295
    assert status["candidate_version_v318"] == 316
    assert status["local_reprice_version_v318"] == 317
    assert status["previous_global_dynamic_gate_version_v318"] == 297
    assert status["full_binary_variables_v318"] == 276869
    assert status["direct_mip_binary_guard_v318"] == 50000
    assert status["direct_full_mip_attempted_v318"] is False
    assert status["valid_full_universe_gap_certificate_v318"] is False
    assert status["post_v316_one_swap_local_optimality_cleared_v318"] is True
    assert status["dynamic_proxy_replay_executed_v318"] is True
    assert status["dynamic_proxy_trace_rows_v318"] == 768
    assert status["dynamic_proxy_policy_count_v318"] == 2
    assert status["dynamic_proxy_period_count_v318"] == 3
    assert status["period_set_match_v318"] is True
    assert status["period_distribution_match_v318"] is False
    assert status["delta_return_v316_vs_v295_v318"] == pytest.approx(1169.7993436376019)
    assert status["delta_cvar90_v316_vs_v295_v318"] == pytest.approx(-242.35883626915165)
    assert status["v316_dynamic_proxy_beats_v295_v318"] is True
    assert status["v295_observed_v47_proxy_rows_v318"] == 95
    assert status["v316_observed_v47_proxy_rows_v318"] == 98
    assert status["v295_missing_v47_proxy_rows_v318"] == 76
    assert status["v316_missing_v47_proxy_rows_v318"] == 73
    assert status["v316_observed_proxy_delta_vs_v295_v318"] == 3
    assert status["v316_missing_proxy_delta_vs_v295_v318"] == -3
    assert status["matched_period_dynamic_claim_allowed_v318"] is False
    assert status["cashflow_online_holdout_v316_claim_allowed_v318"] is False
    assert status["contractual_ifrs9_claim_allowed_v318"] is False
    assert status["strict_live_deployability_claim_allowed_v318"] is False
    assert status["working_champion_claim_allowed_v318"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v318"] is False
    assert status["paper1_promotion_allowed_v318"] is False
    assert status["paper4_working_champion_changed_v318"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v318"] == "paper4_v319_v316_cashflow_online_ifrs9_gate.csv"

    gate = _read_csv("paper4_v318_dynamic_proxy_or_global_bound_after_v316.csv")
    row = gate.iloc[0]
    assert row["gate_id_v318"] == "v318_dynamic_proxy_or_global_bound_after_v316"
    assert bool(row["post_v316_one_swap_local_optimality_cleared_v318"]) is True
    assert bool(row["dynamic_proxy_replay_executed_v318"]) is True
    assert int(row["dynamic_proxy_trace_rows_v318"]) == 768
    assert bool(row["period_set_match_v318"]) is True
    assert bool(row["period_distribution_match_v318"]) is False
    assert json.loads(row["v295_period_distribution_v318"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert json.loads(row["v316_period_distribution_v318"]) == {
        "2018": 98,
        "2019": 66,
        "2020": 7,
    }
    assert float(row["v295_objective_return_v318"]) == pytest.approx(3249.9586479471764)
    assert float(row["v316_objective_return_v318"]) == pytest.approx(4419.757991584778)
    assert float(row["v295_cvar90_v318"]) == pytest.approx(97226.40959615848)
    assert float(row["v316_cvar90_v318"]) == pytest.approx(96984.05075988933)
    assert bool(row["v316_dynamic_proxy_beats_v295_v318"]) is True
    assert bool(row["matched_period_dynamic_claim_allowed_v318"]) is False
    assert bool(row["working_champion_claim_allowed_v318"]) is False
    assert "not a live or global proof" in str(row["claim_boundary_v318"])

    proxy = _read_csv("paper4_v318_dynamic_proxy_summary.csv")
    proxy_map = {str(row["policy_id"]): row for _, row in proxy.iterrows()}
    v295_proxy = proxy_map["v295_broader_pool_after_v296_gate"]
    v316_proxy = proxy_map["v316_terminal_repair_after_v317_gate"]
    assert int(v295_proxy["trace_rows_v318"]) == 384
    assert int(v316_proxy["trace_rows_v318"]) == 384
    assert float(v295_proxy["final_objective_return_v318"]) == pytest.approx(3249.9586479471764)
    assert float(v316_proxy["final_objective_return_v318"]) == pytest.approx(4419.757991584778)
    assert float(v295_proxy["final_loss_cvar90_v318"]) == pytest.approx(97226.40959615848)
    assert float(v316_proxy["final_loss_cvar90_v318"]) == pytest.approx(96984.05075988933)
    assert float(v316_proxy["final_wealth_proxy_mean_v318"]) > float(
        v295_proxy["final_wealth_proxy_mean_v318"]
    )

    trace = pd.read_parquet(TABLE_DIR / "paper4_v318_dynamic_proxy_trace.parquet")
    assert len(trace) == 768
    assert trace["policy_id"].nunique() == 2
    assert trace.groupby("policy_id")[f"period_v318"].nunique().eq(3).all()
    assert trace.groupby(["policy_id", "period_v318"])["scenario_id_v318"].nunique().eq(128).all()

    coverage = _read_csv("paper4_v318_proxy_coverage_comparison.csv")
    coverage_map = {str(row["policy_id"]): row for _, row in coverage.iterrows()}
    v295_coverage = coverage_map["v295_broader_pool_after_v296_gate"]
    v316_coverage = coverage_map["v316_terminal_repair_after_v317_gate"]
    assert int(v295_coverage["observed_v47_proxy_rows_v318"]) == 95
    assert int(v295_coverage["missing_v47_proxy_rows_v318"]) == 76
    assert float(v295_coverage["observed_v47_proxy_share_v318"]) == pytest.approx(
        0.5555555555555556
    )
    assert int(v295_coverage["ifrs9_proxy_panel_rows_v318"]) == 3420
    assert int(v316_coverage["observed_v47_proxy_rows_v318"]) == 98
    assert int(v316_coverage["missing_v47_proxy_rows_v318"]) == 73
    assert float(v316_coverage["observed_v47_proxy_share_v318"]) == pytest.approx(
        0.5730994152046783
    )
    assert int(v316_coverage["ifrs9_proxy_panel_rows_v318"]) == 3528
    assert not coverage["contractual_ifrs9_claim_allowed_v318"].astype(bool).any()

    requirements = _read_csv("paper4_v318_gate_requirements.csv")
    requirement_map = dict(
        zip(requirements["requirement_id_v318"], requirements["met_v318"], strict=False)
    )
    assert bool(requirement_map["post_v316_one_swap_gate_cleared"]) is True
    assert bool(requirement_map["dynamic_proxy_replay_executed"]) is True
    assert bool(requirement_map["common_period_set_available"]) is True
    assert bool(requirement_map["matched_period_distribution"]) is False
    assert bool(requirement_map["observed_or_contractual_ifrs9_complete"]) is False
    assert bool(requirement_map["valid_full_universe_gap_certificate"]) is False

    blockers = _read_csv("paper4_v318_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v318"], blockers["blocking_v318"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v318"], blockers["evidence_count_v318"], strict=False)
    )
    assert bool(blocker_map["valid_global_gap_certificate_missing"]) is True
    assert bool(blocker_map["matched_period_distribution_missing"]) is True
    assert int(evidence_map["matched_period_distribution_missing"]) == 1
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 73
    assert bool(blocker_map["v316_cashflow_online_holdout_missing"]) is True
    assert bool(blocker_map["dynamic_proxy_not_live_deployment_replay"]) is True
    assert int(evidence_map["dynamic_proxy_not_live_deployment_replay"]) == 768
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v318_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v318_dynamic_global_gate_executed"]) is True
    assert bool(claim_map["v318_v316_final_static_proxy_beats_v295"]) is True
    assert bool(claim_map["v318_matched_period_dynamic_superiority"]) is False
    assert bool(claim_map["v318_contractual_ifrs9_or_full_proxy_coverage"]) is False
    assert bool(claim_map["v318_full_universe_gap_certificate"]) is False
    assert bool(claim_map["v318_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v318 dynamic proxy/global-bound audit after v316."])
    assert bool(boundary_map["v318 final-period static-book proxy favors v316 over v295."])
    assert bool(boundary_map["v318 proves matched-period dynamic or live deployability."]) is False
    assert (
        bool(boundary_map["v318 resolves contractual IFRS9 or full cashflow coverage for v316."])
        is False
    )
    assert bool(boundary_map["v318 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v318 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v318_rows = backlog.loc[backlog["last_wave"].eq("v318")]
    assert len(v318_rows) == 1
    backlog_row = v318_rows.iloc[0]
    assert backlog_row["status"] == "v316_dynamic_proxy_executed_global_proxy_gates_blocked"
    assert backlog_row["next_artifact"] == "paper4_v319_v316_cashflow_online_ifrs9_gate.csv"
    assert backlog_row["execution_result"] == (
        "v316_static_proxy_beats_v295_but_claims_remain_blocked"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v318: Dynamic Proxy / Global Bound After v316" in notebook
    assert "Delta return v316 vs v295:\n  `1169.7993436376019`" in notebook
    assert "Delta CVaR90 v316 vs v295:\n  `-242.35883626915165`" in notebook
    assert "Working champion claim allowed:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v319_v316_cashflow_online_ifrs9_gate_remains_bounded() -> None:
    status = _read_json("paper4_v319_status.json")

    assert status["phase"] == "v319_v316_cashflow_online_ifrs9_gate"
    assert status["schema_version"] == "2026-05-16.319"
    assert status["source_candidate_version_v319"] == 316
    assert status["gate_audit_version_v319"] == 318
    assert status["selected_rows_v319"] == 171
    assert status["cashflow_proxy_panel_rows_v319"] == 6156
    assert status["observed_proxy_loan_rows_v319"] == 98
    assert status["imputed_proxy_loan_rows_v319"] == 73
    assert status["post_imputation_coverage_share_v319"] == pytest.approx(1.0)
    assert status["observed_coverage_share_v319"] == pytest.approx(0.5730994152046783)
    assert status["cashflow_proxy_months_v319"] == 36
    assert status["cashflow_proxy_scenarios_v319"] == 1
    assert status["cashflow_proxy_source_rows_v319"] == 3
    assert status["online_temporal_cell_rows_v319"] == 6
    assert status["online_temporal_summary_rows_v319"] == 2
    assert status["online_internal_all_gate_family_rows_v319"] == 2
    assert status["strict_live_deployability_claim_allowed_v319"] is False
    assert status["contractual_ifrs9_claim_allowed_v319"] is False
    assert status["working_champion_claim_allowed_v319"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v319"] is False
    assert status["paper1_promotion_allowed_v319"] is False
    assert status["paper4_working_champion_changed_v319"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v319"] == 6
    assert status["claim_matrix_rows_v319"] == 6
    assert status["next_artifact_v319"] == (
        "paper4_v320_v316_matched_period_or_branch_price_protocol.csv"
    )

    gate = _read_csv("paper4_v319_v316_cashflow_online_ifrs9_gate.csv")
    gate_row = gate.iloc[0]
    assert gate_row["gate_id_v319"] == "v319_v316_cashflow_online_ifrs9_gate"
    assert int(gate_row["candidate_version_v319"]) == 316
    assert int(gate_row["gate_audit_version_v319"]) == 318
    assert int(gate_row["observed_proxy_loan_rows_v319"]) == 98
    assert int(gate_row["imputed_proxy_loan_rows_v319"]) == 73
    assert int(gate_row["online_internal_all_gate_family_rows_v319"]) == 2
    assert bool(gate_row["contractual_ifrs9_claim_allowed_v319"]) is False
    assert bool(gate_row["strict_live_deployability_claim_allowed_v319"]) is False
    assert bool(gate_row["working_champion_claim_allowed_v319"]) is False
    assert "not live or contractual" in str(gate_row["claim_boundary_v319"])

    cashflow = _read_csv("paper4_v319_v316_cashflow_proxy_summary.csv")
    cashflow_row = cashflow.iloc[0]
    assert int(cashflow_row["candidate_version_v319"]) == 316
    assert int(cashflow_row["observed_proxy_loan_rows_v319"]) == 98
    assert int(cashflow_row["imputed_proxy_loan_rows_v319"]) == 73
    assert int(cashflow_row["cashflow_proxy_panel_rows_v319"]) == 6156
    assert float(cashflow_row["post_imputation_coverage_share_v319"]) == pytest.approx(1.0)
    assert float(cashflow_row["observed_coverage_share_v319"]) == pytest.approx(0.5730994152046783)
    assert float(cashflow_row["ecl_proxy_total_v319"]) == pytest.approx(467155.3223102825)
    assert float(cashflow_row["net_cash_proxy_total_v319"]) == pytest.approx(1270711.6366337065)
    assert bool(cashflow_row["contractual_ifrs9_claim_allowed_v319"]) is False

    panel = pd.read_parquet(TABLE_DIR / "paper4_v319_v316_cashflow_proxy_panel.parquet")
    assert len(panel) == 6156
    assert panel["loan_id"].astype(str).nunique() == 171
    assert panel["month_index"].nunique() == 36
    assert panel["scenario"].nunique() == 1
    assert panel["proxy_source_v319"].value_counts().sort_index().to_dict() == {
        "imputed_grade_score": 36,
        "imputed_grade_score_period": 2592,
        "observed_v47_proxy": 3528,
    }

    source = _read_csv("paper4_v319_v316_cashflow_proxy_source_summary.csv")
    source_map = {str(row["proxy_source_v319"]): row for _, row in source.iterrows()}
    assert int(source_map["imputed_grade_score"]["loan_rows_v319"]) == 1
    assert int(source_map["imputed_grade_score"]["panel_rows_v319"]) == 36
    assert int(source_map["imputed_grade_score_period"]["loan_rows_v319"]) == 72
    assert int(source_map["imputed_grade_score_period"]["panel_rows_v319"]) == 2592
    assert int(source_map["observed_v47_proxy"]["loan_rows_v319"]) == 98
    assert int(source_map["observed_v47_proxy"]["panel_rows_v319"]) == 3528
    assert float(source_map["observed_v47_proxy"]["net_cash_proxy_total_v319"]) == pytest.approx(
        383807.00743055553
    )

    online = _read_csv("paper4_v319_v316_online_temporal_summary.csv")
    assert set(online["source_family"]) == {"period", "term"}
    assert online["holdout_periods_v319"].eq(3).all()
    assert online["source_cell_rows_v319"].eq(3).all()
    assert np.allclose(online["worst_source_coverage_v319"], 0.9242424242424242)
    assert np.allclose(online["worst_policy_period_coverage_v319"], 0.9242424242424242)
    assert np.allclose(online["max_policy_period_width_v319"], 0.3183280895629744)
    assert online["source_gate_pass_rows_v319"].eq(3).all()
    assert online["policy_gate_pass_rows_v319"].eq(3).all()
    assert online["width_gate_pass_rows_v319"].eq(3).all()
    assert online["all_internal_gates_pass_v319"].astype(bool).all()
    assert not online["external_holdout_available_v319"].astype(bool).any()
    assert not online["strict_live_claim_allowed_v319"].astype(bool).any()

    cells = _read_csv("paper4_v319_v316_online_temporal_cells.csv")
    assert len(cells) == 6
    assert cells.groupby("source_family")["holdout_period_v319"].nunique().eq(3).all()
    assert cells["source_gate80_v319"].astype(bool).all()
    assert cells["policy_gate90_v319"].astype(bool).all()
    assert cells["width_gate95_v319"].astype(bool).all()

    blockers = _read_csv("paper4_v319_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v319"], blockers["blocking_v319"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v319"], blockers["evidence_count_v319"], strict=False)
    )
    assert bool(blocker_map["cashflow_proxy_imputation_required"]) is True
    assert int(evidence_map["cashflow_proxy_imputation_required"]) == 73
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert int(evidence_map["external_online_holdout_missing"]) == 0
    assert bool(blocker_map["matched_period_dynamic_gap_from_v318"]) is True
    assert int(evidence_map["matched_period_dynamic_gap_from_v318"]) == 1
    assert bool(blocker_map["global_branch_price_certificate_missing"]) is True
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v319_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v319_v316_cashflow_proxy_panel_executed"]) is True
    assert bool(claim_map["v319_v316_online_temporal_rerun_executed"]) is True
    assert bool(claim_map["v319_v316_internal_online_all_gates"]) is True
    assert bool(claim_map["v319_contractual_ifrs9"]) is False
    assert bool(claim_map["v319_strict_live_online_deployability"]) is False
    assert bool(claim_map["v319_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v319 v316-specific cashflow proxy panel."])
    assert bool(boundary_map["Paper 4 has a v319 v316 temporal online rerun."])
    assert bool(boundary_map["v319 implements contractual IFRS9 for v316."]) is False
    assert bool(boundary_map["v319 authorizes live online deployability for v316."]) is False
    assert (
        bool(boundary_map["v319 resolves v316 global or matched-period dynamic blockers."]) is False
    )
    assert bool(boundary_map["v319 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v319_rows = backlog.loc[backlog["last_wave"].eq("v319")]
    assert len(v319_rows) == 1
    backlog_row = v319_rows.iloc[0]
    assert backlog_row["status"] == "v316_cashflow_proxy_and_temporal_online_rerun_executed"
    assert backlog_row["next_artifact"] == (
        "paper4_v320_v316_matched_period_or_branch_price_protocol.csv"
    )
    assert backlog_row["execution_result"] == (
        "cashflow_proxy_complete_with_imputation_online_temporal_not_live"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v319: v316 Cashflow / Online / IFRS9 Gate" in notebook
    assert "Imputed proxy loan rows: `73`" in notebook
    assert "Online internal all-gate family rows:\n  `2`" in notebook
    assert "Working champion claim allowed:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v320_matched_period_bounded_milp_requires_reprice() -> None:
    status = _read_json("paper4_v320_status.json")

    assert status["phase"] == "v320_v316_matched_period_or_branch_price_protocol"
    assert status["schema_version"] == "2026-05-16.320"
    assert status["base_version_v320"] == 316
    assert status["match_target_version_v320"] == 295
    assert status["gate_version_v320"] == 319
    assert status["candidate_pool_limit_v320"] == 1500
    assert status["pool_rows_v320"] == 1671
    assert status["omitted_2019_rows_outside_pool_v320"] == 73839
    assert status["constraint_rows_v320"] == 177
    assert status["variable_count_v320"] == 1800
    assert status["milp_success_v320"] is True
    assert status["milp_gap_v320"] == pytest.approx(0.0)
    assert status["selected_rows_v320"] == 171
    assert status["added_rows_v320"] == 8
    assert status["dropped_rows_v320"] == 8
    assert status["portfolio_exposure_v320"] == pytest.approx(846500.0)
    assert status["objective_return_v320"] == pytest.approx(4433.2752487568005)
    assert status["delta_return_vs_v316_v320"] == pytest.approx(13.517257172022255)
    assert status["delta_return_vs_v295_v320"] == pytest.approx(1183.316600809624)
    assert status["scenario_loss_cvar90_v320"] == pytest.approx(96827.9133726903)
    assert status["delta_cvar90_vs_v316_v320"] == pytest.approx(-156.13738719902176)
    assert status["delta_cvar90_vs_v295_v320"] == pytest.approx(-398.49622346818796)
    assert status["period_distribution_match_v320"] is True
    assert status["period_matched_candidate_found_v320"] is True
    assert status["observed_v47_proxy_rows_v320"] == 92
    assert status["missing_v47_proxy_rows_v320"] == 79
    assert status["source_cap_violations_v320"] == 0
    assert status["min_source_slack_v320"] == pytest.approx(4.337422476963226e-06)
    assert status["budget_feasible_v320"] is True
    assert status["cvar_feasible_v320"] is True
    assert status["source_feasible_v320"] is True
    assert status["post_v320_repricing_required_v320"] is True
    assert status["full_universe_integer_optimality_claim_allowed_v320"] is False
    assert status["working_champion_claim_allowed_v320"] is False
    assert status["paper1_promotion_allowed_v320"] is False
    assert status["paper4_working_champion_changed_v320"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v320"] == "paper4_v321_post_v320_matched_period_reprice.csv"

    summary = _read_csv("paper4_v320_v316_matched_period_or_branch_price_protocol.csv")
    row = summary.iloc[0]
    assert row["probe_id_v320"] == "v316_matched_period_or_branch_price_protocol"
    assert int(row["pool_rows_v320"]) == 1671
    assert bool(row["milp_success_v320"]) is True
    assert bool(row["period_distribution_match_v320"]) is True
    assert bool(row["period_matched_candidate_found_v320"]) is True
    assert bool(row["post_v320_repricing_required_v320"]) is True
    assert bool(row["working_champion_claim_allowed_v320"]) is False
    assert "post-repair repricing/global gates still missing" in str(row["claim_boundary_v320"])

    milp_summary = _read_csv("paper4_v320_matched_period_milp_summary.csv")
    milp_row = milp_summary.iloc[0]
    assert bool(milp_row["solver_success_v320"]) is True
    assert int(milp_row["milp_status_v320"]) == 0
    assert float(milp_row["milp_fun_v320"]) == pytest.approx(-4433.2752487568)
    assert float(milp_row["milp_dual_bound_v320"]) == pytest.approx(-4433.2752487568)
    assert int(milp_row["milp_node_count_v320"]) == 427
    assert json.loads(milp_row["target_period_distribution_v320"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert json.loads(milp_row["solution_period_distribution_v320"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert bool(milp_row["budget_feasible_v320"]) is True
    assert bool(milp_row["cvar_feasible_v320"]) is True
    assert bool(milp_row["source_feasible_v320"]) is True
    assert bool(milp_row["post_v320_repricing_required_v320"]) is True

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v320_matched_period_allocations.parquet")
    assert len(allocations) == 171
    assert allocations["loan_id"].astype(str).nunique() == 171
    assert float(allocations["loan_amnt"].sum()) == pytest.approx(846500.0)
    assert allocations["period"].astype(str).value_counts().sort_index().to_dict() == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert allocations["pool_role_v320"].value_counts().to_dict() == {
        "v316_selected_base": 163,
        "top_omitted_2019_candidate": 8,
    }
    assert int(allocations["observed_v47_proxy_v320"].sum()) == 92
    assert int((~allocations["observed_v47_proxy_v320"]).sum()) == 79

    actions = _read_csv("paper4_v320_matched_period_actions.csv")
    assert len(actions) == 16
    assert actions["action_v320"].value_counts().to_dict() == {
        "add_2019_candidate": 8,
        "drop_v316_selected": 8,
    }
    assert "157784068" in set(
        actions.loc[actions["action_v320"].eq("add_2019_candidate"), "loan_id"].astype(str)
    )
    assert "131848660" in set(
        actions.loc[actions["action_v320"].eq("drop_v316_selected"), "loan_id"].astype(str)
    )
    assert (
        not actions.loc[actions["action_v320"].eq("add_2019_candidate"), "observed_v47_proxy_v320"]
        .astype(bool)
        .all()
    )

    source = _read_csv("paper4_v320_matched_period_source_summary.csv")
    assert not source["source_cap_violated_v320"].astype(bool).any()
    grade_a = source.loc[source["source_family"].eq("grade") & source["source_id"].eq("A")].iloc[0]
    assert float(grade_a["source_exposure_v320"]) == pytest.approx(722175.0)
    assert float(grade_a["source_share_v320"]) == pytest.approx(0.8531305375073833)
    assert float(grade_a["source_slack_v320"]) == pytest.approx(4.337422476963226e-06)

    protocol = _read_csv("paper4_v320_protocol_steps.csv")
    protocol_map = dict(zip(protocol["step_id_v320"], protocol["executed_v320"], strict=False))
    assert bool(protocol_map["bounded_matched_period_milp"]) is True
    assert bool(protocol_map["post_v320_one_swap_reprice"]) is False
    assert bool(protocol_map["full_universe_branch_price_certificate"]) is False
    assert bool(protocol_map["cashflow_online_gate_after_repair"]) is False

    blockers = _read_csv("paper4_v320_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v320"], blockers["blocking_v320"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v320"], blockers["evidence_count_v320"], strict=False)
    )
    assert bool(blocker_map["bounded_pool_not_full_universe"]) is True
    assert int(evidence_map["bounded_pool_not_full_universe"]) == 73839
    assert bool(blocker_map["post_v320_reprice_missing"]) is True
    assert bool(blocker_map["branch_price_certificate_missing"]) is True
    assert bool(blocker_map["cashflow_online_gate_after_repair_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v320_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v320_bounded_matched_period_milp_executed"]) is True
    assert bool(claim_map["v320_period_matched_candidate_found"]) is True
    assert bool(claim_map["v320_improves_return_and_lowers_cvar_vs_v316"]) is True
    assert bool(claim_map["v320_post_repair_local_optimality"]) is False
    assert bool(claim_map["v320_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v320_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v320 bounded matched-period MILP after v316."])
    assert bool(
        boundary_map[
            "v320 finds a period-matched bounded repair improving return and CVaR vs v316."
        ]
    )
    assert bool(boundary_map["v320 repaired portfolio is post-repair locally optimal."]) is False
    assert bool(boundary_map["v320 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v320 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v320_rows = backlog.loc[backlog["last_wave"].eq("v320")]
    assert len(v320_rows) == 1
    backlog_row = v320_rows.iloc[0]
    assert backlog_row["status"] == "bounded_matched_period_repair_found_requires_repricing"
    assert backlog_row["next_artifact"] == "paper4_v321_post_v320_matched_period_reprice.csv"
    assert backlog_row["execution_result"] == (
        "period_matched_candidate_improves_return_and_cvar_bounded_pool"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v320: Matched-Period Bounded MILP" in notebook
    assert "Period distribution matched:\n  `True`" in notebook
    assert "Delta return vs v316:\n  `13.517257172022255`" in notebook
    assert "Delta CVaR90 vs v316:\n  `-156.13738719902176`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v321_post_v320_reprice_clears_local_one_swap_scope() -> None:
    status = _read_json("paper4_v321_status.json")

    assert status["phase"] == "v321_post_v320_reprice"
    assert status["schema_version"] == "2026-05-16.321"
    assert status["base_repair_version_v321"] == 320
    assert status["previous_reprice_version_v321"] == 320
    assert status["selected_rows_v321"] == 171
    assert status["base_selected_rows_v321"] == 171
    assert status["cardinality_restored_v321"] is True
    assert status["candidate_add_rows_v321"] == 276698
    assert status["total_pair_rows_screened_v321"] == 47315358
    assert status["return_improving_pair_rows_v321"] == 1457197
    assert status["budget_return_feasible_pair_rows_v321"] == 602756
    assert status["source_prefilter_pair_rows_v321"] == 53131
    assert status["source_exact_pair_rows_v321"] == 7
    assert status["cvar_feasible_pair_rows_v321"] == 0
    assert status["one_swap_improving_rows_v321"] == 0
    assert status["best_one_swap_return_delta_v321"] == pytest.approx(1.4233158764891733)
    assert status["best_one_swap_cvar90_after_v321"] == pytest.approx(96832.72712302524)
    assert status["best_feasible_one_swap_return_delta_v321"] is None
    assert status["best_feasible_one_swap_cvar90_after_v321"] is None
    assert status["current_missing_v47_proxy_rows_v321"] == 79
    assert status["best_feasible_delta_missing_v47_proxy_rows_v321"] == 0
    assert status["current_exposure_v321"] == pytest.approx(846500.0)
    assert status["current_objective_return_v321"] == pytest.approx(4433.2752487568005)
    assert status["current_loss_mean_v321"] == pytest.approx(60364.3108328269)
    assert status["current_cvar90_v321"] == pytest.approx(96827.9133726903)
    assert status["post_v320_one_swap_local_optimality_cleared_v321"] is True
    assert status["dynamic_gate_ready_v321"] is True
    assert status["working_champion_claim_allowed_v321"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v321"] is False
    assert status["paper1_promotion_allowed_v321"] is False
    assert status["paper4_working_champion_changed_v321"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v321"] == (
        "paper4_v322_dynamic_proxy_or_global_bound_after_v320.csv"
    )

    summary = _read_csv("paper4_v321_post_v320_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v320_bounded_matched_period_repair_candidate"
    assert row["regime_v321"] == "post_v320_matched_period_repair_candidate"
    assert int(row["source_exact_pair_rows_v321"]) == 7
    assert int(row["cvar_feasible_pair_rows_v321"]) == 0
    assert int(row["one_swap_improving_rows_v321"]) == 0
    assert pd.isna(row["best_feasible_one_swap_return_delta_v321"])
    assert pd.isna(row["best_feasible_one_swap_cvar90_after_v321"])
    assert bool(row["post_v320_one_swap_local_optimality_cleared_v321"]) is True
    assert bool(row["dynamic_gate_ready_v321"]) is True
    assert bool(row["working_champion_claim_allowed_v321"]) is False
    assert "post-v320 one-swap screen cleared" in str(row["claim_boundary_v321"])

    stage = _read_csv("paper4_v321_post_v320_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v321"], stage["pair_rows_v321"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1457197,
        "budget_return_feasible": 602756,
        "source_prefilter_feasible": 53131,
        "source_exact_feasible": 7,
        "cvar_feasible_improving": 0,
    }

    top = _read_csv("paper4_v321_post_v320_one_swap_top_candidates.csv")
    assert len(top) == 7
    assert not top["one_swap_improves_return_v321"].astype(bool).any()
    assert not top["cvar_swap_feasible_v321"].astype(bool).any()
    best_source_exact = top.loc[top["return_delta_v321"].idxmax()]
    assert str(best_source_exact["added_loan_id_v321"]) == "151825245"
    assert str(best_source_exact["dropped_loan_id_v321"]) == "160128011"
    assert float(best_source_exact["return_delta_v321"]) == pytest.approx(1.4233158764891733)
    assert float(best_source_exact["cvar90_after_swap_v321"]) == pytest.approx(96832.72712302524)
    assert bool(best_source_exact["cvar_swap_feasible_v321"]) is False
    assert bool(best_source_exact["one_swap_improves_return_v321"]) is False
    assert bool(best_source_exact["added_observed_v47_proxy_v321"]) is False
    assert bool(best_source_exact["dropped_observed_v47_proxy_v321"]) is False
    assert int(best_source_exact["delta_missing_v47_proxy_rows_v321"]) == 0

    blockers = _read_csv("paper4_v321_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v321"], blockers["blocking_v321"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v321"], blockers["evidence_count_v321"], strict=False)
    )
    assert bool(blocker_map["post_v320_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v320_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["post_v320_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 79
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v321_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v321_post_v320_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v321_post_v320_one_swap_local_optimality"]) is True
    assert bool(claim_map["v321_dynamic_gate_ready"]) is True
    assert bool(claim_map["v321_working_champion"]) is False
    assert bool(claim_map["v321_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v321 post-v320 one-swap repricing gate."])
    assert bool(boundary_map["v321 clears post-v320 one-swap local optimality."])
    assert bool(boundary_map["v321 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v321 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v321 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v321_rows = backlog.loc[backlog["last_wave"].eq("v321")]
    assert len(v321_rows) == 1
    backlog_row = v321_rows.iloc[0]
    assert backlog_row["status"] == "post_v320_one_swap_local_optimality_cleared"
    assert backlog_row["next_artifact"] == (
        "paper4_v322_dynamic_proxy_or_global_bound_after_v320.csv"
    )
    assert backlog_row["execution_result"] == "post_v320_one_swap_reprice_cleared"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v321: Post-v320 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert "Best source-exact return delta: `1.4233158764891733`" in notebook
    assert "Dynamic/global gate ready:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v322_dynamic_proxy_after_v320_keeps_claims_bounded() -> None:
    status = _read_json("paper4_v322_status.json")

    assert status["phase"] == "v322_dynamic_proxy_or_global_bound_after_v320"
    assert status["schema_version"] == "2026-05-16.322"
    assert status["incumbent_version_v322"] == 295
    assert status["base_comparison_version_v322"] == 316
    assert status["candidate_version_v322"] == 320
    assert status["local_reprice_version_v322"] == 321
    assert status["previous_global_dynamic_gate_version_v322"] == 318
    assert status["full_binary_variables_v322"] == 276869
    assert status["direct_mip_binary_guard_v322"] == 50000
    assert status["direct_full_mip_attempted_v322"] is False
    assert status["valid_full_universe_gap_certificate_v322"] is False
    assert status["post_v320_one_swap_local_optimality_cleared_v322"] is True
    assert status["dynamic_proxy_replay_executed_v322"] is True
    assert status["dynamic_proxy_trace_rows_v322"] == 1152
    assert status["dynamic_proxy_policy_count_v322"] == 3
    assert status["dynamic_proxy_period_count_v322"] == 3
    assert status["period_set_match_v322"] is True
    assert status["period_distribution_match_vs_v295_v322"] is True
    assert status["period_distribution_match_vs_v316_v322"] is False
    assert status["delta_return_v320_vs_v295_v322"] == pytest.approx(1183.316600809624)
    assert status["delta_cvar90_v320_vs_v295_v322"] == pytest.approx(-398.49622346818796)
    assert status["delta_return_v320_vs_v316_v322"] == pytest.approx(13.517257172022255)
    assert status["delta_cvar90_v320_vs_v316_v322"] == pytest.approx(-156.1373871990363)
    assert status["v320_dynamic_proxy_beats_v295_v322"] is True
    assert status["v320_dynamic_proxy_beats_v316_v322"] is True
    assert status["v295_observed_v47_proxy_rows_v322"] == 95
    assert status["v316_observed_v47_proxy_rows_v322"] == 98
    assert status["v320_observed_v47_proxy_rows_v322"] == 92
    assert status["v295_missing_v47_proxy_rows_v322"] == 76
    assert status["v316_missing_v47_proxy_rows_v322"] == 73
    assert status["v320_missing_v47_proxy_rows_v322"] == 79
    assert status["v320_observed_proxy_delta_vs_v295_v322"] == -3
    assert status["v320_missing_proxy_delta_vs_v295_v322"] == 3
    assert status["v320_observed_proxy_delta_vs_v316_v322"] == -6
    assert status["v320_missing_proxy_delta_vs_v316_v322"] == 6
    assert status["matched_period_dynamic_claim_allowed_v322"] is False
    assert status["cashflow_online_holdout_v320_claim_allowed_v322"] is False
    assert status["contractual_ifrs9_claim_allowed_v322"] is False
    assert status["strict_live_deployability_claim_allowed_v322"] is False
    assert status["working_champion_claim_allowed_v322"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v322"] is False
    assert status["paper1_promotion_allowed_v322"] is False
    assert status["paper4_working_champion_changed_v322"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_matrix_rows_v322"] == 8
    assert status["next_artifact_v322"] == "paper4_v323_v320_cashflow_online_ifrs9_gate.csv"

    gate = _read_csv("paper4_v322_dynamic_proxy_or_global_bound_after_v320.csv")
    row = gate.iloc[0]
    assert row["gate_id_v322"] == "v322_dynamic_proxy_or_global_bound_after_v320"
    assert int(row["base_comparison_version_v322"]) == 316
    assert bool(row["post_v320_one_swap_local_optimality_cleared_v322"]) is True
    assert bool(row["dynamic_proxy_replay_executed_v322"]) is True
    assert int(row["dynamic_proxy_trace_rows_v322"]) == 1152
    assert bool(row["period_distribution_match_vs_v295_v322"]) is True
    assert bool(row["period_distribution_match_vs_v316_v322"]) is False
    assert json.loads(row["v295_period_distribution_v322"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert json.loads(row["v316_period_distribution_v322"]) == {
        "2018": 98,
        "2019": 66,
        "2020": 7,
    }
    assert json.loads(row["v320_period_distribution_v322"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert float(row["v320_objective_return_v322"]) == pytest.approx(4433.2752487568005)
    assert float(row["v320_cvar90_v322"]) == pytest.approx(96827.9133726903)
    assert bool(row["working_champion_claim_allowed_v322"]) is False
    assert "not a live or global proof" in str(row["claim_boundary_v322"])

    proxy = _read_csv("paper4_v322_dynamic_proxy_summary.csv")
    proxy_map = {str(policy_id): group.iloc[0] for policy_id, group in proxy.groupby("policy_id")}
    assert set(proxy_map) == {
        "v295_broader_pool_after_v296_gate",
        "v316_terminal_repair_after_v317_gate",
        "v320_matched_period_after_v321_gate",
    }
    v295_proxy = proxy_map["v295_broader_pool_after_v296_gate"]
    v316_proxy = proxy_map["v316_terminal_repair_after_v317_gate"]
    v320_proxy = proxy_map["v320_matched_period_after_v321_gate"]
    assert int(v295_proxy["trace_rows_v322"]) == 384
    assert int(v316_proxy["trace_rows_v322"]) == 384
    assert int(v320_proxy["trace_rows_v322"]) == 384
    assert float(v295_proxy["final_objective_return_v322"]) == pytest.approx(3249.9586479471764)
    assert float(v316_proxy["final_objective_return_v322"]) == pytest.approx(4419.757991584778)
    assert float(v320_proxy["final_objective_return_v322"]) == pytest.approx(4433.2752487568005)
    assert float(v295_proxy["final_loss_cvar90_v322"]) == pytest.approx(97226.40959615848)
    assert float(v316_proxy["final_loss_cvar90_v322"]) == pytest.approx(96984.05075988933)
    assert float(v320_proxy["final_loss_cvar90_v322"]) == pytest.approx(96827.9133726903)

    trace = pd.read_parquet(TABLE_DIR / "paper4_v322_dynamic_proxy_trace.parquet")
    assert len(trace) == 1152
    assert trace["policy_id"].nunique() == 3
    assert trace.groupby("policy_id")["period_v322"].nunique().eq(3).all()
    assert trace.groupby(["policy_id", "period_v322"])["scenario_id_v322"].nunique().eq(128).all()

    coverage = _read_csv("paper4_v322_proxy_coverage_comparison.csv")
    coverage_map = {
        str(policy_id): group.iloc[0] for policy_id, group in coverage.groupby("policy_id")
    }
    assert (
        int(coverage_map["v295_broader_pool_after_v296_gate"]["observed_v47_proxy_rows_v322"]) == 95
    )
    assert (
        int(coverage_map["v316_terminal_repair_after_v317_gate"]["observed_v47_proxy_rows_v322"])
        == 98
    )
    assert (
        int(coverage_map["v320_matched_period_after_v321_gate"]["observed_v47_proxy_rows_v322"])
        == 92
    )
    assert (
        int(coverage_map["v320_matched_period_after_v321_gate"]["missing_v47_proxy_rows_v322"])
        == 79
    )
    assert float(
        coverage_map["v320_matched_period_after_v321_gate"]["observed_v47_proxy_share_v322"]
    ) == pytest.approx(0.5380116959064327)
    assert not coverage["contractual_ifrs9_claim_allowed_v322"].astype(bool).any()

    requirements = _read_csv("paper4_v322_gate_requirements.csv")
    requirement_map = dict(
        zip(requirements["requirement_id_v322"], requirements["met_v322"], strict=False)
    )
    assert bool(requirement_map["post_v320_one_swap_gate_cleared"]) is True
    assert bool(requirement_map["dynamic_proxy_replay_executed"]) is True
    assert bool(requirement_map["common_period_set_available"]) is True
    assert bool(requirement_map["matched_period_distribution"]) is True
    assert bool(requirement_map["observed_or_contractual_ifrs9_complete"]) is False
    assert bool(requirement_map["valid_full_universe_gap_certificate"]) is False

    blockers = _read_csv("paper4_v322_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v322"], blockers["blocking_v322"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v322"], blockers["evidence_count_v322"], strict=False)
    )
    assert bool(blocker_map["valid_global_gap_certificate_missing"]) is True
    assert bool(blocker_map["target_period_distribution_matched_but_live_replay_missing"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 79
    assert bool(blocker_map["dynamic_proxy_not_live_deployment_replay"]) is True
    assert int(evidence_map["dynamic_proxy_not_live_deployment_replay"]) == 1152
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v322_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v322_dynamic_global_gate_executed"]) is True
    assert bool(claim_map["v322_v320_final_static_proxy_beats_v295"]) is True
    assert bool(claim_map["v322_v320_final_static_proxy_beats_v316"]) is True
    assert bool(claim_map["v322_v320_matches_v295_period_distribution"]) is True
    assert bool(claim_map["v322_matched_period_dynamic_superiority"]) is False
    assert bool(claim_map["v322_contractual_ifrs9_or_full_proxy_coverage"]) is False
    assert bool(claim_map["v322_full_universe_gap_certificate"]) is False
    assert bool(claim_map["v322_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v322 dynamic proxy/global-bound audit after v320."])
    assert bool(boundary_map["v322 final-period static-book proxy favors v320 over v295."])
    assert bool(boundary_map["v322 final-period static-book proxy favors v320 over v316."])
    assert bool(boundary_map["v322 proves matched-period dynamic or live deployability."]) is False
    assert bool(boundary_map["v322 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v322 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v322_rows = backlog.loc[backlog["last_wave"].eq("v322")]
    assert len(v322_rows) == 1
    backlog_row = v322_rows.iloc[0]
    assert backlog_row["status"] == "v320_dynamic_proxy_executed_global_live_proxy_gates_blocked"
    assert backlog_row["next_artifact"] == "paper4_v323_v320_cashflow_online_ifrs9_gate.csv"
    assert backlog_row["execution_result"] == (
        "v320_static_proxy_beats_v316_and_v295_but_proxy_gap_remains"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v322: Dynamic Proxy / Global Bound After v320" in notebook
    assert "Dynamic proxy trace rows: `1152`" in notebook
    assert "v320 static-book proxy beats v316:\n  `True`" in notebook
    assert "v320 observed v47 proxy rows:\n  `92`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v323_v320_cashflow_online_ifrs9_gate_remains_bounded() -> None:
    status = _read_json("paper4_v323_status.json")

    assert status["phase"] == "v323_v320_cashflow_online_ifrs9_gate"
    assert status["schema_version"] == "2026-05-16.323"
    assert status["source_candidate_version_v323"] == 320
    assert status["gate_audit_version_v323"] == 322
    assert status["selected_rows_v323"] == 171
    assert status["cashflow_proxy_panel_rows_v323"] == 6156
    assert status["observed_proxy_loan_rows_v323"] == 92
    assert status["imputed_proxy_loan_rows_v323"] == 79
    assert status["post_imputation_coverage_share_v323"] == pytest.approx(1.0)
    assert status["observed_coverage_share_v323"] == pytest.approx(0.5380116959064327)
    assert status["observed_proxy_delta_vs_v295_v323"] == -3
    assert status["missing_proxy_delta_vs_v295_v323"] == 3
    assert status["observed_proxy_delta_vs_v316_v323"] == -6
    assert status["missing_proxy_delta_vs_v316_v323"] == 6
    assert status["cashflow_proxy_months_v323"] == 36
    assert status["cashflow_proxy_scenarios_v323"] == 1
    assert status["cashflow_proxy_source_rows_v323"] == 3
    assert status["online_temporal_cell_rows_v323"] == 6
    assert status["online_temporal_summary_rows_v323"] == 2
    assert status["online_internal_all_gate_family_rows_v323"] == 2
    assert status["strict_live_deployability_claim_allowed_v323"] is False
    assert status["contractual_ifrs9_claim_allowed_v323"] is False
    assert status["working_champion_claim_allowed_v323"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v323"] is False
    assert status["paper1_promotion_allowed_v323"] is False
    assert status["paper4_working_champion_changed_v323"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v323"] == 7
    assert status["claim_matrix_rows_v323"] == 7
    assert status["next_artifact_v323"] == (
        "paper4_v324_v320_proxy_gap_repair_or_branch_price_protocol.csv"
    )

    gate = _read_csv("paper4_v323_v320_cashflow_online_ifrs9_gate.csv")
    gate_row = gate.iloc[0]
    assert gate_row["gate_id_v323"] == "v323_v320_cashflow_online_ifrs9_gate"
    assert int(gate_row["candidate_version_v323"]) == 320
    assert int(gate_row["gate_audit_version_v323"]) == 322
    assert int(gate_row["observed_proxy_loan_rows_v323"]) == 92
    assert int(gate_row["imputed_proxy_loan_rows_v323"]) == 79
    assert int(gate_row["observed_proxy_delta_vs_v316_v323"]) == -6
    assert int(gate_row["online_internal_all_gate_family_rows_v323"]) == 2
    assert bool(gate_row["contractual_ifrs9_claim_allowed_v323"]) is False
    assert bool(gate_row["strict_live_deployability_claim_allowed_v323"]) is False
    assert bool(gate_row["working_champion_claim_allowed_v323"]) is False
    assert "not live or contractual" in str(gate_row["claim_boundary_v323"])

    cashflow = _read_csv("paper4_v323_v320_cashflow_proxy_summary.csv")
    cashflow_row = cashflow.iloc[0]
    assert int(cashflow_row["candidate_version_v323"]) == 320
    assert int(cashflow_row["observed_proxy_loan_rows_v323"]) == 92
    assert int(cashflow_row["imputed_proxy_loan_rows_v323"]) == 79
    assert int(cashflow_row["cashflow_proxy_panel_rows_v323"]) == 6156
    assert float(cashflow_row["post_imputation_coverage_share_v323"]) == pytest.approx(1.0)
    assert float(cashflow_row["observed_coverage_share_v323"]) == pytest.approx(0.5380116959064327)
    assert float(cashflow_row["ecl_proxy_total_v323"]) == pytest.approx(465823.50046265195)
    assert float(cashflow_row["net_cash_proxy_total_v323"]) == pytest.approx(1265496.919050211)
    assert bool(cashflow_row["contractual_ifrs9_claim_allowed_v323"]) is False

    panel = pd.read_parquet(TABLE_DIR / "paper4_v323_v320_cashflow_proxy_panel.parquet")
    assert len(panel) == 6156
    assert panel["loan_id"].nunique() == 171
    assert panel["month_index"].nunique() == 36
    assert panel["scenario"].nunique() == 1
    assert panel["proxy_source_v323"].value_counts().sort_index().to_dict() == {
        "imputed_grade_score": 36,
        "imputed_grade_score_period": 2808,
        "observed_v47_proxy": 3312,
    }

    source = _read_csv("paper4_v323_v320_cashflow_proxy_source_summary.csv")
    source_map = {str(row["proxy_source_v323"]): row for _, row in source.iterrows()}
    assert int(source_map["imputed_grade_score"]["loan_rows_v323"]) == 1
    assert int(source_map["imputed_grade_score"]["panel_rows_v323"]) == 36
    assert int(source_map["imputed_grade_score_period"]["loan_rows_v323"]) == 78
    assert int(source_map["imputed_grade_score_period"]["panel_rows_v323"]) == 2808
    assert int(source_map["observed_v47_proxy"]["loan_rows_v323"]) == 92
    assert int(source_map["observed_v47_proxy"]["panel_rows_v323"]) == 3312
    assert float(source_map["observed_v47_proxy"]["net_cash_proxy_total_v323"]) == pytest.approx(
        359663.2754513889
    )

    online = _read_csv("paper4_v323_v320_online_temporal_summary.csv")
    assert online["holdout_periods_v323"].eq(3).all()
    assert online["source_cell_rows_v323"].eq(3).all()
    assert np.allclose(online["worst_source_coverage_v323"], 0.9285714285714286)
    assert np.allclose(online["worst_policy_period_coverage_v323"], 0.9285714285714286)
    assert np.allclose(online["max_policy_period_width_v323"], 0.3183280895629744)
    assert online["source_gate_pass_rows_v323"].eq(3).all()
    assert online["policy_gate_pass_rows_v323"].eq(3).all()
    assert online["width_gate_pass_rows_v323"].eq(3).all()
    assert online["all_internal_gates_pass_v323"].astype(bool).all()
    assert not online["external_holdout_available_v323"].astype(bool).any()
    assert not online["strict_live_claim_allowed_v323"].astype(bool).any()

    cells = _read_csv("paper4_v323_v320_online_temporal_cells.csv")
    assert cells.groupby("source_family")["holdout_period_v323"].nunique().eq(3).all()
    assert cells["source_gate80_v323"].astype(bool).all()
    assert cells["policy_gate90_v323"].astype(bool).all()
    assert cells["width_gate95_v323"].astype(bool).all()

    blockers = _read_csv("paper4_v323_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v323"], blockers["blocking_v323"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v323"], blockers["evidence_count_v323"], strict=False)
    )
    assert bool(blocker_map["cashflow_proxy_imputation_required"]) is True
    assert int(evidence_map["cashflow_proxy_imputation_required"]) == 79
    assert bool(blocker_map["proxy_coverage_regression_vs_v316"]) is True
    assert int(evidence_map["proxy_coverage_regression_vs_v316"]) == 6
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert bool(blocker_map["dynamic_live_replay_missing_after_v322"]) is True
    assert bool(blocker_map["global_branch_price_certificate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v323_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v323_v320_cashflow_proxy_panel_executed"]) is True
    assert bool(claim_map["v323_v320_online_temporal_rerun_executed"]) is True
    assert bool(claim_map["v323_v320_internal_online_all_gates"]) is True
    assert bool(claim_map["v323_v320_proxy_coverage_regression_vs_v316_documented"]) is True
    assert bool(claim_map["v323_contractual_ifrs9"]) is False
    assert bool(claim_map["v323_strict_live_online_deployability"]) is False
    assert bool(claim_map["v323_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v323 v320-specific cashflow proxy panel."])
    assert bool(boundary_map["Paper 4 has a v323 v320 temporal online rerun."])
    assert bool(boundary_map["v323 documents v320 proxy coverage regression versus v316."])
    assert bool(boundary_map["v323 implements contractual IFRS9 for v320."]) is False
    assert bool(boundary_map["v323 authorizes live online deployability for v320."]) is False
    assert (
        bool(boundary_map["v323 resolves v320 global or matched-period dynamic blockers."]) is False
    )
    assert bool(boundary_map["v323 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v323_rows = backlog.loc[backlog["last_wave"].eq("v323")]
    assert len(v323_rows) == 1
    backlog_row = v323_rows.iloc[0]
    assert backlog_row["status"] == "v320_cashflow_proxy_and_temporal_online_rerun_executed"
    assert backlog_row["next_artifact"] == (
        "paper4_v324_v320_proxy_gap_repair_or_branch_price_protocol.csv"
    )
    assert backlog_row["execution_result"] == (
        "cashflow_proxy_complete_with_79_imputed_rows_online_temporal_not_live"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v323: v320 Cashflow / Online / IFRS9 Gate" in notebook
    assert "Observed proxy loan rows: `92`" in notebook
    assert "Imputed proxy loan rows: `79`" in notebook
    assert "Observed proxy delta vs v316:\n  `-6`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v324_v320_proxy_gap_repair_documents_tradeoff() -> None:
    status = _read_json("paper4_v324_status.json")

    assert status["phase"] == "v324_v320_proxy_gap_repair_or_branch_price_protocol"
    assert status["schema_version"] == "2026-05-16.324"
    assert status["base_version_v324"] == 320
    assert status["reference_version_v324"] == 316
    assert status["gate_version_v324"] == 323
    assert status["pool_rows_v324"] == 1361
    assert status["observed_omitted_candidate_rows_v324"] == 1190
    assert status["candidate_pool_limit_per_period_v324"] == 750
    assert status["strict_v320_preserving_repair_feasible_v324"] is False
    assert status["strict_milp_status_v324"] == 2
    assert status["relaxed_repair_feasible_v324"] is True
    assert status["relaxed_selected_rows_v324"] == 171
    assert status["relaxed_added_rows_v324"] == 8
    assert status["relaxed_dropped_rows_v324"] == 8
    assert status["relaxed_observed_proxy_rows_v324"] == 100
    assert status["relaxed_missing_proxy_rows_v324"] == 71
    assert status["relaxed_observed_delta_vs_v320_v324"] == 8
    assert status["relaxed_missing_delta_vs_v320_v324"] == -8
    assert status["relaxed_objective_return_v324"] == pytest.approx(4423.147879398481)
    assert status["relaxed_delta_return_vs_v320_v324"] == pytest.approx(-10.127369358319811)
    assert status["relaxed_delta_return_vs_v316_v324"] == pytest.approx(3.389887813702444)
    assert status["relaxed_cvar90_v324"] == pytest.approx(96592.54051502704)
    assert status["relaxed_delta_cvar90_vs_v320_v324"] == pytest.approx(-235.37285766325658)
    assert status["relaxed_delta_cvar90_vs_v316_v324"] == pytest.approx(-391.51024486227834)
    assert status["source_cap_violations_v324"] == 0
    assert status["min_source_slack_v324"] == pytest.approx(4.597308456477656e-06)
    assert status["post_v324_repricing_required_v324"] is True
    assert status["strict_repair_or_champion_claim_allowed_v324"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v324"] is False
    assert status["working_champion_claim_allowed_v324"] is False
    assert status["paper1_promotion_allowed_v324"] is False
    assert status["paper4_working_champion_changed_v324"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v324"] == 6
    assert status["claim_matrix_rows_v324"] == 6
    assert status["next_artifact_v324"] == (
        "paper4_v325_v324_relaxed_reprice_or_dual_bound_gate.csv"
    )

    summary = _read_csv("paper4_v324_v320_proxy_gap_repair_or_branch_price_protocol.csv")
    row = summary.iloc[0]
    assert row["gate_id_v324"] == "v324_v320_proxy_gap_repair_or_branch_price_protocol"
    assert int(row["pool_rows_v324"]) == 1361
    assert bool(row["strict_v320_preserving_repair_feasible_v324"]) is False
    assert bool(row["relaxed_repair_feasible_v324"]) is True
    assert int(row["relaxed_observed_proxy_rows_v324"]) == 100
    assert int(row["relaxed_missing_proxy_rows_v324"]) == 71
    assert float(row["relaxed_delta_return_vs_v320_v324"]) == pytest.approx(-10.127369358319811)
    assert float(row["relaxed_delta_cvar90_vs_v320_v324"]) == pytest.approx(-235.37285766325658)
    assert bool(row["strict_repair_or_champion_claim_allowed_v324"]) is False
    assert "strict v320-preserving repair is infeasible" in str(row["claim_boundary_v324"])

    pool = _read_csv("paper4_v324_proxy_gap_pool_summary.csv")
    pool_row = pool.iloc[0]
    assert int(pool_row["pool_rows_v324"]) == 1361
    assert int(pool_row["selected_base_rows_v324"]) == 171
    assert int(pool_row["observed_omitted_candidate_rows_v324"]) == 1190
    assert int(pool_row["total_observed_omitted_rows_v324"]) == 1556
    assert bool(pool_row["pool_limit_binding_v324"]) is True

    tiers = _read_csv("paper4_v324_proxy_gap_tier_summary.csv")
    tier_map = {str(tier_id): group.iloc[0] for tier_id, group in tiers.groupby("tier_id_v324")}
    strict = tier_map["strict_v320_return_v320_cvar"]
    relaxed = tier_map["relaxed_v316_return_v320_cvar"]
    assert bool(strict["solver_success_v324"]) is False
    assert int(strict["milp_status_v324"]) == 2
    assert "Infeasible" in str(strict["milp_message_v324"])
    assert bool(relaxed["solver_success_v324"]) is True
    assert int(relaxed["selected_rows_v324"]) == 171
    assert float(relaxed["portfolio_exposure_v324"]) == pytest.approx(843775.0)
    assert float(relaxed["objective_return_v324"]) == pytest.approx(4423.147879398481)
    assert float(relaxed["scenario_loss_cvar90_v324"]) == pytest.approx(96592.54051502704)
    assert int(relaxed["observed_proxy_rows_v324"]) == 100
    assert int(relaxed["missing_proxy_rows_v324"]) == 71
    assert json.loads(relaxed["period_distribution_v324"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v324_relaxed_proxy_gap_allocations.parquet")
    assert len(allocations) == 171
    assert allocations["loan_id"].nunique() == 171
    assert int(allocations["observed_v47_proxy_v324"].sum()) == 100
    assert int((~allocations["observed_v47_proxy_v324"]).sum()) == 71
    assert allocations["portfolio_label_v324"].eq("relaxed_proxy_gap_repair_candidate").all()

    actions = _read_csv("paper4_v324_relaxed_proxy_gap_actions.csv")
    assert actions["action_v324"].value_counts().to_dict() == {
        "add_observed_candidate": 8,
        "drop_v320_selected": 8,
    }
    assert (
        actions.loc[actions["action_v324"].eq("add_observed_candidate"), "observed_v47_proxy_v324"]
        .astype(bool)
        .all()
    )
    assert (
        not actions.loc[actions["action_v324"].eq("drop_v320_selected"), "observed_v47_proxy_v324"]
        .astype(bool)
        .any()
    )

    source = _read_csv("paper4_v324_relaxed_proxy_gap_source_summary.csv")
    assert not source["source_cap_violated_v324"].astype(bool).any()
    grade_a = source.loc[
        source["source_family"].eq("grade") & source["source_id"].astype(str).eq("A")
    ].iloc[0]
    assert float(grade_a["source_exposure_v324"]) == pytest.approx(719850.0)
    assert float(grade_a["source_share_v324"]) == pytest.approx(0.8531302776214038)
    assert float(grade_a["source_slack_v324"]) == pytest.approx(4.597308456477656e-06)

    blockers = _read_csv("paper4_v324_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v324"], blockers["blocking_v324"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v324"], blockers["evidence_count_v324"], strict=False)
    )
    assert bool(blocker_map["strict_v320_preserving_proxy_repair_infeasible"]) is True
    assert bool(blocker_map["relaxed_repair_requires_return_concession"]) is True
    assert float(evidence_map["relaxed_repair_requires_return_concession"]) == pytest.approx(
        10.127369358319811
    )
    assert bool(blocker_map["post_v324_reprice_missing"]) is True
    assert bool(blocker_map["bounded_pool_not_full_universe"]) is True
    assert int(evidence_map["bounded_pool_not_full_universe"]) == 1190
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v324_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v324_strict_v320_proxy_gap_repair_found"]) is False
    assert bool(claim_map["v324_relaxed_proxy_gap_repair_found"]) is True
    assert bool(claim_map["v324_relaxed_observed_proxy_rows_reach_100"]) is True
    assert bool(claim_map["v324_preserves_full_v320_return"]) is False
    assert bool(claim_map["v324_full_universe_integer_optimality"]) is False
    assert bool(claim_map["v324_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["v324 tests strict v320-preserving proxy-gap repair."])
    assert bool(boundary_map["v324 finds a relaxed proxy-gap repair with 100 observed rows."])
    assert (
        bool(boundary_map["v324 preserves the full v320 return while repairing proxy coverage."])
        is False
    )
    assert bool(boundary_map["v324 proves full-universe branch-price optimality."]) is False
    assert bool(boundary_map["v324 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v324_rows = backlog.loc[backlog["last_wave"].eq("v324")]
    assert len(v324_rows) == 1
    backlog_row = v324_rows.iloc[0]
    assert backlog_row["status"] == "strict_v320_proxy_repair_infeasible_relaxed_repair_found"
    assert backlog_row["next_artifact"] == (
        "paper4_v325_v324_relaxed_reprice_or_dual_bound_gate.csv"
    )
    assert backlog_row["execution_result"] == (
        "coverage_improves_to_100_observed_only_with_return_concession"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v324: v320 Proxy-Gap Repair / Branch-Price Protocol" in notebook
    assert "Strict v320-preserving repair feasible:\n  `False`" in notebook
    assert "Relaxed observed proxy rows:\n  `100`" in notebook
    assert "Relaxed return delta vs v320:\n  `-10.127369358319811`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v325_post_v324_reprice_finds_next_repair_signal() -> None:
    status = _read_json("paper4_v325_status.json")

    assert status["phase"] == "v325_post_v324_reprice"
    assert status["schema_version"] == "2026-05-16.325"
    assert status["base_repair_version_v325"] == 324
    assert status["previous_reprice_version_v325"] == 324
    assert status["selected_rows_v325"] == 171
    assert status["base_selected_rows_v325"] == 171
    assert status["cardinality_restored_v325"] is True
    assert status["candidate_add_rows_v325"] == 276698
    assert status["total_pair_rows_screened_v325"] == 47315358
    assert status["return_improving_pair_rows_v325"] == 1452243
    assert status["budget_return_feasible_pair_rows_v325"] == 736211
    assert status["source_prefilter_pair_rows_v325"] == 12361
    assert status["source_exact_pair_rows_v325"] == 17
    assert status["cvar_feasible_pair_rows_v325"] == 4
    assert status["one_swap_improving_rows_v325"] == 4
    assert status["best_one_swap_return_delta_v325"] == pytest.approx(3.749478320776632)
    assert status["best_one_swap_cvar90_after_v325"] == pytest.approx(96658.20463333692)
    assert status["best_feasible_one_swap_return_delta_v325"] == pytest.approx(2.61210260364512)
    assert status["best_feasible_one_swap_cvar90_after_v325"] == pytest.approx(96589.8525904196)
    assert status["current_missing_v47_proxy_rows_v325"] == 71
    assert status["best_feasible_delta_missing_v47_proxy_rows_v325"] == 1
    assert status["current_exposure_v325"] == pytest.approx(843775.0)
    assert status["current_objective_return_v325"] == pytest.approx(4423.147879398481)
    assert status["current_loss_mean_v325"] == pytest.approx(60268.842598930736)
    assert status["current_cvar90_v325"] == pytest.approx(96592.54051502704)
    assert status["post_v324_one_swap_local_optimality_cleared_v325"] is False
    assert status["dynamic_gate_ready_v325"] is False
    assert status["working_champion_claim_allowed_v325"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v325"] is False
    assert status["paper1_promotion_allowed_v325"] is False
    assert status["paper4_working_champion_changed_v325"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v325"] == "paper4_v326_apply_next_post_v324_swap.csv"

    summary = _read_csv("paper4_v325_post_v324_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v324_relaxed_proxy_gap_repair_candidate"
    assert row["regime_v325"] == "post_v324_relaxed_proxy_gap_repair_candidate"
    assert int(row["source_exact_pair_rows_v325"]) == 17
    assert int(row["cvar_feasible_pair_rows_v325"]) == 4
    assert int(row["one_swap_improving_rows_v325"]) == 4
    assert bool(row["post_v324_one_swap_local_optimality_cleared_v325"]) is False
    assert bool(row["dynamic_gate_ready_v325"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v325"])

    stage = _read_csv("paper4_v325_post_v324_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v325"], stage["pair_rows_v325"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1452243,
        "budget_return_feasible": 736211,
        "source_prefilter_feasible": 12361,
        "source_exact_feasible": 17,
        "cvar_feasible_improving": 4,
    }

    top = _read_csv("paper4_v325_post_v324_one_swap_top_candidates.csv")
    assert len(top) == 17
    feasible = top.loc[top["one_swap_improves_return_v325"].astype(bool)].copy()
    assert len(feasible) == 4
    best_feasible = feasible.sort_values("return_delta_v325", ascending=False).iloc[0]
    assert str(best_feasible["added_loan_id_v325"]) == "148128009"
    assert str(best_feasible["dropped_loan_id_v325"]) == "151566947"
    assert float(best_feasible["return_delta_v325"]) == pytest.approx(2.61210260364512)
    assert float(best_feasible["cvar90_after_swap_v325"]) == pytest.approx(96589.8525904196)
    assert bool(best_feasible["cvar_swap_feasible_v325"]) is True
    assert bool(best_feasible["one_swap_improves_return_v325"]) is True
    assert bool(best_feasible["added_observed_v47_proxy_v325"]) is False
    assert bool(best_feasible["dropped_observed_v47_proxy_v325"]) is True
    assert int(best_feasible["delta_missing_v47_proxy_rows_v325"]) == 1

    blockers = _read_csv("paper4_v325_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v325"], blockers["blocking_v325"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v325"], blockers["evidence_count_v325"], strict=False)
    )
    assert bool(blocker_map["post_v324_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v324_one_swap_improvement_found"]) == 4
    assert bool(blocker_map["post_v324_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 71
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v325_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v325_post_v324_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v325_post_v324_one_swap_local_optimality"]) is False
    assert bool(claim_map["v325_dynamic_gate_ready"]) is False
    assert bool(claim_map["v325_working_champion"]) is False
    assert bool(claim_map["v325_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v325 post-v324 one-swap repricing gate."])
    assert bool(boundary_map["v325 clears post-v324 one-swap local optimality."]) is False
    assert bool(boundary_map["v325 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v325 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v325 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v325_rows = backlog.loc[backlog["last_wave"].eq("v325")]
    assert len(v325_rows) == 1
    backlog_row = v325_rows.iloc[0]
    assert backlog_row["status"] == "post_v324_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v326_apply_next_post_v324_swap.csv"
    assert backlog_row["execution_result"] == "post_v324_one_swap_reprice_found_improvements"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v325: Post-v324 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `4`" in notebook
    assert "Best CVaR-feasible return delta: `2.61210260364512`" in notebook
    assert "Post-v324 one-swap local optimality cleared:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v326_applies_best_post_v324_swap_without_promotion() -> None:
    status = _read_json("paper4_v326_status.json")

    assert status["phase"] == "v326_apply_next_post_v324_swap"
    assert status["schema_version"] == "2026-05-16.326"
    assert status["base_version_v326"] == 324
    assert status["reprice_version_v326"] == 325
    assert status["applied_added_loan_id_v326"] == "148128009"
    assert status["applied_dropped_loan_id_v326"] == "151566947"
    assert status["selected_rows_v326"] == 171
    assert status["cardinality_restored_v326"] is True
    assert status["portfolio_exposure_v326"] == pytest.approx(843775.0)
    assert status["objective_return_v326"] == pytest.approx(4425.759982002126)
    assert status["delta_return_vs_v324_v326"] == pytest.approx(2.612102603645326)
    assert status["delta_return_vs_v320_v326"] == pytest.approx(-7.515266754674485)
    assert status["delta_return_vs_v316_v326"] == pytest.approx(6.00199041734777)
    assert status["scenario_loss_mean_v326"] == pytest.approx(60266.01786850243)
    assert status["scenario_loss_cvar90_v326"] == pytest.approx(96589.8525904196)
    assert status["delta_cvar90_vs_v324_v326"] == pytest.approx(-2.6879246074386174)
    assert status["delta_cvar90_vs_v320_v326"] == pytest.approx(-238.0607822706952)
    assert status["delta_cvar90_vs_v316_v326"] == pytest.approx(-394.19816946971696)
    assert status["observed_proxy_rows_v326"] == 99
    assert status["missing_proxy_rows_v326"] == 72
    assert status["observed_proxy_delta_vs_v324_v326"] == -1
    assert status["missing_proxy_delta_vs_v324_v326"] == 1
    assert status["source_cap_violations_v326"] == 0
    assert status["min_source_slack_v326"] == pytest.approx(4.597308456477656e-06)
    assert status["post_v326_repricing_required_v326"] is True
    assert status["working_champion_claim_allowed_v326"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v326"] is False
    assert status["paper1_promotion_allowed_v326"] is False
    assert status["paper4_working_champion_changed_v326"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v326"] == "paper4_v327_post_v326_reprice.csv"

    summary = _read_csv("paper4_v326_apply_next_post_v324_swap.csv")
    row = summary.iloc[0]
    assert row["gate_id_v326"] == "v326_apply_next_post_v324_swap"
    assert str(row["applied_added_loan_id_v326"]) == "148128009"
    assert str(row["applied_dropped_loan_id_v326"]) == "151566947"
    assert bool(row["cardinality_restored_v326"]) is True
    assert json.loads(row["period_distribution_v326"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert int(row["observed_proxy_rows_v326"]) == 99
    assert int(row["missing_proxy_rows_v326"]) == 72
    assert bool(row["post_v326_repricing_required_v326"]) is True
    assert bool(row["working_champion_claim_allowed_v326"]) is False
    assert "post-v326 repricing" in str(row["claim_boundary_v326"])

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v326_post_v324_swap_allocations.parquet")
    assert len(allocations) == 171
    assert allocations["loan_id"].nunique() == 171
    assert int(allocations["observed_v47_proxy_v326"].sum()) == 99
    assert int((~allocations["observed_v47_proxy_v326"]).sum()) == 72
    assert allocations["portfolio_label_v326"].eq("post_v324_best_feasible_swap_candidate").all()
    assert "148128009" in set(allocations["loan_id"].astype(str))
    assert "151566947" not in set(allocations["loan_id"].astype(str))

    actions = _read_csv("paper4_v326_post_v324_swap_actions.csv")
    action_map = {str(row["action_v326"]): row for _, row in actions.iterrows()}
    assert str(action_map["add_v325_best_feasible"]["loan_id"]) == "148128009"
    assert bool(action_map["add_v325_best_feasible"]["observed_v47_proxy_v326"]) is False
    assert str(action_map["drop_v325_best_feasible"]["loan_id"]) == "151566947"
    assert bool(action_map["drop_v325_best_feasible"]["observed_v47_proxy_v326"]) is True

    source = _read_csv("paper4_v326_post_v324_swap_source_summary.csv")
    assert not source["source_cap_violated_v326"].astype(bool).any()
    grade_a = source.loc[
        source["source_family"].eq("grade") & source["source_id"].astype(str).eq("A")
    ].iloc[0]
    assert float(grade_a["source_exposure_v326"]) == pytest.approx(719850.0)
    assert float(grade_a["source_share_v326"]) == pytest.approx(0.8531302776214038)
    assert float(grade_a["source_slack_v326"]) == pytest.approx(4.597308456477656e-06)

    blockers = _read_csv("paper4_v326_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v326"], blockers["blocking_v326"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v326"], blockers["evidence_count_v326"], strict=False)
    )
    assert bool(blocker_map["post_v326_reprice_missing"]) is True
    assert bool(blocker_map["proxy_coverage_worsens_vs_v324"]) is True
    assert int(evidence_map["proxy_coverage_worsens_vs_v324"]) == 1
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v326_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v326_best_v325_swap_applied"]) is True
    assert bool(claim_map["v326_improves_return_and_cvar_vs_v324"]) is True
    assert bool(claim_map["v326_preserves_v324_proxy_coverage"]) is False
    assert bool(claim_map["v326_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["v326 applies the best v325 post-v324 feasible one-swap."])
    assert bool(boundary_map["v326 improves return and CVaR versus the v324 relaxed repair."])
    assert bool(boundary_map["v326 preserves v324 relaxed proxy coverage exactly."]) is False
    assert bool(boundary_map["v326 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v326 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v326_rows = backlog.loc[backlog["last_wave"].eq("v326")]
    assert len(v326_rows) == 1
    backlog_row = v326_rows.iloc[0]
    assert backlog_row["status"] == "post_v324_best_feasible_swap_applied_requires_reprice"
    assert backlog_row["next_artifact"] == "paper4_v327_post_v326_reprice.csv"
    assert backlog_row["execution_result"] == "return_and_cvar_improve_proxy_missing_worsens_by_one"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v326: Apply Next Post-v324 Swap" in notebook
    assert "Applied added loan: `148128009`" in notebook
    assert "Missing proxy delta vs v324:\n  `1`" in notebook
    assert "Post-v326 repricing required:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v327_post_v326_reprice_finds_next_repair_signal() -> None:
    status = _read_json("paper4_v327_status.json")

    assert status["phase"] == "v327_post_v326_reprice"
    assert status["schema_version"] == "2026-05-16.327"
    assert status["base_repair_version_v327"] == 326
    assert status["previous_reprice_version_v327"] == 326
    assert status["selected_rows_v327"] == 171
    assert status["base_selected_rows_v327"] == 171
    assert status["cardinality_restored_v327"] is True
    assert status["candidate_add_rows_v327"] == 276698
    assert status["total_pair_rows_screened_v327"] == 47315358
    assert status["return_improving_pair_rows_v327"] == 1452166
    assert status["budget_return_feasible_pair_rows_v327"] == 736161
    assert status["source_prefilter_pair_rows_v327"] == 12358
    assert status["source_exact_pair_rows_v327"] == 14
    assert status["cvar_feasible_pair_rows_v327"] == 3
    assert status["one_swap_improving_rows_v327"] == 3
    assert status["best_one_swap_return_delta_v327"] == pytest.approx(2.6026418487841774)
    assert status["best_one_swap_cvar90_after_v327"] == pytest.approx(96594.71511932419)
    assert status["best_feasible_one_swap_return_delta_v327"] == pytest.approx(1.8728137199666328)
    assert status["best_feasible_one_swap_cvar90_after_v327"] == pytest.approx(96579.4999137334)
    assert status["current_missing_v47_proxy_rows_v327"] == 72
    assert status["best_feasible_delta_missing_v47_proxy_rows_v327"] == 1
    assert status["current_exposure_v327"] == pytest.approx(843775.0)
    assert status["current_objective_return_v327"] == pytest.approx(4425.759982002126)
    assert status["current_loss_mean_v327"] == pytest.approx(60266.01786850243)
    assert status["current_cvar90_v327"] == pytest.approx(96589.8525904196)
    assert status["post_v326_one_swap_local_optimality_cleared_v327"] is False
    assert status["dynamic_gate_ready_v327"] is False
    assert status["working_champion_claim_allowed_v327"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v327"] is False
    assert status["paper1_promotion_allowed_v327"] is False
    assert status["paper4_working_champion_changed_v327"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v327"] == "paper4_v328_apply_next_post_v326_swap.csv"

    summary = _read_csv("paper4_v327_post_v326_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v326_post_v324_best_feasible_swap_candidate"
    assert row["regime_v327"] == "post_v326_best_feasible_swap_candidate"
    assert int(row["source_exact_pair_rows_v327"]) == 14
    assert int(row["cvar_feasible_pair_rows_v327"]) == 3
    assert int(row["one_swap_improving_rows_v327"]) == 3
    assert bool(row["post_v326_one_swap_local_optimality_cleared_v327"]) is False
    assert bool(row["dynamic_gate_ready_v327"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v327"])

    stage = _read_csv("paper4_v327_post_v326_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v327"], stage["pair_rows_v327"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1452166,
        "budget_return_feasible": 736161,
        "source_prefilter_feasible": 12358,
        "source_exact_feasible": 14,
        "cvar_feasible_improving": 3,
    }

    top = _read_csv("paper4_v327_post_v326_one_swap_top_candidates.csv")
    assert len(top) == 14
    feasible = top.loc[top["one_swap_improves_return_v327"].astype(bool)].copy()
    assert len(feasible) == 3
    best_feasible = feasible.sort_values("return_delta_v327", ascending=False).iloc[0]
    assert str(best_feasible["added_loan_id_v327"]) == "127648167"
    assert str(best_feasible["dropped_loan_id_v327"]) == "136360475"
    assert float(best_feasible["return_delta_v327"]) == pytest.approx(1.8728137199666328)
    assert float(best_feasible["cvar90_after_swap_v327"]) == pytest.approx(96579.4999137334)
    assert bool(best_feasible["cvar_swap_feasible_v327"]) is True
    assert bool(best_feasible["one_swap_improves_return_v327"]) is True
    assert bool(best_feasible["added_observed_v47_proxy_v327"]) is False
    assert bool(best_feasible["dropped_observed_v47_proxy_v327"]) is True
    assert int(best_feasible["delta_missing_v47_proxy_rows_v327"]) == 1

    best_source_exact = top.loc[top["return_delta_v327"].idxmax()]
    assert str(best_source_exact["added_loan_id_v327"]) == "148017643"
    assert str(best_source_exact["dropped_loan_id_v327"]) == "136360475"
    assert float(best_source_exact["return_delta_v327"]) == pytest.approx(2.6026418487841774)
    assert float(best_source_exact["cvar90_after_swap_v327"]) == pytest.approx(96594.71511932419)
    assert bool(best_source_exact["cvar_swap_feasible_v327"]) is False
    assert bool(best_source_exact["one_swap_improves_return_v327"]) is False

    blockers = _read_csv("paper4_v327_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v327"], blockers["blocking_v327"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v327"], blockers["evidence_count_v327"], strict=False)
    )
    assert bool(blocker_map["post_v326_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v326_one_swap_improvement_found"]) == 3
    assert bool(blocker_map["post_v326_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 72
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v327_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v327_post_v326_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v327_post_v326_one_swap_local_optimality"]) is False
    assert bool(claim_map["v327_dynamic_gate_ready"]) is False
    assert bool(claim_map["v327_working_champion"]) is False
    assert bool(claim_map["v327_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v327 post-v326 one-swap repricing gate."])
    assert bool(boundary_map["v327 clears post-v326 one-swap local optimality."]) is False
    assert bool(boundary_map["v327 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v327 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v327 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v327_rows = backlog.loc[backlog["last_wave"].eq("v327")]
    assert len(v327_rows) == 1
    backlog_row = v327_rows.iloc[0]
    assert backlog_row["status"] == "post_v326_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v328_apply_next_post_v326_swap.csv"
    assert backlog_row["execution_result"] == "post_v326_one_swap_reprice_found_improvements"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v327: Post-v326 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `3`" in notebook
    assert "Best CVaR-feasible return delta: `1.8728137199666328`" in notebook
    assert "Post-v326 one-swap local optimality cleared:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v328_applies_best_post_v326_swap_without_promotion() -> None:
    status = _read_json("paper4_v328_status.json")

    assert status["phase"] == "v328_apply_next_post_v326_swap"
    assert status["schema_version"] == "2026-05-16.328"
    assert status["base_version_v328"] == 326
    assert status["reprice_version_v328"] == 327
    assert status["applied_added_loan_id_v328"] == "127648167"
    assert status["applied_dropped_loan_id_v328"] == "136360475"
    assert status["selected_rows_v328"] == 171
    assert status["cardinality_restored_v328"] is True
    assert status["portfolio_exposure_v328"] == pytest.approx(843775.0)
    assert status["objective_return_v328"] == pytest.approx(4427.632795722093)
    assert status["delta_return_vs_v326_v328"] == pytest.approx(1.8728137199668708)
    assert status["delta_return_vs_v320_v328"] == pytest.approx(-5.642453034707614)
    assert status["delta_return_vs_v316_v328"] == pytest.approx(7.874804137314641)
    assert status["scenario_loss_mean_v328"] == pytest.approx(60264.61449916206)
    assert status["scenario_loss_cvar90_v328"] == pytest.approx(96579.4999137334)
    assert status["delta_cvar90_vs_v326_v328"] == pytest.approx(-10.352676686205086)
    assert status["delta_cvar90_vs_v320_v328"] == pytest.approx(-248.41345895690029)
    assert status["delta_cvar90_vs_v316_v328"] == pytest.approx(-404.55084615592204)
    assert status["observed_proxy_rows_v328"] == 98
    assert status["missing_proxy_rows_v328"] == 73
    assert status["observed_proxy_delta_vs_v326_v328"] == -1
    assert status["missing_proxy_delta_vs_v326_v328"] == 1
    assert status["source_cap_violations_v328"] == 0
    assert status["min_source_slack_v328"] == pytest.approx(4.597308456477656e-06)
    assert status["post_v328_repricing_required_v328"] is True
    assert status["working_champion_claim_allowed_v328"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v328"] is False
    assert status["paper1_promotion_allowed_v328"] is False
    assert status["paper4_working_champion_changed_v328"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v328"] == "paper4_v329_post_v328_reprice.csv"

    summary = _read_csv("paper4_v328_apply_next_post_v326_swap.csv")
    row = summary.iloc[0]
    assert row["gate_id_v328"] == "v328_apply_next_post_v326_swap"
    assert str(row["applied_added_loan_id_v328"]) == "127648167"
    assert str(row["applied_dropped_loan_id_v328"]) == "136360475"
    assert bool(row["cardinality_restored_v328"]) is True
    assert json.loads(row["period_distribution_v328"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert int(row["observed_proxy_rows_v328"]) == 98
    assert int(row["missing_proxy_rows_v328"]) == 73
    assert bool(row["post_v328_repricing_required_v328"]) is True
    assert bool(row["working_champion_claim_allowed_v328"]) is False
    assert "post-v328 repricing" in str(row["claim_boundary_v328"])

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v328_post_v326_swap_allocations.parquet")
    assert len(allocations) == 171
    assert allocations["loan_id"].nunique() == 171
    assert int(allocations["observed_v47_proxy_v328"].sum()) == 98
    assert int((~allocations["observed_v47_proxy_v328"]).sum()) == 73
    assert allocations["portfolio_label_v328"].eq("post_v326_best_feasible_swap_candidate").all()
    assert "127648167" in set(allocations["loan_id"].astype(str))
    assert "136360475" not in set(allocations["loan_id"].astype(str))

    actions = _read_csv("paper4_v328_post_v326_swap_actions.csv")
    action_map = {str(row["action_v328"]): row for _, row in actions.iterrows()}
    assert str(action_map["add_v327_best_feasible"]["loan_id"]) == "127648167"
    assert bool(action_map["add_v327_best_feasible"]["observed_v47_proxy_v328"]) is False
    assert str(action_map["drop_v327_best_feasible"]["loan_id"]) == "136360475"
    assert bool(action_map["drop_v327_best_feasible"]["observed_v47_proxy_v328"]) is True

    source = _read_csv("paper4_v328_post_v326_swap_source_summary.csv")
    assert not source["source_cap_violated_v328"].astype(bool).any()
    grade_a = source.loc[
        source["source_family"].eq("grade") & source["source_id"].astype(str).eq("A")
    ].iloc[0]
    assert float(grade_a["source_exposure_v328"]) == pytest.approx(719850.0)
    assert float(grade_a["source_share_v328"]) == pytest.approx(0.8531302776214038)
    assert float(grade_a["source_slack_v328"]) == pytest.approx(4.597308456477656e-06)

    blockers = _read_csv("paper4_v328_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v328"], blockers["blocking_v328"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v328"], blockers["evidence_count_v328"], strict=False)
    )
    assert bool(blocker_map["post_v328_reprice_missing"]) is True
    assert bool(blocker_map["proxy_coverage_worsens_vs_v326"]) is True
    assert int(evidence_map["proxy_coverage_worsens_vs_v326"]) == 1
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v328_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v328_best_v327_swap_applied"]) is True
    assert bool(claim_map["v328_improves_return_and_cvar_vs_v326"]) is True
    assert bool(claim_map["v328_preserves_v326_proxy_coverage"]) is False
    assert bool(claim_map["v328_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["v328 applies the best v327 post-v326 feasible one-swap."])
    assert bool(boundary_map["v328 improves return and CVaR versus the v326 candidate."])
    assert bool(boundary_map["v328 preserves v326 proxy coverage exactly."]) is False
    assert bool(boundary_map["v328 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v328 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v328_rows = backlog.loc[backlog["last_wave"].eq("v328")]
    assert len(v328_rows) == 1
    backlog_row = v328_rows.iloc[0]
    assert backlog_row["status"] == "post_v326_best_feasible_swap_applied_requires_reprice"
    assert backlog_row["next_artifact"] == "paper4_v329_post_v328_reprice.csv"
    assert backlog_row["execution_result"] == "return_and_cvar_improve_proxy_missing_worsens_by_one"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v328: Apply Next Post-v326 Swap" in notebook
    assert "Applied added loan: `127648167`" in notebook
    assert "Missing proxy delta vs v326:\n  `1`" in notebook
    assert "Post-v328 repricing required:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v329_post_v328_reprice_finds_last_repair_signal() -> None:
    status = _read_json("paper4_v329_status.json")

    assert status["phase"] == "v329_post_v328_reprice"
    assert status["schema_version"] == "2026-05-16.329"
    assert status["base_repair_version_v329"] == 328
    assert status["previous_reprice_version_v329"] == 328
    assert status["selected_rows_v329"] == 171
    assert status["base_selected_rows_v329"] == 171
    assert status["cardinality_restored_v329"] is True
    assert status["candidate_add_rows_v329"] == 276698
    assert status["total_pair_rows_screened_v329"] == 47315358
    assert status["return_improving_pair_rows_v329"] == 1452082
    assert status["budget_return_feasible_pair_rows_v329"] == 736108
    assert status["source_prefilter_pair_rows_v329"] == 12353
    assert status["source_exact_pair_rows_v329"] == 9
    assert status["cvar_feasible_pair_rows_v329"] == 1
    assert status["one_swap_improving_rows_v329"] == 1
    assert status["best_one_swap_return_delta_v329"] == pytest.approx(1.4233158764891733)
    assert status["best_one_swap_cvar90_after_v329"] == pytest.approx(96584.31366406834)
    assert status["best_feasible_one_swap_return_delta_v329"] == pytest.approx(0.4981193830302644)
    assert status["best_feasible_one_swap_cvar90_after_v329"] == pytest.approx(96551.59286009285)
    assert status["current_missing_v47_proxy_rows_v329"] == 73
    assert status["best_feasible_delta_missing_v47_proxy_rows_v329"] == 1
    assert status["current_exposure_v329"] == pytest.approx(843775.0)
    assert status["current_objective_return_v329"] == pytest.approx(4427.632795722093)
    assert status["current_loss_mean_v329"] == pytest.approx(60264.61449916206)
    assert status["current_cvar90_v329"] == pytest.approx(96579.4999137334)
    assert status["post_v328_one_swap_local_optimality_cleared_v329"] is False
    assert status["dynamic_gate_ready_v329"] is False
    assert status["working_champion_claim_allowed_v329"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v329"] is False
    assert status["paper1_promotion_allowed_v329"] is False
    assert status["paper4_working_champion_changed_v329"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v329"] == "paper4_v330_apply_next_post_v328_swap.csv"

    summary = _read_csv("paper4_v329_post_v328_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v328_post_v326_best_feasible_swap_candidate"
    assert row["regime_v329"] == "post_v328_best_feasible_swap_candidate"
    assert int(row["source_exact_pair_rows_v329"]) == 9
    assert int(row["cvar_feasible_pair_rows_v329"]) == 1
    assert int(row["one_swap_improving_rows_v329"]) == 1
    assert bool(row["post_v328_one_swap_local_optimality_cleared_v329"]) is False
    assert bool(row["dynamic_gate_ready_v329"]) is False
    assert "repair/reprice loop must continue" in str(row["claim_boundary_v329"])

    stage = _read_csv("paper4_v329_post_v328_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v329"], stage["pair_rows_v329"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1452082,
        "budget_return_feasible": 736108,
        "source_prefilter_feasible": 12353,
        "source_exact_feasible": 9,
        "cvar_feasible_improving": 1,
    }

    top = _read_csv("paper4_v329_post_v328_one_swap_top_candidates.csv")
    assert len(top) == 9
    feasible = top.loc[top["one_swap_improves_return_v329"].astype(bool)].copy()
    assert len(feasible) == 1
    best_feasible = feasible.sort_values("return_delta_v329", ascending=False).iloc[0]
    assert str(best_feasible["added_loan_id_v329"]) == "129082051"
    assert str(best_feasible["dropped_loan_id_v329"]) == "144622469"
    assert float(best_feasible["return_delta_v329"]) == pytest.approx(0.4981193830302644)
    assert float(best_feasible["cvar90_after_swap_v329"]) == pytest.approx(96551.59286009285)
    assert bool(best_feasible["cvar_swap_feasible_v329"]) is True
    assert bool(best_feasible["one_swap_improves_return_v329"]) is True
    assert bool(best_feasible["added_observed_v47_proxy_v329"]) is False
    assert bool(best_feasible["dropped_observed_v47_proxy_v329"]) is True
    assert int(best_feasible["delta_missing_v47_proxy_rows_v329"]) == 1

    best_source_exact = top.loc[top["return_delta_v329"].idxmax()]
    assert str(best_source_exact["added_loan_id_v329"]) == "151825245"
    assert str(best_source_exact["dropped_loan_id_v329"]) == "160128011"
    assert float(best_source_exact["return_delta_v329"]) == pytest.approx(1.4233158764891733)
    assert float(best_source_exact["cvar90_after_swap_v329"]) == pytest.approx(96584.31366406834)
    assert bool(best_source_exact["cvar_swap_feasible_v329"]) is False
    assert bool(best_source_exact["one_swap_improves_return_v329"]) is False

    blockers = _read_csv("paper4_v329_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v329"], blockers["blocking_v329"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v329"], blockers["evidence_count_v329"], strict=False)
    )
    assert bool(blocker_map["post_v328_one_swap_improvement_found"]) is True
    assert int(evidence_map["post_v328_one_swap_improvement_found"]) == 1
    assert bool(blocker_map["post_v328_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 73
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v329_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v329_post_v328_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v329_post_v328_one_swap_local_optimality"]) is False
    assert bool(claim_map["v329_dynamic_gate_ready"]) is False
    assert bool(claim_map["v329_working_champion"]) is False
    assert bool(claim_map["v329_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v329 post-v328 one-swap repricing gate."])
    assert bool(boundary_map["v329 clears post-v328 one-swap local optimality."]) is False
    assert bool(boundary_map["v329 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v329 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v329 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v329_rows = backlog.loc[backlog["last_wave"].eq("v329")]
    assert len(v329_rows) == 1
    backlog_row = v329_rows.iloc[0]
    assert backlog_row["status"] == "post_v328_one_swap_improvement_found"
    assert backlog_row["next_artifact"] == "paper4_v330_apply_next_post_v328_swap.csv"
    assert backlog_row["execution_result"] == "post_v328_one_swap_reprice_found_improvements"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v329: Post-v328 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `1`" in notebook
    assert "Best CVaR-feasible return delta: `0.4981193830302644`" in notebook
    assert "Post-v328 one-swap local optimality cleared:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v330_applies_best_post_v328_swap_without_promotion() -> None:
    status = _read_json("paper4_v330_status.json")

    assert status["phase"] == "v330_apply_next_post_v328_swap"
    assert status["schema_version"] == "2026-05-16.330"
    assert status["base_version_v330"] == 328
    assert status["reprice_version_v330"] == 329
    assert status["applied_added_loan_id_v330"] == "129082051"
    assert status["applied_dropped_loan_id_v330"] == "144622469"
    assert status["selected_rows_v330"] == 171
    assert status["cardinality_restored_v330"] is True
    assert status["portfolio_exposure_v330"] == pytest.approx(843775.0)
    assert status["objective_return_v330"] == pytest.approx(4428.130915105124)
    assert status["delta_return_vs_v328_v330"] == pytest.approx(0.4981193830308257)
    assert status["delta_return_vs_v320_v330"] == pytest.approx(-5.144333651676789)
    assert status["delta_return_vs_v316_v330"] == pytest.approx(8.372923520345466)
    assert status["scenario_loss_mean_v330"] == pytest.approx(60249.51352836411)
    assert status["scenario_loss_cvar90_v330"] == pytest.approx(96551.59286009285)
    assert status["delta_cvar90_vs_v328_v330"] == pytest.approx(-27.90705364054884)
    assert status["delta_cvar90_vs_v320_v330"] == pytest.approx(-276.3205125974491)
    assert status["delta_cvar90_vs_v316_v330"] == pytest.approx(-432.4578997964709)
    assert status["observed_proxy_rows_v330"] == 97
    assert status["missing_proxy_rows_v330"] == 74
    assert status["observed_proxy_delta_vs_v328_v330"] == -1
    assert status["missing_proxy_delta_vs_v328_v330"] == 1
    assert status["source_cap_violations_v330"] == 0
    assert status["min_source_slack_v330"] == pytest.approx(4.597308456477656e-06)
    assert status["post_v330_repricing_required_v330"] is True
    assert status["working_champion_claim_allowed_v330"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v330"] is False
    assert status["paper1_promotion_allowed_v330"] is False
    assert status["paper4_working_champion_changed_v330"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v330"] == "paper4_v331_post_v330_reprice.csv"

    summary = _read_csv("paper4_v330_apply_next_post_v328_swap.csv")
    row = summary.iloc[0]
    assert row["gate_id_v330"] == "v330_apply_next_post_v328_swap"
    assert str(row["applied_added_loan_id_v330"]) == "129082051"
    assert str(row["applied_dropped_loan_id_v330"]) == "144622469"
    assert bool(row["cardinality_restored_v330"]) is True
    assert json.loads(row["period_distribution_v330"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert int(row["observed_proxy_rows_v330"]) == 97
    assert int(row["missing_proxy_rows_v330"]) == 74
    assert bool(row["post_v330_repricing_required_v330"]) is True
    assert bool(row["working_champion_claim_allowed_v330"]) is False
    assert "post-v330 repricing" in str(row["claim_boundary_v330"])

    allocations = pd.read_parquet(TABLE_DIR / "paper4_v330_post_v328_swap_allocations.parquet")
    assert len(allocations) == 171
    assert allocations["loan_id"].nunique() == 171
    assert int(allocations["observed_v47_proxy_v330"].sum()) == 97
    assert int((~allocations["observed_v47_proxy_v330"]).sum()) == 74
    assert allocations["portfolio_label_v330"].eq("post_v328_best_feasible_swap_candidate").all()
    assert "129082051" in set(allocations["loan_id"].astype(str))
    assert "144622469" not in set(allocations["loan_id"].astype(str))

    actions = _read_csv("paper4_v330_post_v328_swap_actions.csv")
    action_map = {str(row["action_v330"]): row for _, row in actions.iterrows()}
    assert str(action_map["add_v329_best_feasible"]["loan_id"]) == "129082051"
    assert bool(action_map["add_v329_best_feasible"]["observed_v47_proxy_v330"]) is False
    assert str(action_map["drop_v329_best_feasible"]["loan_id"]) == "144622469"
    assert bool(action_map["drop_v329_best_feasible"]["observed_v47_proxy_v330"]) is True

    source = _read_csv("paper4_v330_post_v328_swap_source_summary.csv")
    assert not source["source_cap_violated_v330"].astype(bool).any()
    grade_a = source.loc[
        source["source_family"].eq("grade") & source["source_id"].astype(str).eq("A")
    ].iloc[0]
    assert float(grade_a["source_exposure_v330"]) == pytest.approx(719850.0)
    assert float(grade_a["source_share_v330"]) == pytest.approx(0.8531302776214038)
    assert float(grade_a["source_slack_v330"]) == pytest.approx(4.597308456477656e-06)

    blockers = _read_csv("paper4_v330_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v330"], blockers["blocking_v330"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v330"], blockers["evidence_count_v330"], strict=False)
    )
    assert bool(blocker_map["post_v330_reprice_missing"]) is True
    assert bool(blocker_map["proxy_coverage_worsens_vs_v328"]) is True
    assert int(evidence_map["proxy_coverage_worsens_vs_v328"]) == 1
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v330_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v330_best_v329_swap_applied"]) is True
    assert bool(claim_map["v330_improves_return_and_cvar_vs_v328"]) is True
    assert bool(claim_map["v330_preserves_v328_proxy_coverage"]) is False
    assert bool(claim_map["v330_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["v330 applies the best v329 post-v328 feasible one-swap."])
    assert bool(boundary_map["v330 improves return and CVaR versus the v328 candidate."])
    assert bool(boundary_map["v330 preserves v328 proxy coverage exactly."]) is False
    assert bool(boundary_map["v330 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v330 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v330_rows = backlog.loc[backlog["last_wave"].eq("v330")]
    assert len(v330_rows) == 1
    backlog_row = v330_rows.iloc[0]
    assert backlog_row["status"] == "post_v328_best_feasible_swap_applied_requires_reprice"
    assert backlog_row["next_artifact"] == "paper4_v331_post_v330_reprice.csv"
    assert backlog_row["execution_result"] == "return_and_cvar_improve_proxy_missing_worsens_by_one"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v330: Apply Next Post-v328 Swap" in notebook
    assert "Applied added loan: `129082051`" in notebook
    assert "Missing proxy delta vs v328:\n  `1`" in notebook
    assert "Post-v330 repricing required:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v331_post_v330_reprice_clears_local_one_swap_scope() -> None:
    status = _read_json("paper4_v331_status.json")

    assert status["phase"] == "v331_post_v330_reprice"
    assert status["schema_version"] == "2026-05-16.331"
    assert status["base_repair_version_v331"] == 330
    assert status["previous_reprice_version_v331"] == 330
    assert status["selected_rows_v331"] == 171
    assert status["base_selected_rows_v331"] == 171
    assert status["cardinality_restored_v331"] is True
    assert status["candidate_add_rows_v331"] == 276698
    assert status["total_pair_rows_screened_v331"] == 47315358
    assert status["return_improving_pair_rows_v331"] == 1452070
    assert status["budget_return_feasible_pair_rows_v331"] == 736098
    assert status["source_prefilter_pair_rows_v331"] == 12352
    assert status["source_exact_pair_rows_v331"] == 8
    assert status["cvar_feasible_pair_rows_v331"] == 0
    assert status["one_swap_improving_rows_v331"] == 0
    assert status["best_one_swap_return_delta_v331"] == pytest.approx(1.4233158764891733)
    assert status["best_one_swap_cvar90_after_v331"] == pytest.approx(96556.40661042774)
    assert status["best_feasible_one_swap_return_delta_v331"] is None
    assert status["best_feasible_one_swap_cvar90_after_v331"] is None
    assert status["current_missing_v47_proxy_rows_v331"] == 74
    assert status["best_feasible_delta_missing_v47_proxy_rows_v331"] == 0
    assert status["current_exposure_v331"] == pytest.approx(843775.0)
    assert status["current_objective_return_v331"] == pytest.approx(4428.130915105124)
    assert status["current_loss_mean_v331"] == pytest.approx(60249.51352836411)
    assert status["current_cvar90_v331"] == pytest.approx(96551.59286009285)
    assert status["post_v330_one_swap_local_optimality_cleared_v331"] is True
    assert status["dynamic_gate_ready_v331"] is True
    assert status["working_champion_claim_allowed_v331"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v331"] is False
    assert status["paper1_promotion_allowed_v331"] is False
    assert status["paper4_working_champion_changed_v331"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["next_artifact_v331"] == "paper4_v332_dynamic_or_dual_bound_after_v330.csv"

    summary = _read_csv("paper4_v331_post_v330_one_swap_summary.csv")
    row = summary.iloc[0]
    assert row["policy_id"] == "v330_post_v328_best_feasible_swap_candidate"
    assert row["regime_v331"] == "post_v330_best_feasible_swap_candidate"
    assert int(row["source_exact_pair_rows_v331"]) == 8
    assert int(row["cvar_feasible_pair_rows_v331"]) == 0
    assert int(row["one_swap_improving_rows_v331"]) == 0
    assert pd.isna(row["best_feasible_one_swap_return_delta_v331"])
    assert pd.isna(row["best_feasible_one_swap_cvar90_after_v331"])
    assert bool(row["post_v330_one_swap_local_optimality_cleared_v331"]) is True
    assert bool(row["dynamic_gate_ready_v331"]) is True
    assert "post-v330 candidate one-swap screen cleared" in str(row["claim_boundary_v331"])

    stage = _read_csv("paper4_v331_post_v330_one_swap_stage_summary.csv")
    stage_map = dict(zip(stage["stage_v331"], stage["pair_rows_v331"], strict=False))
    assert stage_map == {
        "all_pairs": 47315358,
        "return_improving": 1452070,
        "budget_return_feasible": 736098,
        "source_prefilter_feasible": 12352,
        "source_exact_feasible": 8,
        "cvar_feasible_improving": 0,
    }

    top = _read_csv("paper4_v331_post_v330_one_swap_top_candidates.csv")
    assert len(top) == 8
    assert not top["one_swap_improves_return_v331"].astype(bool).any()
    assert not top["cvar_swap_feasible_v331"].astype(bool).any()
    best_source_exact = top.loc[top["return_delta_v331"].idxmax()]
    assert str(best_source_exact["added_loan_id_v331"]) == "151825245"
    assert str(best_source_exact["dropped_loan_id_v331"]) == "160128011"
    assert float(best_source_exact["return_delta_v331"]) == pytest.approx(1.4233158764891733)
    assert float(best_source_exact["cvar90_after_swap_v331"]) == pytest.approx(96556.40661042774)
    assert bool(best_source_exact["cvar_swap_feasible_v331"]) is False
    assert bool(best_source_exact["one_swap_improves_return_v331"]) is False
    assert bool(best_source_exact["added_observed_v47_proxy_v331"]) is False
    assert bool(best_source_exact["dropped_observed_v47_proxy_v331"]) is False
    assert int(best_source_exact["delta_missing_v47_proxy_rows_v331"]) == 0

    blockers = _read_csv("paper4_v331_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v331"], blockers["blocking_v331"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v331"], blockers["evidence_count_v331"], strict=False)
    )
    assert bool(blocker_map["post_v330_one_swap_improvement_found"]) is False
    assert int(evidence_map["post_v330_one_swap_improvement_found"]) == 0
    assert bool(blocker_map["post_v330_repair_not_working_champion"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 74
    assert bool(blocker_map["global_dynamic_online_gates_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v331_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v331_post_v330_one_swap_reprice_executed"]) is True
    assert bool(claim_map["v331_post_v330_one_swap_local_optimality"]) is True
    assert bool(claim_map["v331_dynamic_gate_ready"]) is True
    assert bool(claim_map["v331_working_champion"]) is False
    assert bool(claim_map["v331_paper1_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v331 post-v330 one-swap repricing gate."])
    assert bool(boundary_map["v331 clears post-v330 one-swap local optimality."])
    assert bool(boundary_map["v331 authorizes a Paper 4 working champion."]) is False
    assert bool(boundary_map["v331 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v331 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v331_rows = backlog.loc[backlog["last_wave"].eq("v331")]
    assert len(v331_rows) == 1
    backlog_row = v331_rows.iloc[0]
    assert backlog_row["status"] == "post_v330_one_swap_local_optimality_cleared"
    assert backlog_row["next_artifact"] == "paper4_v332_dynamic_or_dual_bound_after_v330.csv"
    assert backlog_row["execution_result"] == "post_v330_one_swap_reprice_cleared"

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v331: Post-v330 One-Swap Repricing Gate" in notebook
    assert "CVaR-feasible improving one-swaps: `0`" in notebook
    assert "Best CVaR-feasible return delta: `not applicable" in notebook
    assert "Post-v330 one-swap local optimality cleared:\n  `True`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v332_dynamic_proxy_after_v330_keeps_claims_bounded() -> None:
    status = _read_json("paper4_v332_status.json")

    assert status["phase"] == "v332_dynamic_proxy_or_global_bound_after_v330"
    assert status["schema_version"] == "2026-05-16.332"
    assert status["incumbent_version_v332"] == 295
    assert status["base_comparison_version_v332"] == 316
    assert status["candidate_version_v332"] == 330
    assert status["local_reprice_version_v332"] == 331
    assert status["previous_global_dynamic_gate_version_v332"] == 322
    assert status["full_binary_variables_v332"] == 276869
    assert status["direct_mip_binary_guard_v332"] == 50000
    assert status["direct_full_mip_attempted_v332"] is False
    assert status["valid_full_universe_gap_certificate_v332"] is False
    assert status["post_v330_one_swap_local_optimality_cleared_v332"] is True
    assert status["dynamic_proxy_replay_executed_v332"] is True
    assert status["dynamic_proxy_trace_rows_v332"] == 1152
    assert status["dynamic_proxy_policy_count_v332"] == 3
    assert status["dynamic_proxy_period_count_v332"] == 3
    assert status["period_set_match_v332"] is True
    assert status["period_distribution_match_vs_v295_v332"] is True
    assert status["period_distribution_match_vs_v316_v332"] is False
    assert status["delta_return_v330_vs_v295_v332"] == pytest.approx(1178.1722671579473)
    assert status["delta_cvar90_v330_vs_v295_v332"] == pytest.approx(-674.8167360656371)
    assert status["v330_dynamic_proxy_beats_v295_v332"] is True
    assert status["delta_return_v330_vs_v316_v332"] == pytest.approx(8.372923520345466)
    assert status["delta_cvar90_v330_vs_v316_v332"] == pytest.approx(-432.45789979648544)
    assert status["v330_dynamic_proxy_beats_v316_v332"] is True
    assert status["v295_observed_v47_proxy_rows_v332"] == 95
    assert status["v316_observed_v47_proxy_rows_v332"] == 98
    assert status["v330_observed_v47_proxy_rows_v332"] == 97
    assert status["v295_missing_v47_proxy_rows_v332"] == 76
    assert status["v316_missing_v47_proxy_rows_v332"] == 73
    assert status["v330_missing_v47_proxy_rows_v332"] == 74
    assert status["v330_observed_proxy_delta_vs_v295_v332"] == 2
    assert status["v330_missing_proxy_delta_vs_v295_v332"] == -2
    assert status["v330_observed_proxy_delta_vs_v316_v332"] == -1
    assert status["v330_missing_proxy_delta_vs_v316_v332"] == 1
    assert status["matched_period_dynamic_claim_allowed_v332"] is False
    assert status["cashflow_online_holdout_v330_claim_allowed_v332"] is False
    assert status["contractual_ifrs9_claim_allowed_v332"] is False
    assert status["strict_live_deployability_claim_allowed_v332"] is False
    assert status["working_champion_claim_allowed_v332"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v332"] is False
    assert status["paper1_promotion_allowed_v332"] is False
    assert status["paper4_working_champion_changed_v332"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["dynamic_proxy_summary_rows_v332"] == 3
    assert status["proxy_coverage_rows_v332"] == 3
    assert status["gate_requirement_rows_v332"] == 6
    assert status["claim_blocker_rows_v332"] == 6
    assert status["claim_matrix_rows_v332"] == 8
    assert status["next_artifact_v332"] == "paper4_v333_v330_cashflow_online_ifrs9_gate.csv"

    gate = _read_csv("paper4_v332_dynamic_proxy_or_global_bound_after_v330.csv")
    row = gate.iloc[0]
    assert row["gate_id_v332"] == "v332_dynamic_proxy_or_global_bound_after_v330"
    assert bool(row["post_v330_one_swap_local_optimality_cleared_v332"]) is True
    assert bool(row["dynamic_proxy_replay_executed_v332"]) is True
    assert int(row["dynamic_proxy_trace_rows_v332"]) == 1152
    assert bool(row["period_distribution_match_vs_v295_v332"]) is True
    assert bool(row["period_distribution_match_vs_v316_v332"]) is False
    assert json.loads(row["v295_period_distribution_v332"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert json.loads(row["v316_period_distribution_v332"]) == {
        "2018": 98,
        "2019": 66,
        "2020": 7,
    }
    assert json.loads(row["v330_period_distribution_v332"]) == {
        "2018": 94,
        "2019": 70,
        "2020": 7,
    }
    assert float(row["v330_objective_return_v332"]) == pytest.approx(4428.130915105124)
    assert float(row["v330_cvar90_v332"]) == pytest.approx(96551.59286009285)
    assert bool(row["working_champion_claim_allowed_v332"]) is False
    assert "not a live or global proof" in str(row["claim_boundary_v332"])

    proxy = _read_csv("paper4_v332_dynamic_proxy_summary.csv")
    proxy_map = {str(policy_id): group.iloc[0] for policy_id, group in proxy.groupby("policy_id")}
    assert set(proxy_map) == {
        "v295_broader_pool_after_v296_gate",
        "v316_terminal_repair_after_v317_gate",
        "v330_post_v328_after_v331_gate",
    }
    v330_proxy = proxy_map["v330_post_v328_after_v331_gate"]
    assert int(v330_proxy["trace_rows_v332"]) == 384
    assert float(v330_proxy["final_objective_return_v332"]) == pytest.approx(4428.130915105124)
    assert float(v330_proxy["final_loss_cvar90_v332"]) == pytest.approx(96551.59286009285)
    assert float(v330_proxy["final_loss_mean_v332"]) == pytest.approx(60249.51352836411)

    trace = pd.read_parquet(TABLE_DIR / "paper4_v332_dynamic_proxy_trace.parquet")
    assert len(trace) == 1152
    assert trace["policy_id"].nunique() == 3
    assert trace.groupby("policy_id")["period_v332"].nunique().eq(3).all()
    assert trace.groupby(["policy_id", "period_v332"])["scenario_id_v332"].nunique().eq(128).all()

    coverage = _read_csv("paper4_v332_proxy_coverage_comparison.csv")
    coverage_map = {
        str(policy_id): group.iloc[0] for policy_id, group in coverage.groupby("policy_id")
    }
    assert (
        int(coverage_map["v295_broader_pool_after_v296_gate"]["observed_v47_proxy_rows_v332"]) == 95
    )
    assert (
        int(coverage_map["v316_terminal_repair_after_v317_gate"]["observed_v47_proxy_rows_v332"])
        == 98
    )
    assert int(coverage_map["v330_post_v328_after_v331_gate"]["observed_v47_proxy_rows_v332"]) == 97
    assert int(coverage_map["v330_post_v328_after_v331_gate"]["missing_v47_proxy_rows_v332"]) == 74
    assert (
        bool(coverage_map["v330_post_v328_after_v331_gate"]["contractual_ifrs9_claim_allowed_v332"])
        is False
    )

    requirements = _read_csv("paper4_v332_gate_requirements.csv")
    req_map = dict(zip(requirements["requirement_id_v332"], requirements["met_v332"], strict=False))
    assert bool(req_map["post_v330_one_swap_gate_cleared"]) is True
    assert bool(req_map["dynamic_proxy_replay_executed"]) is True
    assert bool(req_map["common_period_set_available"]) is True
    assert bool(req_map["matched_period_distribution"]) is True
    assert bool(req_map["observed_or_contractual_ifrs9_complete"]) is False
    assert bool(req_map["valid_full_universe_gap_certificate"]) is False

    blockers = _read_csv("paper4_v332_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v332"], blockers["blocking_v332"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v332"], blockers["evidence_count_v332"], strict=False)
    )
    assert bool(blocker_map["valid_global_gap_certificate_missing"]) is True
    assert bool(blocker_map["target_period_distribution_matched_but_live_replay_missing"]) is True
    assert bool(blocker_map["proxy_coverage_gap_persists"]) is True
    assert int(evidence_map["proxy_coverage_gap_persists"]) == 74
    assert bool(blocker_map["v330_cashflow_online_holdout_missing"]) is True
    assert bool(blocker_map["dynamic_proxy_not_live_deployment_replay"]) is True
    assert int(evidence_map["dynamic_proxy_not_live_deployment_replay"]) == 1152
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v332_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v332_dynamic_global_gate_executed"]) is True
    assert bool(claim_map["v332_v330_final_static_proxy_beats_v295"]) is True
    assert bool(claim_map["v332_v330_final_static_proxy_beats_v316"]) is True
    assert bool(claim_map["v332_v330_matches_v295_period_distribution"]) is True
    assert bool(claim_map["v332_matched_period_dynamic_superiority"]) is False
    assert bool(claim_map["v332_contractual_ifrs9_or_full_proxy_coverage"]) is False
    assert bool(claim_map["v332_full_universe_gap_certificate"]) is False
    assert bool(claim_map["v332_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v332 dynamic proxy/global-bound audit after v330."])
    assert bool(boundary_map["v332 final-period static-book proxy favors v330 over v295."])
    assert bool(boundary_map["v332 final-period static-book proxy favors v330 over v316."])
    assert bool(boundary_map["v332 proves matched-period dynamic or live deployability."]) is False
    assert (
        bool(boundary_map["v332 resolves contractual IFRS9 or full cashflow coverage for v330."])
        is False
    )
    assert bool(boundary_map["v332 proves full-universe global integer optimality."]) is False
    assert bool(boundary_map["v332 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v332_rows = backlog.loc[backlog["last_wave"].eq("v332")]
    assert len(v332_rows) == 1
    backlog_row = v332_rows.iloc[0]
    assert backlog_row["status"] == "v330_dynamic_proxy_executed_global_live_proxy_gates_blocked"
    assert backlog_row["next_artifact"] == "paper4_v333_v330_cashflow_online_ifrs9_gate.csv"
    assert backlog_row["execution_result"] == (
        "v330_static_proxy_beats_v316_and_v295_but_proxy_gap_remains"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v332: Dynamic Proxy / Global Bound After v330" in notebook
    assert "Delta return v330 vs v295:\n  `1178.1722671579473`" in notebook
    assert "v330 observed v47 proxy rows:\n  `97`" in notebook
    assert "Working champion claim allowed:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_v333_v330_cashflow_online_ifrs9_gate_stays_proxy_only() -> None:
    status = _read_json("paper4_v333_status.json")

    assert status["phase"] == "v333_v330_cashflow_online_ifrs9_gate"
    assert status["schema_version"] == "2026-05-16.333"
    assert status["source_candidate_version_v333"] == 330
    assert status["gate_audit_version_v333"] == 332
    assert status["selected_rows_v333"] == 171
    assert status["cashflow_proxy_panel_rows_v333"] == 6156
    assert status["observed_proxy_loan_rows_v333"] == 97
    assert status["imputed_proxy_loan_rows_v333"] == 74
    assert status["post_imputation_coverage_share_v333"] == pytest.approx(1.0)
    assert status["observed_coverage_share_v333"] == pytest.approx(0.5672514619883041)
    assert status["cashflow_proxy_months_v333"] == 36
    assert status["cashflow_proxy_scenarios_v333"] == 1
    assert status["cashflow_proxy_source_rows_v333"] == 3
    assert status["observed_proxy_delta_vs_v295_v333"] == 2
    assert status["missing_proxy_delta_vs_v295_v333"] == -2
    assert status["observed_proxy_delta_vs_v316_v333"] == -1
    assert status["missing_proxy_delta_vs_v316_v333"] == 1
    assert status["online_temporal_cell_rows_v333"] == 6
    assert status["online_temporal_summary_rows_v333"] == 2
    assert status["online_internal_all_gate_family_rows_v333"] == 2
    assert status["strict_live_deployability_claim_allowed_v333"] is False
    assert status["contractual_ifrs9_claim_allowed_v333"] is False
    assert status["working_champion_claim_allowed_v333"] is False
    assert status["full_universe_integer_optimality_claim_allowed_v333"] is False
    assert status["paper1_promotion_allowed_v333"] is False
    assert status["paper4_working_champion_changed_v333"] is False
    assert status["paper4_final_promotion_created"] is False
    assert status["claim_blocker_rows_v333"] == 7
    assert status["claim_matrix_rows_v333"] == 7
    assert status["next_artifact_v333"] == (
        "paper4_v334_v330_proxy_gap_repair_or_branch_price_protocol.csv"
    )

    gate = _read_csv("paper4_v333_v330_cashflow_online_ifrs9_gate.csv")
    row = gate.iloc[0]
    assert row["gate_id_v333"] == "v333_v330_cashflow_online_ifrs9_gate"
    assert int(row["candidate_version_v333"]) == 330
    assert int(row["gate_audit_version_v333"]) == 332
    assert int(row["observed_proxy_loan_rows_v333"]) == 97
    assert int(row["imputed_proxy_loan_rows_v333"]) == 74
    assert bool(row["contractual_ifrs9_claim_allowed_v333"]) is False
    assert bool(row["strict_live_deployability_claim_allowed_v333"]) is False
    assert bool(row["working_champion_claim_allowed_v333"]) is False
    assert "not live or contractual" in str(row["claim_boundary_v333"])

    cashflow = _read_csv("paper4_v333_v330_cashflow_proxy_summary.csv")
    cashflow_row = cashflow.iloc[0]
    assert int(cashflow_row["cashflow_proxy_panel_rows_v333"]) == 6156
    assert int(cashflow_row["observed_proxy_loan_rows_v333"]) == 97
    assert int(cashflow_row["imputed_proxy_loan_rows_v333"]) == 74
    assert float(cashflow_row["ecl_proxy_total_v333"]) == pytest.approx(468968.19485551177)
    assert float(cashflow_row["net_cash_proxy_total_v333"]) == pytest.approx(1264842.0933294352)
    assert bool(cashflow_row["contractual_ifrs9_claim_allowed_v333"]) is False

    proxy_sources = _read_csv("paper4_v333_v330_cashflow_proxy_source_summary.csv")
    source_map = {
        str(source): group.iloc[0]
        for source, group in proxy_sources.groupby("proxy_source_v333", dropna=False)
    }
    assert int(source_map["observed_v47_proxy"]["loan_rows_v333"]) == 97
    assert int(source_map["observed_v47_proxy"]["panel_rows_v333"]) == 3492
    assert int(source_map["imputed_grade_score_period"]["loan_rows_v333"]) == 73
    assert int(source_map["imputed_grade_score_period"]["panel_rows_v333"]) == 2628
    assert int(source_map["imputed_grade_score"]["loan_rows_v333"]) == 1
    assert int(source_map["imputed_grade_score"]["panel_rows_v333"]) == 36

    panel = pd.read_parquet(TABLE_DIR / "paper4_v333_v330_cashflow_proxy_panel.parquet")
    assert len(panel) == 6156
    assert panel["loan_id"].astype(str).nunique() == 171
    assert panel["proxy_source_v333"].value_counts().to_dict() == {
        "observed_v47_proxy": 3492,
        "imputed_grade_score_period": 2628,
        "imputed_grade_score": 36,
    }

    online_summary = _read_csv("paper4_v333_v330_online_temporal_summary.csv")
    assert len(online_summary) == 2
    assert online_summary["all_internal_gates_pass_v333"].astype(bool).all()
    assert not online_summary["external_holdout_available_v333"].astype(bool).any()
    assert not online_summary["strict_live_claim_allowed_v333"].astype(bool).any()
    assert online_summary["worst_source_coverage_v333"].min() == pytest.approx(0.9285714285714286)
    online_cells = _read_csv("paper4_v333_v330_online_temporal_cells.csv")
    assert len(online_cells) == 6
    assert online_cells["source_gate80_v333"].astype(bool).all()
    assert online_cells["policy_gate90_v333"].astype(bool).all()
    assert online_cells["width_gate95_v333"].astype(bool).all()

    blockers = _read_csv("paper4_v333_claim_blockers.csv")
    blocker_map = dict(zip(blockers["blocker_id_v333"], blockers["blocking_v333"], strict=False))
    evidence_map = dict(
        zip(blockers["blocker_id_v333"], blockers["evidence_count_v333"], strict=False)
    )
    assert bool(blocker_map["cashflow_proxy_imputation_required"]) is True
    assert int(evidence_map["cashflow_proxy_imputation_required"]) == 74
    assert bool(blocker_map["proxy_coverage_regression_vs_v316"]) is True
    assert int(evidence_map["proxy_coverage_regression_vs_v316"]) == 1
    assert bool(blocker_map["external_online_holdout_missing"]) is True
    assert bool(blocker_map["dynamic_live_replay_missing_after_v332"]) is True
    assert bool(blocker_map["global_branch_price_certificate_missing"]) is True
    assert bool(blocker_map["paper4_working_champion_gate_missing"]) is True
    assert bool(blocker_map["paper4_final_promotion_forbidden"]) is True

    claim_delta = _read_csv("paper4_v333_claim_matrix_delta.csv")
    claim_map = dict(zip(claim_delta["claim_id"], claim_delta["allowed"], strict=False))
    assert bool(claim_map["v333_v330_cashflow_proxy_panel_executed"]) is True
    assert bool(claim_map["v333_v330_online_temporal_rerun_executed"]) is True
    assert bool(claim_map["v333_v330_internal_online_all_gates"]) is True
    assert bool(claim_map["v333_v330_proxy_coverage_regression_vs_v316_documented"]) is True
    assert bool(claim_map["v333_contractual_ifrs9"]) is False
    assert bool(claim_map["v333_strict_live_online_deployability"]) is False
    assert bool(claim_map["v333_working_champion_or_final_promotion"]) is False

    current_boundaries = _read_csv("paper4_current_claim_boundaries.csv")
    boundary_map = dict(
        zip(current_boundaries["claim"], current_boundaries["allowed"], strict=False)
    )
    assert bool(boundary_map["Paper 4 has a v333 v330-specific cashflow proxy panel."])
    assert bool(boundary_map["Paper 4 has a v333 v330 temporal online rerun."])
    assert bool(boundary_map["v333 documents v330 proxy coverage regression versus v316."])
    assert bool(boundary_map["v333 implements contractual IFRS9 for v330."]) is False
    assert bool(boundary_map["v333 authorizes live online deployability for v330."]) is False
    assert (
        bool(boundary_map["v333 resolves v330 global or matched-period dynamic blockers."]) is False
    )
    assert bool(boundary_map["v333 replaces Paper Estrella or finalizes Paper 4."]) is False

    backlog = _read_csv("paper4_living_lab_backlog.csv")
    v333_rows = backlog.loc[backlog["last_wave"].eq("v333")]
    assert len(v333_rows) == 1
    backlog_row = v333_rows.iloc[0]
    assert backlog_row["status"] == "v330_cashflow_proxy_and_temporal_online_rerun_executed"
    assert backlog_row["next_artifact"] == (
        "paper4_v334_v330_proxy_gap_repair_or_branch_price_protocol.csv"
    )
    assert backlog_row["execution_result"] == (
        "cashflow_proxy_complete_with_74_imputed_rows_online_temporal_not_live"
    )

    notebook = (PAPER4_ROOT / "notes" / "paper4_living_lab_notebook.md").read_text(encoding="utf-8")
    assert "Wave v333: v330 Cashflow / Online / IFRS9 Gate" in notebook
    assert "Observed proxy loan rows: `97`" in notebook
    assert "Imputed proxy loan rows: `74`" in notebook
    assert "Strict live deployability claim allowed:\n  `False`" in notebook
    assert set(_registered_paper4_pages()) == CURATED_PAPER4_PAGES
    assert not (STATUS_DIR / "paper4_final_promotion.json").exists()


def test_paper4_quarto_chapter_renders() -> None:
    if shutil.which("quarto") is None:
        pytest.skip("quarto CLI is not installed")

    env = os.environ.copy()
    env["QUARTO_PYTHON"] = str((Path.cwd() / ".venv/bin/python").absolute())
    subprocess.run(
        [
            "quarto",
            "render",
            "chapters/19-paper-mega-extension",
            "--to",
            "html",
            "--execute-daemon-restart",
        ],
        cwd=BOOK_DIR,
        env=env,
        check=True,
        timeout=1200,
    )
