# Paper 2 legacy mechanical outputs — non-citable

Status: `historical_non_citable`

These files were generated by the superseded notebook
`notebooks/11_paper2_ifrs9_e2e.ipynb`. Names such as `stage*`, `ECL`, `PD` and
`LGD` are legacy schema labels for mechanical transformations over a
terminal-outcome archive. They do not identify reporting-date PD, 12-month or
lifetime ECL, SICR, IFRS9 stage, provisions or an accounting policy.

The active Paper 2 contract is
`reports/paper_material/paper2/paper2_claim_contract.yml` with
`claim_status: parked_ifrs9`. Nothing in this directory is paper-facing or an
artifact sink for a current claim.

Any deliberate rerun of the notebook writes only beneath this directory and
must preserve `historical_non_citable` plus `mechanical_proxy_not_ifrs9` in new
tabular outputs.
