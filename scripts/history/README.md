# Historical Scripts

This directory stores archived one-off workflows and transitional utilities that no longer belong to the live pipeline topology.

- Keep them only for provenance, reproducibility of old snapshots, or emergency recovery.
- Prefer the active wrappers under `scripts/core`, `scripts/search`, `scripts/papers`, `scripts/diagnostics`, `scripts/labs`, or the active root scripts still documented in the runbook.
